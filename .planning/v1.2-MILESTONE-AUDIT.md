---
milestone: v1.2
milestone_name: Rozbudowa edukacyjna i realizm
audited: 2026-06-20
auditor: Claude (gsd-verifier, goal-backward milestone audit)
status: tech_debt
score: 19/19 requirements delivered (3 carry honest placeholder/deferred items)
test_baseline: 1010 passed, 1 skipped (grew from 903)
bundle: 834.88 KB / 850 KB (verified via npm run build)
---

# v1.2 Milestone Audit — PM-300 Trener (Rozbudowa edukacyjna i realizm)

**Milestone goal:** Pogłębić warstwę szkoleniową PM-300 — szczegółowe instrukcje obsługi + BHP oparte na realnych materiałach, egzamin hybrydowy (3D + quiz BHP), menu startowe i pełnoekranowy overlay zamiast bocznego panelu.

**Verdict:** All 19 requirements are functionally delivered in code and the test suite is green at 1010. Status is **tech_debt** (not `passed`) for one honest reason: the "realne media" requirement (MED-02 / NAME-01) ships **company-owned placeholder images**, not the sourced real CC photos the milestone intent describes. The pipeline, isolation, wiring, and degradation are all production-correct — only the asset *content* is a placeholder to be swapped at the same URLs. Plus three deferred manual/e2e checks remain unrun.

---

## 1. Per-Requirement Coverage (19 rows)

| # | Req | Status | Evidence (file:line / symbol) |
|---|-----|--------|-------------------------------|
| 1 | MENU-01 | ✅ Delivered | `src/ui/StartMenuOverlay.js:20` `MODES=['free','nauka','egzamin']`; instantiated `src/main.js:406`; `showStartMenu` bootstrap `main.js:143-150` |
| 2 | MENU-02 | ✅ Delivered | `StartMenuOverlay.js:189-202` per-card last-session badge from `localStorage` (`lastSessionPrefix … /100 …, date`); writer subscriber `main.js:255-264` (per-mode) |
| 3 | MENU-03 | ✅ Delivered | `trainingStore.js:266-270` `showMenu/hideMenu` flag **separate from `activeModal`** — sim never pauses for menu; `StartMenuOverlay.js:166-168` toggles `display` only |
| 4 | OVL-01 | ✅ Delivered | `src/ui/ElementInfoPanel.js` **deleted** (file absent); store contract preserved `trainingStore.js:242-249` (`openElementInfo`, `activeModal==='element-info'`, `_elementInfoMeshId`); invariant 15 holds (test §4) |
| 5 | OVL-02 | ✅ Delivered | `ElementInfoOverlay.js:62-77` 3 tabs (budowa / bhp / instrukcja) + media slot; full-screen `dialog.showModal()` `:48` |
| 6 | OVL-03 | ✅ Delivered | ESC via native `cancel` event `ElementInfoOverlay.js:98-102`; backdrop click `:104+`; close btn `:94-95`; `dialog.showModal()` gives native focus-trap; media slot `:77` |
| 7 | EDU-01 | ✅ Delivered | `src/data/elementInfo.js` — exactly **15 keyed entries** (kolo-zamachowe…dzwignia-sprzegla), each with function/instrukcja content; additive |
| 8 | EDU-02 | ✅ Delivered | `elementInfo.js` bhp blocks grouped: oslon/interlock, panel-oburezny, estop, kolo-zamachowe energy, sprzeglo-hamulec, LOTO, inspekcja (grep confirmed `loto`, `interlock`, `oslon` groupings) |
| 9 | EDU-03 | ✅ Delivered | Norm citations present inline: `ISO 16092`, `2006/42/EC`, `OSHA 1910.217`, `IEC 60204` in `elementInfo.js`; Polish content |
| 10 | MED-01 | ✅ Delivered | `vite.config.js:34` `assetsInlineLimit:0`; **zero** media `import` in `src/` (grep clean); build output has no webp/png in JS chunks |
| 11 | MED-02 | ⚠️ Partial (placeholder) | `public/media/ATTRIBUTION.txt` complete, **zero CC-BY-NC**; but all 3 assets are `własność firmy (placeholder — do podmiany)`, not sourced real CC photos. License gate satisfied; asset *content* is placeholder |
| 12 | MED-03 | ✅ Delivered | `ElementInfoOverlay.js:278-280` `img.onerror = () => img.remove()` set **before** `src` — silent 404/offline degrade, no console.error |
| 13 | NAME-01 | ⚠️ Partial (placeholder) | `PressModel.js:512-518` `TextureLoader` → `/media/tabliczka-znamionowa.webp`, `SRGBColorSpace`, mipmaps off, registry dispose; test `PressModel.nameplate.phase14.test.js`. Texture is a **placeholder** image, not real nameplate photo/synthesis |
| 14 | EXAM-01 | ✅ Delivered | `src/data/quizData.js` **36 questions** (24 mc / 12 tf / 6 sequence), 38 `normRef`; `quizSelection.js:16-21` maps scenarioId → questions via `scenarioIds` |
| 15 | EXAM-02 | ✅ Delivered | `trainingStore.js:528-553` finishedAt subscriber: `mode==='egzamin'` → `selectQuizQuestions` → `startQuiz` → `setState({activeModal:'bhp-quiz'})`; QuizController renders `QuizController.js:150-152` |
| 16 | EXAM-03 | ✅ Delivered | `quiz` slice isolated `trainingStore.js:118,282-305`; `QUIZ_PASS_THRESHOLD` (80%) imported `QuizController.js:24,343`; per-question feedback + normRef `QuizController.js:288-291` |
| 17 | EXAM-04 | ✅ Delivered | PDF Sekcja 5 (BHP) `PdfExporter.js:262-278` additive, separate from Sekcja 4 (scoring); JSON `JsonExporter.js:50-61` additive `result.quiz` |
| 18 | TEST-09 | ✅ Delivered | `npm test` → **1010 passed, 1 skipped** (verified); invariant `getInteractables().size===15` in multiple tests (phase9/phase10); mode state machine green |
| 19 | TEST-10 | ✅ Delivered | `npm run build` → main chunk **834.88 KB < 850 KB** (verified); quiz-data 26 KB separate chunk |

**Tally:** 17 fully delivered, 2 partial (MED-02 / NAME-01 — placeholder asset content, production-correct pipeline).

---

## 2. Cross-Phase Integration Trace — Hybrid Exam End-to-End (CORE VALUE)

| Link | Verdict | Evidence |
|------|---------|----------|
| StartMenu mode select → `setMode('egzamin')` | ✅ Connected | `StartMenuOverlay.js:141-142` `setMode(selected)+hideMenu()` |
| 3D SOP done → `session.finishedAt` set | ✅ Connected | `trainingStore.js:558-565` currentStepId→null triggers `finishSession()` → `finishedAt` `:466-471` |
| finishedAt subscriber (egzamin branch) → `startQuiz` + `activeModal='bhp-quiz'` | ✅ Connected | `trainingStore.js:537-550`: guard scenarioId → `selectQuizQuestions` → `startQuiz` → `bhp-quiz` |
| `activeModal='bhp-quiz'` → QuizController renders | ✅ Connected | `QuizController.js:139-152` subscriber on `activeModal` → `_render()` opens dialog |
| `submitAnswer` → `finishQuiz` → `quiz.score` | ✅ Connected | `QuizController.js:295,121`; store `submitAnswer:291-293`, `finishQuiz:297-305` |
| `endExam` → exit egzamin → `closeModal` | ✅ Connected | `QuizController._onFinish:127-131` calls `endExam()+closeModal()` |
| export PDF/JSON with separate procedural + BHP sections | ✅ Connected | `PdfExporter.js:262` Sekcja 5; `JsonExporter.js:50` `result.quiz` — both additive, scoring untouched |

**Integration verdict: PASS.** Every link in the hybrid exam chain exists and connects in actual code. The store-level subscriber (not main.js) owns the auto-trigger, which is architecturally sound (survives HMR with the store). The nauka-mode branch (`exam-prompt` via ExamPromptModal) is preserved unchanged alongside the new egzamin branch.

---

## 3. Isolation Invariant (CRIT-V12-5) — PASS

- `submitAnswer` (`trainingStore.js:291-293`) writes **only** `quiz.{answers,currentIndex}` — never `scoring`. Verified by code read + comment `:288`.
- `finishQuiz` (`:297-305`) writes **only** `quiz.{score,finishedAt}` — never `scoring`. Comment `:299` "NIE dotyka scoring.score".
- `QuizController.js` touches **only** `submitAnswer/finishQuiz/endExam/closeModal` (`:117,121,129,130,295`) — no `scoring.score` write anywhere (grep clean).
- Export separation: PDF Sekcja 4 = `scoring`, Sekcja 5 = `quiz` (`PdfExporter.js:256,266`); JSON `scoring` vs `result.quiz` (`JsonExporter.js:31,56`). Both quiz sections are conditional on `quiz.finishedAt !== null` and additive — procedural payload byte-identical when no quiz.
- Backing tests: `quizSlice.test.js`, `QuizController.test.js`, `boundaries.test.js` all green.

---

## 4. Cross-Cutting Invariants

| Invariant | Verdict | Evidence |
|-----------|---------|----------|
| `getInteractables().size === 15` | ✅ Holds | Tests `PressModel.cables.phase9.test.js:88`, `bolts-welds.phase9.test.js:96`, `phase10.test.js:150` — all green in 1010-test run |
| Bundle < 850 KB | ✅ Holds | `npm run build`: main 834.88 KB (matches STATE 834.98 ±rounding); quiz-data 26 KB separate chunk |
| `ElementInfoPanel.js` deleted (atomic migration) | ✅ Holds | File absent; only stale **comment** references remain in RaycastController/store/pl.js (harmless, no import). ElementInfoOverlay is the live impl |
| ATTRIBUTION.txt complete, zero CC-BY-NC | ✅ Holds | 3 blocks, all `własność firmy`; only NC mention is the policy gate line. No NC-licensed asset |
| No JS asset imports for media | ✅ Holds | grep for `import …(webp\|png\|jpg\|mp4)` in `src/` → none (excluding Vite scaffold svg) |

---

## 5. Test Baseline — VERIFIED

```
Test Files  70 passed (70)
     Tests  1010 passed | 1 skipped (1011)
```

Grew 903 → **1010** as claimed in STATE.md. 1 skipped test is pre-existing/non-blocking. Suite is green.

---

## 6. Deferred / Placeholder Items (honest close)

These do **not** block milestone close but must be tracked — the pipeline is production-ready, the content is not:

1. **Real CC media assets (MED-02)** — `kolo-zamachowe-placeholder.webp`, `hamulec-placeholder.webp` are pure-Python PNG-as-webp placeholders (`własność firmy`). Swap real CC0/CC-BY/CC-BY-SA photos at the **same URLs** + update ATTRIBUTION.txt. STATE Open Q2 (CC YouTube/photo URLs unverified).
2. **Real nameplate texture (NAME-01)** — `tabliczka-znamionowa.webp` is a placeholder raster. Replace with real nameplate photo or Directive-2006/42/EC §1.7.3-compliant synthesis. Texture pipeline (SRGB, POT, dispose) is correct.
3. **Manual e2e browser checks (deferred, non-blocking)** — Phase 15 start-menu visual; Phase 16 media render/404 graceful degrade; Phase 17 Task 3 e2e smoke. Run via `npm run dev`. Logic is unit-tested; only visual/real-browser confirmation pending.
4. **Combined-score weights open question (STATE Q1)** — **Resolved by design**: SOP and BHP scores are kept **separate** (no 60/40 combined weight), consistent with the CRIT-V12-5 isolation invariant. PDF/JSON report two independent sections. No combined metric was implemented; this is intentional, not a gap.

---

## Summary

v1.2 delivers its core intent: a working **hybrid exam** (3D SOP → BHP quiz → separated PDF/JSON export), a **start menu** with three modes and last-session badges, a **full-screen educational overlay** replacing the deleted side panel, **15 elements** with norm-cited BHP content, **36 quiz questions**, and a **media pipeline** that keeps assets out of the JS bundle and degrades gracefully. All 7 integration links connect in real code, the CRIT-V12-5 quiz/scoring isolation holds end-to-end, all cross-cutting invariants pass, the suite is green at 1010, and the bundle is 834.88 KB < 850 KB.

Status is **tech_debt** solely because MED-02 and NAME-01 ship placeholder images instead of the sourced real media the milestone name ("realne media") promises — a content swap, not a code defect — plus three unrun manual/e2e visual checks. The milestone is honestly closeable with these items tracked.

## AUDIT COMPLETE
