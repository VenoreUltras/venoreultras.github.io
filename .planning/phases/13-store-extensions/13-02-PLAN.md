---
phase: 13-store-extensions
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/state/trainingStore.js
  - tests/quizSlice.test.js
  - tests/examPromptFlow.test.js
  - tests/StatusPanel.test.js
autonomous: true
requirements: [EXAM-02, EXAM-03]
nyquist_compliant: true
must_haves:
  truths:
    - "trainingStore exposes an isolated quiz slice (questions, currentIndex, answers, score, finishedAt)"
    - "startQuiz/submitAnswer/finishQuiz mutate only the quiz slice, never scoring.score"
    - "quiz.score is a 0-100 numeric value, not a boolean"
    - "QUIZ_PASS_THRESHOLD === 80 is an importable named export"
    - "In egzamin mode, SOP completion (session.finishedAt null→ts) routes to startQuiz + activeModal='bhp-quiz' instead of endExam()"
    - "In nauka mode, SOP completion still opens the exam-prompt modal (no regression)"
    - "startScenario resets the quiz slice"
  artifacts:
    - path: "src/state/trainingStore.js"
      provides: "quiz slice, startQuiz/submitAnswer/finishQuiz, isCorrect helper, QUIZ_PASS_THRESHOLD export, modified finishedAt subscriber, selectQuizQuestions import"
      contains: "QUIZ_PASS_THRESHOLD"
    - path: "tests/quizSlice.test.js"
      provides: "EXAM-02/EXAM-03 quiz state machine + score isolation + threshold assertions"
      contains: "QUIZ_PASS_THRESHOLD"
    - path: "tests/examPromptFlow.test.js"
      provides: "updated test #3 (egzamin → bhp-quiz); tests #1/#2/#4/#5 unchanged"
      contains: "bhp-quiz"
    - path: "tests/StatusPanel.test.js"
      provides: "updated S4 assertion (egzamin finishedAt → bhp-quiz instead of auto-endExam→free)"
      contains: "bhp-quiz"
  key_links:
    - from: "finishedAt subscriber (egzamin branch)"
      to: "selectQuizQuestions(session.scenarioId) + startQuiz + activeModal='bhp-quiz'"
      via: "store.subscribe on session.finishedAt"
      pattern: "bhp-quiz"
    - from: "submitAnswer()"
      to: "quiz.answers (NOT scoring.score)"
      via: "functional set on s.quiz only"
      pattern: "submitAnswer"
    - from: "finishQuiz()"
      to: "quiz.score (0-100) via isCorrect count"
      via: "Math.round(correct/total*100)"
      pattern: "finishQuiz"
---

<objective>
Add the isolated `quiz` slice and its three actions (`startQuiz`, `submitAnswer`, `finishQuiz`) to `trainingStore.js`, export `QUIZ_PASS_THRESHOLD = 80`, add a private `isCorrect` helper, and modify the existing `finishedAt` subscriber's `egzamin` branch to launch the quiz (`startQuiz` + `activeModal='bhp-quiz'`) instead of calling `endExam()`. Update the one intentionally-affected existing test (`examPromptFlow.test.js` #3).

Purpose: EXAM-02 (post-SOP quiz launch in egzamin mode, hooked into the existing finishedAt subscriber) and EXAM-03 (quiz scoring isolated from procedure scoring; 80% threshold as a testable constant; score 0-100).
Output: Extended `trainingStore.js`, new `tests/quizSlice.test.js`, updated `tests/examPromptFlow.test.js`.

CRITICAL ISOLATION (CRIT-V12-5): `submitAnswer()` must NEVER write to `scoring.score`. `quiz.score` is set only by `finishQuiz()`; `scoring.score` is set only by `applyScoringEvent`. The `nauka` exam-prompt branch and `endExam()` itself are UNCHANGED — only the `egzamin` branch of the subscriber changes. `endExam()` is called later by the Phase 17 QuizController, not here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-store-extensions/13-RESEARCH.md
@.planning/phases/13-store-extensions/13-PATTERNS.md

<interfaces>
<!-- Contracts the executor must use directly. Do NOT explore the codebase. -->

selectQuizQuestions (src/training/quizSelection.js — Phase 12, verified):
  selectQuizQuestions(scenarioId: string) → QuizQuestion[]
  Valid scenarioIds: 'uruchomienie' | 'cykl-pracy' | 'zatrzymanie' | 'awaria'
  Throws a Polish Error on unrecognized scenarioId. Returns a fresh array, no side effects.

QuizQuestion shape (src/data/quizData.js — verified):
  { id, type: 'mc'|'tf'|'sequence', scenarioIds: string[], category, question,
    normRef, explanation,
    options?: string[], correctIdx?: number,    // mc / tf
    steps?: string[], correctOrder?: number[] }  // sequence

Existing scoring slice nesting analog (trainingStore.js line 40):
  scoring: { score: 100, criticalCount: 0, mediumCount: 0, minorCount: 0 }

Existing functional-update analog (finishSession, lines 406-423):
  set(s => { ... return { session: { ...s.session, finishedAt: t } }; })

Existing private scoring helper analog (applyScoringEvent, lines 565-574) —
  module-level function, NOT exported. Mirror this placement for isCorrect.

The finishedAt subscriber to modify (lines 468-481): the nauka branch
  (activeModal='exam-prompt' + _examPromptShown=true) is UNCHANGED. Only the
  `if (cur.mode === 'egzamin') { cur.endExam(); }` line changes.

Existing import line precedent (lines 11-13): trainingStore already imports from
  '../training/' (ProcedureEngine, faultRules). boundaries.test.js only blocks
  ['three','gsap'] for this file — importing quizSelection.js is boundary-clean.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing tests for quiz slice + state machine (EXAM-02/EXAM-03)</name>
  <files>tests/quizSlice.test.js</files>
  <read_first>
    - src/state/trainingStore.js (lines 28-133 initial state + startScenario; lines 468-481 finishedAt subscriber)
    - tests/examPromptFlow.test.js (analog: subscriber/state-machine tests, startScenario fixture, setState trigger pattern)
    - tests/modeStateMachine.test.js (analog: pure-store state machine assertions, `@vitest-environment node`)
    - .planning/phases/13-store-extensions/13-PATTERNS.md (section "tests/quizSlice.test.js (new file)" — full test list and trigger patterns)
    - src/data/quizData.js (QuizQuestion shape for building mock question arrays)
  </read_first>
  <behavior>
    - quiz slice exists in initial state with shape {questions:[], currentIndex:0, answers:[], score:0, finishedAt:null}
    - startQuiz(questions) populates quiz.questions and resets currentIndex/answers/score/finishedAt
    - submitAnswer(answer) appends to quiz.answers and increments quiz.currentIndex
    - submitAnswer() does NOT change scoring.score (stays 100) — CRIT-V12-5 isolation
    - finishQuiz() with 100% correct answers → quiz.score === 100
    - finishQuiz() with 0% correct answers → quiz.score === 0
    - finishQuiz() with mixed answers → quiz.score === Math.round(correct/total*100)
    - finishQuiz() sets quiz.finishedAt to a non-null timestamp
    - QUIZ_PASS_THRESHOLD === 80 (imported named export)
    - mode=egzamin + finishedAt null→ts → activeModal==='bhp-quiz' AND quiz.questions.length > 0 (EXAM-02 state machine)
    - startScenario() resets the quiz slice (Pitfall 3)
  </behavior>
  <action>
    Create tests/quizSlice.test.js with header `// @vitest-environment node` and a Polish comment block referencing EXAM-02/EXAM-03. Import `{ createTrainingStore, QUIZ_PASS_THRESHOLD }` from `../src/state/trainingStore.js` and the `uruchomienie` scenario from `../src/training/scenarios/uruchomienie.js`. For the EXAM-02 subscriber test, bootstrap with `startScenario(uruchomienie)` (sets session.scenarioId), then trigger via `store.setState({ session: { ...store.getState().session, finishedAt: Date.now() } })` after `setMode('egzamin')`. For startQuiz/submitAnswer/finishQuiz unit tests, build small mock QuizQuestion arrays (mc with correctIdx, sequence with correctOrder) and call actions directly — no scenario needed. Assert each behavior in the behavior block. These tests MUST fail initially (RED): quiz slice, actions, and QUIZ_PASS_THRESHOLD do not exist yet.
  </action>
  <verify>
    <automated>npm test -- tests/quizSlice.test.js 2>&1 | grep -qiE "(QUIZ_PASS_THRESHOLD|startQuiz|quiz|fail|FAIL|undefined)" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - tests/quizSlice.test.js exists with `@vitest-environment node` header
    - File imports QUIZ_PASS_THRESHOLD and asserts it equals 80
    - File contains the EXAM-02 state-machine test asserting activeModal==='bhp-quiz' and quiz.questions.length > 0
    - File contains a submitAnswer test asserting scoring.score stays 100 (CRIT-V12-5)
    - File contains finishQuiz tests for 100% / 0% / mixed score
    - Running `npm test -- tests/quizSlice.test.js` fails (RED) before Task 2
  </acceptance_criteria>
  <done>Eleven failing tests exist covering the quiz slice, three actions, score isolation, threshold export, and the egzamin state machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement quiz slice, actions, threshold export, isCorrect helper, and modified subscriber (GREEN)</name>
  <files>src/state/trainingStore.js</files>
  <read_first>
    - src/state/trainingStore.js (lines 9-13 imports; line 40 scoring nesting; lines 105-133 startScenario; lines 406-423 functional-update; lines 462-481 subscriber; lines 565-574 applyScoringEvent helper placement)
    - .planning/phases/13-store-extensions/13-PATTERNS.md (sections "quiz slice initial state", "startScenario quiz reset", "quiz actions", "QUIZ_PASS_THRESHOLD export", "finishedAt subscriber modification")
    - tests/quizSlice.test.js (the contract from Task 1)
  </read_first>
  <action>
    1) Add import `import { selectQuizQuestions } from '../training/quizSelection.js';` after the existing '../data/lectorVoices.js' import.
    2) Add named export `export const QUIZ_PASS_THRESHOLD = 80;` (with Polish comment: testable constant, Success Criterion 4) immediately before the `export function createTrainingStore` line.
    3) Add the `quiz` slice to initial state after `showStartMenu: false` (added in 13-01), before startScenario: `quiz: { questions: [], currentIndex: 0, answers: [], score: 0, finishedAt: null }`, with a Polish comment noting EXAM-02/03 isolation from scoring.score (CRIT-V12-5), score is 0-100 numeric, and quiz.finishedAt ≠ session.finishedAt.
    4) Add quiz reset to startScenario's set call, immediately after `_examPromptShown: false` (Pitfall 3): the same fresh quiz object.
    5) Add three actions near the other actions: `startQuiz(questions)` (simple set replacing the whole quiz object, currentIndex/answers/score/finishedAt reset; NEVER touches scoring); `submitAnswer(answer)` (functional set appending to s.quiz.answers and incrementing s.quiz.currentIndex, spread-preserving questions/score/finishedAt; NEVER touches s.scoring — CRIT-V12-5); `finishQuiz()` (functional set computing correct count via isCorrect, score = questions.length > 0 ? Math.round(correct/total*100) : 0, sets quiz.finishedAt = Date.now(); does NOT call endExam — that is Phase 17).
    6) Add private module-level helper `isCorrect(question, answer)` (NOT exported) near applyScoringEvent: mc/tf compare `answer === question.correctIdx`; sequence compare `JSON.stringify(answer) === JSON.stringify(question.correctOrder)`; default return false (graceful for type-mismatched answers — security T-13-03).
    7) Modify the finishedAt subscriber egzamin branch ONLY: replace `cur.endExam();` with a guard `if (!cur.session.scenarioId) return;` (Pitfall 2), then `const questions = selectQuizQuestions(cur.session.scenarioId); cur.startQuiz(questions); store.setState({ activeModal: 'bhp-quiz' });`. Update the surrounding Polish comment to note EXAM-02 (endExam now deferred to Phase 17 QuizController). The nauka branch and endExam() action are UNCHANGED. Preserve all existing Polish comments and fields.
  </action>
  <verify>
    <automated>npm test -- tests/quizSlice.test.js tests/examPromptFlow.test.js tests/boundaries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const QUIZ_PASS_THRESHOLD = 80" src/state/trainingStore.js` returns 1
    - `grep -c "selectQuizQuestions" src/state/trainingStore.js` returns ≥2 (import + subscriber call)
    - `grep -c "bhp-quiz" src/state/trainingStore.js` returns 1
    - submitAnswer body contains no `scoring` token (isolation — CRIT-V12-5)
    - `grep -c "quiz: { questions: \[\], currentIndex: 0, answers: \[\], score: 0, finishedAt: null }" src/state/trainingStore.js` returns ≥2 (initial state + startScenario reset)
    - `npm test -- tests/quizSlice.test.js` exits 0 (all GREEN)
    - `npm test -- tests/boundaries.test.js` exits 0 (no boundary violation from quizSelection import)
  </acceptance_criteria>
  <done>Quiz slice, three isolated actions, QUIZ_PASS_THRESHOLD export, isCorrect helper, and the modified egzamin subscriber all exist; quizSlice tests green; boundaries green.</done>
</task>

<task type="auto">
  <name>Task 3: Update examPromptFlow #3 + StatusPanel S4 + full-suite regression gate</name>
  <files>tests/examPromptFlow.test.js, tests/StatusPanel.test.js</files>
  <read_first>
    - tests/examPromptFlow.test.js (test #3 currently at lines 45-53; tests #1/#2/#4/#5 must remain unchanged)
    - tests/StatusPanel.test.js (S4 at lines 173-188 — asserts `mode === 'free'` at line 187 relying on the OLD auto-endExam subscriber; jsdom environment)
    - .planning/phases/13-store-extensions/13-PATTERNS.md (section "tests/examPromptFlow.test.js — update test #3 only")
    - src/state/trainingStore.js (the egzamin subscriber behavior from Task 2)
  </read_first>
  <action>
    Two intentional in-place test updates driven by the egzamin subscriber change (NOT deletions — test count must stay ≥929):

    (a) tests/examPromptFlow.test.js — update ONLY test #3. Keep its setup (setMode('egzamin'), then setState finishedAt). Replace the old assertions (`mode === 'free'`) with the Phase 13 behavior: `activeModal === 'bhp-quiz'`, `quiz.questions.length > 0`, `mode === 'egzamin'` (mode stays egzamin — endExam is Phase 17), and `activeModal !== 'exam-prompt'`. Update the test title to reference Phase 13 EXAM-02. Do NOT modify tests #1, #2, #4, #5.

    (b) tests/StatusPanel.test.js — update ONLY S4 (lines 173-188). At line 187 the assertion `expect(store.getState().mode).toBe('free')` and its comment `// auto-endExam już zadziałał` rely on the OLD subscriber that auto-called endExam() in egzamin mode. After Task 2, the egzamin finishedAt subscriber routes to bhp-quiz instead. Replace the assertion with `expect(store.getState().mode).toBe('egzamin')` and `expect(store.getState().activeModal).toBe('bhp-quiz')`. Update the comment block (lines 181-185) to note that auto-return (endExam) now happens in Phase 17 QuizController, not on finishedAt. Keep the rest of S4 (the 3-state toggle cycle free→nauka→egzamin) and tests S1/S2/S3/S5/S6+ byte-for-byte unchanged.

    Then run the full suite to confirm exactly two intentional behavior updates (examPromptFlow #3, StatusPanel S4) and zero unintended regressions.
  </action>
  <verify>
    <automated>npm test -- tests/examPromptFlow.test.js tests/StatusPanel.test.js; npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bhp-quiz" tests/examPromptFlow.test.js` returns ≥1
    - examPromptFlow #3 asserts `mode === 'egzamin'` (NOT 'free') after finishedAt in egzamin mode
    - examPromptFlow tests #1, #2, #4, #5 are byte-for-byte unchanged
    - `grep -c "bhp-quiz" tests/StatusPanel.test.js` returns ≥1
    - StatusPanel S4 asserts `mode === 'egzamin'` and `activeModal === 'bhp-quiz'` (NOT `mode === 'free'`)
    - StatusPanel tests S1/S2/S3/S5/S6 are byte-for-byte unchanged
    - `npm test -- tests/examPromptFlow.test.js tests/StatusPanel.test.js` exits 0
    - `npm test` reports ≥929 tests passing, 0 failing (two changed tests are updated, not deleted)
  </acceptance_criteria>
  <done>examPromptFlow #3 and StatusPanel S4 both reflect the new egzamin→bhp-quiz flow; full suite green with no unintended regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finishedAt subscriber → selectQuizQuestions | session.scenarioId flows into the question selector; must be a recognized id or null-guarded |
| QuizController (Phase 17) / tests → submitAnswer | answer value (number \| number[]) crosses into the quiz slice; isCorrect must degrade gracefully on type mismatch |
| quiz slice ↔ scoring slice | hard isolation boundary — quiz scoring must never write procedure scoring (CRIT-V12-5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-S1 | Tampering | submitAnswer() writing scoring.score (score-integrity) | mitigate | Task 2 acceptance forbids `scoring` token in submitAnswer body; Task 1 test asserts scoring.score stays 100 after submitAnswer (CRIT-V12-5) |
| T-13-02 | Tampering | Invalid/null session.scenarioId passed to selectQuizQuestions | mitigate | Subscriber null-guard `if (!cur.session.scenarioId) return;` (Pitfall 2); selectQuizQuestions allowlist-validates and throws on unknown ids (Phase 12) |
| T-13-03 | Tampering | submitAnswer called with type-mismatched answer (object instead of number) | mitigate | isCorrect() returns false for unrecognized question types / answer shapes — graceful degradation, no throw |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none); no install task exists |
</threat_model>

<verification>
- `npm test -- tests/quizSlice.test.js` — all quiz-slice tests green (EXAM-02/EXAM-03)
- `npm test -- tests/examPromptFlow.test.js` — 5 green (test #3 updated, nauka path unchanged)
- `npm test -- tests/StatusPanel.test.js` — green (S4 updated for bhp-quiz, rest unchanged)
- `npm test -- tests/boundaries.test.js` — green (quizSelection import boundary-clean)
- `npm test` — ≥929 tests, 0 failing (two intentional updates: examPromptFlow #3 + StatusPanel S4, no deletion)
- submitAnswer() never writes scoring.score; quiz.score is 0-100 numeric; QUIZ_PASS_THRESHOLD === 80 importable
</verification>

<success_criteria>
- Isolated quiz slice with startQuiz/submitAnswer/finishQuiz; submitAnswer never touches scoring.score (EXAM-03 / CRIT-V12-5)
- quiz.score is 0-100 numeric; QUIZ_PASS_THRESHOLD === 80 exported constant (EXAM-03)
- egzamin SOP completion → startQuiz + activeModal='bhp-quiz' (EXAM-02); nauka path unchanged
- Full suite green (≥929), two intentional test updates (examPromptFlow #3 + StatusPanel S4)
</success_criteria>

<output>
Create `.planning/phases/13-store-extensions/13-02-SUMMARY.md` when done.
</output>
