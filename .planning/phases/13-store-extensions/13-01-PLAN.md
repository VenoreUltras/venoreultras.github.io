---
phase: 13-store-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/trainingStore.js
  - tests/showStartMenu.test.js
autonomous: true
requirements: [MENU-01, MENU-03]
nyquist_compliant: true
must_haves:
  truths:
    - "trainingStore exposes showStartMenu boolean defaulting to false"
    - "showMenu() sets showStartMenu=true without touching activeModal"
    - "hideMenu() sets showStartMenu=false"
    - "showStartMenu is orthogonal to activeModal so the simulation never pauses behind the start menu"
  artifacts:
    - path: "src/state/trainingStore.js"
      provides: "showStartMenu flag + showMenu()/hideMenu() actions"
      contains: "showStartMenu"
    - path: "tests/showStartMenu.test.js"
      provides: "MENU-01/MENU-03 flag + orthogonality assertions"
      contains: "showStartMenu"
  key_links:
    - from: "showMenu()/hideMenu()"
      to: "showStartMenu boolean"
      via: "set({ showStartMenu })"
      pattern: "showStartMenu"
    - from: "showMenu()"
      to: "activeModal (must remain null)"
      via: "no write to activeModal"
      pattern: "activeModal"
---

<objective>
Add the `showStartMenu` flag and its `showMenu()` / `hideMenu()` actions to `trainingStore.js`. The flag is a flat boolean, completely separate from `activeModal`, so the start menu can be shown/hidden without pausing the 3D simulation (the GSAP ticker pause predicate reads `activeModal !== null` only).

Purpose: Store-level prerequisite (MENU-01) for the Phase 15 StartMenuOverlay, and the orthogonality guarantee (MENU-03) that the menu does not pause the simulator.
Output: Extended `trainingStore.js` + new `tests/showStartMenu.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-store-extensions/13-RESEARCH.md
@.planning/phases/13-store-extensions/13-PATTERNS.md

<interfaces>
<!-- Existing trainingStore patterns the executor must follow. Do NOT explore the codebase — these are the contracts. -->

Existing flat boolean-flag analog (src/state/trainingStore.js line 51):
  hcOutlineMode: false   // user-preference boolean, NOT reset by startScenario

Existing simple-setter action analog (lines 231-235 openElementInfo, 222 closeModal):
  showMenu/hideMenu use the minimal set({...}) form — no toggle logic, no payload.

GSAP ticker pause predicate (documented at lines 64-66): simulation pauses when
  activeModal !== null. showStartMenu MUST NOT be a value of activeModal.

Test analog (tests/modeStateMachine.test.js) — flat-flag tests with
  `// @vitest-environment node`, no scenario fixture needed for basic flag tests:
    import { describe, it, expect } from 'vitest';
    import { createTrainingStore } from '../src/state/trainingStore.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing tests for showStartMenu flag (MENU-01/MENU-03)</name>
  <files>tests/showStartMenu.test.js</files>
  <read_first>
    - src/state/trainingStore.js (the file being extended — lines 28-103 initial state, 217-235 action patterns)
    - tests/modeStateMachine.test.js (analog: flat boolean flag tests, `@vitest-environment node`, minimal store setup)
    - .planning/phases/13-store-extensions/13-PATTERNS.md (section "tests/showStartMenu.test.js (new file)")
  </read_first>
  <behavior>
    - Test 1: initial state has `showStartMenu === false`
    - Test 2: `showMenu()` sets `showStartMenu === true`
    - Test 3: `hideMenu()` sets `showStartMenu === false`
    - Test 4 (orthogonality, Pitfall 4): after `showMenu()`, `activeModal === null` (unchanged)
    - Test 5: `showMenu()` then `hideMenu()` returns `showStartMenu` to `false`
  </behavior>
  <action>
    Create tests/showStartMenu.test.js with header `// @vitest-environment node` and a Polish comment block referencing MENU-01/MENU-03 per the PATTERNS analog. Import `createTrainingStore` from `../src/state/trainingStore.js`. Write the five assertions in the behavior block. Use a fresh `createTrainingStore()` per test (no scenario fixture needed). Test 4 asserts `store.getState().activeModal` is `null` after `showMenu()` — this is the MENU-03 orthogonality guarantee. These tests MUST fail initially (RED) because `showStartMenu`/`showMenu`/`hideMenu` do not exist yet.
  </action>
  <verify>
    <automated>npm test -- tests/showStartMenu.test.js 2>&1 | grep -qE "(showMenu is not a function|showStartMenu|fail|FAIL)" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - tests/showStartMenu.test.js exists with `@vitest-environment node` header
    - File contains 5 `it(...)` assertions matching the behavior block
    - Running `npm test -- tests/showStartMenu.test.js` fails (RED) before Task 2
    - Test 4 asserts `activeModal === null` after `showMenu()`
  </acceptance_criteria>
  <done>Five failing tests exist covering MENU-01/MENU-03 flag behavior and activeModal orthogonality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add showStartMenu flag + showMenu()/hideMenu() actions (GREEN)</name>
  <files>src/state/trainingStore.js</files>
  <read_first>
    - src/state/trainingStore.js (lines 28-103 initial state; lines 217-235 setter action patterns)
    - .planning/phases/13-store-extensions/13-PATTERNS.md (section "showStartMenu flag + showMenu/hideMenu actions")
    - tests/showStartMenu.test.js (the contract from Task 1)
  </read_first>
  <action>
    In the initial state object, add `showStartMenu: false` immediately after `_spinUpTimerHandle: null` (last existing field), with a Polish comment noting MENU-03: flag separate from `activeModal`, simulation does not pause behind menu, flag NOT reset by startScenario (user-navigation state, mirrors the hcOutlineMode user-preference pattern). Add two minimal setter actions `showMenu: () => set({ showStartMenu: true })` and `hideMenu: () => set({ showStartMenu: false })` near the other UI actions. Neither action may write to `activeModal`. Do NOT use `activeModal: 'start-menu'` anywhere (Pitfall 4). Preserve all existing Polish comments and fields.
  </action>
  <verify>
    <automated>npm test -- tests/showStartMenu.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/state/trainingStore.js | grep -c "showStartMenu: false"` returns 1
    - `grep -c "showMenu:" src/state/trainingStore.js` returns 1 and `grep -c "hideMenu:" src/state/trainingStore.js` returns 1
    - Neither `showMenu` nor `hideMenu` sets `activeModal` (no `activeModal` token in their bodies)
    - `npm test -- tests/showStartMenu.test.js` exits 0 (all 5 GREEN)
    - `npm test -- tests/boundaries.test.js` exits 0 (no boundary violation)
  </acceptance_criteria>
  <done>showStartMenu flag and showMenu()/hideMenu() actions exist; all showStartMenu tests pass; boundaries green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI consumer → store action | Phase 15 StartMenuOverlay will call showMenu()/hideMenu(); same-origin trusted JS, no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | showMenu() accidentally writing activeModal | mitigate | Task 2 acceptance forbids activeModal token in showMenu/hideMenu bodies; Task 1 Test 4 asserts activeModal stays null after showMenu() |
| T-13-02 | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none); no install task exists |
</threat_model>

<verification>
- `npm test -- tests/showStartMenu.test.js` — 5 tests green
- `npm test -- tests/boundaries.test.js` — green (trainingStore import boundary intact)
- showStartMenu is a flat boolean, never a value of activeModal
</verification>

<success_criteria>
- showStartMenu boolean exists, defaults false (MENU-01 store prerequisite)
- showMenu()/hideMenu() toggle the flag without touching activeModal (MENU-03 orthogonality)
- New test file green; no existing test regressed
</success_criteria>

<output>
Create `.planning/phases/13-store-extensions/13-01-SUMMARY.md` when done.
</output>
