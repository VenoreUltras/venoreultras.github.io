---
phase: 11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs
plan: 06
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05"]
files_modified:
  - tests/phase11.integration.test.js
  - tests/boundaries.test.js
  - vite.config.js
autonomous: false
requirements: [FUNC-11-13]

must_haves:
  truths:
    - "getInteractables().size === 15 zachowane (15 baseline Phase 7-03)"
    - "PressModel.js i PhysicsEngine.js NIE importują DOM/state/training (D-Phase7-05 boundary preserved)"
    - "Cały suite testów Phase 9 baseline (777) + nowe Phase 11 testy = wszystkie zielone"
    - "npm run build → main bundle < 850 KB (limit z v1.1 zachowany)"
    - "Manualny smoke: flow free → nauka → SOP done → ExamPrompt → egzamin → endExam → free; wszystkie modale działają; 🔊 lektor faktycznie odtwarza audio (z prawdziwym kluczem); CORS resolved (direct fetch lub proxy uncommented)"
  artifacts:
    - path: "tests/phase11.integration.test.js"
      provides: "Aggregate audit: 15 interactables, boundary, 4 new modules registered, mode flow integration"
  key_links:
    - from: "Manual smoke checkpoint"
      to: "User approval"
      via: "blocking gate (autonomous: false)"
      pattern: "checkpoint:human-verify"
---

<objective>
Closing audit Phase 11: aggregate test sprawdza wszystkie inwarianty (15 interactables, boundary, bundle budget); manual smoke gate weryfikuje flow trybów + lektor + CORS resolution. Plan jest non-autonomous (autonomous: false) — kończy się checkpoint:human-verify który blokuje aż użytkownik potwierdzi.

Wave 4 — depends_on wszystkich pozostałych planów. Wymaga ich ukończenia (5 wave 1-3 plans green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md
@.planning/ROADMAP.md sekcja Phase 11 Success Criteria
@CLAUDE.md
@src/PressModel.js
@src/PhysicsEngine.js
@tests/boundaries.test.js

<interfaces>
Phase 11 nowe moduły (po wave 1-3):
- src/data/elementInfo.js (Plan 11-03)
- src/data/lectorVoices.js (Plan 11-05)
- src/lector/LectorService.js (Plan 11-05)
- src/ui/ElementInfoPanel.js (Plan 11-03)
- src/ui/ExamPromptModal.js (Plan 11-04)

Phase 11 nowe localStorage keys:
- 'pm300:mode:v1' (Plan 11-01)
- 'pm300:lector:enabled' (Plan 11-05)
- 'pm300:lector:voice' (Plan 11-05)

Bundle budget v1.1: < 850 KB (current 780.21 KB baseline z Phase 9). Phase 11 dodaje ~5-10 KB code (LectorService ~3 KB + ElementInfoPanel ~3 KB + ExamPromptModal ~2 KB + elementInfo data ~5 KB) — comfortable margin ~50 KB headroom oczekiwany.

Phase 9 baseline = 777 testów. Phase 11 nowe testy (z poprzednich planów):
- Plan 11-01: 7 modeStateMachine + 2 trainingStore + 4 application + 5 StatusPanel = 18
- Plan 11-02: 6 statusIndicator + 4 application = 10
- Plan 11-03: 6 elementInfo + 8 ElementInfoPanel + 3 RaycastController + 2 trainingStore = 19
- Plan 11-04: 5 examPromptFlow + 6 ExamPromptModal = 11
- Plan 11-05: 8 lectorService + 2 store + 2 ElementInfoPanel + 2 StatusPanel = 14
- Plan 11-06: ~8 integration audit testów
- Total Phase 11: ~80 nowych testów → ~857+ total
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate integration audit test</name>
  <read_first>
    - tests/PressModel.phase9.integration.test.js (wzorzec aggregate audit z 13 testów Phase 9)
    - tests/boundaries.test.js (lista FORBIDDEN_PAIRS — sprawdź czy wszystkie 5 nowych Phase 11 moduły mają entries)
    - src/PressModel.js linia 1503 (getInteractables)
  </read_first>
  <files>tests/phase11.integration.test.js, tests/boundaries.test.js</files>
  <action>
    Utwórz `tests/phase11.integration.test.js` z describe 'Phase 11 — integration audit' zawierającym ~8 testów:
    1. PressModel.getInteractables().size === 15 (no regression Phase 7-03)
    2. PressModel.js NIE importuje z '../state/' lub '../training/' (regex source check; analog boundary patterns)
    3. PhysicsEngine.js NIE importuje 'three' lub '../state/' lub '../training/' (regex)
    4. Wszystkie 15 interactable IDs są pokryte w src/data/elementInfo.js (Object.keys(elementInfo) ⊇ getInteractables().keys())
    5. boundaries.test.js zawiera entries dla wszystkich 5 nowych Phase 11 modułów (regex check tests/boundaries.test.js source dla 'src/data/elementInfo.js', 'src/data/lectorVoices.js', 'src/lector/LectorService.js', 'src/ui/ElementInfoPanel.js', 'src/ui/ExamPromptModal.js')
    6. trainingStore initial state ma pole `mode === 'free'` (cold-start invariant FUNC-11-01)
    7. Wszystkie 13 FUNC-11 requirements ID są pokryte przez at-least-one PLAN frontmatter — meta test czytający .planning/phases/11-*/11-0?-PLAN.md i sprawdzający że suma `requirements` field zawiera FUNC-11-01 do FUNC-11-13.
    8. Bundle size sanity (mocked / skip with note): test commenced jako test.skip dla manual `npm run build && stat dist/...` — komentarz "Verify manually in Task 2 smoke gate".
    Zmodyfikuj `tests/boundaries.test.js` — final sweep, dodaj brakujące entries jeśli któryś z planów 11-03/04/05 ich nie dorzucił. (Defensywne — powinno być no-op jeśli wave 1-3 wszystko poprawnie dodały.)
  </action>
  <verify>
    <automated>npx vitest run tests/phase11.integration.test.js tests/boundaries.test.js --reporter=dot && npm test -- --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase11.integration.test.js: 7/7 PASS (1 skipped dla bundle — sprawdzony manualnie)
    - tests/boundaries.test.js: PASS
    - Pełen suite: 777 baseline + Phase 11 nowe = ZIELONE (~857+)
    - npm run build → main bundle visible w stdout < 850 KB (manualnie sprawdź; jeśli nie, FAILURE — przerwij i flag)
  </acceptance_criteria>
  <done>Aggregate audit pokrywa wszystkie inwarianty, suite zielony, bundle pod limit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke gate — flow trybów + lektor + CORS</name>
  <what-built>
    Phase 11 zaimplementowana:
    - Mode state machine (free / nauka / egzamin) z alias do legacy difficulty/freeRoam
    - Cold start mode='free'
    - Status indicator #status-text reaguje na (isRunning, _omega) z 3 stanami
    - ElementInfoPanel renderuje 4 sekcje w nauce / 1 sekcję w free
    - ExamPromptModal po SOP done w nauce + auto endExam po egzaminie
    - LectorService (ElevenLabs TTS) z cache LRU + 🔊 button w panel + toggle/voice picker w StatusPanel
    - Persist 3 nowych localStorage keys ('pm300:mode:v1', 'pm300:lector:enabled', 'pm300:lector:voice')
    - .env.example dorzucony z VITE_ELEVENLABS_API_KEY
    - Vite proxy fallback skomentowany w vite.config.js dla CORS contingency
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    - `.env` w root projektu z prawdziwym `VITE_ELEVENLABS_API_KEY=...` (free tier wystarczy)
    - Speakers/headphones podłączone
    - Restart `npm run dev` po dorzuceniu .env (Vite inline env przy starcie)

    **Flow trybów (FUNC-11-01..06):**
    1. Otwórz http://localhost:5173 w świeżym browser context (clear localStorage). Oczekiwane: scene renderuje, brak modalu blokującego, status indicator w panelu sterowania == 'Nieaktywny' (lub 'Aktywny' jeśli auto-start), tryb badge == 'Swobodny'.
    2. Hover na koło zamachowe — pokazuje się tooltip (TooltipManager, Phase 5 zachowane).
    3. Klik na koło zamachowe w mode='free' — pojawia się modal z 1-zdaniowym opisem (pl.parts.kolo-zamachowe.description). Zamknij X.
    4. Klik na toggler trybu (z 'Tryb: Nauka' = przeciwny stan) → mode 'nauka', badge 'Nauka'.
    5. Klik na koło zamachowe w mode='nauka' — pojawia się ElementInfoPanel z 4 sekcjami (Funkcja, Parametry, SOP, BHP). Sprawdź widoczność 🔊 button (jeśli lektor enabled — patrz krok 8+).
    6. Wykonaj wszystkie kroki SOP scenariusza uruchomienia (klikaj zgodnie z StepPanel). Po ostatnim — ExamPromptModal "Przejść do egzaminu?". Klik "Nie" → modal zamknięty, mode='free', badge='Swobodny'.
    7. Reset (restart scenariusza), powtórz ale klik "Tak" → mode='egzamin', scenariusz restartuje. Wykonaj egzamin do końca (lub abort przez reset). Po finished → auto endExam, mode='free', toggler unlocked.
    
    **Status indicator (FUNC-11-04):**
    8. Klik Start w panel sterowania (btn-toggle), RPM=30. Po ~1s sprawdź status-text='Aktywny', status-dot ma klasę 'running'.
    9. Klik Stop → status-text='Nieaktywny', dot 'stopped'.
    10. Start + zsuń slider do RPM=10 (min). Po stabilizacji _omega sprawdź czy dla bardzo niskich RPM przechodzi przez 'Bezczynny (idle)' (threshold 0.01 rad/s ≈ 0.1 RPM — może nie być osiągalne przez slider, OK jeśli pozostaje 'Aktywny').

    **Lektor (FUNC-11-09..12):**
    11. Bez klucza w .env: 🔊 button w ElementInfoPanel disabled, tooltip "Lektor wymaga konfiguracji klucza ElevenLabs". W StatusPanel toggle lektora disabled.
    12. Dorzuć klucz do .env, restart npm run dev. Włącz toggle lektora w StatusPanel.
    13. W mode='nauka' otwórz ElementInfoPanel dowolnego elementu → klik 🔊. Oczekiwane: audio gra (~1-3s opóźnienia loading).
    14. CORS check: jeśli DevTools Network pokazuje failed preflight OPTIONS / CORS error:
        - Uncomment proxy block w vite.config.js
        - Zmień URL w LectorService z `https://api.elevenlabs.io/v1/text-to-speech/...` na `/elevenlabs/v1/text-to-speech/...` (relative path)
        - Restart npm run dev
        - Powtórz krok 13
        Udokumentuj w SUMMARY co działało (direct fetch vs proxy).
    15. Cache test: klik 🔊 drugi raz na tym samym elemencie — audio powinno zacząć grać natychmiast (cache hit, brak fetch w DevTools Network).
    16. Voice picker: zmień głos w StatusPanel select, klik 🔊 → inny głos (po refresh — bo cache key zmienił się voiceId).

    **Persist:**
    17. Toggle lektora ON, voice select, mode='nauka'. Refresh strony. Oczekiwane: lektor ON zachowany, mode='nauka' zachowany (chyba że było mode='egzamin' — wtedy fallback do 'free' per Plan 11-01 design).

    **Boundary + bundle:**
    18. Otwórz Terminal: `npm run build`. Sprawdź czy main bundle (`dist/assets/index-*.js`) < 850 KB.
    19. `npm test -- --run` — wszystkie testy zielone (777 baseline + Phase 11 nowe).
  </how-to-verify>
  <resume-signal>
    Wpisz "approved" jeśli wszystkie 19 punktów przeszły (lub udokumentuj odstępstwa i napisz "approved with notes: [...]" — wszelkie odstępstwa będą notatkami w SUMMARY).
    Jeśli krytyczna regresja (mode flow blokuje user, lektor crashuje app, bundle > 850 KB) → opisz problem; planner przygotuje fix-up plan w follow-up phase 11.1.
  </resume-signal>
</task>

</tasks>

<verification>
- Task 1 aggregate audit zielony.
- Task 2 manual smoke approved.
- Phase 11 ZAMKNIĘTA.
</verification>

<success_criteria>
- FUNC-11-13: all preserved invariants verified — 15 interactables, boundary clean, 777+ tests green, bundle < 850 KB.
- Phase 11 wszystkie 13 wymagań FUNC-11-01..13 zakończone.
- ROADMAP Phase 11 może zostać oznaczone ✅.
- CORS resolution udokumentowane (direct fetch lub proxy uncommented).
- Security warning VITE_ leak zaznaczony jako akceptowalne MVP w SUMMARY (long-term backend proxy v1.2+).
</success_criteria>

<output>
Utwórz `.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-06-SUMMARY.md` z:
- Wynik 19-punktowego smoke flow (approved / approved with notes)
- CORS resolution path (direct fetch vs proxy)
- Bundle size measurement (KB)
- Total tests count (baseline+Phase 11 delta)
- Phase 11 close metrics + 13/13 wymagań DONE
- Recommendations dla v1.2 (backend proxy, 2 dodatkowe PL głosy, etc.)
- ROADMAP update flag (Phase 11 ✅ ready)
</output>
