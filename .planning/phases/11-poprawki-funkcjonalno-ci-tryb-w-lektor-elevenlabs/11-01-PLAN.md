---
phase: 11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/trainingStore.js
  - src/main.js
  - src/ui/StatusPanel.js
  - src/i18n/pl.js
  - tests/trainingStore.test.js
  - tests/modeStateMachine.test.js
  - tests/application.test.js
  - tests/StatusPanel.test.js
autonomous: true
requirements: [FUNC-11-01, FUNC-11-02, FUNC-11-03, FUNC-11-06]

must_haves:
  truths:
    - "Cold start → store.mode === 'free' (FUNC-11-01)"
    - "setMode('nauka') synchronicznie ustawia difficulty='nauka' i freeRoam=false (alias)"
    - "setMode('egzamin') jest zablokowane gdy mode==='egzamin' i session.finishedAt===null (lock podczas sesji egzaminu)"
    - "StatusPanel renderuje 3-stanowy toggler trybu (free/nauka/egzamin); klik cykli swobodny→nauka→egzamin→swobodny"
    - "777 baseline testów Phase 9 pozostaje zielone — alias do difficulty/freeRoam nie zmienia istniejących subskryberów"
  artifacts:
    - path: "src/state/trainingStore.js"
      provides: "Field `mode` + actions setMode/endExam + persist key 'pm300:mode:v1'"
      contains: "setMode"
    - path: "src/main.js"
      provides: "Cold-start bootstrap mode='free' (lub localStorage) PRZED startScenario; subscriber persist mode"
    - path: "src/ui/StatusPanel.js"
      provides: "3-state mode toggler (replaces 2-state difficulty toggler) z lockiem podczas egzaminu"
    - path: "tests/modeStateMachine.test.js"
      provides: "Unit testy: cold-start free, transitions free⇄nauka⇄egzamin, lock w trakcie egzaminu, alias projekcja"
  key_links:
    - from: "src/main.js bootstrap"
      to: "store.setMode('free')"
      via: "import.meta env + localStorage 'pm300:mode:v1' read"
      pattern: "pm300:mode:v1"
    - from: "src/ui/StatusPanel.js"
      to: "store.getState().setMode"
      via: "click handler na difficulty-toggle button (rebrand do mode-toggle)"
      pattern: "setMode\\("
---

<objective>
Wprowadzić canonical `mode: 'free'|'nauka'|'egzamin'` jako single source of truth dla flow trybów Phase 11. `setMode` alias-projektuje legacy pola `difficulty`/`freeRoam` synchronicznie, dzięki czemu 777 testów Phase 9 pozostają zielone. Cold start = `mode='free'` (FUNC-11-01). StatusPanel toggler cyklotwarczo przełącza 3 stany z lockiem podczas aktywnej sesji egzaminu (FUNC-11-02). Click w mode='free' nie wywołuje attemptStep — RaycastController już dziś early-returnuje na `freeRoam` (linia 173), co pokrywa FUNC-11-03 część "klik nie advansuje SOP". Część "klik = krótki opis 1 zdanie" zostaje delegowana do Plan 11-03 (info panel).

Purpose: bez tego canonical mode'a Plany 11-03..11-05 nie mają na czym budować flow (info panel otwiera się tylko w `mode==='nauka'`, exam prompt triggeruje tylko gdy `mode==='nauka'` i SOP done).
Output: store.mode + actions + UI toggler + 7 unit testów.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md
@CLAUDE.md
@src/state/trainingStore.js
@src/main.js
@src/ui/StatusPanel.js
@src/i18n/pl.js

<interfaces>
trainingStore (existing fields/actions relevant):
- session: { scenarioId, startedAt, finishedAt, attempts, retryCount }
- difficulty: 'nauka' | 'egzamin' (default 'nauka')
- freeRoam: boolean (default false)
- activeModal: null | 'help' | 'confirm-scenario-switch' | ...
- setDifficulty(mode), toggleFreeRoam(), openConfirmModal(payload), closeModal()
- subscribeWithSelector — store.subscribe(selector, cb, {fireImmediately?})

main.js bootstrap pattern (linie 43-101):
- HC_STORAGE_KEY = 'pm300:hc-outline:v1'
- DIFFICULTY_KEY = 'pm300:difficulty:v1'
- AUDIO_MUTE_KEY = 'pm300:audio-mute:v1'
- Read via try/catch localStorage.getItem → setState PRZED startScenario
- Persist subscriber: store.subscribe((s)=>s.difficulty, (v)=>{ try{ localStorage.setItem(...) }catch{} })

StatusPanel difficulty toggler (linie 108-112, 211-212):
- _difficultyToggleBtn → klik → setDifficulty(cur==='nauka'?'egzamin':'nauka')
- _render: difficultyBadge variant + toggle button label przeciwny stan
- freeRoamIndicator visibility-toggle
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rozszerzyć trainingStore o `mode` + `setMode` + `endExam` (alias do difficulty/freeRoam)</name>
  <read_first>
    - src/state/trainingStore.js (linie 1-170: initial state + setDifficulty/toggleFreeRoam/openConfirmModal pattern)
    - tests/trainingStore.test.js (wzorzec describe blocków + asercji na set actions)
    - .planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md sekcja "Store extension: `mode`" (linie 419-444)
  </read_first>
  <files>src/state/trainingStore.js, tests/trainingStore.test.js, tests/modeStateMachine.test.js</files>
  <behavior>
    - RED: tests/modeStateMachine.test.js asercje:
      1. Initial: store.getState().mode === 'free' (cold-start default)
      2. setMode('nauka') → mode='nauka', difficulty='nauka', freeRoam=false (alias sync)
      3. setMode('egzamin') gdy session.finishedAt===null I mode==='egzamin' (aktywna sesja) → no-op (mode pozostaje 'egzamin', warning log opcjonalny)
      4. setMode('egzamin') gdy mode!=='egzamin' → mode='egzamin', difficulty='egzamin', freeRoam=false
      5. setMode('free') → mode='free', difficulty='nauka' (legacy), freeRoam=true
      6. endExam() → mode='free', difficulty='nauka', freeRoam=true, niezależnie od poprzedniego mode
      7. Po startScenario(scenario) → mode NIE jest resetowany (orthogonal do scenario lifecycle, analog hcOutlineMode D-Phase4-09)
    - tests/trainingStore.test.js dodać 2 asercje: existujący setDifficulty + toggleFreeRoam nadal działają niezależnie (backward compat — testy Phase 5 nie nakreślone, my dorzucamy ortogonalność).
  </behavior>
  <action>
    Zmodyfikuj `src/state/trainingStore.js`:
    1. Dodaj pole `mode: 'free'` do initial state (DEFAULT, po liniach z difficulty/freeRoam).
    2. Dodaj akcję `setMode: (next) => { ... }`:
       - Pobierz `state = get()`. Jeśli `state.mode === 'egzamin' && state.session.finishedAt === null && next !== 'egzamin'` → log warn `[trainingStore] setMode zablokowany podczas aktywnej sesji egzaminu` i return (FUNC-11-02 lock per discuss-decision).
       - Inaczej: `set({ mode: next, difficulty: next === 'egzamin' ? 'egzamin' : 'nauka', freeRoam: next === 'free' })` (alias projekcja per discuss-decision #1: 'free'→difficulty='nauka'+freeRoam=true; 'nauka'→difficulty='nauka'+freeRoam=false; 'egzamin'→difficulty='egzamin'+freeRoam=false).
    3. Dodaj akcję `endExam: () => set({ mode: 'free', difficulty: 'nauka', freeRoam: true })` — FUNC-11-06.
    4. NIE modyfikuj setDifficulty/toggleFreeRoam — pozostają ortogonalne dla testów Phase 5 (backward compat).
    5. NIE modyfikuj startScenario — mode nie jest resetowany (per goal-backward truth).
    Utwórz nowy plik `tests/modeStateMachine.test.js` z 7 asercjami z behavior. Test używa `createTrainingStore({ now: ()=>1000, scheduleTimer: ()=>0 })` — pattern z istniejących trainingStore.test.js.
    Dodaj 2 asercje do tests/trainingStore.test.js w nowym describe 'mode alias backward compat': setMode('nauka') pozostawia toggleFreeRoam() funkcjonalnym, setDifficulty('egzamin') po setMode('free') nie zmienia mode (pola ortogonalne).
  </action>
  <verify>
    <automated>npx vitest run tests/modeStateMachine.test.js tests/trainingStore.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/modeStateMachine.test.js: 7/7 PASS
    - tests/trainingStore.test.js: 0 regresji (baseline+2 nowe PASS)
    - `grep -c "setMode" src/state/trainingStore.js` ≥ 2 (definicja + ewentualne referencje)
    - `grep -c "endExam" src/state/trainingStore.js` ≥ 1
    - mode pole obecne w initial state (grep "mode:" w bloku initial)
  </acceptance_criteria>
  <done>Store ma `mode` + `setMode` + `endExam`, alias projekcja zsynchronizowana, lock w trakcie egzaminu działa, 7 nowych testów zielonych.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cold start mode='free' w main.js bootstrap + persist subscriber 'pm300:mode:v1'</name>
  <read_first>
    - src/main.js (linie 43-110: HC_STORAGE_KEY + DIFFICULTY_KEY + AUDIO_MUTE_KEY bootstrap pattern, try/catch graceful catch, setState PRZED startScenario)
    - tests/application.test.js (wzorzec Phase 4 wiring describe block)
    - src/state/trainingStore.js (po Task 1: store.subscribe selector pattern)
  </read_first>
  <files>src/main.js, tests/application.test.js</files>
  <behavior>
    - Cold start: gdy localStorage nie ma 'pm300:mode:v1' → bootstrap setState({mode:'free'}). Gdy ma valid value ('free'|'nauka'|'egzamin') → użyj go (z wyjątkiem 'egzamin' → fallback do 'free', bo session jest świeży po reload — nie wpychaj usera w stary egzamin).
    - Persist subscriber: store.subscribe(s=>s.mode, v => localStorage.setItem('pm300:mode:v1', v)) z try/catch (T-04-13 pattern).
    - mode bootstrap setState MUSI się odpalić PRZED store.getState().startScenario(uruchomienie) (linia 108 main.js), tak żeby subskrybery (StatusPanel, RaycastController) widzieli prawidłowy initial state.
    - tests/application.test.js dodać describe 'Phase 11 mode bootstrap':
      1. Świeży localStorage → app.store.getState().mode === 'free' po construct
      2. localStorage 'pm300:mode:v1'='nauka' → mode==='nauka' po construct
      3. localStorage 'pm300:mode:v1'='egzamin' → fallback 'free' (świeża sesja)
      4. store.setMode('nauka') → localStorage.getItem('pm300:mode:v1') === 'nauka' po flush
  </behavior>
  <action>
    Zmodyfikuj `src/main.js`:
    1. Dodaj stałą `const MODE_KEY = 'pm300:mode:v1';` koło istniejących KEY constów (linia 43-45).
    2. Dodaj blok bootstrap modeInitial analogiczny do `difficultyInitial` (linie 80-85):
       - try { const v = localStorage.getItem(MODE_KEY); return (v==='free'||v==='nauka') ? v : 'free'; } catch { return 'free'; } 
       - 'egzamin' fallback do 'free' (nie chcemy startować z zablokowanym togglerem).
    3. Dodaj `this.store.setState({ mode: modeInitial })` BEZPOŚREDNIO PO istniejącym setState({difficulty, audioMuted}) (linia 90) — przed startScenario na linii 108.
    4. Dodaj persist subscriber w bloku `_unsubscribers.push(...)` (linie 94-101) analogicznie do difficulty:
       `this.store.subscribe((s) => s.mode, (v) => { try { localStorage.setItem(MODE_KEY, v); } catch { /* silent */ } })`
    Zmodyfikuj `tests/application.test.js` — dodaj describe 'Phase 11 mode bootstrap' z 4 asercjami z behavior. Użyj `localStorage.clear()` w beforeEach, `vi.spyOn(localStorage,'setItem')` dla testu persist subscriber.
  </action>
  <verify>
    <automated>npx vitest run tests/application.test.js -t "Phase 11 mode bootstrap" --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/application.test.js "Phase 11 mode bootstrap": 4/4 PASS
    - `grep -c "MODE_KEY\|pm300:mode:v1" src/main.js` ≥ 3 (const, read, persist write)
    - `grep "store.setState.*mode:" src/main.js` matches modeInitial bootstrap line
    - mode bootstrap setState pojawia się PRZED `startScenario(uruchomienie)` linia 108 (manualnie zweryfikuj kolejność w pliku)
  </acceptance_criteria>
  <done>Cold start ustawia mode='free', persist trwa across reload (z egzamin→free safety), 4 nowe testy zielone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StatusPanel 3-stanowy mode toggler z lockiem podczas egzaminu</name>
  <read_first>
    - src/ui/StatusPanel.js (cały plik: _build, _onDifficultyClick linie 108-112, _render linie 195-232)
    - src/i18n/pl.js (sekcja pl.ui — pattern setDifficultyEgzamin/setDifficultyNauka, difficultyNauka/difficultyEgzamin)
    - tests/StatusPanel.test.js (wzorzec describe + subscriber state tests)
  </read_first>
  <files>src/ui/StatusPanel.js, src/i18n/pl.js, tests/StatusPanel.test.js</files>
  <behavior>
    - _difficultyToggleBtn (zachowuje selector .status-panel__difficulty-toggle — minimalny CSS diff) cyklotwarczo: free→nauka→egzamin→free.
    - Disabled (aria-disabled + button.disabled=true) gdy `mode==='egzamin' && session.finishedAt===null` (lock podczas aktywnej sesji egzaminu) — per discuss-decision FUNC-11-02.
    - Label opisuje "co stanie się po kliknięciu" (next mode): "Tryb: Nauka" / "Tryb: Egzamin" / "Tryb: Swobodny".
    - difficultyBadge pokazuje current mode (3 warianty): "Swobodny" / "Nauka" / "Egzamin" z odpowiednimi klasami `difficulty-badge--{free,nauka,egzamin}`.
    - Subscriber dorzucony na s.mode + s.session.finishedAt (dla unlock po endExam).
    - tests/StatusPanel.test.js dodać describe 'Phase 11 mode toggler':
      1. Initial mode='free' → button text "Tryb: Nauka" (next), badge "Swobodny"
      2. Klik gdy mode='free' → setMode('nauka') called (spy)
      3. Klik gdy mode='nauka' → setMode('egzamin') called
      4. Klik gdy mode='egzamin' I finishedAt===null → button.disabled true, klik no-op (spy nie wołany)
      5. Klik gdy mode='egzamin' I finishedAt!==null → button enabled, klik → setMode('free') called
  </behavior>
  <action>
    Zmodyfikuj `src/ui/StatusPanel.js`:
    1. Zrekonstruuj `_onDifficultyClick` (linia 108-112): cyklotwarcza tablica `['free','nauka','egzamin']`, next = arr[(arr.indexOf(cur)+1)%3]; wywołaj `store.getState().setMode(next)` (NIE setDifficulty — używamy Plan 11-01 akcji). 
    2. W `_render` (linia 195+) dodaj logikę dla `s.mode`:
       - this._difficultyBadge.className = `difficulty-badge difficulty-badge--${s.mode}` (3 warianty)
       - this._difficultyBadge.textContent = pl.ui.modeLabel[s.mode] (zdefiniowane w pl.js Task action poniżej)
       - this._difficultyToggleBtn.textContent = pl.ui.setModeNext[next] gdzie next = cyklotwarczy następny stan
       - lock: const locked = s.mode==='egzamin' && s.session.finishedAt===null; this._difficultyToggleBtn.disabled = locked; aria-disabled=String(locked)
    3. Dorzuć subskryberów w `_wireSubscribers`: `s.mode`, `s.session.finishedAt` (każdy → this._render()).
    Zmodyfikuj `src/i18n/pl.js`:
    - W `pl.ui` dodaj: `modeLabel: { free: 'Swobodny', nauka: 'Nauka', egzamin: 'Egzamin' }, setModeNext: { free: 'Tryb: Nauka', nauka: 'Tryb: Egzamin', egzamin: 'Tryb: Swobodny' }` (per CLAUDE.md polski w UI).
    Zmodyfikuj `tests/StatusPanel.test.js` — dodaj describe 'Phase 11 mode toggler' z 5 asercjami z behavior; spy: `vi.spyOn(store.getState(), 'setMode')` lub direct akcja-callback.
  </action>
  <verify>
    <automated>npx vitest run tests/StatusPanel.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/StatusPanel.test.js: baseline + 5 nowych testów PASS (zero regresji)
    - `grep "setMode\\(" src/ui/StatusPanel.js` matches (toggler woła setMode)
    - `grep "modeLabel\\|setModeNext" src/i18n/pl.js` ≥ 2 occurrences
    - Lock: gdy `s.mode==='egzamin' && finishedAt===null` → button.disabled true (sprawdzane w teście)
    - Brak regresji w pełnym suite: `npm test -- --run` po Task 3 → 777 + Phase 11 nowych zielone
  </acceptance_criteria>
  <done>StatusPanel cykli 3 trybami, lockuje egzamin, pełen suite (777+) zielony.</done>
</task>

</tasks>

<verification>
- Wszystkie 3 nowe testy zielone (modeStateMachine, application Phase 11 bootstrap, StatusPanel Phase 11 mode toggler).
- Baseline 777 testów Phase 9 niezachwiane (npm test -- --run pełny suite).
- Cold start manualnie: brak localStorage → mode='free'; po setMode('nauka') reload → mode='nauka'.
</verification>

<success_criteria>
- store.mode + setMode + endExam zaimplementowane z alias sync do difficulty/freeRoam (FUNC-11-01, 11-02, 11-06).
- Cold start mode='free' z fallback dla 'egzamin' value (FUNC-11-01).
- StatusPanel 3-state toggler z lock w trakcie egzaminu (FUNC-11-02).
- RaycastController early-return na freeRoam (linia 173) pokrywa FUNC-11-03 część "klik w free mode nie wywołuje attemptStep" — info panel część FUNC-11-03 jest w Plan 11-03.
- Pełen suite 777+ green, baseline preserved.
</success_criteria>

<output>
Utwórz `.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-01-SUMMARY.md` zawierający: pliki zmienione, decyzje (alias sync convention, egzamin→free fallback przy reload), liczba testów dodanych, baseline status, hints dla Plan 11-03/11-04 (jak konsumować s.mode).
</output>
