---
phase: 11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs
plan: 05
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - src/lector/LectorService.js
  - src/data/lectorVoices.js
  - src/state/trainingStore.js
  - src/ui/ElementInfoPanel.js
  - src/ui/StatusPanel.js
  - src/main.js
  - src/i18n/pl.js
  - .env.example
  - vite.config.js
  - tests/lectorService.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [FUNC-11-09, FUNC-11-10, FUNC-11-11, FUNC-11-12]
user_setup:
  - service: ElevenLabs
    why: "TTS dla element info i instrukcji SOP (FUNC-11-09)"
    env_vars:
      - name: VITE_ELEVENLABS_API_KEY
        source: "ElevenLabs Dashboard → Profile → API Keys; KOPIA do .env w root projektu"
    dashboard_config:
      - task: "Założenie konta ElevenLabs (free tier 10k znaków/miesiąc wystarczy dla MVP edukacyjnego)"
        location: "https://elevenlabs.io/sign-up"
    security_warning: "VITE_ env vars wyciekają do production bundle (Vite inline). Klucz widoczny w DevTools po build. Akceptowalne dla MVP edukacyjnego (PRD decision); long-term wymaga backend proxy (out-of-scope v1.2)."

must_haves:
  truths:
    - "LectorService.speak(text, voiceId) wykonuje POST https://api.elevenlabs.io/v1/text-to-speech/{voiceId} z xi-api-key header"
    - "Drugi speak z tym samym (text, voiceId) NIE wykonuje fetch (cache hit)"
    - "Brak klucza VITE_ELEVENLABS_API_KEY → isAvailable()===false; UI button disabled+tooltip"
    - "Cache LRU max 20 wpisów; eviction woła URL.revokeObjectURL (anti-leak)"
    - "localStorage persist 'pm300:lector:enabled' + 'pm300:lector:voice'"
    - "ElementInfoPanel renderuje 🔊 button w .element-info-panel__lector-slot"
    - "StatusPanel renderuje toggle lektora on/off + voice picker (3 PL głosy)"
  artifacts:
    - path: "src/lector/LectorService.js"
      provides: "fetch + Blob URL cache + LRU eviction + isAvailable()"
      contains: "URL.revokeObjectURL"
    - path: "src/data/lectorVoices.js"
      provides: "3 frozen PL voice entries (id + label)"
      contains: "Object.freeze"
    - path: ".env.example"
      provides: "Template z VITE_ELEVENLABS_API_KEY= (pusty)"
    - path: "vite.config.js"
      provides: "Skomentowany dev proxy fallback dla CORS"
  key_links:
    - from: "ElementInfoPanel 🔊 button"
      to: "LectorService.speak(text, voiceId)"
      via: "click handler"
      pattern: "speak\\("
    - from: "LectorService cache"
      to: "URL.revokeObjectURL"
      via: "LRU eviction + dispose"
      pattern: "revokeObjectURL"
---

<objective>
Wprowadzić ElevenLabs TTS Lektor: nowa klasa serwisu (boundary-clean DI z store), 3 polskie głosy w pure-data module, integracja z ElementInfoPanel (🔊 button) i StatusPanel (toggle + voice picker), persist localStorage, .env.example, opcjonalny Vite dev proxy fallback dla CORS.

Per discuss-decision Q3 (Wybór A): Full-response Blob URL approach (NIE streaming, NIE SDK).
Per discuss-decision implicit: 3 polskie głosy w `src/data/lectorVoices.js`.
Security: klucz VITE_* wycieka do bundle — akceptowalne MVP, udokumentowane w komentarzu LectorService + .env.example + README warning.

Wave 3 depends on 11-03 (ElementInfoPanel slot dla 🔊 button) + 11-04 (paralel z którym nie współdzielimy plików kluczowych — pl.js minor merge konflikt jeśli oba zostaną wykonane).
Output: Lektor service + UI integration + persist + Vite proxy fallback + 8+ unit testów.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md
@CLAUDE.md
@src/education/AudioController.js
@src/ui/ElementInfoPanel.js
@src/ui/StatusPanel.js
@src/main.js
@src/state/trainingStore.js

<interfaces>
AudioController DI pattern (wzorzec do LectorService):
- constructor({ store }) — subscribe(s.audioMuted) i toggle audio
- dispose() — unsubscribers + audio cleanup

trainingStore (po Plan 11-01 + 11-04 merged):
- mode, setMode, endExam (Plan 11-01)
- _examPromptShown (Plan 11-04)
- _elementInfoMeshId (Plan 11-03)
- Plan 11-05 dorzuca: lectorEnabled: boolean (default false), lectorVoiceId: string (default voice IDs[0]), setLectorEnabled, setLectorVoiceId

main.js bootstrap pattern (linie 43-101): KEY consty + try/catch graceful + setState bootstrap + persist subscriber.

ElementInfoPanel.element-info-panel__lector-slot (z Plan 11-03 Task 2):
- Pusty div w modal body; Plan 11-05 dorzuca innerHTML button.element-info-panel__lector-btn z 🔊 + label
- Klik → LectorService.speak(currentInfoText, currentVoiceId)

ElevenLabs API contract (RESEARCH linie 235-247):
- POST https://api.elevenlabs.io/v1/text-to-speech/{voiceId}
- Headers: { 'xi-api-key': key, 'Content-Type': 'application/json', 'Accept': 'audio/mpeg' }
- Body: { text, model_id: 'eleven_multilingual_v2', output_format: 'mp3_44100_128' }
- Response: audio/mpeg Blob

3 PL voice IDs (per discuss-decision implicit + research A3):
- Damian PL: S1JKkpuAQNsowB8ZvKRO (research-referenced; [ASSUMED] tertiary source — może wymagać weryfikacji w runtime)
- 2 inne PL voiceIDs do dobrania przez executora podczas runtime — research nie nazywa konkretnych. **DECYZJA dla executora:** jeśli executor nie ma drugich 2 IDs w ręku, użyje fallback: 1 voice tylko (Damian) + komentarz "TODO Phase 11.1: dodać 2 dodatkowe głosy po user-driven testach". Lepiej 1 działający niż 3 zmyślone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lector/LectorService.js + lectorVoices.js + cache LRU + isAvailable</name>
  <read_first>
    - src/education/AudioController.js (cały — wzorzec DI store + dispose pattern)
    - .planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md linie 196-263 (Pattern 1 example) + 374-397 (TTS request example) + 322-330 (Pitfall 3 Blob URL leak)
    - tests/AudioController.test.js (jeśli istnieje) lub inny test DI pattern z spies
  </read_first>
  <files>src/lector/LectorService.js, src/data/lectorVoices.js, tests/lectorService.test.js, tests/boundaries.test.js</files>
  <behavior>
    - tests/lectorService.test.js (8 asercji):
      1. isAvailable() === false gdy apiKey===null (constructor opt apiKey=null)
      2. isAvailable() === true gdy apiKey=string
      3. speak(text, voiceId) wykonuje fetch z poprawnym URL (`https://api.elevenlabs.io/v1/text-to-speech/{voiceId}`), method POST, header xi-api-key, body zawiera text+model_id+output_format (test spy: vi.fn() jako fetchImpl DI)
      4. Drugi speak z tym samym (text, voiceId) → fetchImpl called RAZ (cache hit)
      5. Cache LRU max 20 — 21szy wpis powoduje URL.revokeObjectURL na najstarszym (mock URL.revokeObjectURL + URL.createObjectURL)
      6. dispose() woła URL.revokeObjectURL na wszystkich pozostałych blob URLs + clear cache
      7. Brak klucza → speak() return undefined (no-throw, graceful)
      8. Fetch error (res.ok===false) → throw Error zawierający status code; cache NIE rejestruje failed entry
  </behavior>
  <action>
    Utwórz `src/data/lectorVoices.js`:
    - Header: Phase 11 FUNC-11-12 — 3 PL voiceIds (frozen).
    - Eksport:
      ```
      export const LECTOR_VOICES = Object.freeze([
        Object.freeze({ id: 'S1JKkpuAQNsowB8ZvKRO', label: 'Damian (PL, mężczyzna)' }),
        // TODO Phase 11.1: dorzucić 2 dodatkowe PL głosy po user-driven testach z ElevenLabs Voice Library
        // (research A3 — tylko Damian potwierdzony jako stabilny; pozostałe wymagają empirycznej weryfikacji)
      ]);
      export const DEFAULT_LECTOR_VOICE_ID = LECTOR_VOICES[0].id;
      ```
    Utwórz `src/lector/LectorService.js` (wg wzorca z research linie 200-263):
    - Header z security warning: "Phase 11 FUNC-11-09..11. SECURITY: VITE_ELEVENLABS_API_KEY wycieka do production bundle (Vite inline). Akceptowalne MVP edukacyjny; long-term wymaga backend proxy (deferred do v1.2+). Klucz przed deployment NIE może być commercial production key."
    - Boundary clean: tylko `./data/lectorVoices.js` import + browser globals (fetch, URL, Audio). NIE THREE/gsap/training/highlight/state.
    - Class:
      - constructor({ store, fetchImpl = fetch, apiKey = import.meta.env?.VITE_ELEVENLABS_API_KEY ?? null, audioCtor = (typeof Audio !== 'undefined' ? Audio : null) }) — DI dla testowalności
      - _cache: Map<key, { blobUrl, lastUsed, size? }>; _maxEntries = 20
      - isAvailable(): Boolean(apiKey)
      - async speak(text, voiceId): null-key graceful return; jeśli text length > 5000 → trim do 4900 (V5 input validation guard); cache key = `${voiceId}::${text}`; cache miss → _fetchTTS + URL.createObjectURL + cache.set + _evictLRU; cache hit → update lastUsed; setAudio src + .play().catch(noop) (autoplay safety Pitfall 7)
      - _fetchTTS(text, voiceId): fetch POST URL z header xi-api-key + body JSON {text, model_id:'eleven_multilingual_v2', output_format:'mp3_44100_128'}; res.ok check throw; return res.blob()
      - _evictLRU(): jeśli cache.size > maxEntries — znajdź wpis o najmniejszym lastUsed (sort entries), URL.revokeObjectURL na blobUrl, cache.delete
      - dispose(): pause/clean audio, iter cache values + URL.revokeObjectURL każdy, cache.clear
    Utwórz `tests/lectorService.test.js` z 8 testami z behavior. Mock fetchImpl jako vi.fn().mockResolvedValue({ ok: true, blob: () => Promise.resolve(new Blob(['fake-mp3'], { type: 'audio/mpeg' })) }). Mock URL.createObjectURL = vi.fn(()=>'blob:fake-url') i URL.revokeObjectURL = vi.fn() w beforeEach.
    Zmodyfikuj `tests/boundaries.test.js` — dodaj entries:
       `{ file: 'src/lector/LectorService.js', mustNotImport: ['three', 'gsap', '../state/', '../training/', '../highlight/', '../ui/', '../RaycastController'] }`
       `{ file: 'src/data/lectorVoices.js', mustNotImport: ['three', 'gsap', '../state/', '../training/', '../highlight/', '../ui/'] }`
  </action>
  <verify>
    <automated>npx vitest run tests/lectorService.test.js tests/boundaries.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/lectorService.test.js: 8/8 PASS
    - tests/boundaries.test.js: PASS z nowymi entries
    - `grep "URL.revokeObjectURL" src/lector/LectorService.js` ≥ 2 (eviction + dispose)
    - `grep "xi-api-key" src/lector/LectorService.js` ≥ 1
    - `grep "eleven_multilingual_v2" src/lector/LectorService.js` ≥ 1
    - `grep "Object.freeze" src/data/lectorVoices.js` ≥ 2 (array + entries)
    - SECURITY warning comment widoczny w LectorService.js
  </acceptance_criteria>
  <done>LectorService z fetch+cache+LRU+isAvailable+dispose, 3 voices module (1 confirmed + TODO), 8 testów zielone, boundary updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: trainingStore lector state + UI integration (ElementInfoPanel 🔊 button + StatusPanel toggle/voice picker) + main.js wiring + .env.example + vite proxy fallback</name>
  <read_first>
    - src/state/trainingStore.js (po Plan 11-04 — initial state + actions; gdzie dodać lectorEnabled/lectorVoiceId)
    - src/ui/ElementInfoPanel.js (po Plan 11-03 — slot .element-info-panel__lector-slot do wypełnienia)
    - src/ui/StatusPanel.js (po Plan 11-01 — gdzie dorzucić lector toggle + select)
    - src/main.js (po Plan 11-01 + 11-03 + 11-04 — bootstrap KEY pattern; wiring instantiate LectorService DI do ElementInfoPanel i StatusPanel)
    - src/data/lectorVoices.js (z Task 1)
    - .gitignore — sprawdź czy .env tam jest; jeśli nie — dorzuć
    - vite.config.js — sprawdź czy istnieje; jeśli nie, utworzyć minimal
  </read_first>
  <files>src/state/trainingStore.js, src/ui/ElementInfoPanel.js, src/ui/StatusPanel.js, src/main.js, src/i18n/pl.js, .env.example, vite.config.js, .gitignore, tests/lectorService.test.js, tests/ElementInfoPanel.test.js, tests/StatusPanel.test.js</files>
  <behavior>
    - Store extension:
      - lectorEnabled: false default
      - lectorVoiceId: DEFAULT_LECTOR_VOICE_ID (import z lectorVoices.js) default
      - setLectorEnabled(v): set({ lectorEnabled: v })
      - setLectorVoiceId(id): set({ lectorVoiceId: id })
    - main.js bootstrap (analog DIFFICULTY_KEY pattern):
      - LECTOR_ENABLED_KEY = 'pm300:lector:enabled'
      - LECTOR_VOICE_KEY = 'pm300:lector:voice'
      - bootstrap read → setState PRZED startScenario
      - persist subscribers (try/catch)
      - LectorService instantiate: `this.lectorService = new LectorService({ store: this.store });`
      - DI do ElementInfoPanel: zmodyfikuj ElementInfoPanel konstruktor by przyjmował opcjonalnie `lectorService` — po-hoc assign po LectorService instantiate: `this.elementInfoPanel._lectorService = this.lectorService;` LUB constructor extension. Preferowane: opcjonalny DI w ctor (DI uniform). Jeśli Plan 11-03 nie przewidział, dorzuć tutaj.
      - DI do StatusPanel: analog `lectorVoices` props lub po-hoc.
      - Dispose: `if (this.lectorService) this.lectorService.dispose();` w pozycji analogicznej do audioController.
    - ElementInfoPanel 🔊 button:
      - W _render gdy panel widoczny + lectorService.isAvailable() && s.lectorEnabled → renderuj button.element-info-panel__lector-btn (🔊 + pl.ui.lectorListenButton) w .element-info-panel__lector-slot.
      - Disabled gdy !lectorService.isAvailable(); tooltip "Lektor wymaga konfiguracji klucza ElevenLabs" (pl.ui.lectorMissingKeyTooltip).
      - Klik → text = full content of currently shown info (np. `${name}. Funkcja: ${function}. BHP: ${safety}.`); voiceId = s.lectorVoiceId; lectorService.speak(text, voiceId).catch(noop).
    - StatusPanel lector toggle + voice picker (dorzucone do hamburger controls):
      - button.status-panel__lector-toggle (aria-pressed bound to s.lectorEnabled) — klik wywołuje setLectorEnabled toggle
      - select.status-panel__lector-voice z opcjami z LECTOR_VOICES (label) — change wywołuje setLectorVoiceId
      - Disabled gdy !lectorService.isAvailable() (oba elementy)
      - Pass lectorService + lectorVoices przez konstruktor (DI extension analog scenarios).
    - .env.example: utworzyć w root z treścią:
      ```
      # ElevenLabs TTS Lektor (Phase 11 FUNC-11-10)
      # SECURITY: VITE_* env vars wyciekają do production bundle (Vite inline).
      # Klucz widoczny w DevTools po build — akceptowalne MVP, NIE deployować production key.
      # Long-term: backend proxy (deferred do v1.2+).
      VITE_ELEVENLABS_API_KEY=
      ```
    - .gitignore: sprawdź czy `.env` jest; jeśli nie — dorzuć linię `.env`.
    - vite.config.js: jeśli plik istnieje, dorzuć skomentowany blok dev proxy (komentarz "uncomment if CORS blokuje direct fetch — wymaga restart npm run dev"); jeśli nie istnieje, utwórz minimal:
      ```
      // Phase 11 FUNC-11-09: dev proxy fallback dla ElevenLabs CORS. UNCOMMENT IF NEEDED.
      // import { defineConfig } from 'vite';
      // export default defineConfig({
      //   server: {
      //     proxy: {
      //       '/elevenlabs': {
      //         target: 'https://api.elevenlabs.io',
      //         changeOrigin: true,
      //         rewrite: (path) => path.replace(/^\/elevenlabs/, ''),
      //       },
      //     },
      //   },
      // });
      ```
      Jeśli już istnieje — dodaj skomentowany blok bez modyfikacji aktywnej konfiguracji.
    - pl.js: dodać pl.ui.lectorListenButton ('Odsłuchaj' — może już dodane w Plan 11-03), lectorMissingKeyTooltip ('Lektor wymaga konfiguracji klucza ElevenLabs'), lectorToggleOn ('Lektor: ON'), lectorToggleOff ('Lektor: OFF'), lectorVoicePickerLabel ('Głos lektora').
    - Testy dorzucone:
      - tests/lectorService.test.js + 2 asercje: store integration — setLectorEnabled persist; setLectorVoiceId persist (mock localStorage).
      - tests/ElementInfoPanel.test.js + 2 asercje: 🔊 button renderowany gdy lectorService.isAvailable() && lectorEnabled; disabled+tooltip gdy isAvailable===false.
      - tests/StatusPanel.test.js + 2 asercje: lector toggle button + voice select renderowane; klik toggle → setLectorEnabled called.
  </behavior>
  <action>
    Zmodyfikuj `src/state/trainingStore.js`:
    1. Initial state: `lectorEnabled: false, lectorVoiceId: DEFAULT_LECTOR_VOICE_ID` (import z '../data/lectorVoices.js').
    2. Akcje: `setLectorEnabled: (v) => set({ lectorEnabled: !!v }), setLectorVoiceId: (id) => set({ lectorVoiceId: id })`.
    Zmodyfikuj `src/main.js`:
    1. Import: `import { LectorService } from './lector/LectorService.js'; import { LECTOR_VOICES, DEFAULT_LECTOR_VOICE_ID } from './data/lectorVoices.js';`
    2. Stałe: `const LECTOR_ENABLED_KEY = 'pm300:lector:enabled'; const LECTOR_VOICE_KEY = 'pm300:lector:voice';`
    3. Bootstrap analog do difficulty (po linii 90): lectorEnabledInitial (try-catch read), lectorVoiceIdInitial (try-catch read; valid value check przeciw LECTOR_VOICES.map(v=>v.id)), setState({lectorEnabled, lectorVoiceId}).
    4. Persist subscribers po linii 101: `s.lectorEnabled → localStorage.setItem(LECTOR_ENABLED_KEY, String(v))`; `s.lectorVoiceId → localStorage.setItem(LECTOR_VOICE_KEY, v)`.
    5. Instantiate LectorService: po AudioController linii 222: `this.lectorService = new LectorService({ store: this.store });`
    6. ElementInfoPanel DI: zmodyfikuj instantiation by przekazać `lectorService: this.lectorService` i `lectorVoices: LECTOR_VOICES`.
    7. StatusPanel DI: dorzucić `lectorService: this.lectorService, lectorVoices: LECTOR_VOICES`.
    8. Dispose: `if (this.lectorService) this.lectorService.dispose();` w pozycji koło audioController.dispose().
    Zmodyfikuj `src/ui/ElementInfoPanel.js`:
    - Constructor opt: `lectorService = null, lectorVoices = []`
    - W _render po wypełnieniu sekcji: zbuduj fullText (concat name + function + parameters + safety); jeśli lectorService && lectorService.isAvailable() && s.lectorEnabled → render button.element-info-panel__lector-btn (text 🔊 + pl.ui.lectorListenButton) z click handler `lectorService.speak(fullText, s.lectorVoiceId).catch(()=>{})`. Jeśli !lectorService.isAvailable() → button visible disabled + title=pl.ui.lectorMissingKeyTooltip.
    - Slot div przed-stworzony w Plan 11-03 Task 2; teraz wypełniamy.
    - Subscriber dorzuć: s.lectorEnabled + s.lectorVoiceId (każdy → _render).
    Zmodyfikuj `src/ui/StatusPanel.js`:
    - Constructor opt: `lectorService = null, lectorVoices = []`
    - W _build dorzuć w hamburger controls: button.status-panel__lector-toggle + select.status-panel__lector-voice (options z lectorVoices).
    - Click toggle → store.getState().setLectorEnabled(!current); Change select → setLectorVoiceId(value).
    - _render: bind aria-pressed + textContent + disabled state per isAvailable.
    - Subscriber: s.lectorEnabled + s.lectorVoiceId.
    Zmodyfikuj `src/i18n/pl.js`:
    - pl.ui: lectorListenButton ('Odsłuchaj'), lectorMissingKeyTooltip ('Lektor wymaga konfiguracji klucza ElevenLabs (.env)'), lectorToggleOn ('Lektor: ON'), lectorToggleOff ('Lektor: OFF'), lectorVoicePickerLabel ('Głos lektora').
    Utwórz `.env.example` (treść z behavior).
    Sprawdź/utwórz `.gitignore` — upewnij się że `.env` jest listowany.
    Sprawdź/utwórz `vite.config.js` — dorzuć skomentowany proxy block (treść z behavior).
    Zmodyfikuj `tests/lectorService.test.js`, `tests/ElementInfoPanel.test.js`, `tests/StatusPanel.test.js` — dorzuć po 2 asercje każdy.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Wszystkie testy zielone: 777 baseline + Plan 11-01..04 + Plan 11-05 nowe (8 lectorService + 2 store + 2 ElementInfoPanel + 2 StatusPanel)
    - `.env.example` istnieje z VITE_ELEVENLABS_API_KEY= line
    - `.gitignore` zawiera `.env`
    - `vite.config.js` zawiera skomentowany proxy block (grep "elevenlabs")
    - `grep "LECTOR_ENABLED_KEY\\|LECTOR_VOICE_KEY" src/main.js` ≥ 4 (2 consty + 2 persist subscribers)
    - `grep "lectorService" src/main.js` ≥ 3 (import, instantiate, dispose)
    - `grep "lectorService.speak\\|speak(" src/ui/ElementInfoPanel.js` ≥ 1
  </acceptance_criteria>
  <done>LectorService wired w UI, persist działa, 🔊 button renderowany w panel + StatusPanel toggle/picker, fallback proxy gotowy do uncomment, pełen suite zielony.</done>
</task>

</tasks>

<verification>
- 14+ nowych testów zielone (8 lector + 6 integracji).
- Pełen suite preserved.
- Manualna weryfikacja (jeśli klucz ElevenLabs dostępny): npm run dev → włącz lektor w StatusPanel → klik na element w mode='nauka' → klik 🔊 → audio gra (jeśli CORS nie blokuje).
- Jeśli CORS blokuje: uncomment vite.config.js proxy block, zmienić URL w LectorService na `/elevenlabs/v1/...`. Decyzja CORS deferred do Plan 11-06 manual smoke gate.
</verification>

<success_criteria>
- FUNC-11-09: 🔊 button + fetch + play działa (smoke gate w Plan 11-06).
- FUNC-11-10: graceful fallback gdy brak klucza (disabled+tooltip).
- FUNC-11-11: cache LRU 20-wpis + revokeObjectURL.
- FUNC-11-12: toggle + voice picker + persist localStorage.
- Security warning udokumentowany w LectorService.js + .env.example + komentarz przy VITE_ inline.
- Boundary clean.
</success_criteria>

<output>
Utwórz `.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-05-SUMMARY.md` z: cache stats (max 20), security warning, voice list status (Damian confirmed + TODO 2 more), CORS smoke test plan dla 11-06, persist keys, instrukcje dla user setup (.env).
</output>
