---
phase: 11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/data/elementInfo.js
  - src/ui/ElementInfoPanel.js
  - src/state/trainingStore.js
  - src/RaycastController.js
  - src/main.js
  - src/i18n/pl.js
  - tests/elementInfo.test.js
  - tests/ElementInfoPanel.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [FUNC-11-03, FUNC-11-07, FUNC-11-08]

must_haves:
  truths:
    - "elementInfo.js eksportuje 15 wpisów (Object.keys.length === 15) zgodnych z getInteractables().keys()"
    - "Każdy wpis ma 4 sekcje: name, function, parameters, sopSteps, safety (BHP) — min 4 wymagane przez FUNC-11-07; piąta (sopSteps) bonusowa"
    - "Klik na element w mode==='nauka' otwiera ElementInfoPanel z 4 sekcjami"
    - "Klik na element w mode==='free' → krótki tooltip/modal z 1-zdaniowym opisem (pl.parts[id].description), NIE pełen InfoPanel"
    - "Klik w mode==='egzamin' → no-op (jak dziś — RaycastController dispatchuje attemptStep)"
  artifacts:
    - path: "src/data/elementInfo.js"
      provides: "Frozen object z 15 wpisami {name, function, parameters, sopSteps, safety} po polsku"
      contains: "Object.freeze"
    - path: "src/ui/ElementInfoPanel.js"
      provides: "Modal panel klasa (wzorzec HelpModal) renderujący 4 sekcje + miejsce na 🔊 button (Plan 11-05 wstawi)"
    - path: "src/state/trainingStore.js"
      provides: "Akcja openElementInfo(meshId) + pole _elementInfoMeshId + activeModal='element-info'"
  key_links:
    - from: "src/RaycastController.js _handlePointerUp"
      to: "store.openElementInfo / store.openShortDescription"
      via: "branch po mode read"
      pattern: "openElementInfo\\|openShortDescription"
    - from: "src/ui/ElementInfoPanel.js"
      to: "src/data/elementInfo.js"
      via: "import elementInfo + read by meshId"
      pattern: "elementInfo\\["
---

<objective>
Wprowadzić edukacyjny rozszerzony panel informacyjny dla 15 interactables w trybie 'nauka' (FUNC-11-07) oraz krótki opis 1-zdaniowy w trybie 'free' (FUNC-11-03). Dane edukacyjne (nazwa/funkcja/parametry/BHP/SOP-link) w pure-data module `src/data/elementInfo.js` (FUNC-11-08). Panel jako modal-card analogiczny do HelpModal (XSS-safe, statyczny szkielet + textContent, subscriber na `activeModal==='element-info'`).

Wave 2 depends on Plan 11-01: konsumuje `store.mode` dla brancha mode==='nauka' vs 'free' w RaycastController.
Output: 15-entry dataset + nowy modal + click branch w RaycastController + 2 nowe testy + boundary entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md
@CLAUDE.md
@src/PressModel.js
@src/RaycastController.js
@src/ui/HelpModal.js
@src/ui/ConfirmModal.js
@src/state/trainingStore.js
@src/i18n/pl.js

<interfaces>
PressModel.getInteractables() → Map<meshId, THREE.Mesh> (linia 1503).
15 interactables IDs (od Phase 2-05 baseline, Phase 7-03 audit):
- kolo-zamachowe, hamulec, wziernik-smarowania, oslona-tylna, kurtyna-lewa, kurtyna-prawa
- tabliczka-znamionowa, panel-oburezny, przycisk-start-lewy, przycisk-start-prawy, lampka-gotowosci, estop
- oslona-przednia, wylacznik-glowny, dzwignia-sprzegla
(Verify by reading PressModel.js + grep _registerInteractable)

pl.parts[id] obecny (i18n/pl.js linie 178+) z `description` (1 zdanie) — reuse w trybie 'free'.

HelpModal pattern (wzorzec do skopiowania):
- constructor({ store, rootElementId='modal-container' })
- _build() — statyczny <dialog> z szkielet innerHTML literałami + textContent dla wszystkich data fields
- _wireSubscribers — subscribe(s.activeModal === 'help')
- _render — pokazuje/ukrywa dialog
- dispose — odpina event listeners + subscribers

trainingStore.openConfirmModal/closeModal pattern (linie 157-164):
- openConfirmModal: (payload) => set({ activeModal: 'confirm-scenario-switch', _confirmPayload: payload })
- closeModal: () => set({ activeModal: null, _confirmPayload: null })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/data/elementInfo.js — 15 wpisów × 5 sekcji + test coverage</name>
  <read_first>
    - src/PressModel.js — grep `_registerInteractable\\|userData.id =` żeby potwierdzić 15 dokładnych meshId
    - src/i18n/pl.js linie 174-240 (pl.parts[15] — istniejące label+description; bazowa nazwa dla `name` field w elementInfo)
    - src/training/scenarios/uruchomienie.js (opcjonalnie inne scenariusze) — odczytaj step.targetMeshId per krok dla sekcji sopSteps (powiązanie mesh→SOP)
  </read_first>
  <files>src/data/elementInfo.js, tests/elementInfo.test.js, tests/boundaries.test.js</files>
  <behavior>
    - tests/elementInfo.test.js:
      1. Object.keys(elementInfo).length === 15
      2. Każdy klucz ∈ EXPECTED_IDS (hardcoded set 15 IDs — assert .has)
      3. Każdy wpis ma pola: name, function, parameters, sopSteps, safety (5 pól; FUNC-11-07 wymaga min 4 — name/function/parameters/safety, sopSteps = bonus dla zlinkowania)
      4. Każde pole jest typu string z length > 10 (sanity: brak placeholder "TODO")
      5. sopSteps może być stringiem ("scenarioId/stepId, ...") — assert > 0 jeśli nie 'brak'
      6. `Object.isFrozen(elementInfo)` === true
    - boundary entry: src/data/elementInfo.js MUSI NIE importować three/gsap/state/training (pure data; allowed: nothing or pl.js)
  </behavior>
  <action>
    Utwórz `src/data/elementInfo.js`:
    1. Header komentarz: "Phase 11 FUNC-11-08 — edukacyjny rozszerzony content dla 15 interactables. Pure data module (analog src/i18n/pl.js, src/training/scoringWeights.js). Polski per CLAUDE.md."
    2. Eksport: `export const elementInfo = Object.freeze({ ... })` z 15 wpisami. Każdy wpis (na podstawie pl.parts label+description + research wzorca dydaktycznego):
       - name (np. 'Koło zamachowe')
       - function (≥ 60 znaków, dydaktyczne wyjaśnienie do czego służy)
       - parameters (parametry techniczne: średnica/masa/moment/napięcie/itp. — wartości realistyczne dla PM-300, mogą być orientacyjne ['~600 mm', 'klasy XYZ'])
       - sopSteps (string z linkowaniem do scenarioId/stepId które używają tego mesh, np. 'uruchomienie/sprawdz-kolo-zamachowe, cykl-pracy/zwolnij-sprzeglo'; jeśli mesh nie jest target żadnego kroku → 'brak — element dekoracyjny/informacyjny')
       - safety (BHP warning — typowe ryzyko + procedura ochrony)
    3. Wszystkie 15 IDs muszą zostać pokryte. Lista IDs: kolo-zamachowe, hamulec, wziernik-smarowania, oslona-tylna, kurtyna-lewa, kurtyna-prawa, tabliczka-znamionowa, panel-oburezny, przycisk-start-lewy, przycisk-start-prawy, lampka-gotowosci, estop, oslona-przednia, wylacznik-glowny, dzwignia-sprzegla.
    Utwórz `tests/elementInfo.test.js` z 6 asercjami z behavior. EXPECTED_IDS jako Set hardcoded.
    Zmodyfikuj `tests/boundaries.test.js` — dodaj entry:
       `{ file: 'src/data/elementInfo.js', mustNotImport: ['three', 'gsap', '../state/', '../training/', './state/', './training/', '../RaycastController', '../ui/'] }`
       Jeśli FORBIDDEN_PAIRS array ma tail, dorzuć przed `];`.
  </action>
  <verify>
    <automated>npx vitest run tests/elementInfo.test.js tests/boundaries.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/elementInfo.test.js: 6/6 PASS
    - tests/boundaries.test.js: PASS (entry dla src/data/elementInfo.js włączony)
    - `grep -c "name:\\|function:\\|parameters:\\|sopSteps:\\|safety:" src/data/elementInfo.js` ≥ 75 (15 wpisów × 5 pól)
    - Object.freeze widoczny w grep
    - `Object.keys(elementInfo).length === 15` w teście
  </acceptance_criteria>
  <done>elementInfo.js z 15 wpisami × 5 sekcji, frozen, boundary-clean, 6 testów zielonych.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ElementInfoPanel klasa (modal wzorzec HelpModal) + store actions openElementInfo/closeModal extension</name>
  <read_first>
    - src/ui/HelpModal.js (cały plik — wzorzec _build/_wireSubscribers/_render/dispose)
    - src/ui/ConfirmModal.js (cały plik — wzorzec z dynamicznym body (_confirmPayload), bo InfoPanel też potrzebuje dynamicznego content per meshId)
    - src/state/trainingStore.js (linie 157-164 openConfirmModal/closeModal pattern)
    - src/data/elementInfo.js (utworzone w Task 1)
    - src/i18n/pl.js (pattern pl.modals.confirmScenarioSwitch / pl.modals.help — gdzie dodać pl.modals.elementInfo)
  </read_first>
  <files>src/ui/ElementInfoPanel.js, src/state/trainingStore.js, src/i18n/pl.js, tests/ElementInfoPanel.test.js, tests/boundaries.test.js, tests/trainingStore.test.js</files>
  <behavior>
    - store.openElementInfo(meshId) → set({ activeModal: 'element-info', _elementInfoMeshId: meshId })
    - store.closeModal() już istnieje → rozszerzyć by clearował też _elementInfoMeshId (analog _confirmPayload)
    - ElementInfoPanel modal:
      - Pokazany gdy activeModal === 'element-info' AND _elementInfoMeshId !== null
      - Renderuje 4+1 sekcje (h3 nagłówek + p/ul content): "Funkcja", "Parametry techniczne", "Powiązane kroki SOP", "BHP" (ostrzeżenia). Tytuł = elementInfo[meshId].name.
      - Miejsce na 🔊 button: pusty div.element-info-panel__lector-slot (Plan 11-05 wstawi). Komentarz: "Phase 11-05 LectorService button inject point".
      - Close: X, klik overlay, Esc handler (analog HelpModal).
    - tests/ElementInfoPanel.test.js:
      1. activeModal='element-info', _elementInfoMeshId='kolo-zamachowe' → dialog widoczny (open attribute) + tytuł 'Koło zamachowe'
      2. 4 sekcje obecne: textContent zawiera fragmenty z elementInfo['kolo-zamachowe'].function / .parameters / .sopSteps / .safety
      3. Brak meshId LUB activeModal !== 'element-info' → dialog ukryty (no open attribute)
      4. Klik X button → store.closeModal called (spy) + activeModal=null
      5. _elementInfoMeshId='nieznany-mesh' → graceful (dialog renderuje tytuł 'Nieznany element' bez crash; w teście assert that brak throw)
      6. lector-slot div obecny (querySelector '.element-info-panel__lector-slot' !== null) — placeholder dla Plan 11-05
    - tests/trainingStore.test.js dodać 2 asercje: openElementInfo ustawia activeModal+_elementInfoMeshId; closeModal czyści _elementInfoMeshId
  </behavior>
  <action>
    Zmodyfikuj `src/state/trainingStore.js`:
    1. Dodaj pole `_elementInfoMeshId: null` w initial state (po _confirmPayload, linia ~66).
    2. Dodaj akcję `openElementInfo: (meshId) => set({ activeModal: 'element-info', _elementInfoMeshId: meshId })`.
    3. Zmodyfikuj istniejące closeModal: `closeModal: () => set({ activeModal: null, _confirmPayload: null, _elementInfoMeshId: null })`.
    Zmodyfikuj `src/i18n/pl.js`:
    - W `pl.modals` dodaj: `elementInfo: { titleFallback: 'Nieznany element', sectionFunction: 'Funkcja', sectionParameters: 'Parametry techniczne', sectionSopSteps: 'Powiązane kroki SOP', sectionSafety: 'BHP — ostrzeżenia' }`
    - W pl.modals albo pl.ui dodaj label "Odsłuchaj" — placeholder dla Plan 11-05: `lectorListenButton: 'Odsłuchaj'`.
    Utwórz `src/ui/ElementInfoPanel.js`:
    - Header komentarz: Phase 11 FUNC-11-07. Boundary clean: tylko imports './pl' + './data/elementInfo.js' + DOM globals. NIE THREE/gsap/training/highlight.
    - Class shape skopiowany z HelpModal: constructor({store, rootElementId='modal-container'}), _build() statyczny innerHTML szkielet (XSS-safe) ze slotami h2 tytułu + 4 sekcje (h3 + p) + .element-info-panel__lector-slot + close button. _wireSubscribers — subscribe s.activeModal + s._elementInfoMeshId. _render: jeśli activeModal==='element-info' && meshId → showDialog + wypełnij sekcje z elementInfo[meshId] (textContent XSS-safe); inaczej hide. dispose: removeEventListener + unsub.
    - Z elementInfo[meshId] === undefined: tytuł = pl.modals.elementInfo.titleFallback; sekcje puste lub '—'.
    Utwórz `tests/ElementInfoPanel.test.js` z 6 asercjami z behavior. Pattern jsdom: beforeEach setup #modal-container w body.
    Zmodyfikuj `tests/boundaries.test.js` — dodaj entry:
       `{ file: 'src/ui/ElementInfoPanel.js', mustNotImport: ['three', 'gsap', '../state/', '../training/', '../highlight/', './state/', './training/', '../RaycastController'] }`
    Zmodyfikuj `tests/trainingStore.test.js` — dodaj 2 asercje (openElementInfo/closeModal extension).
  </action>
  <verify>
    <automated>npx vitest run tests/ElementInfoPanel.test.js tests/trainingStore.test.js tests/boundaries.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/ElementInfoPanel.test.js: 6/6 PASS
    - tests/trainingStore.test.js: nowe 2 asercje PASS (zero regresji)
    - tests/boundaries.test.js: PASS z nową entry
    - `grep "_elementInfoMeshId" src/state/trainingStore.js` ≥ 3 (init + open + close)
    - `grep "element-info-panel__lector-slot" src/ui/ElementInfoPanel.js` ≥ 1 (slot dla Plan 11-05)
  </acceptance_criteria>
  <done>ElementInfoPanel renderuje 4 sekcje + lector slot, store actions działają, 8 nowych testów zielone, boundary updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RaycastController mode branch + Application wiring ElementInfoPanel</name>
  <read_first>
    - src/RaycastController.js linie 158-208 (_handlePointerUp + bimanual branch + freeRoam early-return na linii 173)
    - src/main.js linie 240-275 (helpModal/confirmModal wiring pattern + dispose order linie 354-389)
    - src/state/trainingStore.js (po Task 2 — mode, openElementInfo, attemptStep)
    - src/i18n/pl.js (pattern modal fallback strings — short description w mode='free' używa istniejącego pl.parts[id].description)
  </read_first>
  <files>src/RaycastController.js, src/main.js</files>
  <behavior>
    - W `_handlePointerUp` w RaycastController: usuń `freeRoam` early-return (linia 173), zastąp branch na podstawie store.mode:
      - mode==='free' → emit informational click przez callback `_onShortDescriptionClick(meshId, mesh)` (analog _onManipulationClick); attemptStep NIE wołany; bimanual NIE wołany.
      - mode==='nauka' → BEFORE existing attemptStep branch, jeśli mesh.userData.kind != 'manipulation' (czyli kind === 'click' visual targets) LUB ZAWSZE dla każdego klika w mode='nauka': wywołaj `store.openElementInfo(meshId)` + ZWRACAJ wcześnie (NIE wołaj attemptStep — info panel jest sygnałem edukacji, nie advance SOP).
        - UWAGA per discuss-decision: w mode='nauka' SOP jest aktywny — ale klik powinien otworzyć info panel ZAMIAST advance SOP, bo to tryb dydaktyczny. **Per FUNC-11-07 description: klik = otwiera panel info. SOP advance w nauce odbywa się przez przeciąganie do następnego mesh? Re-read.**
        - Re-read FUNC-11-07: "Tryb nauki: klik na element otwiera rozszerzony panel informacyjny". To znaczy klik w mode='nauka' = ZAWSZE info panel; SOP advance w nauce: wciąż przez attemptStep ale **TYLKO gdy panel zamknięty**? Zakładamy: klik otwiera panel. attemptStep odpalany tylko gdy mode==='egzamin'. To upraszcza: 'egzamin' = czysty SOP, 'nauka' = edukacyjny info, 'free' = ciekawy opis. Ta interpretacja zgadza się z research lines 162-171 ("`if mode==='free' && hit: _onLearnClick(meshId)`" w architectural diagram).
      - mode==='egzamin' → istniejący flow attemptStep / bimanual (current behavior).
    - Application._main.js: instantiate ElementInfoPanel po HelpModal/ConfirmModal. Wpięcie po-hoc do RaycastController:
      - this.raycastController._onShortDescriptionClick = (id, mesh) => { /* mode='free' callback */ this.elementInfoPanel.showShortDescription(id); } — LUB prościej: w mode='free' też wywołać openElementInfo, ale ElementInfoPanel decyduje (na podstawie mode) czy render full vs short. **Decyzja: prościej.** Plan 11-03 final: oba mode='free' i 'nauka' wywołują `store.openElementInfo(meshId)`. ElementInfoPanel sprawdza s.mode w _render: 'free' → renderuje tylko sekcję 'short description' (z pl.parts[meshId].description), 'nauka' → 4 pełne sekcje.
      - Dla minimal-diff: ElementInfoPanel w Task 2 przygotuj kondycyjnie — sprawdź czy w Task 2 już dorzucono check s.mode. Jeśli nie — krótkie patch tutaj w Task 3 (modify ElementInfoPanel _render: read s.mode; if 'free' show single .description from pl.parts[id]; if 'nauka' show 4 sections).
    - Integration tests:
      - tests/RaycastController.test.js (modify): dodaj 3 asercje:
        1. mode='egzamin' → klik na mesh → store.attemptStep called (spy), openElementInfo NOT called
        2. mode='nauka' → klik na mesh → store.openElementInfo(meshId) called, attemptStep NOT called
        3. mode='free' → klik na mesh → store.openElementInfo(meshId) called, attemptStep NOT called
  </behavior>
  <action>
    Zmodyfikuj `src/RaycastController.js` `_handlePointerUp`:
    1. Usuń linie 171-173 (`if (this._store.getState().freeRoam) return;` + komentarz D-Phase5-05).
    2. Po raycast hit (linia 180-183, mesh+meshId extracted), dorzuć branch PRZED bimanual check:
       ```
       const storeState = this._store.getState();
       const mode = storeState.mode;
       if (mode === 'free' || mode === 'nauka') {
         // Phase 11 FUNC-11-03/07: klik otwiera ElementInfoPanel zamiast advance SOP.
         storeState.openElementInfo?.(meshId);
         return;
       }
       // mode === 'egzamin' lub mode undefined (backward compat): istniejący flow.
       ```
    3. (Nie usuwaj _onManipulationClick branch dla mesh z userData.poses linia 188-190 — fizyczna animacja zasłon/dźwigni jest ortogonalna do mode i powinna dalej działać w każdym trybie. Per Plan 10 inwarianty.)
    Zmodyfikuj `src/main.js`:
    1. Import: `import { ElementInfoPanel } from './ui/ElementInfoPanel.js';`
    2. Instantiate po linii ConfirmModal (linia 248): `this.elementInfoPanel = new ElementInfoPanel({ store: this.store });`
    3. Dispose: dorzuć `if (this.elementInfoPanel) this.elementInfoPanel.dispose();` w analogicznej pozycji co confirmModal/helpModal (linia 372-374).
    Zmodyfikuj `src/ui/ElementInfoPanel.js` (mode-conditional render — extension Task 2):
    - W _render: const mode = s.mode; jeśli mode==='free' → wyświetl tylko name + jedną sekcję 'Opis' = pl.parts[meshId]?.description ?? '—'. Jeśli mode==='nauka' → wszystkie 4 sekcje. Subscriber dorzucić: subscribe(s.mode).
    Zmodyfikuj `tests/RaycastController.test.js` — dodaj describe 'Phase 11 mode branch' z 3 asercjami z behavior; spy: vi.spyOn(store, 'getState') zwracający fake state z mode + spies openElementInfo/attemptStep.
    Zmodyfikuj `tests/ElementInfoPanel.test.js` — dodaj 2 asercje: mode='free' renderuje 1 sekcję (Opis), mode='nauka' renderuje 4 sekcje.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/RaycastController.test.js: 3 nowe + baseline PASS
    - tests/ElementInfoPanel.test.js: 8/8 PASS (6 Task 2 + 2 nowe mode-conditional)
    - tests/elementInfo.test.js: 6/6 PASS
    - Pełen suite: 777 baseline + Plan 11-01 + 11-02 + Plan 11-03 testy zielone, zero regresji
    - `grep "openElementInfo" src/RaycastController.js` ≥ 1
    - `grep "elementInfoPanel" src/main.js` ≥ 3 (import, instantiate, dispose)
    - `grep "freeRoam" src/RaycastController.js` === 0 dla early-return (usunięty)
  </acceptance_criteria>
  <done>RaycastController branchuje na mode; ElementInfoPanel renderuje conditional content; Application wired; pełen suite zielony.</done>
</task>

</tasks>

<verification>
- Manualny smoke: w mode='free' klik na koło zamachowe → 1-zdaniowy opis modal. W mode='nauka' klik → 4 sekcje.
- 15 wpisów elementInfo pokryte (test enforced).
- Pełen suite 777 + 11-01 + 11-02 + 11-03 zielone.
</verification>

<success_criteria>
- FUNC-11-03: klik w mode='free' renderuje krótki opis (z pl.parts[id].description).
- FUNC-11-07: klik w mode='nauka' otwiera ElementInfoPanel z 4 sekcjami.
- FUNC-11-08: src/data/elementInfo.js z 15/15 wpisami, frozen, boundary-clean.
- Phase 10 inwariant: _onManipulationClick (osłona przednia + dźwignia sprzęgła) zachowany dla wszystkich trybów.
</success_criteria>

<output>
Utwórz `.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-03-SUMMARY.md` z: 15 IDs list, hierarchia sekcji modal, wzorzec mode-conditional render, hint dla Plan 11-05 (gdzie wstawi 🔊 button — `.element-info-panel__lector-slot`).
</output>
