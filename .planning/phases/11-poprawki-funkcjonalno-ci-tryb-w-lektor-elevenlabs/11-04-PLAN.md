---
phase: 11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/ui/ExamPromptModal.js
  - src/state/trainingStore.js
  - src/main.js
  - src/i18n/pl.js
  - tests/ExamPromptModal.test.js
  - tests/examPromptFlow.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [FUNC-11-05, FUNC-11-06]

must_haves:
  truths:
    - "Gdy session.finishedAt zmienia z null → ts AND mode==='nauka' → activeModal='exam-prompt' (FUNC-11-05)"
    - "Modal 'Tak, przejdź do egzaminu' → setMode('egzamin') + reset session (startScenario uruchomienie z czystym scoringiem)"
    - "Modal 'Nie, wróć do swobodnego' → endExam() + closeModal (FUNC-11-06)"
    - "Po session.finishedAt w trybie egzamin → endExam() automatycznie, modal NIE pokazuje się ponownie"
    - "Modal NIE pokazuje się gdy mode==='free' lub 'egzamin' (tylko po nauce, per FUNC-11-05)"
  artifacts:
    - path: "src/ui/ExamPromptModal.js"
      provides: "Modal klasa (wzorzec ConfirmModal) — 2 buttony Tak/Nie + tytuł 'Przejść do egzaminu?'"
    - path: "src/state/trainingStore.js"
      provides: "Subscriber wewnętrzny: session.finishedAt + mode==='nauka' → openExamPrompt() action"
    - path: "src/main.js"
      provides: "Application instantiation ExamPromptModal + dispose"
  key_links:
    - from: "trainingStore subscriber"
      to: "ExamPromptModal render"
      via: "activeModal='exam-prompt'"
      pattern: "exam-prompt"
    - from: "ExamPromptModal Tak/Nie buttons"
      to: "store.setMode('egzamin') / endExam()"
      via: "click handler"
      pattern: "setMode\\|endExam"
---

<objective>
Implementacja flow exam prompt: po ukończeniu trybu 'nauka' (SOP done — session.finishedAt !== null) modal "Czy przejść do egzaminu?" (FUNC-11-05) z dwoma wyborami. Po ukończeniu sesji egzaminacyjnej (lub abort) automatyczny powrót do 'free' i unlock togglera (FUNC-11-06). Wzorzec modal skopiowany z ConfirmModal (XSS-safe, dynamiczny body via store payload, dialog `open` attribute).

Wave 2 depends on Plan 11-01 — konsumuje `store.mode` i `setMode`/`endExam` actions.
Parallel z Plan 11-03 — modyfikujemy różne pliki (Plan 11-03: ElementInfoPanel + RaycastController + elementInfo; Plan 11-04: ExamPromptModal + trainingStore exam subscriber + main.js wiring). Konflikt tylko w trainingStore.js (oba dodają akcje/pola) i pl.js (oba dodają stringi) — pliki nakładające się prowadzą do późniejszej fali, ALE: konflikty trywialne (różne sekcje pl.modals.*, różne pola w initial state). DECYZJA: Plan 11-04 idzie w wave 2 razem z 11-03; executor musi merge'ować rozdzielnie. UWAGA: jeśli executor uzna ryzyko za zbyt wysokie, może wykonać sekwencyjnie 11-03 → 11-04.
Output: ExamPromptModal + store subscriber + main.js wiring + 2 testy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md
@CLAUDE.md
@src/ui/ConfirmModal.js
@src/state/trainingStore.js
@src/main.js
@src/i18n/pl.js

<interfaces>
trainingStore istniejący subscriber (linie 367-377) auto-openuje SessionOverlay gdy finishedAt !== null:
- store.subscribe((s) => s.session.finishedAt, (finishedAt, prev) => { if (prev === null && finishedAt !== null) set({ overlayOpen: true }); })

Plan 11-04 dodaje DRUGI subscriber równolegle — sprawdza mode==='nauka' i otwiera 'exam-prompt' modal. NIE konflikt z overlayOpen — overlay i prompt mogą wystąpić jednocześnie (overlay pokazuje raport, prompt pyta o egzamin — to dwa różne kanały informacyjne; możliwe że ExamPromptModal pojawia się PO close SessionOverlay; uproszczenie MVP: oba widoczne, użytkownik najpierw zamyka overlay potem widzi prompt).

ConfirmModal (wzorzec do skopiowania):
- payload schema: { current, next, nextScenarioId }
- _build() — statyczny innerHTML szkielet
- _render() podpina payload przez textContent
- Click Tak → s.startScenario(scenarios[payload.nextScenarioId]) + closeModal
- Click Nie → closeModal

trainingStore.attemptStep flow ustawia session.finishedAt automatycznie gdy wszystkie kroki done (linie 380-391).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: trainingStore exam subscriber + openExamPrompt action</name>
  <read_first>
    - src/state/trainingStore.js linie 367-391 (istniejące subscribery na session.finishedAt + auto-open overlayOpen)
    - src/state/trainingStore.js linie 157-164 (openConfirmModal/closeModal pattern)
    - tests/trainingStore.test.js (wzorzec describe action tests)
  </read_first>
  <files>src/state/trainingStore.js, tests/trainingStore.test.js, src/i18n/pl.js</files>
  <behavior>
    - Nowe pole `_examPromptShown: false` w initial state (idempotent flag — nie pokazuj prompt drugi raz dla tej samej sesji).
    - Nowy subscriber w store init (po existing finishedAt subscriber):
      `store.subscribe((s) => s.session.finishedAt, (finishedAt, prev) => { const s = store.getState(); if (prev === null && finishedAt !== null && s.mode === 'nauka' && !s._examPromptShown) { store.setState({ activeModal: 'exam-prompt', _examPromptShown: true }); } if (prev === null && finishedAt !== null && s.mode === 'egzamin') { s.endExam(); /* FUNC-11-06: auto-return to free */ } })`
    - Reset `_examPromptShown` w startScenario (świeży scenariusz → świeży flag).
    - tests/trainingStore.test.js (lub nowy tests/examPromptFlow.test.js) — 5 asercji:
      1. mode='nauka', finishedAt=null → finishedAt=Date.now() → activeModal='exam-prompt', _examPromptShown=true
      2. mode='free' → finishedAt set → activeModal NIE jest 'exam-prompt' (no prompt)
      3. mode='egzamin' → finishedAt set → endExam called (mode='free' po), activeModal NIE jest 'exam-prompt'
      4. mode='nauka', _examPromptShown=true → drugi finishedAt set → no-op (nie pokazuj prompt drugi raz)
      5. startScenario reset _examPromptShown=false
  </behavior>
  <action>
    Zmodyfikuj `src/state/trainingStore.js`:
    1. Dodaj `_examPromptShown: false` w initial state (po _confirmPayload i — jeśli Plan 11-03 już merged — _elementInfoMeshId).
    2. W startScenario action: dorzuć `_examPromptShown: false` do set() (linia 100 — analog `bimanualHintState: 'idle'`).
    3. PO istniejącym subscriber `s.session.finishedAt → overlayOpen` (linia 369-376), dodaj DRUGI subscriber:
       ```
       store.subscribe(
         (s) => s.session.finishedAt,
         (finishedAt, prev) => {
           if (prev !== null || finishedAt === null) return;
           const cur = store.getState();
           // FUNC-11-05: po SOP done w trybie nauka → exam prompt
           if (cur.mode === 'nauka' && !cur._examPromptShown) {
             store.setState({ activeModal: 'exam-prompt', _examPromptShown: true });
             return;
           }
           // FUNC-11-06: po egzaminie auto-powrót do free
           if (cur.mode === 'egzamin') {
             cur.endExam();
           }
         },
       );
       ```
    4. Rozszerz closeModal by clearował też _examPromptShown? **NIE** — _examPromptShown ma być "shown for this session", reset tylko przez startScenario. Inaczej można retriggerować przez ręczne closeModal+manipulację state.
    Zmodyfikuj `src/i18n/pl.js`:
    - W pl.modals dodaj: `examPrompt: { title: 'Przejść do egzaminu?', body: 'Ukończyłeś procedurę w trybie Nauka. Czy chcesz teraz wykonać tę samą procedurę w trybie Egzamin (bez podpowiedzi i z oceną)?', confirmYes: 'Tak, przejdź do egzaminu', confirmNo: 'Nie, wróć do trybu swobodnego' }`
    Utwórz `tests/examPromptFlow.test.js` z 5 asercjami z behavior. Pattern: createTrainingStore + mock + ustawienie scenariusza + manualne set finishedAt.
  </action>
  <verify>
    <automated>npx vitest run tests/examPromptFlow.test.js tests/trainingStore.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/examPromptFlow.test.js: 5/5 PASS
    - tests/trainingStore.test.js: zero regresji
    - `grep "_examPromptShown" src/state/trainingStore.js` ≥ 3 (initial, startScenario reset, subscriber)
    - `grep "exam-prompt" src/state/trainingStore.js` ≥ 1
    - `grep "examPrompt" src/i18n/pl.js` ≥ 1
  </acceptance_criteria>
  <done>Store auto-trigger exam-prompt po nauce, auto-endExam po egzaminie, idempotent flag, 5 testów zielonych.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExamPromptModal klasa + Application wiring</name>
  <read_first>
    - src/ui/ConfirmModal.js (cały plik — wzorzec _build z 2 buttonami Tak/Nie, click handlers, dispose)
    - src/main.js linie 240-275 (helpModal/confirmModal wiring + ElementInfoPanel z Plan 11-03 jeśli merged) + dispose linie 354-389
    - src/i18n/pl.js (po Task 1 — pl.modals.examPrompt strings)
    - src/state/trainingStore.js (po Task 1 — activeModal='exam-prompt', setMode, endExam, startScenario)
  </read_first>
  <files>src/ui/ExamPromptModal.js, src/main.js, tests/ExamPromptModal.test.js, tests/boundaries.test.js</files>
  <behavior>
    - tests/ExamPromptModal.test.js (6 asercji):
      1. activeModal='exam-prompt' → dialog widoczny (open attribute)
      2. activeModal=null → dialog ukryty
      3. Tytuł = pl.modals.examPrompt.title
      4. Klik "Tak" → store.setMode('egzamin') called + startScenario re-start z czystym scoringiem (test sprawdza że scoring.score===100 i finishedAt===null po klik) + closeModal
      5. Klik "Nie" → store.endExam() called + closeModal; mode === 'free' po
      6. Klik overlay → closeModal (analog ConfirmModal)
  </behavior>
  <action>
    Utwórz `src/ui/ExamPromptModal.js`:
    - Header: Phase 11 FUNC-11-05 + 11-06.
    - Boundary: imports './pl' + DOM. NIE THREE/gsap/training/highlight/data.
    - Class shape skopiowany z ConfirmModal:
      - constructor({ store, scenarios, rootElementId='modal-container' }) — `scenarios` DI map (analog ConfirmModal) dla re-start uruchomienie scenariusza po "Tak"
      - _build: statyczny szkielet dialog z modal-card--exam-prompt, tytuł placeholder, body p.exam-prompt__body-text, dwa buttons data-action="yes"/"no"
      - _wireSubscribers: subscribe(s.activeModal) → _render
      - _render: jeśli activeModal==='exam-prompt' show dialog (setAttribute 'open' ''), wypełnij textContent z pl.modals.examPrompt; inaczej hide (removeAttribute 'open')
      - Click "yes": const s = this._store.getState(); s.setMode('egzamin'); s.startScenario(this._scenarios['uruchomienie']); s.closeModal();
      - Click "no": const s = this._store.getState(); s.endExam(); s.closeModal();
      - Click overlay: s.closeModal();
      - dispose: removeEventListener wszystkie + unsubscribers
    Zmodyfikuj `src/main.js`:
    1. Import: `import { ExamPromptModal } from './ui/ExamPromptModal.js';`
    2. Instantiate po ConfirmModal (linia 248 lub po ElementInfoPanel jeśli Plan 11-03 już merged):
       `this.examPromptModal = new ExamPromptModal({ store: this.store, scenarios: allScenarios });`
    3. Dispose w analogicznej pozycji: `if (this.examPromptModal) this.examPromptModal.dispose();` (przed confirmModal lub po — pozycja nie krytyczna, modal-container jest wspólny).
    Zmodyfikuj `tests/boundaries.test.js` — dodaj entry:
       `{ file: 'src/ui/ExamPromptModal.js', mustNotImport: ['three', 'gsap', '../state/', '../training/', '../highlight/', './state/', './training/', '../data/'] }`
    Utwórz `tests/ExamPromptModal.test.js` z 6 asercjami z behavior. Pattern jsdom: setup #modal-container, mock scenarios.uruchomienie minimal {id, steps:[]}.
  </action>
  <verify>
    <automated>npx vitest run tests/ExamPromptModal.test.js tests/boundaries.test.js --reporter=dot && npm test -- --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/ExamPromptModal.test.js: 6/6 PASS
    - tests/boundaries.test.js: PASS z nową entry
    - Pełen suite: zielone (777 baseline + Plan 11-01 + 11-02 + 11-03 + 11-04)
    - `grep "examPromptModal" src/main.js` ≥ 3 (import, instantiate, dispose)
    - `grep "setMode\\|endExam" src/ui/ExamPromptModal.js` ≥ 2
  </acceptance_criteria>
  <done>ExamPromptModal renderuje 2-button dialog, wired w Application, 6 testów zielone, pełen suite green.</done>
</task>

</tasks>

<verification>
- 5 store subscriber + 6 modal = 11 nowych testów zielone.
- Pełen suite preserved.
- Manualny smoke (po wave 2 complete): nauka → ukończ procedurę → modal → Tak → mode='egzamin' + scenariusz restartuje; LUB Nie → mode='free' + toggler unlocked.
</verification>

<success_criteria>
- FUNC-11-05: SOP done w nauce → modal "Przejść do egzaminu?".
- FUNC-11-06: Po egzaminie (pass/fail/abort) → endExam() automatycznie → mode='free' + toggler unlock.
- Idempotent: _examPromptShown zapobiega wielokrotnemu modal po jednej sesji.
- Boundary-clean ExamPromptModal.
</success_criteria>

<output>
Utwórz `.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-04-SUMMARY.md` z opisem flow (nauka done → prompt → Tak/Nie ścieżki, egzamin done → auto endExam), uwagi dla Plan 11-06 manual smoke flow.
</output>
