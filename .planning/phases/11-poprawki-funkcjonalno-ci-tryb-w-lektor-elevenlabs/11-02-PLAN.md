---
phase: 11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/UI.js
  - src/main.js
  - src/i18n/pl.js
  - tests/statusIndicator.test.js
  - tests/application.test.js
autonomous: true
requirements: [FUNC-11-04]

must_haves:
  truths:
    - "Po Start (UI.isRunning=true) i RPM > 0 (_omega > 0.01) → #status-text === 'Aktywny'"
    - "Po Stop (UI.isRunning=false) → #status-text === 'Nieaktywny'"
    - "Po Start ale RPM=0 lub _omega ≤ 0.01 → #status-text === 'Bezczynny (idle)'"
    - "Re-bind nie psuje StatusPanel — to dwa różne widgety (StatusPanel renderuje SOP machineState, #status-text renderuje ω-driven hardware state)"
  artifacts:
    - path: "src/UI.js"
      provides: "updateStatus(isRunning, omega) projekcja na #status-text + #status-dot CSS class"
      contains: "updateStatus"
    - path: "src/main.js"
      provides: "Wywołanie ui.updateStatus(this.ui.isRunning, this._omega) w simulationTick"
    - path: "tests/statusIndicator.test.js"
      provides: "Unit testy 3 stanów + bound thresholds"
  key_links:
    - from: "src/main.js simulationTick"
      to: "src/UI.js updateStatus"
      via: "per-tick call po _omega update, przed updateTelemetry"
      pattern: "updateStatus\\("
---

<objective>
Re-aktywować odłączony w D-Phase4-17 widget `#status-text`/`#status-dot` w panelu sterowania (index.html linie 32-35) jako projekcję ω-driven hardware state (kanał ortogonalny od StatusPanel SOP machineState). Implementuje FUNC-11-04 minimal-diff: zachowuje istniejący DOM layout, restartuje per-tick projekcję 3 stanów: `Aktywny` / `Nieaktywny` / `Bezczynny (idle)`.

Per discuss-decision #2 (Q2: Wybór A — Re-bind istniejący widget): NIE tworzymy nowego widgetu w StatusPanel. Modyfikujemy tylko UI.js + main.js call site.

Purpose: niezależny od Plan 11-01 (wave 1, parallel) — żaden plik z 11-01 nie jest tu modyfikowany (poza ewentualnie pl.js, który ma rozłączne klucze).
Output: 3 nowe testy, jeden bound check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-RESEARCH.md
@CLAUDE.md
@src/UI.js
@src/main.js
@src/i18n/pl.js
@index.html

<interfaces>
src/UI.js current shape:
- constructor: this.elements = { statusDot, statusText, speedSlider, speedValue, btnToggle, valAngle, valDisplacement }
- this.isRunning, this.speed
- getAngularVelocity() — zwraca rad/s
- updateTelemetry(angleRad, displacement) — per-tick call z main.js

index.html linie 32-35:
- <span id="status-dot" class="dot stopped"></span>
- <span id="status-text">Zatrzymana</span>

src/main.js simulationTick:
- linia 318: this._omega += ... (lerp do desiredOmega)
- linia 337: this.ui.updateTelemetry(this.currentAngle, displacement) — punkt wstawki dla updateStatus

D-Phase4-17 disposition note: legacy projekcja USUNIĘTA, StatusPanel SOP single source. Plan 11-02 PRZYWRACA jako orthogonal channel (Hardware state vs SOP state — dwa różne widgety, dwa różne sygnały).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UI.updateStatus(isRunning, omega) — 3 stany projekcja</name>
  <read_first>
    - src/UI.js (cały plik, 62 linie — pattern bindEvents + updateTelemetry)
    - src/i18n/pl.js (sekcja pl.ui — wzór statyczne stringi PL)
    - index.html linie 32-35 (#status-dot/#status-text DOM)
    - tests/UI.test.js — sprawdź czy istnieje (`ls tests/UI.test.js`); jeśli nie, utwórz pełen nowy plik wzorem tests/RaycastController.test.js shape (jsdom env, beforeEach mock DOM)
  </read_first>
  <files>src/UI.js, src/i18n/pl.js, tests/statusIndicator.test.js</files>
  <behavior>
    - tests/statusIndicator.test.js (jsdom env):
      1. beforeEach: utwórz minimal DOM z #status-dot, #status-text, #speed-slider value=30, #speed-value, #btn-toggle, #val-angle, #val-displacement
      2. ui = new UI(); ui.updateStatus(false, 0) → #status-text textContent === 'Nieaktywny', #status-dot className contains 'stopped'
      3. ui.updateStatus(true, 5) (omega > IDLE_THRESHOLD=0.01) → 'Aktywny', className 'running'
      4. ui.updateStatus(true, 0.005) (omega ≤ threshold) → 'Bezczynny (idle)', className 'idle'
      5. Boundary: ui.updateStatus(true, 0.01) → idle (≤ threshold inclusive)
      6. ui.updateStatus(true, 0.0101) → aktywny (just over)
  </behavior>
  <action>
    Zmodyfikuj `src/UI.js`:
    1. Usuń (lub zaktualizuj) komentarz nagłówka D-Phase4-17 — dorzuć notę "Phase 11 FUNC-11-04: re-bind #status-text jako ortogonalny kanał ω-driven hardware state; StatusPanel pozostaje single source dla SOP machineState (D-Phase4-03)."
    2. Dodaj stałą `const IDLE_OMEGA_THRESHOLD = 0.01;` (rad/s) na początku pliku (po imports — UI.js nie ma imports, więc bezpośrednio przed `export class UI`).
    3. Dodaj metodę `updateStatus(isRunning, omega)`:
       - Brak isRunning → text=pl.ui.statusInactive ('Nieaktywny'), dot class 'stopped'
       - isRunning && omega > IDLE_OMEGA_THRESHOLD → text=pl.ui.statusActive ('Aktywny'), dot class 'running'
       - isRunning && omega ≤ IDLE_OMEGA_THRESHOLD → text=pl.ui.statusIdle ('Bezczynny (idle)'), dot class 'idle'
       - Implementacja: this.elements.statusText.textContent = ...; this.elements.statusDot.className = `dot ${state}` (zachowuje istniejącą `dot` base class).
    4. NIE zmieniaj bindEvents / btn-toggle handler (slider RPM tor zachowany per D-Phase4-17 komentarz).
    Zmodyfikuj `src/i18n/pl.js`:
    - W pl.ui dodaj: `statusActive: 'Aktywny', statusInactive: 'Nieaktywny', statusIdle: 'Bezczynny (idle)'` (per CLAUDE.md polski).
    Utwórz `tests/statusIndicator.test.js` z 6 testami z behavior. Pattern z istniejących UI-related tests w Phase 4 (StatusPanel.test.js w zakresie DOM scaffolding).
  </action>
  <verify>
    <automated>npx vitest run tests/statusIndicator.test.js --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/statusIndicator.test.js: 6/6 PASS
    - `grep "updateStatus" src/UI.js` ≥ 1 (definicja metody)
    - `grep "IDLE_OMEGA_THRESHOLD" src/UI.js` ≥ 1
    - `grep "statusActive\\|statusInactive\\|statusIdle" src/i18n/pl.js` ≥ 3
    - boundary thresholds: 0.01 → idle (test 5), 0.0101 → active (test 6)
  </acceptance_criteria>
  <done>UI.updateStatus zwraca 3 stany, testy zielone, polski tekst w pl.ui.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wywołać ui.updateStatus z Application.simulationTick + integration test</name>
  <read_first>
    - src/main.js (simulationTick linie 282-342, zwłaszcza linia 318 _omega update i 337 updateTelemetry call site)
    - tests/application.test.js (wzorzec describe Phase 4 wiring, jak testy zaglądają w simulationTick poprzez gsap.ticker fake albo direct call)
  </read_first>
  <files>src/main.js, tests/application.test.js</files>
  <behavior>
    - simulationTick po update _omega (linia 318) ale przed updateTelemetry (linia 337) ma wywołać `this.ui.updateStatus(this.ui.isRunning, this._omega)`.
    - integration test (application.test.js) describe 'Phase 11 status indicator binding':
      1. Po start (ui.isRunning=false fresh, simulationTick raz) → #status-text == 'Nieaktywny'
      2. Manually set ui.isRunning=true + ui.speed=60 + force simulationTick (deltaTime sufficient żeby _omega > threshold) → status-text == 'Aktywny'
      3. ui.isRunning=true, ui.speed=0 → _omega lerps to 0 → status-text == 'Bezczynny (idle)' (po wystarczająco długim ticku)
      4. Replay path (replayOpen=true) — updateStatus NIE jest wołany (replayOpen early-return na linii 290-299; status-text pozostaje nietknięty od poprzedniego frame)
  </behavior>
  <action>
    Zmodyfikuj `src/main.js`:
    1. W simulationTick, po linii `this._omega += Math.sign(delta) * Math.min(Math.abs(delta), maxStep);` (linia 318), ale PRZED `if (!integrationPaused && this._omega > 0)` (linia 320) — DODAĆ wywołanie:
       `this.ui.updateStatus(this.ui.isRunning, this._omega);`
       Wstaw między linią 318 i 320 z komentarzem "// Phase 11 FUNC-11-04 — ω-driven status indicator (orthogonal channel od StatusPanel SOP machineState)".
    2. NIE modyfikuj replayOpen early-return (linie 290-299) — w replay status indicator zamarza (acceptable: replay to historic data, nie live hardware).
    Zmodyfikuj `tests/application.test.js`:
    - Dodaj describe 'Phase 11 status indicator binding' z 4 asercjami z behavior. Test pattern: bezpośrednie wywołanie `app.simulationTick(16)` (gsap.ticker fake setup już w istniejących testach Phase 4 wiring) + odczyt `document.getElementById('status-text').textContent`. Dla testu replayOpen: `app.store.setState({ replayOpen: true })` przed simulationTick, sprawdź że text nie zmienił się od poprzedniego frame.
  </action>
  <verify>
    <automated>npx vitest run tests/application.test.js -t "Phase 11 status indicator" --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/application.test.js "Phase 11 status indicator binding": 4/4 PASS
    - `grep "ui.updateStatus" src/main.js` ≥ 1 (wywołanie w simulationTick)
    - Pełen suite: `npm test -- --run` → 777 baseline + Plan 11-01 testy (jeśli już wykonany) + Plan 11-02 nowe = wszystkie zielone (zero regresji StatusPanel)
    - Manualny sanity: index.html DOM #status-text element zachowany (NIE usuwamy)
  </acceptance_criteria>
  <done>simulationTick wywołuje updateStatus per tick, integration test pokrywa 3 stany + replay edge case, pełen suite zielony.</done>
</task>

</tasks>

<verification>
- 6 unit + 4 integration nowych testów zielone.
- Pełen suite 777+ baseline preserved.
- Manualna inspekcja po `npm run dev`: Start → Aktywny; Stop → Nieaktywny; Start + RPM=10 (low) → Aktywny gdy ω>0.01; po szybkim Stop dot zmienia kolor.
</verification>

<success_criteria>
- FUNC-11-04: 3 stany (Aktywny/Nieaktywny/Bezczynny idle) projekowane z (isRunning, _omega) na #status-text + #status-dot.
- D-Phase4-03 invariant zachowany: StatusPanel pozostaje single source dla SOP machineState (oddzielny kanał, oddzielny DOM).
- Plan 11-02 nie modyfikuje ŻADNYCH plików zmienianych w Plan 11-01 — pełen parallel wave 1.
</success_criteria>

<output>
Utwórz `.planning/phases/11-poprawki-funkcjonalno-ci-tryb-w-lektor-elevenlabs/11-02-SUMMARY.md` z opisem 3 stanów + bound threshold + integration call site.
</output>
