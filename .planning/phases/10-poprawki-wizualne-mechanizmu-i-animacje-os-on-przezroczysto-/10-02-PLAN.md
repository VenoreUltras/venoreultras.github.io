---
phase: 10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interaction/InteractionAnimator.js
  - src/RaycastController.js
  - tests/InteractionAnimator.test.js
  - tests/RaycastController.click.test.js
  - tests/RaycastController.test.js
  - tests/boundaries.test.js
autonomous: true
requirements:
  - D-10-06
  - D-10-07
  - D-10-08
  - D-10-09
  - CRIT-5
  - CRIT-8
must_haves:
  truths:
    - "Klik (pointerdown+pointerup w odległości <5px) na mesh z userData.poses emituje kanał `_onManipulationClick(meshId, mesh)` w RaycastController"
    - "InteractionAnimator.handleClick(meshId, mesh) tweenuje pivot.rotation (parent dla pivotTarget='parent', self dla 'self') GSAP-em 0.4s power2.inOut do następnej pose"
    - "Drugi klik podczas in-flight tween jest no-op (isAnimating lock per-mesh — CRIT-8)"
    - "Po onComplete: currentPose aktualizuje się, lock zwalnia, timeline usuwany z mapy"
    - "Toggle: kolejny klik flipuje pose (closed→open→closed)"
    - "InteractionAnimator boundary clean — importuje TYLKO THREE+gsap, NIE state/training/DOM/ui"
    - "Drag (pointer delta >=5px) NIE emituje `_onManipulationClick`"
  artifacts:
    - path: "src/interaction/InteractionAnimator.js"
      provides: "Klasa InteractionAnimator z handleClick, getCurrentPose, dispose"
      min_lines: 60
    - path: "src/RaycastController.js"
      provides: "Pole _onManipulationClick callback + emit w _handlePointerUp gdy mesh.userData.poses"
    - path: "tests/InteractionAnimator.test.js"
      provides: "Animator state machine + boundary + dispose tests"
    - path: "tests/RaycastController.click.test.js"
      provides: "Manipulation click channel — emit on tap, no-emit on drag"
  key_links:
    - from: "RaycastController._handlePointerUp"
      to: "InteractionAnimator.handleClick"
      via: "callback this._onManipulationClick?.(meshId, mesh)"
      pattern: "_onManipulationClick"
    - from: "InteractionAnimator.handleClick"
      to: "pivot.rotation (parent group dla guard/lever, self dla switch)"
      via: "gsap.to(pivot.rotation, {...poses[next].rot, duration: 0.4, ease: 'power2.inOut'})"
      pattern: "gsap\\.to\\(pivot\\.rotation"
---

<objective>
Phase 10 Plan 2: klik-driven manipulation animator. Tworzy nową klasę `src/interaction/InteractionAnimator.js` (boundary: THREE+gsap, zero state/training/DOM/ui) — odpowiednik EmissiveController w warstwie `pivot.rotation`. Rozszerza `src/RaycastController.js` o `_onManipulationClick` callback channel emitowany w `_handlePointerUp` przed istniejącym bimanual/attemptStep flow.

Purpose: zaadresować D-10-06/07/08/09 — klik flipuje pose poprzez `userData.poses` + `pivotTarget` (kontrakt Phase 2), per-mesh `isAnimating` lock (CRIT-8), GSAP timeline kill przed retoggle (CRIT-5 analog), zero coupling do trainingStore (D-10-07). Plan jest niezależny od Plan 01 (files_modified: zero overlap z PressModel.js i jego testami) — moż uruchomić równolegle, ale wave=2 w VALIDATION strategy stage; tu deklarujemy `depends_on: []` bo czysto plikowo niezależny.

Output: nowa klasa + extension RaycastControllera + 2 nowe pliki testów + entry w boundaries.test.js + extension istniejącego RaycastController.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md
@CLAUDE.md
@src/RaycastController.js
@src/highlight/EmissiveController.js
@tests/RaycastController.test.js
@tests/EmissiveController.test.js
@tests/boundaries.test.js

<interfaces>
Z `src/RaycastController.js`:
- Konstruktor: `{ renderer, camera, interactables, store, emissive, onHoverChange = null }` — nie zmieniamy sygnatury (nowy callback po-hoc assign, analog `_onHoverChange`)
- `_handlePointerUp(event)` (linie 161-197) — po `dist < CLICK_DRAG_THRESHOLD_PX` (5px), po `freeRoam` check, raycast, `if hits.length === 0 return`, `const mesh = hits[0].object; const meshId = mesh.userData.id;`
- Bimanual branch zaczyna się linia 184-192; attemptStep linia 194-196
- Pole `_onHoverChange` jako wzorzec po-hoc callback (linia 39)
- CLICK_DRAG_THRESHOLD_PX = 5 (linia 16)
- `dispose()` (linia 261-275) — clearTimeout bimanual; nie zmieniamy

Z `src/highlight/EmissiveController.js` (wzorce do replikacji):
- `Map<THREE.Mesh, Timeline>` per-mesh (`_timelines`)
- `_applyTopLayer(mesh)` pattern: pobranie istniejącego timeline, `tl.kill(); this._timelines.delete(mesh)` przed nowym (anti-pattern: 2 równoległe tweens na ten sam target)
- Boundary entry w `tests/boundaries.test.js`: identyczny mustNotImport list jak dla src/highlight/

Z `tests/RaycastController.test.js`:
- Wzorzec fixture: mock renderer z `domElement` (HTMLCanvasElement stub), mock camera (Three.PerspectiveCamera), mock store z `getState() => { attemptStep, freeRoam, activeScenario, currentStepId, setBimanualHintState, attemptBimanualStep }`, mock emissive (real EmissiveController na pustej Mapie lub spy `{setLayer, clearLayer}`)
- Sposób emitowania pointer events przez `el.dispatchEvent(new PointerEvent(...))` lub bezpośrednie wywołanie `controller.handlePointerDown(event)` + `controller._handlePointerUp(event)` przy event jako `{ clientX, clientY }` literal
- Click-vs-drag test: kontrolować deltę przez różne clientX/Y między down i up

Z `tests/boundaries.test.js`:
- `FORBIDDEN_PAIRS` array: `{ from: 'src/<plik>.js', mustNotImport: ['../state/', '../training/', './state/', './training/', '../ui/', './ui/'] }`
- Skanowane pliki: src/highlight/*.js (4 entries), src/PressModel.js, src/MaterialRegistry.js, etc.
- Pattern: regex import statement w pliku źródłowym

Z `userData.poses` kontraktu (Phase 2 D-Phase2-04):
- `oslona-przednia`: `{ closed: {rot:{x:0,y:0,z:0}}, open: {rot:{x:-π/2,y:0,z:0}} }`, pivotTarget 'parent'
- `dzwignia-sprzegla`: `{ released: {rot:{x:0,y:0,z:0}}, engaged: {rot:{x:0,y:0,z:0.7}} }`, pivotTarget 'parent'
- `wylacznik-glowny`: `{ off: {rot:{x:0,y:0,z:0}}, on: {rot:{x:0,y:0,z:π/2}} }`, pivotTarget 'self'
- Object.keys(poses)[0] dla każdego z 3 = pose default (rot wszystko 0) — Pitfall 7 RESEARCH

GSAP użycie (per package.json ~3.15.0):
- `gsap.to(target, { x, y, z, duration, ease, onComplete })` zwraca `gsap.core.Tween` (typ `gsap.core.Tween` ma `.kill()`)
- W jsdom tests gsap działa per Phase 4 (EmissiveController.test.js) — `vi.useFakeTimers()` nie jest potrzebne; gsap ticker sam progresuje, ale testy mogą czekać przez `await new Promise(r => setTimeout(r, ms))` lub wymuszać onComplete przez bezpośrednie wywołanie po `.progress(1)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stuby — InteractionAnimator + RaycastController click channel testy (RED state)</name>
  <files>tests/InteractionAnimator.test.js, tests/RaycastController.click.test.js</files>
  <read_first>
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md (sekcje "InteractionAnimator class skeleton" + "RaycastController extension" Code Examples + "Validation Architecture" → Wave 0 Gaps)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md (D-10-06/07/08/09)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md (Wave 0 Requirements)
    - tests/EmissiveController.test.js (wzorzec gsap.to tween z onComplete w jsdom — sprawdzić jak czekać na timeline complete)
    - tests/RaycastController.test.js (wzorzec mockowania renderer/camera/store/emissive + pointer event dispatch)
    - src/RaycastController.js (kontrakt _handlePointerUp i `_onHoverChange` callback po-hoc)
  </read_first>
  <behavior>
    `tests/InteractionAnimator.test.js` (NOWY):
    - Helper `makeMesh(id, poses, pivotTarget, parentGroup)`: tworzy Three.Mesh z `userData = { id, poses, pivotTarget }`, dodaje do `parentGroup` (THREE.Group); zwraca mesh
    - Test 1 (ctor bootstrap, D-10-07): animator stworzony nad Mapą z 3 meshami (guard closed/open, lever released/engaged, switch off/on) — `getCurrentPose('oslona-przednia')==='closed'`, `getCurrentPose('dzwignia-sprzegla')==='released'`, `getCurrentPose('wylacznik-glowny')==='off'` (pierwszy klucz w poses)
    - Test 2 (handleClick mesh bez poses graceful, D-10-09): wywołać `handleClick('hamulec', meshBezPoses)` — żaden tween nie powstaje (`pivot.rotation.x === 0` po wywołaniu), no-throw
    - Test 3 (D-10-06 — guard click tweens parent): handleClick na guard mesh; sprawdzić że `mesh.parent.rotation.x` startuje 0, po `await waitForTweenComplete()` (gsap.progress(1) lub setTimeout 500ms) wynosi `-Math.PI/2`
    - Test 4 (D-10-06 — lever click tweens parent z.0.7): handleClick na lever; po complete `mesh.parent.rotation.z === 0.7` (±EPSILON)
    - Test 5 (D-10-06 — switch pivotTarget 'self'): handleClick na switch knob; po complete `knob.rotation.z === Math.PI/2`, ALE `knob.parent.rotation.z === 0` (parent nieruszany — pivotTarget self)
    - Test 6 (D-10-07 toggle): handleClick guard → wait → handleClick guard → wait; po drugim onComplete `mesh.parent.rotation.x === 0` (z powrotem do closed), `getCurrentPose('oslona-przednia')==='closed'`
    - Test 7 (D-10-08 isAnimating lock): handleClick guard (start tweena); NATYCHMIAST drugi handleClick (przed onComplete) → assert że pierwszy tween nie został killed (mesh.parent.rotation.x progresuje do -π/2, nie cofa się), `getCurrentPose` po pierwszym complete === 'open'
    - Test 8 (timeline-kill safety): patchować `_tweens.get(mesh).kill = vi.fn()`; force-set `isAnimating=false` przed drugim klikiem; drugi klik wywołuje `kill` istniejącego tweenu przed nowym (defensive path)
    - Test 9 (dispose): handleClick guard, natychmiast `animator.dispose()` → asercja: `_tweens` cleared, `_currentPose` cleared, `_isAnimating` cleared (przez geter helper lub spy na `tween.kill()`)
    - Test 10 (boundary marker): plik `src/interaction/InteractionAnimator.js` istnieje — `fs.existsSync` check. (Pełną boundary asercję robi Task 3 w boundaries.test.js)

    `tests/RaycastController.click.test.js` (NOWY) — describe "Phase 10 manipulation click channel (D-10-09)":
    - Helper: utwórz RaycastController z mock renderer (canvas stub), camera (PerspectiveCamera), interactables Map z meshem `oslona-przednia` mającym `userData.poses` (`{closed:{rot:{x:0,y:0,z:0}}, open:{rot:{x:-π/2,y:0,z:0}}}`); store mock `getState()=>{freeRoam:false, attemptStep: vi.fn(), activeScenario: null, currentStepId: null}`; emissive real lub spy
    - Test 1 (D-10-09 — emit on tap z poses): `controller._onManipulationClick = vi.fn()`; pointerdown @ (100,100); raycast hit forcing — bezpośrednie wywołanie `controller._handlePointerUp({clientX:101, clientY:101})` (delta=1.4px < 5) + monkey-patch `this._raycaster.intersectObjects` zwraca `[{object: guardMesh}]`; asercja: `_onManipulationClick` wywołany z `('oslona-przednia', guardMesh)`
    - Test 2 (D-10-09 — no-emit on drag): pointerdown @ (100,100); _handlePointerUp @ (110,110) (delta ~14px > 5); asercja: `_onManipulationClick` NIE wywołany
    - Test 3 (D-10-09 — no-emit gdy mesh bez poses): hit zwraca mesh bez `userData.poses` (np. mock z `userData={id:'kolo-zamachowe'}`); klik (delta<5); asercja: `_onManipulationClick` NIE wywołany (graceful skip)
    - Test 4 (D-10-09 — emit współistnieje z attemptStep): podczas testu 1 sprawdzić również że `store.getState().attemptStep` BYŁ wywołany z `{kind:'click', meshId:'oslona-przednia'}` (oba kanały firują z jednego pointerup; SOP nadal działa)
    - Test 5 (D-10-09 — emit ZANIM bimanual branch): podstaw step `kind:'bimanual', targetMeshIds:['oslona-przednia']` przez store; klik na guard; asercja: `_onManipulationClick` wywołany (Phase 10 emit dzieje się NIEZALEŻNIE od bimanual flow — wizualna animacja zawsze tweenuje)

    Wszystkie testy używają vitest jsdom env (default).
  </behavior>
  <action>
    Utwórz oba pliki testów ze stubami opisanymi powyżej. Import `InteractionAnimator` z `'../src/interaction/InteractionAnimator.js'` — plik nie istnieje, więc Test 10 + większość będzie FAILED (ERR_MODULE_NOT_FOUND). To jest oczekiwany RED state.

    Helper `waitForTweenComplete(duration_ms = 500)` w InteractionAnimator.test.js: `await new Promise(r => setTimeout(r, duration_ms))` — gsap.ticker w jsdom progresuje real-time per Phase 4 doświadczenie; 500ms > 400ms tween duration zapewnia onComplete fired. Alternatywa: `tween.progress(1)` jeśli animator zwraca tween (dodać method `_getTween(meshId)` test helper lub jako `_tweens.get(mesh)` przez spy w teście).

    Test 5 i 7 wymagają nieruszania bimanual/store flow (mock store getState `freeRoam:false`, no scenariusza) — RaycastController emit dzieje się PRZED bimanual branch (zob. action Task 2).

    Commit: `test(10-02): RED stubs for InteractionAnimator + RaycastController click channel`.
  </action>
  <verify>
    <automated>npx vitest run tests/InteractionAnimator.test.js tests/RaycastController.click.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `tests/InteractionAnimator.test.js` istnieje z 10 testami pokrywającymi D-10-06/07/08/09 + dispose
    - `tests/RaycastController.click.test.js` istnieje z 5 testami pokrywającymi click channel emit/no-emit + bimanual interplay
    - Oba pliki FAILED przy uruchomieniu (ERR_MODULE_NOT_FOUND dla InteractionAnimator OR brak `_onManipulationClick` w RaycastController)
    - `grep -c 'D-10-06\|D-10-07\|D-10-08\|D-10-09' tests/InteractionAnimator.test.js` >= 4
    - `grep -c 'D-10-09' tests/RaycastController.click.test.js` >= 1
    - Inne testy w suite NIE zmienione w tym tasku
  </acceptance_criteria>
  <done>Oba pliki testów istnieją; uruchomienie pokazuje RED dla wszystkich asercji; reszta suite bez zmian.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementacja InteractionAnimator + rozszerzenie RaycastController o click channel (GREEN state)</name>
  <files>src/interaction/InteractionAnimator.js, src/RaycastController.js</files>
  <read_first>
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md (sekcja "Code Examples" → InteractionAnimator class skeleton + RaycastController extension; sekcja "Architecture Patterns" → Pattern 1/2/3; sekcja "Anti-Patterns to Avoid")
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md (D-10-06/07/08/09)
    - src/highlight/EmissiveController.js (linie 31-202 — pattern timeline kill + per-mesh Maps; boundary clean import only THREE+gsap)
    - src/RaycastController.js (linie 30-79 ctor, 149-197 _handlePointerUp, 256-275 dispose)
    - tests/InteractionAnimator.test.js (Task 1 — asercje do spełnienia)
    - tests/RaycastController.click.test.js (Task 1 — asercje do spełnienia)
  </read_first>
  <action>
    A. Utwórz nowy plik `src/interaction/InteractionAnimator.js` — klasa per skeleton z RESEARCH §"Code Examples — InteractionAnimator class skeleton". Kluczowe pola:
      - constants `DURATION_S = 0.4`, `EASE = 'power2.inOut'` module-level
      - constructor `{ interactables }` — buduje `_meshes = Array.from(interactables.values())`, inicjalizuje 3 Maps: `_currentPose`, `_isAnimating`, `_tweens`
      - bootstrap: dla każdego mesh z `userData.poses` → `_currentPose.set(mesh, Object.keys(poses)[0])`, `_isAnimating.set(mesh, false)`
      - `handleClick(meshId, mesh)`:
        - graceful skip gdy `!mesh.userData?.poses`
        - lock check `if (this._isAnimating.get(mesh)) return` (D-10-08 CRIT-8)
        - resolve pivot przez `mesh.userData.pivotTarget === 'parent' ? mesh.parent : mesh`
        - oblicz `nextPose` jako kolejny w `Object.keys(poses)` (circular index)
        - kill istniejący tween defensywnie (`const existing = this._tweens.get(mesh); if (existing) existing.kill()`)
        - set `_isAnimating.set(mesh, true)`
        - `const tween = gsap.to(pivot.rotation, { x: target.x, y: target.y, z: target.z, duration: DURATION_S, ease: EASE, onComplete: () => { this._currentPose.set(mesh, nextPose); this._isAnimating.set(mesh, false); this._tweens.delete(mesh); } })`
        - `this._tweens.set(mesh, tween)`
      - `getCurrentPose(meshId)`: iteruj `_currentPose`, znajdź mesh z `userData.id === meshId`, zwróć pose lub null
      - `dispose()`: kill wszystkich tweenów, clear wszystkich 3 Map

      Importy: TYLKO `import { gsap } from 'gsap';` (THREE jest typem JSDoc, nie wymaga importu). Komentarze polskie (klasa, metody, pola). Identyfikatory angielskie.

    B. Modyfikacja `src/RaycastController.js`:
      - Konstruktor (linia ~30): w destrukturyzacji `{ ..., onManipulationClick = null }` dodać; przypisać `this._onManipulationClick = onManipulationClick`. Komentarz polski: "Phase 10 D-10-09: callback (meshId, mesh) wołany w _handlePointerUp dla mesh z userData.poses. InteractionAnimator wstrzykuje się przez po-hoc assign (analog _onHoverChange)."
      - W `_handlePointerUp` (linia ~178, po `const mesh = hits[0].object; const meshId = mesh.userData.id;`), PRZED bimanual branch (linia 184):
        - Dorzucić blok: `// Phase 10 D-10-09: emit manipulation click do animatora niezależnie od SOP flow. // Animator graceful skip dla mesh bez poses; nasz check tutaj jest opcjonalny, ale unika nie-needed callback. if (mesh.userData?.poses) { this._onManipulationClick?.(meshId, mesh); }`
      - `dispose()`: nie wymaga zmian (callback ref clearuje się sam przy garbage collect Application).

    Wzorzec po-hoc assign jak TooltipManager — Application (Plan 03) ustawia `raycastController._onManipulationClick = (id, mesh) => animator.handleClick(id, mesh)`. Eksponujemy callback jako PUBLIC field (nie underscored API), ale per istniejący styl (`_onHoverChange`) zostaje underscore.

    Commit: `feat(10-02): InteractionAnimator + RaycastController click channel (D-10-06/07/08/09)`.
  </action>
  <verify>
    <automated>npx vitest run tests/InteractionAnimator.test.js tests/RaycastController.click.test.js tests/RaycastController.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Plik `src/interaction/InteractionAnimator.js` istnieje
    - `grep -c "^import" src/interaction/InteractionAnimator.js` === 1 (tylko gsap)
    - `grep -c "from 'gsap'" src/interaction/InteractionAnimator.js` === 1
    - `grep -E "from ['\"]\\.\\./(state|training|ui)/" src/interaction/InteractionAnimator.js` zwraca 0 matches (boundary clean — dowód że Task 3 entry przejdzie)
    - `grep -c "class InteractionAnimator" src/interaction/InteractionAnimator.js` === 1
    - `grep -c "handleClick" src/interaction/InteractionAnimator.js` >= 2 (definicja + ref do this.handleClick — jeśli wewnątrz)
    - `grep -c "_onManipulationClick" src/RaycastController.js` >= 2 (ctor init + emit w _handlePointerUp)
    - `grep -c "userData?.poses" src/RaycastController.js` >= 1 (filter emit)
    - `npx vitest run tests/InteractionAnimator.test.js tests/RaycastController.click.test.js` — wszystkie 15 testów GREEN
    - `npx vitest run tests/RaycastController.test.js` — istniejące testy bez regresji (hover hysteresis, bimanual, click-vs-drag baseline)
  </acceptance_criteria>
  <done>15 testów Task 1 GREEN; RaycastController istniejący kontrakt bez regresji; InteractionAnimator boundary clean (zero state/training/DOM/ui imports).</done>
</task>

<task type="auto">
  <name>Task 3: Boundary entry + rozszerzenie RaycastController.test.js o manipulation click in original suite</name>
  <files>tests/boundaries.test.js, tests/RaycastController.test.js</files>
  <read_first>
    - tests/boundaries.test.js (cała — FORBIDDEN_PAIRS pattern, identyfikacja entry dla src/highlight/EmissiveController.js jako wzorca)
    - tests/RaycastController.test.js (cała — wzorzec testów + describe blocks)
    - src/interaction/InteractionAnimator.js (po Task 2 — zweryfikować że nie importuje zakazanego)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md (boundary entry wymóg)
  </read_first>
  <action>
    A. `tests/boundaries.test.js` — dorzucić entry dla nowego pliku w tablicy `FORBIDDEN_PAIRS` (lub odpowiedniej struktury):
      - `{ from: 'src/interaction/InteractionAnimator.js', mustNotImport: ['../state/', '../training/', './state/', './training/', '../ui/', './ui/'] }` (analog do entry dla `src/highlight/EmissiveController.js`)
      - Komentarz polski: "Phase 10 D-10-07: InteractionAnimator klik-driven, NIE store-driven — żaden import z state/training/ui."
      - Jeśli aktualna struktura `boundaries.test.js` zawiera również scan listę dozwolonych importów (sprawdzić Phase 9 baseline 4+2 imports), dorzucić odpowiedni assertion że `src/interaction/InteractionAnimator.js` ma EXACTLY 1 import (`from 'gsap'`)

    B. `tests/RaycastController.test.js` — dorzucić describe `'Phase 10 manipulation click channel — integration sanity'`:
      - Test 1: ctor accept `onManipulationClick` w deps OR po-hoc assign `controller._onManipulationClick = vi.fn()`; spy działa po pointerup na mesh z `userData.poses`
      - Test 2 (regresja): istniejący test hover hysteresis NIE jest naruszony przez nowy callback (callback nigdy nie wołany podczas hover; tylko w pointerup branch)
      - Test 3 (regresja): bimanual click flow nadal działa — `_onManipulationClick` emit dzieje się PRZED bimanual branch, ale BIMANUAL flow (drugi mesh, window) nadal zwraca early-return po `_handleBimanualClick`. Spy assertion: oba zostają wywołane przy klik na mesh z poses w trybie bimanual

    Te testy duplikują kilka asercji z `tests/RaycastController.click.test.js` ale w istniejącym pliku — jako safety net regresji RaycastController.test.js (Phase 4 covered behaviors).

    Commit: `test(10-02): boundary entry for InteractionAnimator + RaycastController click channel regression`.
  </action>
  <verify>
    <automated>npx vitest run tests/boundaries.test.js tests/RaycastController.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/interaction/InteractionAnimator.js" tests/boundaries.test.js` >= 1
    - `grep -c "InteractionAnimator" tests/boundaries.test.js` >= 1
    - `grep -c "Phase 10 manipulation click" tests/RaycastController.test.js` >= 1
    - `npx vitest run tests/boundaries.test.js` — GREEN (Plan 10-02 boundary entry zaakceptowany)
    - `npx vitest run tests/RaycastController.test.js` — GREEN (3 nowe regresje + cały Phase 3/4/5/6 baseline)
    - `npx vitest run` (full suite) — GREEN; baseline 777 + ~12 z Plan 01 + ~18 z Plan 02 (10 InteractionAnimator + 5 RaycastController.click + 3 RaycastController regression) ≈ 807 testów
  </acceptance_criteria>
  <done>Boundary entry dodany, RaycastController regression tests dodane, full suite GREEN, Plan 02 gotowy do wiringu w Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer event → InteractionAnimator | Każdy pointerup może wybić animację — trust kontrolowany przez RaycastController click-vs-drag threshold (5px) + mesh.userData.poses filter |
| Animator → scene-graph (pivot.rotation) | Animator pisze do `pivot.rotation` (parent group dla guard/lever, self dla switch); brak innych kanałów |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial of Service | GSAP tween na unmounted mesh (HMR / dispose race) | mitigate | `Application.dispose()` (Plan 03) wywołuje `animator.dispose()` PRZED `raycastController.dispose()` — tweens killed + Maps cleared; pointer event po dispose nie ma callback (cleared) |
| T-10-05 | Tampering | Click flooding podczas tween — race | mitigate | D-10-08 isAnimating lock per-mesh; test 7 InteractionAnimator.test.js dowodzi że drugi klik no-op |
| T-10-06 | Tampering | Ghost emissive write z animatora (CRIT-5 naruszenie) | mitigate | Animator pisze TYLKO do `pivot.rotation`, NIE do `material.emissive*`. Boundary entry tests/boundaries.test.js + grep w teście Task 2 acceptance ("zero import z highlight/" — pośrednio zapewnia że animator nie wywołuje setLayer). EmissiveController pozostaje single writer emissive |
| T-10-07 | Information Disclosure | Animator subskrybuje store i wycieka stan SOP do warstwy wizualnej | mitigate | D-10-07: zero coupling do trainingStore; boundary scanner asercja w tests/boundaries.test.js |
</threat_model>

<verification>
- `npx vitest run` — full suite green
- Boundary: `src/interaction/InteractionAnimator.js` imports tylko gsap (`grep -c '^import' === 1`)
- RaycastController kontrakt: nowy `_onManipulationClick` po-hoc assign (analog `_onHoverChange`) — Phase 3/4/5/6 testy bez regresji
- InteractionAnimator state machine: 10 testów (bootstrap, no-poses graceful, guard/lever/switch tween, toggle, isAnimating lock, timeline-kill safety, dispose, file exists)
</verification>

<success_criteria>
- D-10-06 ✓ Klik tweenuje pivot.rotation z duration 0.4s ease power2.inOut do następnej pose
- D-10-07 ✓ Toggle flipuje pose lokalnie (zero coupling do trainingStore — boundary scanner enforces)
- D-10-08 ✓ isAnimating lock per-mesh odrzuca drugi klik podczas in-flight tween
- D-10-09 ✓ RaycastController emituje `_onManipulationClick(meshId, mesh)` w `_handlePointerUp` PRZED bimanual branch dla mesh z `userData.poses`; drag (>=5px) NIE emituje
- CRIT-5 ✓ Animator NIE pisze do material — tylko pivot.rotation (boundary scanner + brak `material.emissive` w src/interaction/InteractionAnimator.js)
- CRIT-8 ✓ isAnimating lock + onComplete cleanup
- Architecture open dla `wylacznik-glowny` (pivotTarget: 'self') — animator handle generycznie (Test 5 w Task 1)
</success_criteria>

<output>
Po wszystkich taskach utwórz `.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-02-SUMMARY.md` z sekcjami: nowa klasa InteractionAnimator (LOC, imports), RaycastController extension (linia callback emit), testy (18 nowych asercji łącznie), Wave handoff (Plan 03 może wiring'ować — `Application` ctor po RaycastController, dispose order animator → raycast → emissive).
</output>
