---
phase: 10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PressModel.js
  - tests/PressModel.transparency.test.js
  - tests/PressModel.phase10.test.js
  - tests/PressModel.anchoring.test.js
  - tests/PressModel.smoke.test.js
  - tests/EmissiveController.test.js
autonomous: true
requirements:
  - D-10-01
  - D-10-02
  - D-10-03
  - D-10-04
  - D-10-05
  - D-10-10
  - D-10-11
  - KIN-01
  - ANCHOR-01
  - ANCHOR-02
  - MAT-04
  - CRIT-6
must_haves:
  truths:
    - "Osłona przednia (oslona-przednia) jest stale półprzezroczysta — przy zamkniętej pozie mechanizm wewnątrz (suwak, korbowód, mimośród) jest widoczny"
    - "Środkowy wał (shaftAxis) jest stale wycentrowany X=0, Z=0 przez pełen obrót"
    - "Wał↔mimośród łączą dwa wizualne kołnierze (flange) flankujące mimośród; mimośród↔korbowód łączy wizualny czop (pin) w pozycji eccentricPin"
    - "Wszystkie nowe łączniki (kołnierze + czop) rotują razem z wałem pod update(angle) (KIN-01 dynamic)"
    - "Wspornik dźwigni sprzęgła łączy kolumnę ramy (~x=-2) z podstawą leverGroup (-3,7,0.5) i jest statyczny pod update(angle)"
    - "EmissiveController flash (Wong palette) działa na cloned matGuardOrange z transparent:true — nie crashuje, emissive.getHex() === Wong hex po setLayer"
    - "Baseline kontraktu zachowany: getInteractables().size === 15 (nowe meshe to decoration/dynamic-child, NIE interactable)"
  artifacts:
    - path: "src/PressModel.js"
      provides: "matGuardOrange transparent + shaftAxis explicit center + _buildShaftConnectors() + _buildLeverBracket()"
      contains: "transparent: true"
    - path: "tests/PressModel.transparency.test.js"
      provides: "D-10-01/02 transparent + emissive flash compat asercje"
    - path: "tests/PressModel.phase10.test.js"
      provides: "D-10-03/04/10 geometry + hierarchy asercje + size===15"
  key_links:
    - from: "PressModel.matGuardOrange"
      to: "EmissiveController._applyTopLayer"
      via: "cloned material per interactable (CRIT-6)"
      pattern: "transparent:\\s*true"
    - from: "PressModel.shaftAxis"
      to: "nowy _buildShaftConnectors flange + pin"
      via: "this.shaftAxis.add(mesh) — dziecko shaftAxis, rotuje z wałem"
      pattern: "this\\.shaftAxis\\.add"
    - from: "PressModel.group"
      to: "nowy _buildLeverBracket bracket"
      via: "this.group.add(bracket) — statyczny pod update(angle)"
      pattern: "this\\.group\\.add\\(bracket\\)"
---

<objective>
Phase 10 Plan 1: warstwa materiałów + geometrii. Wprowadza przezroczystość osłony przedniej, doprecyzowuje centrowanie shaftAxis, dorzuca wizualne łączniki wał↔mimośród (kołnierze) + mimośród↔korbowód (czop) jako dzieci `this.shaftAxis` (dynamic), oraz dekoracyjny wspornik dźwigni sprzęgła jako dziecko `this.group` (statyczny). Wave 0 stuby testów + Wave 1 implementacja w jednym planie — wszystkie zmiany dotykają tylko `src/PressModel.js` i powiązanych testów PressModel.

Purpose: zaadresować D-10-01/02/03/04/10/11 (+ opcjonalnie D-10-05 bearings tweak po smoke); preserve invariants KIN-01, ANCHOR-01, ANCHOR-02, CRIT-6, MAT-04. Plan 02 (animator) i Plan 03 (wiring + smoke) ruszają osobno bez konfliktów na plikach.

Output: zaktualizowany `src/PressModel.js`, nowe pliki testowe + rozszerzenia istniejących testów (anchoring, smoke, EmissiveController).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md
@CLAUDE.md
@src/PressModel.js
@src/highlight/EmissiveController.js
@tests/PressModel.anchoring.test.js
@tests/PressModel.smoke.test.js
@tests/EmissiveController.test.js

<interfaces>
Z `src/PressModel.js` istniejące pola/metody (D-10 zmiany muszą się w to wpiąć bez łamania):
- `this.matGuardOrange = new THREE.MeshStandardMaterial({ color: 0xC8B400, metalness: 0.1, roughness: 0.85 })` (linia 72)
- `this.shaftAxis = new THREE.Group(); this.shaftAxis.position.set(0, this.shaftY, 0); this.group.add(this.shaftAxis)` (linie 177-179)
- `this.eccentricPin = new THREE.Object3D(); this.eccentricPin.position.set(0, this.r, 0); this.shaftAxis.add(this.eccentricPin)` (linie 200-202)
- `this.r` (eccentric radius, default 0.8), `this.l` (rod length), `this.shaftY` (canonical wartość 8)
- Eccentric mesh: `CylinderGeometry(r+0.3, r+0.3, 1, 32)` z `rotateZ(π/2)` (oś wzdłuż X), `position.set(0, this.r, 0)`
- Shaft mesh: `CylinderGeometry(0.4, 0.4, 4.5, 32)` z `rotateZ(π/2)`
- `_buildFrontGuard()` (linie 658-686): rejestruje `oslona-przednia` przez `_registerInteractable({ baseMaterial: this.matGuardOrange, poses: {closed, open}, pivotTarget: 'parent' })`
- `_buildClutchLever()` (linie 792-827): leverGroup @ (-3, 7, 0.5), pivotTarget 'parent', poses {released, engaged}
- `_buildBearings()` (linie 845+): bearing `CylinderGeometry(0.6, 0.6, 0.8, 32)` w (±2.0, shaftY, 0) — D-10-05 ewentualny tweak R
- `getInteractables(): Map<string, THREE.Mesh>` — kontrakt size===15 (Phase 8/9 audit)
- `_registerInteractable({mesh, id, kind, baseMaterial, poses, pivotTarget})` — registry path NIE musi być wołany dla decoration

Z `src/highlight/EmissiveController.js`:
- `setLayer(name: 'hover'|'state', mesh, opts: {color, pulse?, flash?})` — pisze do `material.emissive.setHex(...)` + `material.emissiveIntensity` przez gsap timeline
- `_savePreFlash(mesh)` snapshot color/emissive/metalness/roughness (NIE opacity/transparent — Pitfall 2)
- `clearLayer(name, mesh)` — restoruje
- CRIT-5: tylko `gsap.to(material, {emissiveIntensity:...})`, NIGDY THREE.Color

Z `tests/PressModel.anchoring.test.js`:
- `const DYNAMIC_IDS = new Set(['kolo-zamachowe'])` linia 44 — meshe które rotują pod update(angle) i mają być wykluczone z KIN-01 static check
- describe blocks: "KIN-01: rotation invariants (post Phase 7 fix)" + "decoration meshy są statyczne pod update(angle) — children this.group"
- Pattern: snapshot `worldPosition.clone()` przed/po `update(angle)`, po `pressModel.group.updateMatrixWorld(true)`
- Floor invariant test #4: interactables y > -0.8 - EPSILON

Z `tests/PressModel.smoke.test.js`:
- TWIN-11/12/13 asercje: `getInteractables().size === 15`, `disposeMaterials()` count, mesh registry baseline
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stuby — nowe testy transparency + Phase 10 geometry (RED state)</name>
  <files>tests/PressModel.transparency.test.js, tests/PressModel.phase10.test.js</files>
  <read_first>
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md (D-10-01..04, 10, 11)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md (sekcja "Validation Architecture" → Wave 0 Gaps; sekcja "PressModel modifications" code example)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
    - tests/PressModel.smoke.test.js (wzorzec setup PressModel + assertGetInteractables size)
    - tests/PressModel.bearings.test.js (wzorzec testowania geometrii decoration children)
    - src/PressModel.js (struktura buildPress, matGuardOrange, shaftAxis composition, _buildClutchLever)
  </read_first>
  <behavior>
    `tests/PressModel.transparency.test.js` (NOWY) — describe "Phase 10 D-10-01/02 — guard transparency":
    - Test 1: `pressModel.matGuardOrange.transparent === true && opacity === 0.5` (literal sprawdzenie pola material)
    - Test 2: `depthWrite === true && alphaTest === 0` (default; Pitfall 1 — NIE zmieniamy)
    - Test 3: EmissiveController flash compat — utworzyć EmissiveController nad `pressModel.getInteractables()`, wywołać `setLayer('state', guardMesh, { color: 0xD55E00, flash: true })` na osłonie, asercja: nie rzuca + `guardMesh.material.emissive.getHex() === 0xD55E00` po `_applyTopLayer`

    `tests/PressModel.phase10.test.js` (NOWY) — describe "Phase 10 geometry":
    - Test 1 (D-10-03): `pressModel.shaftAxis.position.x === 0 && pressModel.shaftAxis.position.z === 0 && pressModel.shaftAxis.position.y === pressModel.shaftY` (explicit assertion)
    - Test 2 (D-10-04 kołnierze): w `pressModel.shaftAxis.children` znaleźć 2 mesh-e z `CylinderGeometry` o `parameters.radiusTop===0.5 && height===0.15 && radialSegments===24`; ich `position.x` to ±0.575 (±EPSILON 1e-6), `position.y===0`, `position.z===0`; obie mesh-e mają materiał `=== pressModel.matShaft`
    - Test 3 (D-10-04 czop): w `pressModel.shaftAxis.children` znaleźć 1 mesh z `CylinderGeometry` o `radiusTop===0.15 && height===0.3 && radialSegments===16`; `position.x===0 && position.y===pressModel.r && position.z===0`; materiał `=== pressModel.matEccentric`
    - Test 4 (D-10-10 wspornik): w `pressModel.group.children` znaleźć mesh z `BoxGeometry parameters.width===1.0 && height===0.3 && depth===0.3`; `position.x===-2.5 && position.y===7 && position.z===0.5`; `userData.kind === 'decoration'`; materiał `=== pressModel.matBody`
    - Test 5 (D-10-11 + baseline): `pressModel.getInteractables().size === 15` (nowe meshe NIE są interactable)
    - Test 6 (KIN-01 dynamic łączniki): snapshot worldPosition flange+pin przed/po `update(Math.PI/2)`, po `pressModel.group.updateMatrixWorld(true)` — wartości MUSZĄ się różnić (proof rotują z wałem)
    - Test 7 (KIN-01 static wspornik): snapshot worldPosition wspornika dźwigni przed/po `update(Math.PI/2)` — wartości MUSZĄ być równe (statyczny dekorat)

    Wszystkie testy używają pattern z `tests/PressModel.smoke.test.js` (mock canvas context, materialRegistry stub, `new PressModel(...).buildPress()`).
    Stan RED: testy zielenią się dopiero po Task 2.
  </behavior>
  <action>
    Utwórz oba pliki testów ze stubami opisanymi powyżej. Komentarze testów po polsku, identyfikatory (`describe`, `it`) mogą być po angielsku per konwencja Vitest. Helper `pressModelFor10()` wewnątrz każdego pliku — instancja `new PressModel({materialRegistry: ..., r: 0.8, l: 4.5, shaftY: 8}).buildPress()`. Mock kontekstu canvas identycznie jak `tests/PressModel.smoke.test.js` (`HTMLCanvasElement.prototype.getContext = () => null` przed importem PressModel — patrz Phase 2 D-Phase2-06).

    Dla testu EmissiveController flash compat — zaimportuj `EmissiveController` z `src/highlight/EmissiveController.js`, mockuj `gsap.to` (lub użyj real gsap; gsap CJS w jsdom działa per Phase 4 tests). Po `setLayer('state', mesh, {color: 0xD55E00, flash: true})` wywołaj `vi.runAllTimers()` lub asercję natychmiastową stanu `mesh.material.emissive.getHex()` (zależnie od pattern z `tests/EmissiveController.test.js` — czytanie tego pliku pokaże real-gsap vs spy).

    Commit: `test(10-01): RED stubs for transparency + phase10 geometry`.
  </action>
  <verify>
    <automated>npx vitest run tests/PressModel.transparency.test.js tests/PressModel.phase10.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Plik `tests/PressModel.transparency.test.js` istnieje z 3 testami w 1 describe
    - Plik `tests/PressModel.phase10.test.js` istnieje z 7 testami pokrywającymi D-10-03/04/10/11 + KIN-01 dynamic + static
    - Testy RED: oba pliki FAILUJĄ (`transparent !== true`, brak nowych mesh-y) — execution na obecnym `src/PressModel.js` daje co najmniej 6 failed/per file
    - `grep -v '^//' tests/PressModel.phase10.test.js | grep -c 'getInteractables().size === 15'` >= 1
    - Inne testy w suite (np. anchoring, kinematics, smoke) NIE są zmieniane w tym tasku
  </acceptance_criteria>
  <done>Oba pliki testów istnieją z asercjami z `<behavior>`; running suite shows RED dla nowych testów; nic w `src/` ani innych testach nie zmienione.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementacja PressModel — transparency + shaftAxis explicit + _buildShaftConnectors + _buildLeverBracket (GREEN state)</name>
  <files>src/PressModel.js</files>
  <read_first>
    - src/PressModel.js (linie 60-103 materials, 175-258 buildPress, 658-686 _buildFrontGuard, 792-827 _buildClutchLever, 845+ _buildBearings)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md (D-10-01..05, 10, 11)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md (sekcja "PressModel modifications" Code Examples + Pitfall 1, 4, 5, 6)
    - tests/PressModel.transparency.test.js (Task 1 — asercje do spełnienia)
    - tests/PressModel.phase10.test.js (Task 1 — asercje do spełnienia)
    - tests/PressModel.anchoring.test.js (KIN-01 invariant pattern + DYNAMIC_IDS — sprawdzić czy nowe łączniki nie naruszają)
  </read_first>
  <action>
    Modyfikacje w `src/PressModel.js`:

    A. Material — `this.matGuardOrange` (linia 72): dorzucić `transparent: true, opacity: 0.5` do MeshStandardMaterial config. Komentarz po polsku nad polem: "D-10-01: stała półprzezroczystość — mechanizm wewnątrz widoczny przy zamkniętej osłonie. NIE zmieniać depthWrite/alphaTest (Pitfall 1). NIE animować opacity w flash (Pitfall 2 — preFlash backup NIE zapisuje opacity)."

    B. shaftAxis explicit center (linia 177-179): pozostawić istniejące `this.shaftAxis.position.set(0, this.shaftY, 0)` — D-10-03 jest assertion, nie zmianą; dorzucić komentarz po polsku: "D-10-03: explicit X=0, Z=0 — KIN-01 regression w tests/PressModel.phase10.test.js."

    C. Nowa metoda `_buildShaftConnectors()` (dziecko `this.shaftAxis`, wołana po `_buildBearings()` w `buildPress()` — czyli po obecnej linii 249, w sekcji Wave 5; alternatywnie w bloku który tworzy shaftAxis hierarchy linia 175-202). Decyzja: wołać z `buildPress()` PO `this.update(0)` byłoby błędem — wołać w istniejącym bloku gdzie tworzony jest shaftAxis (po linii 202, po `this.eccentricPin`). Implementacja:
      - 2 kołnierze flankujące (Opcja A z Pitfall 4 RESEARCH): `CylinderGeometry(0.5, 0.5, 0.15, 24)` z `rotateZ(Math.PI/2)` (oś wzdłuż X), `position.set(±0.575, 0, 0)`, materiał `this.matShaft`, `castShadow = true`, dodane jako `this.shaftAxis.add(...)`
      - 1 czop: `CylinderGeometry(0.15, 0.15, 0.3, 16)` z `rotateZ(Math.PI/2)` (oś wzdłuż X — sterczy z boków eccentric ±X, NIE ±Z; RESEARCH Pitfall 5 rozstrzyga że oś X jest niezmiennicza pod shaftAxis.rotation.x), `position.set(0, this.r, 0)` (kopia eccentricPin local), materiał `this.matEccentric`, `castShadow = true`
      - Geometrie tworzyć inline w metodzie (analog `_buildBearings()`) — bez shared geo registry; geometry buffers domknie `disposeMaterials` przez fakt że materiały są reused (matShaft/matEccentric już trackowane); geometrie zwolnione przy `this.group.traverse` w zewnętrznym dispose path (poza scope Phase 10).

    D. Nowa metoda `_buildLeverBracket()` (dziecko `this.group` — statyczny, KIN-01 static): `BoxGeometry(1.0, 0.3, 0.3)`, `position.set(-2.5, 7, 0.5)`, materiał `this.matBody`, `castShadow = true, receiveShadow = true`, `userData = { kind: 'decoration' }`. Wołana z `buildPress()` po `_buildClutchLever()` (czyli po linii 248).

    E. D-10-05 (Claude's discretion): NIE zmieniać `_buildBearings()` w tym task. Decyzja po manual smoke w Plan 03 — jeśli wymagana zmiana, dorzucić jako follow-up task lub ręczny tweak R 0.6→0.7-0.8. Komentarz nad `_buildBearings` nie jest modyfikowany.

    Wszystkie komentarze polskie. Identyfikatory metod (`_buildShaftConnectors`, `_buildLeverBracket`) po angielsku per konwencja `src/PressModel.js`.

    Commit: `feat(10-01): guard transparent + shaft connectors + lever bracket (D-10-01/02/03/04/10/11)`.
  </action>
  <verify>
    <automated>npx vitest run tests/PressModel.transparency.test.js tests/PressModel.phase10.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'transparent: true' src/PressModel.js` matchuje linię w deklaracji `matGuardOrange`
    - `grep -n 'opacity: 0.5' src/PressModel.js` matchuje tę samą sekcję
    - `grep -c '_buildShaftConnectors' src/PressModel.js` >= 2 (definicja + wywołanie w buildPress)
    - `grep -c '_buildLeverBracket' src/PressModel.js` >= 2 (definicja + wywołanie w buildPress)
    - `grep -c "userData = { kind: 'decoration' }" src/PressModel.js` >= 1 (nowy wspornik dźwigni; istniejące `kind:'decoration'` z Phase 7 bearings + Phase 8 foundation już są)
    - `npx vitest run tests/PressModel.transparency.test.js tests/PressModel.phase10.test.js` — wszystkie 10 testów GREEN
    - `npx vitest run tests/PressModel.anchoring.test.js tests/PressModel.smoke.test.js` — istniejące invarianty (size===15, ANCHOR-01 floor) NIE łamią się (zero regresji)
    - `grep -c 'depthWrite' src/PressModel.js` w bloku matGuardOrange nie pojawia się (Pitfall 1 — default zachowany)
  </acceptance_criteria>
  <done>10 testów Task 1 GREEN; istniejący suite (777 baseline) bez regresji w plikach `tests/PressModel.*`; PressModel ma 2 nowe metody + transparent matGuardOrange.</done>
</task>

<task type="auto">
  <name>Task 3: Rozszerzenie istniejących testów — anchoring KIN-01 + smoke size + EmissiveController flash compat</name>
  <files>tests/PressModel.anchoring.test.js, tests/PressModel.smoke.test.js, tests/EmissiveController.test.js</files>
  <read_first>
    - tests/PressModel.anchoring.test.js (cała — KIN-01 invariant + ANCHOR-01 floor + decoration static; identyfikacja DYNAMIC_IDS linia 44 + describe "decoration meshy są statyczne" linia ~276)
    - tests/PressModel.smoke.test.js (cała — TWIN-11/12/13 baseline)
    - tests/EmissiveController.test.js (cała — wzorzec testowania setLayer/clearLayer z real gsap; sprawdzić jak inicjalizować EmissiveController + minimal mesh fixture)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md (Wave 0 Requirements — 4 extensions)
    - src/PressModel.js (po Task 2 — zweryfikować że nowe meshe siedzą gdzie testy oczekują)
  </read_first>
  <behavior>
    `tests/PressModel.anchoring.test.js` — rozszerzenia:
    - W describe "KIN-01: rotation invariants" dodać `it('shaftAxis position stays X=0 Z=0 through full cycle (D-10-03)')` — sample `[0, π/4, π/2, 3π/4, π, 5π/4, 3π/2, 7π/4]`, po każdym `pressModel.update(angle) + group.updateMatrixWorld(true)` asercja `worldPos.x` i `.z` |.| < 1e-6 dla `pressModel.shaftAxis`
    - W describe "decoration meshy są statyczne pod update(angle)" dodać snapshot wspornika dźwigni: znaleźć mesh z `userData.kind === 'decoration'` i `geometry.type === 'BoxGeometry'` z parametrami (1.0, 0.3, 0.3) w `pressModel.group.children`; snapshot worldPos przed/po `update(Math.PI/2)`, asercja equal (decoration KIN-01 static)
    - W teście floor (ANCHOR-01 #4) — dorzucić explicit asercję: `bracket.worldPosition.y > -0.8 - EPSILON` (trivially passes @ y=7, ale eksplicytna dla regresji)

    `tests/PressModel.smoke.test.js` — rozszerzenia:
    - Test "getInteractables().size === 15": pozostaje bez zmian (kontrakt — nowe meshe Phase 10 NIE są interactable per D-10-11)
    - Dorzucić nowy `it('TWIN-13 (Phase 10): nowe łączniki i wspornik nie powiększają interactables map (D-10-11)')` — eksplicytna asercja że `getInteractables()` NIE zawiera kluczy `shaft-eccentric-flange*`, `eccentric-rod-pin`, `lever-bracket` (lub jakichkolwiek nazw dla nowych mesh-y); size pozostaje 15

    `tests/EmissiveController.test.js` — rozszerzenie:
    - Dorzucić `it('Phase 10: setLayer(state) na meshu z material.transparent=true nie crashuje i ustawia emissive Wong (D-10-02)')` — utworzyć dummy mesh z `MeshStandardMaterial({color: 0xC8B400, transparent: true, opacity: 0.5})`, `userData.id = 'oslona-przednia'`; interactables Map = `new Map([['oslona-przednia', mesh]])`; emissive = `new EmissiveController({interactables})`; `emissive.setLayer('state', mesh, { color: 0xD55E00, pulse: true })`; asercja: `mesh.material.emissive.getHex() === 0xD55E00`, no throw
  </behavior>
  <action>
    Edytuj 3 pliki testów dodając opisane testy. Wszystkie testy oczekiwane GREEN — `src/PressModel.js` po Task 2 zawiera już potrzebne meshe i transparent material; EmissiveController już obsługuje transparent materials (graceful skip dla brak `emissive` field per Phase 4 fix).

    Identyfikacja "wspornika dźwigni" w `tests/PressModel.anchoring.test.js`: użyć filter `(m) => m.geometry?.type === 'BoxGeometry' && m.geometry.parameters.width === 1.0 && m.geometry.parameters.height === 0.3 && m.userData?.kind === 'decoration'` po `pressModel.group.traverse`. Nazw mesh-y nie wymuszamy.

    Wave 0 entry dla `tests/RaycastController.click.test.js` + `tests/InteractionAnimator.test.js` + boundary entry — należą do Plan 10-02, NIE tworzymy ich tutaj.

    Komentarze polskie, identyfikatory testów (`it`, `describe`) angielski/polski mixed per istniejący file styl.

    Commit: `test(10-01): extend KIN-01 + smoke + EmissiveController for Phase 10 invariants`.
  </action>
  <verify>
    <automated>npx vitest run tests/PressModel.anchoring.test.js tests/PressModel.smoke.test.js tests/EmissiveController.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-10-03' tests/PressModel.anchoring.test.js` >= 1 (shaftAxis X/Z stays zero)
    - `grep -c 'D-10-11' tests/PressModel.smoke.test.js` >= 1 (size===15 extension)
    - `grep -c 'D-10-02' tests/EmissiveController.test.js` >= 1 (transparent material flash compat)
    - `npx vitest run tests/PressModel.anchoring.test.js tests/PressModel.smoke.test.js tests/EmissiveController.test.js` — wszystkie testy GREEN (baseline + nowe rozszerzenia)
    - `npx vitest run` (full suite) — całość zielona; baseline 777 powiększa się o nowe testy z Task 1+3 (estymata +12 testów: 3 transparency + 7 phase10 + 1 anchoring shaftAxis + 1 anchoring bracket + 1 smoke + 1 emissive)
  </acceptance_criteria>
  <done>3 pliki testów rozszerzone; full suite zielony; KIN-01 dynamic (flange+pin) + static (bracket) + shaftAxis center invariants potwierdzone w testach; emissive flash na transparent material zweryfikowany.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brak nowych | Phase 10 nie wprowadza nowych input vectors — czysty refactor scene-graph + materials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | matGuardOrange (cloned per-mesh per CRIT-6) | accept | Phase 4 CRIT-6 invariant zapewnia że klon jest per-interactable; transparent=true na bazowym (przed `_registerInteractable`) jest propagowany do klona; brak nowego attack vector |
| T-10-02 | Denial of Service | EmissiveController flash + transparent material | mitigate | Pitfall 2 RESEARCH: `_savePreFlash` NIE zapisuje `transparent/opacity` — Phase 10 NIE animuje opacity, więc backup pozostaje kompletny. Test EmissiveController extension waliduje no-crash + emissive hex |
| T-10-03 | Tampering (visual state) | KIN-01 invariant — nowe łączniki dzieci shaftAxis | mitigate | Testy KIN-01 dynamic + static w tests/PressModel.phase10.test.js + extension anchoring.test.js — proof że flange+pin rotują, bracket pozostaje statyczny |
</threat_model>

<verification>
- `npx vitest run` — full suite green; ~789 testów (777 baseline + ~12 nowych Phase 10 Plan 1)
- `npx vitest run tests/PressModel.transparency.test.js tests/PressModel.phase10.test.js tests/PressModel.anchoring.test.js tests/PressModel.smoke.test.js tests/EmissiveController.test.js` — selectywny pass
- Boundary scanner (`tests/boundaries.test.js`) nadal zielony — Plan 1 nie wprowadza nowych plików `src/`, więc boundary nie wymaga zmiany (Plan 02 doda entry dla `InteractionAnimator`)
- KIN-01 invariant rozszerzony o shaft connectors dynamic + lever bracket static + shaftAxis position center
</verification>

<success_criteria>
- D-10-01 ✓ `matGuardOrange.transparent === true && opacity === 0.5`
- D-10-02 ✓ EmissiveController flash compat na transparent material zweryfikowany testem
- D-10-03 ✓ `shaftAxis.position` X=0, Z=0 stałe przez pełen cykl
- D-10-04 ✓ 2 kołnierze flankujące eccentric + 1 czop w eccentricPin local — dzieci shaftAxis, KIN-01 dynamic
- D-10-10 ✓ Wspornik dźwigni BoxGeometry(1.0, 0.3, 0.3) @ (-2.5, 7, 0.5) — dziecko this.group, KIN-01 static
- D-10-11 ✓ Wspornik decoration only — `userData.kind==='decoration'`, NIE w `getInteractables()`, size===15 preserved
- D-10-05 odroczone do smoke gate Plan 03 (Claude's discretion — bearings tweak warunkowy)
- Wszystkie pre-existing invarianty (KIN-01, ANCHOR-01, CRIT-5/6/7, MAT-04, TWIN-11/12/13) preserved
</success_criteria>

<output>
Po wszystkich taskach utwórz `.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-01-SUMMARY.md` z sekcjami: Decyzje implementacyjne (D-10-01..05, 10, 11 status), Zmiany w plikach (PressModel.js diff scope), Testy (12 nowych asercji), Wave handoff (Plan 02 może zacząć — InteractionAnimator pliki nie kolidują).
</output>
