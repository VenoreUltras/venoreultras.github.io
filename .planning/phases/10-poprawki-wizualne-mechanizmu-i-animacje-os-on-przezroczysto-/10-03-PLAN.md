---
phase: 10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/main.js
  - tests/application.test.js
autonomous: false
requirements:
  - wiring
  - D-10-05
  - D-10-06
  - D-10-07
  - D-10-08
  - D-10-09
  - MAT-04
  - TEST-08
must_haves:
  truths:
    - "Application konstruuje InteractionAnimator PO RaycastController (potrzebuje raycast jako kanał klik)"
    - "Po-hoc assign: `raycastController._onManipulationClick = (id, mesh) => animator.handleClick(id, mesh)` (analog _onHoverChange)"
    - "Application.dispose() kolejność: animator → raycast → emissive (animator NIE pisze do material, tylko pivot.rotation — kolejność vs emissive nieistotna, ale konsystentna z T-04-14)"
    - "Manualny smoke przeszedł: osłona transparenta, klik na osłonę i dźwignię tweenuje 0.4s, mechanizm widoczny przez osłonę, łączniki wału spójne wizualnie, wspornik dźwigni siedzi przy ramie"
    - "Bundle size budget preserved: main bundle < 850KB hard limit"
  artifacts:
    - path: "src/main.js"
      provides: "Application wiring InteractionAnimator + dispose order"
    - path: "tests/application.test.js"
      provides: "Lifecycle dispose order test rozszerzony o animator"
  key_links:
    - from: "Application.ctor (src/main.js)"
      to: "InteractionAnimator(interactables)"
      via: "import { InteractionAnimator } from './interaction/InteractionAnimator.js'"
      pattern: "new InteractionAnimator"
    - from: "Application.ctor po raycastController"
      to: "raycastController._onManipulationClick = (id, mesh) => this.interactionAnimator.handleClick(id, mesh)"
      via: "po-hoc callback assignment (analog _onHoverChange / TooltipManager)"
      pattern: "_onManipulationClick\\s*="
    - from: "Application.dispose"
      to: "interactionAnimator.dispose() PRZED raycastController.dispose()"
      via: "T-04-14 pattern — kolejność dispose"
      pattern: "interactionAnimator.*dispose"
---

<objective>
Phase 10 Plan 3: integracja InteractionAnimator (Plan 02) + transparent guard / shaft connectors / lever bracket (Plan 01) w `Application` (src/main.js). Wpięcie dispose chain, lifecycle test (analog T-04-14 mock.invocationCallOrder), oraz manualny smoke gate dla 4 obszarów: transparency, łączniki wału, animacja klik, wspornik dźwigni. Po smoke — Claude's discretion: D-10-05 bearings tweak (R 0.6→0.7-0.8 lub kontrastowy kołnierz) jeśli wizualnie potrzebne; opacity tweak w [0.35, 0.6] dla osłony jeśli przesłania za mocno lub za słabo.

Purpose: ostatni krok Phase 10 — wszystkie locked decyzje D-10-01..11 zaadresowane, milestone visual polish v1.1+ udokumentowany, bundle budget zweryfikowany.

Output: zaktualizowany `src/main.js`, rozszerzony `tests/application.test.js`, manualny smoke checklist verdict, opcjonalny tweak D-10-05 w `src/PressModel.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-CONTEXT.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-01-PLAN.md
@.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-02-PLAN.md
@CLAUDE.md
@src/main.js
@tests/application.test.js

<interfaces>
Z `src/main.js`:
- Application ctor `_init()` sekwencja kluczowa (linie 177-253):
  - linia 178-180: `this.emissiveController = new EmissiveController({...})`
  - linia 184-190: `this.raycastController = new RaycastController({..., emissive: this.emissiveController})`
  - linia 191: `this.tickables.push((dt) => this.raycastController._runHysteresis(dt))`
  - linia 194-198: HighlightManager
  - linia 201-204: EdgeOutlineController
  - linia 209-210: StatusPanel + StepPanel
  - linia 242-253: TooltipManager + po-hoc `raycastController._onHoverChange = ...` (wzorzec po-hoc dla naszego `_onManipulationClick`)
- Application `dispose()` (linie 346-377) — kolejność T-04-14 documented:
  - sessionOverlay → replayDrawer → replayEngine → disclaimerBanner → stepPanel → statusPanel → tooltipManager → confirmModal → helpModal → labelOverlay → keyboardController → audioController → highlightManager → edgeOutlineController → raycastController → emissiveController → pressModel.disposeMaterials → sceneSetup.dispose
- `this.tickables: Array<(dt) => void>` — gsap.ticker callback push pattern

Z `tests/application.test.js`:
- Phase 4 wiring describe (per STATE: 9 asercji) — w tym dispose order spy przez `mock.invocationCallOrder`
- Wzorzec testowania: mock DOM (jsdom), mock canvas, instanciacja `new Application()`; spy na `xxx.dispose` methods; po `app.dispose()` sprawdzić kolejność wywołań przez `expect(a.dispose.mock.invocationCallOrder[0]).toBeLessThan(b.dispose.mock.invocationCallOrder[0])`

Z `src/PressModel.js` (po Plan 01):
- `getInteractables()` baseline 15 — InteractionAnimator iteruje tę mapę i podpina się TYLKO do mesh-y z `userData.poses` (graceful skip dla pozostałych 12)

Z `src/interaction/InteractionAnimator.js` (po Plan 02):
- Konstruktor: `{ interactables: Map<string, THREE.Mesh> }`
- `handleClick(meshId, mesh): void`
- `dispose(): void` — kill tweens + clear Maps
- `getCurrentPose(meshId): string|null` (test helper)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stuby — rozszerzenie application.test.js o animator wiring + dispose order (RED state)</name>
  <files>tests/application.test.js</files>
  <read_first>
    - tests/application.test.js (cała — wzorzec Phase 4 wiring describe + dispose order spy z mock.invocationCallOrder)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md (sekcja "Application wiring" Code Examples)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-VALIDATION.md (Per-Task Verification Map row "10-03-* wiring")
    - src/main.js (sekcje ctor 177-253 + dispose 346-377)
    - src/interaction/InteractionAnimator.js (po Plan 02)
  </read_first>
  <behavior>
    `tests/application.test.js` — dorzucić describe `'Phase 10 InteractionAnimator wiring'`:
    - Test 1 (wiring presence): po `new Application()` instancja `app.interactionAnimator` istnieje, jest instanceof `InteractionAnimator`
    - Test 2 (callback wired): `app.raycastController._onManipulationClick` jest funkcją (NIE null po Plan 03 — Plan 02 zostawia null jako default)
    - Test 3 (callback delegates): wywołanie `app.raycastController._onManipulationClick('oslona-przednia', guardMesh)` woła `app.interactionAnimator.handleClick(...)` (spy na handleClick)
    - Test 4 (construction order): w mock.invocationCallOrder ctor lub odczyt definition order: raycastController stworzony PRZED interactionAnimator (animator może użyć raycastController, ale per RESEARCH wiring tylko po-hoc — niemniej weryfikacja że animator ctor po raycast)
    - Test 5 (dispose order T-04-14 extension): spy na `interactionAnimator.dispose`, `raycastController.dispose`, `emissiveController.dispose`; po `app.dispose()` asercja: `animator.dispose.mock.invocationCallOrder[0] < raycast.dispose.mock.invocationCallOrder[0] < emissive.dispose.mock.invocationCallOrder[0]`
    - Test 6 (dispose safety): podwójne `app.dispose()` nie throw (jeśli istniejący Phase 4 wzorzec na to pozwala — sprawdzić istniejące testy; jeśli nie, pominąć)

    Pattern stub'owania: użyj `vi.spyOn(InteractionAnimator.prototype, 'dispose')` lub spy bezpośrednio na `app.interactionAnimator.dispose = vi.fn(orig)` zaraz po new Application() (pre-dispose).
  </behavior>
  <action>
    Edytuj `tests/application.test.js` dorzucając describe `'Phase 10 InteractionAnimator wiring (D-10-06/07/08/09 integration)'` z 5-6 testami opisanymi w `<behavior>`.

    Import: `import { InteractionAnimator } from '../src/interaction/InteractionAnimator.js'`. Jeśli istniejący test fil już mockuje moduły, dorzucić mock lub real import (preferred — real, by tests sprawdzały integrację).

    Wszystkie testy będą FAILED (red) — `app.interactionAnimator` jest undefined przed Task 2. Dispose order spy też failuje bo nie ma w chain.

    Commit: `test(10-03): RED stubs for Application animator wiring + dispose order`.
  </action>
  <verify>
    <automated>npx vitest run tests/application.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 10 InteractionAnimator" tests/application.test.js` >= 1
    - `grep -c "interactionAnimator" tests/application.test.js` >= 4 (multiple test refs)
    - Nowe testy FAILED przy uruchomieniu (app.interactionAnimator undefined)
    - Istniejące Phase 4 dispose order testy NIE zmienione (regresja zero)
  </acceptance_criteria>
  <done>application.test.js rozszerzony o Phase 10 wiring describe; nowe testy RED; pozostałe testy w pliku bez zmian.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wiring InteractionAnimator w Application (GREEN state)</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js (cała sekcja ctor 1-260 + dispose 346-377)
    - .planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-RESEARCH.md (sekcja "Application wiring (main.js)" Code Example)
    - src/interaction/InteractionAnimator.js (Plan 02)
    - tests/application.test.js (Task 1 — asercje do spełnienia)
  </read_first>
  <action>
    Modyfikacje w `src/main.js`:

    A. Import (sekcja importów na górze pliku): `import { InteractionAnimator } from './interaction/InteractionAnimator.js';` Komentarz: "Phase 10 D-10-06: klik-driven manipulation animator (oslona-przednia + dzwignia-sprzegla; architektura otwarta dla wylacznik-glowny)."

    B. W ctor `_init()` (lub równoważnym, koło linii 191 po `this.tickables.push((dt) => this.raycastController._runHysteresis(dt))` ALE przed TooltipManager linia 242):
      - Stworzyć `this.interactionAnimator = new InteractionAnimator({ interactables: this.pressModel.getInteractables() });`
      - Po-hoc callback assign: `this.raycastController._onManipulationClick = (id, mesh) => { this.interactionAnimator.handleClick(id, mesh); };`
      - Komentarz polski: "Phase 10 D-10-09: RaycastController emituje (id, mesh) dla mesh z userData.poses; animator filtruje per `userData.poses` i tweenuje pivot.rotation. Brak coupling do trainingStore (D-10-07)."

    C. W `dispose()` (linia 346-377) — dorzucić `if (this.interactionAnimator) this.interactionAnimator.dispose();` PRZED `if (this.raycastController) this.raycastController.dispose();` (linia 373):
      - Pozycja: po `if (this.edgeOutlineController) this.edgeOutlineController.dispose();` (linia 372), przed raycastController
      - Komentarz polski: "Phase 10 — animator dispose PRZED raycast: zatrzymuje GSAP timelines piszące do pivot.rotation, czyści Mapy. Raycast `_onManipulationClick` callback ref clearuje się w cleanup zewnętrznym (GC), ale animator dispose zapobiega ghost tween po HMR."

    Po tych zmianach testy z Task 1 zielenieją.

    Commit: `feat(10-03): wire InteractionAnimator w Application — ctor + callback + dispose order`.
  </action>
  <verify>
    <automated>npx vitest run tests/application.test.js tests/InteractionAnimator.test.js tests/RaycastController.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "new InteractionAnimator" src/main.js` === 1
    - `grep -c "_onManipulationClick" src/main.js` >= 1 (po-hoc assign)
    - `grep -c "interactionAnimator.*dispose\\|interactionAnimator) this.interactionAnimator.dispose" src/main.js` >= 1
    - `grep -c "InteractionAnimator" src/main.js` >= 2 (import + new)
    - W `dispose()`: linia `interactionAnimator.dispose()` POPRZEDZA linię `raycastController.dispose()` (sprawdzić przez `grep -n` — numer linii animator < numer linii raycast w bloku dispose())
    - `npx vitest run tests/application.test.js` — Phase 10 wiring describe GREEN (5-6 nowych testów); istniejący Phase 4 dispose order test NADAL GREEN (nie złamaliśmy go)
    - `npx vitest run` (full suite) — całość GREEN
  </acceptance_criteria>
  <done>Application wpina InteractionAnimator, po-hoc callback działa, dispose order animator → raycast → emissive zachowany; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manualny smoke gate — Phase 10 visual verification + D-10-05 / opacity tweak decyzje</name>
  <what-built>
    - Plan 01: matGuardOrange transparent (opacity 0.5), shaftAxis explicit center, 2 kołnierze wał↔mimośród (flange ±0.575,0,0), 1 czop mimośród↔korbowód (pin @ 0,r,0), wspornik dźwigni (BoxGeometry @ -2.5,7,0.5)
    - Plan 02: InteractionAnimator (klik-driven, 0.4s power2.inOut tween) + RaycastController click channel
    - Plan 03 (Task 2): Application wpina animator po-hoc callback + dispose order
    - Tests: ~30 nowych asercji Phase 10 wszystkie GREEN; baseline 777 zachowany
    - Bundle: oczekiwany impact ~+3-5 KB (1 nowa klasa ~70 linii + 4 nowe meshe geometrii); main bundle musi pozostać < 850 KB hard limit
  </what-built>
  <how-to-verify>
    1. **Bundle budget gate (TEST-08):**
       - Uruchom: `npm run build`
       - Sprawdź rozmiar: `dist/assets/index-*.js` — odczytaj wartość lub użyj komendy systemowej (`Get-Item dist/assets/index-*.js | Select Length` w PowerShell, lub `dir dist/assets`)
       - **Próg:** main bundle < 850 KB (Phase 9 baseline 780.21 KB; headroom ~70 KB). Jeśli przekroczone — fail gate, zgłoś rozmiar.

    2. **Uruchom dev server:**
       - `npm run dev` → otwórz http://localhost:5173/
       - Sprawdź console — zero errors/warnings (Phase 4 invariant)

    3. **Smoke checklist (D-10-01/02 transparency):**
       - [ ] Osłona przednia jest półprzezroczysta (widać przez nią suwak, korbowód, mimośród)
       - [ ] Mechanizm wewnątrz osłony jest CZYTELNY pod różnymi kątami kamery (orbituj wokół frontu prasy)
       - [ ] Hover osłony — flash żółty/szary (HOVER_HINT 0x222222 lift) widoczny mimo transparency
       - [ ] Trigger flash błędu (klik na zły mesh w scenariuszu uruchomienie) — czerwony pulse Wong #D55E00 widoczny na osłonie (D-10-02; akceptujemy ~50% attenuację per Pitfall 1)
       - [ ] Jeśli opacity 0.5 wygląda źle — zmień w `src/PressModel.js` na 0.4 lub 0.6 (zakres [0.35, 0.6] dyskrecjonalny); commit `fix(10-03): opacity tweak osłony do <wartość>`

    4. **Smoke checklist (D-10-04 łączniki wału + D-10-03 shaftAxis center):**
       - [ ] Uruchom symulator (slider RPM > 0); orbituj kamerą wokół wału
       - [ ] 2 kołnierze flankujące eccentric (po obu stronach mimośrodu wzdłuż X) — widoczne, rotują z wałem
       - [ ] Czop mimośród↔korbowód sterczy z boków głowicy korbowodu (oś X świata, niezmienniczo pod rotacją wału)
       - [ ] Brak gap / Z-fighting między kołnierzami a shaftem (R kołnierza 0.5 > R shaft 0.4 = bezpieczne)
       - [ ] Wał wizualnie wycentrowany (brak dryfu X/Z pod cyklem)
       - [ ] D-10-05 decision: czy łożyska wyglądają mizernie?
         - **Jeśli TAK** → w `src/PressModel.js _buildBearings()` zmień R 0.6 → 0.75 (lub dorzuć kontrastowy kołnierz `CylinderGeometry(0.85, 0.85, 0.1)`); commit `fix(10-03): bump bearings R per D-10-05`. Update KIN-01 invariant tests jeśli ANCHOR-02 floor test ich dotyczy.
         - **Jeśli NIE** → zaznacz w resume signal "bearings OK, no tweak"

    5. **Smoke checklist (D-10-06/07/08/09 animacje):**
       - [ ] Klik na osłonę przednią → płynna animacja 0.4s do open (rotacja parent.rotation.x do -π/2); kolejny klik → płynna do closed (rot.x do 0)
       - [ ] Klik na dźwignię sprzęgła → płynna animacja 0.4s do engaged (rot.z 0.7); kolejny klik → do released (rot.z 0)
       - [ ] Spam-klik (5x w jednej sekundzie) na osłonę → brak jitter; animacja "queue-free" (drugi klik podczas tween = no-op)
       - [ ] OPCJONALNIE — klik na wyłącznik główny tweenuje knob.rotation.z (rot.z 0 → π/2) — architecture-by-design, nie testowane
       - [ ] Hover + klik kombinacja: hover żółty/szary, klik wyzwala tween — oba kanały działają niezależnie

    6. **Smoke checklist (D-10-10/11 wspornik dźwigni):**
       - [ ] Wspornik łączący ramę (~x=-2) z podstawą dźwigni (-3, 7, 0.5) — widoczny
       - [ ] Wspornik NIE wisi w powietrzu (siedzi na y=7, łączy obudowę i lever base)
       - [ ] Wspornik NIE jest klikalny (raycast trafia w dźwignię lub w nic, NIE w wspornik) — sprawdź klikając w środek wspornika; tooltip nie pojawia się, animacja nie startuje
       - [ ] Wspornik jest statyczny pod cyklem wału (KIN-01 static)

    7. **Smoke checklist (regresja Phase 7/8/9):**
       - [ ] Scenariusz "uruchomienie" do końca przechodzi bez błędów (Phase 1-6 functional)
       - [ ] Bundle budget zaznaczony powyżej (TEST-08)
       - [ ] Console clean (zero errors / unhandled rejections)
       - [ ] Vite HMR: zmień opacity w src/PressModel.js (live), zapisz; brak GSAP "tween on disposed mesh" warning; aplikacja kontynuuje pracę

    8. **Final verification:** Uruchom `npx vitest run` ostatni raz — pełna suite green (estymata ~810 testów).
  </how-to-verify>
  <resume-signal>
    Napisz "approved" jeśli wszystkie smoke punkty PASS i bundle < 850KB; w przeciwnym razie opisz failed obserwacje z numerem checkpoint'a (np. "D-10-04 czop nie widać pod kątem +Z", "bundle 870KB — overflow"). Dla D-10-05 i opacity tweak: dorzuć wartość ("bearings R 0.6 OK / bumped to 0.75", "opacity 0.5 OK / changed to 0.45").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application lifecycle (init/dispose) | InteractionAnimator wpięty w Vite HMR przez Application.dispose() — ghost tween prevention |
| Manual smoke verdict | Operator (user) ocenia visual fidelity — subjective gate, blocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Denial of Service | HMR ghost tween (animator nie disposed → tween na unmounted mesh) | mitigate | Plan 03 Task 2 wpina `interactionAnimator.dispose()` PRZED `raycastController.dispose()` w Application.dispose; test 5 application.test.js dowodzi przez `mock.invocationCallOrder` |
| T-10-09 | Tampering (bundle bloat) | Phase 10 dodaje >70 KB code → przekracza 850 KB hard limit (TEST-08) | mitigate | Estymata 3-5 KB (1 klasa ~70 linii + 4 meshe geometrii inline); Task 3 smoke gate weryfikuje rzeczywisty rozmiar; jeśli overflow — split InteractionAnimator na lazy-import lub odroczenie D-10-04 łączników do v1.2 |
| T-10-10 | Information Disclosure | Console.log w InteractionAnimator (debug leak) | mitigate | Boundary scanner (Plan 02) + smoke checklist krok 7 "Console clean" — manualne potwierdzenie |
</threat_model>

<verification>
- `npm run build` — sukces; main bundle < 850 KB
- `npx vitest run` — full suite green (~810 testów; 777 baseline + 30 z Phase 10)
- `npm run dev` smoke verdict: approved (lub explicit tweak D-10-05 / opacity z follow-up commitem)
- `tests/application.test.js` Phase 10 wiring describe GREEN (5-6 nowych asercji)
- Dispose order (T-04-14 extension): animator → raycast → emissive
</verification>

<success_criteria>
- D-10-01..11 wszystkie zaadresowane (Plan 01 + 02 + 03 łącznie)
- Smoke verdict: visual fidelity OK (lub tweak applied)
- D-10-05 decision logged (bearings tweak yes/no z wartością)
- Bundle budget: main bundle < 850 KB (TEST-08 hard limit)
- Full suite green (~810 testów)
- Phase 10 ZAMKNIĘTA — gotowa do `/gsd:verify-work 10`
</success_criteria>

<output>
Po wszystkich taskach utwórz `.planning/phases/10-poprawki-wizualne-mechanizmu-i-animacje-os-on-przezroczysto-/10-03-SUMMARY.md` z sekcjami:
- Wiring zmiany w src/main.js (ctor + dispose)
- Application.test.js extension (5-6 nowych asercji + dispose order)
- Smoke verdict (per checklist 1-8): PASS / FAIL per punkt + tweaks applied (D-10-05, opacity)
- Bundle size: actual KB / 850 KB budget
- Phase 10 close metrics: liczba testów dodanych (Plan 01+02+03), commit hashes, files modified
- Next steps: `/gsd:verify-work 10` → ROADMAP update Phase 10 ✓
</output>
