---
phase: 15-startmenu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/pl.js
  - src/ui/StartMenuOverlay.js
  - style.css
  - tests/StartMenuOverlay.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [MENU-01, MENU-02]
must_haves:
  truths:
    - "StartMenuOverlay shows 3 mode cards (Swobodny/Nauka/Egzamin) each with title + description when showStartMenu is true"
    - "Selecting a card and clicking Rozpocznij calls setMode(selectedMode), hideMenu(), and writes pm300:start-menu-shown:v1='true'"
    - "A card shows 'Ostatnia sesja: {score}/100 pkt, {date}' when pm300:last-session:<mode>:v1 exists; absent (not error) when missing or corrupt"
    - "Overlay visibility is driven by store.showStartMenu via root.style.display (block/none) — never dialog.showModal()"
  artifacts:
    - path: "src/ui/StartMenuOverlay.js"
      provides: "Store-driven start-menu overlay (lifecycle: _build/_wireSubscribers/_render/dispose)"
      contains: "class StartMenuOverlay"
      min_lines: 100
    - path: "tests/StartMenuOverlay.test.js"
      provides: "Unit coverage for MENU-01/02 behaviors"
      contains: "describe"
    - path: "src/i18n/pl.js"
      provides: "pl.startMenu.* string namespace"
      contains: "startMenu"
  key_links:
    - from: "src/ui/StartMenuOverlay.js"
      to: "store.showStartMenu"
      via: "subscribe selector + _render display toggle"
      pattern: "showStartMenu"
    - from: "src/ui/StartMenuOverlay.js"
      to: "localStorage pm300:last-session"
      via: "try/catch getItem in _renderLastSession"
      pattern: "pm300:last-session"
    - from: "src/ui/StartMenuOverlay.js"
      to: "pl.startMenu"
      via: "textContent writes"
      pattern: "pl\\.startMenu"
---

<objective>
Create `StartMenuOverlay` — a store-driven, full-screen mode-selection overlay shown when `store.showStartMenu` is true. Three mode cards (Swobodny/Nauka/Egzamin) with descriptions from `pl.startMenu.*`; selecting a card + "Rozpocznij" applies the mode and dismisses the menu; each card shows a last-session indicator read lazily from localStorage with graceful absence. Visibility is controlled by `root.style.display`, NOT `dialog.showModal()` (LOCKED decision — canvas must stay interactive under the menu).

Purpose: Deliver the user-facing entry screen (MENU-01) and per-mode last-session indicators (MENU-02) as a self-contained, store-subscribing component with no `main.js` wiring yet (wiring is Plan 15-02).
Output: `src/ui/StartMenuOverlay.js`, `pl.startMenu.*` keys, `.start-menu__*` CSS, `tests/StartMenuOverlay.test.js`, boundaries entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-startmenu/15-CONTEXT.md
@.planning/phases/15-startmenu/15-PATTERNS.md
@.planning/phases/15-startmenu/15-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Store API (src/state/trainingStore.js) — actions live on getState(): -->
<!--   showStartMenu: boolean (default false) -->
<!--   showMenu(): set showStartMenu=true   hideMenu(): set showStartMenu=false -->
<!--   setMode(next): 'free' | 'nauka' | 'egzamin' (no-op during active egzamin session) -->
<!--   mode: 'free' | 'nauka' | 'egzamin' -->
<!--   subscribe(selector, fn): from Zustand subscribeWithSelector -->
<!-- Lifecycle analog: src/ui/ElementInfoOverlay.js constructor({ store, rootElementId }) -->
<!--   builds DOM, this._unsubscribers=[], _build/_wireSubscribers/_render, dispose() unsubs+removes -->
<!-- localStorage try/catch analog: src/DisclaimerBanner.js _readPersisted/_writePersisted -->
<!-- Existing mode labels available: pl.ui.modeLabel.{free,nauka,egzamin} (src/i18n/pl.js) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pl.startMenu strings + boundaries entry + Wave 0 test scaffold</name>
  <read_first>
    - src/i18n/pl.js (existing pl.ui.* namespace block, flat-key style)
    - tests/boundaries.test.js lines 118-144 (FORBIDDEN_PAIRS, ElementInfoOverlay entry as analog)
    - .planning/phases/15-startmenu/15-RESEARCH.md "pl.js Strings to Add" section
  </read_first>
  <files>src/i18n/pl.js, tests/boundaries.test.js, tests/StartMenuOverlay.test.js</files>
  <action>
    Add a new top-level `startMenu` namespace to the `pl` object in `src/i18n/pl.js` (place after the `ui` block) with Polish keys: `title`, `subtitle`, `startButton` ('Rozpocznij'), `changeModeButton` ('Zmień tryb'), `freeTitle`/`freeDesc`, `naukaTitle`/`naukaDesc`, `egzaminTitle`/`egzaminDesc`, `lastSessionPrefix` ('Ostatnia sesja: '), `lastSessionPts` ('pkt'). Use proper Polish diacritics; wording per 15-RESEARCH.md proposal.
    Add a FORBIDDEN_PAIRS entry to `tests/boundaries.test.js` for `src/ui/StartMenuOverlay.js` mirroring the ElementInfoOverlay entry, with mustNotImport: ['three', 'gsap', '@floating-ui/dom', '../training/', './training/', '../highlight/', './highlight/', '../data/', './data/'] (StartMenuOverlay needs only DOM + store + i18n).
    Create Wave 0 scaffold `tests/StartMenuOverlay.test.js`: import StartMenuOverlay from '../src/ui/StartMenuOverlay.js'; build a mock store object whose `getState()` returns `{ showStartMenu, mode, showMenu, hideMenu, setMode }` and a `subscribe(selector, fn)` recording unsub fns. In `beforeEach` set `document.body.innerHTML = '<div id="start-menu-container"></div>'` and `localStorage.clear()`. Write the describe blocks and `it.todo`/failing-then-implemented tests covering: (a) 3 cards rendered with pl.startMenu titles when showStartMenu true; (b) root display 'none' when showStartMenu false, 'block' when true; (c) clicking a card then Rozpocznij calls setMode(selected) + hideMenu() + localStorage 'pm300:start-menu-shown:v1' === 'true'; (d) card shows last-session text when 'pm300:last-session:free:v1' is set to JSON {score:85,date:'2026-06-12'}; (e) no indicator and NO throw when key absent or corrupt; (f) dispose() removes container and clears subscribers. Tests must FAIL initially (StartMenuOverlay does not exist yet) — this is the RED scaffold.
    No fenced code in production files. All Polish strings live ONLY in pl.js.
  </action>
  <acceptance_criteria>
    - `grep -c "startMenu" src/i18n/pl.js` >= 1 and keys `startButton`, `changeModeButton`, `lastSessionPrefix` present
    - `grep -c "StartMenuOverlay" tests/boundaries.test.js` >= 1
    - `tests/StartMenuOverlay.test.js` exists with `describe` and the six behaviors above
    - `npm test -- tests/StartMenuOverlay.test.js` runs and FAILS (overlay not yet implemented) — RED state confirmed
    - `npm test -- tests/boundaries.test.js` exits 0 (new entry valid; file does not exist yet is tolerated by scanner — confirm scanner behavior, otherwise create empty stub in Task 2 order)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
  </verify>
  <done>pl.startMenu keys added; boundaries entry added; failing StartMenuOverlay test scaffold committed (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement StartMenuOverlay class + glassmorphism CSS</name>
  <read_first>
    - src/ui/ElementInfoOverlay.js (constructor, _build, _wireSubscribers, _render, dispose — full lifecycle analog)
    - src/DisclaimerBanner.js lines 99-112 (_readPersisted/_writePersisted try/catch localStorage)
    - src/ui/StatusPanel.js lines 238-241 (textContent XSS-safe writes)
    - style.css (existing .modal-card / .status-panel glassmorphism rules: background rgba, backdrop-filter blur, border)
    - .planning/phases/15-startmenu/15-PATTERNS.md "src/ui/StartMenuOverlay.js" assignments
  </read_first>
  <behavior>
    - showStartMenu false → root.style.display === 'none'; true → 'block'
    - 3 cards rendered, each with title + description from pl.startMenu (via textContent)
    - Card click sets selected state (.start-menu__card--selected) and tracks this._selectedMode
    - Rozpocznij with a selected mode → getState().setMode(selected) + getState().hideMenu() + localStorage.setItem('pm300:start-menu-shown:v1','true') (try/catch)
    - _renderLastSession(mode) reads pm300:last-session:<mode>:v1, parses JSON in try/catch, returns formatted string or null (graceful absence on missing/corrupt)
    - dispose() removes card/button listeners, calls all unsubscribers, removes container; no throw on double dispose
  </behavior>
  <files>src/ui/StartMenuOverlay.js, style.css</files>
  <action>
    Create `src/ui/StartMenuOverlay.js` (per MENU-01/02). Import ONLY `{ pl } from '../i18n/pl.js'` — no THREE/gsap/training/highlight/data imports (boundary). Class `StartMenuOverlay`, constructor `{ store, rootElementId = 'start-menu-container' }`: store ref, `this._root = document.getElementById(rootElementId)`, throw `StartMenuOverlay: brak #${rootElementId} w DOM` if absent, `this._unsubscribers = []`, `this._selectedMode = null`, then `_build()` / `_wireSubscribers()` / `_render()`.
    `_build()`: create a `<div class="start-menu__overlay">` appended to root with ONE static innerHTML skeleton (literals only — title h2, subtitle, a `.start-menu__cards` container, a `.start-menu__start-btn` button). After innerHTML, set static text via textContent (pl.startMenu.title/subtitle/startButton). Build 3 card elements for modes ['free','nauka','egzamin'] with `.start-menu__card`, `.start-menu__card-title`, `.start-menu__card-desc`, `.start-menu__last-session` children; fill title/desc via textContent from pl.startMenu (freeTitle/freeDesc, naukaTitle/naukaDesc, egzaminTitle/egzaminDesc). Bind one click handler per card (stored refs `this._onCardClick` map or array) that sets `this._selectedMode` and toggles `.start-menu__card--selected`. Bind `this._onStartClick` on start button → if `this._selectedMode` set: `this._store.getState().setMode(this._selectedMode)`, `this._store.getState().hideMenu()`, write 'pm300:start-menu-shown:v1'='true' in try/catch.
    `_wireSubscribers()`: push `this._store.subscribe((s) => s.showStartMenu, () => this._render())` and `this._store.subscribe((s) => s.mode, () => this._render())`.
    `_render()`: `const { showStartMenu } = this._store.getState(); this._root.style.display = showStartMenu ? 'block' : 'none'; if (!showStartMenu) return; this._updateCards();` — do NOT use dialog.showModal/close/setAttribute('open').
    `_updateCards()`: for each mode card, compute `_renderLastSession(mode)`; if non-null set `.start-menu__last-session`.textContent and show it, else clear textContent / hide it (no error when missing).
    `_renderLastSession(mode)`: read `pm300:last-session:${mode}:v1`, try/catch around getItem + JSON.parse; on success return `${pl.startMenu.lastSessionPrefix}${score}/100 ${pl.startMenu.lastSessionPts}, ${date}`; on missing/corrupt return null.
    `dispose()`: remove all stored card listeners + start listener; run every unsubscriber; clear array; `this._container?.remove()`. Guard against double-dispose.
    Add `.start-menu__*` rules to `style.css`: `#start-menu-container`/`.start-menu__overlay` `position: fixed; inset: 0; z-index: 200` (above canvas, below modal-container z-index 300); glassmorphism copied from existing `.modal-card` (rgba background, backdrop-filter blur, 1px rgba border); `.start-menu__card`, `.start-menu__card--selected` (highlighted border/background), `.start-menu__card-title/-desc/-last-session`, `.start-menu__start-btn`. Polish only via pl.js — no Polish literals in JS.
    NEVER write localStorage data via innerHTML — textContent only (XSS / V5 mitigation).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/StartMenuOverlay.test.js` exits 0 (all six behaviors green — GREEN state)
    - `grep -c "showModal\|\.close()\|setAttribute('open'" src/ui/StartMenuOverlay.js` === 0 (visibility via display only)
    - `grep -vE '^\s*//' src/ui/StartMenuOverlay.js | grep -cE "from 'three'|from 'gsap'|/training/|/highlight/|/data/"` === 0
    - `npm test -- tests/boundaries.test.js` exits 0 (StartMenuOverlay passes forbidden-import scan)
    - `grep -c "innerHTML" src/ui/StartMenuOverlay.js` reflects only the single static skeleton (no dynamic/localStorage innerHTML)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/StartMenuOverlay.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>StartMenuOverlay renders 3 cards, toggles visibility via display, applies mode on Rozpocznij, shows graceful last-session indicators; CSS glassmorphism added; boundary clean; all unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → DOM | Untrusted persisted JSON (`pm300:last-session:<mode>:v1`) read and displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `_renderLastSession` JSON.parse of localStorage | mitigate | Wrap getItem + JSON.parse in try/catch; return null on corrupt (graceful absence); never Object.assign parsed onto globals |
| T-15-02 | Tampering | last-session indicator DOM write | mitigate | Render score/date via `textContent` only — never `innerHTML` with localStorage data (analog DisclaimerBanner/StatusPanel) |
| T-15-03 | Spoofing | localStorage score injection | accept | Score is cosmetic display only (`<n>/100 pkt`); no business logic reads it |
</threat_model>

<verification>
- `npm test -- tests/StartMenuOverlay.test.js tests/boundaries.test.js` exits 0
- No `dialog.showModal`/`close`/`open` usage in StartMenuOverlay
- No forbidden imports in StartMenuOverlay (boundaries green)
- All dynamic/localStorage content rendered via textContent
</verification>

<success_criteria>
- StartMenuOverlay class exists with full lifecycle and display-based visibility (MENU-01)
- 3 mode cards with descriptions + graceful last-session indicators (MENU-02)
- pl.startMenu.* strings added; zero Polish literals in JS
- boundaries entry green; unit tests green
</success_criteria>

<output>
Create `.planning/phases/15-startmenu/15-01-SUMMARY.md` when done
</output>
