---
phase: 15-startmenu
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - index.html
  - src/main.js
  - src/ui/StatusPanel.js
  - tests/application.test.js
autonomous: false
requirements: [MENU-01, MENU-02, MENU-03]
must_haves:
  truths:
    - "On first launch (no pm300:start-menu-shown:v1 key) the app bootstraps showStartMenu=true BEFORE StartMenuOverlay is constructed; returning visitor (key='true') boots with menu hidden"
    - "When session.finishedAt transitions null→timestamp, a subscriber writes pm300:last-session:<mode>:v1 = JSON {score, date} for the current mode"
    - "StatusPanel has a 'Zmień tryb' button that calls store.getState().showMenu(); removed in dispose()"
    - "GSAP ticker still advances currentAngle while showStartMenu=true (activeModal===null) — MENU-03 holds"
    - "All existing application.test.js describe blocks construct Application without throwing (start-menu-container present + first-launch suppressed)"
  artifacts:
    - path: "index.html"
      provides: "#start-menu-container mount point"
      contains: "start-menu-container"
    - path: "src/main.js"
      provides: "first-launch bootstrap + StartMenuOverlay instantiation + last-session subscriber + dispose"
      contains: "StartMenuOverlay"
  key_links:
    - from: "src/main.js bootstrap"
      to: "store.setState showStartMenu"
      via: "IIFE try/catch read of pm300:start-menu-shown:v1 before new StartMenuOverlay"
      pattern: "start-menu-shown"
    - from: "src/main.js subscriber"
      to: "localStorage pm300:last-session"
      via: "subscribe session.finishedAt null→ts"
      pattern: "pm300:last-session"
    - from: "src/ui/StatusPanel.js"
      to: "store.showMenu"
      via: "Zmień tryb button click handler"
      pattern: "showMenu"
---

<objective>
Wire `StartMenuOverlay` into the application: add `#start-menu-container` to `index.html`; bootstrap `showStartMenu=true` on first launch BEFORE constructing the overlay; instantiate and dispose `StartMenuOverlay`; add a second `session.finishedAt` subscriber that persists `pm300:last-session:<mode>:v1`; add a "Zmień tryb" button to `StatusPanel`; and fix the `application.test.js` DOM-template regression so the full suite stays green.

Purpose: Complete MENU-01 (first-launch entry), MENU-02 (last-session write path), and MENU-03 (re-callable menu, ticker not paused) — the integration that makes Plan 15-01's overlay live. The application.test.js fix is BLOCKING: without it, every Application-constructing test throws on the missing container.
Output: edits to `index.html`, `src/main.js`, `src/ui/StatusPanel.js`, `tests/application.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-startmenu/15-CONTEXT.md
@.planning/phases/15-startmenu/15-PATTERNS.md
@.planning/phases/15-startmenu/15-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- src/main.js Application constructor: localStorage key registry ~lines 48-53; -->
<!--   bootstrap IIFE chain lines 81-133 (read key in try/catch → store.setState before component ctors); -->
<!--   finishedAt persist subscriber lines 214-231 (pushes to this._unsubscribers); -->
<!--   UI ctors ~lines 276 (StatusPanel), 316 (ElementInfoOverlay), 347 (SessionOverlay); -->
<!--   dispose() chain lines 438-475 (reverse construction order). -->
<!-- GSAP ticker: simulationTick reads `activeModal !== null` ONLY (main.js:381) — showStartMenu never read. -->
<!-- Store: getState().showMenu() / hideMenu() / setMode(); state.mode; state.scoring.score; state.session.finishedAt. -->
<!-- src/ui/StatusPanel.js: _build() button innerHTML + querySelector ref + addEventListener; -->
<!--   bound-ref pattern this._onHcClick (~lines 107-112); dispose() removeEventListener (~lines 314-338). -->
<!-- index.html: <div id="modal-container" style="...z-index:300;..."> exists (line 74). -->
<!-- StartMenuOverlay (from Plan 15-01): new StartMenuOverlay({ store }), default rootElementId 'start-menu-container'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add container, bootstrap, instantiate + dispose StartMenuOverlay, last-session subscriber</name>
  <read_first>
    - index.html line 74 (#modal-container element)
    - src/main.js lines 48-53 (key registry), 81-133 (bootstrap IIFE chain), 214-231 (finishedAt subscriber), 270-360 (UI ctors), 438-475 (dispose chain)
    - .planning/phases/15-startmenu/15-PATTERNS.md "src/main.js" bootstrap + subscriber assignments
  </read_first>
  <files>index.html, src/main.js</files>
  <action>
    In `index.html`, add a sibling overlay mount `<div id="start-menu-container"></div>` near `#modal-container` (do NOT set pointer-events:none — the menu must be interactive; CSS from Plan 15-01 sets z-index 200 and display toggling, so leave styling to style.css). It must exist in DOM before Application constructs StartMenuOverlay.
    In `src/main.js`: add `const START_MENU_SHOWN_KEY = 'pm300:start-menu-shown:v1';` to the key registry block (~lines 48-53) with a Phase 15 MENU-01 comment.
    Add the first-launch bootstrap IIFE AFTER the existing `lectorVoiceId` bootstrap block (~line 133) and BEFORE any StartMenuOverlay instantiation: read START_MENU_SHOWN_KEY in try/catch returning boolean (`=== 'true'`); if NOT shown, `this.store.setState({ showStartMenu: true })`. Bootstrap order is a hard constraint (Pitfall 1) — setState must precede `new StartMenuOverlay`.
    Import StartMenuOverlay and instantiate `this.startMenuOverlay = new StartMenuOverlay({ store: this.store });` in the UI-component construction section (alongside StatusPanel/ElementInfoOverlay/SessionOverlay).
    Add a SECOND independent `session.finishedAt` subscriber immediately after the existing `pm300:session:v1` persist subscriber (per MENU-02, orthogonal — Phase 17 must NOT replace it; add comment): on finishedAt !== null read state, `mode = state.mode`, `key = pm300:last-session:${mode}:v1`, `date = new Date(finishedAt).toISOString().slice(0,10)`, write `JSON.stringify({ score: state.scoring.score, date })` in try/catch. Push its unsub to `this._unsubscribers`.
    In `dispose()`, add `if (this.startMenuOverlay) this.startMenuOverlay.dispose();` in reverse construction order (near sessionOverlay/elementInfoOverlay dispose calls).
  </action>
  <acceptance_criteria>
    - `grep -c "start-menu-container" index.html` >= 1
    - `grep -c "START_MENU_SHOWN_KEY\|start-menu-shown" src/main.js` >= 1
    - `grep -c "new StartMenuOverlay" src/main.js` === 1 and `grep -c "startMenuOverlay.dispose" src/main.js` === 1
    - `grep -c "pm300:last-session" src/main.js` >= 1
    - bootstrap setState appears before `new StartMenuOverlay` (manual order check; line-number of setState < line-number of instantiation)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Container present; first-launch bootstrap precedes overlay construction; overlay instantiated + disposed; last-session subscriber writes per-mode JSON; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Zmień tryb" button to StatusPanel + fix application.test.js regression</name>
  <read_first>
    - src/ui/StatusPanel.js _build() button block (~lines 66-162), _onHcClick bound-ref (~lines 107-112), dispose() (~lines 314-338)
    - tests/application.test.js beforeEach DOM templates (lines ~72, 195, 333, 535, 702, 813, 896) + localStorage cleanup blocks (~lines 212-216)
    - .planning/phases/15-startmenu/15-PATTERNS.md "StatusPanel" + "application.test.js" assignments
  </read_first>
  <files>src/ui/StatusPanel.js, tests/application.test.js</files>
  <action>
    StatusPanel (MENU-03 re-call trigger): in `_build()`, add `<button class="status-panel__change-mode" type="button"></button>` to the controls innerHTML block. After the block, capture `this._changeModeBtn = this._root.querySelector('.status-panel__change-mode')`; set `this._changeModeBtn.textContent = pl.startMenu.changeModeButton` (textContent only — Polish via pl.js). Wire bound ref `this._onChangeModeClick = () => { this._store.getState().showMenu(); }` (canonical: showMenu is an action on getState(), not on store directly) and addEventListener. In `dispose()`, add the matching removeEventListener guarded by `if (this._changeModeBtn && this._onChangeModeClick)`.
    application.test.js regression fix (BLOCKING — boot throws otherwise): in EVERY `beforeEach` `document.body.innerHTML` template (7 templates per PATTERNS map), add `<div id="start-menu-container"></div>`. In each `beforeEach` localStorage cleanup block, add `localStorage.setItem('pm300:start-menu-shown:v1', 'true')` so unrelated tests boot with the menu suppressed.
    Add a first-launch test: in a fresh-boot describe (or a new one), `localStorage.removeItem('pm300:start-menu-shown:v1')` before constructing Application, then assert `app.store.getState().showStartMenu === true` after construction (MENU-01 bootstrap).
    Add a MENU-03 ticker assertion: with `showStartMenu: true` and `activeModal: null`, call the simulation tick (mirror existing W7 modal-pause test mechanics) with `omega > 0` and assert `currentAngle` advances (ticker NOT paused by showStartMenu).
  </action>
  <acceptance_criteria>
    - `grep -c "status-panel__change-mode" src/ui/StatusPanel.js` >= 1 and `grep -c "_onChangeModeClick" src/ui/StatusPanel.js` === 2 (wire + dispose)
    - `grep -c "showMenu" src/ui/StatusPanel.js` >= 1
    - `grep -c "start-menu-container" tests/application.test.js` >= 7 (one per template)
    - `grep -c "pm300:start-menu-shown:v1" tests/application.test.js` >= 7 (suppression) plus first-launch removeItem
    - `npm test -- tests/application.test.js` exits 0 (regression fixed + first-launch + ticker assertions green)
    - `npm test -- tests/boundaries.test.js` exits 0 (StatusPanel still clean)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/application.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>StatusPanel "Zmień tryb" button calls showMenu and is disposed; all application.test.js templates carry the container + suppression; first-launch + ticker-not-paused assertions pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>StartMenuOverlay wired into the app: first-launch menu over a live (rotating) simulation; mode cards with last-session indicators; "Rozpocznij" dismisses; "Zmień tryb" in StatusPanel re-opens — all without pausing the 3D ticker.</what-built>
  <how-to-verify>
    1. Clear localStorage key: in devtools run `localStorage.removeItem('pm300:start-menu-shown:v1')`, then reload `npm run dev` (http://localhost:5173/).
    2. Confirm the start menu appears with 3 cards and the simulation is visibly rotating UNDERNEATH the menu (MENU-03 ticker not paused).
    3. Select a mode card (it highlights), click "Rozpocznij" — menu hides, simulator active.
    4. Click "Zmień tryb" in the status panel — menu re-opens without page reload.
    5. Complete a session, return to menu, confirm the card now shows "Ostatnia sesja: {score}/100 pkt, {date}" for that mode.
    6. Confirm existing modals (H = help, element-info on click) still open/close normally (SC#4).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → store/DOM | First-launch flag + last-session JSON read/written across reload boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | first-launch bootstrap getItem | mitigate | Read in try/catch IIFE returning boolean default false; corrupt/blocked storage falls through to no-menu rather than throwing at boot |
| T-15-05 | Tampering | last-session write subscriber setItem | mitigate | Wrap setItem + JSON.stringify in try/catch; quota/private-mode failure is silent (no boot/runtime break) |
| T-15-06 | Denial of Service | dispose leak on HMR | mitigate | startMenuOverlay.dispose() added to chain; last-session subscriber unsub pushed to this._unsubscribers |
</threat_model>

<verification>
- `npm test` full suite green (965 + 1 skipped baseline, plus new tests)
- `npm run build` exits 0 and main bundle < 850 KB (currently 820.34)
- bootstrap setState precedes StartMenuOverlay construction
- GSAP ticker unaffected by showStartMenu (assertion in application.test.js)
</verification>

<success_criteria>
- First-launch entry screen wired (MENU-01); returning visitor skips menu
- Last-session write subscriber persists per-mode score/date (MENU-02)
- "Zmień tryb" re-opens menu without restart; ticker not paused (MENU-03)
- application.test.js regression fixed; full suite green; bundle < 850 KB
</success_criteria>

<output>
Create `.planning/phases/15-startmenu/15-02-SUMMARY.md` when done
</output>
