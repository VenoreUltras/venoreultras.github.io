---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/DisclaimerBanner.js
  - src/SceneSetup.js
  - src/main.js
  - index.html
  - style.css
  - tests/disclaimerBanner.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [UI-05, INFRA-02, INFRA-05, TEST-03]
must_haves:
  truths:
    - "`src/DisclaimerBanner.js` istnieje jako klasa DOM (analog `src/UI.js`); idempotent insert do `document.body` jako pierwsze dziecko"
    - "Banner czyta `pl.disclaimer.full` z `src/i18n/pl.js` i wstrzykuje przez `textContent` (XSS-safe)"
    - "Banner ma stan persistowany w `localStorage` pod kluczem `pm300:disclaimer:collapsed:v1` (D-12)"
    - "Banner ma JSDoc komentarz dokumentujący D-13 interpretację 'widoczny stale' (kod-fence przeciw przyszłemu `dismiss=true`)"
    - "Banner ma `dispose()` które removeEventListener z dokładnie tym samym handler reference"
    - "`SceneSetup` ma WebGL context-loss listeners: `webglcontextlost` woła `event.preventDefault()` + `gsap.ticker.sleep()` + render polskiego overlay; `webglcontextrestored` woła `gsap.ticker.wake()` + ukrycie overlay (INFRA-05)"
    - "`SceneSetup.dispose()` zwalnia oba context-loss listeners + resize listener + renderer.dispose"
    - "`tests/boundaries.test.js` regex-based scanner dla 8 forbidden import pairs FAIL'uje gdy zabroniony import się pojawi (TEST-03, INFRA-02)"
    - "`tests/boundaries.test.js` Polish-literal scanner przeszukuje `src/**/*.js` poza `src/i18n/` i `src/training/scenarios/` na obecność diakrytyków w string literals (UI-06 enforcement)"
    - "`index.html` NIE ładuje `<div id=\"disclaimer-banner\">` ręcznie — banner JS auto-inserts (RESEARCH §G pattern)"
    - "`style.css` (root) zawiera reguły `.disclaimer-banner`, `.disclaimer-banner__bar`, `.disclaimer-banner__toggle`, `.disclaimer-banner__icon`, `.disclaimer-banner__chevron`, `.disclaimer-banner__content`, `.disclaimer-banner--collapsed` zgodnie z UI-SPEC §"Component Inventory" + §"Color""
    - "Banner używa Wong amber `#E69F00` background (15% opacity); collapsed bar 8px wysoki z `!` icon button (44x44px touch target — WCAG 2.5.5)"
    - "ARIA: `role=\"region\"`, `aria-label`, `aria-expanded`, `aria-controls` ustawione zgodnie z UI-SPEC"
  artifacts:
    - path: "src/DisclaimerBanner.js"
      provides: "UI-05 banner component"
      exports: ["DisclaimerBanner"]
      min_lines: 80
    - path: "src/SceneSetup.js"
      provides: "rozszerzone WebGL context-loss handling (INFRA-05) + Polish overlay"
      contains: "webglcontextlost"
    - path: "src/main.js"
      provides: "instantiated DisclaimerBanner (this.disclaimerBanner = new DisclaimerBanner())"
      contains: "new DisclaimerBanner"
    - path: "index.html"
      provides: "(brak zmian strukturalnych — ewentualne usunięcie martwego markupu, ale nie wymagane)"
    - path: "style.css"
      provides: "DisclaimerBanner styles + WebGL context-loss overlay styles"
      contains: ".disclaimer-banner"
    - path: "tests/boundaries.test.js"
      provides: "INFRA-02 + UI-06 + TEST-03 static enforcement"
      min_lines: 100
    - path: "tests/disclaimerBanner.test.js"
      provides: "UI-05 jsdom unit tests"
      min_lines: 60
  key_links:
    - from: "src/DisclaimerBanner.js"
      to: "src/i18n/pl.js"
      via: "import { pl }"
      pattern: "from '\\./i18n/pl\\.js'"
    - from: "src/main.js"
      to: "src/DisclaimerBanner.js"
      via: "import + instantiate after SceneSetup"
      pattern: "new DisclaimerBanner"
    - from: "src/SceneSetup.js"
      to: "src/i18n/pl.js"
      via: "import { pl } dla overlay text"
      pattern: "pl\\.webgl\\.contextLost"
    - from: "tests/boundaries.test.js"
      to: "src/training/ProcedureEngine.js + 7 innych"
      via: "readFileSync + regex extract imports"
      pattern: "FORBIDDEN_PAIRS"
---

<objective>
Wave 3 finałowy plan Phase 1: zaimplementuj DisclaimerBanner (UI-05), dodaj WebGL context-loss handling (INFRA-05), włóż statyczny boundaries test (INFRA-02 + UI-06 + TEST-03). Po tym planie Phase 1 jest kompletna — wszystkie 21 wymagań spełnione.

Plan 05 zależy od Plan 04 (sekwencyjnie w Wave — Plan 04 dotknął `src/SceneSetup.js` dispose shell + bound resize, Plan 04 dotknął `src/main.js` z `disclaimerBanner = null` placeholder). Plan 05 ROZSZERZA oba pliki: SceneSetup dostaje dwa context-loss listenery (PreventDefault + ticker.sleep/wake + overlay), main.js zamienia `disclaimerBanner = null` na `new DisclaimerBanner()`. Brak konfliktu z Plan 04 — różne sekcje plików.

Decyzja: Plan 05 jest w **Wave 3** (nie Wave 2 sekwencyjnie po Plan 04). Powód: file overlap z Plan 04 wymaga sekwencji; oznaczamy to jako odrębną wave dla jasności w `wave` field. Alternatywa: Wave 2 z `depends_on: ["01-04"]` (oba w wave 2 ale sekwencyjne) — zgodnie z guidance "Same-wave plans must have zero `files_modified` overlap. After assigning waves, scan each wave; if any file appears in 2+ plans, bump the later plan to the next wave." → file overlap → Plan 05 = Wave 3.

Purpose: UI-05 disclaimer cementuje disclaimer-mindset Phase 1 (CRIT-1 prevention; Phase 6 PDF stopka dziedziczy). INFRA-02/TEST-03 boundaries chroni warstwowość projektu na zawsze (Phase 2-6 nie może przypadkiem złamać granic ProcedureEngine). INFRA-05 chroni przed background-tab WebGL context loss (CRIT-5 prevention).

Output: 7 plików (1 nowy banner + 1 nowy boundary test + 1 nowy disclaimer test + 4 modyfikacje source/style/html). Po tym planie `npm test` zielony, banner widoczny w `npm run dev`, boundaries scanner łapie zabronione importy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.planning/phases/01-foundation/01-04-SUMMARY.md

<interfaces>
<!-- DisclaimerBanner full source RESEARCH §G linie 1163–1265 — verbatim do skopiowania -->

```javascript
// DisclaimerBanner key methods:
class DisclaimerBanner {
  constructor()                  // tworzy DOM idempotentnie + reads localStorage + sets initial state
  _create()                      // buduje root element (button + chevron + content textContent)
  toggle()                       // flip aria-expanded + add/remove .disclaimer-banner--collapsed + persist
  _setExpanded(boolean)          // applies state (aria + class + title)
  _readPersisted(): boolean      // try/catch localStorage
  _writePersisted(boolean): void // try/catch localStorage
  dispose()                      // removeEventListener('click', this._onToggleClick)
}
const STORAGE_KEY = 'pm300:disclaimer:collapsed:v1';
```

<!-- SceneSetup po Plan 04 — current state -->
```javascript
// src/SceneSetup.js po Plan 04:
// - constructor: this._onWindowResizeBound = this.onWindowResize.bind(this); window.addEventListener('resize', this._onWindowResizeBound);
// - dispose() { window.removeEventListener('resize', this._onWindowResizeBound); this.renderer.dispose(); }

// Plan 05 doda:
// - constructor: this._onContextLost = (e) => { e.preventDefault(); gsap.ticker.sleep(); this._showOverlay(); };
//                this._onContextRestored = () => { gsap.ticker.wake(); this._hideOverlay(); };
//                this.renderer.domElement.addEventListener('webglcontextlost', this._onContextLost, false);
//                this.renderer.domElement.addEventListener('webglcontextrestored', this._onContextRestored, false);
// - _showOverlay() / _hideOverlay() — DOM manipulation (overlay div w document.body)
// - dispose() rozszerzone: usuwa oba context-loss listenery
```

<!-- src/main.js po Plan 04 — current state -->
```javascript
// Plan 04 zostawił: this.disclaimerBanner = null;
// Plan 05 zmienia na: this.disclaimerBanner = new DisclaimerBanner();
// (po `this.ui = new UI();`, przed `this.store = createTrainingStore();`)
//
// Import: import { DisclaimerBanner } from './DisclaimerBanner';
```

<!-- Boundaries test FORBIDDEN_PAIRS (RESEARCH §B linie 680–698 verbatim) -->
```javascript
const FORBIDDEN_PAIRS = [
  { file: 'src/training/ProcedureEngine.js',  mustNotImport: ['three', 'gsap', '../state/', './state/'] },
  { file: 'src/training/ScoringService.js',   mustNotImport: ['three', 'gsap', '../state/', './state/'] },
  { file: 'src/training/scoringWeights.js',   mustNotImport: ['three', 'gsap', '../state/', './state/'] },
  { file: 'src/training/faultRules.js',       mustNotImport: ['three', 'gsap', '../state/', './state/'] },
  { file: 'src/PressModel.js',     mustNotImport: ['../state/', '../training/', './state/', './training/'] },
  { file: 'src/PhysicsEngine.js',  mustNotImport: ['three', 'gsap', '../state/', '../training/'] },
  { file: 'src/SceneSetup.js',     mustNotImport: ['../state/', '../training/'] },
  { file: 'src/UI.js',             mustNotImport: ['three'] },
  { file: 'src/state/trainingStore.js', mustNotImport: ['three', 'gsap'] },
  { file: 'src/DisclaimerBanner.js', mustNotImport: ['three', 'gsap', '../state/', '../training/'] },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utwórz src/DisclaimerBanner.js + style.css updates + tests/disclaimerBanner.test.js</name>
  <files>src/DisclaimerBanner.js, style.css, tests/disclaimerBanner.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "G. DisclaimerBanner.js" (linie 1161–1265) — VERBATIM full file
    - .planning/phases/01-foundation/01-UI-SPEC.md § "Component Inventory" (linie 105–158), § "Color" (linie 78–101), § "Spacing Scale" (linie 38–55), § "Integration Notes for Executor" (linie 200–222)
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-10, D-11, D-12, D-13, D-14
    - .planning/phases/01-foundation/01-PATTERNS.md § "DisclaimerBanner.js" (linie 302–349)
    - src/i18n/pl.js (Plan 02 — `pl.disclaimer.full`/`short`/`ariaLabel`/`toggleExpand`/`toggleCollapse`)
    - src/UI.js (Plan 01 fixed — analog DOM class idiom)
    - style.css (root — istniejące CSS variables `--bg-color`, `--glass-bg`, `--text-main`)
  </read_first>
  <behavior>
    - **Mount**: `new DisclaimerBanner()` w document.body wstawia element jako pierwsze dziecko, idempotentnie (drugi `new DisclaimerBanner()` reuse'uje istniejący element).
    - **Initial state default**: brak localStorage entry → expanded (aria-expanded="true", visible full text).
    - **Initial state persisted collapsed**: localStorage `pm300:disclaimer:collapsed:v1`==='true' → collapsed (aria-expanded="false", `.disclaimer-banner--collapsed` class).
    - **Toggle expand→collapse**: klik chevron button gdy expanded → aria-expanded="false", class added, localStorage set "true".
    - **Toggle collapse→expand**: klik gdy collapsed → aria-expanded="true", class removed, localStorage set "false".
    - **textContent injection**: `pl.disclaimer.full` wstrzyknięte przez `textContent`, NIE `innerHTML` (XSS prevention).
    - **localStorage error handling**: gdy `localStorage.setItem` rzuca (private mode/quota), nie crash — silent skip.
    - **dispose()**: `removeEventListener('click', this._onToggleClick)` z DOKŁADNIE tym samym handler reference (Plan 04 STATE-03 pattern).
    - **D-13 docs comment**: klasa ma JSDoc komentarz cytujący "collapsed state z widoczną ikoną `!` JEST spełnieniem 'widoczny stale'" (zacementowanie przeciw przyszłemu `dismiss=true`).
    - **ARIA**: `role="region"` na root, `aria-label="Zastrzeżenie symulatora"`, button ma `aria-expanded`, `aria-controls`, `aria-label` (toggleExpand/toggleCollapse zależnie od stanu).
    - **Touch target**: button ma minimum 44x44px (CSS).
  </behavior>
  <action>
    1. **Utwórz `src/DisclaimerBanner.js`** (VERBATIM z RESEARCH §G linie 1163–1265). Struktura zgodna z &lt;interfaces&gt; key methods. Krytyczne fragmenty:
       ```javascript
       // src/DisclaimerBanner.js
       //
       // Disclaimer banner (UI-05). LOCKED: D-13 interpretacja — collapsed state
       // z widoczną ikoną `!` JEST spełnieniem "widoczny stale". NIE dodawaj
       // `dismiss=true` — banner ma być permanentnie obecny w DOM.
       //
       // CRIT-1: ten komponent jest jednym z dwóch architektonicznych zabezpieczeń
       // (drugie to PDF stopka w Phase 6) przeciw misinterpretacji symulatora
       // jako substytutu szkolenia BHP. NIE skracaj, NIE ukrywaj, NIE optimizuj
       // w "mniej intruzywny" sposób.

       import { pl } from './i18n/pl.js';

       const STORAGE_KEY = 'pm300:disclaimer:collapsed:v1';   // D-12

       export class DisclaimerBanner {
         constructor() {
           this.root = document.getElementById('disclaimer-banner');
           if (!this.root) {
             this.root = this._create();
             document.body.insertBefore(this.root, document.body.firstChild);
           }
           this.toggleBtn = this.root.querySelector('.disclaimer-banner__toggle');
           this.contentEl = this.root.querySelector('#disclaimer-banner__content');

           const persisted = this._readPersisted();
           this._setExpanded(!persisted);

           this._onToggleClick = () => this.toggle();
           this.toggleBtn.addEventListener('click', this._onToggleClick);
         }

         _create() {
           const root = document.createElement('div');
           root.id = 'disclaimer-banner';
           root.className = 'disclaimer-banner';
           root.setAttribute('role', 'region');
           root.setAttribute('aria-label', pl.disclaimer.ariaLabel);
           root.innerHTML = `
             <div class="disclaimer-banner__bar">
               <button
                 class="disclaimer-banner__toggle"
                 type="button"
                 aria-expanded="true"
                 aria-controls="disclaimer-banner__content">
                 <span class="disclaimer-banner__icon" aria-hidden="true">!</span>
                 <span class="disclaimer-banner__chevron" aria-hidden="true">▾</span>
               </button>
               <div id="disclaimer-banner__content" class="disclaimer-banner__content"></div>
             </div>
           `;
           // textContent — XSS-safe (RESEARCH § Security threat patterns row 1)
           root.querySelector('#disclaimer-banner__content').textContent = pl.disclaimer.full;
           return root;
         }

         toggle() {
           const isExpanded = this.toggleBtn.getAttribute('aria-expanded') === 'true';
           this._setExpanded(!isExpanded);
           this._writePersisted(isExpanded);
         }

         _setExpanded(expanded) {
           this.toggleBtn.setAttribute('aria-expanded', String(expanded));
           this.toggleBtn.setAttribute(
             'aria-label',
             expanded ? pl.disclaimer.toggleCollapse : pl.disclaimer.toggleExpand
           );
           if (expanded) {
             this.root.classList.remove('disclaimer-banner--collapsed');
             this.toggleBtn.removeAttribute('title');
           } else {
             this.root.classList.add('disclaimer-banner--collapsed');
             this.toggleBtn.setAttribute('title', pl.disclaimer.full);
           }
         }

         _readPersisted() {
           try {
             return localStorage.getItem(STORAGE_KEY) === 'true';
           } catch {
             return false;
           }
         }

         _writePersisted(collapsed) {
           try {
             localStorage.setItem(STORAGE_KEY, String(collapsed));
           } catch {
             // ignoruj — private mode / quota exceeded
           }
         }

         /** Zwalnia event listener (STATE-03; wywoływane z Application.dispose()). */
         dispose() {
           this.toggleBtn.removeEventListener('click', this._onToggleClick);
         }
       }
       ```

    2. **Modyfikuj root `style.css`** — dodaj sekcję na końcu pliku (BEZ ruszania istniejących `:root` variables ani innych selectorów):
       ```css
       /* ──────────────────────────────────────────────────────────
          DisclaimerBanner (UI-05) — Phase 1
          Wong amber #E69F00 (Wong palette CRIT-4 prevention)
          ────────────────────────────────────────────────────────── */
       .disclaimer-banner {
         position: sticky;
         top: 0;
         left: 0;
         right: 0;
         z-index: 100;
         background-color: rgba(230, 159, 0, 0.15);
         border-bottom: 1px solid rgba(230, 159, 0, 0.4);
         font-family: var(--font-sans);
         color: var(--text-main);
         pointer-events: auto;
       }

       .disclaimer-banner__bar {
         display: flex;
         align-items: center;
         gap: 16px;
         padding: 8px 16px;
         min-height: 44px;
       }

       .disclaimer-banner__toggle {
         /* WCAG 2.5.5 hard floor — 44x44 touch target */
         min-width: 44px;
         min-height: 44px;
         padding: 4px 8px;
         display: inline-flex;
         align-items: center;
         gap: 4px;
         background: transparent;
         border: 1px solid rgba(230, 159, 0, 0.6);
         border-radius: 4px;
         color: #E69F00;
         font-family: inherit;
         font-size: 14px;
         font-weight: 600;
         cursor: pointer;
       }

       .disclaimer-banner__toggle:hover,
       .disclaimer-banner__toggle:focus-visible {
         background: rgba(230, 159, 0, 0.1);
         outline: 2px solid #E69F00;
         outline-offset: 2px;
       }

       .disclaimer-banner__icon {
         font-weight: 700;
         font-size: 16px;
       }

       .disclaimer-banner__chevron {
         transition: transform 150ms ease;
       }

       .disclaimer-banner__content {
         flex: 1;
         font-size: 14px;
         line-height: 1.5;
       }

       /* Collapsed state — bar 8px, tylko ikona widoczna (D-13 "widoczny stale") */
       .disclaimer-banner--collapsed .disclaimer-banner__bar {
         padding: 0 16px;
         min-height: 8px;
       }

       .disclaimer-banner--collapsed .disclaimer-banner__content {
         display: none;
       }

       .disclaimer-banner--collapsed .disclaimer-banner__chevron {
         transform: rotate(180deg);
       }

       .disclaimer-banner--collapsed {
         border-left: 3px solid #E69F00;
         background-color: rgba(230, 159, 0, 0.6);
       }

       /* ──────────────────────────────────────────────────────────
          WebGL context-loss overlay (INFRA-05) — Phase 1
          ────────────────────────────────────────────────────────── */
       .webgl-overlay {
         position: fixed;
         inset: 0;
         z-index: 200;
         display: flex;
         align-items: center;
         justify-content: center;
         background: rgba(11, 15, 25, 0.92);
         color: var(--text-main);
         font-family: var(--font-sans);
         font-size: 18px;
         text-align: center;
         padding: 24px;
       }

       .webgl-overlay--hidden {
         display: none;
       }
       ```

    3. **Utwórz `tests/disclaimerBanner.test.js`** (jsdom env per `vitest.config.js` `environmentMatchGlobs`):
       ```javascript
       // tests/disclaimerBanner.test.js
       // @vitest-environment jsdom
       // UI-05: DisclaimerBanner mount + toggle + persistence + dispose.

       import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
       import { DisclaimerBanner } from '../src/DisclaimerBanner.js';
       import { pl } from '../src/i18n/pl.js';

       const STORAGE_KEY = 'pm300:disclaimer:collapsed:v1';

       describe('DisclaimerBanner — mount (UI-05)', () => {
         beforeEach(() => {
           document.body.innerHTML = '';
           localStorage.clear();
         });

         it('wstrzykuje element do document.body jako pierwsze dziecko', () => {
           new DisclaimerBanner();
           const el = document.getElementById('disclaimer-banner');
           expect(el).not.toBeNull();
           expect(document.body.firstChild).toBe(el);
         });

         it('idempotent insert — drugi new DisclaimerBanner() nie tworzy duplikatu', () => {
           new DisclaimerBanner();
           new DisclaimerBanner();
           const all = document.querySelectorAll('#disclaimer-banner');
           expect(all).toHaveLength(1);
         });

         it('content używa textContent (XSS-safe) z pl.disclaimer.full', () => {
           new DisclaimerBanner();
           const content = document.getElementById('disclaimer-banner__content');
           expect(content.textContent).toBe(pl.disclaimer.full);
         });

         it('ARIA: role=region + aria-label = "Zastrzeżenie symulatora"', () => {
           new DisclaimerBanner();
           const root = document.getElementById('disclaimer-banner');
           expect(root.getAttribute('role')).toBe('region');
           expect(root.getAttribute('aria-label')).toBe('Zastrzeżenie symulatora');
         });

         it('default state: expanded (aria-expanded=true, brak collapsed class)', () => {
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           expect(btn.getAttribute('aria-expanded')).toBe('true');
           expect(document.getElementById('disclaimer-banner').classList.contains('disclaimer-banner--collapsed')).toBe(false);
         });
       });

       describe('DisclaimerBanner — persistence (D-12)', () => {
         beforeEach(() => {
           document.body.innerHTML = '';
           localStorage.clear();
         });

         it('localStorage "true" → mount jako collapsed', () => {
           localStorage.setItem(STORAGE_KEY, 'true');
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           expect(btn.getAttribute('aria-expanded')).toBe('false');
           expect(document.getElementById('disclaimer-banner').classList.contains('disclaimer-banner--collapsed')).toBe(true);
         });

         it('localStorage "false" → mount jako expanded', () => {
           localStorage.setItem(STORAGE_KEY, 'false');
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           expect(btn.getAttribute('aria-expanded')).toBe('true');
         });

         it('toggle expanded → collapsed zapisuje "true" do localStorage', () => {
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           btn.click();
           expect(btn.getAttribute('aria-expanded')).toBe('false');
           expect(localStorage.getItem(STORAGE_KEY)).toBe('true');
         });

         it('toggle collapsed → expanded zapisuje "false"', () => {
           localStorage.setItem(STORAGE_KEY, 'true');
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           btn.click();
           expect(btn.getAttribute('aria-expanded')).toBe('true');
           expect(localStorage.getItem(STORAGE_KEY)).toBe('false');
         });

         it('aria-label zmienia się przy toggle (toggleExpand ↔ toggleCollapse)', () => {
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           expect(btn.getAttribute('aria-label')).toBe(pl.disclaimer.toggleCollapse);
           btn.click();
           expect(btn.getAttribute('aria-label')).toBe(pl.disclaimer.toggleExpand);
         });

         it('collapsed state ma title attribute z pełnym disclaimer (progressive enhancement)', () => {
           new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           btn.click(); // collapse
           expect(btn.getAttribute('title')).toBe(pl.disclaimer.full);
         });
       });

       describe('DisclaimerBanner — defensive (private mode localStorage)', () => {
         beforeEach(() => {
           document.body.innerHTML = '';
           localStorage.clear();
         });

         it('NIE crashuje gdy localStorage.setItem rzuca', () => {
           const origSet = Storage.prototype.setItem;
           Storage.prototype.setItem = () => { throw new Error('quota'); };
           try {
             new DisclaimerBanner();
             const btn = document.querySelector('.disclaimer-banner__toggle');
             expect(() => btn.click()).not.toThrow();
           } finally {
             Storage.prototype.setItem = origSet;
           }
         });

         it('NIE crashuje gdy localStorage.getItem rzuca → default expanded', () => {
           const origGet = Storage.prototype.getItem;
           Storage.prototype.getItem = () => { throw new Error('access denied'); };
           try {
             new DisclaimerBanner();
             const btn = document.querySelector('.disclaimer-banner__toggle');
             expect(btn.getAttribute('aria-expanded')).toBe('true');
           } finally {
             Storage.prototype.getItem = origGet;
           }
         });
       });

       describe('DisclaimerBanner — dispose (STATE-03)', () => {
         beforeEach(() => {
           document.body.innerHTML = '';
           localStorage.clear();
         });

         it('dispose() removeEventListener — kolejne klik nie zmienia stanu', () => {
           const banner = new DisclaimerBanner();
           const btn = document.querySelector('.disclaimer-banner__toggle');
           banner.dispose();
           // Po dispose, klik nie powinien wpłynąć na aria-expanded
           const before = btn.getAttribute('aria-expanded');
           btn.click();
           expect(btn.getAttribute('aria-expanded')).toBe(before);
         });
       });

       describe('DisclaimerBanner — D-13 code-fence comment', () => {
         it('plik źródłowy zawiera komentarz dokumentujący D-13 interpretację', () => {
           const fs = require('fs');
           const src = fs.readFileSync('src/DisclaimerBanner.js', 'utf-8');
           expect(src).toMatch(/D-13/i);
           expect(src).toMatch(/widoczny stale|dismiss/i);
         });
       });
       ```

  </action>
  <verify>
    <automated>npx vitest run tests/disclaimerBanner.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/DisclaimerBanner.js` istnieje, eksportuje `DisclaimerBanner`.
    - Plik importuje TYLKO `from './i18n/pl.js'`. Brak THREE/gsap/store/training.
    - Plik zawiera komentarz cytujący "D-13" i "widoczny stale" (kod-fence).
    - `tests/disclaimerBanner.test.js` ma ≥14 testów, wszystkie PASS pod jsdom env.
    - Pragma `// @vitest-environment jsdom` na górze testu (lub mapping w `vitest.config.js` z Plan 01 — both work).
    - `style.css` (root) zawiera selectors: `.disclaimer-banner`, `.disclaimer-banner__bar`, `.disclaimer-banner__toggle`, `.disclaimer-banner__icon`, `.disclaimer-banner__chevron`, `.disclaimer-banner__content`, `.disclaimer-banner--collapsed`, `.webgl-overlay`, `.webgl-overlay--hidden`.
    - `style.css` używa `#E69F00` (Wong amber) co najmniej 3x.
    - Touch target 44x44px (CSS `min-width: 44px; min-height: 44px;` na `.disclaimer-banner__toggle`).
  </acceptance_criteria>
  <done>UI-05 spełniony: banner mount, toggle, persistence, dispose, ARIA, ≥14 testów zielone.</done>
</task>

<task type="auto">
  <name>Task 2: WebGL context-loss handling w SceneSetup (INFRA-05) + instantiate DisclaimerBanner w main.js</name>
  <files>src/SceneSetup.js, src/main.js</files>
  <read_first>
    - src/SceneSetup.js (po Plan 04 — `_onWindowResizeBound` i `dispose()` shell istnieją)
    - src/main.js (po Plan 04 — `this.disclaimerBanner = null` placeholder istnieje)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pitfall 7: WebGL context-loss bez preventDefault" (linie 580–602) — VERBATIM listener pattern
    - .planning/phases/01-foundation/01-PATTERNS.md § "src/SceneSetup.js MODIFIED" (linie 384–419)
    - .planning/phases/01-foundation/01-UI-SPEC.md § "Copywriting Contract" — `pl.webgl.contextLost` key
    - src/i18n/pl.js (Plan 02 — `pl.webgl.contextLost === 'Utracono kontekst grafiki. Próba odzyskania...'`)
  </read_first>
  <action>
    1. **Modyfikuj `src/SceneSetup.js`** — rozszerz importy i dodaj WebGL context-loss handling:
       - Na górze pliku, po `import * as THREE from 'three';`, dodaj:
         ```javascript
         import { gsap } from 'gsap';
         import { pl } from './i18n/pl.js';
         ```
       - W konstruktorze, PO `this.container.appendChild(this.renderer.domElement);` (przed sekcją Lights/Grid), dodaj setup overlay + listenery:
         ```javascript
         // INFRA-05: WebGL context-loss handling
         this._overlayEl = this._createWebglOverlay();
         this._onContextLost = (event) => {
           // KRYTYCZNE preventDefault — bez tego browser może odmówić restore (Pitfall 7)
           event.preventDefault();
           gsap.ticker.sleep();
           this._showOverlay();
         };
         this._onContextRestored = () => {
           gsap.ticker.wake();
           this._hideOverlay();
         };
         this.renderer.domElement.addEventListener('webglcontextlost', this._onContextLost, false);
         this.renderer.domElement.addEventListener('webglcontextrestored', this._onContextRestored, false);
         ```
       - Dodaj 3 metody PRZED istniejącym `dispose()` (lub przed `}` klasy):
         ```javascript
         /** Tworzy overlay element (hidden by default). */
         _createWebglOverlay() {
           let el = document.getElementById('webgl-overlay');
           if (!el) {
             el = document.createElement('div');
             el.id = 'webgl-overlay';
             el.className = 'webgl-overlay webgl-overlay--hidden';
             el.setAttribute('role', 'alert');
             el.textContent = pl.webgl.contextLost;
             document.body.appendChild(el);
           }
           return el;
         }

         _showOverlay() {
           if (this._overlayEl) this._overlayEl.classList.remove('webgl-overlay--hidden');
         }

         _hideOverlay() {
           if (this._overlayEl) this._overlayEl.classList.add('webgl-overlay--hidden');
         }
         ```
       - Rozszerz istniejące `dispose()` (po Plan 04):
         ```javascript
         dispose() {
           window.removeEventListener('resize', this._onWindowResizeBound);
           // INFRA-05 cleanup
           this.renderer.domElement.removeEventListener('webglcontextlost', this._onContextLost);
           this.renderer.domElement.removeEventListener('webglcontextrestored', this._onContextRestored);
           if (this._overlayEl &amp;&amp; this._overlayEl.parentNode) {
             this._overlayEl.parentNode.removeChild(this._overlayEl);
           }
           this.renderer.dispose();
         }
         ```

    2. **Modyfikuj `src/main.js`** — wpinj DisclaimerBanner:
       - Po linii `import { createTrainingStore } from './state/trainingStore.js';` dodaj:
         ```javascript
         import { DisclaimerBanner } from './DisclaimerBanner';
         ```
       - W konstruktorze `Application`, ZAMIEŃ:
         ```javascript
         this.disclaimerBanner = null;  // Plan 05 wpina
         ```
         na:
         ```javascript
         this.disclaimerBanner = new DisclaimerBanner();
         ```
         (placeholder z Plan 04 zostaje wymieniony; pozostała logika `Application.dispose()` z Plan 04 — `if (this.disclaimerBanner) this.disclaimerBanner.dispose();` — będzie nadal działać poprawnie, bo banner jest zawsze truthy).
       - Opcjonalnie: uprość warunek na `this.disclaimerBanner.dispose();` (bez `if` — banner zawsze istnieje); ALE bezpieczniej zostawić defensywny `if` na wypadek konstruktora błąd.

  </action>
  <verify>
    <automated>
      node --check src/SceneSetup.js
      &amp;&amp; node --check src/main.js
      &amp;&amp; node -e "const fs=require('fs'); const ss=fs.readFileSync('src/SceneSetup.js','utf-8'); if(!/webglcontextlost/.test(ss))process.exit(1); if(!/webglcontextrestored/.test(ss))process.exit(2); if(!/preventDefault/.test(ss))process.exit(3); if(!/gsap\.ticker\.sleep/.test(ss))process.exit(4); if(!/gsap\.ticker\.wake/.test(ss))process.exit(5); if(!/pl\.webgl\.contextLost/.test(ss))process.exit(6); console.log('SceneSetup INFRA-05 OK');"
      &amp;&amp; node -e "const fs=require('fs'); const m=fs.readFileSync('src/main.js','utf-8'); if(!/import\s+{\s*DisclaimerBanner\s*}\s+from\s+['\"]\.\/DisclaimerBanner['\"]/.test(m))process.exit(1); if(!/new DisclaimerBanner\(\)/.test(m))process.exit(2); console.log('main.js wires banner OK');"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/SceneSetup.js` parsuje się czysto (`node --check`).
    - Plik zawiera literały: `webglcontextlost`, `webglcontextrestored`, `preventDefault`, `gsap.ticker.sleep`, `gsap.ticker.wake`, `pl.webgl.contextLost`.
    - Plik zawiera 3 nowe metody: `_createWebglOverlay`, `_showOverlay`, `_hideOverlay`.
    - `dispose()` removeEventListener oba context-loss listenery.
    - `src/main.js` importuje `DisclaimerBanner` z `./DisclaimerBanner` i instantiuje `new DisclaimerBanner()`.
    - `npm run dev` smoke: aplikacja startuje, banner widoczny u góry, `Symulator szkoleniowy — NIE zastępuje obowiązkowego szkolenia BHP ani instruktażu stanowiskowego.` wyświetla się jako tekst banera (manualny test executora w SUMMARY).
    - Manual test (DevTools): otwórz Rendering → "Force WebGL context loss" → overlay pojawia się z polskim tekstem; restore → overlay znika, animacja wraca (executor wykonuje raz, dokumentuje w SUMMARY).
  </acceptance_criteria>
  <done>INFRA-05 spełniony: WebGL listenery + Polish overlay + ticker pause/wake; banner instantiated.</done>
</task>

<task type="auto">
  <name>Task 3: tests/boundaries.test.js (INFRA-02 + UI-06 Polish-literal scanner + TEST-03)</name>
  <files>tests/boundaries.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "B. tests/boundaries.test.js" (linie 658–769) — VERBATIM full file z FORBIDDEN_PAIRS + Polish-literal scanner
    - .planning/phases/01-foundation/01-CONTEXT.md decyzja D-04 (Polish strings tylko w pl.js + scenarios), D-Claude'a-Discretion (regex-based — zero deps, eskalacja do dependency-cruiser w Phase 6)
    - .planning/phases/01-foundation/01-PATTERNS.md § "Boundary-safe imports" (linie 478–496) — pełna tabela 10 forbidden pairs
    - ROADMAP.md Phase 1 SC1 — boundaries musi failować na ProcedureEngine import THREE/DOM/store/gsap
  </read_first>
  <action>
    Utwórz `tests/boundaries.test.js` (VERBATIM z RESEARCH §B linie 660–770, z drobnym refinementem dla Windows path separators):
    ```javascript
    // tests/boundaries.test.js
    // @vitest-environment node
    //
    // INFRA-02 + TEST-03: Statyczne enforcement granic importów. Jeśli zabroniony import
    // pojawi się w pliku, ten test failuje build → CI red → PR blocked.
    //
    // UI-06: Polish-literal scanner — diakrytyki w string literals tylko w
    // src/i18n/ i src/training/scenarios/.
    //
    // Zwiększaj listę FORBIDDEN_PAIRS gdy dodajesz nowy moduł z boundary contract.

    import { describe, it, expect } from 'vitest';
    import { readFileSync, readdirSync, statSync, existsSync } from 'node:fs';
    import { join, relative } from 'node:path';
    import { fileURLToPath } from 'node:url';

    const ROOT = fileURLToPath(new URL('../', import.meta.url));

    /**
     * Forbidden import pairs (RESEARCH §B linie 680–698).
     * - file: path relatywny do ROOT
     * - mustNotImport: substring match w `import ... from '...'` lub `import('...')`
     */
    const FORBIDDEN_PAIRS = [
      // ProcedureEngine + ScoringService + faultRules + scoringWeights — pure (Layer 2)
      { file: 'src/training/ProcedureEngine.js',  mustNotImport: ['three', 'gsap', '../state/', './state/'] },
      { file: 'src/training/ScoringService.js',   mustNotImport: ['three', 'gsap', '../state/', './state/'] },
      { file: 'src/training/scoringWeights.js',   mustNotImport: ['three', 'gsap', '../state/', './state/'] },
      { file: 'src/training/faultRules.js',       mustNotImport: ['three', 'gsap', '../state/', './state/'] },
      // PressModel — bez DOM/store/training (THREE/gsap allowed for geometry/animations)
      { file: 'src/PressModel.js',     mustNotImport: ['../state/', '../training/', './state/', './training/'] },
      // PhysicsEngine — pure math, zero deps
      { file: 'src/PhysicsEngine.js',  mustNotImport: ['three', 'gsap', '../state/', '../training/'] },
      // SceneSetup — może używać THREE i gsap (context-loss listener) — NIE store/training
      { file: 'src/SceneSetup.js',     mustNotImport: ['../state/', '../training/'] },
      // UI nie importuje THREE
      { file: 'src/UI.js',             mustNotImport: ['three'] },
      // TrainingStore — tylko zustand + training/
      { file: 'src/state/trainingStore.js', mustNotImport: ['three', 'gsap'] },
      // DisclaimerBanner — pure DOM, no THREE/gsap/store/training
      { file: 'src/DisclaimerBanner.js', mustNotImport: ['three', 'gsap', '../state/', '../training/'] },
    ];

    /** Regex: static + dynamic imports. Capturuje string specifier. */
    const IMPORT_RE = /(?:^|\s)import\s+(?:[\s\S]+?from\s+)?['"]([^'"]+)['"]|import\s*\(\s*['"]([^'"]+)['"]/g;

    function extractImports(filePath) {
      const src = readFileSync(filePath, 'utf-8');
      const specs = [];
      let m;
      // reset lastIndex bo regex jest /g
      IMPORT_RE.lastIndex = 0;
      while ((m = IMPORT_RE.exec(src)) !== null) {
        specs.push(m[1] ?? m[2]);
      }
      return specs;
    }

    describe('boundaries: import-graph guard (INFRA-02, TEST-03)', () => {
      for (const rule of FORBIDDEN_PAIRS) {
        it(`${rule.file} must not import ${rule.mustNotImport.join(', ')}`, () => {
          const filePath = join(ROOT, rule.file);
          if (!existsSync(filePath)) {
            // Plik jeszcze nie istnieje (Phase 1 in-progress) — pomiń, nie failuj.
            // Po Phase 1 complete wszystkie pliki muszą istnieć — ten skip znika sam.
            return;
          }
          const imports = extractImports(filePath);
          const violations = imports.filter(spec =>
            rule.mustNotImport.some(forbidden => spec.includes(forbidden))
          );
          expect(violations, `Forbidden imports in ${rule.file}: ${violations.join(', ')}`).toEqual([]);
        });
      }
    });

    describe('boundaries: Polish string literal scanner (UI-06, MOD-3)', () => {
      it('no Polish string literal in src/*.js outside src/i18n/ and src/training/scenarios/', () => {
        const POLISH_DIACRITIC = /[ąćęłńóśźżĄĆĘŁŃÓŚŹŻ]/;
        const ALLOWED_PATHS = ['src/i18n/', 'src/training/scenarios/'];
        const violations = [];

        function walk(dir) {
          for (const entry of readdirSync(dir)) {
            const full = join(dir, entry);
            const rel = relative(ROOT, full).replace(/\\/g, '/');
            if (statSync(full).isDirectory()) {
              if (rel.startsWith('node_modules') || rel.startsWith('dist')) continue;
              walk(full);
              continue;
            }
            if (!full.endsWith('.js')) continue;
            if (ALLOWED_PATHS.some(p => rel.startsWith(p))) continue;
            const src = readFileSync(full, 'utf-8');
            // Komentarze (// + /* */) są OK; literały string nie są.
            // Aproksymacja: szuka cudzysłów + polski znak + cudzysłów na tej samej linii.
            const stringLitWithPolish = /(['"`])[^'"`\n]*[ąćęłńóśźżĄĆĘŁŃÓŚŹŻ][^'"`\n]*\1/;
            const lines = src.split('\n');
            for (const line of lines) {
              const trimmed = line.trim();
              if (trimmed.startsWith('//') || trimmed.startsWith('*') || trimmed.startsWith('/*')) continue;
              if (stringLitWithPolish.test(line)) {
                violations.push(`${rel}: ${line.trim()}`);
                break;
              }
            }
          }
        }
        walk(join(ROOT, 'src'));
        expect(violations, `Polish string literals outside i18n/scenarios:\n${violations.join('\n')}`).toEqual([]);
      });
    });

    describe('boundaries: negative test — scanner WYKRYWA syntetyczny zabroniony import', () => {
      it('FORBIDDEN_PAIRS contains explicit ProcedureEngine.js × three pair (sanity)', () => {
        const proc = FORBIDDEN_PAIRS.find(r => r.file === 'src/training/ProcedureEngine.js');
        expect(proc).toBeDefined();
        expect(proc.mustNotImport).toContain('three');
        expect(proc.mustNotImport).toContain('gsap');
      });

      it('extractImports correctly parses static + named imports', () => {
        // Sanity check on source file — ProcedureEngine importuje faultRules.js
        const procPath = join(ROOT, 'src/training/ProcedureEngine.js');
        if (!existsSync(procPath)) return; // Plan 03 not done yet — skip
        const imports = extractImports(procPath);
        expect(imports.some(i => i.includes('faultRules'))).toBe(true);
      });
    });
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/boundaries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/boundaries.test.js` istnieje, ma 3 `describe` blocks: import-graph guard (10 it'ów), Polish string literal (1 it), negative-sanity (2 its) = 13 testów.
    - `npx vitest run tests/boundaries.test.js` exit 0; wszystkie testy PASS (po finiszu Plan 02-04 wszystkie pliki istnieją; Plan 05 jest ostatni).
    - Test pokrywa wszystkie 10 plików z FORBIDDEN_PAIRS.
    - Polish-literal scanner przechodzi tabelę walk po `src/`, pomija `src/i18n/` i `src/training/scenarios/`, znajduje 0 violations (cały kod używa `pl.errors[code]` lookup pattern; testy fixture mają polskie diakrytyki w `tests/` które są poza scope scannera).
    - Pragma `// @vitest-environment node`.
  </acceptance_criteria>
  <done>INFRA-02 + TEST-03 + UI-06 enforcement spełnione; 13 testów zielone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pl.disclaimer.full` text → DOM | wstrzykiwane przez `textContent` (XSS-safe) — nigdy `innerHTML` dla user-controlled content (RESEARCH § Security V5) |
| WebGL context-lost browser event → ticker pause | event handler musi być sync (`event.preventDefault()` w pierwszej linii) |
| `localStorage` private mode / quota errors | try/catch wokół get/set zapobiega crashowi z `pm300:disclaimer:collapsed:v1` |
| `tests/boundaries.test.js` filesystem read | hardcoded ROOT z `import.meta.url`; brak user input → brak path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | XSS via DisclaimerBanner | mitigate | `textContent` injection dla pełnego tekstu; `innerHTML` tylko dla statycznego markup wrappera (button + spans bez user-controlled content); ASVS V5 spełnione |
| T-05-02 | Tampering | localStorage poisoning (`pm300:disclaimer:collapsed:v1`) | mitigate | Wartość binary `'true'/'false'` parsowana przez `=== 'true'` coercion; każda inna value = treated as expanded (default safe state); RESEARCH § Security tabela row 2 |
| T-05-03 | DoS | WebGL context loss bez restore (background tab) | mitigate | `event.preventDefault()` w pierwszej linii listener'a (CRIT-5/Pitfall 7); INFRA-05 spełnione |
| T-05-04 | DoS | banner subscriber leak na HMR | mitigate | `dispose()` removeEventListener z dokładnie tym samym handler reference (`this._onToggleClick`); Application.dispose() (Plan 04) chains banner.dispose |
| T-05-05 | Tampering | przyszły PR dodający `dismiss=true` do DisclaimerBanner | mitigate | D-13 code-fence komentarz w klasie; `tests/disclaimerBanner.test.js` weryfikuje obecność komentarza; review-gate |
| T-05-06 | Tampering | ProcedureEngine przypadkowo importuje gsap w Phase 4-6 PR | mitigate | `tests/boundaries.test.js` static enforcement — failuje CI; INFRA-02/TEST-03 spełnione |
| T-05-07 | Tampering | nowy moduł Phase 4-6 z polskimi stringami inline (poza pl.js) | mitigate | Polish-literal scanner walk po `src/`; UI-06 enforcement |
| T-05-08 | DoS | overlay element pozostaje w DOM po dispose | mitigate | `dispose()` w SceneSetup usuwa `this._overlayEl` przez `parentNode.removeChild` |
</threat_model>

<verification>
- `npx vitest run tests/disclaimerBanner.test.js` zielone (≥14 testów).
- `npx vitest run tests/boundaries.test.js` zielone (13 testów).
- `npm test` cała Phase 1 (po wszystkich 5 planach): zielona z ≥9 plikami testowymi i ≥110 testami PASS.
- `npm test -- --coverage` daje coverage ≥95% lines/functions/statements i ≥90% branches na `src/training/**` + `src/state/**`.
- `npm run dev` smoke (manualny — executor wykonuje raz):
  - Banner widoczny u góry przy starcie z amber tłem.
  - Klik chevron zwija; reload → state persisted.
  - DevTools Rendering → "Force WebGL context loss" → polski overlay; "Force restore" → overlay znika, animacja wraca.
- `git status` pokazuje: 3 nowe pliki (`src/DisclaimerBanner.js`, `tests/disclaimerBanner.test.js`, `tests/boundaries.test.js`), 4 modified (`src/SceneSetup.js`, `src/main.js`, `style.css`, `package-lock.json` jeśli wcześniejsze install nie był committed).
</verification>

<success_criteria>
- UI-05: Banner permanent presence; collapsed state z `!` icon spełnia "widoczny stale" (D-13); persistence przez localStorage; D-13 code-fence komentarz w klasie.
- INFRA-05: WebGL context-loss listener z `preventDefault` + ticker.sleep + Polish overlay; restore wake.
- INFRA-02: 10 forbidden import pairs egzekwowane statycznie.
- TEST-03: `tests/boundaries.test.js` istnieje i działa.
- UI-06: Polish-literal scanner enforce'uje że nowy kod używa `pl.errors[code]` zamiast inline.
- Wszystkie 21 wymagań Phase 1 spełnione (audit w SUMMARY).
- Wszystkie acceptance criteria 3 tasków spełnione.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` zawierający:
- Lista 7 plików (3 nowe + 4 modyfikacje).
- Audit 21 wymagań Phase 1 — każde requirement ID z TICK + plik(i) który je realizuje.
- Coverage report z `npm test -- --coverage` (full suite po wszystkich 5 planach).
- Smoke test wyniki: `npm run dev` aplikacja startuje, banner visible, manualny WebGL context-loss test (DevTools → Rendering).
- Wskazówka dla `/gsd-verify-work`: phase gate komendy.
- Lista znanych przyszłych zadań nie zrealizowanych w Phase 1 (PressModel.dispose stub w Phase 2; full STATE-02 enforcement gdy meshe lądują w Phase 2; pełny stress test 100-click w Phase 3 INTERACT-05).
</output>
