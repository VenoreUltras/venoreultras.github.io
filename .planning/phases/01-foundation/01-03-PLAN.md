---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/training/ProcedureEngine.js
  - src/training/ScoringService.js
  - tests/procedureEngine.test.js
  - tests/scoringService.test.js
  - tests/faultRules.test.js
autonomous: true
requirements: [SOP-01, SOP-08, SCORE-01, TEST-01, TEST-02, TEST-04]
must_haves:
  truths:
    - "`validateStep(intent, state, scenario)` jest pure top-level function: zero importów THREE/DOM/store/gsap"
    - "`validateStep` zwraca `{ok:true, ...}` tylko gdy intent dopasowany do `state.currentStepId` i `validateBefore` nie blokuje; w przeciwnym razie `{ok:false, reason, effects:[step.violation event]}`"
    - "`validateStep` NIGDY nie zwraca `{ok:true}` cicho gdy intent nie pasuje (SOP-08 hard gating)"
    - "`evaluateFaultRules` jest re-eksportowane z `ProcedureEngine.js` (delegacja do `evaluateFaultRulesData` w `faultRules.js`) — store importuje z jednego miejsca"
    - "`ScoringService.calculate(events, opts?)` jest pure: zwraca `{score, criticalCount, mediumCount, minorCount}`; `score = max(0, 100 + sum(severity_weights))`"
    - "ScoringService floor: 4 critical events → score=0; 5. critical event NIE schodzi poniżej 0 (D-15)"
    - "ScoringService override: `calculate(events, {weights:{critical:-50}})` używa override zamiast default -25"
    - "ProcedureEngine + ScoringService coverage ≥95% lines/functions/statements, ≥90% branches"
    - "Idempotency-zalążek: `validateStep` wywołane 100 razy z tym samym state-snapshot zwraca 100 razy ten sam wynik (pure function determinism — TEST-04 zalążek)"
  artifacts:
    - path: "src/training/ProcedureEngine.js"
      provides: "validateStep + nextStep + isScenarioComplete + re-exported evaluateFaultRules"
      exports: ["validateStep", "evaluateFaultRules", "nextStep", "isScenarioComplete"]
      min_lines: 80
    - path: "src/training/ScoringService.js"
      provides: "pure calculate(events, opts)"
      exports: ["calculate"]
      min_lines: 30
    - path: "tests/procedureEngine.test.js"
      provides: "happy path + ≥6 error matrix tests + idempotency"
      min_lines: 120
    - path: "tests/scoringService.test.js"
      provides: "default + override + floor edge cases"
      min_lines: 60
    - path: "tests/faultRules.test.js"
      provides: "guard-open-during-cycle invariant test"
  key_links:
    - from: "src/training/ProcedureEngine.js"
      to: "src/training/faultRules.js"
      via: "import { evaluateFaultRulesData } + re-export jako evaluateFaultRules"
      pattern: "from '\\./faultRules\\.js'"
    - from: "src/training/ScoringService.js"
      to: "src/training/scoringWeights.js"
      via: "import DEFAULT_WEIGHTS, SCORE_BASELINE, SCORE_FLOOR"
      pattern: "from '\\./scoringWeights\\.js'"
    - from: "tests/procedureEngine.test.js"
      to: "src/training/ProcedureEngine.js"
      via: "import (testy używają inline mini-stuby — NIE importują uruchomienie ani fixture żeby plan był parallel z 02)"
      pattern: "from '\\.\\./src/training/ProcedureEngine\\.js'"
---

<objective>
Wave 1 część 2: utwórz **logikę** Phase 1 — pure `ProcedureEngine` (validateStep + nextStep + isScenarioComplete + re-exported evaluateFaultRules) i pure `ScoringService` (calculate). Plus 3 pliki testowe z pokryciem ≥95%.

Plan 03 jest **niezależny od Plan 02**: testy używają inline mini-scenariuszy zdefiniowanych w samym pliku testowym, NIE importują `uruchomienie` ani `tests/fixtures/scenario.fixture.js`. Wyjątek: `tests/faultRules.test.js` importuje z `src/training/faultRules.js` (utworzonego w Plan 02) — dlatego Plan 03 ma `depends_on: ["01-01"]` (Wave 0), ALE `tests/faultRules.test.js` execution wymaga `src/training/faultRules.js`. **Resolution**: w wave 1 oba plany wykonują się równolegle; jeśli Plan 03 finiszuje przed Plan 02, `tests/faultRules.test.js` może chwilowo failować (file not found) — `npx vitest run` zwróci skip lub red dla tego jednego testu, reszta zielona. Po Plan 02 finish, retry `npm test` zielony. Executor Plan 03 NIE czeka na Plan 02 — koncentruje się na ProcedureEngine + ScoringService + ich testach (te są ortogonalne wobec Plan 02).

Purpose: ProcedureEngine to serce SOP gating'u — TUTAJ definiowana jest decyzja "ten klik jest bezpieczny" lub "ten klik = critical violation". ScoringService daje deterministyczny wynik sesji. Oba są pure — testowalne w Node bez WebGL/DOM (RESEARCH § Pitfall 5 prevention).

Output: 2 nowe moduły w `src/training/` + 3 pliki testowe. Po Plan 02 + Plan 03 finish, `npm test -- --coverage` daje zieloną sumę z coverage ≥95% na `src/training/**`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- ProcedureEngine signature (CONTEXT D-01..D-08, RESEARCH §"Pattern 1" verbatim). -->

```typescript
// src/training/ProcedureEngine.js exports
export function validateStep(intent, state, scenario): { ok: boolean, reason: string|null, effects: Array<object> }
export function evaluateFaultRules(state, rules?): Array<object>  // re-eksportowane z faultRules.js
export function nextStep(state, scenario): string|null              // zwraca id następnego stepa lub null jeśli ostatni
export function isScenarioComplete(state, scenario): boolean        // true gdy wszystkie steps mają status 'done' LUB currentStepId === null

// intent shape:
//   { kind: 'click', meshId: string }   — dla manipulation/visual-target
//   { kind: 'check', stepId: string }   — dla visual-attest

// state shape (subset used by engine):
//   { currentStepId: string|null, steps: { [id]: { status, ... } }, machineState: string, meshStates: object, _now?: () => number }

// Return effects format (D-02 closed set):
//   { type: 'appendEvent', event: { type, ...payload } }
//   { type: 'setMachineState', value: string }
//   { type: 'setMeshState', meshId: string, value: string }
//   { type: 'startSpinUpTimer', ms: number }
//   { type: 'advanceStep' }   ← ProcedureEngine generuje to TYLKO, scenariusz NIE
//   { type: 'playAudio', soundId: string }   ← Phase 5
```

```typescript
// src/training/ScoringService.js exports
export function calculate(events, opts?: { weights?: { critical?, medium?, minor? } }): {
  score: number,
  criticalCount: number,
  mediumCount: number,
  minorCount: number,
}
// Algorithm (D-15/D-16):
//   raw = SCORE_BASELINE + criticalCount * weights.critical + mediumCount * weights.medium + minorCount * weights.minor
//   score = Math.max(SCORE_FLOOR, raw)
//   Counts only events of type 'step.violation' or 'fault.triggered' with severity in {'critical','medium','minor'}.
```

ProcedureEngine three-branch decision tree (RESEARCH §"Pattern 1" linie 306–376 — VERBATIM):
1. **No active step** (`scenario.steps.find(s => s.id === state.currentStepId)` === undefined) → `{ok:false, reason:'no-active-step', effects:[]}`
2. **Forbidden state** (`expectedStep.validateBefore(state) === false`) → `{ok:false, reason:'forbidden-state', effects:[step.violation event z errorCode/severity z scenariusza, ...effectsOnError]}`
3. **Wrong target** (intent NIE matchuje expectedStep.kind/targetMeshId) → `{ok:false, reason:'wrong-target', effects:[step.violation z severity:'medium', errorCode:'E-NIEPRAWIDLOWY-MESH']}`
4. **Success** → `{ok:true, reason:null, effects:[step.done event, ...effectsOnSuccess, advanceStep]}`

Intent matching (D-05):
```javascript
const intentMatchesStep =
  (expectedStep.kind === 'manipulation' || expectedStep.kind === 'visual-target')
    ? (intent.kind === 'click' && intent.meshId === expectedStep.targetMeshId)
    : (intent.kind === 'check' && intent.stepId === expectedStep.id);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utwórz src/training/ProcedureEngine.js + tests/procedureEngine.test.js</name>
  <files>src/training/ProcedureEngine.js, tests/procedureEngine.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pattern 1: Effect-record / store-applies" (linie 299–376) — VERBATIM validateStep implementation
    - .planning/phases/01-foundation/01-RESEARCH.md § "I. tests/procedureEngine.test.js" (linie 1311–1409) — table-driven examples
    - .planning/phases/01-foundation/01-PATTERNS.md § "ProcedureEngine.js" (linie 64–98) — pure idiom, named exports, brak importów THREE/DOM/store/gsap
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-01 (implicit order), D-02 (effects), D-05 (kind), D-07 (validateBefore), D-08 (timer effect declarative — store odpala setTimeout)
    - .planning/codebase/CONVENTIONS.md (Polish JSDoc + English identifiers)
  </read_first>
  <behavior>
    - **Happy path 1 — visual-target click**: state.currentStepId='step-A' (kind='visual-target', targetMeshId='mesh-X'); `validateStep({kind:'click', meshId:'mesh-X'}, state, scenario)` zwraca `{ok:true, reason:null, effects:[appendEvent step.done, ...effectsOnSuccess, advanceStep]}`.
    - **Happy path 2 — visual-attest check**: state.currentStepId='step-B' (kind='visual-attest'); `validateStep({kind:'check', stepId:'step-B'}, state, scenario)` zwraca `{ok:true}`.
    - **Happy path 3 — manipulation click**: state.currentStepId='step-C' (kind='manipulation', targetMeshId='mesh-Y'); `validateStep({kind:'click', meshId:'mesh-Y'}, state, scenario)` zwraca `{ok:true}`.
    - **Error: no active step** — state.currentStepId=null → `{ok:false, reason:'no-active-step', effects:[]}`.
    - **Error: wrong meshId** — state.currentStepId='step-A' (targetMeshId='mesh-X'), intent meshId='mesh-Z' → `{ok:false, reason:'wrong-target', effects:[step.violation severity:'medium' errorCode:'E-NIEPRAWIDLOWY-MESH']}`.
    - **Error: visual-attest dostaje click** — state.currentStepId='step-B' (kind='visual-attest'), intent kind='click' → `{ok:false, reason:'wrong-target'}`.
    - **Error: manipulation dostaje check** — state.currentStepId='step-C' (kind='manipulation'), intent kind='check' → `{ok:false, reason:'wrong-target'}`.
    - **Error: forbidden state (validateBefore returns false)** — step ma `validateBefore: s => s.machineState === 'ready'`, state.machineState='loading' → `{ok:false, reason:'forbidden-state', effects:[step.violation z severity z scenariusza]}`.
    - **Idempotency (TEST-04 zalążek)**: 100× wywołanie `validateStep` z tym samym (state, intent, scenario) zwraca 100× ten sam wynik (deep-equal — pure function).
    - **`evaluateFaultRules` re-export**: `import { evaluateFaultRules } from '../src/training/ProcedureEngine.js'` działa i jest equivalent do `evaluateFaultRulesData` z faultRules.
    - **`nextStep` po finalu**: state.currentStepId='last-step' → `nextStep` zwraca null.
    - **`isScenarioComplete` po wszystkich done**: wszystkie steps[id].status='done' → zwraca true; mieszane → false.
    - **Boundary: brak importów THREE/DOM/store/gsap** — sprawdzane statycznie przez Plan 05 boundaries.test.js. Plan 03 testy mogą zerknąć na to inline (read source file + assert no forbidden imports).
  </behavior>
  <action>
    1. **RED**: napisz `tests/procedureEngine.test.js` PRZED implementacją. Treść (verbatim — wszystkie 13 case'ów):
       ```javascript
       // tests/procedureEngine.test.js
       // @vitest-environment node
       // SOP-01, SOP-08, TEST-01 (≥95% coverage), TEST-04 (idempotency zalążek)

       import { describe, it, expect } from 'vitest';
       import { readFileSync } from 'node:fs';
       import { fileURLToPath } from 'node:url';
       import {
         validateStep,
         evaluateFaultRules,
         nextStep,
         isScenarioComplete,
       } from '../src/training/ProcedureEngine.js';

       // Inline mini-scenariusz — NIE importujemy uruchomienie żeby Plan 03
       // był niezależny od Plan 02 (parallel Wave 1).
       const inlineScenario = {
         id: 'mini',
         titlePL: 'Mini',
         descriptionPL: 'Mini scenariusz testowy',
         initialMachineState: 'oczekiwanie-na-inspekcje',
         steps: [
           {
             id: 'step-A',
             kind: 'visual-target',
             targetMeshId: 'mesh-X',
             labelPL: 'A',
             descriptionPL: 'A',
             rationalePL: 'A',
             effectsOnSuccess: [],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-NIEPRAWIDLOWY-MESH', severity: 'medium' } }],
           },
           {
             id: 'step-B',
             kind: 'visual-attest',
             labelPL: 'B',
             descriptionPL: 'B',
             rationalePL: 'B',
             effectsOnSuccess: [],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-POMINIETO-KONTROLE', severity: 'minor' } }],
           },
           {
             id: 'step-C',
             kind: 'manipulation',
             targetMeshId: 'mesh-Y',
             labelPL: 'C',
             descriptionPL: 'C',
             rationalePL: 'C',
             effectsOnSuccess: [{ type: 'setMeshState', meshId: 'mesh-Y', value: 'engaged' }],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-NIEPRAWIDLOWY-MESH', severity: 'medium' } }],
           },
           {
             id: 'step-D',
             kind: 'manipulation',
             targetMeshId: 'mesh-Z',
             validateBefore: (state) => state.machineState === 'ready',
             labelPL: 'D',
             descriptionPL: 'D',
             rationalePL: 'D',
             effectsOnSuccess: [{ type: 'setMachineState', value: 'done' }],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-FORBIDDEN', severity: 'critical' } }],
           },
         ],
       };

       function makeState(currentStepId, machineState = 'oczekiwanie-na-inspekcje') {
         return {
           currentStepId,
           steps: Object.fromEntries(inlineScenario.steps.map(s => [s.id, { status: s.id === currentStepId ? 'active' : 'pending' }])),
           machineState,
           meshStates: {},
           events: [],
           _now: () => 1000,
         };
       }

       describe('ProcedureEngine.validateStep — happy paths (SOP-01)', () => {
         it('akceptuje visual-target click na poprawnym meshu', () => {
           const r = validateStep({ kind: 'click', meshId: 'mesh-X' }, makeState('step-A'), inlineScenario);
           expect(r.ok).toBe(true);
           expect(r.reason).toBeNull();
           expect(r.effects.some(e => e.type === 'advanceStep')).toBe(true);
           expect(r.effects.some(e => e.type === 'appendEvent' && e.event.type === 'step.done')).toBe(true);
         });

         it('akceptuje visual-attest check', () => {
           const r = validateStep({ kind: 'check', stepId: 'step-B' }, makeState('step-B'), inlineScenario);
           expect(r.ok).toBe(true);
         });

         it('akceptuje manipulation click i włącza effectsOnSuccess', () => {
           const r = validateStep({ kind: 'click', meshId: 'mesh-Y' }, makeState('step-C'), inlineScenario);
           expect(r.ok).toBe(true);
           expect(r.effects.some(e => e.type === 'setMeshState' && e.meshId === 'mesh-Y' && e.value === 'engaged')).toBe(true);
         });

         it('akceptuje step z validateBefore gdy guard PASS', () => {
           const r = validateStep({ kind: 'click', meshId: 'mesh-Z' }, makeState('step-D', 'ready'), inlineScenario);
           expect(r.ok).toBe(true);
         });
       });

       describe('ProcedureEngine.validateStep — error matrix (SOP-08)', () => {
         it('rejectuje gdy currentStepId === null (no-active-step)', () => {
           const r = validateStep({ kind: 'click', meshId: 'mesh-X' }, makeState(null), inlineScenario);
           expect(r.ok).toBe(false);
           expect(r.reason).toBe('no-active-step');
           expect(r.effects).toEqual([]);
         });

         it('rejectuje wrong meshId z severity:medium (E-NIEPRAWIDLOWY-MESH)', () => {
           const r = validateStep({ kind: 'click', meshId: 'mesh-WRONG' }, makeState('step-A'), inlineScenario);
           expect(r.ok).toBe(false);
           expect(r.reason).toBe('wrong-target');
           const v = r.effects.find(e => e.event?.type === 'step.violation');
           expect(v.event.severity).toBe('medium');
           expect(v.event.errorCode).toBe('E-NIEPRAWIDLOWY-MESH');
         });

         it('rejectuje click gdy step jest visual-attest', () => {
           const r = validateStep({ kind: 'click', meshId: 'cokolwiek' }, makeState('step-B'), inlineScenario);
           expect(r.ok).toBe(false);
           expect(r.reason).toBe('wrong-target');
         });

         it('rejectuje check gdy step jest manipulation', () => {
           const r = validateStep({ kind: 'check', stepId: 'step-C' }, makeState('step-C'), inlineScenario);
           expect(r.ok).toBe(false);
           expect(r.reason).toBe('wrong-target');
         });

         it('rejectuje forbidden-state gdy validateBefore = false', () => {
           const r = validateStep({ kind: 'click', meshId: 'mesh-Z' }, makeState('step-D', 'NOT-ready'), inlineScenario);
           expect(r.ok).toBe(false);
           expect(r.reason).toBe('forbidden-state');
           const v = r.effects.find(e => e.event?.type === 'step.violation');
           expect(v.event.errorCode).toBe('E-FORBIDDEN');
           expect(v.event.severity).toBe('critical');
         });

         it('NIGDY nie zwraca {ok:true} dla mismatched intent (SOP-08 hard gating)', () => {
           // 50 losowych mismatched intentów — żaden nie ma prawa wrócić ok:true
           const variations = [
             { kind: 'click', meshId: 'random-1' },
             { kind: 'click', meshId: 'random-2' },
             { kind: 'check', stepId: 'random-stepid' },
             { kind: 'click', meshId: '' },
             { kind: 'foo', meshId: 'mesh-X' },
           ];
           const state = makeState('step-A'); // expects mesh-X
           for (const v of variations) {
             if (v.kind === 'click' && v.meshId === 'mesh-X') continue; // skip the one that would be ok
             const r = validateStep(v, state, inlineScenario);
             expect(r.ok).toBe(false);
           }
         });
       });

       describe('ProcedureEngine.validateStep — idempotency (TEST-04 zalążek)', () => {
         it('100x wywołanie z tym samym state-snapshot zwraca 100x ten sam wynik (pure)', () => {
           const state = makeState('step-A');
           const intent = { kind: 'click', meshId: 'mesh-X' };
           const first = validateStep(intent, state, inlineScenario);
           for (let i = 0; i < 99; i++) {
             const r = validateStep(intent, state, inlineScenario);
             expect(r.ok).toBe(first.ok);
             expect(r.reason).toBe(first.reason);
             expect(r.effects.length).toBe(first.effects.length);
           }
         });
       });

       describe('ProcedureEngine helpers', () => {
         it('nextStep zwraca id następnego kroku', () => {
           expect(nextStep(makeState('step-A'), inlineScenario)).toBe('step-B');
           expect(nextStep(makeState('step-B'), inlineScenario)).toBe('step-C');
         });
         it('nextStep zwraca null po ostatnim kroku', () => {
           expect(nextStep(makeState('step-D'), inlineScenario)).toBeNull();
         });
         it('isScenarioComplete = true gdy wszystkie steps są done', () => {
           const state = {
             currentStepId: null,
             steps: Object.fromEntries(inlineScenario.steps.map(s => [s.id, { status: 'done' }])),
             machineState: 'done', meshStates: {}, events: [],
           };
           expect(isScenarioComplete(state, inlineScenario)).toBe(true);
         });
         it('isScenarioComplete = false gdy mieszane statusy', () => {
           const state = makeState('step-A');
           expect(isScenarioComplete(state, inlineScenario)).toBe(false);
         });
       });

       describe('ProcedureEngine.evaluateFaultRules — re-export from faultRules.js', () => {
         it('jest funkcją (re-eksportowaną z faultRules.js)', () => {
           expect(typeof evaluateFaultRules).toBe('function');
         });
         it('akceptuje (state, rules?) i zwraca array', () => {
           const r = evaluateFaultRules({ machineState: 'idle', meshStates: {} }, []);
           expect(Array.isArray(r)).toBe(true);
           expect(r).toHaveLength(0);
         });
       });

       describe('ProcedureEngine — boundary check (SOP-01 pre-flight)', () => {
         it('source pliku NIE importuje three, gsap, ../state/, DOM globals', () => {
           const url = new URL('../src/training/ProcedureEngine.js', import.meta.url);
           const src = readFileSync(fileURLToPath(url), 'utf-8');
           expect(src).not.toMatch(/from\s+['"]three['"]/);
           expect(src).not.toMatch(/from\s+['"]gsap['"]/);
           expect(src).not.toMatch(/from\s+['"]\.\.\/state\//);
           expect(src).not.toMatch(/from\s+['"]\.\/state\//);
           // DOM access (document/window — by safety check, even though Node would throw at load)
           expect(src).not.toMatch(/\bdocument\./);
           expect(src).not.toMatch(/\bwindow\./);
         });
       });
       ```
    2. Uruchom `npm test -- tests/procedureEngine.test.js` → RED (file `ProcedureEngine.js` doesn't exist yet).
    3. **GREEN**: utwórz `src/training/ProcedureEngine.js`. Treść (zaadaptowana z RESEARCH §"Pattern 1" linie 306–376 + dodane `nextStep`, `isScenarioComplete`, re-export `evaluateFaultRules`):
       ```javascript
       // src/training/ProcedureEngine.js
       // Czysty silnik walidacji kroków SOP (SOP-01). Brak importów THREE/DOM/store/gsap.
       // evaluateFaultRules re-eksportowane z faultRules.js — single import point dla store.

       import { evaluateFaultRulesData } from './faultRules.js';

       /**
        * Czysta funkcja walidacji kroku — nie mutuje state ani scenariusza.
        *
        * @param {{kind:'click'|'check', meshId?:string, stepId?:string}} intent
        * @param {object} state - snapshot store (currentStepId, steps, machineState, meshStates, _now?)
        * @param {object} scenario - załadowany scenariusz
        * @returns {{ok:boolean, reason:string|null, effects:Array<object>}}
        */
       export function validateStep(intent, state, scenario) {
         const expectedStep = scenario.steps.find(s => s.id === state.currentStepId);
         const now = state._now ? state._now() : Date.now();

         // Branch 1: brak aktywnego kroku
         if (!expectedStep) {
           return { ok: false, reason: 'no-active-step', effects: [] };
         }

         // Branch 2: forbidden-state (D-07 validateBefore guard)
         if (expectedStep.validateBefore && !expectedStep.validateBefore(state)) {
           const errFromScenario = expectedStep.effectsOnError?.[0]?.event ?? {};
           return {
             ok: false,
             reason: 'forbidden-state',
             effects: [
               { type: 'appendEvent', event: {
                   type: 'step.violation',
                   stepId: expectedStep.id,
                   errorCode: errFromScenario.errorCode ?? 'E-NIEZNANY',
                   severity: errFromScenario.severity ?? 'critical',
                   timestamp: now,
               }},
               ...(expectedStep.effectsOnError ?? []),
             ],
           };
         }

         // Branch 3: kind matching (D-05)
         const intentMatchesStep =
           (expectedStep.kind === 'manipulation' || expectedStep.kind === 'visual-target')
             ? (intent.kind === 'click' && intent.meshId === expectedStep.targetMeshId)
             : (intent.kind === 'check' && intent.stepId === expectedStep.id);

         if (!intentMatchesStep) {
           return {
             ok: false,
             reason: 'wrong-target',
             effects: [
               { type: 'appendEvent', event: {
                   type: 'step.violation',
                   stepId: expectedStep.id,
                   errorCode: 'E-NIEPRAWIDLOWY-MESH',
                   severity: 'medium',
                   timestamp: now,
               }},
             ],
           };
         }

         // Branch 4: success — D-02 effectsOnSuccess verbatim + advance
         return {
           ok: true,
           reason: null,
           effects: [
             { type: 'appendEvent', event: {
                 type: 'step.done',
                 stepId: expectedStep.id,
                 timestamp: now,
             }},
             ...(expectedStep.effectsOnSuccess ?? []),
             { type: 'advanceStep' },
           ],
         };
       }

       /**
        * Re-eksportowane z `faultRules.js` (delegacja).
        * Store importuje `evaluateFaultRules` z ProcedureEngine.js — single import point.
        *
        * @param {object} state
        * @param {ReadonlyArray<object>} [rules]
        * @returns {Array<object>}
        */
       export function evaluateFaultRules(state, rules) {
         return evaluateFaultRulesData(state, rules);
       }

       /**
        * Zwraca id następnego kroku po `state.currentStepId`, lub null jeśli ostatni.
        *
        * @param {object} state
        * @param {object} scenario
        * @returns {string|null}
        */
       export function nextStep(state, scenario) {
         const idx = scenario.steps.findIndex(s => s.id === state.currentStepId);
         if (idx === -1 || idx === scenario.steps.length - 1) return null;
         return scenario.steps[idx + 1].id;
       }

       /**
        * True gdy wszystkie kroki scenariusza mają status 'done'.
        *
        * @param {object} state
        * @param {object} scenario
        * @returns {boolean}
        */
       export function isScenarioComplete(state, scenario) {
         return scenario.steps.every(s => state.steps?.[s.id]?.status === 'done');
       }
       ```
    4. Uruchom `npx vitest run tests/procedureEngine.test.js` → wszystkie ≥17 testów GREEN.
  </action>
  <verify>
    <automated>
      npx vitest run tests/procedureEngine.test.js
      &amp;&amp; node -e "const fs=require('fs'); const s=fs.readFileSync('src/training/ProcedureEngine.js','utf-8'); if(/from\s+['\"]three['\"]/.test(s))process.exit(1); if(/from\s+['\"]gsap['\"]/.test(s))process.exit(2); if(/document\./.test(s))process.exit(3); if(/window\./.test(s))process.exit(4); console.log('boundary OK');"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/training/ProcedureEngine.js` istnieje, eksportuje 4 named: `validateStep`, `evaluateFaultRules`, `nextStep`, `isScenarioComplete`.
    - Plik NIE zawiera regex `from 'three'`, `from 'gsap'`, `from '../state/'`, `from './state/'`, `document.`, `window.`.
    - `tests/procedureEngine.test.js` ma ≥17 `it(...)` (4 happy + 6 error + 1 idempotency + 4 helpers + 2 evaluateFaultRules + 1 boundary = 18).
    - `npx vitest run tests/procedureEngine.test.js` exit 0; wszystkie testy PASS.
    - Pragma `// @vitest-environment node` na górze pliku testowego.
    - `validateStep` kolejność branchy: no-active → forbidden-state → wrong-target → success (sprawdzane przez kolejność testów).
  </acceptance_criteria>
  <done>SOP-01 i SOP-08 spełnione: pure validateStep z 4-branch decision tree; ≥18 testów zielone; brak forbidden imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Utwórz src/training/ScoringService.js + tests/scoringService.test.js</name>
  <files>src/training/ScoringService.js, tests/scoringService.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "E. ScoringService" (linie 1054–1110) — VERBATIM full file
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-15 (subtractive od 100, floor 0), D-16 (placeholder weights), D-17 (event log discriminated union), D-18 (configurable opts)
    - src/training/scoringWeights.js (utworzony w Plan 02 — `DEFAULT_WEIGHTS`, `SCORE_BASELINE`, `SCORE_FLOOR`)
    - .planning/phases/01-foundation/01-PATTERNS.md § "ScoringService.js" (linie 102–135)
  </read_first>
  <behavior>
    - **Default weights**: pusta tablica events → `{score: 100, criticalCount: 0, mediumCount: 0, minorCount: 0}`.
    - **Single critical**: `[{type:'step.violation', severity:'critical'}]` → `{score:75, criticalCount:1, mediumCount:0, minorCount:0}`.
    - **Single medium**: → `{score:90, mediumCount:1, ...}`.
    - **Single minor**: → `{score:98, minorCount:1, ...}`.
    - **Mixed**: 2 critical + 1 medium + 3 minor → score = max(0, 100 - 50 - 10 - 6) = 34; counts {2,1,3}.
    - **Floor 0**: 4 critical → score = max(0, 100 - 100) = 0.
    - **Floor 0**: 5 critical → score = max(0, 100 - 125) = 0 (NIE -25; D-15 floor).
    - **Filter**: events typu `'session.start'`, `'step.done'`, `'step.attempted'` SĄ IGNOROWANE w scoringu (D-17 — tylko `step.violation` i `fault.triggered` z severity).
    - **Filter**: event typu `'step.violation'` BEZ `severity` field → ignorowane (defensywnie).
    - **fault.triggered scoring**: `[{type:'fault.triggered', severity:'critical'}]` → `score:75, criticalCount:1`.
    - **Override weights**: `calculate(events, {weights:{critical:-50}})` z 1 critical event → score = max(0, 100 - 50) = 50.
    - **Partial override**: `{weights:{critical:-50}}` zachowuje default medium=-10 i minor=-2 (deep merge nie wymagane — flat shallow merge OK).
    - **NIE mutuje DEFAULT_WEIGHTS**: `Object.isFrozen(DEFAULT_WEIGHTS)` po wywołaniu nadal true.
  </behavior>
  <action>
    1. **RED**: napisz `tests/scoringService.test.js`:
       ```javascript
       // tests/scoringService.test.js
       // @vitest-environment node
       // SCORE-01, TEST-02

       import { describe, it, expect } from 'vitest';
       import { calculate } from '../src/training/ScoringService.js';
       import { DEFAULT_WEIGHTS, SCORE_BASELINE, SCORE_FLOOR } from '../src/training/scoringWeights.js';

       describe('ScoringService.calculate — defaults (SCORE-01, D-15/D-16)', () => {
         it('pusty event log → score 100', () => {
           expect(calculate([])).toEqual({ score: 100, criticalCount: 0, mediumCount: 0, minorCount: 0 });
         });

         it('1 critical → score 75 (-25)', () => {
           const r = calculate([{ type: 'step.violation', severity: 'critical' }]);
           expect(r).toEqual({ score: 75, criticalCount: 1, mediumCount: 0, minorCount: 0 });
         });

         it('1 medium → score 90 (-10)', () => {
           expect(calculate([{ type: 'step.violation', severity: 'medium' }])).toMatchObject({ score: 90, mediumCount: 1 });
         });

         it('1 minor → score 98 (-2)', () => {
           expect(calculate([{ type: 'step.violation', severity: 'minor' }])).toMatchObject({ score: 98, minorCount: 1 });
         });

         it('mix: 2 critical + 1 medium + 3 minor → score 34', () => {
           const events = [
             { type: 'step.violation', severity: 'critical' },
             { type: 'step.violation', severity: 'critical' },
             { type: 'step.violation', severity: 'medium' },
             { type: 'step.violation', severity: 'minor' },
             { type: 'step.violation', severity: 'minor' },
             { type: 'step.violation', severity: 'minor' },
           ];
           expect(calculate(events)).toEqual({ score: 34, criticalCount: 2, mediumCount: 1, minorCount: 3 });
         });

         it('fault.triggered z severity również wlicza się', () => {
           const r = calculate([{ type: 'fault.triggered', severity: 'critical' }]);
           expect(r.score).toBe(75);
           expect(r.criticalCount).toBe(1);
         });
       });

       describe('ScoringService.calculate — floor 0 (D-15)', () => {
         it('4 critical → score = 0', () => {
           const events = Array(4).fill({ type: 'step.violation', severity: 'critical' });
           expect(calculate(events).score).toBe(0);
         });

         it('5 critical → score = 0 (NIE -25)', () => {
           const events = Array(5).fill({ type: 'step.violation', severity: 'critical' });
           expect(calculate(events).score).toBe(0);
         });

         it('10 critical + 5 medium → score = 0', () => {
           const events = [
             ...Array(10).fill({ type: 'step.violation', severity: 'critical' }),
             ...Array(5).fill({ type: 'step.violation', severity: 'medium' }),
           ];
           expect(calculate(events).score).toBe(0);
         });
       });

       describe('ScoringService.calculate — filtering (D-17)', () => {
         it('ignoruje step.done events', () => {
           const events = [
             { type: 'step.done', stepId: 'a' },
             { type: 'step.done', stepId: 'b' },
             { type: 'step.violation', severity: 'minor' },
           ];
           expect(calculate(events).score).toBe(98);
         });

         it('ignoruje session.start / session.done events', () => {
           const events = [
             { type: 'session.start', timestamp: 0 },
             { type: 'session.done', timestamp: 100 },
             { type: 'step.violation', severity: 'medium' },
           ];
           expect(calculate(events)).toMatchObject({ score: 90, mediumCount: 1 });
         });

         it('ignoruje step.violation BEZ severity (defensywnie)', () => {
           const events = [{ type: 'step.violation', stepId: 'x' }];
           expect(calculate(events).score).toBe(100);
         });

         it('ignoruje severity spoza enum {critical,medium,minor}', () => {
           const events = [{ type: 'step.violation', severity: 'unknown' }];
           expect(calculate(events).score).toBe(100);
         });
       });

       describe('ScoringService.calculate — override (D-18)', () => {
         it('override critical=-50 daje score 50 dla 1 critical', () => {
           const r = calculate([{ type: 'step.violation', severity: 'critical' }], { weights: { critical: -50 } });
           expect(r.score).toBe(50);
         });

         it('partial override zachowuje default medium i minor', () => {
           // override critical -50, ale 1 medium używa default -10
           const events = [
             { type: 'step.violation', severity: 'critical' },
             { type: 'step.violation', severity: 'medium' },
           ];
           const r = calculate(events, { weights: { critical: -50 } });
           // 100 - 50 - 10 = 40
           expect(r.score).toBe(40);
         });

         it('opts={} zachowuje się jak brak opts', () => {
           const events = [{ type: 'step.violation', severity: 'critical' }];
           expect(calculate(events, {}).score).toBe(75);
         });

         it('NIE mutuje DEFAULT_WEIGHTS po override', () => {
           const before = { ...DEFAULT_WEIGHTS };
           calculate([{ type: 'step.violation', severity: 'critical' }], { weights: { critical: -999 } });
           expect(DEFAULT_WEIGHTS.critical).toBe(before.critical);
           expect(Object.isFrozen(DEFAULT_WEIGHTS)).toBe(true);
         });

         it('uses SCORE_BASELINE constant (sanity)', () => {
           expect(SCORE_BASELINE).toBe(100);
           expect(SCORE_FLOOR).toBe(0);
         });
       });
       ```
    2. Uruchom `npm test -- tests/scoringService.test.js` → RED.
    3. **GREEN**: utwórz `src/training/ScoringService.js` (VERBATIM z RESEARCH §E linie 1072–1110, z drobną modyfikacją filter):
       ```javascript
       // src/training/ScoringService.js
       // Czysta funkcja kalkulująca finalny wynik na bazie event log (D-17).
       // Subtractive od 100, floor 0 (D-15). Wagi konfigurowalne (D-18).

       import { DEFAULT_WEIGHTS, SCORE_BASELINE, SCORE_FLOOR } from './scoringWeights.js';

       const SCORABLE_TYPES = new Set(['step.violation', 'fault.triggered']);
       const VALID_SEVERITIES = new Set(['critical', 'medium', 'minor']);

       /**
        * Kalkuluje finalny score sesji.
        *
        * @param {Array<{type:string, severity?:string}>} events - log eventów (D-17)
        * @param {object} [opts]
        * @param {{critical?:number, medium?:number, minor?:number}} [opts.weights] - override defaults
        * @returns {{score:number, criticalCount:number, mediumCount:number, minorCount:number}}
        */
       export function calculate(events, opts = {}) {
         const weights = { ...DEFAULT_WEIGHTS, ...(opts.weights ?? {}) };

         let criticalCount = 0;
         let mediumCount = 0;
         let minorCount = 0;

         for (const ev of events) {
           if (!SCORABLE_TYPES.has(ev.type)) continue;
           if (!VALID_SEVERITIES.has(ev.severity)) continue;
           if (ev.severity === 'critical') criticalCount += 1;
           else if (ev.severity === 'medium') mediumCount += 1;
           else if (ev.severity === 'minor') minorCount += 1;
         }

         const raw = SCORE_BASELINE
                   + criticalCount * weights.critical
                   + mediumCount   * weights.medium
                   + minorCount    * weights.minor;
         const score = Math.max(SCORE_FLOOR, raw);

         return { score, criticalCount, mediumCount, minorCount };
       }
       ```
    4. Uruchom `npx vitest run tests/scoringService.test.js` → GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/scoringService.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/training/ScoringService.js` istnieje, eksportuje `calculate`.
    - Plik importuje TYLKO z `./scoringWeights.js`. Brak THREE/DOM/store/gsap.
    - `tests/scoringService.test.js` ma ≥17 `it(...)`, wszystkie PASS.
    - Override `{weights:{critical:-50}}` z 1 critical → score 50 (testowane).
    - 4 critical → score 0 (floor); 5 critical → score 0 (floor nie schodzi poniżej).
    - Partial override (`{weights:{critical:-50}}`) zachowuje defaults dla medium/minor.
    - `Object.isFrozen(DEFAULT_WEIGHTS)` pozostaje true po wszystkich wywołaniach (immutability).
    - Pragma `// @vitest-environment node`.
  </acceptance_criteria>
  <done>SCORE-01 + TEST-02 spełnione: pure calculate z floor i override; ≥17 testów zielone; brak mutacji DEFAULT_WEIGHTS.</done>
</task>

<task type="auto">
  <name>Task 3: Utwórz tests/faultRules.test.js (SOP-07 invariant test)</name>
  <files>tests/faultRules.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md decyzja D-03 (faultRules globalny moduł, lista [{id, when, then, severity}])
    - src/training/faultRules.js (utworzony w Plan 02 — `faultRules` array + `evaluateFaultRulesData(state, rules?)`)
    - .planning/phases/01-foundation/01-VALIDATION.md § "Wave 0 Requirements" — `tests/faultRules.test.js` covers SOP-07
    - .planning/phases/01-foundation/01-RESEARCH.md § "Common Pitfalls" — guard-open-during-cycle to fundamentalna invariant
    - **Uwaga**: ten test importuje `src/training/faultRules.js` z Plan 02. Jeśli Plan 02 jeszcze nie ukończony, ten test wpada w "file not found" — wave 1 paraleliza. Po Plan 02 finish jeden retry `npm test` zielony. To jest acceptable per parallel execution model.
  </read_first>
  <action>
    Utwórz `tests/faultRules.test.js`:
    ```javascript
    // tests/faultRules.test.js
    // @vitest-environment node
    // SOP-07: evaluateFaultRules wykrywa cross-cutting safety invariants.

    import { describe, it, expect } from 'vitest';
    import { faultRules, evaluateFaultRulesData } from '../src/training/faultRules.js';

    describe('faultRules — data integrity (D-03)', () => {
      it('eksportuje niepustą tablicę', () => {
        expect(Array.isArray(faultRules)).toBe(true);
        expect(faultRules.length).toBeGreaterThan(0);
      });

      it('jest zamrożona (immutable, Object.isFrozen)', () => {
        expect(Object.isFrozen(faultRules)).toBe(true);
      });

      it('każda reguła ma keys {id, when, then, severity}', () => {
        for (const rule of faultRules) {
          expect(typeof rule.id).toBe('string');
          expect(typeof rule.when).toBe('function');
          expect(rule.then).toBeDefined();
          expect(Array.isArray(rule.then.effects)).toBe(true);
          expect(['critical', 'medium', 'minor']).toContain(rule.severity);
        }
      });

      it('reguła "oslona-otwarta-w-cyklu" istnieje (SOP-07 fundamentalny invariant)', () => {
        expect(faultRules.some(r => r.id === 'oslona-otwarta-w-cyklu')).toBe(true);
      });
    });

    describe('evaluateFaultRulesData — guard-open-during-cycle (SOP-07)', () => {
      it('wykrywa otwartą osłonę w cyklu pracy → emituje fault.triggered + setMachineState awaria', () => {
        const state = {
          machineState: 'w-cyklu',
          meshStates: { 'oslona-przednia': 'open' },
        };
        const effects = evaluateFaultRulesData(state);
        const fault = effects.find(e => e.type === 'appendEvent' && e.event?.type === 'fault.triggered');
        expect(fault).toBeDefined();
        expect(fault.event.severity).toBe('critical');
        expect(effects.some(e => e.type === 'setMachineState' && e.value === 'awaria')).toBe(true);
      });

      it('NIE wyzwala reguły gdy osłona zamknięta', () => {
        const state = {
          machineState: 'w-cyklu',
          meshStates: { 'oslona-przednia': 'closed' },
        };
        expect(evaluateFaultRulesData(state)).toEqual([]);
      });

      it('NIE wyzwala reguły gdy maszyna nie w cyklu', () => {
        const state = {
          machineState: 'oczekiwanie-na-inspekcje',
          meshStates: { 'oslona-przednia': 'open' },
        };
        expect(evaluateFaultRulesData(state)).toEqual([]);
      });

      it('przyjmuje custom rules array (override)', () => {
        const customRules = [{
          id: 'always-fires',
          when: () => true,
          then: { effects: [{ type: 'appendEvent', event: { type: 'fault.triggered', severity: 'minor' } }] },
          severity: 'minor',
        }];
        const r = evaluateFaultRulesData({ machineState: 'idle', meshStates: {} }, customRules);
        expect(r).toHaveLength(1);
      });

      it('catch w predykacie nie wywala scoringu (defensive)', () => {
        const buggyRules = [{
          id: 'buggy',
          when: () => { throw new Error('bug'); },
          then: { effects: [{ type: 'appendEvent', event: { type: 'fault.triggered', severity: 'critical' } }] },
          severity: 'critical',
        }];
        // Nie powinno rzucać; powinno zwrócić [] bo predicate failuje
        expect(() => evaluateFaultRulesData({ machineState: 'idle', meshStates: {} }, buggyRules)).not.toThrow();
        expect(evaluateFaultRulesData({ machineState: 'idle', meshStates: {} }, buggyRules)).toEqual([]);
      });

      it('akceptuje state bez meshStates field (defensive optional chaining)', () => {
        // state.meshStates może być undefined we wczesnym lifecycle
        expect(() => evaluateFaultRulesData({ machineState: 'oczekiwanie-na-inspekcje' })).not.toThrow();
      });
    });
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/faultRules.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/faultRules.test.js` istnieje, ma ≥10 `it(...)`.
    - `npx vitest run tests/faultRules.test.js` exit 0 (po Plan 02 finish — `src/training/faultRules.js` istnieje).
    - Pragma `// @vitest-environment node`.
    - Test guard-open-during-cycle pokazuje że `meshStates: {'oslona-przednia':'open'}` + `machineState:'w-cyklu'` → emituje `fault.triggered` event z severity:'critical' + `setMachineState awaria`.
    - Test "buggy predicate" potwierdza defensive try/catch w `evaluateFaultRulesData`.
  </acceptance_criteria>
  <done>SOP-07 spełniony: ≥10 testów dla faultRules zielone (po Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `intent` z UI/raycaster (Phase 3) → `validateStep` | future input — w Phase 1 tylko z testów |
| `state` snapshot z store → engine | trust boundary; engine traktuje state jako pure read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | malicious intent (Phase 3) — meshId spoofing aby pominąć krok | mitigate | `validateStep` SOP-08 hard gating: jakikolwiek mismatch = `step.violation` event z severity:medium; brak silent skip path |
| T-03-02 | Tampering | bypass `validateBefore` guard | mitigate | guard zwraca `{ok:false, reason:'forbidden-state'}` przed jakimikolwiek effects-on-success; testowane w `step-D` happy/fail case |
| T-03-03 | DoS | infinite recursion przez `evaluateFaultRules` w cyklu store-apply → fault → effect → fault... | accept | Phase 1 ma 1 regułę bez recursive trigger surface; Phase 4+ doda guard if needed (research note) |
| T-03-04 | Information Disclosure | `step.violation` event leakuje sensitive state | accept | Wszystkie pola eventu są kontrolowane przez scenariusz — brak surface dla user-controlled content |
| T-03-05 | DoS | rapid-fire validateStep z mismatched intent — czy generuje memory leak? | accept | `validateStep` jest pure i nic nie alokuje poza zwracanym objectem; GC normal pace; Phase 3 doda isAnimating lock dla full mitigation (CRIT-8) |
| T-03-06 | Tampering | mutowanie DEFAULT_WEIGHTS przez ScoringService | mitigate | `Object.freeze` w `scoringWeights.js`; `calculate` używa shallow spread `{...DEFAULT_WEIGHTS, ...opts}` → brak in-place modification |
</threat_model>

<verification>
- `npx vitest run tests/procedureEngine.test.js` zielone (≥18 testów).
- `npx vitest run tests/scoringService.test.js` zielone (≥17 testów).
- `npx vitest run tests/faultRules.test.js` zielone (≥10 testów) — wymaga Plan 02 ukończenia.
- `npm test -- --coverage` na końcu Wave 1 (po Plan 02 + Plan 03): coverage `src/training/**` (excluding scenarios) ≥95% lines/functions/statements, ≥90% branches.
- `git status` pokazuje untracked: `src/training/ProcedureEngine.js`, `src/training/ScoringService.js`, `tests/procedureEngine.test.js`, `tests/scoringService.test.js`, `tests/faultRules.test.js`.
</verification>

<success_criteria>
- SOP-01: `validateStep` jest pure top-level funkcją bez THREE/DOM/store/gsap imports (potwierdzone source-scan testem).
- SOP-08: Hard gating — żaden mismatched intent nie zwraca `{ok:true}` cicho.
- SCORE-01: `ScoringService.calculate` jest pure, używa frozen DEFAULT_WEIGHTS, supports override.
- TEST-01: ProcedureEngine coverage ≥95% (po complete Wave 1 — full coverage report w Plan 04 lub finalnie w Plan 05).
- TEST-02: ScoringService testy pokrywają default + override + floor + filtering.
- TEST-04 zalążek: idempotency test demonstruje pure-function determinism (100x identical results).
- Wszystkie acceptance criteria 3 tasków spełnione.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` zawierający:
- Lista 5 utworzonych plików.
- Kontrakt: `evaluateFaultRules` re-eksportowane z `ProcedureEngine.js` (delegacja do `evaluateFaultRulesData` z `faultRules.js`) — Plan 04 store importuje WYŁĄCZNIE z `ProcedureEngine.js`.
- Pokrycie po Wave 1 (jeśli Plan 02 + Plan 03 ukończone): wynik `npm test -- --coverage`.
- Idempotency strategy: w Phase 1 idempotency to property pure function; pełny lock `isAnimating` w storze trafia do Phase 3 (INTERACT-05). Plan 04 tylko subscribuje effects — żadnego async work.
- Komendy weryfikacji + output.
</output>
