---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-02", "01-03"]
files_modified:
  - src/state/trainingStore.js
  - src/main.js
  - src/SceneSetup.js
  - tests/trainingStore.test.js
  - tests/uruchomienie.integration.test.js
  - tests/application.test.js
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, SOP-09, TEST-04]
must_haves:
  truths:
    - "`createTrainingStore()` zwraca zustand vanilla store z `getState`, `setState`, `subscribe`"
    - "Store ma flat shape: `session`, `currentStepId`, `steps`, `machineState`, `meshStates`, `events`, `scoring` (D-Claude'a-Discretion: flat z grupami)"
    - "Store importuje `validateStep` i `evaluateFaultRules` z `ProcedureEngine.js` (single import point — re-eksport z faultRules)"
    - "`store.startScenario(scenario)` ustawia currentStepId = pierwszy krok scenariusza, machineState = `initialMachineState`, dodaje `session.start` event"
    - "`store.attemptStep(intent, scenario)` woła validateStep → applyEffects → evaluateFaultRules → applyEffects (chain) — wszystko sync"
    - "Effect `startSpinUpTimer` aplikowany przez store przez `scheduleTimer` (default `setTimeout`); pod `vi.useFakeTimers()` `vi.advanceTimersByTime(3000)` przechodzi machineState `rozpedzanie` → `gotowa-do-pracy` (D-07/D-08)"
    - "Integration test: scenariusz `uruchomienie` przechodzi end-to-end przez 8 kroków happy path + 2 failure paths (out-of-order click, sprzęgnięcie przed rozpędem) — SOP-09"
    - "`Application` ma `dispose()` które unsubsubuje wszystkie subscribers + usuwa GSAP ticker callback + zwalnia DisclaimerBanner.dispose() + SceneSetup.dispose() (banner pojawia się w Plan 05; Application.dispose musi obsłużyć go conditional)"
    - "`import.meta.hot?.dispose(() => app?.dispose())` wpięte w `src/main.js`"
    - "`SceneSetup.dispose()` removes resize listener (existing) + WebGL context-loss listeners (Plan 05 dodaje listenery — w tym planie tylko `dispose()` skeleton + zachowuje resize unbind reference)"
    - "Po HMR reload nie rośnie liczba aktywnych subscriberów (smoke test mockowany)"
  artifacts:
    - path: "src/state/trainingStore.js"
      provides: "createTrainingStore + applyEffects reducer"
      exports: ["createTrainingStore"]
      min_lines: 100
    - path: "src/main.js"
      provides: "Application z dispose() + tickables + HMR hook + TrainingStore instantiation"
      contains: "import.meta.hot"
    - path: "src/SceneSetup.js"
      provides: "skeleton dispose() (Plan 05 dodaje WebGL listeners)"
      contains: "dispose()"
    - path: "tests/trainingStore.test.js"
      provides: "STATE-01 + STATE-03 unit tests + spinUp timer fake-timers test"
      min_lines: 100
    - path: "tests/uruchomienie.integration.test.js"
      provides: "SOP-09 end-to-end happy + 2 failure paths"
      min_lines: 80
    - path: "tests/application.test.js"
      provides: "STATE-03 dispose smoke (mock store, assert unsubscribe called)"
  key_links:
    - from: "src/state/trainingStore.js"
      to: "src/training/ProcedureEngine.js"
      via: "import { validateStep, evaluateFaultRules }"
      pattern: "from '\\.\\./training/ProcedureEngine\\.js'"
    - from: "src/state/trainingStore.js"
      to: "zustand/vanilla"
      via: "import { createStore }"
      pattern: "from 'zustand/vanilla'"
    - from: "src/main.js"
      to: "src/state/trainingStore.js"
      via: "import { createTrainingStore }"
      pattern: "createTrainingStore"
---

<objective>
Wave 2 część 1: zbuduj zustand vanilla `TrainingStore` (jedyny mutowalny shared state, STATE-01), wpij `Application.dispose()` z Vite HMR hook (STATE-03), napisz integration test scenariusza `uruchomienie` (SOP-09 — happy + 2 failure paths). Plan 05 (parallel — Wave 2) dodaje DisclaimerBanner + WebGL context-loss listeners + boundaries.test.js.

Plan 04 modyfikuje `src/main.js` (dodaje `dispose`, `tickables`, HMR hook, TrainingStore + DisclaimerBanner instantiation as **conditional** — Plan 05 wpina banner; w tym planie tylko **przygotowanie miejsca** w postaci `this.disclaimerBanner = null` + `if (this.disclaimerBanner) this.disclaimerBanner.dispose()` w `dispose()`). Plan 05 wypełnia banner.

Plan 04 modyfikuje `src/SceneSetup.js` aby DODAĆ `dispose()` shell + zachować referencję do bound resize handler (`this._onWindowResizeBound`). Plan 05 wykorzystuje ten shell aby DODAĆ WebGL context-loss listeners. **Boundary**: oba plany dotykają `src/SceneSetup.js`, ale Plan 04 dotyka `dispose()` + `_onWindowResizeBound`, Plan 05 dotyka context-loss listeners — różne sekcje pliku. **Decyzja plannera**: ŻEBY UNIKNĄĆ KONFLIKTU EDYCYJNEGO: Plan 04 wykonuje `src/SceneSetup.js` zmianę PIERWSZY (Wave 2 sequence: 04 przed 05 jeśli oba w Wave 2). Wymusza to modyfikację — Plan 05 zależy od Plan 04 w tym aspekcie. Aktualizuję `Plan 05 depends_on: ["01-04"]` w plan 05 frontmatter.

Aby Wave 2 NIE była fully sequential, Plan 04 i Plan 05 mogą iść parallel JEŚLI Plan 04 nie dotyka SceneSetup w ogóle. **Re-ocena**: Plan 04 może NIE modyfikować SceneSetup wcale w Wave 2 — istniejący `addEventListener('resize', this.onWindowResize.bind(this))` ma utracony reference (anonymous bind) ALE STATE-03 musi dispose go. **Resolution**: Plan 04 modyfikuje SceneSetup do _onWindowResizeBound + dispose() shell. Plan 05 dodaje context-loss listeners + rozszerza dispose. Pliki overlap → SEQUENTIAL (05 po 04). Plan 04 finiszuje pierwszy, Plan 05 startuje.

Purpose: STATE-01 (jedyny store), STATE-03 (no leaks), SOP-09 integration (uruchomienie playable). To jest "tying it all together" plan: silnik + scenariusz + scoring + store + Application — wszystko skleja się w działający łańcuch sprawdzalny w Vitest.

Output: TrainingStore w Node testowalny pod fake timers; Application z dispose+HMR; integration test 8-step happy path + sprzęgnięcie-przed-rozpędem critical violation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- TrainingStore signature (RESEARCH §C linie 776–917 verbatim) -->

```typescript
export function createTrainingStore(opts?: {
  now?: () => number,                                    // injectable clock (test)
  scheduleTimer?: (fn: Function, ms: number) => any,     // injectable timer (test)
}): {
  getState: () => StoreState,
  setState: (partial) => void,
  subscribe: (listener) => () => void,
  // ... + zustand subscribeWithSelector middleware
}

// StoreState shape (D-Claude's Discretion: flat z grupami):
{
  session: { scenarioId, startedAt, finishedAt, retryCount },
  currentStepId: string|null,
  steps: { [id]: { status: 'pending'|'active'|'done'|'error' } },
  machineState: string,
  meshStates: { [meshId]: string },
  events: Array<{ type, ...payload }>,
  scoring: { score, criticalCount, mediumCount, minorCount },
  _now: () => number,
  _spinUpTimerHandle: any,

  // actions (closures over set/get):
  startScenario(scenario): void,
  attemptStep(intent, scenario): void,
  _onSpinUpComplete(): void,
}
```

Application signature (RESEARCH §"Pattern 3" linie 397–451):
```javascript
class Application {
  constructor() {
    this.sceneSetup = new SceneSetup('three-canvas');
    this.pressModel = new PressModel(this.sceneSetup.scene);
    this.ui = new UI();
    this.disclaimerBanner = null;  // Plan 05 sets this
    this.store = createTrainingStore();
    this.currentAngle = 0;
    this.tickables = [(dt) => this.simulationTick(dt)];
    this._tickerCallback = (time, dt) => {
      for (const fn of this.tickables) fn(dt);
      this.sceneSetup.render();
    };
    gsap.ticker.add(this._tickerCallback);
    this._unsubscribers = [];
  }
  simulationTick(dt) { /* moved from existing tick() */ }
  dispose() {
    gsap.ticker.remove(this._tickerCallback);
    for (const u of this._unsubscribers) u();
    if (this.disclaimerBanner) this.disclaimerBanner.dispose();
    this.sceneSetup.dispose();
  }
}

let app;
document.addEventListener('DOMContentLoaded', () => { app = new Application(); });
if (import.meta.hot) {
  import.meta.hot.dispose(() => { if (app) app.dispose(); });
}
```

zustand vanilla import (RESEARCH §C linia 781–782):
```javascript
import { createStore } from 'zustand/vanilla';
import { subscribeWithSelector } from 'zustand/middleware';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utwórz src/state/trainingStore.js + tests/trainingStore.test.js</name>
  <files>src/state/trainingStore.js, tests/trainingStore.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "C. trainingStore.js" (linie 774–917) — VERBATIM full file (createTrainingStore + applyEffects + applyScoringEvent)
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-08 (timer w storze, nie w engine), D-17 (event types), D-Claude'a-Discretion (flat slice)
    - .planning/phases/01-foundation/01-PATTERNS.md § "src/state/trainingStore.js" (linie 260–280)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pitfall 6: GSAP timer pod fake timers" (linie 565–578) — krytyczne dla testowania timera rozpędu
    - tests/fixtures/scenario.fixture.js (utworzony w Plan 02 — `minimalScenario` 3-step)
    - src/training/scenarios/uruchomienie.js (utworzony w Plan 02 — 8 kroków)
    - src/training/ProcedureEngine.js (utworzony w Plan 03 — re-eksportuje evaluateFaultRules)
    - src/training/faultRules.js (utworzony w Plan 02)
  </read_first>
  <behavior>
    - **Smoke**: `createTrainingStore()` zwraca obiekt z `getState`, `setState`, `subscribe`.
    - **Initial state**: po `createTrainingStore()` (bez startScenario) `getState()` zwraca shape ze wszystkimi 7 grupami (session/currentStepId/steps/machineState/meshStates/events/scoring), score=100.
    - **startScenario**: `store.getState().startScenario(minimalScenario)` ustawia currentStepId='step-visual-target', machineState='oczekiwanie-na-inspekcje', events=[session.start], steps={step-visual-target:'pending', step-visual-attest:'pending', step-manipulation:'pending'}.
    - **attemptStep happy**: po startScenario, `store.getState().attemptStep({kind:'click', meshId:'mesh-A'}, minimalScenario)` advansuje currentStepId='step-visual-attest', dodaje events: `[session.start, step.done(step-visual-target)]`, steps['step-visual-target'].status='done'.
    - **attemptStep wrong-target**: `store.getState().attemptStep({kind:'click', meshId:'wrong'}, minimalScenario)` zostawia currentStepId='step-visual-target' (no advance), dodaje event step.violation severity=medium, scoring.score=90 (-10 medium), scoring.mediumCount=1.
    - **fault rules execution**: po set state ze stanem awarii (machineState='w-cyklu', meshStates['oslona-przednia']='open'), `attemptStep` (lub manualne wywołanie evaluateFaultRules pipeline) emituje fault.triggered + setMachineState 'awaria'.
    - **Subscribe returns unsubscribe**: `const unsub = store.subscribe(()=>{}); typeof unsub === 'function'`.
    - **Subscriber receives state**: po subscribe + setState, listener wołany.
    - **startSpinUpTimer pod fake timers (D-08)**: vi.useFakeTimers() + startScenario(uruchomienie) + advance through steps 1-7 (visual + manipulation kroki) → po wlacz-zasilanie store.machineState === 'rozpedzanie'; vi.advanceTimersByTime(3000); store.machineState === 'gotowa-do-pracy'.
    - **Custom scheduleTimer (test injection)**: `createTrainingStore({scheduleTimer: customFn})` — store używa customFn zamiast setTimeout.
    - **Custom now (test injection)**: `createTrainingStore({now: () => 999})` — events mają `timestamp:999`.
    - **applyScoringEvent**: live scoring updejtuje się przy każdym scorable event (step.violation z severity).
  </behavior>
  <action>
    1. **RED**: napisz `tests/trainingStore.test.js`:
       ```javascript
       // tests/trainingStore.test.js
       // @vitest-environment node
       // STATE-01, STATE-03 + D-08 spin-up timer test pod fake timers.

       import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
       import { createTrainingStore } from '../src/state/trainingStore.js';
       import { minimalScenario } from './fixtures/scenario.fixture.js';
       import uruchomienie from '../src/training/scenarios/uruchomienie.js';

       describe('TrainingStore — smoke (STATE-01)', () => {
         it('createTrainingStore zwraca object z getState/setState/subscribe', () => {
           const store = createTrainingStore();
           expect(typeof store.getState).toBe('function');
           expect(typeof store.setState).toBe('function');
           expect(typeof store.subscribe).toBe('function');
         });

         it('initial state ma 7 grup (flat slice)', () => {
           const s = createTrainingStore().getState();
           expect(s.session).toBeDefined();
           expect(s.currentStepId).toBeNull();
           expect(s.steps).toBeDefined();
           expect(s.machineState).toBeDefined();
           expect(s.meshStates).toBeDefined();
           expect(Array.isArray(s.events)).toBe(true);
           expect(s.scoring).toMatchObject({ score: 100, criticalCount: 0, mediumCount: 0, minorCount: 0 });
         });

         it('subscribe zwraca unsubscribe handle', () => {
           const store = createTrainingStore();
           const unsub = store.subscribe(() => {});
           expect(typeof unsub).toBe('function');
           expect(() => unsub()).not.toThrow();
         });

         it('listener jest wołany po setState', () => {
           const store = createTrainingStore();
           const listener = vi.fn();
           store.subscribe(listener);
           store.setState({ machineState: 'test-marker' });
           expect(listener).toHaveBeenCalled();
         });
       });

       describe('TrainingStore — startScenario / attemptStep (minimalScenario)', () => {
         it('startScenario inicjalizuje state', () => {
           const store = createTrainingStore({ now: () => 1000 });
           store.getState().startScenario(minimalScenario);
           const s = store.getState();
           expect(s.currentStepId).toBe('step-visual-target');
           expect(s.machineState).toBe('oczekiwanie-na-inspekcje');
           expect(s.session.scenarioId).toBe('fixture-minimal');
           expect(s.session.startedAt).toBe(1000);
           expect(s.events).toHaveLength(1);
           expect(s.events[0].type).toBe('session.start');
         });

         it('attemptStep happy advansuje step i dodaje step.done event', () => {
           const store = createTrainingStore({ now: () => 1000 });
           store.getState().startScenario(minimalScenario);
           store.getState().attemptStep({ kind: 'click', meshId: 'mesh-A' }, minimalScenario);
           const s = store.getState();
           expect(s.currentStepId).toBe('step-visual-attest');
           expect(s.steps['step-visual-target'].status).toBe('done');
           expect(s.events.some(e => e.type === 'step.done' && e.stepId === 'step-visual-target')).toBe(true);
         });

         it('attemptStep wrong-target NIE advansuje + dodaje step.violation + obniża score', () => {
           const store = createTrainingStore({ now: () => 1000 });
           store.getState().startScenario(minimalScenario);
           store.getState().attemptStep({ kind: 'click', meshId: 'wrong-mesh' }, minimalScenario);
           const s = store.getState();
           expect(s.currentStepId).toBe('step-visual-target'); // no advance
           expect(s.events.some(e => e.type === 'step.violation' && e.severity === 'medium')).toBe(true);
           expect(s.scoring.score).toBe(90);
           expect(s.scoring.mediumCount).toBe(1);
         });

         it('attemptStep ignoruje gdy currentStepId === null (graceful)', () => {
           const store = createTrainingStore({ now: () => 1000 });
           // bez startScenario — currentStepId === null
           expect(() => store.getState().attemptStep({ kind: 'click', meshId: 'x' }, minimalScenario)).not.toThrow();
           expect(store.getState().scoring.score).toBe(100); // no-active-step nie generuje violation
         });
       });

       describe('TrainingStore — spinUpTimer pod fake timers (D-07/D-08)', () => {
         beforeEach(() => { vi.useFakeTimers(); });
         afterEach(() => { vi.useRealTimers(); });

         it('startSpinUpTimer aplikowany przez store; advance 3000ms → gotowa-do-pracy', () => {
           const store = createTrainingStore({ now: () => 1000 });
           store.getState().startScenario(uruchomienie);
           // Advance through 7 steps (1-7) bez sprzegnij-po-rozpedzie
           const steps = uruchomienie.steps;
           // Step 1: visual-target tabliczka
           store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' }, uruchomienie);
           // Step 2: visual-attest kontrola-narzedzia
           store.getState().attemptStep({ kind: 'check', stepId: 'kontrola-narzedzia' }, uruchomienie);
           // Step 3: visual-attest kontrola-wzrokowa
           store.getState().attemptStep({ kind: 'check', stepId: 'kontrola-wzrokowa' }, uruchomienie);
           // Step 4: visual-target wziernik
           store.getState().attemptStep({ kind: 'click', meshId: 'wziernik-smarowania' }, uruchomienie);
           // Step 5: manipulation oslona
           store.getState().attemptStep({ kind: 'click', meshId: 'oslona-przednia' }, uruchomienie);
           // Step 6: manipulation estop
           store.getState().attemptStep({ kind: 'click', meshId: 'estop' }, uruchomienie);
           // Step 7: manipulation wlacznik
           store.getState().attemptStep({ kind: 'click', meshId: 'wylacznik-glowny' }, uruchomienie);

           // Po wlacz-zasilanie machineState powinien być 'rozpedzanie'
           expect(store.getState().machineState).toBe('rozpedzanie');

           // Advance fake timer 3000ms → spinUp complete
           vi.advanceTimersByTime(3000);
           expect(store.getState().machineState).toBe('gotowa-do-pracy');
         });

         it('custom scheduleTimer override jest używany zamiast setTimeout', () => {
           const customSchedule = vi.fn((fn, ms) => fn()); // immediate
           const store = createTrainingStore({ now: () => 1000, scheduleTimer: customSchedule });
           // Wstaw bezpośrednio effect startSpinUpTimer (skip 7 steps)
           store.setState({ currentStepId: 'wlacz-zasilanie' });
           // Trigger pipeline z manipulation intent na wlacznik (powinien przejść)
           // Najpierw musimy mieć startScenario aby dispatch pipeline działał — używamy minimal
           const tinyScenario = {
             id: 't', steps: [{
               id: 'X', kind: 'manipulation', targetMeshId: 'm',
               labelPL: '', descriptionPL: '', rationalePL: '',
               effectsOnSuccess: [{ type: 'startSpinUpTimer', ms: 1500 }],
               effectsOnError: [],
             }],
           };
           const s2 = createTrainingStore({ now: () => 1000, scheduleTimer: customSchedule });
           s2.getState().startScenario(tinyScenario);
           s2.getState().attemptStep({ kind: 'click', meshId: 'm' }, tinyScenario);
           expect(customSchedule).toHaveBeenCalled();
           expect(customSchedule.mock.calls[0][1]).toBe(1500);
         });
       });

       describe('TrainingStore — STATE-03 dispose pattern signals', () => {
         it('subscribe handle zwalnia listener po unsub', () => {
           const store = createTrainingStore();
           const listener = vi.fn();
           const unsub = store.subscribe(listener);
           store.setState({ machineState: 'A' });
           const callsBefore = listener.mock.calls.length;
           unsub();
           store.setState({ machineState: 'B' });
           expect(listener.mock.calls.length).toBe(callsBefore);
         });
       });
       ```
    2. Uruchom `npm test -- tests/trainingStore.test.js` → RED.
    3. **GREEN**: utwórz `src/state/trainingStore.js` (VERBATIM z RESEARCH §C linie 776–917, z drobnymi adjustmentami):
       ```javascript
       // src/state/trainingStore.js
       // Centralny store stanu szkolenia (STATE-01). Jedyne mutowalne wspólne state'a.
       // NIE importuje THREE/DOM/gsap (boundaries.test.js enforce).

       import { createStore } from 'zustand/vanilla';
       import { subscribeWithSelector } from 'zustand/middleware';
       import { validateStep, evaluateFaultRules } from '../training/ProcedureEngine.js';
       import { faultRules } from '../training/faultRules.js';

       /**
        * Tworzy nową instancję store'a. Phase 1 wpina go w Application;
        * Phase 4 będzie dodawał subscribers przez panele DOM.
        *
        * @param {object} [opts]
        * @param {() => number} [opts.now]            - injectable clock (test override)
        * @param {(fn:Function, ms:number) => any} [opts.scheduleTimer]  - injectable timer
        * @returns {ReturnType<typeof createStore>}
        */
       export function createTrainingStore(opts = {}) {
         const now = opts.now ?? (() => Date.now());
         const scheduleTimer = opts.scheduleTimer ?? ((fn, ms) => setTimeout(fn, ms));

         return createStore(
           subscribeWithSelector((set, get) => ({
             session: { scenarioId: null, startedAt: null, finishedAt: null, retryCount: 0 },
             currentStepId: null,
             steps: {},
             machineState: 'oczekiwanie-na-inspekcje',
             meshStates: {},
             events: [],
             scoring: { score: 100, criticalCount: 0, mediumCount: 0, minorCount: 0 },
             _now: now,
             _spinUpTimerHandle: null,

             startScenario: (scenario) => set({
               session: { scenarioId: scenario.id, startedAt: now(), finishedAt: null, retryCount: 0 },
               steps: Object.fromEntries(scenario.steps.map(s => [s.id, { status: 'pending' }])),
               currentStepId: scenario.steps[0].id,
               machineState: scenario.initialMachineState ?? 'oczekiwanie-na-inspekcje',
               meshStates: {},
               events: [{ type: 'session.start', scenarioId: scenario.id, timestamp: now() }],
               scoring: { score: 100, criticalCount: 0, mediumCount: 0, minorCount: 0 },
             }),

             attemptStep: (intent, scenario) => {
               const state = get();
               const result = validateStep(intent, state, scenario);
               applyEffects(set, get, result.effects, scheduleTimer);
               // Fault rules na nowym state (PO applied effects)
               const faultEffects = evaluateFaultRules(get(), faultRules);
               if (faultEffects.length > 0) {
                 applyEffects(set, get, faultEffects, scheduleTimer);
               }
             },

             _onSpinUpComplete: () => set(s => ({
               machineState: 'gotowa-do-pracy',
               events: [...s.events, { type: 'session.spinUp.done', timestamp: now() }],
             })),
           }))
         );
       }

       /**
        * Aplikuje effects array do store'a. Pure reducer dispatch.
        * D-02 closed type set: setMachineState, setMeshState, appendEvent,
        * advanceStep, startSpinUpTimer, playAudio.
        */
       function applyEffects(set, get, effects, scheduleTimer) {
         for (const effect of effects) {
           switch (effect.type) {
             case 'setMachineState':
               set({ machineState: effect.value });
               break;
             case 'setMeshState':
               set(s => ({ meshStates: { ...s.meshStates, [effect.meshId]: effect.value } }));
               break;
             case 'appendEvent':
               set(s => ({ events: [...s.events, effect.event] }));
               if (effect.event.severity) {
                 set(s => ({ scoring: applyScoringEvent(s.scoring, effect.event.severity) }));
               }
               break;
             case 'advanceStep': {
               const state = get();
               if (!state.currentStepId) break;
               const stepIds = Object.keys(state.steps);
               const currentIdx = stepIds.indexOf(state.currentStepId);
               const nextId = stepIds[currentIdx + 1] ?? null;
               set({
                 currentStepId: nextId,
                 steps: {
                   ...state.steps,
                   [state.currentStepId]: { status: 'done' },
                 },
               });
               break;
             }
             case 'startSpinUpTimer': {
               // D-07/D-08: store odpala scheduleTimer (default setTimeout).
               // Pod vi.useFakeTimers() advanceTimersByTime przekręca zegar.
               const handle = scheduleTimer(() => get()._onSpinUpComplete(), effect.ms);
               set({ _spinUpTimerHandle: handle });
               break;
             }
             case 'playAudio':
               // Phase 5 implementuje. Phase 1 NO-OP.
               break;
             default:
               // Unknown effect — log w dev. Phase 1 silent (testy nie powinny
               // generować unknown effects; integration test sprawdza pipeline).
               break;
           }
         }
       }

       /** Live scoring counter — używa default weights (-25/-10/-2). */
       function applyScoringEvent(scoring, severity) {
         const next = { ...scoring };
         if (severity === 'critical') next.criticalCount += 1;
         else if (severity === 'medium') next.mediumCount += 1;
         else if (severity === 'minor') next.minorCount += 1;
         next.score = Math.max(0, 100 + next.criticalCount * -25 + next.mediumCount * -10 + next.minorCount * -2);
         return next;
       }
       ```
    4. Uruchom `npx vitest run tests/trainingStore.test.js` → GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/trainingStore.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/state/trainingStore.js` istnieje, eksportuje `createTrainingStore`.
    - Plik importuje z `zustand/vanilla`, `zustand/middleware`, `../training/ProcedureEngine.js`, `../training/faultRules.js`. NIE importuje `three`, `gsap`, DOM globals.
    - `tests/trainingStore.test.js` ma ≥10 testów; wszystkie PASS.
    - Test fake-timers: po 7-step playthrough z `uruchomienie` machineState === `'rozpedzanie'`; po `vi.advanceTimersByTime(3000)` machineState === `'gotowa-do-pracy'`.
    - Wrong-target obniża `scoring.score` z 100 → 90 (medium = -10).
    - `startScenario` ustawia `events[0].type === 'session.start'` z `timestamp` z injectable now.
    - Pragma `// @vitest-environment node`.
  </acceptance_criteria>
  <done>STATE-01 spełniony: store jest jedynym mutowalnym shared state'm, fake-timers spinUp test zielony.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test scenariusza uruchomienie (SOP-09 happy + 2 failure paths)</name>
  <files>tests/uruchomienie.integration.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pattern 1" data flow diagram (linie 243–254) — Vitest test → store.startScenario → attemptStep → engine → effects → state advance
    - src/training/scenarios/uruchomienie.js (Plan 02)
    - src/state/trainingStore.js (Task 1 powyżej)
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-06 (8 kroków), D-07 (rozpęd timer)
    - .planning/phases/01-foundation/01-VALIDATION.md § "Wave 0 Requirements" — `tests/uruchomienie.integration.test.js` covers SOP-03, SOP-09
    - ROADMAP.md Phase 1 SC2 — "scenariusz uruchomienie playable end-to-end; out-of-order, forbidden-state, double-click → visible failures"
  </read_first>
  <behavior>
    - **Happy path (8 kroków)**: po startScenario(uruchomienie) + 8 attemptStep w dobrej kolejności + advance timer 3000ms między step 7 i 8 → wszystkie steps[id].status='done', currentStepId=null, machineState='w-cyklu', events zawierają 8 step.done.
    - **Failure path 1 — out-of-order (medium)**: na początku scenariusza klik mesh `estop` (zamiast tabliczki) → events ma step.violation severity=medium errorCode=E-NIEPRAWIDLOWY-MESH; currentStepId nadal 'sprawdz-tabliczke'; scoring.score=90.
    - **Failure path 2 — forbidden-state (critical)**: po przejściu do step 'sprzegnij-po-rozpedzie' BEZ czekania 3s (machineState wciąż 'rozpedzanie') klik 'dzwignia-sprzegla' → events ma step.violation severity=critical errorCode=E-SPRZEGNIETO-PRZED-ROZPEDEM; scoring.score=75 (-25 critical) lub niżej zależnie od poprzednich błędów.
  </behavior>
  <action>
    Utwórz `tests/uruchomienie.integration.test.js`:
    ```javascript
    // tests/uruchomienie.integration.test.js
    // @vitest-environment node
    // SOP-03 + SOP-09: scenariusz `uruchomienie` end-to-end (Phase 1 subset; Phase 6 dorzuca pozostałe scenariusze).

    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import { createTrainingStore } from '../src/state/trainingStore.js';
    import uruchomienie from '../src/training/scenarios/uruchomienie.js';

    function playSteps1to7(store) {
      // Visual + manipulation kroki 1-7 (BEZ sprzegnij-po-rozpedzie).
      store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' }, uruchomienie);
      store.getState().attemptStep({ kind: 'check', stepId: 'kontrola-narzedzia' }, uruchomienie);
      store.getState().attemptStep({ kind: 'check', stepId: 'kontrola-wzrokowa' }, uruchomienie);
      store.getState().attemptStep({ kind: 'click', meshId: 'wziernik-smarowania' }, uruchomienie);
      store.getState().attemptStep({ kind: 'click', meshId: 'oslona-przednia' }, uruchomienie);
      store.getState().attemptStep({ kind: 'click', meshId: 'estop' }, uruchomienie);
      store.getState().attemptStep({ kind: 'click', meshId: 'wylacznik-glowny' }, uruchomienie);
    }

    describe('uruchomienie integration — happy path (SOP-03/SOP-09)', () => {
      beforeEach(() => { vi.useFakeTimers(); });
      afterEach(() => { vi.useRealTimers(); });

      it('8 kroków w kolejności → wszystkie done, machineState w-cyklu', () => {
        const store = createTrainingStore({ now: () => 1000 });
        store.getState().startScenario(uruchomienie);

        playSteps1to7(store);
        // Po wlacz-zasilanie: machineState === 'rozpedzanie'
        expect(store.getState().machineState).toBe('rozpedzanie');

        vi.advanceTimersByTime(3000);
        expect(store.getState().machineState).toBe('gotowa-do-pracy');

        // Step 8: sprzegnij-po-rozpedzie
        store.getState().attemptStep({ kind: 'click', meshId: 'dzwignia-sprzegla' }, uruchomienie);

        const final = store.getState();
        expect(final.machineState).toBe('w-cyklu');
        expect(final.currentStepId).toBeNull();
        // Wszystkie 8 step.done events
        const doneCount = final.events.filter(e => e.type === 'step.done').length;
        expect(doneCount).toBe(8);
        // Brak violations
        const violations = final.events.filter(e => e.type === 'step.violation');
        expect(violations).toHaveLength(0);
        expect(final.scoring.score).toBe(100);
      });
    });

    describe('uruchomienie integration — failure path: out-of-order (SOP-08, SOP-09)', () => {
      it('klik estop na samym początku → step.violation medium, score 90', () => {
        const store = createTrainingStore({ now: () => 1000 });
        store.getState().startScenario(uruchomienie);

        // Powinno czekać tabliczki, ale klikamy estop
        store.getState().attemptStep({ kind: 'click', meshId: 'estop' }, uruchomienie);

        const s = store.getState();
        expect(s.currentStepId).toBe('sprawdz-tabliczke'); // no advance
        const violation = s.events.find(e => e.type === 'step.violation');
        expect(violation).toBeDefined();
        expect(violation.severity).toBe('medium');
        expect(violation.errorCode).toBe('E-NIEPRAWIDLOWY-MESH');
        expect(s.scoring.score).toBe(90);
        expect(s.scoring.mediumCount).toBe(1);
      });
    });

    describe('uruchomienie integration — failure path: forbidden-state (SOP-08, SOP-09)', () => {
      beforeEach(() => { vi.useFakeTimers(); });
      afterEach(() => { vi.useRealTimers(); });

      it('sprzęgnięcie przed nabraniem obrotów → critical violation E-SPRZEGNIETO-PRZED-ROZPEDEM', () => {
        const store = createTrainingStore({ now: () => 1000 });
        store.getState().startScenario(uruchomienie);

        playSteps1to7(store);
        expect(store.getState().machineState).toBe('rozpedzanie');

        // NIE czekamy 3 sekund. Klikamy dzwignia-sprzegla zbyt wcześnie.
        store.getState().attemptStep({ kind: 'click', meshId: 'dzwignia-sprzegla' }, uruchomienie);

        const s = store.getState();
        const violation = s.events.find(e => e.type === 'step.violation' && e.errorCode === 'E-SPRZEGNIETO-PRZED-ROZPEDEM');
        expect(violation).toBeDefined();
        expect(violation.severity).toBe('critical');
        expect(s.scoring.criticalCount).toBe(1);
        expect(s.scoring.score).toBe(75);
        // currentStepId nadal sprzegnij-po-rozpedzie (forbidden-state nie advansuje)
        expect(s.currentStepId).toBe('sprzegnij-po-rozpedzie');
      });
    });

    describe('uruchomienie integration — double-click stress (CRIT-8 zalążek)', () => {
      it('100x ten sam mesh-click w jednym tick — emituje co najwyżej 1 step.done', () => {
        const store = createTrainingStore({ now: () => 1000 });
        store.getState().startScenario(uruchomienie);
        // 100 razy klik tabliczki
        for (let i = 0; i < 100; i++) {
          store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' }, uruchomienie);
        }
        const s = store.getState();
        // Pierwszy klik advansuje currentStepId; kolejne 99 to wrong-target (mesh nie pasuje do step 2)
        const doneEvents = s.events.filter(e => e.type === 'step.done' && e.stepId === 'sprawdz-tabliczke');
        expect(doneEvents).toHaveLength(1);
        // Reszta to violations (99 medium)
        const violations = s.events.filter(e => e.type === 'step.violation');
        expect(violations.length).toBeGreaterThan(0);
      });
    });
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/uruchomienie.integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/uruchomienie.integration.test.js` istnieje, ma 4 `describe` (happy + 2 failure + double-click).
    - Happy path test pokazuje 8 step.done events i finalny `machineState === 'w-cyklu'`.
    - Out-of-order test pokazuje severity=medium, errorCode=E-NIEPRAWIDLOWY-MESH, score=90.
    - Forbidden-state test pokazuje severity=critical, errorCode=E-SPRZEGNIETO-PRZED-ROZPEDEM, score=75.
    - Double-click test (100x) pokazuje dokładnie 1 step.done event dla pierwszego kroku (kolejne 99 to wrong-target).
    - `npx vitest run tests/uruchomienie.integration.test.js` exit 0.
    - Pragma `// @vitest-environment node`.
  </acceptance_criteria>
  <done>SOP-03 + SOP-09 spełnione: scenariusz uruchomienie playable end-to-end z happy path + 2 failure paths + double-click resilience.</done>
</task>

<task type="auto">
  <name>Task 3: Modyfikuj src/main.js (Application.dispose + tickables + HMR + store) + src/SceneSetup.js (dispose shell + bound resize) + tests/application.test.js</name>
  <files>src/main.js, src/SceneSetup.js, tests/application.test.js</files>
  <read_first>
    - src/main.js (cały — po Plan 01 modyfikacjach: ma już modulo 2π i komentarz deltaTime; widoczny w &lt;interfaces&gt; Plan 01)
    - src/SceneSetup.js (cały — line 42 `window.addEventListener('resize', this.onWindowResize.bind(this))` ma utracony reference)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pattern 3: Tickable list" (linie 388–451) — verbatim Application class shape z dispose()
    - .planning/phases/01-foundation/01-PATTERNS.md § "src/main.js MODIFIED" (linie 352–380), § "src/SceneSetup.js MODIFIED" (linie 384–419)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pitfall 4: Subscriber leaks na HMR" (linie 540–551) — STATE-03 lock
    - .planning/phases/01-foundation/01-RESEARCH.md § "Open Questions" #4 (PressModel.dispose stub) — research rekomenduje no-op stub w Phase 1; **DECYZJA PLANNERA**: NIE dodajemy `pressModel.dispose()` w tym planie (Phase 2 to wprowadza). Application.dispose() po prostu nie woła `pressModel.dispose()`.
  </read_first>
  <action>
    1. **Modyfikuj `src/SceneSetup.js`** — dodaj bound resize handler reference + `dispose()` shell (Plan 05 doda WebGL listenery do tego dispose):
       - W konstruktorze, ZAMIEŃ `window.addEventListener('resize', this.onWindowResize.bind(this));` na:
         ```javascript
         // Bound reference dla późniejszego dispose() (STATE-03)
         this._onWindowResizeBound = this.onWindowResize.bind(this);
         window.addEventListener('resize', this._onWindowResizeBound);
         ```
       - Dodaj metodę `dispose()` PRZED ostatnim `}` klasy:
         ```javascript
         /**
          * Zwalnia zasoby SceneSetup (STATE-03). Plan 05 rozszerzy o WebGL context-loss listeners.
          */
         dispose() {
           window.removeEventListener('resize', this._onWindowResizeBound);
           this.renderer.dispose();
         }
         ```

    2. **Modyfikuj `src/main.js`** — przebudowa Application zgodnie z Pattern 3 (linie 397–451):
       Po Plan 01 modyfikacjach `src/main.js` ma już modulo 2π i komentarz deltaTime. Teraz kompletna refaktorka:
       ```javascript
       // Style ładowane przez index.html (link rel=stylesheet href="/style.css") — root style.css jest jedynym source of truth (Phase Z hygiene).
       import { gsap } from 'gsap';
       import { SceneSetup } from './SceneSetup';
       import { PressModel } from './PressModel';
       import { UI } from './UI';
       import { PhysicsEngine } from './PhysicsEngine';
       import { createTrainingStore } from './state/trainingStore.js';

       class Application {
         constructor() {
           this.sceneSetup = new SceneSetup('three-canvas');
           this.pressModel = new PressModel(this.sceneSetup.scene);
           this.ui = new UI();
           this.disclaimerBanner = null;  // Plan 05 wpina (DisclaimerBanner)
           this.store = createTrainingStore();
           this.currentAngle = 0;

           // Tickable list (RESEARCH §"Pattern 3" — Phase 3 dorzuca raycastHover bez merge-conflict)
           this.tickables = [(dt) => this.simulationTick(dt)];
           this._tickerCallback = (time, dt) => {
             for (const fn of this.tickables) fn(dt);
             this.sceneSetup.render();
           };
           gsap.ticker.add(this._tickerCallback);

           // STATE-03: capture każde unsubscribe handle
           this._unsubscribers = [];
         }

         simulationTick(deltaTime) {
           // GSAP 3.x ticker: deltaTime w milisekundach (kontrakt zablokowany ~3.15.0 pin w package.json — INFRA-03).
           const dtSeconds = deltaTime / 1000;
           const angularVelocity = this.ui.getAngularVelocity();
           if (angularVelocity > 0) {
             this.currentAngle = (this.currentAngle + angularVelocity * dtSeconds) % (Math.PI * 2);
           }
           this.pressModel.update(this.currentAngle);
           const displacement = PhysicsEngine.calculateSliderPosition(
             this.currentAngle, this.pressModel.r, this.pressModel.l
           );
           this.ui.updateTelemetry(this.currentAngle, displacement);
         }

         /**
          * Zwalnia wszystkie subskrypcje + GSAP ticker callback + komponenty (STATE-03).
          * Wywoływane przez Vite HMR `import.meta.hot.dispose()`.
          */
         dispose() {
           gsap.ticker.remove(this._tickerCallback);
           for (const unsub of this._unsubscribers) unsub();
           this._unsubscribers = [];
           if (this.disclaimerBanner) this.disclaimerBanner.dispose();
           this.sceneSetup.dispose();
         }
       }

       // Bootstrap
       let app;
       document.addEventListener('DOMContentLoaded', () => {
         app = new Application();
       });

       // Vite HMR — STATE-03: na hot reload zwalniamy stary Application zanim Vite załaduje nowy.
       if (import.meta.hot) {
         import.meta.hot.dispose(() => {
           if (app) app.dispose();
         });
       }
       ```

    3. **Utwórz `tests/application.test.js`** — STATE-03 dispose smoke (mock SceneSetup/PressModel/UI/gsap aby uniknąć WebGL/DOM fail w Node):
       ```javascript
       // tests/application.test.js
       // @vitest-environment jsdom
       // STATE-03 dispose smoke: weryfikuje że Application.dispose woła unsubscribe + ticker.remove.
       //
       // UWAGA: pełny import src/main.js wymaga Three.js + DOM (#three-canvas). Ten test mockuje
       // SceneSetup i PressModel aby uniknąć WebGLRenderer init w jsdom (MOD-6 prevention).

       import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
       import { readFileSync } from 'node:fs';

       describe('Application.dispose() (STATE-03 smoke)', () => {
         let removeSpy;
         let originalAdd;

         beforeEach(() => {
           // Setup minimal DOM
           document.body.innerHTML = '<div id="three-canvas"></div>' +
             '<span id="status-dot"></span><span id="status-text"></span>' +
             '<input id="speed-slider" value="30" />' +
             '<span id="speed-value"></span><button id="btn-toggle"></button>' +
             '<span id="val-angle"></span><span id="val-displacement"></span>';
         });

         afterEach(() => {
           document.body.innerHTML = '';
         });

         it('Application class structure: hooks expected lifecycle', async () => {
           // Symulujemy Application z manualnym dispose() — sprawdzamy kontrakt
           // bez ładowania pełnego src/main.js (uniknięcie WebGL).
           const _unsubscribers = [];
           const unsub1 = vi.fn();
           const unsub2 = vi.fn();
           _unsubscribers.push(unsub1);
           _unsubscribers.push(unsub2);

           // Symulacja dispose
           for (const u of _unsubscribers) u();
           _unsubscribers.length = 0;

           expect(unsub1).toHaveBeenCalledTimes(1);
           expect(unsub2).toHaveBeenCalledTimes(1);
           expect(_unsubscribers).toHaveLength(0);
         });

         it('src/main.js zawiera import.meta.hot dispose hook (STATE-03)', () => {
           // Static check — czy plik referuje import.meta.hot?.dispose
           const src = readFileSync('src/main.js', 'utf-8');
           expect(src).toMatch(/import\.meta\.hot/);
           expect(src).toMatch(/\.dispose\(/);
         });

         it('src/main.js zawiera tickables list pattern', () => {
           const src = readFileSync('src/main.js', 'utf-8');
           expect(src).toMatch(/tickables/);
           expect(src).toMatch(/_tickerCallback/);
           expect(src).toMatch(/_unsubscribers/);
         });

         it('src/main.js instantiuje TrainingStore', () => {
           const src = readFileSync('src/main.js', 'utf-8');
           expect(src).toMatch(/createTrainingStore/);
         });

         it('src/SceneSetup.js eksponuje dispose() i bound resize handler', () => {
           const src = readFileSync('src/SceneSetup.js', 'utf-8');
           expect(src).toMatch(/_onWindowResizeBound/);
           expect(src).toMatch(/dispose\s*\(/);
           expect(src).toMatch(/removeEventListener\(['"]resize['"]/);
         });
       });
       ```

  </action>
  <verify>
    <automated>
      node --check src/main.js
      &amp;&amp; node --check src/SceneSetup.js
      &amp;&amp; npx vitest run tests/application.test.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/main.js` parsuje się czysto.
    - `src/main.js` zawiera literały: `tickables`, `_tickerCallback`, `_unsubscribers`, `import.meta.hot`, `createTrainingStore`, `disclaimerBanner = null` (placeholder dla Plan 05).
    - `src/main.js` HMR hook: `if (import.meta.hot) { import.meta.hot.dispose(() => { if (app) app.dispose(); }); }`.
    - `src/SceneSetup.js` parsuje się czysto.
    - `src/SceneSetup.js` zawiera `_onWindowResizeBound` i metodę `dispose()` która woła `removeEventListener('resize', this._onWindowResizeBound)` + `this.renderer.dispose()`.
    - `tests/application.test.js` istnieje z 5 `it(...)`, wszystkie PASS.
  </acceptance_criteria>
  <manual_verification>
    Manualny smoke wykonywany przez executora RAZ na końcu Plan 04 i dokumentowany w SUMMARY (`01-VALIDATION.md` § "Manual-Only Verifications" — poza `<acceptance_criteria>` per Nyquist 8a):
    - `npm run dev` startuje aplikację bez błędów konsoli — animacja działa (modulo 2π z Plan 01), TrainingStore instantiated cicho, brak runtime crashów.
  </manual_verification>
  <decision_record>
    **D-08 RESOLVED:** store-side `scheduleTimer` (default `setTimeout`, injectable for tests via `createTrainingStore({scheduleTimer})`). ProcedureEngine pozostaje pure — emituje `{type:'startSpinUpTimer', ms}` effect, NIE odpala timera bezpośrednio. Phase 6 replay/retry MUSI inherit tego kontraktu (timer w storze, effect deklaratywny w engine).
  </decision_record>
  <done>STATE-03 spełniony: dispose pattern + HMR hook + tickables list + bound resize handler. Application gotowy aby Plan 05 dorzucił DisclaimerBanner i WebGL listeners. D-08 timer ownership zacementowane w decision_record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite HMR replace cycle | `import.meta.hot.dispose` musi zwolnić wszystkie subskrypcje przed nowym Application init |
| Test fake timers | `vi.useFakeTimers` przejmuje setTimeout — store musi używać setTimeout (nie gsap.delayedCall) per D-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS | Subscriber leak na HMR (MOD-1) | mitigate | `_unsubscribers` array + `Application.dispose()` + `import.meta.hot.dispose(() => app?.dispose())`; `tests/application.test.js` static-check pliku weryfikuje pattern obecny |
| T-04-02 | DoS | Resize listener leak (anonymous bind) | mitigate | `this._onWindowResizeBound = this.onWindowResize.bind(this)` w SceneSetup constructor; `dispose()` removeEventListener z dokładnie tym samym reference |
| T-04-03 | DoS | GSAP ticker callback leak | mitigate | `this._tickerCallback` przechowywane jako property; `dispose()` woła `gsap.ticker.remove(this._tickerCallback)` |
| T-04-04 | DoS | Spinning timer pozostaje aktywny po dispose | accept | Phase 1: `_spinUpTimerHandle` przechowywane; dispose nie clearTimeout (Phase 6 może dodać `clearTimeout(this._spinUpTimerHandle)`); ryzyko niewielkie bo HMR replace dispose'uje przed `_onSpinUpComplete` ma szansę zaszkodzić |
| T-04-05 | Tampering | malformed scenariusz crashuje store.startScenario | accept | Plan 02 `validateScenario` może być wezwany w bootstrappingu Application (Plan 05 może to dodać do Application.constructor jako defensywne); Phase 1 polega na fact że `uruchomienie.js` jest committed code |
| T-04-06 | Information Disclosure | event log w storze leakuje sensitive | accept | Eventy nie zawierają user-controlled content (errorCodes są stałe, timestamps z injectable now) |
</threat_model>

<verification>
- `npx vitest run tests/trainingStore.test.js` zielone (≥10 testów).
- `npx vitest run tests/uruchomienie.integration.test.js` zielone (4 describe blocks z 4 testami).
- `npx vitest run tests/application.test.js` zielone (5 testów w jsdom env).
- `npm test` całość: ≥4 plik testowy (Plan 01 physicsEngine + Plan 02 scenarioShape + Plan 03 procedureEngine/scoringService/faultRules + Plan 04 trainingStore/uruchomienie/application) = ≥7 plików testowych z sumą ≥80 testów PASS.
- `node --check src/main.js && node --check src/SceneSetup.js` exit 0.
- `npm run dev` smoke: scena 3D startuje bez konsola errors.
</verification>

<success_criteria>
- STATE-01: `createTrainingStore` jest jedynym mutowalnym shared state (zweryfikowane przez fact że żaden inny moduł Phase 1 nie eksportuje setState/subscribe).
- STATE-02: code review checklist — Phase 1 NIE tworzy nowych meshy, więc invariant `userData = identity-only` jest no-op (egzekwowany w Phase 2).
- STATE-03: `Application.dispose()` + HMR hook + `_unsubscribers` array + tickables ref + bound resize.
- SOP-09: Integration test pokazuje 8-step happy + 2 failure paths (out-of-order medium + forbidden-state critical) + double-click resilience.
- TEST-04 zalążek: 100-click stress test pokazuje 1 step.done event (engine pure determinism).
- Wszystkie acceptance criteria 3 tasków spełnione.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` zawierający:
- Lista 6 plików (3 nowe testy + 3 modyfikacje source).
- Decyzja: timer rozpędu odpalany przez store przez `scheduleTimer` (default `setTimeout`); Plan 4 polega na `vi.useFakeTimers()` zamiast `gsap.ticker.tick()`.
- Decyzja: Plan 04 NIE dotyka WebGL context-loss listeners — to robi Plan 05 (rozszerza `SceneSetup.dispose()`).
- Decyzja: PressModel.dispose stub NIE jest dodany w Phase 1 (Open Question #4 — Phase 2 to wprowadza wraz z cloned materials registry).
- Komendy weryfikacji + output (cały `npm test -- --coverage` jeśli dostępne).
- Plan 05 zależność: Plan 05 dziedziczy `src/SceneSetup.js` (dispose shell + bound resize) i `src/main.js` (`disclaimerBanner = null` placeholder + dispose włącza banner.dispose); Plan 05 wypełnia oba.
</output>
