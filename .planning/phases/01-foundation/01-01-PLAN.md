---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - src/style.css
  - src/counter.js
  - src/UI.js
  - src/main.js
  - src/PhysicsEngine.js
  - tests/physicsEngine.test.js
autonomous: true
requirements: [INFRA-01, INFRA-03, INFRA-04, TEST-01]
must_haves:
  truths:
    - "`npm test` exits 0 with at least one passing test"
    - "`vitest.config.js` declares coverage thresholds (lines/functions/statements ≥95%, branches ≥90%) on `src/training/**` + `src/state/**` and `environmentMatchGlobs` mapping `tests/disclaimerBanner.test.js` to jsdom"
    - "`src/style.css` and `src/counter.js` no longer exist on disk"
    - "`src/UI.js` parses cleanly (`node -c` exit 0); the stray closing brace on the old line 67 is gone"
    - "`src/main.js` wraps `currentAngle` with modulo 2π and includes the documentary comment about GSAP deltaTime in milliseconds"
    - "`package.json` shows `\"gsap\": \"~3.15.0\"`, `\"zustand\": \"^5.0.13\"`, devDeps `\"vitest\": \"~4.1.5\"`, `\"jsdom\": \"~29.1.1\"`, `\"@vitest/coverage-v8\": \"~4.1.5\"`, scripts `test`, `test:watch`, `test:coverage`"
    - "`PhysicsEngine.calculateSliderPosition` throws on non-finite `angle/r/l`, `r<=0`, `l<=0`, `r>=l` with Polish messages including the offending value"
    - "`tests/physicsEngine.test.js` covers each of the 4 throw conditions plus a happy path that returns a finite number"
  artifacts:
    - path: "vitest.config.js"
      provides: "Vitest 4 configuration with coverage thresholds + environmentMatchGlobs"
      contains: "environmentMatchGlobs"
    - path: "tests/physicsEngine.test.js"
      provides: "INFRA-04 unit tests"
      contains: "PhysicsEngine.calculateSliderPosition"
    - path: "package.json"
      provides: "pinned deps + test scripts"
      contains: "vitest"
    - path: "src/PhysicsEngine.js"
      provides: "input-validated physics math"
      contains: "Number.isFinite"
    - path: "src/main.js"
      provides: "modulo 2π angle wrap + deltaTime comment"
      contains: "Math.PI * 2"
    - path: "src/UI.js"
      provides: "syntactically clean DOM controller"
      min_lines: 60
  key_links:
    - from: "package.json"
      to: "vitest"
      via: "devDependencies + scripts.test"
      pattern: "\"vitest\""
    - from: "tests/physicsEngine.test.js"
      to: "src/PhysicsEngine.js"
      via: "import"
      pattern: "from '\\.\\./src/PhysicsEngine\\.js'"
---

<objective>
Wave 0 phase-foundation: zainstaluj test infrastructure (Vitest 4 + jsdom 29 + coverage-v8 + zustand), zpinuj GSAP na `~3.15.0`, utwórz `vitest.config.js`, spłać Phase Z hygiene (usuń `src/style.css`, `src/counter.js`, popraw `src/UI.js` line 67, dodaj modulo 2π i komentarz deltaTime w `src/main.js`), dodaj walidację wejść do `PhysicsEngine` z testem jednostkowym.

Po tym planie `npm test` przechodzi z testem `physicsEngine.test.js`, build jest czysty od debt-u, a kolejne plany mogą równolegle pisać moduły domenowe (Plan 02) i pure engine (Plan 03).

Purpose: bez działającego `npm test` żaden inny plan tej fazy nie może być automated-verified. Hygiene musi być zrobiona zanim INFRA-02 boundaries scanner zacznie patrzeć na `src/UI.js` i `package.json`.

Output: zielony `npm test`, czysty katalog `src/`, pinned deps, walidacja PhysicsEngine, repozytorium gotowe na Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
<!-- Aktualny stan plików, które plan modyfikuje. Executor NIE eksploruje codebase'u — używa tych snippetów. -->

From src/main.js (current full file — 51 lines, ASCII):
```javascript
import './style.css'; // Vite załaduje ten plik
import { gsap } from 'gsap';
import { SceneSetup } from './SceneSetup';
import { PressModel } from './PressModel';
import { UI } from './UI';
import { PhysicsEngine } from './PhysicsEngine';

class Application {
  constructor() {
    this.sceneSetup = new SceneSetup('three-canvas');
    this.pressModel = new PressModel(this.sceneSetup.scene);
    this.ui = new UI();

    this.currentAngle = 0; // Kąt w radianach

    gsap.ticker.add((time, deltaTime, frame) => this.tick(deltaTime));
  }

  tick(deltaTime) {
    const dtSeconds = deltaTime / 1000;
    const angularVelocity = this.ui.getAngularVelocity();

    if (angularVelocity > 0) {
      this.currentAngle += angularVelocity * dtSeconds;
    }

    this.pressModel.update(this.currentAngle);
    const displacement = PhysicsEngine.calculateSliderPosition(this.currentAngle, this.pressModel.r, this.pressModel.l);
    this.ui.updateTelemetry(this.currentAngle, displacement);
    this.sceneSetup.render();
  }
}

document.addEventListener('DOMContentLoaded', () => {
  new Application();
});
```

From src/UI.js (BUG na linii 67 — zamykający `}` jest podwójny):
```javascript
  updateTelemetry(angleRad, displacement) {
    let deg = (angleRad * 180 / Math.PI) % 360;
    if (deg < 0) deg += 360;

    this.elements.valAngle.innerText = `${deg.toFixed(1)}°`;
    this.elements.valDisplacement.innerText = `${displacement.toFixed(3)} m`;
  }

  }    // ← STRAY BRACE (line 67) — DO USUNIĘCIA
}
```

From src/PhysicsEngine.js (current full file):
```javascript
export class PhysicsEngine {
  /**
   * Oblicza pozycję suwaka na osi Y w mechanizmie korbowo-wodzikowym.
   * Wzór: y = r * cos(alpha) + sqrt(l^2 - (r * sin(alpha))^2)
   *
   * @param {number} angle - Kąt obrotu wału (w radianach, 0 to górne martwe położenie)
   * @param {number} r - Promień korby (skok prasy to 2 * r)
   * @param {number} l - Długość korbowodu
   * @returns {number} Aktualna pozycja Y suwaka
   */
  static calculateSliderPosition(angle, r, l) {
    const term1 = r * Math.cos(angle);
    const term2 = Math.sqrt(Math.pow(l, 2) - Math.pow(r * Math.sin(angle), 2));
    return term1 + term2;
  }
}
```

From package.json (current full file):
```json
{
  "name": "hydraulicpress",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  }
}
```

From src/style.css i src/counter.js: DO USUNIĘCIA. `index.html` ładuje root `/style.css` (NIE `/src/style.css`); `src/main.js` line 1 ma `import './style.css'` ale Vite resolve'uje to wg modułu — po usunięciu `src/style.css` import musi zostać przepisany na ROOT `style.css` lub usunięty (Vite załaduje `style.css` przez `<link>` w `index.html`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zainstaluj devDeps + zustand, zpinuj GSAP, dodaj test scripts, utwórz vitest.config.js</name>
  <files>package.json, vitest.config.js</files>
  <read_first>
    - package.json (cały — przed edycją; widoczny w &lt;interfaces&gt; powyżej)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Standard Stack" (linie 138–183 — dokładne wersje + komenda npm install)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Code Examples > A. vitest.config.js" (linie 608–656)
    - .planning/phases/01-foundation/01-VALIDATION.md § "Test Infrastructure" (linie 17–27) i § "Wave 0 Requirements" (linie 67–84)
  </read_first>
  <action>
    1. Edytuj `package.json` (NIE używaj `npm install gsap@~...` aby nie ruszać innych deps; ręczne edytowanie pól JSON):
       - W `dependencies`: zmień `"gsap": "^3.15.0"` na `"gsap": "~3.15.0"` (D-Phase-Z: tilde blokuje minor bumpy zmieniające deltaTime contract).
       - W `dependencies`: dodaj `"zustand": "^5.0.13"`.
       - W `devDependencies`: dodaj `"vitest": "~4.1.5"`, `"jsdom": "~29.1.1"`, `"@vitest/coverage-v8": "~4.1.5"`. Zachowaj istniejący `"vite": "^8.0.10"`.
       - W `scripts`: dodaj `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:coverage": "vitest run --coverage"`. Zachowaj istniejące `dev`, `build`, `preview`.
       - Zachowaj final newline + 2-space indent (matching obecnego stylu).
    2. Uruchom `npm install` aby pobrać nowe zależności (zaktualizuje `package-lock.json`).
    3. Utwórz `vitest.config.js` w ROOT projektu (NIE w `src/`) o treści verbatim z RESEARCH.md §A linie 612–639:
       ```javascript
       // vitest.config.js
       import { defineConfig } from 'vitest/config';

       export default defineConfig({
         test: {
           environment: 'node',
           environmentMatchGlobs: [
             ['tests/disclaimerBanner.test.js', 'jsdom'],
           ],
           globals: false,
           include: ['tests/**/*.test.js'],
           exclude: ['node_modules', 'dist'],
           coverage: {
             provider: 'v8',
             reporter: ['text', 'html'],
             include: ['src/training/**', 'src/state/**'],
             exclude: ['src/training/scenarios/**'],
             thresholds: {
               lines: 95,
               functions: 95,
               branches: 90,
               statements: 95,
             },
           },
         },
       });
       ```
       Komentarz w pliku po polsku (CONVENTIONS.md): tuż pod `// vitest.config.js` dopisz `// Konfiguracja Vitest 4 — coverage thresholds dla src/training i src/state (ROADMAP Phase 1 SC1).`
  </action>
  <verify>
    <automated>
      node -e "const p=require('./package.json'); if(p.dependencies.gsap!=='~3.15.0') process.exit(1); if(!p.dependencies.zustand) process.exit(2); if(!p.devDependencies.vitest) process.exit(3); if(!p.devDependencies.jsdom) process.exit(4); if(!p.devDependencies['@vitest/coverage-v8']) process.exit(5); if(p.scripts.test!=='vitest run') process.exit(6); console.log('OK');"
      &amp;&amp; node -e "const fs=require('fs'); if(!fs.existsSync('vitest.config.js')) process.exit(1); const s=fs.readFileSync('vitest.config.js','utf-8'); if(!s.includes('environmentMatchGlobs')) process.exit(2); if(!s.includes(\"src/training/**\")) process.exit(3); if(!s.includes('lines: 95')) process.exit(4); console.log('OK');"
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` zawiera dokładnie `"gsap": "~3.15.0"` (tylda, NIE caret).
    - `package.json` zawiera `"zustand": "^5.0.13"` w `dependencies`.
    - `package.json` zawiera w `devDependencies`: `"vitest": "~4.1.5"`, `"jsdom": "~29.1.1"`, `"@vitest/coverage-v8": "~4.1.5"`.
    - `package.json` `scripts` ma klucze: `dev`, `build`, `preview`, `test`, `test:watch`, `test:coverage`. `test` === `"vitest run"`.
    - `vitest.config.js` istnieje w root, eksportuje `defineConfig`, zawiera literały: `environmentMatchGlobs`, `'tests/disclaimerBanner.test.js'`, `'jsdom'`, `'src/training/**'`, `'src/state/**'`, `'src/training/scenarios/**'` (exclude), `lines: 95`, `branches: 90`.
    - `node_modules/vitest/`, `node_modules/jsdom/`, `node_modules/zustand/` istnieją po `npm install`.
    - `package-lock.json` zaktualizowany.
  </acceptance_criteria>
  <done>Wszystkie acceptance criteria spełnione; `npm test` może być uruchomione (nawet jeśli nie ma jeszcze testów — exit 0 lub "no tests found", to OK na tym etapie).</done>
</task>

<task type="auto">
  <name>Task 2: Phase Z hygiene — usuń src/style.css, src/counter.js, popraw src/UI.js:67, dodaj modulo 2π + komentarz deltaTime w src/main.js</name>
  <files>src/style.css, src/counter.js, src/UI.js, src/main.js</files>
  <read_first>
    - src/main.js (cały — widoczny w &lt;interfaces&gt;)
    - src/UI.js (linie 60–68 — bug visible w &lt;interfaces&gt;)
    - .planning/codebase/CONCERNS.md (cały — zwłaszcza §1 stray brace, §2 dwa style.css, §6 deltaTime unit)
    - .planning/phases/01-foundation/01-PATTERNS.md § "src/main.js MODIFIED" (linie 352–380) i § "src/UI.js MODIFIED" (linie 422–438)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Pattern 3: Tickable list" (linie 388–451) — KEEP `simulationTick` extraction dopiero w Plan 04; w tym tasku TYLKO modulo 2π i komentarz
    - index.html linie 9 (`<link rel="stylesheet" href="/style.css">` ← root style.css ładowany przez HTML; src/style.css to dead code)
  </read_first>
  <action>
    1. **Usuń `src/style.css`** (CONCERNS §2 — dead Vite scaffold; `index.html` ładuje root `/style.css`). Użyj polecenia `del` (PowerShell) lub `rm` (bash). Po usunięciu **edytuj `src/main.js` linia 1**: zmień `import './style.css'; // Vite załaduje ten plik` na `// Style ładowane przez index.html (link rel=stylesheet href="/style.css") — root style.css jest jedynym source of truth (Phase Z hygiene).` (linia komentarza, BEZ importu — Vite NIE potrzebuje JS-side importu, bo HTML loaduje style przez `<link>`).
    2. **Usuń `src/counter.js`** (CONCERNS §3 — orphaned scaffold, nigdzie nie importowane).
    3. **Edytuj `src/UI.js`** — usuń linię 67 z osierocony `}`. Po edycji ostatnie linie pliku muszą wyglądać:
       ```javascript
           this.elements.valDisplacement.innerText = `${displacement.toFixed(3)} m`;
         }
       }
       ```
       (jeden `}` zamykający metodę, jeden `}` zamykający klasę). Nie zmieniaj reszty pliku.
    4. **Edytuj `src/main.js`** — w `tick(deltaTime)`:
       - Tuż NAD `const dtSeconds = deltaTime / 1000;` dodaj komentarz: `// GSAP 3.x ticker: deltaTime w milisekundach (kontrakt zablokowany ~3.15.0 pin w package.json — INFRA-03).`
       - Zmień `this.currentAngle += angularVelocity * dtSeconds;` na `this.currentAngle = (this.currentAngle + angularVelocity * dtSeconds) % (Math.PI * 2);` (CONCERNS §"Missing currentAngle Boundary Wrapping" / INFRA-03).
       - Usuń stary komentarz w liniach 22–24 (`Czasem GSAP podaje to w sekundach...`) — niepotrzebny po pinowaniu wersji.
       - **NIE rusz** struktury `Application`, gsap.ticker.add, ani DOMContentLoaded handlera — to robi Plan 04.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); if(fs.existsSync('src/style.css')) process.exit(1); if(fs.existsSync('src/counter.js')) process.exit(2); console.log('deletions OK');"
      &amp;&amp; node --check src/UI.js
      &amp;&amp; node --check src/main.js
      &amp;&amp; node -e "const fs=require('fs'); const m=fs.readFileSync('src/main.js','utf-8'); if(!/%[\s]*\(Math\.PI[\s]*\*[\s]*2\)/.test(m)) process.exit(10); if(!/deltaTime w milisekundach/.test(m)) process.exit(11); if(/import\s+'\.\/style\.css'/.test(m)) process.exit(12); console.log('main.js OK');"
      &amp;&amp; node -e "const fs=require('fs'); const u=fs.readFileSync('src/UI.js','utf-8'); const open=(u.match(/{/g)||[]).length; const close=(u.match(/}/g)||[]).length; if(open!==close) {console.error('brace mismatch open='+open+' close='+close); process.exit(20);} console.log('UI.js braces balanced');"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/style.css` NIE istnieje na dysku.
    - `src/counter.js` NIE istnieje na dysku.
    - `node --check src/UI.js` exit 0 (parsuje się czysto).
    - `node --check src/main.js` exit 0.
    - `src/UI.js` ma równe ilości `{` i `}` (sprawdzane regex'em w verify).
    - `src/main.js` zawiera regex `% (Math.PI * 2)` (lub równoważne formatowanie ze spacjami).
    - `src/main.js` zawiera komentarz `deltaTime w milisekundach` (zlokalizowany przy konwersji `/ 1000`).
    - `src/main.js` NIE zawiera już `import './style.css'` (linia zastąpiona komentarzem).
    - Aplikacja nadal startuje wizualnie — `npm run dev` w smoke teście otwiera scenę 3D bez błędu (executor wykonuje ten ręczny smoke RAZ na końcu i dokumentuje w SUMMARY).
  </acceptance_criteria>
  <done>Hygiene paid: dwa pliki usunięte, brace fixed, modulo wrap działa, komentarz deltaTime obecny, app nadal startuje.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Walidacja wejść PhysicsEngine (INFRA-04) + tests/physicsEngine.test.js</name>
  <files>src/PhysicsEngine.js, tests/physicsEngine.test.js</files>
  <read_first>
    - src/PhysicsEngine.js (cały — widoczny w &lt;interfaces&gt;)
    - .planning/phases/01-foundation/01-RESEARCH.md § "H. PhysicsEngine updated (INFRA-04)" (linie 1267–1309) — verbatim 4 guards + Polish messages
    - .planning/phases/01-foundation/01-PATTERNS.md § "src/PhysicsEngine.js MODIFIED" (linie 441–457)
    - .planning/codebase/CONCERNS.md § dotyczący missing PhysicsEngine input validation
    - .planning/codebase/CONVENTIONS.md (cały — zwłaszcza Polish JSDoc + English identifiers + throw idiom)
  </read_first>
  <behavior>
    - `calculateSliderPosition(NaN, 0.8, 4)` rzuca Error z komunikatem zawierającym "skończonymi liczbami" i wartość NaN.
    - `calculateSliderPosition(0, Infinity, 4)` rzuca Error z "skończonymi liczbami".
    - `calculateSliderPosition(0, 0, 4)` rzuca Error z "r musi być dodatnie" i `r=0`.
    - `calculateSliderPosition(0, -0.5, 4)` rzuca Error z "r musi być dodatnie" i `r=-0.5`.
    - `calculateSliderPosition(0, 0.8, 0)` rzuca Error z "l musi być dodatnie" i `l=0`.
    - `calculateSliderPosition(0, 0.8, -1)` rzuca Error z "l musi być dodatnie".
    - `calculateSliderPosition(0, 4, 4)` rzuca Error z "r musi być mniejsze niż l" i `r=4, l=4`.
    - `calculateSliderPosition(0, 5, 4)` rzuca Error z "r musi być mniejsze niż l" i `r=5, l=4`.
    - Happy path: `calculateSliderPosition(0, 0.8, 4)` zwraca finite number `≈ 4.8` (`r·cos(0) + √(l²-0) = 0.8 + 4 = 4.8`).
    - Happy path: `calculateSliderPosition(Math.PI/2, 0.8, 4)` zwraca finite number `≈ √(16-0.64) ≈ 3.9192`.
    - Wszystkie throw messages ZACZYNAJĄ się prefiksem `"PhysicsEngine: "` (PATTERNS § "Throw-with-Polish-message idiom").
  </behavior>
  <action>
    1. Najpierw napisz `tests/physicsEngine.test.js` (RED). Plik:
       ```javascript
       // tests/physicsEngine.test.js
       // @vitest-environment node
       // INFRA-04: walidacja wejść PhysicsEngine.
       import { describe, it, expect } from 'vitest';
       import { PhysicsEngine } from '../src/PhysicsEngine.js';

       describe('PhysicsEngine.calculateSliderPosition — input validation (INFRA-04)', () => {
         it('rzuca przy angle = NaN', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(NaN, 0.8, 4))
             .toThrow(/PhysicsEngine: parametry muszą być skończonymi liczbami/);
         });
         it('rzuca przy r = Infinity', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, Infinity, 4))
             .toThrow(/skończonymi liczbami/);
         });
         it('rzuca przy l = -NaN-like (NaN)', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, 0.8, NaN))
             .toThrow(/skończonymi liczbami/);
         });
         it('rzuca przy r = 0 z komunikatem "r musi być dodatnie" i wartością r', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, 0, 4))
             .toThrow(/PhysicsEngine: r musi być dodatnie.*r=0/);
         });
         it('rzuca przy r < 0', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, -0.5, 4))
             .toThrow(/r musi być dodatnie/);
         });
         it('rzuca przy l = 0 z komunikatem "l musi być dodatnie"', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, 0.8, 0))
             .toThrow(/PhysicsEngine: l musi być dodatnie.*l=0/);
         });
         it('rzuca przy l < 0', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, 0.8, -1))
             .toThrow(/l musi być dodatnie/);
         });
         it('rzuca przy r === l (geometria zwyrodniała)', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, 4, 4))
             .toThrow(/r musi być mniejsze niż l.*r=4.*l=4/);
         });
         it('rzuca przy r > l', () => {
           expect(() => PhysicsEngine.calculateSliderPosition(0, 5, 4))
             .toThrow(/r musi być mniejsze niż l/);
         });
       });

       describe('PhysicsEngine.calculateSliderPosition — happy path', () => {
         it('zwraca r + l przy angle = 0 (górne martwe położenie)', () => {
           const y = PhysicsEngine.calculateSliderPosition(0, 0.8, 4);
           expect(y).toBeCloseTo(4.8, 6);
           expect(Number.isFinite(y)).toBe(true);
         });
         it('zwraca √(l² - r²) przy angle = π/2', () => {
           const y = PhysicsEngine.calculateSliderPosition(Math.PI / 2, 0.8, 4);
           expect(y).toBeCloseTo(Math.sqrt(16 - 0.64), 6);
         });
       });
       ```
    2. Uruchom `npm test` — RED (testy failują bo brak walidacji w `PhysicsEngine`).
    3. Edytuj `src/PhysicsEngine.js` aby przejść do GREEN. Wstaw 4 guards na początku metody, ZANIM `term1` calculation, używając VERBATIM Polish messages z RESEARCH §H linie 1282–1304:
       ```javascript
       static calculateSliderPosition(angle, r, l) {
         // INFRA-04: walidacja wejść. Walidacja przy każdym wywołaniu — koszt znikomy
         // (3 porównania), pewność że tick loop nie przemyca NaN-ów.
         if (!Number.isFinite(r) || !Number.isFinite(l) || !Number.isFinite(angle)) {
           throw new Error(`PhysicsEngine: parametry muszą być skończonymi liczbami (angle=${angle}, r=${r}, l=${l})`);
         }
         if (r <= 0) {
           throw new Error(`PhysicsEngine: r musi być dodatnie (otrzymano r=${r})`);
         }
         if (l <= 0) {
           throw new Error(`PhysicsEngine: l musi być dodatnie (otrzymano l=${l})`);
         }
         if (r >= l) {
           throw new Error(`PhysicsEngine: r musi być mniejsze niż l (otrzymano r=${r}, l=${l}; geometria zwyrodniała)`);
         }
         const term1 = r * Math.cos(angle);
         const term2 = Math.sqrt(l * l - (r * Math.sin(angle)) ** 2);
         return term1 + term2;
       }
       ```
       Zachowaj istniejący JSDoc nad metodą; rozszerz `@throws` jeśli go nie ma:
       ```
       @throws {Error} jeśli r/l są niedodatnie, nieskończone, lub r >= l (geometrycznie zwyrodniałe)
       ```
    4. Uruchom `npm test -- tests/physicsEngine.test.js` — GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/physicsEngine.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/physicsEngine.test.js` istnieje, zawiera dokładnie 9 `it(...)` w bloku "input validation" + 2 `it(...)` w bloku "happy path" = 11 testów.
    - `npx vitest run tests/physicsEngine.test.js` exit 0, wszystkie 11 testów PASS.
    - `src/PhysicsEngine.js` zawiera literał `Number.isFinite` co najmniej 3 razy (lub równoważne wyrażenie używające `||`).
    - `src/PhysicsEngine.js` zawiera literał `'PhysicsEngine: '` (prefix komunikatów) co najmniej 4 razy.
    - `src/PhysicsEngine.js` zawiera literał `geometria zwyrodniała` (ostatni guard).
    - `src/PhysicsEngine.js` nadal działa dla legalnego wejścia (smoke: app w `npm run dev` nie crashuje — istniejące parametry r=0.8, l=4 są legalne).
    - Plik testowy zawiera pragma `// @vitest-environment node` na górze (PATTERNS § "Test file env declaration").
  </acceptance_criteria>
  <done>RED→GREEN cycle complete; PhysicsEngine ma 4 input guards z Polish messages; 11 testów zielone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → npm registry | npm install pobiera dependencies; pin via tilde/caret limituje surface |
| user inputs → PhysicsEngine | brak user inputu w Phase 1 (tick loop podaje state-controlled args), ale defensive coding na przyszłe Phase 3+ raycaster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json supply chain | accept | Pinning via `~` (patch only) dla GSAP/Vitest/jsdom; `npm audit` raz przy commit; brak high-severity findings w dependency tree per RESEARCH § Security Domain V14 |
| T-01-02 | DoS / Information Disclosure | PhysicsEngine NaN poison | mitigate | INFRA-04 guards rzucają explicit Error z Polish message zamiast cicho zwracać NaN który propagowałby do telemetry display i animacji |
| T-01-03 | DoS | dead `src/style.css` + `src/counter.js` | mitigate | Phase Z hygiene usuwa dead files — eliminuje ryzyko że Vite je przypadkowo zbundluje (low severity) |
| T-01-04 | Tampering | `src/UI.js` syntax error | mitigate | Phase Z fix line 67; `node --check` w verify |
| T-01-05 | DoS | unbounded currentAngle akumulacja (float drift po godzinach) | mitigate | modulo 2π w `src/main.js` — wartości pozostają w bounded range, brak gradual NaN po ekstremalnym uptime |
</threat_model>

<verification>
Po wszystkich taskach:
- `npm test` exit 0 (przynajmniej 11 testów `physicsEngine.test.js` PASS).
- `node --check src/UI.js && node --check src/main.js` exit 0.
- `git status` pokazuje: deleted `src/style.css`, deleted `src/counter.js`, modified `package.json`, `package-lock.json`, `src/UI.js`, `src/main.js`, `src/PhysicsEngine.js`, untracked `vitest.config.js`, `tests/physicsEngine.test.js`.
- `npm run dev` startuje aplikację bez błędów konsoli (smoke; obecny scenariusz prasy ma r=0.8, l=4 — legalny dla nowych guards).
</verification>

<success_criteria>
- INFRA-01: `npm test` przechodzi z >=1 testem.
- INFRA-03: hygiene paid (4 fixes: dwa deletes, brace fix, modulo wrap + komentarz deltaTime, GSAP `~3.15.0` pin).
- INFRA-04: PhysicsEngine rzuca na 4 invalid input shape'ach z Polish messages.
- TEST-01 (config part): `vitest.config.js` declares thresholds — pełne pokrycie ≥95% sprawdzane będzie po Wave 1+2 (gdy `src/training/**` istnieje).
- Wszystkie acceptance criteria każdego z 3 tasków spełnione.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` zawierający:
- Co zostało zrobione (lista 3 tasks z plikami zmienionymi).
- Komendy weryfikacji + ich output (npm test, node --check).
- Link do następnych planów (Wave 1: Plan 02 i Plan 03 mogą równolegle).
- Decyzje implementacyjne (np. czy `npm install` zaktualizował inne deps).
</output>
