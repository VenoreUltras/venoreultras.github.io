---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/i18n/pl.js
  - src/training/scenarios/uruchomienie.js
  - src/training/scenarios/index.js
  - src/training/scenarios/validateScenario.js
  - src/training/faultRules.js
  - src/training/scoringWeights.js
  - tests/scenarioShape.test.js
  - tests/fixtures/scenario.fixture.js
autonomous: true
requirements: [UI-06, SOP-02, SOP-03, SOP-07]
must_haves:
  truths:
    - "`src/i18n/pl.js` jest jedynym miejscem polskich UI strings; eksportuje `pl.disclaimer.full` z dokładnym tekstem D-10"
    - "`src/training/scenarios/uruchomienie.js` ma dokładnie 8 kroków w kolejności D-06"
    - "Step `sprzegnij-po-rozpedzie` ma `validateBefore` zwracający true tylko gdy `state.machineState === 'gotowa-do-pracy'`"
    - "Step `wlacz-zasilanie` ma effects `setMachineState 'rozpedzanie'` + `startSpinUpTimer 3000`"
    - "`scoringWeights.js` eksportuje frozen `DEFAULT_WEIGHTS` z `critical:-25, medium:-10, minor:-2` + `SCORE_BASELINE=100` + `SCORE_FLOOR=0`"
    - "`validateScenario(scenario)` rzuca z polskim komunikatem na 5 typach malformacji"
    - "`faultRules.js` eksportuje array `faultRules` z guard-open-during-cycle invariant + funkcją `evaluateFaultRulesData(state, rules)` zwracającą effects (NIE same effects jako data — funkcja-helper jest pure top-level)"
    - "`tests/scenarioShape.test.js` waliduje wszystkie 8 kroków scenariusza `uruchomienie` przeciw schematowi"
    - "Cały `pl.js` używa wyłącznie ASCII identyfikatorów + Polish string values (D-04)"
  artifacts:
    - path: "src/i18n/pl.js"
      provides: "i18n table — disclaimer + webgl + machineState + errors"
      contains: "Symulator szkoleniowy"
      exports: ["pl"]
    - path: "src/training/scenarios/uruchomienie.js"
      provides: "8-step start-up scenario"
      contains: "sprzegnij-po-rozpedzie"
    - path: "src/training/scenarios/index.js"
      provides: "loadScenario(id) registry"
      exports: ["loadScenario"]
    - path: "src/training/scenarios/validateScenario.js"
      provides: "ad-hoc shape validator"
      exports: ["validateScenario"]
    - path: "src/training/faultRules.js"
      provides: "cross-cutting safety invariants list + evaluator"
      exports: ["faultRules", "evaluateFaultRulesData"]
    - path: "src/training/scoringWeights.js"
      provides: "frozen DEFAULT_WEIGHTS + SCORE_BASELINE + SCORE_FLOOR"
      exports: ["DEFAULT_WEIGHTS", "SCORE_BASELINE", "SCORE_FLOOR"]
    - path: "tests/scenarioShape.test.js"
      provides: "SOP-02 stable-id + kind enforcement test"
    - path: "tests/fixtures/scenario.fixture.js"
      provides: "minimal scenario stub for engine tests"
      exports: ["minimalScenario"]
  key_links:
    - from: "src/training/scenarios/uruchomienie.js"
      to: "src/i18n/pl.js"
      via: "no import (D-04: scenariusz niesie inline polskie teksty; pl.js mapuje errorCode tylko)"
      pattern: "errorCode:.*'E-"
    - from: "src/training/scenarios/index.js"
      to: "src/training/scenarios/uruchomienie.js"
      via: "import default"
      pattern: "from '\\./uruchomienie\\.js'"
    - from: "tests/scenarioShape.test.js"
      to: "src/training/scenarios/validateScenario.js"
      via: "import"
      pattern: "validateScenario"
---

<objective>
Wave 1 część 1: utwórz warstwę DANYCH dla Phase 1 — i18n table (`pl.js`), scenariusz `uruchomienie` (8 kroków per D-06), registry scenariuszy (`scenarios/index.js`), ad-hoc validator (`validateScenario.js`), fault rules (`faultRules.js`), wagi scoringu (`scoringWeights.js`), test fixture (`tests/fixtures/scenario.fixture.js`), i shape test (`tests/scenarioShape.test.js`).

Plan 03 (parallel — Wave 1) tworzy **logikę** (ProcedureEngine + ScoringService) i jego testy używają wyłącznie inline mini-stuby — NIE importuje plików tworzonych tutaj. Plan 04 (Wave 2) skleja wszystko poprzez TrainingStore i pisze integration test, który importuje `uruchomienie` + `faultRules`.

Purpose: separacja danych od logiki. Te pliki to source of truth dla zawartości szkolenia (8 kroków BHP) + wag scoringu (placeholder do review eksperta) + Polish strings. Po edycji jednego pola w `pl.js`/`scoringWeights.js`/`uruchomienie.js` zachowanie zmienia się BEZ modyfikacji silnika.

Output: 8 nowych plików (6 src + 2 tests). `tests/scenarioShape.test.js` przechodzi, używając `validateScenario` na scenariuszu `uruchomienie`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Te pliki nie istnieją yet. Plan tworzy je verbatim z RESEARCH.md sekcji D, E, F + UI-SPEC table. -->

Pl.js shape (UI-SPEC § Copywriting Contract — exact 14 keys required):
```
pl.disclaimer.full         = 'Symulator szkoleniowy — NIE zastępuje obowiązkowego szkolenia BHP ani instruktażu stanowiskowego.'
pl.disclaimer.short        = 'Symulator szkoleniowy — NIE zastępuje szkolenia BHP.'
pl.disclaimer.ariaLabel    = 'Zastrzeżenie symulatora'
pl.disclaimer.toggleExpand = 'Pokaż disclaimer'
pl.disclaimer.toggleCollapse = 'Zwiń disclaimer'
pl.webgl.contextLost       = 'Utracono kontekst grafiki. Próba odzyskania...'
pl.machineState['oczekiwanie-na-inspekcje'] = 'Oczekiwanie na inspekcję'
pl.machineState['gotowa-do-pracy']          = 'Gotowa do pracy'
pl.machineState['rozpedzanie']              = 'Rozpędzanie...'
pl.machineState['w-cyklu']                  = 'W cyklu'
pl.machineState['zatrzymana']               = 'Zatrzymana'
pl.machineState['awaria']                   = 'Awaria — błąd procedury'
pl.machineState['tryb-wolny']               = 'Tryb wolny'
pl.errors[<8 codes>]       = (per RESEARCH §F linie 1148–1156)
```

Scenariusz `uruchomienie` shape (D-06 + RESEARCH §D linie 928–1050) — 8 kroków per kolejność:
1. `sprawdz-tabliczke` — `kind: 'visual-target'`, `targetMeshId: 'tabliczka-znamionowa'`
2. `kontrola-narzedzia` — `kind: 'visual-attest'` (BEZ targetMeshId)
3. `kontrola-wzrokowa` — `kind: 'visual-attest'`
4. `sprawdz-olej` — `kind: 'visual-target'`, `targetMeshId: 'wziernik-smarowania'`
5. `zamknij-oslone` — `kind: 'manipulation'`, `targetMeshId: 'oslona-przednia'`, `effectsOnSuccess: [setMeshState closed]`
6. `odblokuj-estop` — `kind: 'manipulation'`, `targetMeshId: 'estop'`, `effectsOnSuccess: [setMeshState released]`
7. `wlacz-zasilanie` — `kind: 'manipulation'`, `targetMeshId: 'wylacznik-glowny'`, `effectsOnSuccess: [setMachineState 'rozpedzanie', startSpinUpTimer 3000]`
8. `sprzegnij-po-rozpedzie` — `kind: 'manipulation'`, `targetMeshId: 'dzwignia-sprzegla'`, `validateBefore: state =&gt; state.machineState === 'gotowa-do-pracy'`, `effectsOnSuccess: [setMeshState engaged, setMachineState 'w-cyklu']`, `effectsOnError: [step.violation E-SPRZEGNIETO-PRZED-ROZPEDEM severity:critical]`

Effect type closed set (D-02 — używane przez Plan 04 store reducer):
- `setMachineState { type, value }`
- `setMeshState { type, meshId, value }`
- `appendEvent { type, event }`
- `advanceStep { type }` (tylko ProcedureEngine generuje — nie żyje w scenariuszu)
- `startSpinUpTimer { type, ms }`
- `playAudio { type, soundId }` (Phase 5 — w Phase 1 NO-OP w storze)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utwórz src/i18n/pl.js + src/training/scoringWeights.js</name>
  <files>src/i18n/pl.js, src/training/scoringWeights.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "F. src/i18n/pl.js" (linie 1113–1158) — verbatim full file
    - .planning/phases/01-foundation/01-RESEARCH.md § "E. scoringWeights" (linie 1054–1070) — verbatim
    - .planning/phases/01-foundation/01-UI-SPEC.md § "Copywriting Contract" (linie 161–183) — exact strings table z 14+ kluczami
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-04, D-09, D-10, D-11, D-15, D-16, D-18
    - .planning/phases/01-foundation/01-PATTERNS.md § "src/i18n/pl.js" (linie 282–300) i § "scoringWeights.js" (linie 138–154)
    - .planning/codebase/CONVENTIONS.md (Polish JSDoc + English identifiers + ES module exports)
  </read_first>
  <action>
    1. Utwórz katalog `src/i18n/` i plik `src/i18n/pl.js`. Treść VERBATIM z RESEARCH §F linie 1115–1158 (skopiuj cały plik).
       - Uważnie skopiuj polskie diakrytyki: ą, ć, ę, ł, ń, ó, ś, ź, ż.
       - Klucz `pl.disclaimer.full` MUSI dokładnie === `'Symulator szkoleniowy — NIE zastępuje obowiązkowego szkolenia BHP ani instruktażu stanowiskowego.'` (D-10 lock — to jest zacementowane).
       - Klucz `pl.machineState.rozpedzanie` MUSI === `'Rozpędzanie...'` (D-09 — trzy kropki sygnalizują przejściowość).
       - Klucz `pl.machineState.awaria` MUSI === `'Awaria — błąd procedury'` (z em-dash `—`, NIE hyphen `-`).
       - Komentarz nagłówkowy:
         ```
         // src/i18n/pl.js
         // JEDYNY plik z polskimi UI strings (UI-06).
         // Scenariusze JSON niosą własne polskie teksty inline (D-04) — to są dwa różne kontrakty.
         ```
    2. Utwórz katalog `src/training/` i plik `src/training/scoringWeights.js`. Treść VERBATIM z RESEARCH §E linie 1054–1070:
       ```javascript
       // src/training/scoringWeights.js
       // Source of truth dla wag scoringu. Po review eksperta BHP (STATE.md Q6)
       // edytuj TUTAJ — nie w ScoringService.

       /** Domyślne wagi severity. Subtractive od 100, floor 0 (D-15). */
       export const DEFAULT_WEIGHTS = Object.freeze({
         critical: -25,   // life-and-limb violation
         medium:   -10,   // out-of-order action
         minor:     -2,   // skipped visual check on retry
       });

       export const SCORE_BASELINE = 100;
       export const SCORE_FLOOR = 0;
       ```
       `Object.freeze` jest wymagane (boundaries / immutability — Plan 03 ScoringService nie może mutować defaults).
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); if(!fs.existsSync('src/i18n/pl.js')) process.exit(1); if(!fs.existsSync('src/training/scoringWeights.js')) process.exit(2); console.log('exists OK');"
      &amp;&amp; node --input-type=module -e "import('./src/i18n/pl.js').then(m =&gt; { const p=m.pl; const full='Symulator szkoleniowy — NIE zastępuje obowiązkowego szkolenia BHP ani instruktażu stanowiskowego.'; if(p.disclaimer.full!==full){console.error('disclaimer.full mismatch:'+p.disclaimer.full);process.exit(10);} if(p.machineState['rozpedzanie']!=='Rozpędzanie...'){console.error('rozpedzanie label mismatch');process.exit(11);} if(p.machineState['awaria']!=='Awaria — błąd procedury'){console.error('awaria label mismatch');process.exit(12);} if(!p.errors['E-SPRZEGNIETO-PRZED-ROZPEDEM']){process.exit(13);} if(p.disclaimer.ariaLabel!=='Zastrzeżenie symulatora'){process.exit(14);} if(p.webgl.contextLost!=='Utracono kontekst grafiki. Próba odzyskania...'){process.exit(15);} console.log('pl.js OK'); }).catch(e=&gt;{console.error(e);process.exit(99);});"
      &amp;&amp; node --input-type=module -e "import('./src/training/scoringWeights.js').then(m =&gt; { if(m.DEFAULT_WEIGHTS.critical!==-25)process.exit(20); if(m.DEFAULT_WEIGHTS.medium!==-10)process.exit(21); if(m.DEFAULT_WEIGHTS.minor!==-2)process.exit(22); if(m.SCORE_BASELINE!==100)process.exit(23); if(m.SCORE_FLOOR!==0)process.exit(24); if(!Object.isFrozen(m.DEFAULT_WEIGHTS))process.exit(25); console.log('scoringWeights OK'); }).catch(e=&gt;{console.error(e);process.exit(99);});"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/i18n/pl.js` istnieje, eksportuje named `pl` (object).
    - `pl.disclaimer.full` === literał D-10 verbatim (zweryfikowane test'em).
    - `pl.disclaimer.ariaLabel` === `'Zastrzeżenie symulatora'`.
    - `pl.disclaimer.toggleExpand` === `'Pokaż disclaimer'`.
    - `pl.disclaimer.toggleCollapse` === `'Zwiń disclaimer'`.
    - `pl.webgl.contextLost` === `'Utracono kontekst grafiki. Próba odzyskania...'`.
    - `pl.machineState` ma 7 kluczy (kebab-case scenariusza state ids → display labels), każdy ze znanym D-09 stringiem.
    - `pl.errors` zawiera co najmniej te kody: `E-OSLONA-NIEZAMKNIETA`, `E-SPRZEGNIETO-PRZED-ROZPEDEM`, `E-NIEPRAWIDLOWY-MESH`, `E-POMINIETO-KONTROLE`, `E-ESTOP-NIE-ODBLOKOWANY`, `E-ZASILANIE-NIE-WLACZONE`, `E-NIEZNANY` (≥7 kodów).
    - `src/training/scoringWeights.js` eksportuje `DEFAULT_WEIGHTS`, `SCORE_BASELINE` (=100), `SCORE_FLOOR` (=0).
    - `Object.isFrozen(DEFAULT_WEIGHTS) === true`.
    - Oba pliki używają ASCII tylko w identyfikatorach (klucze `'rozpedzanie'`, `'oczekiwanie-na-inspekcje'` to ASCII strings; tylko VALUES mają polskie diakrytyki).
  </acceptance_criteria>
  <done>Pliki istnieją, ładują się jako ESM, dokładne literały D-10/D-09/D-16 obecne, frozen weights.</done>
</task>

<task type="auto">
  <name>Task 2: Utwórz scenariusz uruchomienie (8 kroków D-06) + scenarios/index.js + validateScenario.js + fixture</name>
  <files>src/training/scenarios/uruchomienie.js, src/training/scenarios/index.js, src/training/scenarios/validateScenario.js, tests/fixtures/scenario.fixture.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "D. uruchomienie.js" (linie 921–1052) — VERBATIM 8-step shape, copy each `id`/`kind`/`labelPL`/`descriptionPL`/`rationalePL`/`effectsOn*` field
    - .planning/phases/01-foundation/01-CONTEXT.md decyzje D-01, D-02, D-04, D-05, D-06, D-07
    - .planning/phases/01-foundation/01-PATTERNS.md § "uruchomienie.js" (linie 185–207), § "scenarios/index.js" (linie 211–228), § "validateScenario.js" (linie 232–257)
    - .planning/phases/01-foundation/01-RESEARCH.md § "Open Questions" #2 (validateBefore inline function decision — research rekomenduje INLINE FUNCTION dla v1)
  </read_first>
  <action>
    1. Utwórz katalog `src/training/scenarios/` i plik `src/training/scenarios/uruchomienie.js`. **Skopiuj VERBATIM** z RESEARCH §D linie 921–1050 cały `export default {...}` ze WSZYSTKIMI 8 krokami. Dokładne pola każdego kroku zgodnie z &lt;interfaces&gt; powyżej. Krytyczne wartości:
       - `id: 'uruchomienie'`
       - `initialMachineState: 'oczekiwanie-na-inspekcje'`
       - Steps array length === 8
       - Step 5 `effectsOnSuccess: [{ type: 'setMeshState', meshId: 'oslona-przednia', value: 'closed' }]`
       - Step 7 `effectsOnSuccess: [{ type: 'setMachineState', value: 'rozpedzanie' }, { type: 'startSpinUpTimer', ms: 3000 }]`
       - Step 8 ma `validateBefore: (state) => state.machineState === 'gotowa-do-pracy'` jako INLINE arrow function (Open Question #2 resolution: inline function dla v1, eskalacja do declarative spec dopiero w Phase 6 jeśli 3+ scenariuszy będzie tego wymagać).
       - Step 8 `effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-SPRZEGNIETO-PRZED-ROZPEDEM', severity: 'critical' } }]`
       - Wszystkie polskie teksty `labelPL`/`descriptionPL`/`rationalePL` skopiuj VERBATIM (zachowaj diakrytyki).
       Komentarz nagłówkowy:
       ```
       // src/training/scenarios/uruchomienie.js
       // Scenariusz: bezpieczne uruchomienie prasy PM-300 (D-06 8 kroków).
       // Polskie teksty per krok zgodnie z D-04. Effects deklaratywne per D-02.
       // validateBefore na step #8 to inline function (Open Question #2 — research rekomenduje
       // inline dla v1; eskalacja do declarative spec gdy ≥3 scenariuszy tego wymaga).
       ```
    2. Utwórz `src/training/scenarios/index.js` — registry:
       ```javascript
       // src/training/scenarios/index.js
       // Rejestr scenariuszy. Phase 1: tylko `uruchomienie`. Phase 6 dorzuca 3 kolejne.

       import uruchomienie from './uruchomienie.js';

       const REGISTRY = Object.freeze({ uruchomienie });

       /**
        * Zwraca scenariusz po identyfikatorze stringowym.
        * @param {string} id - identyfikator scenariusza
        * @returns {object} scenariusz
        * @throws {Error} jeśli `id` nie jest zarejestrowane
        */
       export function loadScenario(id) {
         const s = REGISTRY[id];
         if (!s) throw new Error(`ScenarioRegistry: nieznany scenariusz "${id}". Dostępne: ${Object.keys(REGISTRY).join(', ')}`);
         return s;
       }

       /** Zwraca listę zarejestrowanych identyfikatorów. */
       export function listScenarios() {
         return Object.keys(REGISTRY);
       }
       ```
    3. Utwórz `src/training/scenarios/validateScenario.js` — ad-hoc shape validator (rekomendacja CONTEXT § Claude's Discretion: zero deps, ~50 LOC). Treść:
       ```javascript
       // src/training/scenarios/validateScenario.js
       // Ad-hoc walidator kształtu scenariusza (SOP-02). Zero deps — bez zod.
       // Eskalacja do zod możliwa w Phase 6 jeśli powierzchnia wzrośnie.

       const VALID_KINDS = new Set(['manipulation', 'visual-target', 'visual-attest']);
       const VALID_EFFECT_TYPES = new Set([
         'setMachineState', 'setMeshState', 'appendEvent',
         'advanceStep', 'startSpinUpTimer', 'playAudio',
       ]);

       /**
        * Sprawdza kształt scenariusza. Rzuca z polskim komunikatem przy błędzie.
        * @param {object} scenario
        * @throws {Error} przy malformacji
        */
       export function validateScenario(scenario) {
         if (!scenario || typeof scenario !== 'object') {
           throw new Error(`Scenariusz: oczekiwano obiektu, otrzymano ${scenario === null ? 'null' : typeof scenario}`);
         }
         if (typeof scenario.id !== 'string' || scenario.id.length === 0) {
           throw new Error('Scenariusz: pole `id` musi być niepustym stringiem');
         }
         if (!Array.isArray(scenario.steps) || scenario.steps.length === 0) {
           throw new Error(`Scenariusz "${scenario.id}": pole \`steps\` musi być niepustą tablicą`);
         }
         const seenIds = new Set();
         for (const step of scenario.steps) {
           if (!step || typeof step !== 'object') {
             throw new Error(`Scenariusz "${scenario.id}": każdy krok musi być obiektem`);
           }
           if (typeof step.id !== 'string' || step.id.length === 0) {
             throw new Error(`Scenariusz "${scenario.id}": krok bez stringowego \`id\``);
           }
           if (seenIds.has(step.id)) {
             throw new Error(`Scenariusz "${scenario.id}": zduplikowany id kroku "${step.id}"`);
           }
           seenIds.add(step.id);
           if (!VALID_KINDS.has(step.kind)) {
             throw new Error(`Scenariusz "${scenario.id}": krok "${step.id}" ma nieznany kind "${step.kind}"`);
           }
           const needsTarget = step.kind === 'manipulation' || step.kind === 'visual-target';
           if (needsTarget &amp;&amp; (typeof step.targetMeshId !== 'string' || step.targetMeshId.length === 0)) {
             throw new Error(`Scenariusz "${scenario.id}": krok "${step.id}" kind=${step.kind} wymaga niepustego \`targetMeshId\``);
           }
           if (step.kind === 'visual-attest' &amp;&amp; step.targetMeshId !== undefined) {
             throw new Error(`Scenariusz "${scenario.id}": krok "${step.id}" kind=visual-attest nie może mieć \`targetMeshId\``);
           }
           // effects shape (jeśli obecne)
           for (const fld of ['effectsOnSuccess', 'effectsOnError']) {
             const list = step[fld];
             if (list !== undefined &amp;&amp; !Array.isArray(list)) {
               throw new Error(`Scenariusz "${scenario.id}": krok "${step.id}" pole \`${fld}\` musi być tablicą`);
             }
             if (Array.isArray(list)) {
               for (const eff of list) {
                 if (!eff || typeof eff !== 'object' || !VALID_EFFECT_TYPES.has(eff.type)) {
                   throw new Error(`Scenariusz "${scenario.id}": krok "${step.id}" effect.type "${eff?.type}" niewspierany`);
                 }
               }
             }
           }
           if (step.validateBefore !== undefined &amp;&amp; typeof step.validateBefore !== 'function') {
             throw new Error(`Scenariusz "${scenario.id}": krok "${step.id}" \`validateBefore\` musi być funkcją (state) => boolean`);
           }
         }
       }
       ```
    4. Utwórz `tests/fixtures/scenario.fixture.js` — minimalny scenariusz dla testów Plan 03/04:
       ```javascript
       // tests/fixtures/scenario.fixture.js
       // Minimalny scenariusz-stub dla testów ProcedureEngine i TrainingStore.
       // 3 kroki, jeden każdego kind, bez forbidden-state guard.

       export const minimalScenario = {
         id: 'fixture-minimal',
         titlePL: 'Fixture',
         descriptionPL: 'Minimalny scenariusz testowy.',
         initialMachineState: 'oczekiwanie-na-inspekcje',
         steps: [
           {
             id: 'step-visual-target',
             kind: 'visual-target',
             targetMeshId: 'mesh-A',
             labelPL: 'Krok visual-target',
             descriptionPL: 'Pierwszy krok.',
             rationalePL: 'Test fixture.',
             effectsOnSuccess: [],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-NIEPRAWIDLOWY-MESH', severity: 'medium' } }],
           },
           {
             id: 'step-visual-attest',
             kind: 'visual-attest',
             labelPL: 'Krok visual-attest',
             descriptionPL: 'Drugi krok.',
             rationalePL: 'Test fixture.',
             effectsOnSuccess: [],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-POMINIETO-KONTROLE', severity: 'minor' } }],
           },
           {
             id: 'step-manipulation',
             kind: 'manipulation',
             targetMeshId: 'mesh-B',
             labelPL: 'Krok manipulation',
             descriptionPL: 'Trzeci krok.',
             rationalePL: 'Test fixture.',
             effectsOnSuccess: [{ type: 'setMeshState', meshId: 'mesh-B', value: 'engaged' }],
             effectsOnError: [{ type: 'appendEvent', event: { type: 'step.violation', errorCode: 'E-NIEPRAWIDLOWY-MESH', severity: 'medium' } }],
           },
         ],
       };
       ```
  </action>
  <verify>
    <automated>
      node --input-type=module -e "import('./src/training/scenarios/uruchomienie.js').then(m =&gt; { const s=m.default; if(s.id!=='uruchomienie')process.exit(1); if(s.steps.length!==8)process.exit(2); const ids=s.steps.map(x=&gt;x.id); const expected=['sprawdz-tabliczke','kontrola-narzedzia','kontrola-wzrokowa','sprawdz-olej','zamknij-oslone','odblokuj-estop','wlacz-zasilanie','sprzegnij-po-rozpedzie']; if(JSON.stringify(ids)!==JSON.stringify(expected)){console.error('ids mismatch:'+JSON.stringify(ids));process.exit(3);} const last=s.steps[7]; if(typeof last.validateBefore!=='function')process.exit(4); if(last.validateBefore({machineState:'rozpedzanie'})!==false)process.exit(5); if(last.validateBefore({machineState:'gotowa-do-pracy'})!==true)process.exit(6); const sp=s.steps[6]; if(!sp.effectsOnSuccess.some(e=&gt;e.type==='startSpinUpTimer'&amp;&amp;e.ms===3000))process.exit(7); if(!sp.effectsOnSuccess.some(e=&gt;e.type==='setMachineState'&amp;&amp;e.value==='rozpedzanie'))process.exit(8); console.log('uruchomienie OK'); });"
      &amp;&amp; node --input-type=module -e "import('./src/training/scenarios/index.js').then(m =&gt; { const s=m.loadScenario('uruchomienie'); if(s.id!=='uruchomienie')process.exit(1); try{ m.loadScenario('nieistnieje'); process.exit(2);}catch(e){if(!/nieznany scenariusz/.test(e.message))process.exit(3);} if(!m.listScenarios().includes('uruchomienie'))process.exit(4); console.log('registry OK'); });"
      &amp;&amp; node --input-type=module -e "import('./src/training/scenarios/validateScenario.js').then(async m =&gt; { try{ m.validateScenario(null); process.exit(1);}catch{} try{ m.validateScenario({id:'',steps:[]});process.exit(2);}catch{} try{ m.validateScenario({id:'x',steps:[{id:'a',kind:'wrong'}]}); process.exit(3);}catch{} const u=(await import('./src/training/scenarios/uruchomienie.js')).default; m.validateScenario(u); console.log('validator OK'); });"
      &amp;&amp; node --input-type=module -e "import('./tests/fixtures/scenario.fixture.js').then(m =&gt; { if(m.minimalScenario.steps.length!==3)process.exit(1); console.log('fixture OK'); });"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/training/scenarios/uruchomienie.js` istnieje, default-exports object z `id === 'uruchomienie'`.
    - Steps array ma DOKŁADNIE 8 elementów w kolejności: `sprawdz-tabliczke`, `kontrola-narzedzia`, `kontrola-wzrokowa`, `sprawdz-olej`, `zamknij-oslone`, `odblokuj-estop`, `wlacz-zasilanie`, `sprzegnij-po-rozpedzie`.
    - Step 7 (`wlacz-zasilanie`) `effectsOnSuccess` zawiera `{type:'setMachineState', value:'rozpedzanie'}` i `{type:'startSpinUpTimer', ms:3000}`.
    - Step 8 (`sprzegnij-po-rozpedzie`) `validateBefore` to function; `validateBefore({machineState:'gotowa-do-pracy'}) === true`; `validateBefore({machineState:'rozpedzanie'}) === false`.
    - Step 8 `effectsOnError[0].event.errorCode === 'E-SPRZEGNIETO-PRZED-ROZPEDEM'` i `severity === 'critical'`.
    - `loadScenario('uruchomienie')` zwraca scenariusz; `loadScenario('zlid')` rzuca z polskim komunikatem zawierającym "nieznany scenariusz".
    - `validateScenario(uruchomienie)` NIE rzuca (scenariusz przechodzi walidację).
    - `validateScenario(null)`, `validateScenario({id:'',steps:[]})`, `validateScenario({id:'x',steps:[{id:'a',kind:'wrong'}]})` wszystkie rzucają.
    - `tests/fixtures/scenario.fixture.js` eksportuje `minimalScenario` z 3 krokami (jeden każdego kind: visual-target, visual-attest, manipulation).
  </acceptance_criteria>
  <done>Wszystkie 4 pliki ładują się czysto, scenariusz `uruchomienie` ma 8 poprawnie ukształtowanych kroków, validator nie rzuca na poprawny scenariusz, registry działa, fixture dostępny dla planów 03/04.</done>
</task>

<task type="auto">
  <name>Task 3: Utwórz src/training/faultRules.js + tests/scenarioShape.test.js</name>
  <files>src/training/faultRules.js, tests/scenarioShape.test.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md decyzja D-03 (faultRules globalny moduł, lista `[{id, when, then, severity}]`)
    - .planning/phases/01-foundation/01-RESEARCH.md § "faultRules.js" w PATTERNS (linie 156–181) — uwaga: PATTERNS sugeruje że `evaluateFaultRules` żyje w `ProcedureEngine.js` per RESEARCH §C imports. **DECYZJA PLANNERA**: w Phase 1 SOP-07 wymaga że "evaluateFaultRules weryfikuje invarianty"; aby uniknąć cyklicznej zależności (Plan 03 ProcedureEngine NIE importuje faultRules — tylko store w Plan 04 to robi), umieść funkcję `evaluateFaultRulesData(state, rules)` w `src/training/faultRules.js` jako PURE helper. Plan 03 ProcedureEngine eksportuje WŁASNĄ wrapper-funkcję `evaluateFaultRules(state, rules)` która deleguje do `evaluateFaultRulesData` (lub w prostszej wersji re-eksportuje ją). Konsekwencja: faultRules.js zawiera DANE + PURE EVALUATOR; ProcedureEngine.js re-eksportuje evaluator pod nazwą `evaluateFaultRules` aby store mógł importować z jednego miejsca. **Plan 03 musi wiedzieć że re-eksportuje `evaluateFaultRules` z faultRules.** Dokumentuj to w SUMMARY.
    - .planning/phases/01-foundation/01-RESEARCH.md § "C. trainingStore.js" (linie 783–784) — pokazuje import `import { faultRules } from '../training/faultRules.js'`
    - .planning/phases/01-foundation/01-VALIDATION.md § "Wave 0 Requirements" pozycja `tests/scenarioShape.test.js`
    - tests/fixtures/scenario.fixture.js (utworzony w Task 2 — używany w testach scenarioShape jako negatywne fixtures jeśli potrzeba)
    - src/training/scenarios/validateScenario.js (utworzony w Task 2)
    - src/training/scenarios/uruchomienie.js (utworzony w Task 2)
  </read_first>
  <action>
    1. Utwórz `src/training/faultRules.js`:
       ```javascript
       // src/training/faultRules.js
       // Cross-cutting safety invariants (SOP-07, D-03). Lista [{id, when, then, severity}].
       // Evaluator `evaluateFaultRulesData` jest pure top-level — Plan 03 ProcedureEngine
       // re-eksportuje go pod nazwą `evaluateFaultRules` aby store mógł importować z jednego miejsca.
       //
       // Dlaczego DANE + EVALUATOR żyją tu razem (a nie w ProcedureEngine):
       // - faultRules to konfiguracja BHP — może być edytowana przez expert review bez ruszania silnika.
       // - Evaluator jest trywialny (~10 LOC) i pasuje do tego samego modułu domenowego.
       // - Plan 03 ProcedureEngine importuje go i re-eksportuje — single import point dla store.

       /**
        * Reguły bezpieczeństwa wykonywane PO każdym applied-effects w storze.
        * @typedef {object} FaultRule
        * @property {string} id - stabilny identyfikator reguły
        * @property {(state: object) => boolean} when - predykat
        * @property {{effects: Array<object>}} then - efekty do zaaplikowania (np. setMachineState 'awaria')
        * @property {'critical'|'medium'|'minor'} severity
        */

       /** @type {ReadonlyArray<FaultRule>} */
       export const faultRules = Object.freeze([
         {
           id: 'oslona-otwarta-w-cyklu',
           when: (state) => state.machineState === 'w-cyklu' && state.meshStates?.['oslona-przednia'] !== 'closed',
           then: {
             effects: [
               { type: 'appendEvent', event: { type: 'fault.triggered', faultId: 'oslona-otwarta-w-cyklu', severity: 'critical' } },
               { type: 'setMachineState', value: 'awaria' },
             ],
           },
           severity: 'critical',
         },
       ]);

       /**
        * Aplikuje wszystkie reguły do snapshot'u state'a. Zwraca tablicę effects do
        * przekazania do reduktora store'a. NIE mutuje state.
        *
        * @param {object} state - snapshot store
        * @param {ReadonlyArray<FaultRule>} [rules] - lista reguł (default: globalny faultRules)
        * @returns {Array<object>} effects — concat-ed z `rule.then.effects` dla każdego dopasowania
        */
       export function evaluateFaultRulesData(state, rules = faultRules) {
         const effects = [];
         for (const rule of rules) {
           try {
             if (rule.when(state)) {
               for (const eff of rule.then.effects) {
                 effects.push(eff);
               }
             }
           } catch {
             // Defensive: błędne `when` predicate nie ma prawa wywalić silnika scoringu.
             // W produkcji loguje console.warn (dev) — w Phase 1 silently skip.
           }
         }
         return effects;
       }
       ```
    2. Utwórz `tests/scenarioShape.test.js`:
       ```javascript
       // tests/scenarioShape.test.js
       // @vitest-environment node
       // SOP-02: scenariusze mają stabilne stringowe id i deklaratywny shape.

       import { describe, it, expect } from 'vitest';
       import uruchomienie from '../src/training/scenarios/uruchomienie.js';
       import { validateScenario } from '../src/training/scenarios/validateScenario.js';
       import { loadScenario, listScenarios } from '../src/training/scenarios/index.js';
       import { minimalScenario } from './fixtures/scenario.fixture.js';

       describe('Scenario shape (SOP-02)', () => {
         it('uruchomienie ma id "uruchomienie" i 8 kroków', () => {
           expect(uruchomienie.id).toBe('uruchomienie');
           expect(uruchomienie.steps).toHaveLength(8);
         });

         it('wszystkie step.id są stringami i unikalne (zakaz numerycznych indeksów)', () => {
           const ids = uruchomienie.steps.map(s => s.id);
           expect(ids.every(id => typeof id === 'string' && id.length > 0)).toBe(true);
           expect(new Set(ids).size).toBe(ids.length);
         });

         it('kolejność kroków zgodna z D-06', () => {
           expect(uruchomienie.steps.map(s => s.id)).toEqual([
             'sprawdz-tabliczke', 'kontrola-narzedzia', 'kontrola-wzrokowa', 'sprawdz-olej',
             'zamknij-oslone', 'odblokuj-estop', 'wlacz-zasilanie', 'sprzegnij-po-rozpedzie',
           ]);
         });

         it('każdy step ma kind jest jednym z {manipulation, visual-target, visual-attest}', () => {
           const valid = new Set(['manipulation', 'visual-target', 'visual-attest']);
           for (const s of uruchomienie.steps) {
             expect(valid.has(s.kind)).toBe(true);
           }
         });

         it('manipulation/visual-target wymagają targetMeshId; visual-attest nie ma go', () => {
           for (const s of uruchomienie.steps) {
             if (s.kind === 'manipulation' || s.kind === 'visual-target') {
               expect(typeof s.targetMeshId).toBe('string');
               expect(s.targetMeshId.length).toBeGreaterThan(0);
             } else {
               expect(s.targetMeshId).toBeUndefined();
             }
           }
         });

         it('step "wlacz-zasilanie" ma startSpinUpTimer effect z ms=3000 (D-07)', () => {
           const sp = uruchomienie.steps.find(s => s.id === 'wlacz-zasilanie');
           expect(sp.effectsOnSuccess.some(e => e.type === 'setMachineState' && e.value === 'rozpedzanie')).toBe(true);
           expect(sp.effectsOnSuccess.some(e => e.type === 'startSpinUpTimer' && e.ms === 3000)).toBe(true);
         });

         it('step "sprzegnij-po-rozpedzie" ma validateBefore wymagający gotowa-do-pracy (D-07)', () => {
           const sp = uruchomienie.steps.find(s => s.id === 'sprzegnij-po-rozpedzie');
           expect(typeof sp.validateBefore).toBe('function');
           expect(sp.validateBefore({ machineState: 'gotowa-do-pracy' })).toBe(true);
           expect(sp.validateBefore({ machineState: 'rozpedzanie' })).toBe(false);
         });

         it('step "sprzegnij-po-rozpedzie" effectsOnError ma errorCode E-SPRZEGNIETO-PRZED-ROZPEDEM severity=critical', () => {
           const sp = uruchomienie.steps.find(s => s.id === 'sprzegnij-po-rozpedzie');
           const evt = sp.effectsOnError[0].event;
           expect(evt.errorCode).toBe('E-SPRZEGNIETO-PRZED-ROZPEDEM');
           expect(evt.severity).toBe('critical');
         });

         it('validateScenario(uruchomienie) nie rzuca', () => {
           expect(() => validateScenario(uruchomienie)).not.toThrow();
         });

         it('validateScenario(minimalScenario) nie rzuca', () => {
           expect(() => validateScenario(minimalScenario)).not.toThrow();
         });

         it('validateScenario(null) rzuca z komunikatem polskim', () => {
           expect(() => validateScenario(null)).toThrow(/oczekiwano obiektu/);
         });

         it('validateScenario odrzuca step bez id', () => {
           expect(() => validateScenario({ id: 'x', steps: [{ kind: 'manipulation', targetMeshId: 'm' }] }))
             .toThrow(/krok bez stringowego/);
         });

         it('validateScenario odrzuca step z nieznanym kind', () => {
           expect(() => validateScenario({ id: 'x', steps: [{ id: 'a', kind: 'wrong' }] }))
             .toThrow(/nieznany kind/);
         });

         it('validateScenario odrzuca manipulation bez targetMeshId', () => {
           expect(() => validateScenario({ id: 'x', steps: [{ id: 'a', kind: 'manipulation' }] }))
             .toThrow(/targetMeshId/);
         });

         it('validateScenario odrzuca visual-attest z targetMeshId', () => {
           expect(() => validateScenario({ id: 'x', steps: [{ id: 'a', kind: 'visual-attest', targetMeshId: 'm' }] }))
             .toThrow(/visual-attest nie może mieć/);
         });

         it('listScenarios zawiera "uruchomienie"', () => {
           expect(listScenarios()).toContain('uruchomienie');
         });

         it('loadScenario("uruchomienie") zwraca scenariusz', () => {
           expect(loadScenario('uruchomienie').id).toBe('uruchomienie');
         });

         it('loadScenario("nieistnieje") rzuca polskim komunikatem', () => {
           expect(() => loadScenario('nieistnieje')).toThrow(/nieznany scenariusz/);
         });
       });
       ```
  </action>
  <verify>
    <automated>
      node --input-type=module -e "import('./src/training/faultRules.js').then(m =&gt; { if(!Array.isArray(m.faultRules))process.exit(1); if(m.faultRules.length===0)process.exit(2); if(typeof m.evaluateFaultRulesData!=='function')process.exit(3); const eff=m.evaluateFaultRulesData({machineState:'w-cyklu',meshStates:{'oslona-przednia':'open'}}); if(!eff.some(e=&gt;e.type==='setMachineState'&amp;&amp;e.value==='awaria'))process.exit(4); const safe=m.evaluateFaultRulesData({machineState:'w-cyklu',meshStates:{'oslona-przednia':'closed'}}); if(safe.length!==0)process.exit(5); console.log('faultRules OK'); });"
      &amp;&amp; npx vitest run tests/scenarioShape.test.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/training/faultRules.js` eksportuje `faultRules` (frozen array, ≥1 element) i `evaluateFaultRulesData(state, rules?)` (function).
    - `evaluateFaultRulesData({machineState:'w-cyklu', meshStates:{'oslona-przednia':'open'}})` zwraca tablicę zawierającą effect `{type:'setMachineState', value:'awaria'}` i `{type:'appendEvent', event:{type:'fault.triggered', severity:'critical'}}`.
    - `evaluateFaultRulesData({machineState:'w-cyklu', meshStates:{'oslona-przednia':'closed'}})` zwraca pustą tablicę (bezpieczny stan).
    - `evaluateFaultRulesData({machineState:'oczekiwanie-na-inspekcje', meshStates:{}})` zwraca pustą tablicę.
    - `tests/scenarioShape.test.js` ma ≥17 `it(...)`, wszystkie PASS.
    - `npx vitest run tests/scenarioShape.test.js` exit 0.
    - `faultRules` jest `Object.isFrozen() === true`.
    - Każdy element `faultRules` ma keys `id`, `when`, `then`, `severity`.
  </acceptance_criteria>
  <done>SOP-02 i SOP-07 spełnione: scenariusz przechodzi shape walidację, faultRules wykrywa guard-open-during-cycle, 17+ testów zielone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenariusz JSON → ProcedureEngine | scenariusz jest danymi statycznymi w bundle; brak user-controlled content |
| `pl.js` strings → DOM | banner i panele (Phase 4) injectują przez `textContent` (XSS-safe), nie `innerHTML` |
| `faultRules.when(state)` execution | predykaty wywoływane na zaufanym snapshot'cie store; defensive try/catch chroni przed bug w predykacie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | XSS via i18n | mitigate | `pl.js` stringi są STATYCZNE (literally `export const pl = {...}`); DisclaimerBanner (Plan 05) używa `textContent` dla pełnego tekstu — RESEARCH § Security V5 row 1 |
| T-02-02 | Tampering | scenariusz JSON modyfikowany przez build supply chain | accept | Scenariusze to source code w repo; review per PR; brak zewnętrznego loadingu |
| T-02-03 | DoS | malformed scenariusz crashuje silnik | mitigate | `validateScenario` rzuca explicit Error z polskim komunikatem zanim ProcedureEngine widzi malformed input — fail-fast w `Application` constructor (Plan 04 zawoła `validateScenario(loadScenario('uruchomienie'))` przy startScenario) |
| T-02-04 | DoS | bug w `rule.when` predykacie wywala scoring | mitigate | `evaluateFaultRulesData` ma try/catch — rule błędna jest silently skipowana, nie wywala całej oceny |
| T-02-05 | Information Disclosure | polski komunikat błędu leakuje internal path | accept | Komunikaty zawierają tylko nazwę modułu (`Scenariusz "uruchomienie"`) + offending value — brak file paths ani sekretów (RESEARCH § Security V7) |
</threat_model>

<verification>
- `npx vitest run tests/scenarioShape.test.js` zielone (≥17 testów).
- `node --input-type=module -e "import('./src/training/scenarios/uruchomienie.js')..."` smoke check verifikuje `validateScenario(uruchomienie)` nie rzuca.
- `npm test` po Plan 02 pokazuje ≥28 testów PASS (11 z physicsEngine + 17 z scenarioShape).
- `git status` pokazuje untracked: 6 plików w `src/{i18n,training}/`, 2 pliki w `tests/`.
</verification>

<success_criteria>
- UI-06: Wszystkie nowe Polish strings w `src/i18n/pl.js` (lista 14+ kluczy z UI-SPEC).
- SOP-02: Scenariusz `uruchomienie` ma stabilne string ids; `validateScenario` enforce'uje shape.
- SOP-03: Scenariusz zaimplementowany (8 kroków per D-06; integration test żyje w Plan 04).
- SOP-07: `faultRules` array istnieje z guard-open-during-cycle invariant; `evaluateFaultRulesData` pure top-level function.
- Wszystkie acceptance criteria 3 tasków spełnione.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` zawierający:
- Lista 8 utworzonych plików.
- Decyzja: `evaluateFaultRulesData` żyje w `faultRules.js`; Plan 03 re-eksportuje pod nazwą `evaluateFaultRules` w `ProcedureEngine.js` (do potwierdzenia w Plan 03).
- Decyzja: `validateBefore` jako inline arrow function w pliku scenariusza (Open Question #2 resolution).
- Coverage check: te pliki są DANYMI (`src/training/scenarios/**` excluded z coverage threshold per `vitest.config.js`); `faultRules.js` i `scoringWeights.js` w pełnym `src/training/**` scope, więc Plan 04 testy muszą je dotknąć.
- Komendy weryfikacji + output.
</output>
