---
phase: 07-kinematic-fix-anchoring
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/PressModel.js
autonomous: true
task_count: 2
requirements: [ANCHOR-02]
must_haves:
  truths:
    - "Dwa łożyska (lewe, prawe) widoczne jako cylindry między kolumnami ramy a wałem na wysokości shaftY=8"
    - "Żaden z łożysk NIE jest w `getInteractables()` ani `getMeshDictionary()` (decoration only)"
    - "Każdy nowy mesh ma `userData.kind === 'decoration'` (CONTEXT Specifics, D-Phase7-03)"
    - "Łożyska NIE są dziećmi `shaftAxis` — pozostają statyczne podczas `update(angle)` (KIN-01 invariant)"
    - "PressModel.smoke.test.js nadal asseruje `getInteractables().size === 15` (NIE 17) — łożyska nie pojawiają się w mapie"
  artifacts:
    - path: "src/PressModel.js"
      provides: "_buildBearings() metoda + wywołanie w buildPress(), 2 decoration meshy"
      contains: "_buildBearings"
  key_links:
    - from: "src/PressModel.js _buildBearings"
      to: "this.group (NIE this.shaftAxis)"
      via: "this.group.add(bearing)"
      pattern: "this\\.group\\.add\\(bearing"
---

<objective>
Dodać 2 łożyska wału (lewe + prawe) jako decoration meshy w `PressModel.js` per D-Phase7-03 (ANCHOR-02). Cylindry R=0.6 H=0.8 z osią X, pozycje świat (-2.0, 8, 0) i (2.0, 8, 0). NIE klikalne, NIE w `getInteractables()`, NIE rotują z wałem.

Purpose: Eliminuje floating-shaft visual gap — wał wsparty przez 2 widoczne wsporniki między kolumnami a osią obrotu. Spełnia ANCHOR-02; przygotowuje fundament dla Phase 8 GEO-03 (osłony łożysk).

Output: Rozszerzony `src/PressModel.js` o `_buildBearings()` wywołane z `buildPress()`. Plan 07-03 (anchor audit) wykryje że łożyska NIE muszą być w audicie (decoration), audit dotyczy tylko interactables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kinematic-fix-anchoring/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/PressModel.js

<interfaces>
<!-- Konwencje z istniejącego kodu PressModel.js dla decoration meshy -->

Hierarchia (TWIN-11/12/13):
- `this.group` — root group prasy, dodawany do scene
- `this.shaftAxis` — child this.group, rotuje pod `update(angle)`
- Decoration meshy: dzieci `this.group` (NIE shaftAxis), `userData.kind='decoration'`, NIE rejestrowane przez `_registerInteractable`

Frame columns (linie 73–93):
- leftFrame @ (-2, (shaftY+2)/2, -1), BoxGeometry(0.5, shaftY+2, 2)  // x=-2
- rightFrame @ (2, (shaftY+2)/2, -1), BoxGeometry(0.5, shaftY+2, 2)  // x=2
- topFrame @ (0, shaftY+1.5, -1), BoxGeometry(6, 1, 2)
- shaftY = 8

Shaft (linia 95–105):
- shaft cylinder w shaftAxis @ (0, shaftY=8, 0), rotateZ(π/2) → wzdłuż X, długość 4.5
- shaft extends from x=-2.25 do x=+2.25 w lokalnym frame'a shaftAxis = świat (-2.25, 8, 0) do (2.25, 8, 0)
- Frame columns @ x=±2 — łożyska między kolumną a wałem to x ∈ [-2.0, -2.25] (lewe) i [2.0, 2.25] (prawe)
- Łożysko cylinder R=0.6 H=0.8 z osią X — pokrywa shaft (R=0.4) + ma większy promień (R=0.6) żeby było widoczne

Material (z konstrukcji ramy):
- `this.matBody` — używany przez frame i topFrame; industrial grey. CONTEXT D-Phase7-03: "materiał industrial grey matMetal (matness ≈ 0.8)" — Phase 9 doprecyzuje PBR. Możemy użyć `this.matBody` (existing) lub `this.matShaft` (też metaliczne) — wybierz `this.matBody` (ten sam co frame, wizualnie spójny ze wspornikami).

CRIT-7 contract: decoration meshes NIE wywołują `_registerInteractable`. Nie potrzebują `userData.id`, `labelPL`, `descriptionPL`, `restPosition`. Tylko `userData.kind = 'decoration'` per CONTEXT Specifics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dodaj _buildBearings() — 2 łożyska decoration</name>
  <files>src/PressModel.js</files>
  <behavior>
    - Po `new PressModel(scene)`: scene-graph traversal znajduje DOKŁADNIE 2 meshy z `userData.kind === 'decoration'` (po Phase 7; Phase 8/9 dodadzą więcej)
    - Każde łożysko: `geometry.type === 'CylinderGeometry'`, parametry promienia ≈ 0.6, wysokość ≈ 0.8
    - Lewe łożysko: world position ≈ (-2.0, 8, 0); Prawe: (2.0, 8, 0); tolerance 1e-6
    - Łożyska są dziećmi `this.group` (NIE `this.shaftAxis`) — po `pressModel.update(Math.PI / 2)` ich world position NIEZMIENIONA (rotacyjnie statyczne, KIN-01 invariant)
    - `pressModel.getInteractables().size === 15` (NIEZMIENIONE — łożyska nie rejestrowane)
    - `pressModel.getMeshDictionary().size === 15` (NIEZMIENIONE)
    - `pressModel.getInteractables().has('lozysko-lewe')` === false; analogicznie 'lozysko-prawe', 'bearing-left' itp. (NIE klikalne)
  </behavior>
  <action>
    Dodaj metodę `_buildBearings()` w klasie `PressModel` (umieść po `_buildClutchLever()`, przed `_registerInteractable()` invariant block — linia ~740). Wywołaj ją z `buildPress()` PO `this.update(0)` linii inicjalizacji położenia (linia 171) — lub tuż przed nią; ważne że PO geometrii ramy i shaftAxis. Sugerowane miejsce: tuż po `_buildClutchLever();` (linia ~168), przed komentarzem "Inicjalizacja położenia".

    Implementacja `_buildBearings()`:

    1. Geometria współdzielona: `const bearingGeo = new THREE.CylinderGeometry(0.6, 0.6, 0.8, 32);`
    2. Obróć geometrię żeby cylinder leżał wzdłuż X (oś wału): `bearingGeo.rotateZ(Math.PI / 2);` — ta sama konwencja co shaft, eccentric, rim.
    3. Lewe łożysko:
       - `const bearingLeft = new THREE.Mesh(bearingGeo, this.matBody);`
       - `bearingLeft.position.set(-2.0, this.shaftY, 0);`
       - `bearingLeft.castShadow = true;`
       - `bearingLeft.receiveShadow = true;`
       - `bearingLeft.userData = { kind: 'decoration' };` (per CONTEXT Specifics — minimalny kontrakt, brak id/labelPL/poses)
       - `this.group.add(bearingLeft);` (NIE shaftAxis — łożyska statyczne)
    4. Prawe łożysko — sklonuj wzór, pozycja `(2.0, this.shaftY, 0)`:
       - `const bearingRight = new THREE.Mesh(bearingGeo, this.matBody);` (geometria współdzielona — bearingGeo reused; jeśli runtime modyfikuje geometrię to share OK bo nie modyfikujemy)
       - `bearingRight.position.set(2.0, this.shaftY, 0);`
       - Reszta jak lewe + `bearingRight.userData = { kind: 'decoration' };` + `this.group.add(bearingRight);`
    5. JSDoc nad metodą: "ANCHOR-02 / D-Phase7-03: 2 łożyska wału jako decoration meshes. Cylinder R=0.6 H=0.8 oś X. NIE w getInteractables() (decoration, raycaster ignoruje). Dzieci this.group — rotacyjnie statyczne podczas update(). Materiał this.matBody (industrial grey, spójny z frame). Phase 9 doprecyzuje PBR. Phase 8 GEO-03 może dodać osłony nad łożyskami."
    6. Boundary check: NIE wywołuj `_registerInteractable` dla łożysk. NIE dodawaj wpisów w `src/i18n/pl.js parts` (decoration NIE wymaga labelPL — TooltipManager/StepPanel nie dotykają).

    Wywołanie w `buildPress()`:
    ```
    this._buildMainSwitch();
    this._buildClutchLever();
    this._buildBearings();    // NOWE — ANCHOR-02

    // Inicjalizacja położenia
    this.update(0);
    ```

    Geometria sanity post-edit:
    - `grep -n "_buildBearings" src/PressModel.js` → co najmniej 2 matchy (deklaracja + wywołanie)
    - `grep -n "kind: 'decoration'" src/PressModel.js` → co najmniej 2 matchy (lewe + prawe)
    - `grep -c "_registerInteractable" src/PressModel.js | grep -v '^#'` — count nie wzrasta (te same 14 wywołań co przed Phase 7; 15 zarejestrowanych meshes z 1 z dwoma rejestracjami safety panel-oburezny... — sprawdź baseline w git diff żeby upewnić się że łożyska nie zostały podpięte do interactables przypadkiem)
  </action>
  <verify>
    <automated>npm test -- tests/PressModel.smoke.test.js tests/application.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>
    - `_buildBearings()` istnieje i wywoływane w `buildPress()` przed `this.update(0)`
    - 2 cylindry decoration dodane do `this.group` (NIE shaftAxis)
    - `pressModel.getInteractables().size === 15` (NIEZMIENIONE)
    - PressModel.smoke.test.js zielony — wszystkie 12+ testów (15 interactables, identity, materials, dispose, HMR cycle)
    - boundaries.test.js zielony (PressModel nadal imports tylko THREE)
  </done>
</task>

<task type="auto">
  <name>Task 2: Pełna walidacja + scene-graph sanity</name>
  <files>(none — verification only)</files>
  <action>
    1. `npm test` — full run, oczekiwane 642/642 zielone (Phase 7 nie dodał nowych testów jeszcze; Plan 07-03 doda anchor audit, Plan 07-04 doda KIN-03).
    2. Scene-graph sanity (manual jeśli nie ma testu — opcjonalnie zapisz w SUMMARY):
       - `npm run dev` → otwórz scene → DevTools console: `__app__.pressModel.group.children.filter(c => c.userData?.kind === 'decoration').length` powinno zwrócić 2.
       - Wizualnie: po wciśnięciu Start, łożyska NIE rotują z wałem (Plan 07-01 fixed shaftAxis only).
       - Łożyska są widoczne między kolumną ramy (x=±2) a wałem (długość 4.5 wzdłuż X, więc shaft kończy się ~x=±2.25). Łożyska @ x=±2 są wewnątrz tej zakładki.
    3. `npm run build` — sukces, bundle delta < 5KB (2 nowe meshy + ~10 linii kodu).
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>
    - 642/642 (lub bieżąca baseline) testów zielone
    - Build sukces
    - SUMMARY zawiera odnotowanie 2 decoration meshes added
  </done>
</task>

</tasks>

<verification>
- `grep -n "kind: 'decoration'" src/PressModel.js` — co najmniej 2 matchy
- `grep -n "this.group.add" src/PressModel.js` — wzrasta o 2 vs baseline v1.0
- `grep -n "this.shaftAxis.add" src/PressModel.js` — niezmienione vs baseline (łożyska NIE dzieci shaftAxis)
- `npm test -- tests/PressModel.smoke.test.js` — 12+ testów zielone, w szczególności `getInteractables().size === 15` nadal asseruje true
</verification>

<success_criteria>
1. ANCHOR-02: 2 widoczne łożyska między kolumnami ramy a wałem.
2. Łożyska decoration only — NIE klikalne, NIE w getInteractables/getMeshDictionary.
3. Łożyska NIE rotują z wałem (statyczne, dzieci this.group).
4. PressModel boundary preservation (D-Phase7-05): importuje nadal tylko THREE.
5. 642/642 testów zielone, build sukces.
</success_criteria>

<output>
Create `.planning/phases/07-kinematic-fix-anchoring/07-02-SUMMARY.md` when done. Include:
- Pozycje świat 2 łożysk
- Materiał użyty (matBody vs alternatywa)
- Confirm 0 wpisów w getInteractables() dla łożysk
- Bundle delta (KB)
</output>
</content>
</invoke>