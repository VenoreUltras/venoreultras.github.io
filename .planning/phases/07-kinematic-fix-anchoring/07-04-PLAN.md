---
phase: 07-kinematic-fix-anchoring
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-03]
files_modified:
  - tests/PressModel.replayKinematics.test.js
autonomous: true
task_count: 2
requirements: [KIN-03]
must_haves:
  truths:
    - "Nowy test `tests/PressModel.replayKinematics.test.js` symuluje Phase 6 replay flow: ReplayEngine.scrubTo(idx) → liveStore._currentAngle ustawione na event.angle → pressModel.update(_currentAngle) → asercja `shaftAxis.rotation.x === -replayAngle` (NIE rotation.z)"
    - "Test pokrywa zarówno scrubTo z event mającym angle, jak i fallback (scrubTo do eventu BEZ angle → resolvedAngle z ostatniego znanego angle)"
    - "Test pokrywa replay re-execution determinizm: scrubTo(N) → scrubTo(0) → scrubTo(N) daje identyczne shaftAxis.rotation.x ORAZ identyczny slider.position.y"
    - "Test pokrywa interakcję z `main.js simulationTick` replay branch (linie 282–291): mock store z replayOpen=true + _currentAngle ustawiony → pressModel.update(replayAngle) → PhysicsEngine.calculateSliderPosition(replayAngle, r, l) dla telemetry"
    - "642 + Phase 7 nowe testy (07-03 anchoring + 07-04 replay kinematics) zielone"
  artifacts:
    - path: "tests/PressModel.replayKinematics.test.js"
      provides: "KIN-03 replay regression test"
      contains: "replayKinematics"
  key_links:
    - from: "tests/PressModel.replayKinematics.test.js"
      to: "src/replay/ReplayEngine.js scrubTo"
      via: "import + call"
      pattern: "scrubTo"
    - from: "tests/PressModel.replayKinematics.test.js"
      to: "src/PressModel.js update"
      via: "pressModel.update(replayAngle)"
      pattern: "pressModel\\.update"
---

<objective>
Dodać regression test KIN-03 (D-Phase7-06): symuluje Phase 6 replay flow z naprawioną kinematyką (Plan 07-01) — `ReplayEngine.scrubTo(idx)` → `_currentAngle` injection → `pressModel.update(angle)` ustawia `shaftAxis.rotation.x` (nie `.z`) na `-replayAngle`. Confidence: 95% pokrycie regresji bugu rotacji w replay flow.

Purpose: Phase 6 replay (Plan 06-04 angle injection) musi nadal działać po Plan 07-01 rotation fix. Bez tego testu, przyszły refactor mógłby przypadkowo cofnąć fix i Phase 6 replay wyglądałby na zepsuty wizualnie (slider się porusza poprawnie via PhysicsEngine, ale flywheel/eccentric kręcą się złą osią).

Output: Nowy plik testowy `tests/PressModel.replayKinematics.test.js`. Pełny test suite (Phase 6 baseline + Phase 7 anchoring + Phase 7 replay regression) zielony.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kinematic-fix-anchoring/07-CONTEXT.md
@src/PressModel.js
@src/replay/ReplayEngine.js
@src/state/trainingStore.js
@src/main.js
@tests/replayEngine.test.js
@tests/PressModel.smoke.test.js

<interfaces>
<!-- Replay flow (Phase 6 Plan 06-04 + Plan 07-01 fix) -->

src/replay/ReplayEngine.js scrubTo (linie 84–112):
```
scrubTo(targetIdx) {
  // 1. Tworzy fresh store, aplikuje events[0..targetIdx]
  // 2. Resolvuje resolvedAngle: ostatni event z `angle` w [0..targetIdx], fallback 0
  // 3. liveStore.setState({ ..., _currentAngle: resolvedAngle })
}
```

Event log shape (z tests/replayEngine.test.js):
```
{ type: 'step.done', stepId: 's1', timestamp: 1100, angle: 0.5 }
{ type: 'step.violation', stepId: 's2', timestamp: 1300, angle: 1.2, severity: 'medium' }
{ type: 'session.start', timestamp: 1000 }                  // BEZ angle
{ type: 'fault.triggered', timestamp: 1500, faultId: 'awaryjne-zatrzymanie', severity: 'critical' }  // BEZ angle
```

src/main.js simulationTick replay branch (linie 274–291):
```
if (replayOpen) {
  const replayAngle = state._currentAngle ?? 0;
  this.currentAngle = replayAngle;
  this.pressModel.update(replayAngle);
  const displacement = PhysicsEngine.calculateSliderPosition(replayAngle, this.pressModel.r, this.pressModel.l);
  this.ui.updateTelemetry(replayAngle, displacement);
  return;
}
```

createTrainingStore (z src/state/trainingStore.js) — eksportuje:
- `createTrainingStore()` → zwraca store z `{ getState, setState, subscribe }`
- Initial state ma `_currentAngle = 0` (linia ~31–33)
- `startScenario(scenario)` initializuje steps

src/PressModel.js update (PO fix Plan 07-01):
- `shaftAxis.rotation.x = -angle` (NIE `.z`)
- `rod.rotation.x = atan2(dz, -dy)`
- slider.position.y = shaftY - PhysicsEngine.calculateSliderPosition(angle, r, l)

Canvas mock pattern (z PressModel.smoke.test.js linie 9–24) — wymagany dla _buildNameplate w jsdom.

Mock gsapTicker dla ReplayEngine constructor:
```
const fakeTicker = { add: () => {}, remove: () => {} };
```

Mock scenario (minimal — wystarczy `{ steps: [{ id: 's1', effectsOnSuccess: [], effectsOnError: [] }, { id: 's2', ... }] }` żeby `_advanceStepInStore` nie wybuchł).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Napisz tests/PressModel.replayKinematics.test.js (KIN-03)</name>
  <files>tests/PressModel.replayKinematics.test.js</files>
  <behavior>
    Plik zawiera następujące describe/it bloki:

    1. `describe('KIN-03: replay scrubTo + pressModel.update — rotation.x axis')`:
       - `it('scrubTo do step.done z angle=0.5 → _currentAngle=0.5 → pressModel.update(0.5) ustawia shaftAxis.rotation.x = -0.5')`:
         - Setup: liveStore = createTrainingStore(), engine = new ReplayEngine({ liveStore, gsapTicker: fakeTicker })
         - engine.loadAttempt({ events: [...] }, scenario)
         - engine.scrubTo(1)  // step.done @ angle=0.5
         - replayAngle = liveStore.getState()._currentAngle  → expect 0.5
         - pressModel.update(replayAngle)
         - expect `pressModel.shaftAxis.rotation.x` ≈ -0.5 (tolerance 1e-9)
         - expect `pressModel.shaftAxis.rotation.z` ≈ 0 (regression guard)
       - `it('scrubTo do eventu bez angle (session.start) → resolvedAngle=0 → rotation.x=0')`:
         - events: [{ type: 'session.start', timestamp: 1000 }]
         - engine.scrubTo(0)
         - replayAngle = liveStore.getState()._currentAngle → expect 0 (fallback)
         - pressModel.update(replayAngle) → expect rotation.x ≈ 0
       - `it('scrubTo do step.violation z angle=1.2 → rotation.x = -1.2')`:
         - Analogiczne, events: [{ type:'step.done', stepId:'s1', ts:1100, angle:0.5 }, { type:'step.violation', stepId:'s2', ts:1300, angle:1.2, severity:'medium' }]
         - engine.scrubTo(1) → _currentAngle === 1.2 → rotation.x ≈ -1.2

    2. `describe('KIN-03: replay determinism (A → B → A symmetry)')`:
       - `it('scrubTo(N) → scrubTo(0) → scrubTo(N) → identyczny shaftAxis.rotation.x ORAZ slider.position.y')`:
         - 3 events z różnymi angle [0.3, 1.1, 2.2]
         - Forward: scrubTo(2), pressModel.update(_currentAngle), snapshot { rotX, sliderY }
         - Rewind: scrubTo(0), pressModel.update(_currentAngle)
         - Forward again: scrubTo(2), pressModel.update(_currentAngle), snapshot2 { rotX, sliderY }
         - expect snapshot1 ≈ snapshot2 (deterministic re-execution — D-Phase6-07)

    3. `describe('KIN-03: simulationTick replay branch contract (integration shape)')`:
       - `it('replayAngle Z store używany w PhysicsEngine.calculateSliderPosition daje sensowny displacement')`:
         - Symuluje fragment main.js simulationTick replay branch:
         - liveStore.setState({ replayOpen: true, _currentAngle: Math.PI / 4 })
         - const state = liveStore.getState()
         - const replayAngle = state._currentAngle ?? 0
         - pressModel.update(replayAngle)
         - const displacement = PhysicsEngine.calculateSliderPosition(replayAngle, pressModel.r, pressModel.l)
         - expect `displacement > 0` ORAZ `Number.isFinite(displacement)` (PhysicsEngine sanity)
         - expect `pressModel.slider.position.y` ≈ `pressModel.shaftY - displacement` (consistency)
         - expect `pressModel.slider.position.x === 0` ORAZ `pressModel.slider.position.z === 0` (Y-only invariant z Plan 07-01)

    Wszystkie testy używają canvas mock dla jsdom + reuse pattern ze smoke testu.
  </behavior>
  <action>
    Stwórz plik `tests/PressModel.replayKinematics.test.js` z:

    1. Header:
       ```
       // @vitest-environment jsdom
       // KIN-03 / D-Phase7-06: regresja Phase 6 replay flow po Plan 07-01 rotation fix.
       ```

    2. Canvas mock (kopia ze smoke testu linie 9–24).

    3. Importy:
       ```
       import { describe, it, expect, beforeEach } from 'vitest';
       import * as THREE from 'three';
       import { PressModel } from '../src/PressModel.js';
       import { PhysicsEngine } from '../src/PhysicsEngine.js';
       import { ReplayEngine } from '../src/replay/ReplayEngine.js';
       import { createTrainingStore } from '../src/state/trainingStore.js';
       ```

    4. Helper builders:
       ```
       function makeScenario() {
         return {
           id: 'test-scenario',
           steps: [
             { id: 's1', effectsOnSuccess: [], effectsOnError: [] },
             { id: 's2', effectsOnSuccess: [], effectsOnError: [] },
             { id: 's3', effectsOnSuccess: [], effectsOnError: [] },
           ],
         };
       }
       function makeTicker() { return { add: () => {}, remove: () => {} }; }
       ```

    5. beforeEach:
       ```
       scene = new THREE.Scene();
       pressModel = new PressModel(scene);
       liveStore = createTrainingStore();
       liveStore.getState().startScenario(makeScenario());
       engine = new ReplayEngine({ liveStore, gsapTicker: makeTicker() });
       ```

    6. Testy jak w `<behavior>` powyżej. Asercje używają `toBeCloseTo(value, 9)` dla floating-point porównań kąta i `toBeLessThan(1e-9)` dla `Math.abs(diff)` patternów.

    7. UWAGA na `startScenario` mutację: scrubTo woła `fresh.getState().startScenario(this._scenario)` i potem kopiuje slice do liveStore. Mock scenario z plain steps[] wystarczy żeby `_advanceStepInStore` (linie 264–273 ReplayEngine.js) działał. Jeśli `startScenario` w trainingStore wymaga konkretnych pól (np. metadata) — sprawdź `src/state/trainingStore.js` i dostosuj scenario shape. Bezpieczna minimalna wersja widoczna w `tests/replayEngine.test.js` linie ~30–60 (sprawdź realny shape used tam).

    Boundary: Test importuje vitest, three, PressModel, PhysicsEngine, ReplayEngine, trainingStore — wszystkie standardowe entry points. Nie wymaga modyfikacji `tests/boundaries.test.js`.

    Pre-pass sanity: jeśli `startScenario` mock-scenariusza wybucha (np. wymagałby pola którego nie podajesz), uprość test do **NIE używania scrubTo** — zamiast tego bezpośrednio `liveStore.setState({ _currentAngle: 0.5 })`, potem `pressModel.update(liveStore.getState()._currentAngle)`. To redukuje pokrycie z full scrubTo flow do "store→update angle injection" — nadal pokrywa KIN-03 core (angle Z store trafia do rotation.x), tylko nie pokrywa pełnej ścieżki re-execution. Druga opcja zachowania pełnego scrubTo: skopiuj scenario shape z istniejącego replayEngine.test.js (real-world setup).
  </action>
  <verify>
    <automated>npm test -- tests/PressModel.replayKinematics.test.js tests/replayEngine.test.js</automated>
  </verify>
  <done>
    - Plik `tests/PressModel.replayKinematics.test.js` istnieje
    - 5+ testów zielonych (3 axis assertions + 1 determinism + 1 integration contract)
    - Test pokrywa regresję bugu: `shaftAxis.rotation.x` (NIE `.z`), `rod.rotation.x` (NIE `.z`)
    - Test pokrywa fallback (event bez angle → resolvedAngle=0)
    - Test pokrywa determinizm A→B→A
  </done>
</task>

<task type="auto">
  <name>Task 2: Pełna walidacja phase + build sanity</name>
  <files>(none — final verification)</files>
  <action>
    1. `npm test` — full Phase 7 baseline check:
       - Phase 6 baseline: 642 (lub aktualna)
       - + Phase 7 nowe testy: anchoring (5–7 testów z Plan 07-03) + replayKinematics (5+ testów z Plan 07-04)
       - Oczekiwane: ~652+ zielone
    2. `npm run build` — bundle sukces, delta vs Phase 6 < 5KB (Phase 7 zmiany: ~20 linii PressModel + ~5 linii SceneSetup + 2 nowe testy = brak wpływu na bundle bo testy nie wchodzą).
    3. Manual smoke (final dla Phase 7) — w SUMMARY zapisz wynik:
       - `npm run dev`
       - Otwórz scenariusz, dojdź do `finishSession`, otwórz replay drawer
       - Scrub do połowy event log — sprawdź że prasa wizualnie scrubuje (flywheel rotuje wokół X, slider porusza Y-only)
       - Powrót do start, scrub do końca, ponownie scrub do połowy — pozycje deterministyczne (visual confirmation)
       - Camera z dodatniej osi X (Plan 07-01 D-Phase7-01) — flywheel widoczny jak tarcza zegara front-facing
    4. Phase 7 retrospective notes w SUMMARY:
       - Czy 642 (lub aktualna baseline) zostało preserved? (D-Phase7-04 sanity)
       - Czy żaden test nie wymagał Rule 1 deviation? (powinno być: NIE — grep w Plan 07-01 wykazał zero matchy)
       - Czy ANCHOR-03 outstanding items (panel-oburezny brackets, estop/wylacznik cable) zostały udokumentowane w 07-03 SUMMARY dla Phase 8/9?
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>
    - All tests zielone (Phase 6 baseline + Phase 7 delta)
    - Build sukces
    - SUMMARY zawiera Phase 7 retrospective + manual smoke wynik
  </done>
</task>

</tasks>

<verification>
- `npm test` — Phase 7 full suite zielona
- `ls tests/PressModel.replayKinematics.test.js` — istnieje
- `grep -n "rotation\\.x" tests/PressModel.replayKinematics.test.js` — co najmniej 3 asercje (axis correctness, regression guard)
- `grep -n "rotation\\.z" tests/PressModel.replayKinematics.test.js` — asercje że jest 0 (regression guard)
</verification>

<success_criteria>
1. KIN-03: Replay scrubTo → `_currentAngle` → `pressModel.update(angle)` ustawia `shaftAxis.rotation.x = -angle` (NIE `.z`). Test pokrywa happy path, fallback (event bez angle), determinizm.
2. Phase 6 replay flow nadal działa — angle injection Plan 06-04 kompatybilny z Plan 07-01 fix.
3. Phase 7 closed: KIN-01 (Plan 07-01) + KIN-02 (Plan 07-01) + KIN-03 (Plan 07-04) + ANCHOR-01 (Plan 07-03) + ANCHOR-02 (Plan 07-02) + ANCHOR-03 partial (Plan 07-03 documented deferred) + TEST-07 partial (Plan 07-03).
4. Wszystkie testy zielone, build sukces.
</success_criteria>

<output>
Create `.planning/phases/07-kinematic-fix-anchoring/07-04-SUMMARY.md` when done. Include:
- Liczba testów: Phase 6 baseline + Plan 07-03 delta + Plan 07-04 delta
- Phase 7 retrospective: requirements coverage table, czy żaden test nie wymagał modyfikacji
- Manual smoke replay wynik (PASS / N/A / SKIPPED)
- Recommendations dla Phase 8 (ANCHOR-03 outstanding items, camera może wymagać dalszego tuningu jeśli LabelOverlay w live billboard mode wymaga adjustment)
</output>
</content>
</invoke>