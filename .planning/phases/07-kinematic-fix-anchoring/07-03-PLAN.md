---
phase: 07-kinematic-fix-anchoring
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/PressModel.anchoring.test.js
autonomous: true
task_count: 2
requirements: [ANCHOR-01, ANCHOR-03, KIN-01, "TEST-07 (partial)"]
must_haves:
  truths:
    - "Nowy test Vitest `tests/PressModel.anchoring.test.js` iteruje po `pressModel.getInteractables()` (15 meshy) i asseruje że każdy ma `worldPosition.y >= -EPSILON` gdzie EPSILON=0.01"
    - "Test asseruje że po `pressModel.update(angle)` dla angle ∈ {0, π/4, π/2, π, 3π/2, 2π} pozycje meshy KIN-static (frame, columns, guides, panel, nameplate, switches, guards, light curtain) NIE zmieniają się — KIN-01 invariant"
    - "Test asseruje że tylko meshy będące dziećmi `shaftAxis` (kolo-zamachowe) ORAZ slider (Y-only) ORAZ rod (pivot + tilt) zmieniają world position między angle=0 a angle=π"
    - "SUMMARY zawiera `attachsTo` documentation per mesh (15 wpisów: id → 'shaftAxis' | 'this.group (rama)' | 'this.group (kolumna lewa/prawa)' | 'safetyPanel' itp.)"
  artifacts:
    - path: "tests/PressModel.anchoring.test.js"
      provides: "Vitest tests pokrywające ANCHOR-01 + KIN-01 invariants"
      contains: "anchoring"
  key_links:
    - from: "tests/PressModel.anchoring.test.js"
      to: "src/PressModel.js getInteractables()"
      via: "import + iteration"
      pattern: "getInteractables\\(\\)"
---

<objective>
Dodać unit test pokrywający ANCHOR-01 (każdy interactable mesh ma `worldPosition.y >= 0 - EPSILON`) + KIN-01 invariant (po `update(angle)` tylko shaftAxis-children + slider + rod zmieniają position; rama/kolumny/panel/etykiety statyczne). Udokumentować `attachsTo` per mesh w SUMMARY (free-text source-of-truth dla Phase 8/9 planowania osłon).

Purpose: ANCHOR-01 wymaga formalnej weryfikacji że żaden klikalny mesh nie wisi w powietrzu — automatyczne dla wszystkich 15 interactables (lista CONTEXT mówi "13" ale faktyczna implementacja ma 15 wpisów; test obejmuje wszystkie). KIN-01 invariant zabezpiecza regresję rotacji w przyszłych fazach.

Output: Nowy plik testowy `tests/PressModel.anchoring.test.js`. SUMMARY.md zawiera tabelę `attachsTo` dla 15 meshy + 2 decoration łożysk (od Plan 07-02; opcjonalne dla audit, ale wpisane dla kompletności).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kinematic-fix-anchoring/07-CONTEXT.md
@src/PressModel.js
@tests/PressModel.smoke.test.js

<interfaces>
<!-- Wzór pattern Vitest dla PressModel testów, z PressModel.smoke.test.js -->

```
// @vitest-environment jsdom

// Canvas mock wymagany — _buildNameplate woła getContext('2d'). Wzór ze smoke testu (linie 9–24).
const mock2DContext = {
  fillStyle: '', strokeStyle: '', lineWidth: 0, font: '', textBaseline: '',
  imageSmoothingEnabled: true,
  fillRect: () => {}, strokeRect: () => {}, fillText: () => {},
};
HTMLCanvasElement.prototype.getContext = function(type) {
  if (type === '2d') return mock2DContext;
  return null;
};

import { describe, it, expect, beforeEach } from 'vitest';
import * as THREE from 'three';
import { PressModel } from '../src/PressModel.js';
```

Lista 15 interactables (z PressModel.smoke.test.js EXPECTED_IDS):
kolo-zamachowe, dzwignia-sprzegla, hamulec, wziernik-smarowania, oslona-przednia,
oslona-tylna, kurtyna-lewa, kurtyna-prawa, panel-oburezny, przycisk-start-lewy,
przycisk-start-prawy, lampka-gotowosci, estop, wylacznik-glowny, tabliczka-znamionowa

attachsTo mapping (z code reading + CONTEXT linie 33–46):
- kolo-zamachowe → "shaftAxis (rotuje z wałem)" — flywheelGroup @ shaftAxis local (-2.5, 0, 0)
- dzwignia-sprzegla → "this.group (leverGroup) — pivot @ (-3, 7, 0.5) — przy lewej kolumnie"
- hamulec → "this.group — @ (2.9, 8, 0) — przy prawej kolumnie, dociska do brake disc"
- wziernik-smarowania → "this.group — @ (0, 7, 1.1) — front korpusu"
- oslona-przednia → "this.group (guardGroup) — pivot @ (0, 5, 1.5)"
- oslona-tylna → "this.group — @ (0, 4, -1.5)"
- kurtyna-lewa → "this.group — @ (-1.7, 4, 1.5)"
- kurtyna-prawa → "this.group — @ (1.7, 4, 1.5)"
- panel-oburezny (pulpit) → "this.group (safetyPanel) @ (0, 2, 2.5) — front prasy"
- przycisk-start-lewy → "safetyPanel @ local (-0.5, 0.08, 0.15) → świat (-0.5, 2.08, 2.65)"
- przycisk-start-prawy → "safetyPanel @ local (0.5, 0.08, 0.15) → świat (0.5, 2.08, 2.65)"
- lampka-gotowosci → "safetyPanel @ local (0, 0.1, -0.2) → świat (0, 2.1, 2.3)"
- estop (head) → "safetyPanel.estopGroup @ local (0, 0.05, 0) + lathe profile → świat ~(0, 2.32, 2.5)"
- wylacznik-glowny (knob) → "this.group (switchGroup) @ (3.1, 4, -0.5) — bok prawy korpusu"
- tabliczka-znamionowa → "this.group — @ (-3.05, 5.5, 0.05) — lewy bok korpusu"

Wszystkie y values > 0 (CONTEXT EPSILON=0.01; podstawa będzie y=0 w Phase 8). Najniższe interactables: panel-oburezny y=2, kurtyny y=4. Wszystkie z marginesem >>0.01.

KIN-01 static vs dynamic:
- Dynamic pod update(angle): shaftAxis children (kolo-zamachowe rim, spokes, brake disc, eccentric, eccentricPin), slider (Y-only), rod (pos + rotation.x tilt)
- Static: wszystkie inne — można sprawdzić `getWorldPosition` przed/po `update(angle)` dla ID NOT IN ['kolo-zamachowe'] i oczekiwać dx,dy,dz < EPSILON

Pre-allocated Vector3 reuse: `const v = new THREE.Vector3();` — `mesh.getWorldPosition(v)` mutuje v in-place; w pętli pamiętaj o `v.copy(...)` lub osobnym Vector3 per snapshot.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Napisz tests/PressModel.anchoring.test.js (ANCHOR-01 + KIN-01)</name>
  <files>tests/PressModel.anchoring.test.js</files>
  <behavior>
    Plik testowy zawiera następujące describe/it bloki:

    1. `describe('PressModel — ANCHOR-01: position invariants')`:
       - `it('każdy interactable mesh ma worldPosition.y >= -EPSILON (no floating elements)')` — pętla po `pressModel.getInteractables()`, dla każdego: `mesh.getWorldPosition(v)`, `expect(v.y).toBeGreaterThanOrEqual(-0.01)`. Asercja zawiera id w komunikacie błędu.
       - `it('każdy interactable mesh ma worldPosition.y > 0 (strict, podstawa będzie w Phase 8 — Phase 7 baseline)')` — strict version, najniższy obecnie to panel-oburezny y=2.

    2. `describe('PressModel — KIN-01: rotation invariants (post Phase 7 fix)')`:
       - `it('shaftAxis.rotation.x = -angle, shaftAxis.rotation.z = 0 dla angle != 0')` — `pressModel.update(Math.PI / 3)`, expect `shaftAxis.rotation.x ≈ -Math.PI/3` (tolerance 1e-9), `shaftAxis.rotation.z` ≈ 0.
       - `it('statyczne meshy (frame, columns, guides, panel, nameplate, switches) NIE zmieniają worldPosition.y/z między angle=0 a angle=π')` — snapshot worldPosition dla każdego interactable EXCEPT kolo-zamachowe; `pressModel.update(0)` → snapshot, `pressModel.update(Math.PI)` → drugi snapshot, expect distance < 1e-6 dla każdego.
       - `it('kolo-zamachowe (rim w shaftAxis) zmienia worldPosition.y między angle=0 a angle=π/2 (rotuje wokół X)')` — sanity że rim w lokalnym (0,0,0) ale spokes (wirujące belki BoxGeometry) mają world position zmienny pod rotacją shaftAxis. UWAGA: rim sam w (0,0,0) lokalnie w flywheelGroup może mieć identyczny world position; assertion focusuje na spoke[0] (sprawdź getInteractables() — flywheel PRIMARY mesh to rim, ale możemy bezpośrednio sięgnąć do `pressModel.shaftAxis.children` jeśli rim nie zmienia worldPos). Bezpieczniejsza asercja: `pressModel.update(Math.PI/2)` → expect `shaftAxis.rotation.x ≈ -π/2` (proxy dla "shaftAxis rotuje").

    3. `describe('PressModel — KIN-02: korbowód YZ tilt (post Phase 7 fix)')`:
       - `it('po update(angle != 0, π): rod.rotation.x != 0 ORAZ rod.rotation.z === 0')` — np. `pressModel.update(Math.PI / 4)`, expect `Math.abs(pressModel.rod.rotation.x) > 0`, expect `pressModel.rod.rotation.z` ≈ 0.
       - `it('slider.position.x === 0 ORAZ slider.position.z === 0 dla każdego angle (Y-only invariant)')` — pętla angle ∈ [0, π/4, π/2, 3π/4, π, 5π/4, 3π/2, 7π/4]; expect oba ≈ 0.

    4. `describe('PressModel — ANCHOR-02: decoration meshy łożyska (zależne od Plan 07-02 jeśli wave 2 ukończona)')` — OPCJONALNE, oznaczone `it.skip` jeśli `_buildBearings` jeszcze nie istnieje (sprawdź przez `pressModel.group.children.some(c => c.userData?.kind === 'decoration')`). Pełne pokrycie ANCHOR-02 będzie w Plan 07-02 SUMMARY scene-graph check; tu mamy regresję na wypadek przyszłej zmiany.

    Asercje używają `expect(value, message).toBe(...)` żeby failure pokazywało id mesha.
  </behavior>
  <action>
    Stwórz plik `tests/PressModel.anchoring.test.js` z header `// @vitest-environment jsdom` i canvas mock identycznym jak w `tests/PressModel.smoke.test.js` (linie 9–24). Wymagane importy: `vitest` (describe/it/expect/beforeEach), `three`, `../src/PressModel.js`.

    Konwencja stałych:
    - `const EPSILON = 0.01;` (CONTEXT Specifics)
    - `const NUMERIC_TOL = 1e-9;` (dla porównań kątów rotacji)
    - `const STATIC_DRIFT_TOL = 1e-6;` (dla statycznych meshy między update calls)
    - `const DYNAMIC_IDS = new Set(['kolo-zamachowe']);` — jedyny interactable który rotuje z wałem (rod/slider są w `pressModel.rod` / `pressModel.slider` ale NIE w getInteractables, więc lista static = wszystko z getInteractables MINUS kolo-zamachowe)

    Wzór dla snapshot-and-compare:
    ```
    const snapshot = new Map();
    pressModel.update(0);
    for (const [id, mesh] of pressModel.getInteractables()) {
      const v = new THREE.Vector3();
      mesh.getWorldPosition(v);
      snapshot.set(id, v.clone());  // .clone() — bo v będzie reużywany
    }
    pressModel.update(Math.PI);
    for (const [id, mesh] of pressModel.getInteractables()) {
      if (DYNAMIC_IDS.has(id)) continue;
      const v = new THREE.Vector3();
      mesh.getWorldPosition(v);
      const dist = v.distanceTo(snapshot.get(id));
      expect(dist, `${id} world position drifted: ${dist}`).toBeLessThan(STATIC_DRIFT_TOL);
    }
    ```

    BeforeEach: `scene = new THREE.Scene(); pressModel = new PressModel(scene);`

    Boundary: test importuje TYLKO vitest, three, PressModel — żadnych state/training/ui. Nie wymaga modyfikacji `tests/boundaries.test.js`.
  </action>
  <verify>
    <automated>npm test -- tests/PressModel.anchoring.test.js</automated>
  </verify>
  <done>
    - Plik `tests/PressModel.anchoring.test.js` istnieje
    - Wszystkie nowe testy zielone (5–7 testów: position invariants, KIN-01 static invariants, KIN-02 rod tilt, slider Y-only)
    - Test bazuje na rzeczywistym `getInteractables()` (15 meshy) — nie hardcoduje listy ID (loop po Map.entries)
    - Jeśli Plan 07-02 nie ukończony przed 07-03 — assertion w `describe ANCHOR-02` używa `it.skip` lub conditional skip
  </done>
</task>

<task type="auto">
  <name>Task 2: Pełna walidacja + SUMMARY attachsTo table</name>
  <files>(none — verification + documentation)</files>
  <action>
    1. `npm test` — full run; oczekiwane 642 + 5–7 nowych = 647–649 zielonych.
    2. W SUMMARY.md dodaj sekcję `## ANCHOR-01 audit table` z tabelą markdown:

    | ID | kind | worldPosition (y) | attachsTo | uwagi |
    |----|------|-------------------|-----------|-------|
    | kolo-zamachowe | visual-target | 8.0 | shaftAxis @ local (-2.5, 0, 0) | rotuje z wałem |
    | dzwignia-sprzegla | manipulation | ~7.0 | this.group (leverGroup) @ (-3, 7, 0.5) | przy lewej kolumnie |
    | hamulec | manipulation | 8.0 | this.group @ (2.9, 8, 0) | dociska brake disc |
    | wziernik-smarowania | visual-target | 7.0 | this.group @ (0, 7, 1.1) | front korpusu |
    | oslona-przednia | manipulation | ~4.1 | this.group (guardGroup pivot) @ (0, 5, 1.5), pre-translate -0.9Y | zawias u góry |
    | oslona-tylna | visual-target | 4.0 | this.group @ (0, 4, -1.5) | tył korpusu |
    | kurtyna-lewa | visual-target | 4.0 | this.group @ (-1.7, 4, 1.5) | front |
    | kurtyna-prawa | visual-target | 4.0 | this.group @ (1.7, 4, 1.5) | front |
    | panel-oburezny | visual-target | 2.0 | this.group (safetyPanel) @ (0, 2, 2.5) | wymaga widocznego wspornika w Phase 8 (ANCHOR-03) |
    | przycisk-start-lewy | manipulation | 2.08 | safetyPanel @ local (-0.5, 0.08, 0.15) | dziecko safetyPanel — anchor OK |
    | przycisk-start-prawy | manipulation | 2.08 | safetyPanel @ local (0.5, 0.08, 0.15) | dziecko safetyPanel — anchor OK |
    | lampka-gotowosci | visual-target | 2.1 | safetyPanel @ local (0, 0.1, -0.2) | dziecko safetyPanel — anchor OK |
    | estop | manipulation | ~2.32 | safetyPanel.estopGroup @ local (0, 0.05, 0) + lathe Y profile | dziecko safetyPanel; CONTEXT D-Phase7-02 nota: pełny visible cable/bracket w Phase 8/9 |
    | wylacznik-glowny | manipulation | 4.0 | this.group (switchGroup) @ (3.1, 4, -0.5) | bok prawy korpusu; CONTEXT D-Phase7-02 nota: wsporniki kablowe Phase 8/9 |
    | tabliczka-znamionowa | visual-target | 5.5 | this.group @ (-3.05, 5.5, 0.05) | lewy bok korpusu — wbudowana w korpus, OK |

    3. Sekcja `## ANCHOR-03 outstanding for Phase 8/9` w SUMMARY:
       - panel-oburezny pulpit @ (0, 2, 2.5) ma worldPosition.y >= 0 ✓ ALE wizualnie unosi się nad nieistniejącą podstawą → ANCHOR-03 wymaga wsporników/kabli (deferred do Phase 8 GEO-02/04 + Phase 9 DEC-02)
       - estop, wylacznik-glowny — analogiczna sytuacja (panel z elektroniką wymaga wizualnych kabli — Phase 9 DEC-02)
       - Status Phase 7: technicznie ANCHOR-01 spełnione (no negative-y meshes), ANCHOR-03 częściowo (lista pływających "ducks" udokumentowana, fix w Phase 8/9)

    4. Jeśli Plan 07-02 NIEUKOŃCZONY przed 07-03 (równoległe wykonanie wave 2) — odnotuj w SUMMARY że ANCHOR-02 łożyska tested w Plan 07-02 scene-graph check.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - 647+ testów zielone
    - SUMMARY zawiera tabelę 15-mesh attachsTo audit
    - SUMMARY notuje deferred ANCHOR-03 items dla Phase 8/9
  </done>
</task>

</tasks>

<verification>
- `npm test` — wszystkie testy zielone
- `ls tests/PressModel.anchoring.test.js` — istnieje
- Test count rośnie o 5–7 vs Phase 6 baseline
- SUMMARY zawiera tabelę attachsTo dla wszystkich 15 interactables
</verification>

<success_criteria>
1. ANCHOR-01: Automatyczny test asseruje y >= -EPSILON dla każdego z 15 interactables.
2. KIN-01: Automatyczny test asseruje że tylko shaftAxis-dependent meshy zmieniają world position pod update(angle).
3. KIN-02: Automatyczny test asseruje rod.rotation.x != 0, rod.rotation.z === 0, slider Y-only.
4. attachsTo udokumentowane (free-text w SUMMARY) dla wszystkich 15 meshy + 2 decoration łożysk.
5. ANCHOR-03 outstanding items zidentyfikowane (panel-oburezny + estop + wylacznik-glowny require Phase 8/9 brackets).
6. TEST-07 partial: position invariants pokrywa partial TEST-07; final TEST-07 (decorative ignored by raycaster + instancing draw count) zostaje na Phase 9.
</success_criteria>

<output>
Create `.planning/phases/07-kinematic-fix-anchoring/07-03-SUMMARY.md` when done. Include:
- Pełna tabela attachsTo (15 meshes + 2 bearings)
- Lista ANCHOR-03 deferred items dla Phase 8/9
- Test count: baseline + delta
</output>
</content>
</invoke>