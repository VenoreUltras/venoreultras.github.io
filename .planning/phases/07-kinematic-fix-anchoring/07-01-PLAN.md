---
phase: 07-kinematic-fix-anchoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PressModel.js
  - src/SceneSetup.js
autonomous: true
task_count: 3
requirements: [KIN-01, KIN-02]
must_haves:
  truths:
    - "Podczas `pressModel.update(angle)` rotuje wyłącznie `shaftAxis` group (+ jego dzieci: flywheel rim/spokes, brake disc, eccentric, eccentricPin) wokół osi X — reszta hierarchii (rama, kolumny, guidy, panel, tabliczka) jest rotacyjnie statyczna pod każdym kątem"
    - "Flywheel widziany z dodatniej osi X obraca się jak tarcza zegara front-facing (oś obrotu = oś X, spokes orbitują w płaszczyźnie YZ)"
    - "Eccentric pin pivot orbituje w płaszczyźnie YZ — `getWorldPosition(pin)` w `update(angle)` produkuje (0, shaftY + r·cos(angle), -r·sin(angle))` (z dokładnością do znaku zgodnego z `rotation.x = -angle`)"
    - "Korbowód wisi pionowo gdy angle=0; tilt liczony z `atan2(dz, -dy)` zamiast `atan2(dx, -dy)` — slider pozostaje wyłącznie Y-only"
    - "Kamera w `SceneSetup` patrzy z dodatniej osi X (default position) — flywheel/eccentric/korbowód widoczne z boku"
    - "PhysicsEngine.calculateSliderPosition(angle, r, l) sygnaturę niezmieniona, slider.position.y = shaftY - y nadal poprawne"
    - "Test PressModel.smoke.test.js (15 interactables, identity contract, material clones, dispose) pozostaje zielony bez zmian"
  artifacts:
    - path: "src/PressModel.js"
      provides: "update(angle) z rotation.x i atan2(dz, -dy); flywheel/eccentric re-oriented dla horyzontalnej osi X"
      contains: "this.shaftAxis.rotation.x"
    - path: "src/SceneSetup.js"
      provides: "kamera ustawiona na dodatniej osi X dla side-view kinematyki"
      contains: "camera.position.set"
  key_links:
    - from: "src/PressModel.js update(angle)"
      to: "src/PhysicsEngine.js calculateSliderPosition"
      via: "import + static call"
      pattern: "PhysicsEngine\\.calculateSliderPosition"
    - from: "src/PressModel.js update(angle)"
      to: "this.shaftAxis"
      via: "rotation.x assignment"
      pattern: "shaftAxis\\.rotation\\.x"
    - from: "src/PressModel.js update(angle)"
      to: "this.rod"
      via: "atan2(dz, -dy) tilt + rotation.x assignment"
      pattern: "rod\\.rotation\\.x"
---

<objective>
Naprawić bug rotacji w `PressModel.update(angle)` (linia 830) wraz z re-orientacją flywheel/eccentric i kamery na side-view (D-Phase7-01). Po fix: kręci się TYLKO `shaftAxis` i jego dzieci, kamera patrzy z dodatniej osi X, flywheel jak tarcza zegara front-facing, korbowód w YZ, slider Y-only.

Purpose: Fundament dla całej Phase 7 — Plan 07-02 (łożyska) i Plan 07-03 (anchor audit) zakładają poprawnie skonfigurowaną geometrię. Plan 07-04 (replay regression) waliduje że fix nie psuje Phase 6.

Output: Naprawione `src/PressModel.js` (update + buildPress flywheel/eccentric jeśli wymaga re-orient) + `src/SceneSetup.js` (camera position + orbit target). Wszystkie 642 testy v1.0 nadal zielone (CONTEXT D-Phase7-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kinematic-fix-anchoring/07-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/PressModel.js
@src/PhysicsEngine.js
@src/SceneSetup.js

<interfaces>
<!-- Kluczowe kontrakty których executor MUSI przestrzegać. Wyciągnięte z kodu. -->

src/PhysicsEngine.js (SIGNATURE LOCKED — D-Phase7-04):
```
static calculateSliderPosition(angle: number, r: number, l: number): number
// y = r·cos(α) + √(l² − (r·sin(α))²)
```

src/PressModel.js bieżąca implementacja (TO BE FIXED):
```
// linie 828–858
update(angle) {
  this.shaftAxis.rotation.z = -angle;     // BUG: powoduje że flywheel orbituje całym rigiem zamiast rotować wokół własnej osi
  this.eccentricPin.getWorldPosition(pinPosition);
  const currentY = PhysicsEngine.calculateSliderPosition(angle, this.r, this.l);
  this.slider.position.y = this.shaftY - currentY;
  this.slider.position.x = 0;
  this.slider.position.z = 0;
  this.rod.position.copy(pinPosition);
  const dx = this.slider.position.x - pinPosition.x;
  const dy = this.slider.position.y - pinPosition.y;
  const rodAngle = Math.atan2(dx, -dy);   // płaszczyzna XY — po fix musi być YZ: atan2(dz, -dy)
  this.rod.rotation.z = rodAngle;          // po fix: rotation.x = rodAngle
}
```

src/PressModel.js geometria (linie 95–122):
- shaftAxis: THREE.Group @ (0, shaftY=8, 0) — child this.group
- shaft cylinder: rotateZ(π/2) → wzdłuż X — POPRAWNE (oś wału = X). NIE zmieniać.
- eccentric cylinder (r+0.3, h=1): rotateZ(π/2) → wzdłuż X. NIE zmieniać.
- eccentric.position.set(0, r, 0) — w `shaftAxis`. Pod rotacją `.x` orbituje w YZ. OK.
- eccentricPin.position.set(0, r, 0). Pod rotacją `.x` orbituje w YZ. OK.

src/PressModel.js flywheel (_buildFlywheel, linie 187–231):
- flywheelGroup.position.set(-2.5, 0, 0) — w shaftAxis. OK.
- rim cylinder: rotateZ(π/2) → oś rim = X — POPRAWNE (front-facing dla side-view).
- spokes (6×): `spoke.rotation.x = (Math.PI / 6) * i` — POPRAWNE (rotacja wokół X = oś flywheel), spokes leżą w YZ jak prostokątne belki. NIE zmieniać.
- brake disc: rotateZ(π/2) → oś = X. NIE zmieniać.

src/SceneSetup.js (linie 21–32):
- camera.position.set(0, 5, 20) — front view z +Z
- controls.target.set(0, 4, 0)
- camera.lookAt(0, 0, 0)
</interfaces>

<critical-finding>
GREP `rotation.[xyz]` w tests/ → ZERO matchy assertujących konkretną oś rotacji wału/korbowodu/flywheel. PressModel.smoke.test.js sprawdza tylko identity/materials/poses/disposeContract. Application.test.js + LabelOverlay.test.js używają `update()` ale nie sprawdzają wyniku rotacji.

WNIOSEK: Klauzula Rule 1 deviation z D-Phase7-04 (single test asercji konkretnej osi) NIE TRIGGERUJE. Żaden test nie wymaga modyfikacji. 642/642 muszą zostać zielone bez żadnej zmiany w katalogu tests/.

UWAGA: PressModel.smoke.test.js asseruje `getInteractables().size === 15` (NIE 13 jak ROADMAP/CONTEXT sugeruje). Nazwa "13 interactables" w CONTEXT.md odnosi się do listy logicznej z deklaracji audytu — faktyczny licznik to 15 (kolo-zamachowe, dzwignia-sprzegla, hamulec, wziernik-smarowania, oslona-przednia, oslona-tylna, kurtyna-lewa, kurtyna-prawa, panel-oburezny, przycisk-start-lewy, przycisk-start-prawy, lampka-gotowosci, estop, wylacznik-glowny, tabliczka-znamionowa). Plan 07-03 będzie iterował po `getInteractables()` → automatycznie objął wszystkie 15.
</critical-finding>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix update(angle) — rotation.x + atan2(dz, -dy)</name>
  <files>src/PressModel.js</files>
  <behavior>
    - Po `pressModel.update(0)`: `shaftAxis.rotation.x === 0`, `shaftAxis.rotation.z === 0`, `rod.rotation.x === 0`, `rod.rotation.z === 0`
    - Po `pressModel.update(Math.PI / 2)`: `shaftAxis.rotation.x === -Math.PI/2`, `shaftAxis.rotation.z === 0` (nietknięte)
    - Po `pressModel.update(Math.PI)`: eccentricPin world position ma `y ≈ shaftY - r` i `z ≈ 0` (w obrębie tolerance 1e-9; pin orbituje w YZ)
    - Po `pressModel.update(Math.PI / 4)`: rod.position.copy(pinPosition) ustawia rod.position.z ≠ 0; rod.rotation.x = atan2(dz, -dy) gdzie dz = slider.z(=0) − pin.z, dy = slider.y − pin.y. NIE używaj `rotation.z`.
    - `slider.position.y === shaftY - PhysicsEngine.calculateSliderPosition(angle, r, l)` dla każdego angle ∈ {0, π/4, π/2, π, 3π/2}
    - `slider.position.x === 0` ORAZ `slider.position.z === 0` po każdym update (Y-only invariant)
    - PhysicsEngine.calculateSliderPosition signature niezmieniona (wywołanie bez zmian)
  </behavior>
  <action>
    Przepisać metodę `update(angle)` w `src/PressModel.js` (linie 828–858) zgodnie z D-Phase7-01 (side-view kinematics):

    1. Zmień `this.shaftAxis.rotation.z = -angle;` na `this.shaftAxis.rotation.x = -angle;`. Usuń jakąkolwiek pozostałą assignment do `rotation.z` na `shaftAxis` (defensive: nie pozwól żeby stara wartość pozostała w obiekcie po HMR — w razie wątpliwości jawnie `this.shaftAxis.rotation.z = 0;` PRZED `.x` assignment).
    2. Zachowaj reuse `this._pinPosition` Vector3 (zero GC) — `this.eccentricPin.getWorldPosition(pinPosition);` bez zmian.
    3. Wywołanie `PhysicsEngine.calculateSliderPosition(angle, this.r, this.l)` BEZ ZMIAN (signature locked, formula `y = r·cos(α) + √(l² − (r·sin(α))²)` nadal opisuje displacement wzdłuż osi przeciwnej do offset radialnego — tu od shaftY w dół osi Y).
    4. `this.slider.position.y = this.shaftY - currentY;`, `this.slider.position.x = 0;`, `this.slider.position.z = 0;` — bez zmian.
    5. `this.rod.position.copy(pinPosition);` — bez zmian (pin orbituje w YZ, rod nadąża).
    6. Zmień obliczenie tilt korbowodu z płaszczyzny XY na YZ:
       - `const dz = this.slider.position.z - pinPosition.z;` (zamiast dx)
       - `const dy = this.slider.position.y - pinPosition.y;` (bez zmian)
       - `const rodAngle = Math.atan2(dz, -dy);`
       - `this.rod.rotation.x = rodAngle;` (zamiast `.z`). Defensive reset: `this.rod.rotation.z = 0;` na początku update PRZED assignment do `.x` (ten sam powód co przy shaftAxis).
    7. Komentarze JSDoc nad metodą zaktualizuj — wyjaśnij że "wał obraca się wokół osi X (side-view); korbowód odchyla się w płaszczyźnie YZ; suwak Y-only".
    8. NIE dotykaj `_buildFlywheel()`, `_buildBrake()`, ani innych metod budujących. Bieżąca geometria (shaft rotateZ(π/2), eccentric rotateZ(π/2), rim rotateZ(π/2), spokes rotation.x = i·π/6) JEST POPRAWNA dla osi obrotu wału = X — to konsekwencja faktu, że v1.0 build już ułożył wał poziomo wzdłuż X, ale `update()` rotował błędnie wokół Z.

    Sanity post-edit: `grep -n "rotation\\.z" src/PressModel.js` powinien zwrócić tylko (a) `_buildFlywheel` jeśli istnieje (NIE — flywheel używa rotation.x na spoke), (b) `rod.rotation.z = 0;` (defensive reset jeśli dodany), (c) `_buildMainSwitch` knob.rotation.z poses (off/on — UI poses, nie kinematyka — NIE dotykać). Jeśli jakikolwiek match w `update(...)` poza defensive reset → bug.
  </action>
  <verify>
    <automated>npm test -- tests/PressModel.smoke.test.js tests/physicsEngine.test.js tests/replayEngine.test.js tests/application.test.js</automated>
  </verify>
  <done>
    - `update(angle)` używa `rotation.x` na shaftAxis i rod
    - `atan2(dz, -dy)` zamiast `atan2(dx, -dy)`
    - Slider Y-only invariant zachowany
    - PressModel.smoke.test.js (15 interactables, identity, dispose) zielony bez modyfikacji
    - PhysicsEngine signature i calls unchanged
    - `grep -n "shaftAxis\\.rotation\\.z" src/PressModel.js` zwraca co najwyżej defensive reset `= 0` (lub zero matchy)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-orient camera (SceneSetup) na side-view z +X</name>
  <files>src/SceneSetup.js</files>
  <behavior>
    - Po `new SceneSetup('container-id')`: `sceneSetup.camera.position.x > 15` ORAZ `Math.abs(sceneSetup.camera.position.z) < 5` (kamera z boku, nie z przodu jak v1.0)
    - `sceneSetup.controls.target.y === 4` lub w pobliżu (zachowanie patrzenia na środek prasy — shaftY=8 minus rama)
    - `sceneSetup.camera` nadal `THREE.PerspectiveCamera` z fov=45, near=0.1, far=1000 (signature unchanged)
    - OrbitControls min/maxDistance i maxPolarAngle bez zmian (5/60/π·0.49)
  </behavior>
  <action>
    Zmień konfigurację kamery w konstruktorze `SceneSetup` (`src/SceneSetup.js`, linie 21–22) na side-view per D-Phase7-01:

    1. `this.camera.position.set(0, 5, 20);` → `this.camera.position.set(20, 5, 0);` (kamera na dodatniej osi X, ta sama wysokość Y i ta sama odległość 20 jednostek od origin)
    2. `this.camera.lookAt(0, 0, 0);` — pozostaw bez zmian (kamera nadal patrzy w origin sceny; OrbitControls `target.set(0, 4, 0)` poniżej i tak ją przenastawi).
    3. `this.controls.target.set(0, 4, 0);` — bez zmian (target pozostaje w centrum prasy; ten Y współgra z shaftY=8 i podstawą w przyszłej Phase 8 @ y=0).
    4. Reszta konstruktora (renderer, ambientLight, dirLight @ (10,20,10), gridHelper, WebGL context-loss overlay) bez zmian — żaden z tych elementów nie zależy od osi obserwacji.

    Sanity: Po edycji dirLight pozycja (10,20,10) jest nadal sensowna z perspektywy side-view (światło z górno-prawej). NIE przenosić światła — Phase 9 zajmie się materiałami/lighting.

    Komentarz Polish (NIE w string literal — boundary scanner UI-06): nad assignment dodaj comment `// D-Phase7-01: side-view kamera — patrzymy z dodatniej osi X. Flywheel widoczny jak tarcza zegara front-facing.` (jako `//` komentarz, NIE jako string literal).
  </action>
  <verify>
    <automated>npm test -- tests/application.test.js tests/LabelOverlay.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>
    - `camera.position` ustawione na (20, 5, 0)
    - `controls.target` pozostaje (0, 4, 0)
    - Brak regresji w tests/application.test.js (36 testów — DOM fixtures + dispose chain) ani LabelOverlay.test.js (camera-facing labels — jsdom nie weryfikuje live billboard, więc bezpieczne)
    - boundaries.test.js zielony (SceneSetup imports niezmienione)
  </done>
</task>

<task type="auto">
  <name>Task 3: Pełna walidacja regresji + manual sanity</name>
  <files>(none — verification only)</files>
  <action>
    1. Pełny run testów: `npm test` (oczekiwane 642/642 zielone).
    2. Jeśli jakikolwiek test failuje → STOP. Failed test = albo (a) bug w Task 1/2, albo (b) odkryta asercja rotation której grep nie wykrył (multiline lub computed property). Diagnozuj, fixuj, NIE modyfikuj testów (D-Phase7-04 — Rule 1 deviation tylko dla pojedynczego testu asercji rotation.[xyz]).
    3. `npm run build` — bundle musi się zbudować bez błędów (TEST-08 sanity, choć rozmiar full-check będzie w Phase 9).
    4. Manual smoke (CHECKPOINT NIE wymagany — autonomous=true; deweloper sam sprawdzi):
       - `npm run dev`, otwórz http://localhost:5173/
       - Wciśnij Start → flywheel powinien rotować wokół poziomej osi X (jak tarcza zegara front-facing widziana z boku), eccentric pin orbituje w YZ, slider porusza się tylko w pionie
       - Rama, kolumny, guidy, panel oburęczny, tabliczka, kable osłon — NIE rotują razem z wałem
       - OrbitControls działają (drag rotuje kamerę dookoła prasy)
    5. Dokumentuj w SUMMARY.md czy manual smoke przeprowadzony i wynik.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>
    - `npm test` raportuje 642/642 zielone (lub aktualną liczbę v1.0 baseline)
    - `npm run build` kończy się sukcesem
    - SUMMARY.md zawiera notę o manual smoke (PASS / N/A / SKIPPED)
  </done>
</task>

</tasks>

<verification>
- `npm test` — 642/642 (lub aktualna baseline) zielone bez modyfikacji tests/
- `grep -n "rotation\\.z" src/PressModel.js` — co najwyżej defensive resets w `update()`, plus istniejące UI poses w `_buildMainSwitch`/`_buildClutchLever` (knob poses, dźwignia poses — UNCHANGED)
- `grep -n "shaftAxis\\.rotation\\.x" src/PressModel.js` — co najmniej 1 match w `update()`
- `grep -n "atan2(dz" src/PressModel.js` — dokładnie 1 match w `update()`
- `grep -n "camera.position.set" src/SceneSetup.js` — `(20, 5, 0)`
</verification>

<success_criteria>
1. KIN-01: Tylko `shaftAxis` (+ dzieci: flywheel, brake disc, eccentric, eccentricPin) + `rod` rotują podczas `update(angle)`. Reszta hierarchii (frame, columns, guides, panel, nameplate, guards, light curtain, switch) statyczna.
2. KIN-02: Korbowód `atan2(dz, -dy)` w płaszczyźnie YZ; brak "podskoków" przy przekroczeniu martwych stref (slider Y-only zachowany).
3. Wszystkie 642 testy v1.0 pozostają zielone bez modyfikacji testów (D-Phase7-04).
4. `npm run build` sukces.
5. Kamera w `SceneSetup` patrzy z dodatniej osi X (D-Phase7-01).
</success_criteria>

<output>
Create `.planning/phases/07-kinematic-fix-anchoring/07-01-SUMMARY.md` when done. Include:
- Lista plików zmodyfikowanych (PressModel.js update + SceneSetup.js camera)
- Confirmation że żaden test nie wymagał modyfikacji (Rule 1 deviation NIE użyta)
- Wynik manual smoke (jeśli przeprowadzony)
- Potencjalne ryzyko dla Plan 07-02/03/04 (np. odkryte podczas implementacji)
</output>
</content>
</invoke>