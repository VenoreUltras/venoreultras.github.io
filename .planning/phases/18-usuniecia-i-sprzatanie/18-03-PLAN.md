---
phase: 18-usuniecia-i-sprzatanie
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - src/education/AudioController.js
  - src/main.js
  - tests/AudioController.test.js
autonomous: true
requirements: [CLEAN-03]

must_haves:
  truths:
    - "AudioController nie tworzy ani nie odtwarza dźwięku HUM silnika"
    - "Alarm (square 600Hz×2 burst) działa niezależnie po usunięciu HUM"
    - "Confirm (sine 880Hz/200ms) działa niezależnie po usunięciu HUM"
    - "main.js nie wywołuje updateHum w pętli GSAP"
    - "Mute (masterGain ramp) działa bez zmian"
  artifacts:
    - path: "src/education/AudioController.js"
      provides: "Kontroler audio bez ścieżki HUM, z zachowanym alarm/confirm/mute/dispose"
      contains: "playAlarm"
    - path: "src/main.js"
      provides: "simulationTick bez wywołania updateHum"
    - path: "tests/AudioController.test.js"
      provides: "Suite audio bez testów HUM, z poprawionymi indeksami oscylatorów"
  key_links:
    - from: "src/education/AudioController.js"
      to: "AudioContext masterGain"
      via: "_getOrCreateContext (tylko ctx + masterGain, bez humOsc)"
      pattern: "_masterGain"
---

<objective>
Usunięcie ścieżki dźwięku HUM (silnik pracującej prasy) z AudioController, z zachowaniem alarmu i confirm.

Purpose: Cichszy, prostszy audio layer — znika ciągły sawtooth skalowany RPM. Dźwięki bezpieczeństwa (alarm) i UX (confirm) pozostają w pełni funkcjonalne i niezależne.
Output: AudioController bez HUM, main.js bez updateHum, testy audio zaktualizowane (suite zielony).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-usuniecia-i-sprzatanie/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Kontrakty potrzebne wykonawcy — wyekstrahowane z kodu. -->

src/education/AudioController.js — DO USUNIĘCIA:
  Stałe: HUM_FREQ_BASE, HUM_FREQ_SLOPE, HUM_RPM_THRESHOLD, HUM_RAMP_S (linie 25-34).
    ZACHOWAĆ: ALARM_*, CONFIRM_*, MUTE_RAMP_S (MUTE_RAMP_S używane przez _applyMute).
  Pola ctor: this._humOsc, this._humGain (linie 57-58). ZACHOWAĆ this._ctx, this._masterGain.
  _getOrCreateContext: blok tworzenia humOsc/humGain + start (linie 132-140). ZACHOWAĆ masterGain + resume.
  Metoda updateHum(rpmEffective) (linie 242-260) — CAŁA DO USUNIĘCIA.
  dispose: blok if (this._humOsc) { ... stop() } (linie 274-276). ZACHOWAĆ ctx.close + unsubscribery.
  JSDoc nagłówka klasy: usunąć bullet hum + opis sawtooth.
  ZACHOWAĆ: playAlarm, playConfirm, _wireSubscribers, _applyMute, _detectStepDoneTransition.

src/main.js simulationTick — DO USUNIĘCIA (linie ~476-478):
  const rpmEffective = (this._omega / (Math.PI * 2)) * 60;
  this.audioController?.updateHum(rpmEffective);
  (wraz z komentarzem D-Phase5-17)

tests/AudioController.test.js — ZMIANA INDEKSÓW (po usunięciu HUM):
  Wcześniej _getOrCreateContext tworzył humOsc jako createOscillator.mock.results[0]
  oraz humGain jako createGain.mock.results[1]. Po usunięciu HUM:
    - createGain.results[0] = masterGain (BEZ ZMIAN — Test 8 mute pozostaje poprawny)
    - createOscillator: pierwszy oscylator powstaje dopiero przy playAlarm/playConfirm
      → alarm burst = results[0],[1] (było [1],[2]); confirm = results[0] (było [1])
  Test 7a/7b/7c (updateHum) — DO USUNIĘCIA. Test 8 (mute) — BEZ ZMIAN.
  Test 9 (dispose) — usunąć asercję humOsc.stop(); zachować ctx.close() + brak nowych oscylatorów po dispose.
  Test 4 (alarm idempotent) — skorygować liczenie oscylatorów (bez +1 hum).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usuń ścieżkę HUM z AudioController.js</name>
  <read_first>
    - src/education/AudioController.js (cały plik — stałe, ctor, _getOrCreateContext, updateHum, dispose)
  </read_first>
  <action>
    W src/education/AudioController.js usuń:
    - Stałe HUM_FREQ_BASE, HUM_FREQ_SLOPE, HUM_RPM_THRESHOLD, HUM_RAMP_S (linie 25-34). ZACHOWAJ MUTE_RAMP_S (używane przez _applyMute) oraz wszystkie ALARM_* i CONFIRM_*.
    - W konstruktorze pola this._humOsc i this._humGain (linie 57-58). ZACHOWAJ this._ctx, this._masterGain.
    - W _getOrCreateContext blok tworzący humOsc/humGain + this._humOsc.start() (linie 132-140). ZACHOWAJ tworzenie masterGain, połączenie do destination i resume suspended.
    - Całą metodę updateHum (linie 242-260) wraz z JSDoc.
    - W dispose blok if (this._humOsc) { try { this._humOsc.stop() } ... } (linie 274-276). ZACHOWAJ pętlę unsubscribers oraz ctx.close().
    - W JSDoc nagłówka klasy usuń bullet „hum — ciągły sawtooth..." i wzmiankę o trzech rodzajach → zostają dwa (alarm, confirm). Stringi/komentarze polskie zachowaj.
    ZACHOWAJ pełną funkcjonalność playAlarm, playConfirm, _wireSubscribers, _applyMute, _detectStepDoneTransition.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'HUM_FREQ\|HUM_RPM\|HUM_RAMP\|updateHum\|_humOsc\|_humGain' src/education/AudioController.js)" && grep -q 'playAlarm' src/education/AudioController.js && grep -q 'playConfirm' src/education/AudioController.js && grep -q 'MUTE_RAMP_S' src/education/AudioController.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "HUM_FREQ\|HUM_RPM\|HUM_RAMP\|updateHum\|_humOsc\|_humGain\|sawtooth" src/education/AudioController.js` zwraca 0 wyników
    - `grep -c "ALARM_\|CONFIRM_\|MUTE_RAMP_S" src/education/AudioController.js` ≥ 1 (alarm/confirm/mute zachowane)
    - `grep -c "playAlarm\|playConfirm\|_applyMute" src/education/AudioController.js` ≥ 3
  </acceptance_criteria>
  <done>HUM usunięty z AudioController (stałe, pola, oscylator, updateHum, dispose), alarm/confirm/mute/dispose zachowane.</done>
</task>

<task type="auto">
  <name>Task 2: Usuń wywołanie updateHum z main.js i zaktualizuj testy audio</name>
  <read_first>
    - src/main.js (simulationTick, linie ~476-478 — rpmEffective + updateHum)
    - tests/AudioController.test.js (cały plik — indeksy oscylatorów, Test 7a/7b/7c, Test 8, Test 9, Test 4)
  </read_first>
  <action>
    W src/main.js simulationTick usuń linie obliczenia rpmEffective i wywołania this.audioController?.updateHum(rpmEffective) wraz z komentarzem D-Phase5-17 (linie ~476-478). Reszta tick loop bez zmian.

    W tests/AudioController.test.js:
    - Usuń testy HUM: Test 7a (updateHum bez ctx), Test 7b (updateHum rpm<5), Test 7c (updateHum rpm=50) — całe bloki it(...). ZACHOWAJ Test 8 (mute) — używa createGain.results[0]=masterGain, indeks bez zmian.
    - Skoryguj indeksy oscylatorów w testach alarm i confirm: po usunięciu humOsc z _getOrCreateContext pierwszy oscylator powstaje dopiero w playAlarm/playConfirm. Alarm burst: createOscillator.results[0] i [1] (było [1],[2]). Confirm: createOscillator.results[0] (było [1]). Zaktualizuj wszystkie odwołania mock.results[...] i komentarze opisujące kolejność.
    - Test 4 (alarm idempotent): skoryguj liczenie oscylatorów usuwając zakładane +1 dla humOsc (np. pierwszy alarm = 2 oscylatory zamiast 3).
    - Test 9 (dispose): usuń asercję humOsc.stop() i odwołanie do humOsc; ZACHOWAJ asercję ctx.close() oraz sprawdzenie, że po dispose setState nie tworzy nowych oscylatorów.
    - Zaktualizuj komentarz nagłówka pliku (linia ~4) usuwając „hum RPM scaling".
  </action>
  <verify>
    <automated>test -z "$(grep -n 'updateHum' src/main.js)" && test -z "$(grep -n 'updateHum\|humOsc\|humGain\|hum RPM\|sawtooth' tests/AudioController.test.js)" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "updateHum" src/main.js` zwraca 0 wyników
    - `grep -n "updateHum\|humOsc\|humGain\|hum RPM\|sawtooth" tests/AudioController.test.js` zwraca 0 wyników
    - `npm test` kończy się zielono (exit 0) — w szczególności AudioController.test.js (alarm, confirm, mute, dispose) i application.test.js
  </acceptance_criteria>
  <done>Wywołanie updateHum usunięte z main.js; testy audio bez testów HUM i z poprawionymi indeksami oscylatorów; alarm/confirm/mute/dispose zielone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store ↔ AudioController | DI store (subscribeWithSelector); usunięcie HUM nie dotyka subskrypcji alarm/confirm/mute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Denial of Service | dispose chain — martwa referencja humOsc | mitigate | Usunięcie humOsc z dispose; gate application.test.js P9 dispose order + AudioController Test 9 |
| T-18-04 | Tampering | regresja alarm/confirm po usunięciu HUM | mitigate | Zachowanie playAlarm/playConfirm + indeksy testów skorygowane; gate `npm test` |
</threat_model>

<verification>
- `grep -rn "HUM\|updateHum\|_humOsc\|sawtooth" src/education/AudioController.js src/main.js` → 0 wyników
- `npm test` zielony (alarm, confirm, mute, dispose)
- Manual: alarm odtwarza się przy przejściu do 'awaria', confirm przy oznaczeniu kroku 'done', brak ciągłego dźwięku silnika przy obrotach
</verification>

<success_criteria>
- CLEAN-03: HUM usunięty z AudioController i main.js
- Alarm i confirm w pełni funkcjonalne i niezależne od HUM
- Dispose chain bez martwych referencji; suite zielony
</success_criteria>

<output>
Create `.planning/phases/18-usuniecia-i-sprzatanie/18-03-SUMMARY.md` when done
</output>
