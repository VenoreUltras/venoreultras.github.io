---
phase: 18-usuniecia-i-sprzatanie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - src/ui/SessionOverlay.js
  - src/export/PdfExporter.js
  - src/export/JsonExporter.js
  - package.json
  - package-lock.json
  - public/fonts/NotoSans-Regular.ttf
  - tests/pdfExporter.test.js
  - tests/jsonExporter.test.js
  - tests/boundaries.test.js
  - tests/sessionOverlay.test.js
autonomous: true
requirements: [CLEAN-01, EXAM-06]

must_haves:
  truths:
    - "Aplikacja buduje się i działa bez kodu eksportu PDF/JSON"
    - "SessionOverlay nie pokazuje przycisków Eksportuj JSON / Eksportuj PDF — wynik prezentowany wyłącznie na ekranie"
    - "Brak referencji do jspdf, html2canvas, NotoSans i FONT_URL w katalogu src/"
    - "Suite testów pozostaje zielony po usunięciu/aktualizacji testów eksportu"
  artifacts:
    - path: "src/ui/SessionOverlay.js"
      provides: "Modal wyniku bez przycisków i handlerów eksportu"
      contains: "session-overlay__replay-btn"
    - path: "src/main.js"
      provides: "Wiring SessionOverlay bez DI eksporterów"
      contains: "new SessionOverlay"
    - path: "package.json"
      provides: "Brak zależności jspdf"
  key_links:
    - from: "src/main.js"
      to: "src/ui/SessionOverlay.js"
      via: "new SessionOverlay({ store, scenarios, computeMetrics })"
      pattern: "new SessionOverlay"
    - from: "src/ui/SessionOverlay.js"
      to: "pl.pdf"
      via: "on-screen errors table (ZACHOWANE)"
      pattern: "pl\\.pdf\\."
---

<objective>
Całkowite usunięcie ścieżki eksportu wyników (PDF + JSON) oraz domknięcie EXAM-06 (wynik egzaminu wyłącznie na ekranie).

Purpose: Odchudzenie bundla (usunięcie jspdf + transitive html2canvas) i uproszczenie SessionOverlay do prezentacji screen-only. Inwalidacja EXAM-04 (wynik w PDF/JSON) z v1.2.
Output: Skasowane moduły eksportu, SessionOverlay bez przycisków eksportu, brak zależności jspdf, usunięty plik czcionki PDF, zaktualizowane/usunięte testy eksportu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-usuniecia-i-sprzatanie/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Kontrakty potrzebne wykonawcy — wyekstrahowane z kodu. Bez eksploracji repo. -->

SessionOverlay konstruktor (src/ui/SessionOverlay.js) — OBECNY:
  constructor({ store, scenarios, computeMetrics, jsonExporter, pdfExporter, rootElementId })
  → DOCELOWY: constructor({ store, scenarios, computeMetrics, rootElementId })

main.js — OBECNE importy eksportu (linie 41-46) DO USUNIĘCIA:
  import { buildJsonPayload, downloadJson, generateFilename as jsonFilename } from './export/JsonExporter.js';
  import { downloadPdf, generateFilename as pdfFilename } from './export/PdfExporter.js';
  ZACHOWAĆ: import { computeMetrics } from './training/ScoringService.js'; (linia 40 — używany przez SessionOverlay DI)

main.js — OBECNY wiring SessionOverlay (linie 396-402):
  new SessionOverlay({ store, scenarios, computeMetrics,
    jsonExporter: { build: buildJsonPayload, download: downloadJson, generateFilename: jsonFilename },
    pdfExporter:  { download: downloadPdf, generateFilename: pdfFilename } })
  → DOCELOWY: new SessionOverlay({ store: this.store, scenarios: allScenarios, computeMetrics })

KRYTYCZNE — ZACHOWAĆ: pl.pdf.* (colTime/colStep/colSeverity/severityCritical/severityMedium/severityMinor)
jest nadal używane przez SessionOverlay._render do tabeli błędów NA EKRANIE (linie ~216-231).
NIE usuwać bloku pl.pdf z src/i18n/pl.js. Klucze pl.overlay.exportJson/exportPdf stają się
osierocone, ale pozostają (zero zmian w i18n.pl.test.js — minimalizacja churnu testów).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usuń kod eksportu z modeli i wiringu</name>
  <read_first>
    - src/export/PdfExporter.js (moduł do skasowania)
    - src/export/JsonExporter.js (moduł do skasowania)
    - src/ui/SessionOverlay.js (modyfikowany — przyciski + handlery eksportu)
    - src/main.js (modyfikowany — importy + DI eksporterów; linie 40-46, 396-402)
  </read_first>
  <action>
    Skasuj pliki src/export/PdfExporter.js i src/export/JsonExporter.js (po usunięciu katalog src/export/ powinien być pusty — usuń katalog jeśli pusty).

    W src/main.js: usuń dwa importy eksportu (JsonExporter: buildJsonPayload/downloadJson/jsonFilename; PdfExporter: downloadPdf/pdfFilename) — linie 41-46. ZACHOWAJ import computeMetrics z ScoringService.js (linia 40, używany przez SessionOverlay). W wiringu new SessionOverlay (linie 396-402) usuń właściwości jsonExporter i pdfExporter, pozostawiając { store, scenarios, computeMetrics }.

    W src/ui/SessionOverlay.js: usuń z konstruktora parametry jsonExporter, pdfExporter oraz pola this._jsonExporter, this._pdfExporter, this._pdfLoading. W _build() usuń z innerHTML dwa przyciski (.session-overlay__export-json-btn, .session-overlay__export-pdf-btn), ich querySelector refy (_exportJsonBtn/_exportPdfBtn), przypisania textContent (pl.overlay.exportJson/exportPdf), oraz handlery _onExportJson i _onExportPdf wraz z addEventListener. W dispose() usuń removeEventListener dla obu przycisków eksportu. ZACHOWAJ przyciski replay i retry, ZACHOWAJ this._computeMetrics oraz całą tabelę błędów używającą pl.pdf.* (to renderowanie NA EKRANIE, nie eksport). Aktualizuj JSDoc nagłówka klasy usuwając wzmianki o JsonExporter/PdfExporter DI. Discretion: pozostały blok .session-overlay__actions ma teraz tylko replay+retry — nie zmieniaj CSS w tej fazie, layout pozostaje funkcjonalny.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'PdfExporter\|JsonExporter' src/ 2>/dev/null)" && grep -q 'pl\.pdf\.' src/ui/SessionOverlay.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "PdfExporter\|JsonExporter" src/` zwraca 0 wyników
    - `ls src/export/ 2>/dev/null` — katalog nie istnieje lub jest pusty
    - `grep -n "export-json-btn\|export-pdf-btn\|_onExportJson\|_onExportPdf" src/ui/SessionOverlay.js` zwraca 0 wyników
    - `grep -c "pl\.pdf\." src/ui/SessionOverlay.js` ≥ 1 (tabela błędów na ekranie zachowana)
    - `grep -n "new SessionOverlay" src/main.js` pokazuje wiring bez jsonExporter/pdfExporter
  </acceptance_criteria>
  <done>Moduły eksportu skasowane, SessionOverlay i main.js bez referencji do eksporterów, tabela błędów na ekranie (pl.pdf.*) nietknięta.</done>
</task>

<task type="auto">
  <name>Task 2: Usuń zależność jspdf, plik czcionki PDF i potwierdź brak referencji (EXAM-06)</name>
  <read_first>
    - package.json (dependencies — jspdf linia 23)
    - public/fonts/NotoSans-Regular.ttf (plik do usunięcia — był używany tylko przez PdfExporter)
  </read_first>
  <action>
    Usuń zależność jspdf przez npm CLI: uruchom `npm uninstall jspdf`. html2canvas jest transitive (optionalDependency jspdf) — zniknie z package-lock.json automatycznie wraz z jspdf. Usuń plik public/fonts/NotoSans-Regular.ttf (czcionka osadzana w PDF, referencja FONT_URL=/fonts/NotoSans-Regular.ttf żyła wyłącznie w skasowanym PdfExporter — Discretion CONTEXT: zalecane usunięcie skoro brak referencji). Jeśli katalog public/fonts/ pozostaje pusty, usuń katalog.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'jspdf\|html2canvas' package.json src/ 2>/dev/null)" && test ! -f public/fonts/NotoSans-Regular.ttf && test -z "$(grep -rn 'NotoSans\|FONT_URL' src/ 2>/dev/null)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "jspdf\|html2canvas" package.json src/` zwraca 0 wyników
    - `grep -n "html2canvas\|jspdf" package-lock.json` zwraca 0 wyników (lub tylko brak wpisów node_modules)
    - `test -f public/fonts/NotoSans-Regular.ttf` zwraca false (plik nie istnieje)
    - `grep -rn "NotoSans\|FONT_URL" src/` zwraca 0 wyników (kryterium EXAM-06 / TEST-12)
  </acceptance_criteria>
  <done>jspdf i transitive html2canvas usunięte z package.json + package-lock.json, plik czcionki skasowany, zero referencji do NotoSans/FONT_URL w src/.</done>
</task>

<task type="auto">
  <name>Task 3: Zaktualizuj testy eksportu — suite zielony</name>
  <read_first>
    - tests/pdfExporter.test.js (do skasowania)
    - tests/jsonExporter.test.js (do skasowania)
    - tests/boundaries.test.js (wpisy JsonExporter/PdfExporter linie ~84-98)
    - tests/sessionOverlay.test.js (helper makeOverlay + testy przycisków eksportu)
  </read_first>
  <action>
    Skasuj tests/pdfExporter.test.js i tests/jsonExporter.test.js (testują skasowane moduły).

    W tests/boundaries.test.js usuń wpisy boundary dla 'src/export/JsonExporter.js' i 'src/export/PdfExporter.js' (pliki nie istnieją — sprawdzenie boundary próbowałoby je czytać). ZACHOWAJ wpis dla src/ui/SessionOverlay.js (jego mustNotImport listujący '../export/' pozostaje poprawny).

    W tests/sessionOverlay.test.js: usuń z helpera makeOverlay definicje jsonExporter i pdfExporter oraz ich przekazanie do konstruktora (linie ~29-46). Usuń dwa testy przycisków eksportu: '"Eksportuj JSON" click ...' i '"Eksportuj PDF" click async ...' (querySelector tych przycisków zwróci null → throw). W teście walidacji konstruktora (~264-269) usuń przekazywanie jsonExporter/pdfExporter. ZACHOWAJ testy replay/retry/close/render/errors table.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/pdfExporter.test.js` false, `test -f tests/jsonExporter.test.js` false
    - `grep -n "export/JsonExporter\|export/PdfExporter" tests/boundaries.test.js` zwraca 0 wyników
    - `grep -n "export-json-btn\|export-pdf-btn\|jsonExporter\|pdfExporter" tests/sessionOverlay.test.js` zwraca 0 wyników
    - `npm test` kończy się zielono (exit 0)
  </acceptance_criteria>
  <done>Testy skasowanych modułów usunięte, boundaries i sessionOverlay zaktualizowane, pełny suite zielony.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm dependency tree | Usuwanie jspdf + transitive html2canvas (redukcja supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | package-lock.json po `npm uninstall` | accept | uninstall (nie install) — brak nowych pakietów; lock kurczy się deterministycznie, weryfikacja grep=0 |
| T-18-SC | Tampering | npm installs | accept | Faza wyłącznie USUWA zależności; brak install → brak nowego kodu pakietowego do audytu |
</threat_model>

<verification>
- `grep -rn "PdfExporter\|JsonExporter\|jspdf\|html2canvas\|NotoSans\|FONT_URL" src/ package.json` → 0 wyników
- `npm test` zielony
- SessionOverlay renderuje wynik + tabelę błędów na ekranie (manual: otwórz overlay, brak przycisków eksportu, widoczne replay/retry)
</verification>

<success_criteria>
- CLEAN-01: eksport PDF/JSON całkowicie usunięty (kod + zależności + przyciski)
- EXAM-06: SessionOverlay screen-only, zero referencji /fonts/NotoSans w src/
- Suite testów zielony po aktualizacji
</success_criteria>

<output>
Create `.planning/phases/18-usuniecia-i-sprzatanie/18-01-SUMMARY.md` when done
</output>
