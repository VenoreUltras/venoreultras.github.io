---
phase: 18-usuniecia-i-sprzatanie
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - index.html
  - src/UI.js
  - src/main.js
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "Panel Parametry Układu nie istnieje w index.html"
    - "DOM nie zawiera elementów #val-angle ani #val-displacement"
    - "UI nie zapisuje już telemetrii kąta/wychylenia do DOM"
    - "Pętla GSAP integruje kąt i renderuje scenę bez regresji kinematycznej (wychylenie nadal liczone poprawnie)"
  artifacts:
    - path: "index.html"
      provides: "Layout bez bloku info-panel"
    - path: "src/UI.js"
      provides: "UI bez referencji do val-angle/val-displacement i bez DOM-writes telemetrii"
    - path: "src/main.js"
      provides: "simulationTick bez wywołań updateTelemetry, z zachowaną integracją kąta i obliczeniem wychylenia"
  key_links:
    - from: "src/main.js"
      to: "src/PressModel.js"
      via: "pressModel.update(currentAngle) — kinematyka NIETKNIĘTA"
      pattern: "pressModel\\.update"
---

<objective>
Usunięcie panelu „Parametry Układu" (#info-panel) i martwej prezentacji telemetrii (val-angle/val-displacement).

Purpose: Czystszy interfejs — znika nieużywany panel parametrów. Znika WYŁĄCZNIE prezentacja telemetrii; kinematyka (integracja kąta, ruch modelu prasy, obliczenie wychylenia) pozostaje nietknięta.
Output: index.html bez info-panel, UI.js bez DOM-writes telemetrii, main.js bez wywołań updateTelemetry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-usuniecia-i-sprzatanie/18-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Kontrakty potrzebne wykonawcy — wyekstrahowane z kodu. -->

index.html — blok DO USUNIĘCIA (linie ~51-62):
  <div class="info-panel glass-panel"> ... <h2>Parametry Układu</h2> ... </div>
  (zawiera #val-angle i #val-displacement oraz equation-box ze wzorem kinematycznym)

src/UI.js — this.elements (linie 29-30): valAngle, valDisplacement DO USUNIĘCIA.
  Metoda updateTelemetry(angleRad, displacement) (linie 69-76) — DO USUNIĘCIA (DOM-writes).
  ZACHOWAĆ: statusDot, statusText, speedSlider, speedValue, btnToggle oraz metodę updateStatus.

src/main.js simulationTick — DWA wywołania this.ui.updateTelemetry DO USUNIĘCIA:
  - linia ~429 (gałąź replay): po pressModel.update(replayAngle) + obliczeniu displacement
  - linia ~474 (gałąź normalna): po obliczeniu displacement
  INWARIANT (planning_context): "kąt/wychylenie liczone poprawnie" — ZACHOWAĆ integrację
  currentAngle, pressModel.update(...) ORAZ obliczenie const displacement = PhysicsEngine.calculateSliderPosition(...).
  Usunąć TYLKO linie this.ui.updateTelemetry(...). PhysicsEngine import pozostaje używany.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usuń panel Parametry Układu z index.html i telemetrię z UI.js</name>
  <read_first>
    - index.html (blok info-panel, linie ~51-62)
    - src/UI.js (this.elements + updateTelemetry, linie 18-76)
  </read_first>
  <action>
    W index.html usuń cały blok `<div class="info-panel glass-panel"> ... </div>` zawierający nagłówek „Parametry Układu", listę params-list (skok suwaka, długość korbowodu, #val-angle, #val-displacement) oraz equation-box ze wzorem kinematycznym (linie ~51-62). ZACHOWAJ control-panel powyżej i #step-panel poniżej.

    W src/UI.js: usuń z this.elements pola valAngle i valDisplacement (linie 29-30). Usuń całą metodę updateTelemetry (linie 64-76) wraz z jej JSDoc. ZACHOWAJ pola statusDot/statusText/speedSlider/speedValue/btnToggle, konstruktor, bindEvents, getAngularVelocity oraz updateStatus (te zostają — to ortogonalny kanał statusu, nie telemetria parametrów). Stringi polskie nietknięte.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'info-panel\|Parametry Układu\|val-angle\|val-displacement' index.html)" && test -z "$(grep -n 'valAngle\|valDisplacement\|updateTelemetry' src/UI.js)" && grep -q 'updateStatus' src/UI.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "info-panel\|Parametry Układu\|val-angle\|val-displacement" index.html` zwraca 0 wyników
    - `grep -n "valAngle\|valDisplacement\|updateTelemetry" src/UI.js` zwraca 0 wyników
    - `grep -c "updateStatus" src/UI.js` ≥ 1 (kanał statusu zachowany)
    - control-panel oraz #step-panel nadal obecne w index.html
  </acceptance_criteria>
  <done>Blok info-panel usunięty z index.html, UI.js bez referencji telemetrii i bez metody updateTelemetry, updateStatus zachowany.</done>
</task>

<task type="auto">
  <name>Task 2: Usuń wywołania updateTelemetry z pętli GSAP (zachowaj kinematykę)</name>
  <read_first>
    - src/main.js (simulationTick, linie 414-479; gałąź replay ~422-431 i normalna ~457-479)
    - src/UI.js (potwierdzenie że updateTelemetry już nie istnieje — Task 1)
  </read_first>
  <action>
    W src/main.js simulationTick usuń OBA wywołania this.ui.updateTelemetry(...): jedno w gałęzi replay (po pressModel.update(replayAngle) + obliczeniu displacement, ~linia 429) i jedno w gałęzi normalnej (~linia 474).

    INWARIANT — NIE usuwaj: integracji currentAngle (currentAngle += _omega * dtSeconds), state.setCurrentAngle, pressModel.update(...), ani obliczeń const displacement = PhysicsEngine.calculateSliderPosition(...) w obu gałęziach (planning_context: „kąt/wychylenie liczone poprawnie" — obliczenie wychylenia pozostaje jako guard kinematyczny; import PhysicsEngine nadal używany). Po usunięciu wywołań updateTelemetry zmienna displacement pozostaje obliczana — dodaj krótki komentarz po polsku przy obu obliczeniach, że wychylenie liczone jest na potrzeby zachowania inwariantu kinematycznego (prezentacja telemetrii usunięta w Fazie 18).
  </action>
  <verify>
    <automated>test -z "$(grep -n 'updateTelemetry' src/main.js)" && grep -q 'pressModel.update' src/main.js && grep -q 'calculateSliderPosition' src/main.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "updateTelemetry" src/main.js` zwraca 0 wyników
    - `grep -c "pressModel.update" src/main.js` ≥ 2 (kinematyka w obu gałęziach zachowana)
    - `grep -c "calculateSliderPosition" src/main.js` ≥ 2 (obliczenie wychylenia zachowane)
    - `npm test` kończy się zielono (exit 0) — w szczególności application.test.js i statusIndicator.test.js
  </acceptance_criteria>
  <done>Wywołania updateTelemetry usunięte z obu gałęzi simulationTick; integracja kąta, ruch modelu i obliczenie wychylenia zachowane; suite zielony.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI ↔ engine | UI.js (DOM) ↔ main.js tick loop; usunięcie kanału prezentacji telemetrii nie tworzy nowych granic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Denial of Service | simulationTick regresja kinematyczna | mitigate | Zachowanie integracji kąta + pressModel.update + obliczenia wychylenia; gate `npm test` (application/statusIndicator) |
</threat_model>

<verification>
- `grep -rn "info-panel\|val-angle\|val-displacement\|updateTelemetry\|Parametry" src/ index.html` → 0 wyników
- `npm test` zielony
- Manual: scena animuje się, suwak prasy porusza się przy Start (kinematyka bez regresji), brak panelu Parametry Układu w UI
</verification>

<success_criteria>
- CLEAN-02: panel Parametry Układu usunięty, martwa telemetria val-angle/val-displacement wycięta
- Pętla GSAP bez regresji — kąt i wychylenie liczone poprawnie, ruch modelu zachowany
- Suite zielony
</success_criteria>

<output>
Create `.planning/phases/18-usuniecia-i-sprzatanie/18-02-SUMMARY.md` when done
</output>
