---
phase: 16-media-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["16-01", "16-03"]
files_modified:
  - src/data/elementInfo.js
  - src/ui/ElementInfoOverlay.js
  - src/main.js
  - tests/ElementInfoOverlay.test.js
autonomous: true
requirements: [MED-03]
user_setup: []

must_haves:
  truths:
    - "Opening an element whose entry.media has entries renders an <img> (loading=lazy, alt from media[].alt) sourced via MediaManager.resolveSrc"
    - "img.onerror removes the broken <img> silently — no console.error, alt-text/content remain (graceful 404/offline degradation)"
    - "Opening an element with empty media still shows pl.modals.elementInfo.mediaPlaceholder (Test 2d preserved, switched to a still-empty entry)"
    - "main.js instantiates MediaManager and passes it to ElementInfoOverlay; dispose chain releases it"
  artifacts:
    - path: "src/ui/ElementInfoOverlay.js"
      provides: "media slot <img> render with onerror fallback + optional mediaManager DI"
      contains: "createElement('img')"
    - path: "src/data/elementInfo.js"
      provides: "2 representative entries (kolo-zamachowe, hamulec) with media[]; other 13 keep media:[]"
      contains: "kolo-zamachowe-placeholder.webp"
    - path: "src/main.js"
      provides: "MediaManager instantiation + DI into ElementInfoOverlay + dispose"
      contains: "new MediaManager"
    - path: "tests/ElementInfoOverlay.test.js"
      provides: "img render + onerror tests; Test 2d switched to empty-media entry"
      contains: "Event('error')"
  key_links:
    - from: "src/ui/ElementInfoOverlay.js"
      to: "this._mediaManager.resolveSrc"
      via: "media slot render reads resolveSrc, falls back to '/media/'+src when null"
      pattern: "resolveSrc"
    - from: "src/main.js"
      to: "ElementInfoOverlay"
      via: "mediaManager passed in constructor DI"
      pattern: "mediaManager"
    - from: "src/data/elementInfo.js"
      to: "public/media/kolo-zamachowe-placeholder.webp"
      via: "media[].src filename matches generated asset (16-03)"
      pattern: "kolo-zamachowe-placeholder.webp"
---

<objective>
Wire the media pipeline end-to-end through the overlay: extend ElementInfoOverlay._render() to render real <img> elements for populated entry.media with graceful onerror degradation, populate 2 representative elementInfo entries with placeholder media, add MediaManager DI in main.js, and update the overlay tests (including the Test 2d regression switch). Close the phase with the full-suite + bundle gate.

Purpose: Delivers MED-03 (overlay renders media and gracefully degrades on 404/offline with zero JS console error; placeholder branch preserved). Depends on 16-01 (MediaManager) for the DI wiring and on 16-03 for the asset filenames the populated entries reference and the bundle gate.
Output: modified src/ui/ElementInfoOverlay.js, src/data/elementInfo.js, src/main.js, tests/ElementInfoOverlay.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-media-pipeline/16-RESEARCH.md
@.planning/phases/16-media-pipeline/16-PATTERNS.md

<interfaces>
MediaManager (from 16-01) — consumed via DI:
- new MediaManager()            (no args needed; default fetchImpl = globalThis.fetch)
- resolveSrc(filename) -> string '/media/<filename>'
- dispose() -> void (no-op)

ElementInfoOverlay current shape (src/ui/ElementInfoOverlay.js):
- constructor({ store, rootElementId='modal-container', lectorService=null })  line 24  — ADD mediaManager=null param
- media slot injection point: lines 258-265, the `if (mediaEl) { if (!entry?.media?.length) {textContent=mediaPlaceholder} else {textContent=''} }` block — REPLACE with img render
- _render() runs on 5 subscriptions — MUST clear mediaEl each render (Pitfall 3)
- empty-media branch MUST keep `mediaEl.textContent = pl.modals.elementInfo.mediaPlaceholder` (Test 2d, tests/ElementInfoOverlay.test.js line 158)

elementInfo.js (src/data/elementInfo.js) — keys + media lines:
- 'kolo-zamachowe' (line 14, media: [] at line 21)  -> populate
- 'hamulec' (line 23, media: [] at line 30)          -> populate
- 'oslona-przednia' (line 122) — keeps media: [] (use as Test 2d empty-media entry)
- Polish strings ALLOWED in src/data/ (boundaries ALLOWED_PATHS)
- media[] shape: [{ src: 'plik.webp', alt: 'opis PL', caption?: 'PL' }]

main.js wiring (src/main.js):
- LectorService instantiation analog: line ~309 (this.lectorService = new LectorService({ store }))
- ElementInfoOverlay instantiation: lines 351-354 (currently { store, lectorService })
- dispose chain: lines 499-504 (elementInfoOverlay.dispose() then lectorService.dispose())

Test analogs (tests/ElementInfoOverlay.test.js):
- Test 2d describe block: lines 141-160 (opens 'kolo-zamachowe', asserts mediaPlaceholder) — MUST switch entry to 'oslona-przednia'
- onerror test pattern: dispatch new Event('error'), assert img removed (16-RESEARCH.md lines ~456-462)
- fake mediaManager for tests: { resolveSrc: (f) => '/media/' + f } (no real import needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overlay <img> render + onerror degradation + elementInfo media population</name>
  <files>src/ui/ElementInfoOverlay.js, src/data/elementInfo.js, tests/ElementInfoOverlay.test.js</files>
  <read_first>
    - src/ui/ElementInfoOverlay.js (constructor line 24; media slot injection lines 258-265; lectorService null-guard pattern lines ~160-212 for optional-service handling)
    - src/data/elementInfo.js (kolo-zamachowe entry lines 14-22; hamulec lines 23-31; oslona-przednia line 122)
    - tests/ElementInfoOverlay.test.js (Test 2d block lines 141-160 — the entry to switch; describe/beforeEach/afterEach shell)
    - .planning/phases/16-media-pipeline/16-PATTERNS.md (replacement render block lines ~143-169; Test 2d regression note lines ~270-277; Test 2e + onerror test lines ~452-468)
    - .planning/phases/16-media-pipeline/16-RESEARCH.md (Pattern 2 render + Pitfall 3/4/7)
  </read_first>
  <action>
    In src/ui/ElementInfoOverlay.js: (a) add optional `mediaManager = null` to the destructured constructor params (line 24, after lectorService), assign `this._mediaManager = mediaManager`. Backward-compatible — existing tests pass no mediaManager and get null. (b) Replace the media slot block at lines 258-265 with the render-with-onerror logic. Start by clearing: `mediaEl.textContent = ''` (Pitfall 3 — _render runs on 5 subscriptions). If `!entry?.media?.length`, set `mediaEl.textContent = pl.modals.elementInfo.mediaPlaceholder` (Test 2d MUST stay green). Else, for each media item: compute `src` = `this._mediaManager ? this._mediaManager.resolveSrc(item.src) : '/media/' + item.src` (null fallback for existing tests); `const img = document.createElement('img')`; set `img.alt = item.alt ?? ''`, `img.loading = 'lazy'`, `img.className = 'element-info-overlay__media-img'`; attach `img.onerror = () => { img.remove(); }` BEFORE assigning `img.src = src` (Pitfall 7); the onerror MUST be silent — NO console.error/throw (MED-03); append img to mediaEl; if `item.caption`, append a `<p class="element-info-overlay__media-caption">` with textContent = caption. Build the img with createElement only — never innerHTML injection of src/alt.

    In src/data/elementInfo.js: populate exactly two entries (Claude's-discretion set per CONTEXT, recommended by research): 'kolo-zamachowe' media -> `[{ src: 'kolo-zamachowe-placeholder.webp', alt: 'Koło zamachowe prasy PM-300 — placeholder' }]`; 'hamulec' media -> `[{ src: 'hamulec-placeholder.webp', alt: 'Hamulec sprzęgłowo-hamulcowy PM-300 — placeholder' }]`. The src filenames MUST exactly match the assets generated in 16-03. ALL other 13 entries keep `media: []` unchanged — especially 'oslona-przednia' (reused as the Test 2d empty-media entry).

    In tests/ElementInfoOverlay.test.js: (a) Test 2d (lines 141-160) currently opens 'kolo-zamachowe' (now populated) — switch its `openElementInfo('kolo-zamachowe')` to `openElementInfo('oslona-przednia')` (still media:[]) so the mediaPlaceholder assertion stays valid. (b) Add a new describe block ('Test 2e — slot mediów z MediaManager') that constructs `new ElementInfoOverlay({ store, mediaManager })` with a fake `mediaManager = { resolveSrc: (f) => '/media/' + f }`, sets mode 'nauka', opens 'kolo-zamachowe', and asserts: an `<img>` exists under `.element-info-overlay__media`, its src contains '/media/', and `getAttribute('loading')` === 'lazy'. (c) Add an onerror test: query the img, dispatch `new Event('error')` on it, assert the img is removed from the DOM and no exception thrown.
  </action>
  <behavior>
    - Open entry with media[] -> <img> rendered with src containing '/media/', alt set, loading='lazy'
    - img.onerror (dispatch Event('error')) -> img removed from DOM, no console.error, no throw
    - Open entry with media:[] (oslona-przednia) -> mediaEl.textContent contains mediaPlaceholder (Test 2d)
    - Re-render does not accumulate duplicate <img> (mediaEl cleared each render)
  </behavior>
  <verify>
    <automated>npm test -- tests/ElementInfoOverlay.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/ElementInfoOverlay.test.js` exits 0 including Test 2d (placeholder), Test 2e (img render), and the onerror test
    - `grep -q "kolo-zamachowe-placeholder.webp" src/data/elementInfo.js` AND `grep -q "hamulec-placeholder.webp" src/data/elementInfo.js` succeed
    - `grep -c "media: \[\]" src/data/elementInfo.js` returns 13 (only 2 of 15 entries populated)
    - `grep -q "createElement('img')" src/ui/ElementInfoOverlay.js` succeeds; `grep -q "img.onerror" src/ui/ElementInfoOverlay.js` succeeds
    - `grep -vn "^\s*//" src/ui/ElementInfoOverlay.js | grep -q "console.error" && echo HASERR || echo NOERR` prints NOERR (no console.error in the onerror path)
    - Test 2d no longer opens 'kolo-zamachowe': `grep -A8 "Test 2d" tests/ElementInfoOverlay.test.js | grep -q "oslona-przednia"` succeeds
  </acceptance_criteria>
  <done>Overlay renders media <img> with graceful onerror; 2 entries populated; Test 2d switched to empty entry; img-render + onerror tests green.</done>
</task>

<task type="auto">
  <name>Task 2: main.js MediaManager wiring + dispose + phase gate (full suite + bundle)</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js (LectorService instantiation line ~309; ElementInfoOverlay instantiation lines 351-354; dispose chain lines 499-504)
    - .planning/phases/16-media-pipeline/16-PATTERNS.md (main.js wiring lines ~179-217)
    - .planning/phases/16-media-pipeline/16-RESEARCH.md (Injection Point: main.js lines ~497-512)
  </read_first>
  <action>
    In src/main.js: (a) import MediaManager at the top: `import { MediaManager } from './media/MediaManager.js';`. (b) Instantiate it before the ElementInfoOverlay construction (around line 351, mirroring the LectorService pattern but with no store arg): `this.mediaManager = new MediaManager();`. (c) Extend the existing ElementInfoOverlay constructor call (lines 351-354) to also pass `mediaManager: this.mediaManager` alongside the existing `store` and `lectorService` props — do not remove the existing props. (d) Add to the dispose() chain (lines 499-504), after `elementInfoOverlay.dispose()` and before `lectorService.dispose()`: `if (this.mediaManager) this.mediaManager.dispose?.();` (reverse construction order; optional-chaining guards the no-op).

    Then run the PHASE GATE: full test suite must be green (978 + 1 skipped baseline, plus the new MediaManager/overlay tests) and `npm run build` main bundle under 850 KB. Confirm `getInteractables().size === 15` is unaffected (no interactable changes were made). Confirm ATTRIBUTION.txt (from 16-03) still covers every public/media/ file with zero CC-BY-NC.
  </action>
  <verify>
    <automated>npm test && npm run build 2>&1 | grep -oE "index-[A-Za-z0-9_-]+\.js[[:space:]]+[0-9.]+ kB" | grep -oE "[0-9.]+ kB" | head -1 | awk '{ if ($1+0 < 850) print "BUNDLE-OK " $1; else { print "BUNDLE-FAIL " $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "new MediaManager" src/main.js` AND `grep -q "from './media/MediaManager.js'" src/main.js` succeed
    - `grep -q "mediaManager: this.mediaManager" src/main.js` succeeds (DI wired into ElementInfoOverlay)
    - `grep -q "this.mediaManager.dispose" src/main.js` succeeds (dispose chain)
    - `npm test` exits 0 with full suite green (no regression; new tests included)
    - `npm run build` completes and the main bundle is under 850 KB (inspect build output size; placeholder assets in public/ do not enter the bundle)
    - `grep -i "CC-BY-NC" public/media/ATTRIBUTION.txt` returns nothing AND `ls public/media/*.webp | wc -l` equals the `filename:` block count in ATTRIBUTION.txt (gate holds)
  </acceptance_criteria>
  <done>MediaManager wired into main.js with DI + dispose; full suite green; bundle under 850 KB; ATTRIBUTION gate complete — phase done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| media src → DOM <img> | item.src/alt from frozen elementInfo.js render into an <img>; built via createElement (no innerHTML) |
| network → overlay | image load may 404 / time out (no network); overlay must degrade without console error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | XSS via media src/alt injected as HTML | mitigate | img built with document.createElement + property assignment; NO innerHTML for src/alt; src/alt come from frozen elementInfo.js (no user input) |
| T-16-09 | Denial of Service | broken/missing media crashes overlay or spams console (MED-03) | mitigate | img.onerror = () => img.remove() silently; alt-text + content remain; no console.error/throw |
| T-16-10 | Tampering | duplicate <img> accumulation across re-renders | mitigate | mediaEl.textContent = '' at start of media block every _render (Pitfall 3) |
| T-16-11 | Denial of Service | dispose leak of mediaManager (Phase 17 chain) | mitigate | main.js dispose chain calls this.mediaManager.dispose?.() in reverse construction order |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (16-RESEARCH.md "Package Legitimacy Audit: not applicable") |
</threat_model>

<verification>
- npm test full suite green (Test 2d placeholder, Test 2e img render, onerror removal, MediaManager unit tests)
- npm run build main bundle under 850 KB
- main.js: MediaManager imported, instantiated, DI-passed, disposed
- ATTRIBUTION.txt gate holds (block-per-file, zero CC-BY-NC)
- getInteractables().size === 15 unaffected
</verification>

<success_criteria>
- Overlay renders <img> for populated media with loading=lazy + alt; graceful silent onerror removal (MED-03)
- 2 representative entries populated; Test 2d switched to empty-media entry, still green
- main.js wires MediaManager via DI + dispose; full suite green; bundle under 850 KB; ATTRIBUTION gate complete
</success_criteria>

<output>
Create `.planning/phases/16-media-pipeline/16-02-SUMMARY.md` when done
</output>
