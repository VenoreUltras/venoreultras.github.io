---
phase: 16-media-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-media-placeholders.py
  - public/media/kolo-zamachowe-placeholder.webp
  - public/media/hamulec-placeholder.webp
  - public/media/ATTRIBUTION.txt
  - vite.config.js
autonomous: true
requirements: [MED-01, MED-02]
user_setup: []

must_haves:
  truths:
    - "public/media/ holds kolo-zamachowe-placeholder.webp and hamulec-placeholder.webp (PNG bytes, .webp name) generated by pure-Python stdlib"
    - "public/media/ATTRIBUTION.txt has one block per file in public/media/ (including pre-existing tabliczka-znamionowa.webp) with zero CC-BY-NC entries"
    - "vite.config.js has build.assetsInlineLimit:0 alongside the existing manualChunks (no asset inlined as base64)"
  artifacts:
    - path: "scripts/generate-media-placeholders.py"
      provides: "Pure-Python (stdlib zlib+struct) generator emitting PNG bytes to .webp paths for the placeholder set"
      contains: "zlib"
    - path: "public/media/ATTRIBUTION.txt"
      provides: "License provenance gate — one block per public/media/ file, zero CC-BY-NC"
      contains: "tabliczka-znamionowa.webp"
    - path: "vite.config.js"
      provides: "assetsInlineLimit:0 + preserved manualChunks"
      contains: "assetsInlineLimit"
  key_links:
    - from: "public/media/ATTRIBUTION.txt"
      to: "public/media/*.webp"
      via: "one filename block per file on disk"
      pattern: "filename:"
    - from: "vite.config.js"
      to: "build pipeline"
      via: "assetsInlineLimit:0 prevents base64 inline"
      pattern: "assetsInlineLimit"
---

<objective>
Generate the placeholder media assets and license gate for Phase 16: a pure-Python generator script, two placeholder .webp files (PNG bytes, per Phase 14 precedent), the ATTRIBUTION.txt gate covering every file in public/media/, and the vite.config.js assetsInlineLimit:0 addition.

Purpose: Establishes the asset + license substrate the overlay render consumes and enforces MED-01 (no asset inlined into the JS bundle, bundle stays under 850 KB) and MED-02 (CC-compliant attribution per file, zero CC-BY-NC — the phase GATE). This plan is independent of MediaManager (no code coupling), so it runs in Wave 1.
Output: scripts/generate-media-placeholders.py, public/media/kolo-zamachowe-placeholder.webp, public/media/hamulec-placeholder.webp, public/media/ATTRIBUTION.txt, modified vite.config.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-media-pipeline/16-RESEARCH.md
@.planning/phases/16-media-pipeline/16-PATTERNS.md

<interfaces>
Asset filenames are a CONTRACT shared with 16-02 (elementInfo.js media[].src must match these exact names):
- public/media/kolo-zamachowe-placeholder.webp   (consumed by elementInfo 'kolo-zamachowe' in 16-02)
- public/media/hamulec-placeholder.webp          (consumed by elementInfo 'hamulec' in 16-02)
- public/media/tabliczka-znamionowa.webp         (pre-existing, Phase 14 — MUST get an ATTRIBUTION block)

Generator analog: scripts/generate-nameplate-placeholder.py
  - import struct, zlib  ONLY
  - _chunk(tag, data) (CRC32) lines ~53-60
  - _encode_png(rows) (signature + IHDR + IDAT + IEND) lines ~61-87
  - .webp extension is intentional — browser/TextureLoader sniff PNG bytes

vite.config.js current shape: build -> rollupOptions -> output -> manualChunks(id) (quiz-data chunk, Phase 13, DO NOT remove)
ATTRIBUTION format: 16-RESEARCH.md Pattern 5 / 16-PATTERNS.md lines ~338-364
Placeholder license string (LOCKED): wlasnosc firmy (placeholder - do podmiany)  — ZERO "CC-BY-NC" anywhere
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure-Python placeholder generator + two .webp assets</name>
  <files>scripts/generate-media-placeholders.py, public/media/kolo-zamachowe-placeholder.webp, public/media/hamulec-placeholder.webp</files>
  <read_first>
    - scripts/generate-nameplate-placeholder.py (full file — copy _chunk, _encode_png, raster-build pattern; .webp extension rationale in docstring lines ~10-13)
    - .planning/phases/16-media-pipeline/16-PATTERNS.md (generalization pattern lines ~319-326: iterate over (path, bg_color) configs)
  </read_first>
  <action>
    Create scripts/generate-media-placeholders.py generalizing scripts/generate-nameplate-placeholder.py. Use ONLY stdlib struct + zlib — NO PIL/sharp/canvas (unavailable in this environment, per Phase 14). Keep _chunk(tag, data) and _encode_png(rows) copy-paste-stable from the nameplate script. Generalize main() to iterate over a list of configurations, each a tuple of (output_path, bg_color_rgb) (optionally with a bezel/border color), calling _encode_png(_build_raster(bg_color)) per entry and writing raw bytes. Generate exactly two files: public/media/kolo-zamachowe-placeholder.webp (gray bg 0x80,0x80,0x80) and public/media/hamulec-placeholder.webp (dark reddish bg 0x60,0x40,0x40) with a contrasting bezel border like the nameplate. Use POT-friendly dimensions (e.g. 512x320 matching the nameplate). Each output file MUST be under 5 KB. Print each filename + byte size on write (mirror nameplate script). Polish comments/docstring are fine in a Python script (not scanned by JS boundary test). Then RUN the script (python3 scripts/generate-media-placeholders.py) so the two .webp files exist on disk.
  </action>
  <verify>
    <automated>python3 scripts/generate-media-placeholders.py; test -f public/media/kolo-zamachowe-placeholder.webp; test -f public/media/hamulec-placeholder.webp</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/generate-media-placeholders.py` exits 0 and is re-runnable (idempotent overwrite)
    - Both .webp files exist in public/media/ and begin with the 8-byte PNG signature (89 50 4E 47 0D 0A 1A 0A)
    - Each file is under 5 KB (wc -c under 5120)
    - `grep -ci "import PIL" scripts/generate-media-placeholders.py` returns 0 and `grep -q "import zlib" scripts/generate-media-placeholders.py` succeeds (stdlib only)
  </acceptance_criteria>
  <done>Generator script exists (stdlib only), runs, and produces two PNG-bytes .webp placeholders under 5 KB each.</done>
</task>

<task type="auto">
  <name>Task 2: ATTRIBUTION.txt gate + vite.config.js assetsInlineLimit:0</name>
  <files>public/media/ATTRIBUTION.txt, vite.config.js</files>
  <read_first>
    - vite.config.js (existing build -> rollupOptions -> output -> manualChunks block — Phase 13 quiz-data chunk; sibling-key insertion point)
    - .planning/phases/16-media-pipeline/16-RESEARCH.md (Pattern 5 ATTRIBUTION format; Pattern 4 vite assetsInlineLimit placement lines ~258-278; Pitfall 5 tabliczka must be listed lines ~371-375)
  </read_first>
  <action>
    Create public/media/ATTRIBUTION.txt per 16-RESEARCH.md Pattern 5: a header comment (project name; the line "Format: one block per file. Gate: no CC-BY-NC entries allowed."; an updated date) followed by one dash-separated block per file currently in public/media/. There are exactly THREE asset files at this point: tabliczka-znamionowa.webp (pre-existing Phase 14 — list it FIRST, per Pitfall 5), kolo-zamachowe-placeholder.webp, hamulec-placeholder.webp. Each block has fields on their own lines: filename:, author: (PM-300 Trener project (generated placeholder)), source_url: (local), license: set to the LOCKED placeholder string wlasnosc firmy (placeholder - do podmiany) (use proper Polish diacritics), and notes: (placeholder — replace with CC0/CC BY/CC BY-SA photo at same URL). ZERO blocks may contain the token CC-BY-NC. The number of filename: lines MUST equal the number of .webp files in public/media/.

    Modify vite.config.js: add assetsInlineLimit: 0 as a sibling key inside the existing build object, positioned ABOVE rollupOptions. Do NOT remove or alter the existing manualChunks / quiz-data logic (Phase 13). Add a short Polish comment noting Phase 16 MED-01 (no asset inlined as base64).
  </action>
  <verify>
    <automated>node scripts/check-attribution.cjs || node -e "const fs=require('fs');const files=fs.readdirSync('public/media').filter(f=>f.endsWith('.webp')).length;const txt=fs.readFileSync('public/media/ATTRIBUTION.txt','utf8');const blocks=(txt.match(/^filename:/gm)||[]).length;if(/CC-BY-NC/i.test(txt))throw new Error('CC-BY-NC present');if(files!==blocks)throw new Error('mismatch files='+files+' blocks='+blocks);const vc=fs.readFileSync('vite.config.js','utf8');if(!/assetsInlineLimit/.test(vc)||!/manualChunks/.test(vc))throw new Error('vite config bad');console.log('OK files='+files+' blocks='+blocks)"</automated>
  </verify>
  <acceptance_criteria>
    - ATTRIBUTION.txt has exactly one `filename:` block per .webp file in public/media/ (currently 3: tabliczka + 2 placeholders)
    - `grep -i "CC-BY-NC" public/media/ATTRIBUTION.txt` returns nothing (zero CC-BY-NC) — gate
    - `grep -q "tabliczka-znamionowa.webp" public/media/ATTRIBUTION.txt` succeeds (pre-existing file covered)
    - `grep -q "assetsInlineLimit" vite.config.js` AND `grep -q "manualChunks" vite.config.js` both succeed (added without removing Phase 13 chunking)
  </acceptance_criteria>
  <done>ATTRIBUTION.txt covers every public/media/ file with zero CC-BY-NC; vite.config.js has assetsInlineLimit:0 plus intact manualChunks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → dist | Vite decides whether referenced assets get base64-inlined into JS/CSS |
| license audit → release | ATTRIBUTION.txt is a human-and-grep-audited gate over public/media/ contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | asset base64-inlined into bundle (MED-01 violation) | mitigate | `assetsInlineLimit: 0` in vite.config.js forces every asset to stay a referenced file; public/ assets never enter the JS bundle |
| T-16-05 | Repudiation | media file with no license provenance ships | mitigate | ATTRIBUTION.txt gate: one block per public/media/ file (count check); incomplete attribution = phase not done |
| T-16-06 | Tampering | CC-BY-NC (non-commercial) asset slips in — illegal for internal training | mitigate | hard grep gate: zero `CC-BY-NC` tokens permitted in ATTRIBUTION.txt; placeholders use "wlasnosc firmy" license |
| T-16-07 | Tampering | .webp file is actually PNG bytes (MIME confusion) | accept | intentional per Phase 14 precedent; static serving + browser content sniffing handle it; no server-side MIME trust |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (16-RESEARCH.md "Package Legitimacy Audit: not applicable"); Python uses stdlib only |
</threat_model>

<verification>
- python3 scripts/generate-media-placeholders.py exits 0; both placeholder .webp present and under 5 KB
- ATTRIBUTION.txt filename-block count equals public/media/ .webp count; zero CC-BY-NC
- vite.config.js contains assetsInlineLimit and retains manualChunks
</verification>

<success_criteria>
- Two placeholder .webp assets generated by pure-Python stdlib (PNG bytes, .webp name), under 5 KB each
- ATTRIBUTION.txt covers tabliczka-znamionowa.webp + both placeholders, zero CC-BY-NC (MED-02 gate)
- vite.config.js: assetsInlineLimit:0 added, manualChunks preserved (MED-01)
</success_criteria>

<output>
Create `.planning/phases/16-media-pipeline/16-03-SUMMARY.md` when done
</output>
