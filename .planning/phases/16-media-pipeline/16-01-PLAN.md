---
phase: 16-media-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/media/MediaManager.js
  - tests/MediaManager.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [MED-01, MED-03]
user_setup: []

must_haves:
  truths:
    - "MediaManager.resolveSrc(filename) returns '/media/<filename>' (string, no JS asset import)"
    - "MediaManager.validateSrc(src) returns Promise<boolean>: true when fetch HEAD ok, false on not-ok or network error"
    - "MediaManager imports nothing forbidden (no three/gsap/state/ui/training/highlight/education)"
  artifacts:
    - path: "src/media/MediaManager.js"
      provides: "Boundary-clean media service: resolveSrc + validateSrc + no-op dispose"
      exports: ["MediaManager"]
      contains: "class MediaManager"
    - path: "tests/MediaManager.test.js"
      provides: "Unit tests for resolveSrc + validateSrc (ok/404/offline) via injected fetchImpl"
      contains: "validateSrc"
  key_links:
    - from: "tests/MediaManager.test.js"
      to: "src/media/MediaManager.js"
      via: "import { MediaManager }"
      pattern: "import.*MediaManager.*from.*media/MediaManager"
    - from: "tests/boundaries.test.js"
      to: "src/media/MediaManager.js"
      via: "FORBIDDEN_PAIRS entry"
      pattern: "src/media/MediaManager\\.js"
---

<objective>
Create `src/media/MediaManager.js` — a boundary-clean service that maps media filenames to absolute `/media/<filename>` URLs (never a JS asset import) and validates resource reachability via an injected `fetchImpl` HEAD request. Add its unit tests and a boundaries-test entry enforcing the no-framework import contract.

Purpose: MediaManager is the W1 contract every downstream consumer (overlay render, main.js wiring) builds against. It must exist and be green before the overlay plan wires it. This satisfies MED-01 (URL resolution, zero JS asset import) and MED-03 (`validateSrc` returns `Promise<boolean>`).
Output: `src/media/MediaManager.js`, `tests/MediaManager.test.js`, updated `tests/boundaries.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-media-pipeline/16-RESEARCH.md
@.planning/phases/16-media-pipeline/16-PATTERNS.md

<interfaces>
<!-- Target contract (from 16-RESEARCH.md Pattern 1 + 16-PATTERNS.md). Executor implements exactly this shape. -->
<!-- src/media/MediaManager.js -->
- export class MediaManager
- constructor({ fetchImpl } = {})  — store this._fetch = fetchImpl ?? globalThis.fetch.bind(globalThis)
- resolveSrc(filename) -> string   — returns '/media/' + filename (sync, pure)
- validateSrc(src) -> Promise<boolean>  — this._fetch(src, { method: 'HEAD' }).then(r => r.ok).catch(() => false)
- dispose() -> void  — no-op (no subscriptions/timers/cache); present for Application dispose-chain uniformity

Analog (DI + service-method + dispose shape): src/lector/LectorService.js
boundaries.test.js LectorService analog entry: lines ~135 (FORBIDDEN_PAIRS object literal { file, mustNotImport })
boundaries guard: `if (!existsSync(filePath)) return;` (entry can be added before/after file exists)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MediaManager service (resolveSrc + validateSrc + dispose)</name>
  <files>src/media/MediaManager.js, tests/MediaManager.test.js</files>
  <read_first>
    - src/lector/LectorService.js (constructor fetchImpl DI lines ~37-55; service-method HEAD/ok pattern lines ~114-134; no-op-able dispose lines ~161-170 — the direct analog)
    - tests/lectorService.test.js (fetchImpl `vi.fn().mockResolvedValue({ ok: true })` mock helper lines ~18-23; `// @vitest-environment` docblock header lines ~1-8)
    - .planning/phases/16-media-pipeline/16-RESEARCH.md (Pattern 1 + Code Examples: jsdom-compatible MediaManager test block lines ~393-426)
  </read_first>
  <action>
    Create `src/media/MediaManager.js` exporting class `MediaManager` per the contract in this plan's `<interfaces>` block. The file MUST have ZERO import statements (all deps injected or browser-native globals) — this is the LectorService boundary contract. Constructor destructures `{ fetchImpl } = {}` and assigns `this._fetch = fetchImpl ?? globalThis.fetch.bind(globalThis)` (use `.bind(globalThis)` to avoid `this`-binding loss when `_fetch` is called later). `resolveSrc(filename)` returns the literal-prefixed string `'/media/' + filename` — do NOT build a URL via `new URL` or a baseUrl helper; `public/` assets are always served at root by Vite. `validateSrc(src)` returns `this._fetch(src, { method: 'HEAD' }).then(r => r.ok).catch(() => false)` — it MUST NOT throw; swallow network errors as `false`. Add a `dispose()` method with an empty body (no-op) for Application dispose-chain uniformity. NO Polish string literals anywhere in this file (src/media/ is NOT in boundaries ALLOWED_PATHS — the Polish scanner will scan it); MediaManager has no user-facing strings, so use only comments in Polish if needed. NO console.error/throw inside validateSrc. NO three/gsap/state/ui/training imports.

    Create `tests/MediaManager.test.js` with `// @vitest-environment node` docblock (pure service, no DOM). Import `{ MediaManager }` from `'../src/media/MediaManager.js'`. Cover four cases: (1) `resolveSrc('photo.webp')` === `'/media/photo.webp'`; (2) `validateSrc` resolves `true` when `fetchImpl` = `vi.fn().mockResolvedValue({ ok: true })`, and assert `fetchImpl` was called with `(src, { method: 'HEAD' })`; (3) `validateSrc` resolves `false` when `fetchImpl` resolves `{ ok: false }`; (4) `validateSrc` resolves `false` when `fetchImpl` = `vi.fn().mockRejectedValue(new Error('network'))` (offline). Use injected `fetchImpl` in every validateSrc test — never real network.
  </action>
  <behavior>
    - resolveSrc('photo.webp') returns '/media/photo.webp'
    - validateSrc with fetchImpl resolving {ok:true} -> resolves true; fetchImpl called with (src, {method:'HEAD'})
    - validateSrc with fetchImpl resolving {ok:false} -> resolves false
    - validateSrc with fetchImpl rejecting (network error) -> resolves false (no throw)
  </behavior>
  <verify>
    <automated>npm test -- tests/MediaManager.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/MediaManager.test.js` exits 0 with all 4 cases passing
    - `grep -c "^import" src/media/MediaManager.js` returns 0 (zero import statements)
    - `grep -q "method: 'HEAD'" src/media/MediaManager.js` succeeds
    - `grep -q "class MediaManager" src/media/MediaManager.js` succeeds and `MediaManager` is exported
  </acceptance_criteria>
  <done>MediaManager exists with resolveSrc/validateSrc/dispose; all 4 unit tests pass; file has zero imports.</done>
</task>

<task type="auto">
  <name>Task 2: boundaries.test.js entry for MediaManager</name>
  <files>tests/boundaries.test.js</files>
  <read_first>
    - tests/boundaries.test.js (LectorService FORBIDDEN_PAIRS entry lines ~135; existsSync guard line ~169; FORBIDDEN_PAIRS array open line ~24)
    - .planning/phases/16-media-pipeline/16-PATTERNS.md (boundaries entry format lines ~483-491)
  </read_first>
  <action>
    Append a new object literal to the `FORBIDDEN_PAIRS` array (before its closing `]`) for `src/media/MediaManager.js`, modeled exactly on the existing LectorService entry. Set `file: 'src/media/MediaManager.js'` and `mustNotImport: ['three', 'gsap', '../state/', './state/', '../ui/', './ui/', '../training/', './training/', '../highlight/', './highlight/', '../education/', './education/']`. Add a Polish comment above it referencing Phase 16 Plan 16-01 and the boundary (no three/gsap/state/ui/training/highlight/education), matching the LectorService comment style. The existing `if (!existsSync(filePath)) return;` guard makes this safe regardless of file presence — but src/media/MediaManager.js now exists from Task 1, so the rule will actively scan it.
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/boundaries.test.js` exits 0 (MediaManager rule passes — no forbidden imports found)
    - `grep -q "src/media/MediaManager.js" tests/boundaries.test.js` succeeds
    - The MediaManager entry's `mustNotImport` includes `'three'` and `'gsap'`
  </acceptance_criteria>
  <done>boundaries.test.js has a MediaManager FORBIDDEN_PAIRS entry; the boundary test passes against the real file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → `/media/` static URL | `validateSrc` issues a same-origin HEAD; `filename` originates from frozen `elementInfo.js` data, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | resolveSrc(filename) path traversal | accept | filename values come only from frozen `elementInfo.js` (code-controlled); no user input reaches resolveSrc this phase. If user input ever flows here, validate no `..`/leading `/`. |
| T-16-02 | Information Disclosure | validateSrc HEAD to cross-origin | mitigate | validateSrc only receives same-origin `/media/` strings from resolveSrc; no user-controlled absolute URL; CORS not applicable |
| T-16-03 | Denial of Service | validateSrc unhandled rejection (network down) | mitigate | `.catch(() => false)` swallows network errors — no unhandled promise rejection, returns boolean |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (16-RESEARCH.md "Package Legitimacy Audit: not applicable"); no install task exists |
</threat_model>

<verification>
- `npm test -- tests/MediaManager.test.js tests/boundaries.test.js` exits 0
- `grep -c "^import" src/media/MediaManager.js` === 0
- MediaManager boundary rule present and passing
</verification>

<success_criteria>
- MediaManager.resolveSrc + validateSrc + dispose implemented per contract
- 4 MediaManager unit tests green (resolveSrc, validateSrc ok/404/offline)
- boundaries.test.js enforces MediaManager import contract and passes
- Zero import statements in MediaManager.js; zero Polish string literals
</success_criteria>

<output>
Create `.planning/phases/16-media-pipeline/16-01-SUMMARY.md` when done
</output>
