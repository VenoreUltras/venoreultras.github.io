---
phase: 14-elementinfooverlay-nameplate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/ElementInfoOverlay.js
  - src/i18n/pl.js
  - style.css
  - tests/ElementInfoOverlay.test.js
autonomous: true
requirements: [OVL-01, OVL-02, OVL-03]
must_haves:
  truths:
    - "Klik elementu otwiera pełnoekranowy dialog.showModal() (nie tooltip)"
    - "Overlay pokazuje 3 zakładki: Budowa (function), BHP (bhp), Instrukcja obsługi (sopSteps)"
    - "ESC i klik poza overlay wywołują closeModal() przez store"
    - "Tryb swobodny pokazuje tylko zakładkę Budowa; tryb nauka/egzamin pokazuje 3 zakładki"
    - "Slot mediów renderuje placeholder gdy entry.media brak/pusty"
    - "Przycisk 🔊 lektora działa z DI {store, lectorService}"
  artifacts:
    - path: "src/ui/ElementInfoOverlay.js"
      provides: "Fullscreen modal overlay przejmujący kontrakt store element-info"
      contains: "class ElementInfoOverlay"
    - path: "tests/ElementInfoOverlay.test.js"
      provides: "Migrowane + nowe testy overlay (tabs, modal, mode, media, lector)"
      contains: "ElementInfoOverlay"
  key_links:
    - from: "src/ui/ElementInfoOverlay.js"
      to: "trainingStore"
      via: "store.subscribe(activeModal/_elementInfoMeshId/mode/lectorEnabled/lectorVoiceId)"
      pattern: "subscribe.*activeModal"
    - from: "src/ui/ElementInfoOverlay.js"
      to: "elementInfo[meshId]"
      via: "render function/bhp/sopSteps into tab panels"
      pattern: "elementInfo\\["
---

<objective>
Stwórz `src/ui/ElementInfoOverlay.js` — pełnoekranowy blokujący modal (`dialog.showModal()`) z 3 zakładkami (Budowa / BHP / Instrukcja obsługi) i slotem mediów, przejmujący dokładny kontrakt store starego `ElementInfoPanel`. Napisz migrowany + rozszerzony plik testowy `tests/ElementInfoOverlay.test.js`. Dodaj etykiety i18n do `pl.js` oraz reguły CSS `.element-info-overlay__*`.

Purpose: OVL-01/02/03 — zamiana bocznego dymka na lightbox bez zmiany logiki biznesowej store. Ten plan tworzy nowy komponent obok starego (atomowa zamiana następuje w planie 02 po przejściu testów). Pełna realizacja LOCKED decyzji: 3 taby, widoczność per tryb, slot mediów z placeholderem, lektor DI {store, lectorService}.
Output: Nowy komponent overlay zielony w testach; stary ElementInfoPanel.js NIETKNIĘTY (usuwany w 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-elementinfooverlay-nameplate/14-CONTEXT.md
@.planning/phases/14-elementinfooverlay-nameplate/14-RESEARCH.md
@.planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md
@.planning/phases/14-elementinfooverlay-nameplate/14-VALIDATION.md

<interfaces>
<!-- Store contract to take over (from src/ui/ElementInfoPanel.js + trainingStore) -->
Constructor DI: `{ store, rootElementId = 'modal-container', lectorService = null }`
Store fields consumed: `activeModal` (open when === 'element-info'), `_elementInfoMeshId` (open when !== null), `mode` ('free' → Budowa only; otherwise 3 tabs), `lectorEnabled`, `lectorVoiceId`
Store actions called: `closeModal()` (clears activeModal + _elementInfoMeshId + _elementInfoPos)
Data source: `elementInfo[meshId]` with fields `name`, `function`, `parameters`, `sopSteps`, `safety`, `bhp`, `media` (array, currently empty)
Lector: `lectorService.isAvailable()` → bool; `lectorService.speak(text, voiceId)` → Promise
i18n existing: `pl.modals.closeAria`, `pl.modals.elementInfo.lectorListenButton`, `pl.modals.elementInfo.lectorText*`, `pl.ui.lectorMissingKeyTooltip`, `pl.parts[id].description`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate + extend test file tests/ElementInfoOverlay.test.js (Wave 0 — RED)</name>
  <read_first>
    - tests/ElementInfoPanel.test.js (source to migrate — 207 lines, 7 describe blocks, harness lines 14–23)
    - .planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md (lines 294–328 — exact rename map + new test list)
    - .planning/phases/14-elementinfooverlay-nameplate/14-VALIDATION.md (Wave 0 Requirements, line 54)
  </read_first>
  <files>tests/ElementInfoOverlay.test.js</files>
  <action>
    Create tests/ElementInfoOverlay.test.js by migrating tests/ElementInfoPanel.test.js (do NOT delete the old file — plan 02 handles deletion). Apply mechanical renames per 14-PATTERNS.md lines 298–304: import `ElementInfoPanel` → `ElementInfoOverlay`, path `'../src/ui/ElementInfoPanel.js'` → `'../src/ui/ElementInfoOverlay.js'`, `new ElementInfoPanel(` → `new ElementInfoOverlay(`, selectors `.element-info-panel__lector-slot` → `.element-info-overlay__lector-slot`, `.element-info-panel__lector-btn` → `.element-info-overlay__lector-btn`, body selector → `.modal-card__body`.
    Convert the old 4-section nauka test (function/parameters/sopSteps/safety) into 3-tab assertions: function text inside `[data-panel="budowa"]`, bhp text inside `[data-panel="bhp"]`, sopSteps text inside `[data-panel="instrukcja"]`.
    Add NEW describe blocks per 14-VALIDATION.md line 54: (a) dialog.showModal() called when activeModal='element-info' + _elementInfoMeshId!==null, with jsdom fallback asserting `open` attr present; (b) ESC `cancel` event → store.closeModal() called (dispatch a `cancel` event on the dialog, assert activeModal becomes null); (c) backdrop click (click with clientX/clientY outside getBoundingClientRect) → closeModal(); (d) 3 tabs rendered with data-tab="budowa"/"bhp"/"instrukcja"; (e) default active tab is budowa (aria-selected="true"); (f) mode='free' → bhp+instrukcja tab buttons `hidden`; mode='nauka' → all 3 visible; (g) media slot `.element-info-overlay__media` present and shows placeholder text from `pl.modals.elementInfo.mediaPlaceholder` when entry.media empty; (h) 🔊 lector button renders in `.element-info-overlay__lector-slot` when lectorService available + lectorEnabled.
    Use the beforeEach/afterEach harness from 14-PATTERNS.md lines 317–327. Tests MUST fail initially (ElementInfoOverlay.js does not yet exist) — this is the RED state.
  </action>
  <verify>
    <automated>npm test -- tests/ElementInfoOverlay.test.js 2>&1 | grep -qE "Cannot find module|ElementInfoOverlay" && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/ElementInfoOverlay.test.js exists; `grep -c "ElementInfoOverlay" tests/ElementInfoOverlay.test.js` returns ≥ 3
    - `grep -v '^//' tests/ElementInfoOverlay.test.js | grep -c "ElementInfoPanel"` returns 0 (no stale panel refs)
    - Test file references data-tab budowa/bhp/instrukcja and cancel/backdrop close paths
    - tests/ElementInfoPanel.test.js still exists unchanged
  </acceptance_criteria>
  <done>Migrated + extended test file present and fails because implementation is absent (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ElementInfoOverlay.js + pl.js labels + CSS (GREEN)</name>
  <read_first>
    - src/ui/ElementInfoPanel.js (store contract, subscriber list lines 107–117, lector lines 124–176, dispose lines 269–281)
    - src/ui/ExamPromptModal.js (showModal() + jsdom fallback + cancel/backdrop pattern)
    - .planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md (lines 33–204 — verbatim _build/_wireSubscribers/_render/_renderLectorButton/dispose; lines 381–431 CSS; lines 402–414 pl.js keys)
    - src/i18n/pl.js (pl.modals.elementInfo block — add new keys alongside existing)
    - style.css (lines 684–769 — .modal-card + .element-info-tip rules; reuse modal-card, add overlay rules)
  </read_first>
  <files>src/ui/ElementInfoOverlay.js, src/i18n/pl.js, style.css</files>
  <behavior>
    - Opens dialog when activeModal==='element-info' && _elementInfoMeshId!==null; closes otherwise
    - 3 tab buttons + 3 panels; clicking a tab sets aria-selected and shows matching panel
    - mode==='free' → hides bhp + instrukcja tabs and forces budowa active; otherwise all 3 visible
    - cancel event and outside-click both call store.closeModal()
    - media slot shows pl.modals.elementInfo.mediaPlaceholder when entry.media empty
    - 🔊 button renders per lectorService.isAvailable() + lectorEnabled, calls speak()
  </behavior>
  <action>
    Create src/ui/ElementInfoOverlay.js implementing `class ElementInfoOverlay` with constructor signature `{ store, rootElementId = 'modal-container', lectorService = null }` (copy from 14-PATTERNS.md lines 35–48; throw `ElementInfoOverlay: brak #${rootElementId} w DOM` if root missing). Boundary: import ONLY from `../i18n/pl.js` and `../data/elementInfo.js` — NEVER three/gsap/training/highlight/floating-ui (boundaries.test.js gate).
    Implement `_build()` per 14-PATTERNS.md lines 64–127: create `<dialog>` className `modal-card modal-card--element-info-overlay`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby="element-info-overlay-title"`. Static innerHTML skeleton (literals only — XSS-safe): header with h2#element-info-overlay-title + `.element-info-overlay__lector-slot` + `.modal-card__close`; `<nav class="element-info-overlay__tabs" role="tablist">` with 3 buttons `role="tab"` data-tab budowa/bhp/instrukcja; `.modal-card__body` with 3 `.element-info-overlay__panel` (data-panel budowa/bhp/instrukcja, each containing a `.element-info-overlay__text` with data-field function/bhp/sopSteps) plus `.element-info-overlay__media`. Set close button textContent '✕' + aria-label `pl.modals.closeAria`; set tab button textContent from `pl.modals.elementInfo.tabBudowa/tabBhp/tabInstrukcja` via textContent. Append to root. Bind handlers: `_onClose` → closeModal on close button; `_onCancel` (e.preventDefault + closeModal) on `cancel` event; `_onBackdropClick` (getBoundingClientRect guard per RESEARCH Pitfall 3) on `click`; bind a tab-click handler that activates the clicked tab. DROP `_positionTip` and `_onDocPointerDown` entirely (no cursor logic — fullscreen modal).
    Implement `_wireSubscribers()` per 14-PATTERNS.md lines 134–142: subscribe activeModal, _elementInfoMeshId, mode, lectorEnabled, lectorVoiceId (DROP `_elementInfoPos` subscription).
    Implement `_activateTab(name)`: set aria-selected on matching tab button, toggle panel visibility (CSS-driven via aria-selected/active class).
    Implement `_render()` per 14-PATTERNS.md lines 152–173: compute `isOpen`; when open set title (entry.name or `pl.modals.elementInfo.titleFallback`), fill function/bhp/sopSteps via textContent into data-field panels, apply mode visibility (`const isNauka = state.mode !== 'free'`; set `hidden` on bhp + instrukcja tab buttons = !isNauka; if !isNauka call `_activateTab('budowa')`), render media placeholder when `!entry?.media?.length`, render lector button; then showModal() with try/catch + setAttribute('open','') fallback. When closed: clear lector slot, close() with fallback. NEVER use innerHTML for dynamic content — textContent only.
    Implement `_renderLectorButton(meshId, entry, state)` by copying ElementInfoPanel.js lines 124–176 verbatim, changing slot selector to `.element-info-overlay__lector-slot` and button class to `element-info-overlay__lector-btn`.
    Implement `dispose()` per 14-PATTERNS.md lines 190–201: removeEventListener for close/cancel/backdrop, unsubscribe all, remove dialog. Idempotent.
    Add to src/i18n/pl.js under `pl.modals.elementInfo` (per 14-PATTERNS.md lines 405–414, Polish): `tabBudowa: 'Budowa'`, `tabBhp: 'BHP'`, `tabInstrukcja: 'Instrukcja obsługi'`, `mediaPlaceholder: 'Brak materiałów multimedialnych'`. Preserve all existing keys.
    Add to style.css the `.element-info-overlay__*` rules per 14-PATTERNS.md lines 389–428: `.modal-card--element-info-overlay` sizing, tab row, active-tab indicator via `[aria-selected="true"]`, panel show/hide (only active panel visible), media slot, lector slot, `::backdrop` dimming. Reuse existing `.modal-card*` rules. Do NOT remove `.element-info-tip` rules yet (plan 02 removes them after panel deletion).
  </action>
  <verify>
    <automated>npm test -- tests/ElementInfoOverlay.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/ElementInfoOverlay.test.js` exits 0 (all describe blocks green)
    - `grep -cE "import .*(three|gsap|floating-ui|training/|highlight/)" src/ui/ElementInfoOverlay.js` returns 0 (boundary clean)
    - `grep -c "showModal" src/ui/ElementInfoOverlay.js` ≥ 1 and try/catch fallback present
    - `grep -c "addEventListener('cancel'" src/ui/ElementInfoOverlay.js` ≥ 1
    - `grep -c "_positionTip\|_onDocPointerDown\|_elementInfoPos" src/ui/ElementInfoOverlay.js` returns 0 (dropped legacy paths)
    - pl.js contains tabBudowa/tabBhp/tabInstrukcja/mediaPlaceholder; style.css contains `.modal-card--element-info-overlay`
  </acceptance_criteria>
  <done>ElementInfoOverlay green in its own test file; boundary-clean; legacy tooltip logic absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| elementInfo.js data → overlay DOM | Static module data rendered into DOM; treated as untrusted-by-convention |
| lectorService → external TTS | DI'd service; failures must not crash UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering (XSS) | ElementInfoOverlay dynamic content | mitigate | All dynamic strings (entry.name/function/bhp/sopSteps, pl.* keys) set via `textContent`; innerHTML used only for static literal skeleton (existing codebase convention) |
| T-14-02 | Denial of Service | lectorService.speak() rejection | mitigate | Wrap speak() promise in `.catch()` so TTS failure does not crash overlay (copied verbatim from ElementInfoPanel lines 170–174) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npm test -- tests/ElementInfoOverlay.test.js` green (modal, tabs, mode, media, lector, cancel, backdrop)
- Overlay imports stay within boundary (DOM + store + pl + elementInfo only)
- pl.js + style.css additions present; existing keys/rules preserved
</verification>

<success_criteria>
- ElementInfoOverlay.js opens via dialog.showModal() with jsdom fallback; ESC + backdrop drive closeModal()
- 3 tabs (Budowa/BHP/Instrukcja obsługi) backed by function/bhp/sopSteps; default Budowa
- mode='free' → Budowa only; mode='nauka'/egzamin → all 3
- media slot placeholder when entry.media empty; 🔊 lector DI preserved
- New test file green; ElementInfoPanel.js untouched (deleted in plan 02)
</success_criteria>

<output>
Create `.planning/phases/14-elementinfooverlay-nameplate/14-01-SUMMARY.md` when done
</output>
