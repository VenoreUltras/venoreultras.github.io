---
phase: 14-elementinfooverlay-nameplate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-nameplate-placeholder.py
  - public/media/tabliczka-znamionowa.webp
  - src/PressModel.js
  - tests/PressModel.nameplate.phase14.test.js
autonomous: true
requirements: [NAME-01]
must_haves:
  truths:
    - "public/media/tabliczka-znamionowa.webp istnieje jako prawidłowy raster (wygenerowany bez biblioteki obrazów)"
    - "_buildNameplate ładuje teksturę przez THREE.TextureLoader z public/media (nie CanvasTexture, nie bundlowany import)"
    - "Tekstura ma colorSpace = THREE.SRGBColorSpace"
    - "MaterialRegistry.trackTexture + trackMaterial('tabliczka-znamionowa') zachowane; dispose zwalnia teksturę"
    - "_registerInteractable baseMaterial: null path zachowany (EmissiveController guard)"
    - "getInteractables().size === 15 i rotacja/pozycja tabliczki bez zmian"
  artifacts:
    - path: "public/media/tabliczka-znamionowa.webp"
      provides: "Placeholder raster tabliczki znamionowej (nie bundlowany — public/)"
    - path: "scripts/generate-nameplate-placeholder.py"
      provides: "Czysty Python generator rastra (zlib PNG bytes, bez PIL)"
    - path: "src/PressModel.js"
      provides: "_buildNameplate z TextureLoader path"
      contains: "TextureLoader"
  key_links:
    - from: "src/PressModel.js _buildNameplate"
      to: "public/media/tabliczka-znamionowa.webp"
      via: "new THREE.TextureLoader().load('/media/tabliczka-znamionowa.webp')"
      pattern: "TextureLoader\\(\\)\\.load"
    - from: "src/PressModel.js _buildNameplate"
      to: "MaterialRegistry"
      via: "trackTexture + trackMaterial('tabliczka-znamionowa')"
      pattern: "trackTexture\\('tabliczka-znamionowa'"
---

<objective>
Zamień proceduralny `CanvasTexture` na tabliczce znamionowej (#15) na zewnętrzny raster ładowany przez `THREE.TextureLoader` z `public/media/tabliczka-znamionowa.webp`. Wygeneruj placeholder czystym skryptem Python (bajty PNG przez zlib, bez PIL/canvas/ImageMagick), zapisany pod nazwą `.webp` (przeglądarka sniffuje zawartość). Zachowaj kontrakt MaterialRegistry, dispose, baseMaterial:null path, rotację i inwariant 15.

Purpose: NAME-01 — realistyczna tekstura tabliczki ładowana z public/ (nie bundlowana, gotowa na podmianę realnego zdjęcia w Phase 16). LOCKED: media strategia placeholder + slot; pełna ścieżka TextureLoader teraz. LOCKED: generacja przez pure-Python (brak narzędzi obrazów w środowisku — sharp/canvas/ImageMagick nieobecne), commit wygenerowanego pliku.
Output: Placeholder w public/media/; _buildNameplate na TextureLoader; tekstura SRGB tracked + disposowana; invariant 15 + KIN bez zmian.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-elementinfooverlay-nameplate/14-CONTEXT.md
@.planning/phases/14-elementinfooverlay-nameplate/14-RESEARCH.md
@.planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md

<interfaces>
<!-- Current _buildNameplate (src/PressModel.js lines 516–591) — replace texture creation, keep rest -->
Keep unchanged: plate geometry BoxGeometry(0.4,0.25,0.02); plate.position.set(-3.05,5.5,0.05); plate.rotation.y = Math.PI*0.05; plate.castShadow=false; this.group.add(plate)
Keep unchanged: material = new THREE.MeshBasicMaterial({map: texture}) (MeshBasicMaterial — NO emissive; EmissiveController guard depends on this)
Keep unchanged: materialRegistry.trackTexture('tabliczka-znamionowa', texture); materialRegistry.trackMaterial('tabliczka-znamionowa', material)
Keep unchanged: _registerInteractable({ mesh: plate, id: 'tabliczka-znamionowa', kind: 'visual-target', baseMaterial: null })
Replace: lines 517–553 (CanvasTexture creation) → TextureLoader.load() path
MaterialRegistry API: trackTexture(id, texture), trackMaterial(id, material), disposeAll(), size()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate placeholder raster via pure-Python script</name>
  <read_first>
    - .planning/phases/14-elementinfooverlay-nameplate/14-CONTEXT.md (LOCKED placeholder decision — pure-Python PNG bytes via zlib, no PIL, saved as .webp filename)
    - .planning/phases/14-elementinfooverlay-nameplate/14-RESEARCH.md (Pattern 4 + Environment Availability — no image tools in Node)
    - src/PressModel.js (lines 512–547 — nameplate content: PM-300 / Nr ser. 2025/0042 / Producent: Demo Sp. z o.o., silver background #c8c8c8)
  </read_first>
  <files>scripts/generate-nameplate-placeholder.py, public/media/tabliczka-znamionowa.webp</files>
  <action>
    Create scripts/generate-nameplate-placeholder.py — a pure-Python (stdlib only: `zlib`, `struct`) script that writes a valid 512×320 PNG raster to `public/media/tabliczka-znamionowa.webp`. Per LOCKED decision: no PIL/sharp/canvas/ImageMagick dependency — emit raw PNG bytes (signature + IHDR + IDAT zlib-compressed pixel rows + IEND). Fill background matt silver (RGB 0xC8,0xC8,0xC8) matching the current CanvasTexture, with a dark engraved bezel border (RGB 0x3A,0x3A,0x3A) a few pixels wide. POT-friendly dimensions 512×320 are acceptable (matches existing canvas). Text rendering is NOT required for the placeholder (Phase 16 swaps in the real asset) — a silver plate with bezel is sufficient and avoids font rasterization in pure Python. The `.webp` extension is intentional: browsers and THREE.TextureLoader sniff content, so PNG bytes decode regardless of extension.
    Run the script (`python3 scripts/generate-nameplate-placeholder.py`) to produce the committed file. The file MUST be a valid decodable raster (PNG signature bytes 89 50 4E 47).
    Confirm Vite does not bundle it: the file lives in public/media/ and is referenced only by string URL — never via `import`.
  </action>
  <verify>
    <automated>python3 scripts/generate-nameplate-placeholder.py && python3 -c "import sys; b=open('public/media/tabliczka-znamionowa.webp','rb').read(4); sys.exit(0 if b == b'\x89PNG' else 1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate-nameplate-placeholder.py exists; uses only Python stdlib (`grep -cE "import (PIL|cv2|numpy)" scripts/generate-nameplate-placeholder.py` returns 0)
    - public/media/tabliczka-znamionowa.webp exists and starts with a valid raster magic header (PNG signature)
    - File size > 0 and < 50 KB (small placeholder)
    - `grep -rc "import.*tabliczka-znamionowa" src/` returns 0 (never bundled via import)
  </acceptance_criteria>
  <done>Valid placeholder raster committed at public/media/tabliczka-znamionowa.webp, generated reproducibly by stdlib-only Python.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap _buildNameplate to TextureLoader + NAME-01 test</name>
  <read_first>
    - src/PressModel.js (lines 516–591 — full _buildNameplate; keep geometry/position/rotation/registry/_registerInteractable)
    - .planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md (lines 208–252 — new texture loading block; lines 468–490 trackTexture + baseMaterial:null guard)
    - .planning/phases/14-elementinfooverlay-nameplate/14-RESEARCH.md (Pattern 3 + Pitfall 4 async + anti-pattern "do not change baseMaterial:null")
    - src/highlight/EmissiveController.js (line ~88 — MeshBasicMaterial guard on tabliczka; do not regress)
    - tests/PressModel.smoke.test.js (existing smoke harness for assertion style)
  </read_first>
  <files>src/PressModel.js, tests/PressModel.nameplate.phase14.test.js</files>
  <behavior>
    - _buildNameplate creates a texture via THREE.TextureLoader().load('/media/tabliczka-znamionowa.webp')
    - texture.colorSpace === THREE.SRGBColorSpace set synchronously (before onLoad) + in onLoad
    - material is MeshBasicMaterial (no emissive) with silver fallback color while loading
    - materialRegistry.trackTexture + trackMaterial called with id 'tabliczka-znamionowa'
    - _registerInteractable called with baseMaterial: null
    - getInteractables().size === 15 after build; plate.rotation.y === Math.PI*0.05; plate.position unchanged
    - materialRegistry.disposeAll() releases the tracked texture (no leak)
  </behavior>
  <action>
    In src/PressModel.js `_buildNameplate()`, replace the CanvasTexture creation block (current lines 517–553: canvas/ctx/text rendering + `new THREE.CanvasTexture(canvas)`) with the TextureLoader path per 14-PATTERNS.md lines 225–251: `const loader = new THREE.TextureLoader();` then `const texture = loader.load('/media/tabliczka-znamionowa.webp', onLoad, undefined, onError)`. In onLoad set `loadedTex.colorSpace = THREE.SRGBColorSpace`, `minFilter`/`magFilter` = LinearFilter, `material.needsUpdate = true`. ALSO set `texture.colorSpace = THREE.SRGBColorSpace` + LinearFilter min/mag synchronously before first render (RESEARCH A2). onError: `console.warn('[PressModel] tabliczka texture load failed', err)`. Create `const material = new THREE.MeshBasicMaterial({ map: texture, color: 0xc8c8c8 })` (silver fallback while async load completes — RESEARCH Pitfall 4). Delete the canvas/ctx/font/fillText code and the old CanvasTexture comment block.
    KEEP UNCHANGED (do not touch lines 561–591): plate geometry, position.set(-3.05,5.5,0.05), rotation.y = Math.PI*0.05, castShadow=false, group.add(plate); both `materialRegistry.trackTexture('tabliczka-znamionowa', texture)` and `trackMaterial('tabliczka-znamionowa', material)`; `_registerInteractable({ mesh: plate, id: 'tabliczka-znamionowa', kind: 'visual-target', baseMaterial: null })`. The baseMaterial:null path is CRITICAL — EmissiveController guards on `!mesh.material.emissive` (MeshBasicMaterial); changing it would crash highlighting (RESEARCH anti-pattern).
    Create tests/PressModel.nameplate.phase14.test.js asserting NAME-01: build a PressModel (use the existing test harness pattern from tests/PressModel.smoke.test.js), then assert (a) the tabliczka-znamionowa mesh material is a MeshBasicMaterial without `emissive`; (b) its `map` texture has `colorSpace === THREE.SRGBColorSpace`; (c) materialRegistry has 'tabliczka-znamionowa' tracked (size includes it); (d) getInteractables().size === 15; (e) the plate rotation.y ≈ Math.PI*0.05 and position unchanged; (f) after materialRegistry.disposeAll() the texture's dispose path runs (no leak). Mock or stub TextureLoader if jsdom cannot fetch the asset — assert the loader path is invoked with '/media/tabliczka-znamionowa.webp' and colorSpace is set synchronously.
  </action>
  <verify>
    <automated>npm test -- tests/PressModel.nameplate.phase14.test.js tests/PressModel.smoke.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TextureLoader" src/PressModel.js` ≥ 1 within _buildNameplate; `grep -c "CanvasTexture" src/PressModel.js` for the nameplate path returns 0
    - `grep -c "baseMaterial: null" src/PressModel.js` unchanged (tabliczka entry still present)
    - `grep -c "trackTexture('tabliczka-znamionowa'" src/PressModel.js` ≥ 1
    - `grep -c "SRGBColorSpace" src/PressModel.js` ≥ 1 in nameplate path
    - `npm test -- tests/PressModel.nameplate.phase14.test.js tests/PressModel.smoke.test.js` exits 0
    - getInteractables().size === 15 assertion green; rotation/position assertions green
  </acceptance_criteria>
  <done>_buildNameplate loads external WebP via TextureLoader with SRGB; registry + dispose + baseMaterial:null + invariant 15 + kinematics intact; NAME-01 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/ asset → TextureLoader | External raster fetched at runtime by root-relative URL; must not 404 |
| PressModel → EmissiveController | Material shape (MeshBasicMaterial, no emissive) is a contract the highlight guard depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure / Availability | tabliczka texture asset path (404 in dev vs prod) | mitigate | Use root-relative '/media/tabliczka-znamionowa.webp'; onError handler logs warning; silver fallback color 0xc8c8c8 keeps mesh visible (RESEARCH A1/Pitfall 4) |
| T-14-06 | Tampering | baseMaterial:null guard for EmissiveController | mitigate | Keep `baseMaterial: null` in _registerInteractable unchanged; NAME-01 test asserts MeshBasicMaterial without emissive (RESEARCH anti-pattern) |
| T-14-07 | Resource leak | GPU texture not freed on dispose | mitigate | materialRegistry.trackTexture('tabliczka-znamionowa', texture) preserved; NAME-01 test asserts disposeAll() releases it |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages; generator uses Python stdlib only |
</threat_model>

<verification>
- public/media/tabliczka-znamionowa.webp is a valid raster, not bundled
- _buildNameplate uses TextureLoader + SRGBColorSpace; CanvasTexture path removed
- MaterialRegistry track + dispose intact; baseMaterial:null preserved
- getInteractables().size === 15; rotation/position unchanged
</verification>

<success_criteria>
- Placeholder raster generated by stdlib-only Python and committed under public/media/
- Nameplate texture loaded via THREE.TextureLoader (root-relative URL), colorSpace SRGB
- trackTexture/trackMaterial preserved; dispose releases texture; baseMaterial:null intact
- Invariant 15 and kinematic rotation unchanged; NAME-01 + smoke tests green
</success_criteria>

<output>
Create `.planning/phases/14-elementinfooverlay-nameplate/14-03-SUMMARY.md` when done
</output>
