---
phase: 14-elementinfooverlay-nameplate
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/boundaries.test.js
  - tests/phase11.integration.test.js
  - src/main.js
  - style.css
  - src/ui/ElementInfoPanel.js
  - tests/ElementInfoPanel.test.js
autonomous: true
requirements: [OVL-01]
must_haves:
  truths:
    - "ElementInfoPanel.js usunięty z dysku"
    - "main.js instancjonuje i disposuje ElementInfoOverlay zamiast ElementInfoPanel"
    - "boundaries.test.js i phase11.integration.test.js referencują ElementInfoOverlay.js"
    - "Pełny suite zielony (945+ baseline) bez regresji logiki biznesowej"
    - "getInteractables().size === 15 zachowane"
  artifacts:
    - path: "src/main.js"
      provides: "Wiring ElementInfoOverlay w Application (import/instantiate/dispose)"
      contains: "ElementInfoOverlay"
  key_links:
    - from: "src/main.js"
      to: "src/ui/ElementInfoOverlay.js"
      via: "import + new ElementInfoOverlay({store, lectorService})"
      pattern: "new ElementInfoOverlay"
    - from: "tests/boundaries.test.js"
      to: "src/ui/ElementInfoOverlay.js"
      via: "FORBIDDEN_PAIRS file entry"
      pattern: "ElementInfoOverlay"
---

<objective>
Wykonaj atomową zamianę: zaktualizuj referencje do pliku w testach (boundaries + phase11.integration) NAJPIERW, przełącz wiring w `src/main.js` na `ElementInfoOverlay`, usuń stary `src/ui/ElementInfoPanel.js` oraz `tests/ElementInfoPanel.test.js`, usuń martwe reguły CSS `.element-info-tip`. Potwierdź pełny suite zielony bez regresji.

Purpose: OVL-01 — atomowa migracja z zerem regresji logiki biznesowej. Kolejność operacji KRYTYCZNA (RESEARCH Pitfall 5/6): aktualizuj referencje do pliku ZANIM usuniesz plik, inaczej phase11.integration i boundaries failują na brakującym pliku.
Output: ElementInfoPanel.js i jego test usunięte; Application wired do overlay; pełny suite green; getInteractables().size===15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-elementinfooverlay-nameplate/14-RESEARCH.md
@.planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md

<interfaces>
<!-- Wiring points in src/main.js (verified) -->
Line 25: `import { ElementInfoPanel } from './ui/ElementInfoPanel.js';`
Lines 316–319: `this.elementInfoPanel = new ElementInfoPanel({ store: this.store, lectorService: this.lectorService });`
Line 458: `if (this.elementInfoPanel) this.elementInfoPanel.dispose();`

<!-- Test reference points (verified) -->
boundaries.test.js lines 118–121: FORBIDDEN_PAIRS entry for 'src/ui/ElementInfoPanel.js' (mustNotImport three/gsap/@floating-ui/dom/training/highlight)
phase11.integration.test.js lines 89 + 133: 'src/ui/ElementInfoPanel.js' in required[] and modules[] arrays
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test references FIRST + swap main.js wiring</name>
  <read_first>
    - tests/boundaries.test.js (lines 115–129 — ElementInfoPanel FORBIDDEN_PAIRS entry)
    - tests/phase11.integration.test.js (lines 82–139 — required[] line 89, modules[] line 133)
    - src/main.js (lines 25, 313–319, 458 — import/instantiate/dispose)
    - .planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md (lines 256–367 — exact swap map)
  </read_first>
  <files>tests/boundaries.test.js, tests/phase11.integration.test.js, src/main.js</files>
  <action>
    ORDER MATTERS — do all reference updates BEFORE the file deletion in Task 2 (RESEARCH Pitfall 5/6).
    In tests/boundaries.test.js: change the FORBIDDEN_PAIRS file path `'src/ui/ElementInfoPanel.js'` → `'src/ui/ElementInfoOverlay.js'` keeping the same `mustNotImport` array (`['three', 'gsap', '@floating-ui/dom', '../training/', './training/', '../highlight/', './highlight/']`); update the comment to reference Phase 14 ElementInfoOverlay (per 14-PATTERNS.md lines 336–347).
    In tests/phase11.integration.test.js: change `'src/ui/ElementInfoPanel.js'` → `'src/ui/ElementInfoOverlay.js'` at BOTH occurrences (required[] ~line 89 and modules[] ~line 133).
    In src/main.js: change import (line 25) to `import { ElementInfoOverlay } from './ui/ElementInfoOverlay.js';`; change instantiation (lines 316–319) to `this.elementInfoOverlay = new ElementInfoOverlay({ store: this.store, lectorService: this.lectorService });`; change dispose (line 458) to `if (this.elementInfoOverlay) this.elementInfoOverlay.dispose();`. Update the surrounding comment at line 313 to reference Phase 14 overlay. Do NOT change any other logic in main.js.
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js tests/phase11.integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ElementInfoPanel" tests/boundaries.test.js` returns 0; `grep -c "ElementInfoOverlay" tests/boundaries.test.js` ≥ 1
    - `grep -c "ElementInfoPanel" tests/phase11.integration.test.js` returns 0
    - `grep -c "ElementInfoPanel" src/main.js` returns 0; `grep -c "new ElementInfoOverlay" src/main.js` ≥ 1
    - `npm test -- tests/boundaries.test.js tests/phase11.integration.test.js` exits 0 (ElementInfoOverlay.js exists from plan 01 so file-existence + boundary checks pass)
  </acceptance_criteria>
  <done>All references point to ElementInfoOverlay; the two reference tests are green while ElementInfoPanel.js still on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ElementInfoPanel.js + its test + dead CSS; full suite gate</name>
  <read_first>
    - style.css (lines 710–769 — .element-info-tip rules to remove per 14-PATTERNS.md line 431)
    - .planning/phases/14-elementinfooverlay-nameplate/14-PATTERNS.md (line 507 — deletion-last pitfall)
  </read_first>
  <files>src/ui/ElementInfoPanel.js, tests/ElementInfoPanel.test.js, style.css</files>
  <action>
    Only after Task 1's reference updates are green: delete `src/ui/ElementInfoPanel.js` and `tests/ElementInfoPanel.test.js` (use `git rm` or remove + stage). Remove the now-dead `.element-info-tip`, `.element-info-tip--centered`, `.element-info-tip .modal-card__*`, `.element-info-tip .element-info-panel__*`, and `@keyframes element-info-tip-in` rules from style.css (14-PATTERNS.md line 431) — keep all `.modal-card*` and the new `.element-info-overlay__*` rules from plan 01. Run the full suite to confirm zero regression and the 15-interactable invariant.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/ui/ElementInfoPanel.js && echo gone` prints gone
    - `test ! -f tests/ElementInfoPanel.test.js && echo gone` prints gone
    - `grep -c "element-info-tip" style.css` returns 0
    - `npm test` exits 0; suite count ≥ 945 baseline (minus removed panel tests, plus new overlay tests) with no failures
    - getInteractables().size === 15 assertion (PressModel smoke tests) still green
  </acceptance_criteria>
  <done>ElementInfoPanel.js + test + tip CSS gone; full suite green; invariant 15 intact; atomic migration complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time module graph | main.js import must resolve; deletion ordering prevents broken graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Repudiation/Regression | Atomic file deletion ordering | mitigate | Update boundaries.test.js + phase11.integration.test.js + main.js references in Task 1 BEFORE deleting ElementInfoPanel.js in Task 2; full-suite gate confirms no regression (RESEARCH Pitfall 5/6) |
| T-14-04 | Tampering | EmissiveController nameplate guard | accept | Not touched by this plan; nameplate change isolated to plan 03 |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
- Reference tests green BEFORE deletion (boundaries + phase11.integration)
- `npm test` full suite green after deletion; no logic regression
- getInteractables().size === 15 preserved
</verification>

<success_criteria>
- ElementInfoPanel.js + tests/ElementInfoPanel.test.js deleted
- main.js wired to ElementInfoOverlay (import/instantiate/dispose)
- boundaries + phase11.integration reference ElementInfoOverlay.js
- Dead .element-info-tip CSS removed
- Full suite green; invariant 15 intact
</success_criteria>

<output>
Create `.planning/phases/14-elementinfooverlay-nameplate/14-02-SUMMARY.md` when done
</output>
