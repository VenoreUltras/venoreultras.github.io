---
phase: 03-click-to-state-pipeline
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/RaycastController.js
  - tests/RaycastController.test.js
autonomous: true
requirements: [INTERACT-01, INTERACT-02, INTERACT-03, INTERACT-05]
tags: [raycast, three, interaction, hover, hysteresis, stress-test]
must_haves:
  truths:
    - "Pojedyncza klasa RaycastController z 1 instancją THREE.Raycaster"
    - "Raycast wywołuje się WYŁĄCZNIE w odpowiedzi na pointermove/pointerdown — nigdy per-frame bez dirty flag"
    - "Hysteresis ≥2 ticki — adjacent mesh flicker bounded"
    - "Hover commit ustawia jasny emissive hint + cursor:pointer; leave restoruje poprzedni emissive (read-modify-restore na aktualnej wartości)"
    - "Klik (pointerup z deltą <5px od pointerdown) wywołuje store.attemptStep({kind:'click', meshId: userData.id}) — literal 'click' (NIE userData.kind), zgodne z ProcedureEngine Branch 3 (D-Phase3-03 Opcja A)"
    - "TEST-04: 100 wywołań handlePointerDown+handlePointerUp z mockowanym hit estop emituje DOKŁADNIE 1 step.done"
    - "RaycastController.dispose() removeEventListener wszystkie 3 pointer listenery + reset _pendingTarget=null + _pendingCount=0"
    - "INTERACT-01 idle assertion: _runHysteresis(dt) bez wcześniejszego pointermove (czyli _pointerDirty===false) NIE wywołuje raycaster.intersectObjects"
  artifacts:
    - path: "src/RaycastController.js"
      provides: "Klasa RaycastController({renderer, camera, interactables, store}) z handlePointerDown, _runHysteresis, dispose"
      min_lines: 80
      contains: "class RaycastController"
    - path: "tests/RaycastController.test.js"
      provides: "Unit + stress testy (TEST-04, hysteresis 4-tick, drag<5px, wrong-mesh violation, idle SC1)"
      min_lines: 120
      contains: "100"
  key_links:
    - from: "RaycastController._handlePointerUp"
      to: "store.attemptStep"
      via: "intent {kind:'click' literal, meshId: userData.id} — D-Phase3-03 Opcja A (NIE userData.kind)"
      pattern: "kind:\\s*['\"]click['\"]"
    - from: "RaycastController._runHysteresis"
      to: "raycaster.intersectObjects"
      via: "GSAP tickable callback (1 raycast/tick max)"
      pattern: "intersectObjects"
    - from: "RaycastController._commitHover/_commitLeave"
      to: "mesh.material.emissive"
      via: "getHex/setHex read-modify-restore"
      pattern: "emissive\\.(get|set)Hex"
---

<objective>
Utworzenie klasy `RaycastController` w `src/RaycastController.js` — input layer wiążący pointer events z `store.attemptStep`. Implementuje:
- INTERACT-01: 1 Raycaster, throttling 1 raycast/tick przez dirty flag + GSAP tickable
- INTERACT-02: pointerup<5px → store.attemptStep({kind:'click', meshId: userData.id}) — literal 'click' (D-Phase3-03 Opcja A, zgodne z ProcedureEngine Branch 3)
- INTERACT-03: hysteresis hover (≥2 ticki) z emissive read-modify-restore + cursor:pointer
- INTERACT-05: kontrybucja stress test 100-click (pełny pipeline INTERACT-05 zamknięty wraz z lockem isAnimating z Plan 03-01)

Komplementarne testy w `tests/RaycastController.test.js` — TEST-04, hysteresis 4-tick A→B, drag-vs-click, wrong-mesh violation, idle SC1 assertion.

Purpose: Tępa rura 3D events → store. Nie wie o SOP semantyce. Engine pozostaje pure.

Output: 1 nowy moduł, 1 nowy plik testów, ≥10 nowych unit/stress testów. Boundary entry z Plan 03-01 zaczyna egzekwować po utworzeniu pliku.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-click-to-state-pipeline/03-CONTEXT.md
@.planning/phases/03-click-to-state-pipeline/03-RESEARCH.md
@.planning/phases/03-click-to-state-pipeline/03-PATTERNS.md
@.planning/phases/03-click-to-state-pipeline/03-01-SUMMARY.md

@src/PressModel.js
@src/state/trainingStore.js
@tests/PressModel.smoke.test.js
@tests/uruchomienie.integration.test.js

<interfaces>
Konsumowane API:

```js
// src/PressModel.js (Phase 2 — niezmienne):
pressModel.getInteractables(): Map<string, THREE.Mesh>
// Każdy mesh: mesh.userData = {id, kind, restPosition, labelPL, descriptionPL}
//   gdzie kind ∈ {'manipulation', 'visual-target'}
// Każdy mesh.material to sklonowany MeshStandardMaterial (TWIN-11) — bezpieczna mutacja .emissive

// src/state/trainingStore.js (po Plan 03-01):
store.getState().attemptStep(intent: {kind:'click', meshId})  // 1-arg sygnatura, kind LITERAŁ 'click'

// THREE.Raycaster API (r0.184):
raycaster.setFromCamera(ndc: Vector2, camera: Camera): void
raycaster.intersectObjects(meshes: Mesh[], recursive: boolean): Array<{object: Mesh, distance, point, ...}>
// UWAGA: w testach Node camera.updateMatrixWorld() WYMAGANE przed setFromCamera (Pitfall 3)
```

Eksportowane API:

```js
// src/RaycastController.js (NOWY)
export class RaycastController {
  constructor({renderer, camera, interactables, store}: {
    renderer: THREE.WebGLRenderer | {domElement: HTMLElement | MockEl},
    camera: THREE.Camera,
    interactables: Map<string, THREE.Mesh>,
    store: ZustandStore
  })

  // Public — wywoływane z testów + pointer event listenerów
  handlePointerDown(event: PointerEvent | MockEvent): void

  // Tickable — rejestrowany w Application.tickables
  _runHysteresis(dt: number): void

  // Cleanup — wywoływane z Application.dispose()
  dispose(): void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementacja src/RaycastController.js — klasa z hover hysteresis + click-vs-drag + emissive R-M-R</name>
  <read_first>
    - .planning/phases/03-click-to-state-pipeline/03-RESEARCH.md (Pattern 1 — pełna implementacja referencyjna linie 167-287; Pitfall 1 NDC z getBoundingClientRect; Pitfall 4 OrbitControls koegzystencja; Pitfall 7 hysteresis poprawność)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `src/RaycastController.js` — wzorce class header, dispose, emissive mutation, store dispatch)
    - .planning/phases/03-click-to-state-pipeline/03-CONTEXT.md (D-Phase3-03 intent shape — UWAGA Update 2026-05-06 Opcja A: literał 'click' nie userData.kind; D-Phase3-05 R-M-R, D-Phase3-06 hysteresis ≥2, D-Phase3-08 cursor, D-Phase3-13 pixel threshold)
    - src/training/ProcedureEngine.js (linie 44-47 — POTWIERDŹ kontrakt: manipulation/visual-target oczekuje intent.kind === 'click'; visual-attest oczekuje intent.kind === 'check')
    - src/PressModel.js (linie 1-30 — pattern class structure + JSDoc po polsku)
    - src/main.js (linie 19-31 — pattern tickables + _unsubscribers do referencji)
  </read_first>
  <behavior>
    (Testy implementowane w Task 2 — ten task to implementacja klasy zgodna z poniższym shape.)
    - Konstruktor: 1 instancja `THREE.Raycaster`, 1 instancja `THREE.Vector2` reused
    - `_handlePointerMove(event)`: oblicza NDC z `getBoundingClientRect()`, ustawia `_pointerDirty = true` — NIE wywołuje raycastu
    - `_runHysteresis(dt)`: jeśli `_pointerDirty`, robi 1 raycast, zarządza state machine `_pendingTarget`/`_pendingCount`/`_committedTarget`; jeśli `_pointerDirty===false` i brak committed → early return BEZ raycastu (SC1 idle)
    - `_commitHover(mesh)`: zapisuje `_hoverPrevEmissive = mesh.material.emissive.getHex()`, ustawia `setHex(0x222222)`, ustawia `cursor: pointer`
    - `_commitLeave()`: restoruje `setHex(_hoverPrevEmissive)`, ustawia `cursor: default`
    - `handlePointerDown(event)`: zapisuje `_downX, _downY`
    - `_handlePointerUp(event)`: jeśli sqrt(dx²+dy²)<5 → raycast → `store.getState().attemptStep({kind:'click', meshId: userData.id})` — LITERAŁ 'click' (D-Phase3-03 Opcja A)
    - `dispose()`: removeEventListener wszystkich 3 listenerów; restoruje aktualnie committed hover (defensywne); resetuje _pendingTarget=null + _pendingCount=0
  </behavior>
  <action>
Utwórz plik `src/RaycastController.js` zgodnie z RESEARCH Pattern 1. Pełen szablon poniżej — implementuj LITERALNIE z poniższego, bez improwizacji:

```js
// src/RaycastController.js
// Phase 3 — INTERACT-01..05: warstwa wejściowa łącząca pointer events z store.attemptStep.
// Pojedynczy THREE.Raycaster. Throttling 1 raycast/tick przez dirty flag + GSAP tickable.
// Hover hysteresis ≥2 ticki (D-Phase3-06). Click-vs-drag pixel threshold <5px (D-Phase3-13).
// Emissive read-modify-restore (D-Phase3-05). Boundary: NIE importuje training/ — store jest
// jedyną ścieżką do ProcedureEngine (D-Phase3-04, boundaries.test.js egzekwuje).
//
// D-Phase3-03 Opcja A (Update 2026-05-06): intent.kind to LITERAŁ 'click' — nie kopiujemy
// userData.kind. ProcedureEngine Branch 3 (linie 44-47) sprawdza intent.kind === 'click'
// dla manipulation/visual-target. RaycastController jest tępą rurą tłumaczącą pointer event
// na engine-compatible intent shape.

import * as THREE from 'three';

const HOVER_HINT_HEX = 0x222222;        // subtelny szary lift (D-Phase3-05 placeholder)
const CLICK_DRAG_THRESHOLD_PX = 5;      // <5px = click, ≥5px = drag (D-Phase3-13)
const HYSTERESIS_TICKS = 2;             // commit po 2 kolejnych tickach z tym samym targetem (D-Phase3-06)

export class RaycastController {
  /**
   * @param {object} deps
   * @param {{domElement: HTMLElement}} deps.renderer
   * @param {THREE.Camera} deps.camera
   * @param {Map<string, THREE.Mesh>} deps.interactables
   * @param {{getState: () => {attemptStep: (intent:object)=>void}}} deps.store
   */
  constructor({ renderer, camera, interactables, store }) {
    this._renderer = renderer;
    this._camera = camera;
    // Array snapshot raz w ctor — zero alokacji per-tick (CONTEXT code_context: getInteractables stable refs)
    this._meshes = Array.from(interactables.values());
    this._store = store;

    this._raycaster = new THREE.Raycaster();
    this._ndc = new THREE.Vector2(); // reused per-event

    // Hover state machine (D-Phase3-06)
    this._pendingTarget = null;
    this._pendingCount = 0;
    this._committedTarget = null;
    this._hoverPrevEmissive = 0;

    // Click-vs-drag (D-Phase3-13)
    this._downX = 0;
    this._downY = 0;

    // Dirty flag — pointermove ustawia, _runHysteresis konsumuje (1 raycast/tick INTERACT-01)
    this._pointerDirty = false;

    // Bound listeners (zachowujemy referencję dla removeEventListener)
    this._onPointerMove = this._handlePointerMove.bind(this);
    this._onPointerDown = this.handlePointerDown.bind(this);
    this._onPointerUp = this._handlePointerUp.bind(this);

    const el = renderer.domElement;
    el.addEventListener('pointermove', this._onPointerMove);
    el.addEventListener('pointerdown', this._onPointerDown);
    el.addEventListener('pointerup', this._onPointerUp);
  }

  /**
   * Aktualizuje NDC i ustawia dirty flag. RAYCAST NIE jest wykonywany tutaj — INTERACT-01.
   * Pitfall 1: getBoundingClientRect WYMAGANE bo canvas nie jest na (0,0) strony.
   */
  _handlePointerMove(event) {
    const rect = this._renderer.domElement.getBoundingClientRect();
    this._ndc.x = ((event.clientX - rect.left) / rect.width) * 2 - 1;
    this._ndc.y = -((event.clientY - rect.top) / rect.height) * 2 + 1;
    this._pointerDirty = true;
  }

  /**
   * GSAP tickable. Application.tickables.push((dt) => controller._runHysteresis(dt)).
   * 1 raycast/tick max. Hover state machine: tick z tym samym targetem N>=2 → commit.
   * SC1 idle: gdy _pointerDirty===false I brak committed target → early return BEZ raycastu.
   */
  _runHysteresis(_dt) {
    if (!this._pointerDirty) {
      // Brak ruchu myszy — jeśli jest committed target, sprawdzamy stale leave
      if (this._committedTarget) {
        this._pendingCount--;
        if (this._pendingCount <= 0) this._commitLeave();
      }
      return;
    }
    this._pointerDirty = false;

    this._raycaster.setFromCamera(this._ndc, this._camera);
    const hits = this._raycaster.intersectObjects(this._meshes, false);
    const hitMesh = hits.length > 0 ? hits[0].object : null;

    if (hitMesh === this._pendingTarget) {
      this._pendingCount++;
      if (this._pendingCount >= HYSTERESIS_TICKS && hitMesh !== this._committedTarget) {
        if (this._committedTarget) this._commitLeave();
        if (hitMesh) this._commitHover(hitMesh);
      }
    } else {
      // Nowy target — reset hysteresis count
      this._pendingTarget = hitMesh;
      this._pendingCount = 1;
    }
  }

  /**
   * Commit hover: zapisz aktualny emissive, ustaw hint hex, cursor pointer.
   * Read-modify-restore (D-Phase3-05): zapamiętujemy AKTUALNĄ wartość — Phase 4 może w przyszłości
   * pisać emissive po store change, _commitLeave restoruje "ostatni stable" state.
   */
  _commitHover(mesh) {
    this._committedTarget = mesh;
    this._hoverPrevEmissive = mesh.material.emissive.getHex();
    mesh.material.emissive.setHex(HOVER_HINT_HEX);
    this._renderer.domElement.style.cursor = 'pointer'; // D-Phase3-08
  }

  _commitLeave() {
    if (this._committedTarget) {
      this._committedTarget.material.emissive.setHex(this._hoverPrevEmissive);
      this._committedTarget = null;
    }
    this._renderer.domElement.style.cursor = 'default';
    this._pendingCount = 0;
    this._pendingTarget = null;
  }

  /**
   * Public — wywoływane przez pointerdown listener LUB bezpośrednio z testu (D-Phase3-15).
   * Zapisuje pozycję pointera dla późniejszej discriminacji click vs drag.
   */
  handlePointerDown(event) {
    this._downX = event.clientX;
    this._downY = event.clientY;
  }

  /**
   * Pointerup: jeśli delta od pointerdown <5px → click → raycast → store.attemptStep.
   * Drag (≥5px) jest obrabiany przez OrbitControls — Phase 3 nic nie robi.
   * D-Phase3-04: ZAWSZE woła attemptStep przy hicie — wrong-mesh = engine-side violation.
   * D-Phase3-03 Opcja A: intent.kind to LITERAŁ 'click' (NIE userData.kind) — kompatybilne
   * z ProcedureEngine Branch 3. Visual-attest button (Plan 03-04) używa kind:'check'.
   */
  _handlePointerUp(event) {
    const dx = event.clientX - this._downX;
    const dy = event.clientY - this._downY;
    const dist = Math.sqrt(dx * dx + dy * dy);
    if (dist >= CLICK_DRAG_THRESHOLD_PX) return; // drag — nie click

    const rect = this._renderer.domElement.getBoundingClientRect();
    this._ndc.x = ((event.clientX - rect.left) / rect.width) * 2 - 1;
    this._ndc.y = -((event.clientY - rect.top) / rect.height) * 2 + 1;
    this._raycaster.setFromCamera(this._ndc, this._camera);
    const hits = this._raycaster.intersectObjects(this._meshes, false);
    if (hits.length === 0) return;

    const mesh = hits[0].object;
    // D-Phase3-03 Opcja A: literał 'click' (CRIT-7 identity-only — meshId z userData)
    const intent = { kind: 'click', meshId: mesh.userData.id };
    this._store.getState().attemptStep(intent);
  }

  /**
   * Zwalnia event listenery. Defensywnie restoruje committed hover (uniknij dead emissive).
   * Resetuje _pendingTarget/_pendingCount żeby uniknąć stale state w HMR cycle.
   * Wpinane przez Application._unsubscribers (STATE-03) lub Application.dispose() direct.
   */
  dispose() {
    const el = this._renderer.domElement;
    el.removeEventListener('pointermove', this._onPointerMove);
    el.removeEventListener('pointerdown', this._onPointerDown);
    el.removeEventListener('pointerup', this._onPointerUp);
    if (this._committedTarget) this._commitLeave();
    this._pendingTarget = null;
    this._pendingCount = 0;
  }
}
```

Konwencje:
- ZERO importów `pl/` (boundaries Polish-literal scanner sprawdzi `src/RaycastController.js` — komentarze po polsku OK, string literals z diakrytykami NIE)
- ZERO importów `../training/` (boundaries entry z Plan 03-01 to egzekwuje)
- Tylko `import * as THREE from 'three'` jako external dependency
- intent.kind to ZAWSZE literał `'click'` w `_handlePointerUp` — NIGDY `mesh.userData.kind`
  </action>
  <verify>
    <automated>npm test -- --run tests/boundaries.test.js</automated>
    <acceptance_criteria>
      - Plik `src/RaycastController.js` istnieje (`test -f src/RaycastController.js`)
      - `grep -c "^class\|^export class" src/RaycastController.js` zwraca 1 (jedna klasa)
      - `grep -cE "new THREE\.Raycaster" src/RaycastController.js` zwraca dokładnie 1 (CRIT-5: jedna instancja)
      - `grep -nE "from ['\"]\.\./training/" src/RaycastController.js | grep -v '^#' | grep -c .` zwraca 0 (boundary)
      - `grep -nE "from ['\"]\.\./i18n" src/RaycastController.js | grep -v '^#' | grep -c .` zwraca 0 (no i18n imports — komentarze po polsku tak, literały nie)
      - `grep -c "addEventListener" src/RaycastController.js` zwraca 3 (pointermove, pointerdown, pointerup)
      - `grep -c "removeEventListener" src/RaycastController.js` zwraca 3 (dispose)
      - `grep -nE "intersectObjects" src/RaycastController.js | grep -c .` zwraca 2 (raz w _runHysteresis, raz w _handlePointerUp)
      - `grep -nE "emissive\.getHex|emissive\.setHex" src/RaycastController.js | grep -c .` zwraca ≥3 (1× getHex w commitHover, 2× setHex w commitHover/commitLeave)
      - `grep -nE "store\.getState\(\)\.attemptStep" src/RaycastController.js | grep -c .` zwraca 1
      - `grep -cE "kind:\s*['\"]click['\"]" src/RaycastController.js` zwraca 1 (D-Phase3-03 Opcja A — literał 'click')
      - `grep -cE "mesh\.userData\.kind" src/RaycastController.js` zwraca 0 (NIE kopiujemy userData.kind do intent)
      - `npm test -- --run tests/boundaries.test.js` zielony (RaycastController entry teraz aktywny — boundary scanner egzekwuje + Polish literal scanner)
    </acceptance_criteria>
  </verify>
  <done>
    Plik `src/RaycastController.js` utworzony, zgodny z RESEARCH Pattern 1, intent.kind to literał 'click' (D-Phase3-03 Opcja A), przechodzi boundaries.test.js (zarówno import-graph guard jak i Polish literal scanner).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/RaycastController.test.js — TEST-04 stress + hysteresis 4-tick + drag<5px + wrong-mesh violation + idle SC1 + dispose state reset</name>
  <read_first>
    - src/RaycastController.js (właśnie utworzony — referencja API)
    - tests/PressModel.smoke.test.js (linie 1-30 — wzorzec mock canvas + vitest-environment)
    - tests/uruchomienie.integration.test.js (linie 50-67 — wzorzec wrong-mesh violation assertion)
    - .planning/phases/03-click-to-state-pipeline/03-RESEARCH.md (sekcja "Kluczowe wzorce mockowania" linie 503-547 — mockRenderer, vi.spyOn, hysteresis test)
    - .planning/phases/03-click-to-state-pipeline/03-CONTEXT.md (D-Phase3-15 — szczegółowy opis stress test; D-Phase3-03 Opcja A — intent.kind literał 'click')
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `tests/RaycastController.test.js` — wzorce mock setup, beforeEach, vi.spyOn)
    - src/training/scenarios/uruchomienie.js (krok `odblokuj-estop` jest 6. — pozycja w stepIds)
  </read_first>
  <behavior>
    - Test 1: konstruktor — instancjuje 1 Raycaster, rejestruje 3 listenery
    - Test 2: dispose — wywołuje removeEventListener 3x i defensywnie commitLeave (jeśli był committed target) ORAZ resetuje _pendingTarget=null + _pendingCount=0 (warning #6)
    - Test 3: _handlePointerMove ustawia dirty flag bez wywołania raycastu (INTERACT-01 — sprawdzić że intersectObjects spy NIE został wywołany)
    - Test 3b: **INTERACT-01 idle SC1 (BLOCKER #4):** wywołanie `controller._runHysteresis(16)` BEZ żadnego pointermove (czyli _pointerDirty===false od konstruktora) i BRAK committed target → spy `intersectObjects` NIE wywołany (zero raycaster calls w idle)
    - Test 4: hysteresis 4-tick A→B (zgodnie z CONTEXT specifics):
      tick 1: pointermove A → pending=A,count=1; emissive A bez zmian
      tick 2: pointermove A → count=2 → commit A; emissive A === HOVER_HINT_HEX
      tick 3: pointermove B → reset pending=B,count=1; emissive A wciąż HOVER_HINT_HEX (committed)
      tick 4: pointermove B → count=2 → leave A (restore prevEmissive A) + commit B
    - Test 5: cursor toggle — po commit hover `cursor === 'pointer'`, po leave `cursor === 'default'`
    - Test 6: click-vs-drag — pointerdown@(100,100), pointerup@(102,103) → dist≈3.6 <5 → attemptStep WYWOŁANY z `{kind:'click', meshId:'tabliczka-znamionowa'}` (literał 'click', NIE 'visual-target' z userData); pointerdown@(100,100), pointerup@(110,110) → dist≈14.1 ≥5 → attemptStep NIE wywołany
    - Test 7: TEST-04 100-click stress — przesuń store do `currentStepId='odblokuj-estop'`, mock intersectObjects → estop mesh, 100x sekwencja `handlePointerDown+_handlePointerUp` z dist<5. Asercje: `events.filter(e => e.type==='step.done' && e.stepId==='odblokuj-estop').length === 1` ORAZ `currentStepId !== 'odblokuj-estop'` (advansowane). Klikany mesh estop ma userData.kind='manipulation' ale intent.kind=='click' (Opcja A) — engine match przez meshId.
    - Test 8: wrong-mesh violation — store na pierwszym kroku (`sprawdz-tabliczke`), mockowany hit estop, klik → `events` zawiera `step.violation` z `errorCode === 'E-NIEPRAWIDLOWY-MESH'`
  </behavior>
  <action>
Utwórz plik `tests/RaycastController.test.js`:

```js
// tests/RaycastController.test.js
// @vitest-environment node
// Phase 3 — INTERACT-01..05: unit + stress testy RaycastController.
// TEST-04: 100-click stress test mockujący handlePointerDown/Up bez Three.js WebGL (D-Phase3-15).
// Hysteresis 4-tick A→B (D-Phase3-06). Click-vs-drag <5px (D-Phase3-13).
// Wrong-mesh = engine-side violation (D-Phase3-04).
// Intent.kind LITERAŁ 'click' (D-Phase3-03 Opcja A) — NIE userData.kind.

import { describe, it, expect, vi, beforeEach } from 'vitest';
import * as THREE from 'three';
import { RaycastController } from '../src/RaycastController.js';
import { createTrainingStore } from '../src/state/trainingStore.js';
import uruchomienie from '../src/training/scenarios/uruchomienie.js';

/** Mock renderer — bez WebGL, z Pointer event API surface */
function makeMockRenderer() {
  const listeners = {};
  return {
    domElement: {
      addEventListener: vi.fn((name, cb) => { listeners[name] = cb; }),
      removeEventListener: vi.fn(),
      getBoundingClientRect: () => ({ left: 0, top: 0, width: 800, height: 600 }),
      style: { cursor: 'default' },
      _listeners: listeners,
    },
  };
}

/** Mock kamera z updateMatrixWorld (Pitfall 3) */
function makeCamera() {
  const cam = new THREE.PerspectiveCamera(45, 16/9, 0.1, 1000);
  cam.position.set(0, 5, 20);
  cam.updateMatrixWorld();
  return cam;
}

/** Mock mesh z userData identity-only + clone material */
function makeMesh(id, kind, prevEmissiveHex = 0x000000) {
  const geo = new THREE.BoxGeometry(1, 1, 1);
  const mat = new THREE.MeshStandardMaterial({ emissive: prevEmissiveHex });
  const mesh = new THREE.Mesh(geo, mat);
  mesh.userData = { id, kind, restPosition: mesh.position.clone(), labelPL: id, descriptionPL: id };
  return mesh;
}

describe('RaycastController — konstruktor i dispose', () => {
  it('rejestruje 3 listenery (pointermove, pointerdown, pointerup)', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    const interactables = new Map([['estop', makeMesh('estop', 'manipulation')]]);

    const controller = new RaycastController({ renderer, camera, interactables, store });

    expect(renderer.domElement.addEventListener).toHaveBeenCalledTimes(3);
    const calls = renderer.domElement.addEventListener.mock.calls.map(c => c[0]);
    expect(calls).toContain('pointermove');
    expect(calls).toContain('pointerdown');
    expect(calls).toContain('pointerup');
    controller.dispose();
  });

  it('dispose() wywołuje removeEventListener 3x + resetuje state machine (warning #6)', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    const interactables = new Map([['estop', makeMesh('estop', 'manipulation')]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    // Symuluj jakiś pending state
    controller._pendingTarget = interactables.get('estop');
    controller._pendingCount = 1;

    controller.dispose();
    expect(renderer.domElement.removeEventListener).toHaveBeenCalledTimes(3);
    // Warning #6: dispose musi resetować pending state machine
    expect(controller._pendingTarget).toBeNull();
    expect(controller._pendingCount).toBe(0);
  });

  it('dispose() defensywnie restoruje emissive committed hover', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    const mesh = makeMesh('estop', 'manipulation', 0x111111);
    const interactables = new Map([['estop', mesh]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    // Symulujemy committed hover
    vi.spyOn(controller._raycaster, 'intersectObjects').mockReturnValue([{ object: mesh }]);
    controller._pointerDirty = true;
    controller._runHysteresis(16);  // tick 1: pending count=1
    controller._runHysteresis(16);  // sam target, count=2 → commit
    expect(mesh.material.emissive.getHex()).toBe(0x222222);

    controller.dispose();
    expect(mesh.material.emissive.getHex()).toBe(0x111111); // restored
    expect(controller._pendingTarget).toBeNull(); // warning #6
    expect(controller._pendingCount).toBe(0);     // warning #6
  });
});

describe('RaycastController — INTERACT-01 SC1: idle = zero raycaster calls', () => {
  it('_handlePointerMove nie wywołuje intersectObjects', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    const interactables = new Map([['estop', makeMesh('estop', 'manipulation')]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    const spy = vi.spyOn(controller._raycaster, 'intersectObjects');
    // Symuluj pointermove
    controller._handlePointerMove({ clientX: 400, clientY: 300 });
    expect(spy).not.toHaveBeenCalled();
    expect(controller._pointerDirty).toBe(true);
    controller.dispose();
  });

  it('BLOCKER #4 — _runHysteresis(16) bez pointermove (idle) NIE wywołuje intersectObjects', () => {
    // SC1: w stanie idle (brak ruchu myszy) raycaster MUSI być w pełni uśpiony.
    // Przed jakimkolwiek pointermove _pointerDirty===false od konstruktora i nie ma
    // committed targetu → _runHysteresis musi wcześnie wyjść BEZ raycastu.
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    const interactables = new Map([['estop', makeMesh('estop', 'manipulation')]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    const intersectObjectsSpy = vi.spyOn(controller._raycaster, 'intersectObjects');
    // Bez żadnego pointermove — _pointerDirty===false; brak committed target
    controller._runHysteresis(16);
    expect(intersectObjectsSpy).not.toHaveBeenCalled();

    controller.dispose();
  });
});

describe('RaycastController — INTERACT-03: hysteresis 4-tick A→B (D-Phase3-06)', () => {
  it('commit A po 2 tickach, leave A + commit B po 2 tickach z B', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    const meshA = makeMesh('estop', 'manipulation', 0x111111);
    const meshB = makeMesh('wylacznik-glowny', 'manipulation', 0x222200);
    const interactables = new Map([['estop', meshA], ['wylacznik-glowny', meshB]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    const spy = vi.spyOn(controller._raycaster, 'intersectObjects');

    // tick 1: pointermove → A; commit count=1, brak hover
    spy.mockReturnValue([{ object: meshA }]);
    controller._pointerDirty = true;
    controller._runHysteresis(16);
    expect(meshA.material.emissive.getHex()).toBe(0x111111);

    // tick 2: A znowu → count=2 → commit A
    controller._pointerDirty = true;
    controller._runHysteresis(16);
    expect(meshA.material.emissive.getHex()).toBe(0x222222);
    expect(renderer.domElement.style.cursor).toBe('pointer');

    // tick 3: B → reset pending, A wciąż committed
    spy.mockReturnValue([{ object: meshB }]);
    controller._pointerDirty = true;
    controller._runHysteresis(16);
    expect(meshA.material.emissive.getHex()).toBe(0x222222); // A wciąż committed
    expect(meshB.material.emissive.getHex()).toBe(0x222200); // B nie committed yet

    // tick 4: B znowu → count=2 → leave A + commit B
    controller._pointerDirty = true;
    controller._runHysteresis(16);
    expect(meshA.material.emissive.getHex()).toBe(0x111111); // A restored
    expect(meshB.material.emissive.getHex()).toBe(0x222222); // B committed

    controller.dispose();
  });
});

describe('RaycastController — INTERACT-02: click-vs-drag pixel threshold + intent.kind LITERAŁ click (D-Phase3-13, D-Phase3-03 Opcja A)', () => {
  it('pointerup z dist <5px wywołuje attemptStep z {kind:\'click\', meshId} (literał click — NIE userData.kind)', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    // userData.kind='visual-target' celowo — żeby zweryfikować że intent.kind to LITERAŁ 'click', NIE userData.kind
    const mesh = makeMesh('tabliczka-znamionowa', 'visual-target');
    const interactables = new Map([['tabliczka-znamionowa', mesh]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    vi.spyOn(controller._raycaster, 'intersectObjects').mockReturnValue([{ object: mesh }]);
    const spy = vi.spyOn(store.getState(), 'attemptStep');

    controller.handlePointerDown({ clientX: 400, clientY: 300 });
    controller._handlePointerUp({ clientX: 402, clientY: 303 }); // dist ≈ 3.6
    expect(spy).toHaveBeenCalledTimes(1);
    // KLUCZ: intent.kind to literał 'click' — NIE 'visual-target' z userData.kind
    expect(spy).toHaveBeenCalledWith({ kind: 'click', meshId: 'tabliczka-znamionowa' });
    controller.dispose();
  });

  it('pointerup z dist >=5px NIE wywołuje attemptStep (drag)', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    const mesh = makeMesh('tabliczka-znamionowa', 'visual-target');
    const interactables = new Map([['tabliczka-znamionowa', mesh]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    vi.spyOn(controller._raycaster, 'intersectObjects').mockReturnValue([{ object: mesh }]);
    const spy = vi.spyOn(store.getState(), 'attemptStep');

    controller.handlePointerDown({ clientX: 400, clientY: 300 });
    controller._handlePointerUp({ clientX: 410, clientY: 310 }); // dist ≈ 14.1
    expect(spy).not.toHaveBeenCalled();
    controller.dispose();
  });
});

describe('RaycastController — TEST-04 (INTERACT-05): 100-click stress na estop, dokładnie 1 step.done', () => {
  it('100x klik tej samej mesh estop emituje 1 step.done dla aktualnego kroku', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);

    // Przesuń ręcznie store do kroku 'odblokuj-estop' (D-Phase3-15: stub state directly)
    const stepIds = uruchomienie.steps.map(s => s.id);
    const targetIdx = stepIds.indexOf('odblokuj-estop');
    expect(targetIdx).toBeGreaterThanOrEqual(0); // sanity — krok istnieje
    const stepsObj = {};
    for (let i = 0; i < targetIdx; i++) stepsObj[stepIds[i]] = { status: 'done' };
    for (let i = targetIdx; i < stepIds.length; i++) stepsObj[stepIds[i]] = { status: 'pending' };
    store.setState({
      currentStepId: 'odblokuj-estop',
      machineState: 'oczekiwanie-na-inspekcje',
      steps: stepsObj,
    });

    const meshEstop = makeMesh('estop', 'manipulation');
    const interactables = new Map([['estop', meshEstop]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    vi.spyOn(controller._raycaster, 'intersectObjects').mockReturnValue([{ object: meshEstop }]);

    // 100x sekwencja down+up z dist=0
    for (let i = 0; i < 100; i++) {
      controller.handlePointerDown({ clientX: 400, clientY: 300 });
      controller._handlePointerUp({ clientX: 400, clientY: 300 });
    }

    const s = store.getState();
    const doneEvents = s.events.filter(e => e.type === 'step.done' && e.stepId === 'odblokuj-estop');
    expect(doneEvents).toHaveLength(1);
    expect(s.currentStepId).not.toBe('odblokuj-estop'); // advansowane

    controller.dispose();
  });
});

describe('RaycastController — D-Phase3-04: wrong-mesh emituje engine-side violation', () => {
  it('klik estop na pierwszym kroku (wymaga tabliczki) emituje step.violation E-NIEPRAWIDLOWY-MESH', () => {
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie); // currentStepId === 'sprawdz-tabliczke'

    const meshEstop = makeMesh('estop', 'manipulation');
    const interactables = new Map([['estop', meshEstop]]);
    const controller = new RaycastController({ renderer, camera, interactables, store });

    vi.spyOn(controller._raycaster, 'intersectObjects').mockReturnValue([{ object: meshEstop }]);

    controller.handlePointerDown({ clientX: 400, clientY: 300 });
    controller._handlePointerUp({ clientX: 400, clientY: 300 });

    const s = store.getState();
    const violation = s.events.find(e => e.type === 'step.violation');
    expect(violation).toBeDefined();
    expect(violation.errorCode).toBe('E-NIEPRAWIDLOWY-MESH');
    expect(s.currentStepId).toBe('sprawdz-tabliczke'); // NIE advansowane
    controller.dispose();
  });
});
```
  </action>
  <verify>
    <automated>npm test -- --run tests/RaycastController.test.js</automated>
    <acceptance_criteria>
      - Plik `tests/RaycastController.test.js` istnieje (`test -f tests/RaycastController.test.js`)
      - `grep -c "describe(" tests/RaycastController.test.js` zwraca ≥6 (konstruktor, idle SC1, hysteresis, drag, TEST-04, wrong-mesh)
      - `grep -nE "100" tests/RaycastController.test.js | grep -c .` zwraca ≥1 (TEST-04)
      - `grep -nE "for \(let i = 0; i < 100" tests/RaycastController.test.js | grep -c .` zwraca 1 (loop stress test)
      - `grep -nE "E-NIEPRAWIDLOWY-MESH" tests/RaycastController.test.js | grep -c .` zwraca ≥1 (wrong-mesh assertion)
      - `grep -cE "intersectObjectsSpy.*not\.toHaveBeenCalled|spy.*not\.toHaveBeenCalled" tests/RaycastController.test.js` zwraca ≥2 (idle SC1 + pointermove-no-raycast)
      - `grep -cE "kind:\s*['\"]click['\"]" tests/RaycastController.test.js` zwraca ≥1 (asercja literału 'click' w intent shape)
      - `grep -cE "_pendingTarget.*toBeNull|_pendingCount.*toBe\(0\)" tests/RaycastController.test.js` zwraca ≥2 (warning #6 dispose state reset)
      - `npm test -- --run tests/RaycastController.test.js` — wszystkie testy zielone, ≥9 testów
      - Pełna suite: `npm test -- --run` zielona, suma ≥165 testów
    </acceptance_criteria>
  </verify>
  <done>
    `tests/RaycastController.test.js` istnieje, ≥9 testów zielonych, TEST-04 stress test z dokładnie 1 step.done potwierdzony, hysteresis 4-tick A→B potwierdzony, drag<5px discrimination potwierdzony, intent.kind === 'click' literal asercja, idle SC1 (zero raycast calls) potwierdzony, dispose state reset (_pendingTarget/_pendingCount) potwierdzony.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM pointer events → RaycastController | Browser dostarcza zaufane event coords (clientX/Y); brak user-controlled string injection |
| RaycastController → store.attemptStep | Intent shape `{kind:'click', meshId}` z `userData.id` PressModel — kind to literał, meshId z trusted scenario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | mesh.userData (CRIT-7) | mitigate | RaycastController WYŁĄCZNIE odczytuje `userData.id` (NIE userData.kind — Opcja A); nigdy nie zapisuje. Plan 03-01 store + boundaries.test.js TWIN-13 enforce identity-only. |
| T-03-02 | DoS (perf) | per-frame raycast (CRIT-5) | mitigate | Dirty flag pattern + GSAP tickable: max 1 raycast/tick (INTERACT-01). Test "INTERACT-01 SC1 idle" egzekwuje (zero intersectObjects calls bez pointermove). |
| T-03-03 | DoS (memory leak) | event listenery na HMR | mitigate | dispose() removeEventListener 3 listenery + reset _pendingTarget/_pendingCount + Application._unsubscribers (STATE-03). Plan 03-04 wpina dispose. |
| T-03-04 | Tampering | OrbitControls vs RaycastController coupling | accept | Pixel-distance threshold (<5px) — nie manipulujemy `controls.enabled`. Pitfall 4: setPointerCapture nie blokuje listenerów na tym samym domElement. |
</threat_model>

<verification>
- Plik `src/RaycastController.js` egzekwowany przez boundaries.test.js (entry z Plan 03-01) — boundary scanner i Polish literal scanner zielone.
- 1 instancja Raycaster, 3 listenery, dispose pełen + state reset.
- Hysteresis state machine pokryty 4-tick A→B testem.
- Idle SC1 (zero raycast calls bez pointermove) potwierdzony osobnym testem.
- TEST-04 stress test jednoznacznie potwierdza brak race condition (dokładnie 1 step.done).
- Wrong-mesh recordowanie engine-side potwierdzone.
- Intent.kind === 'click' literal egzekwowany w grep gate i dedicated test.
</verification>

<success_criteria>
- INTERACT-01 ✓ (1 Raycaster, dirty-flag throttling 1/tick, zero raycast w idle — testy egzekwują)
- INTERACT-02 ✓ (pointerup<5px → store.attemptStep z poprawnym intent shape: {kind:'click', meshId})
- INTERACT-03 ✓ (hysteresis ≥2 + emissive R-M-R + cursor pointer)
- INTERACT-05 ✓ (TEST-04 100-click → dokładnie 1 step.done; lock isAnimating z Plan 03-01 współpracuje)
</success_criteria>

<output>
Po zakończeniu utwórz `.planning/phases/03-click-to-state-pipeline/03-02-SUMMARY.md`:
- Lokalizacja pliku (`src/RaycastController.js`) + decyzja "top-level vs `src/interaction/`" (rekomendacja: top-level dla v1)
- Wynik TEST-04 (faktyczna liczba step.done events na 100 wywołań)
- Pomiary perf (jeśli dostępne — `intersectObjects(15)` cost; w testach Node nie jest to wymagane)
- Lista wszystkich nowych testów + ich status
- Potwierdzenie Opcji A: intent.kind to literał 'click' w `_handlePointerUp`, NIE `userData.kind`
</output>
