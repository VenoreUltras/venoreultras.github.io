---
phase: 03-click-to-state-pipeline
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - index.html
  - style.css
autonomous: true
requirements: [INTERACT-04]
tags: [dom, css, ui, visual-attest, glassmorphism]
must_haves:
  truths:
    - "DOM zawiera element #phase3-step-readout dla wyświetlenia 'Krok N/M: {labelPL}'"
    - "DOM zawiera element #phase3-attest-container do dynamicznego wstrzyknięcia przycisku visual-attest"
    - "Nowe elementy stylistycznie spójne z istniejącym glassmorphism panelem (.glass-panel, .info-panel)"
  artifacts:
    - path: "index.html"
      provides: "Container elements dla Phase 3 step readout i visual-attest button"
      contains: "phase3-step-readout"
    - path: "style.css"
      provides: "CSS klasy .phase3-readout + .phase3-attest-check spójne z dark glassmorphism theme"
      contains: ".phase3-attest-check"
  key_links:
    - from: "index.html#phase3-step-readout"
      to: "Plan 03-04 store subscriber"
      via: "document.getElementById('phase3-step-readout')"
      pattern: "phase3-step-readout"
    - from: "index.html#phase3-attest-container"
      to: "Plan 03-04 visual-attest button injection"
      via: "container.appendChild(button)"
      pattern: "phase3-attest-container"
---

<objective>
Dodanie minimalnego DOM scaffolding + CSS dla Phase 3:
- `<div id="phase3-step-readout">` w panelu bocznym dla "Krok N/M: {labelPL}" (D-Phase3-12)
- `<div id="phase3-attest-container">` jako anchor do dynamicznego mountu visual-attest button (D-Phase3-09)
- CSS klasy `.phase3-readout` + `.phase3-attest-check` spójne z istniejącym glassmorphism (`.glass-panel`, `.info-panel`)

Phase 4 zastąpi to pełnym `StepPanel` (UI-01) — Phase 3 dostarcza minimum dla happy-path uruchomienia.

Purpose: DOM-only — żadnej logiki JS. Subscribery w `src/main.js` (Plan 03-04) konsumują te elementy. Rozdział pozwala na równoległą pracę z Plan 03-02 (RaycastController).

Output: Zmodyfikowany `index.html` + `style.css`. Brak testów (DOM pure — Phase 3 happy path E2E w Plan 03-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-click-to-state-pipeline/03-CONTEXT.md
@.planning/phases/03-click-to-state-pipeline/03-PATTERNS.md

@index.html
@style.css

<interfaces>
Istniejący panel boczny (`index.html` linie 22-44) ma strukturę:
```html
<div class="control-panel glass-panel">
  <h2>Panel Sterowania</h2>
  <div class="control-group">...</div>  <!-- Status Maszyny -->
  <div class="control-group">...</div>  <!-- RPM slider -->
  <div class="button-group">...</div>   <!-- btn-toggle -->
</div>
<div class="info-panel glass-panel">
  <h2>Parametry Układu</h2>
  ...
</div>
```

Phase 3 dodaje NOWY panel po `info-panel`:
```html
<div id="phase3-panel" class="glass-panel">
  <h2>Procedura szkoleniowa</h2>
  <div id="phase3-step-readout" class="phase3-readout">Procedura zakończona</div>
  <div id="phase3-attest-container"></div>
</div>
```

Subscribery z Plan 03-04 zaktualizują `phase3-step-readout.textContent` przy zmianie `currentStepId` i wstrzykną `<button class="phase3-attest-check">` do `phase3-attest-container`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodanie DOM scaffolding w index.html — Phase 3 panel z readout + attest container</name>
  <read_first>
    - index.html (cały — patrz strukturę panelu bocznego linie 16-58)
    - .planning/phases/03-click-to-state-pipeline/03-CONTEXT.md (D-Phase3-09 visual-attest button, D-Phase3-12 step readout)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `index.html` + `style.css` — DOM element pattern)
  </read_first>
  <action>
W `index.html`, PO zamykającym `</div>` panelu `.info-panel` (po linii 57, przed `</div>` zamykającym `#ui-layer` na linii 58), dodaj nowy panel:

```html
      <div id="phase3-panel" class="glass-panel">
        <h2>Procedura szkoleniowa</h2>
        <div id="phase3-step-readout" class="phase3-readout">Procedura zakończona</div>
        <div id="phase3-attest-container" class="phase3-attest-container"></div>
      </div>
```

Uwaga: NIE używamy `class="info-panel glass-panel"` bo `info-panel` ma własne layouty dla `params-list`. Nowy panel używa tylko `glass-panel` jako shared shell.

Pozostawiamy domyślny tekst "Procedura zakończona" jako fallback dla momentu przed startem scenariusza (subscriber Plan 03-04 nadpisze gdy `currentStepId !== null`).

Polski tekst "Procedura szkoleniowa" + "Procedura zakończona" jest w `index.html` (NIE plik JS), więc UI-06 boundary scanner nie egzekwuje (skanuje tylko pliki `.js` w `src/`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf-8'); const must=['phase3-step-readout','phase3-attest-container','phase3-panel','glass-panel']; for(const m of must){ if(!html.includes(m)) { console.error('Missing:',m); process.exit(1); } } console.log('OK index.html');"</automated>
    <acceptance_criteria>
      - `grep -c 'id="phase3-step-readout"' index.html` zwraca 1
      - `grep -c 'id="phase3-attest-container"' index.html` zwraca 1
      - `grep -c 'id="phase3-panel"' index.html` zwraca 1
      - `grep -c 'class="glass-panel"' index.html` zwraca ≥3 (control-panel + info-panel + phase3-panel)
      - HTML jest valid — brak unclosed `<div>` (sprawdzić visual review że nesting prawidłowy)
    </acceptance_criteria>
  </verify>
  <done>
    `index.html` zawiera nowy `#phase3-panel` z `#phase3-step-readout` i `#phase3-attest-container`. Subscriber z Plan 03-04 może je odnaleźć przez `document.getElementById`.
  </done>
</task>

<task type="auto">
  <name>Task 2: CSS dla nowego Phase 3 panelu — spójny z dark glassmorphism theme</name>
  <read_first>
    - style.css (cały — referencja do istniejących klas .glass-panel, .info-panel, .control-panel, .btn, .params-list)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (CSS glassmorphism spójność pattern)
  </read_first>
  <action>
Na końcu `style.css` dodaj sekcję Phase 3 (wzoruj się na istniejących klasach `.glass-panel`, `.btn` — sprawdź padding/border-radius/colors w pliku):

```css
/* Phase 3 — INTERACT-04: visual-attest checkbox + step readout */
/* Spójne z dark glassmorphism — dziedziczy z .glass-panel */

#phase3-panel {
  /* nic dodatkowego — .glass-panel już dostarcza tła, blur, border */
}

.phase3-readout {
  font-size: 0.95rem;
  font-weight: 400;
  opacity: 0.92;
  margin-bottom: 0.75rem;
  padding: 0.5rem 0.75rem;
  background: rgba(255, 255, 255, 0.04);
  border-left: 3px solid rgba(0, 158, 115, 0.6);  /* Wong success #009E73 — Phase 4 użyje pełnego pulse */
  border-radius: 4px;
  font-family: 'Inter', sans-serif;
  letter-spacing: 0.01em;
}

.phase3-attest-container {
  /* Pusty container — przycisk wstrzykiwany dynamicznie przez subscriber */
  min-height: 0;
}

.phase3-attest-check {
  display: block;
  width: 100%;
  padding: 0.65rem 1rem;
  font-family: 'Inter', sans-serif;
  font-size: 0.9rem;
  font-weight: 600;
  color: #ffffff;
  background: rgba(0, 158, 115, 0.18);  /* Wong success tint — Phase 4 doda full state-driven color */
  border: 1px solid rgba(0, 158, 115, 0.5);
  border-radius: 6px;
  cursor: pointer;
  transition: background 0.15s ease, border-color 0.15s ease;
  letter-spacing: 0.01em;
}

.phase3-attest-check:hover {
  background: rgba(0, 158, 115, 0.32);
  border-color: rgba(0, 158, 115, 0.8);
}

.phase3-attest-check:active {
  background: rgba(0, 158, 115, 0.45);
}

.phase3-attest-check:focus-visible {
  outline: 2px solid rgba(0, 158, 115, 0.9);
  outline-offset: 2px;
}
```

Wong success `#009E73` użyty zamiast generic green — zgodne z paletą redundancji (FEEDBACK-04, Phase 4). Phase 3 statyczny preview; Phase 4 będzie zmieniał kolor dynamicznie per state.

Tekstu inline w CSS (np. `content:`) nie dodajemy — wszystkie polskie stringi są w HTML lub generowane z JS przez subscribers.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const css=fs.readFileSync('style.css','utf-8'); const must=['.phase3-readout','.phase3-attest-check','.phase3-attest-container','#phase3-panel']; for(const m of must){ if(!css.includes(m)) { console.error('Missing:',m); process.exit(1); } } console.log('OK style.css');"</automated>
    <acceptance_criteria>
      - `grep -c "\.phase3-readout" style.css` zwraca ≥1
      - `grep -c "\.phase3-attest-check" style.css` zwraca ≥4 (base + :hover + :active + :focus-visible)
      - `grep -c "\.phase3-attest-container" style.css` zwraca ≥1
      - `grep -c "#009E73\|009e73" style.css` zwraca ≥2 (Wong success palette dla Phase 3 button)
      - Brak warningów w CSS (visual review — selector matching, no syntax errors)
      - `npm test -- --run` pozostaje zielony (CSS nie wpływa na testy)
    </acceptance_criteria>
  </verify>
  <done>
    `style.css` zawiera klasy Phase 3 spójne z istniejącym glassmorphism. Wong palette używana statycznie; Phase 4 może budować na tej bazie pełny state-driven encoding.
  </done>
</task>

</tasks>

<verification>
- Plan 03-03 nie modyfikuje żadnego `.js` — wszystkie pliki to DOM/CSS.
- Subscriber w Plan 03-04 odczyta `#phase3-step-readout` i `#phase3-attest-container` przez `document.getElementById`.
- Wave 1 parallel-safe: brak konfliktu z Plan 03-02 (RaycastController) ani Plan 03-01 (store).
</verification>

<success_criteria>
- INTERACT-04 (DOM scaffolding) ✓ — Plan 03-04 wstrzykuje JS subscribery konsumujące te elementy.
- Glassmorphism spójność zachowana — Wong success palette (#009E73) zgodna z Phase 4 FEEDBACK-04.
- Brak regresji — `npm test` zielony bez zmian.
</success_criteria>

<output>
Po zakończeniu utwórz `.planning/phases/03-click-to-state-pipeline/03-03-SUMMARY.md`:
- Lokalizacja nowego panelu w `index.html` (po `.info-panel`)
- Lista nowych klas CSS i ich semantyka (.phase3-readout = readout aktualnego kroku; .phase3-attest-check = visual-attest button; etc.)
- Decyzję o użyciu Wong palette (#009E73) statycznie w Phase 3 (Phase 4 zastąpi state-driven)
</output>
