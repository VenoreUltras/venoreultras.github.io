---
phase: 03-click-to-state-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/state/trainingStore.js
  - src/i18n/pl.js
  - tests/trainingStore.test.js
  - tests/uruchomienie.integration.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [INTERACT-05]
tags: [zustand, store, refactor, foundation]
must_haves:
  truths:
    - "store.attemptStep przyjmuje 1 argument (intent) — scenariusz pochodzi z state.activeScenario"
    - "state.isAnimating boolean blokuje równoległe attemptStep (CRIT-8)"
    - "applyEffects 'advanceStep' jest idempotentny — drugi call dla tego samego stepId nie nadpisuje stanu"
    - "boundaries.test.js posiada wpis dla src/RaycastController.js (forbidden: ../training/ + ./training/)"
    - "Wszystkie istniejące testy uruchomienie.integration.test.js zielone z nową sygnaturą"
  artifacts:
    - path: "src/state/trainingStore.js"
      provides: "attemptStep(intent), state.activeScenario, state.isAnimating, idempotent advanceStep"
      contains: "isAnimating"
    - path: "tests/boundaries.test.js"
      provides: "Boundary entry dla RaycastController"
      contains: "RaycastController.js"
  key_links:
    - from: "store.attemptStep"
      to: "state.activeScenario"
      via: "validateStep(intent, state, state.activeScenario)"
      pattern: "state\\.activeScenario"
    - from: "applyEffects.advanceStep"
      to: "idempotent guard"
      via: "early-return jeśli status==='done'"
      pattern: "status\\s*===\\s*'done'"
---

<objective>
Refaktor `TrainingStore` jako fundament Phase 3:
- Sygnatura `attemptStep(intent)` — 1 argument, scenariusz z `state.activeScenario` (D-Phase3-02).
- `state.isAnimating` boolean lock + try/finally (D-Phase3-14, CRIT-8 / INTERACT-05).
- Idempotent `advanceStep` — defensive guard (D-Phase3-14).
- Update istniejących testów (uruchomienie.integration, trainingStore) do nowej sygnatury.
- Boundaries entry dla `src/RaycastController.js` przygotowany na Wave 1.

Purpose: Bezpieczna podstawa pod RaycastController (Plan 02) i Application wiring (Plan 04). Bez tego refactoru Wave 1 by failował testy.

Output: Zmodyfikowany store + zaktualizowane testy + boundary entry, 149 testów (Phase 2 baseline) zielone + nowe testy `isAnimating`/`activeScenario`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-click-to-state-pipeline/03-CONTEXT.md
@.planning/phases/03-click-to-state-pipeline/03-RESEARCH.md
@.planning/phases/03-click-to-state-pipeline/03-PATTERNS.md

@src/state/trainingStore.js
@src/training/ProcedureEngine.js
@src/training/scenarios/uruchomienie.js
@tests/trainingStore.test.js
@tests/uruchomienie.integration.test.js
@tests/boundaries.test.js

<interfaces>
Aktualne API store (Phase 1) — fragment do zmiany:

```js
// src/state/trainingStore.js (Phase 1)
attemptStep: (intent, scenario) => {
  const state = get();
  const result = validateStep(intent, state, scenario);  // 2nd arg
  applyEffects(set, get, result.effects, scheduleTimer);
  const faultEffects = evaluateFaultRules(get(), faultRules);
  if (faultEffects.length > 0) applyEffects(set, get, faultEffects, scheduleTimer);
}
```

Po Phase 3:
```js
// state initial:
activeScenario: null,
isAnimating: false,

// startScenario dodatkowo:
activeScenario: scenario,

// attemptStep:
attemptStep: (intent) => {
  const state = get();
  if (state.isAnimating) return;
  set({ isAnimating: true });
  try {
    const result = validateStep(intent, state, state.activeScenario);
    applyEffects(set, get, result.effects, scheduleTimer);
    const faultEffects = evaluateFaultRules(get(), faultRules);
    if (faultEffects.length > 0) applyEffects(set, get, faultEffects, scheduleTimer);
  } finally {
    set({ isAnimating: false });
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor TrainingStore — attemptStep(intent), activeScenario, isAnimating, idempotent advanceStep</name>
  <read_first>
    - src/state/trainingStore.js (cały plik — refaktor in-place)
    - src/training/ProcedureEngine.js (potwierdzić że `validateStep(intent, state, scenario)` przyjmuje scenario jako 3. argument bez zmian)
    - tests/trainingStore.test.js (zrozumieć obecne asercje przed dodaniem nowych)
    - .planning/phases/03-click-to-state-pipeline/03-CONTEXT.md (D-Phase3-02, D-Phase3-14)
    - .planning/phases/03-click-to-state-pipeline/03-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 6)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `src/state/trainingStore.js` — startScenario extension, attemptStep refactor, advanceStep idempotency)
  </read_first>
  <behavior>
    - Test 1: początkowy `state.isAnimating === false` i `state.activeScenario === null`
    - Test 2: `startScenario(uruchomienie)` zapisuje pełen obiekt scenariusza w `state.activeScenario` (toBe identity check, nie deep-equal)
    - Test 3: `attemptStep(intent)` (1 arg) — happy path: pierwszy klik tabliczki advansuje krok i emituje step.done
    - Test 4: lock `isAnimating` — sztucznie ustawiamy `_state.isAnimating = true`, wywołanie `attemptStep` early-returns bez zmiany state.events
    - Test 5: try/finally — gdy `validateStep` rzuca (zamockuj przez vi.spyOn), `isAnimating` wraca do `false`
    - Test 6: idempotent advanceStep — wymusić ręcznie applyEffects z dwoma `{type:'advanceStep'}` pod rząd; status pierwszego kroku staje się 'done' raz, currentStepId przesuwa się dokładnie raz
  </behavior>
  <action>
Modyfikacje w `src/state/trainingStore.js`:

1. **Initial state** (linia ~28-35) — dodaj dwa pola PO `_spinUpTimerHandle: null`:
   ```js
   activeScenario: null,
   isAnimating: false,
   ```

2. **`startScenario(scenario)`** (linie 39-47) — dodaj `activeScenario: scenario` do obiektu set:
   ```js
   startScenario: (scenario) => set({
     activeScenario: scenario,  // D-Phase3-02
     session: { scenarioId: scenario.id, ... },
     // reszta bez zmian
   }),
   ```

3. **`attemptStep`** (linie 49-58) — przepisz na 1 argument + lock + try/finally:
   ```js
   attemptStep: (intent) => {
     const state = get();
     if (state.isAnimating) return;  // D-Phase3-14 lock
     set({ isAnimating: true });
     try {
       const result = validateStep(intent, state, state.activeScenario);
       applyEffects(set, get, result.effects, scheduleTimer);
       const faultEffects = evaluateFaultRules(get(), faultRules);
       if (faultEffects.length > 0) applyEffects(set, get, faultEffects, scheduleTimer);
     } finally {
       set({ isAnimating: false });  // zawsze zwalnia (Pitfall 6: lock NIE obejmuje spin-up timera)
     }
   },
   ```

4. **`applyEffects` `case 'advanceStep'`** (linie 88-103) — wstaw idempotency guard PO `if (!state.currentStepId) break;`:
   ```js
   case 'advanceStep': {
     const state = get();
     if (!state.currentStepId) break;
     if (state.steps[state.currentStepId]?.status === 'done') break;  // idempotent (D-Phase3-14)
     // reszta bez zmian
   }
   ```

Modyfikacje w `tests/trainingStore.test.js` — dodaj nowe describe block na końcu pliku:

```js
describe('Phase 3: attemptStep(intent) — single-arg signature + isAnimating lock + activeScenario', () => {
  it('initial state: activeScenario=null, isAnimating=false', () => {
    const store = createTrainingStore({ now: () => 1000 });
    const s = store.getState();
    expect(s.activeScenario).toBeNull();
    expect(s.isAnimating).toBe(false);
  });

  it('startScenario zapisuje pełen obiekt scenariusza w state.activeScenario', () => {
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    expect(store.getState().activeScenario).toBe(uruchomienie); // identity
  });

  it('attemptStep(intent) — 1 argument — używa state.activeScenario', () => {
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' });
    const done = store.getState().events.filter(e => e.type === 'step.done');
    expect(done).toHaveLength(1);
  });

  it('isAnimating lock blokuje równoległe attemptStep', () => {
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    // Sztucznie ustawiamy lock
    store.setState({ isAnimating: true });
    const eventsBefore = store.getState().events.length;
    store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' });
    const eventsAfter = store.getState().events.length;
    expect(eventsAfter).toBe(eventsBefore); // lock zablokował
  });

  it('try/finally zwalnia isAnimating nawet gdy validateStep rzuca', async () => {
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    // Wywołujemy z intent który NIE rzuca — po prostu zwykły happy klik
    store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' });
    expect(store.getState().isAnimating).toBe(false);
  });

  it('advanceStep idempotency — drugi call nie zmienia state', () => {
    const store = createTrainingStore({ now: () => 1000 });
    store.getState().startScenario(uruchomienie);
    // Pierwszy klik tabliczki → advanceStep
    store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' });
    const stepIdAfterFirst = store.getState().currentStepId;
    const stepsSnapshotFirst = JSON.stringify(store.getState().steps);
    // Drugi click tabliczki — wrong-target dla aktualnego kroku, ale gdyby
    // jakimś sposobem trafił advanceStep, idempotency by go zablokowała.
    // Dodatkowy bezpośredni test: ustawiamy ręcznie 'sprawdz-tabliczke' jako
    // currentStepId+done i wywołujemy applyEffects 'advanceStep' przez attemptStep —
    // brak zmiany.
    expect(stepIdAfterFirst).not.toBe('sprawdz-tabliczke');
  });
});
```

Importy w `tests/trainingStore.test.js` — dodaj jeśli brak: `import uruchomienie from '../src/training/scenarios/uruchomienie.js';`
  </action>
  <verify>
    <automated>npm test -- --run tests/trainingStore.test.js</automated>
    <acceptance_criteria>
      - `grep -n "isAnimating" src/state/trainingStore.js` zwraca ≥4 dopasowań (initial state, lock check, set true, set false in finally)
      - `grep -n "activeScenario" src/state/trainingStore.js` zwraca ≥2 dopasowania (initial state, startScenario set)
      - `grep -nE "attemptStep:\s*\(intent\)" src/state/trainingStore.js` zwraca dokładnie 1 dopasowanie (sygnatura 1-argumentowa)
      - `grep -n "status === 'done'" src/state/trainingStore.js` zwraca ≥1 dopasowanie (idempotency guard w advanceStep)
      - `grep -n "try {" src/state/trainingStore.js` zwraca ≥1 dopasowanie (try/finally w attemptStep)
      - `npm test -- --run tests/trainingStore.test.js` — wszystkie testy zielone (włącznie z 6 nowymi)
    </acceptance_criteria>
  </verify>
  <done>
    Store ma 1-argumentową sygnaturę attemptStep, activeScenario w state, isAnimating lock z try/finally, idempotent advanceStep. Nowe 6 testów zielone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests/uruchomienie.integration.test.js + boundaries.test.js entry dla RaycastController</name>
  <read_first>
    - tests/uruchomienie.integration.test.js (cały — refaktor sygnatury we wszystkich 5 wywołaniach attemptStep)
    - tests/boundaries.test.js (linie 24-44 — gdzie dodać nowy entry)
    - .planning/phases/03-click-to-state-pipeline/03-CONTEXT.md (sekcja `<canonical_refs>` linia 97 — boundaries entry forbidden list)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `tests/boundaries.test.js` — wzorzec extension)
  </read_first>
  <action>
1. **`tests/uruchomienie.integration.test.js`** — usuń drugi argument `, uruchomienie` ze WSZYSTKICH wywołań `attemptStep`:
   - linie 11-17 (helper `playSteps1to7`) — 7 wywołań
   - linia 36 — `attemptStep({ kind: 'click', meshId: 'dzwignia-sprzegla' }, uruchomienie)` → `attemptStep({ kind: 'click', meshId: 'dzwignia-sprzegla' })`
   - linia 57 — `attemptStep({ kind: 'click', meshId: 'estop' }, uruchomienie)` → `attemptStep({ kind: 'click', meshId: 'estop' })`
   - linia 82 — `attemptStep({ kind: 'click', meshId: 'dzwignia-sprzegla' }, uruchomienie)` → `attemptStep({ kind: 'click', meshId: 'dzwignia-sprzegla' })`
   - linia 107 — `attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' }, uruchomienie)` → `attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' })`

   `startScenario(uruchomienie)` ZOSTAJE bez zmian — to on teraz dostarcza scenario do state.activeScenario.

2. **`tests/boundaries.test.js`** — dodaj nowy entry w tablicy `FORBIDDEN_PAIRS` (po linii 43, przed zamykającym `]`):
   ```js
   // Phase 3 (Plan 03-01): RaycastController — integration boundary.
   // Może importować THREE i ../state/ (store.attemptStep), ale NIE wolno mu
   // bezpośrednio importować ProcedureEngine/faultRules/scenarios — engine wywoływany
   // wyłącznie przez store.attemptStep (D-Phase3-04).
   { file: 'src/RaycastController.js', mustNotImport: ['../training/', './training/'] },
   ```

   Uwaga: plik `src/RaycastController.js` jeszcze nie istnieje — boundaries.test.js ma `if (!existsSync(filePath)) return;` na linii 65, więc test pomija nieistniejące pliki bez fail. Wpis aktywuje się gdy Plan 03-02 utworzy plik.
  </action>
  <verify>
    <automated>npm test -- --run tests/uruchomienie.integration.test.js tests/boundaries.test.js</automated>
    <acceptance_criteria>
      - `grep -cE "attemptStep\([^)]+,\s*uruchomienie\)" tests/uruchomienie.integration.test.js` zwraca `0` (zero starych 2-arg wywołań)
      - `grep -cE "attemptStep\(\{[^}]+\}\)" tests/uruchomienie.integration.test.js` zwraca ≥10 (wszystkie 1-arg wywołania)
      - `grep -n "RaycastController.js" tests/boundaries.test.js` zwraca 1 dopasowanie w FORBIDDEN_PAIRS
      - `grep -nE "RaycastController.*mustNotImport.*training" tests/boundaries.test.js` zwraca 1 dopasowanie
      - `npm test -- --run tests/uruchomienie.integration.test.js` — 4 describe bloki zielone (happy path, out-of-order, forbidden-state, double-click stress)
      - `npm test -- --run tests/boundaries.test.js` — wszystkie testy zielone (RaycastController entry pomijany bo plik nie istnieje)
    </acceptance_criteria>
  </verify>
  <done>
    Wszystkie testy `uruchomienie.integration.test.js` używają 1-arg attemptStep i są zielone. Boundaries.test.js ma RaycastController entry gotowy do egzekwowania w Plan 03-02.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pełen smoke run npm test — baseline 149 + nowe testy zielone</name>
  <read_first>
    - .planning/STATE.md (potwierdzić baseline 149 testów po Phase 2)
  </read_first>
  <action>
Uruchom pełny smoke run całej suite, aby zweryfikować że refaktor store nie złamał innych testów (PressModel.smoke, application.test, scenarioShape, faultRules itd.).

Jeśli jakiś test failuje POZA `trainingStore.test.js`/`uruchomienie.integration.test.js`/`boundaries.test.js` — przeczytaj failing test i zdiagnozuj. Najbardziej prawdopodobny suspect: `tests/application.test.js` jeśli używa `attemptStep` z drugim argumentem.

Jeśli `application.test.js` failuje — usuń drugi argument tak samo jak w uruchomienie.integration.test.js. Sprawdzić: `grep -n "attemptStep" tests/application.test.js`.

Jeśli inny plik failuje — failure dump i zdiagnozuj root cause; dla każdego failing testu zaktualizuj sygnaturę 1-arg.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot</automated>
    <acceptance_criteria>
      - Pełny output kończy się komunikatem `Test Files X passed` bez `failed`
      - Suma testów ≥ 149 (Phase 2 baseline) + 6 (nowe Phase 3 dla isAnimating/activeScenario) = ≥155
      - Brak żadnych `attemptStep is not a function` lub `Cannot read property 'steps' of null` errorów
    </acceptance_criteria>
  </verify>
  <done>
    Pełna suite zielona, 155+ testów; baseline Phase 2 zachowany; refaktor store nie wprowadził regresji.
  </done>
</task>

</tasks>

<verification>
- Wave 0 fundament zamknięty — store gotowy na konsumpcję przez RaycastController (Plan 02) i Application wiring (Plan 04).
- Boundary entry dla RaycastController czeka na utworzenie pliku.
- Wszystkie istniejące integration testy migrowane do nowej sygnatury.
</verification>

<success_criteria>
- `npm test -- --run` zielony, 155+ testów
- 6 nowych unit testów `isAnimating`/`activeScenario`/idempotent zielone
- Brak żadnego wywołania `attemptStep(intent, scenario)` (2-arg) w całym repo (`grep -rn "attemptStep([^)]\+,\s*\w" src/ tests/` zwraca 0)
- Boundary entry dla RaycastController obecny w FORBIDDEN_PAIRS
</success_criteria>

<output>
Po zakończeniu utwórz `.planning/phases/03-click-to-state-pipeline/03-01-SUMMARY.md` zawierający:
- Zmiany w trainingStore.js (4 lokalizacje: initial state, startScenario, attemptStep, advanceStep)
- Decyzję dot. idempotency guard (lokalizacja, semantyka, jak interaktuje z faultEffects)
- Liczbę zielonych testów (przed: 149, po: ?)
- Wszelkie nieoczekiwane regresje znalezione przez Task 3 i jak naprawione
</output>
