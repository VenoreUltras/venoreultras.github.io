---
phase: 03-click-to-state-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - tests/phase3.e2e.test.js
autonomous: false
requirements: [INTERACT-02, INTERACT-04, INTERACT-05]
tags: [e2e, smoke, integration, manual-verify, happy-path]
must_haves:
  truths:
    - "End-to-end test: 8 kroków uruchomienia advansuje w storze + DOM aktualizuje się po każdym (mockowane click przez RaycastController._handlePointerUp + visual-attest button click)"
    - "Po wszystkich 8 krokach: machineState='w-cyklu', currentStepId=null, scoring.score=100, events ma 8 step.done"
    - "DOM #phase3-step-readout pokazuje 'Procedura zakończona' po ostatnim kroku"
    - "DOM #status-text pokazuje 'W cyklu — 100/100'"
    - "Brak step.violation w events (happy path)"
    - "Visual-attest button emituje intent {kind:'check', stepId} (Opcja A z Pitfall 2)"
    - "Manipulation/visual-target click przez RaycastController emituje intent {kind:'click', meshId} (literał 'click', D-Phase3-03 Opcja A)"
    - "MANUAL CHECKPOINT: 60 FPS na zintegrowanej grafice + visual hover hint w przeglądarce"
  artifacts:
    - path: "tests/phase3.e2e.test.js"
      provides: "End-to-end smoke test wireup całego Phase 3 — RaycastController + store + DOM subscribers + visual-attest button"
      min_lines: 80
      contains: "8"
  key_links:
    - from: "Mock pointer event sequence"
      to: "RaycastController._handlePointerUp"
      via: "controller.handlePointerDown + _handlePointerUp z mockowanym intersectObjects per krok → intent {kind:'click', meshId}"
      pattern: "_handlePointerUp"
    - from: "Visual-attest button click"
      to: "store.attemptStep"
      via: "button DOM click → wirePhase3 listener → {kind:'check', stepId}"
      pattern: "kind:\\s*['\"]check['\"]"
    - from: "store events log"
      to: "DOM rendering via subscribers"
      via: "wirePhase3 helper (kopia logiki Application._wireStoreSubscribers)"
      pattern: "step\\.done"
---

<objective>
End-to-end smoke test pokrywający Phase 3 SC3:
> "The first end-to-end happy path (Phase 1's `uruchomienie` scenario) is now fully playable in the browser: hover lights the target, click advances the step, scoring updates, status badge updates."

Test odgrywa pełen happy path scenariusza `uruchomienie` (8 kroków, mix manipulation + visual-attest) przez:
- 6 manipulation/visual-target kliknięć symulowanych przez `controller._handlePointerUp` z mockowanym `intersectObjects` (intent.kind to LITERAŁ 'click' per D-Phase3-03 Opcja A)
- 2 visual-attest kliknięcia symulowane przez `attest button.click()` w DOM (intent.kind:'check' per Pitfall 2 Opcja A)
- Spin-up timer (3000ms) — `vi.useFakeTimers` + `advanceTimersByTime`

Asercje:
- Każdy krok advansuje (`currentStepId` zmienia się sekwencyjnie)
- Każdy step.done emitowany dokładnie raz
- DOM `#phase3-step-readout` aktualizuje się po każdym kroku
- DOM `#status-text` pokazuje "W cyklu — 100/100" na końcu
- Brak violation events
- Po ostatnim kroku — `Procedura zakończona` w readout

**MANUAL CHECKPOINT:** Po automated tests zielone — uruchom `npm run dev` i ręcznie zweryfikuj 60 FPS oraz visual hover hint w przeglądarce (jsdom nie renderuje WebGL).

Output: 1 nowy plik testów E2E + manual sign-off przez deweloperа.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-click-to-state-pipeline/03-CONTEXT.md
@.planning/phases/03-click-to-state-pipeline/03-VALIDATION.md
@.planning/phases/03-click-to-state-pipeline/03-04-SUMMARY.md

@src/RaycastController.js
@src/state/trainingStore.js
@src/training/scenarios/uruchomienie.js
@tests/uruchomienie.integration.test.js
@tests/RaycastController.test.js

<interfaces>
Sekwencja 8 kroków `uruchomienie` (z `src/training/scenarios/uruchomienie.js` — należy potwierdzić PRZED implementacją testu):

1. `sprawdz-tabliczke` — visual-target — click tabliczki (RaycastController emituje intent {kind:'click'})
2. `kontrola-narzedzia` — visual-attest — button click ({kind:'check', stepId})
3. `kontrola-wzrokowa` — visual-attest — button click
4. `wziernik-smarowania` — visual-target — click wziernika
5. `oslona-przednia` — manipulation — click osłony
6. `odblokuj-estop` — manipulation — click estop
7. `wlacz-zasilanie` — manipulation — click wyłącznika → triggers spin-up timer 3000ms
8. (po 3000ms machineState='gotowa-do-pracy')
9. `sprzegnij-po-rozpedzie` — manipulation — click dźwigni sprzęgła → machineState='w-cyklu'

Reset asercji: po Step 9 `currentStepId === null`, `events.filter(e=>e.type==='step.done').length === 8`, `scoring.score === 100`.

Uwaga: lista kroków powyżej jest BEST-EFFORT z `tests/uruchomienie.integration.test.js` linie 9-18. Test w tym planie MUSI najpierw przeczytać `src/training/scenarios/uruchomienie.js` aby potwierdzić dokładne id kroków i ich `kind`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/phase3.e2e.test.js — pełen happy-path uruchomienia 8/8 kroków przez RaycastController + visual-attest button</name>
  <read_first>
    - src/training/scenarios/uruchomienie.js (cały — POTWIERDŹ dokładną sekwencję 8 kroków + kind każdego)
    - src/RaycastController.js (Plan 03-02 — handlePointerDown/_handlePointerUp API, intent.kind to literał 'click')
    - src/main.js (Plan 03-04 — Application konstruktor + _renderStepAndAttest button rendering)
    - tests/uruchomienie.integration.test.js (linie 20-49 — wzorzec happy path bez DOM/RaycastController; linie 9-18 — sekwencja kroków)
    - tests/RaycastController.test.js (Plan 03-02 — wzorzec mock renderer + intersectObjects spy)
    - tests/application.test.js (Plan 03-04 Task 2 — wzorzec setup DOM + Application instancja)
    - .planning/phases/03-click-to-state-pipeline/03-VALIDATION.md (Phase 3 SC3 — happy path)
  </read_first>
  <behavior>
    - Setup: jsdom DOM kompletny (wszystkie elementy z Plan 03-03 + base UI z Plan 01-04), Application instancja LUB ręczny wirePhase3, fake timers
    - Helper `simulateClickOnMesh(controller, mesh)` — sequence: handlePointerDown(@400,300) + _handlePointerUp(@400,300) z spied intersectObjects → [{object: mesh}]; RaycastController emituje intent {kind:'click', meshId} (D-Phase3-03 Opcja A)
    - Helper `clickAttestButton()` — `document.querySelector('.phase3-attest-check').click()`; wirePhase3 listener emituje intent {kind:'check', stepId} (Pitfall 2 Opcja A)
    - Replay 8-step sequence z 1 advanceTimers(3000) między step 7 i 8
    - Po każdym kroku assert: `store.currentStepId` zmieniło się + DOM readout zawiera "Krok N+1" lub "Procedura zakończona"
    - Final assert: 8 step.done, 0 step.violation, scoring.score=100, machineState='w-cyklu', currentStepId=null
  </behavior>
  <action>
Utwórz `tests/phase3.e2e.test.js`:

```js
// tests/phase3.e2e.test.js
// @vitest-environment jsdom
// Phase 3 SC3 (E2E happy path): pełen scenariusz uruchomienia 8/8 kroków przez
// RaycastController + visual-attest button + DOM subscribers. Symulujemy click
// pointer events bez WebGL — vi.spyOn na intersectObjects.
//
// Test zasłania potencjalne luki w Plan 03-04 wiringu — jeśli któryś subscriber
// nie zarejestrowany lub button nie wstrzykiwany, ten test failuje.

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import * as THREE from 'three';
import { createTrainingStore } from '../src/state/trainingStore.js';
import { RaycastController } from '../src/RaycastController.js';
import uruchomienie from '../src/training/scenarios/uruchomienie.js';
import { pl } from '../src/i18n/pl.js';

/** Mock renderer (jak w RaycastController.test.js) */
function makeMockRenderer() {
  const listeners = {};
  return {
    domElement: {
      addEventListener: vi.fn((name, cb) => { listeners[name] = cb; }),
      removeEventListener: vi.fn(),
      getBoundingClientRect: () => ({ left: 0, top: 0, width: 800, height: 600 }),
      style: { cursor: 'default' },
      _listeners: listeners,
    },
  };
}

function makeCamera() {
  const cam = new THREE.PerspectiveCamera(45, 16/9, 0.1, 1000);
  cam.position.set(0, 5, 20);
  cam.updateMatrixWorld();
  return cam;
}

/** Mock mesh z stable userData identity */
function makeMesh(id, kind) {
  const geo = new THREE.BoxGeometry(1, 1, 1);
  const mat = new THREE.MeshStandardMaterial({ emissive: 0x000000 });
  const mesh = new THREE.Mesh(geo, mat);
  mesh.userData = { id, kind, restPosition: mesh.position.clone(), labelPL: id, descriptionPL: id };
  return mesh;
}

/**
 * wirePhase3 — helper E2E test odpowiadający Application._wireStoreSubscribers (z Plan 03-04)
 * + _renderStatusText + _renderStepAndAttest, ale zaaplikowany ręcznie zamiast importowania
 * całego src/main.js (Application).
 *
 * ŚWIADOMY TRADE-OFF (warning #7):
 * Decyzja: kopiujemy logikę inline zamiast importować Application z src/main.js, ponieważ:
 *  1. Application konstruktor instancjuje SceneSetup → THREE.WebGLRenderer, który w jsdom
 *     wymaga albo vi.mock('../src/SceneSetup.js') albo canvas-mock library.
 *  2. Test E2E ma pokazać że WIRING (subscribery + button intent shape + DOM updates) działa
 *     end-to-end od mockowanych eventów do DOM. Nie testuje SceneSetup/WebGL — to robi
 *     Plan 03-04 Task 2 (tests/application.test.js).
 *  3. Kopia ~30 linii pozwala na pełną izolację E2E od Three.js renderera bez zależności
 *     od konfiguracji vi.mock w innym pliku.
 *
 * ALTERNATYWA (do rozważenia przez executora): jeśli Plan 03-04 Task 2 ustawił działający
 * `vi.mock('../src/SceneSetup.js')` na poziomie pliku, executor MOŻE uprościć ten test do
 * `import { Application } from '../src/main.js'` + reuse mocka. Decyzja executora —
 * obie ścieżki są poprawne.
 */
function wirePhase3({ store, statusTextEl, stepReadoutEl, attestContainerEl }) {
  const unsubscribers = [];

  function renderStatusText() {
    const state = store.getState();
    const label = pl.machineState[state.machineState] ?? state.machineState;
    statusTextEl.textContent = `${label} — ${state.scoring.score}/100`;
  }

  function renderStepAndAttest(currentStepId) {
    const activeScenario = store.getState().activeScenario;
    if (!activeScenario || !currentStepId) {
      stepReadoutEl.textContent = pl.ui?.procedureComplete ?? 'Procedura zakończona';
      attestContainerEl.replaceChildren();
      return;
    }
    const steps = activeScenario.steps;
    const idx = steps.findIndex((s) => s.id === currentStepId);
    const step = idx >= 0 ? steps[idx] : null;
    if (!step) return;
    const prefix = pl.ui?.stepFormatPrefix ?? 'Krok ';
    stepReadoutEl.textContent = `${prefix}${idx + 1}/${steps.length}: ${step.labelPL}`;

    attestContainerEl.replaceChildren();
    if (step.kind === 'visual-attest') {
      const btn = document.createElement('button');
      btn.className = 'phase3-attest-check';
      const ap = pl.ui?.attestPrefix ?? 'Potwierdź: ';
      btn.textContent = `${ap}${step.labelPL}`;
      btn.addEventListener('click', () => {
        // Opcja A z Pitfall 2: intent.kind 'check' (NIE 'visual-attest') — kompatybilne z ProcedureEngine Branch 3.
        store.getState().attemptStep({ kind: 'check', stepId: currentStepId });
      });
      attestContainerEl.appendChild(btn);
    }
  }

  unsubscribers.push(
    store.subscribe((s) => s.machineState, () => renderStatusText()),
    store.subscribe((s) => s.scoring.score, () => renderStatusText()),
    store.subscribe((s) => s.currentStepId, (next) => renderStepAndAttest(next)),
  );

  renderStatusText();
  renderStepAndAttest(store.getState().currentStepId);

  return () => { for (const u of unsubscribers) u(); };
}

/** Symuluj click w mesh przez RaycastController (mockujemy intersectObjects per kliknięcie) */
function simulateClickOnMesh(controller, mesh) {
  vi.spyOn(controller._raycaster, 'intersectObjects').mockReturnValueOnce([{ object: mesh }]);
  controller.handlePointerDown({ clientX: 400, clientY: 300 });
  // RaycastController._handlePointerUp emituje intent {kind:'click', meshId} (D-Phase3-03 Opcja A)
  controller._handlePointerUp({ clientX: 400, clientY: 300 });
}

describe('Phase 3 E2E — happy path uruchomienie 8/8 (Phase 3 SC3)', () => {
  let store, controller, unsubscribe;

  beforeEach(() => {
    vi.useFakeTimers();
    document.body.innerHTML = `
      <span id="status-text"></span>
      <div id="phase3-step-readout"></div>
      <div id="phase3-attest-container"></div>
    `;
    const renderer = makeMockRenderer();
    const camera = makeCamera();
    store = createTrainingStore({ now: () => 1000, scheduleTimer: (fn, ms) => setTimeout(fn, ms) });
    store.getState().startScenario(uruchomienie);

    // Tworzymy mesh dla każdego unique meshId w scenariuszu (gather z target/effects)
    const meshIds = new Set();
    for (const step of uruchomienie.steps) {
      if (step.targetMeshId) meshIds.add(step.targetMeshId);
    }
    const interactables = new Map();
    for (const id of meshIds) {
      const stepUsingThis = uruchomienie.steps.find((s) => s.targetMeshId === id);
      const kind = stepUsingThis?.kind === 'manipulation' ? 'manipulation' : 'visual-target';
      interactables.set(id, makeMesh(id, kind));
    }

    controller = new RaycastController({ renderer, camera, interactables, store });
    unsubscribe = wirePhase3({
      store,
      statusTextEl: document.getElementById('status-text'),
      stepReadoutEl: document.getElementById('phase3-step-readout'),
      attestContainerEl: document.getElementById('phase3-attest-container'),
    });
  });

  afterEach(() => {
    if (unsubscribe) unsubscribe();
    if (controller) controller.dispose();
    vi.useRealTimers();
  });

  it('initial render — Krok 1 widoczny, status "Oczekiwanie na inspekcję — 100/100"', () => {
    expect(document.getElementById('phase3-step-readout').textContent).toMatch(/Krok 1\//);
    const status = document.getElementById('status-text').textContent;
    expect(status).toContain('Oczekiwanie na inspekcję');
    expect(status).toContain('100/100');
  });

  it('odgrywa pełen happy path 8/8 kroków — finalny machineState=w-cyklu, score=100, 0 violations', () => {
    const meshById = (id) => controller._meshes.find((m) => m.userData.id === id);
    const stepIds = uruchomienie.steps.map((s) => s.id);
    expect(stepIds.length).toBe(8); // sanity — 8 kroków scenariusza

    for (let i = 0; i < stepIds.length; i++) {
      const step = uruchomienie.steps[i];
      const currentBeforeClick = store.getState().currentStepId;
      expect(currentBeforeClick).toBe(step.id); // jesteśmy na właściwym kroku

      if (step.kind === 'visual-attest') {
        // Klik visual-attest button w DOM (Pitfall 2 Opcja A: intent {kind:'check', stepId})
        const btn = document.querySelector('.phase3-attest-check');
        expect(btn).toBeTruthy(); // subscriber wstrzyknął button
        btn.click();
      } else if (step.kind === 'manipulation' || step.kind === 'visual-target') {
        // Klik 3D mesh przez RaycastController (D-Phase3-03 Opcja A: intent {kind:'click', meshId})
        const mesh = meshById(step.targetMeshId);
        expect(mesh).toBeTruthy();
        simulateClickOnMesh(controller, mesh);
      }

      // Spin-up timer po kroku 'wlacz-zasilanie'
      if (step.id === 'wlacz-zasilanie') {
        expect(store.getState().machineState).toBe('rozpedzanie');
        vi.advanceTimersByTime(3000);
        expect(store.getState().machineState).toBe('gotowa-do-pracy');
      }
    }

    // Final state
    const finalState = store.getState();
    expect(finalState.currentStepId).toBeNull();
    expect(finalState.machineState).toBe('w-cyklu');
    expect(finalState.scoring.score).toBe(100);
    const doneCount = finalState.events.filter((e) => e.type === 'step.done').length;
    expect(doneCount).toBe(8);
    const violations = finalState.events.filter((e) => e.type === 'step.violation');
    expect(violations).toHaveLength(0);

    // Final DOM
    const readout = document.getElementById('phase3-step-readout').textContent;
    expect(readout).toMatch(/Procedura zakończona/);
    const status = document.getElementById('status-text').textContent;
    expect(status).toContain('W cyklu');
    expect(status).toContain('100/100');
  });
});
```

**Krytyczne uwagi do executora:**
1. PRZED implementacją PRZECZYTAJ `src/training/scenarios/uruchomienie.js` — potwierdź dokładną sekwencję `step.id` i `step.kind`.
2. **Trade-off w wirePhase3 helper jest świadomy (warning #7):** kopia logiki Application._wireStoreSubscribers + _renderStatusText + _renderStepAndAttest zamiast importowania Application z `src/main.js`. Powód: izolacja E2E od SceneSetup/WebGL bez zależności od vi.mock w innym pliku. JSDoc po polsku w wirePhase3() helperze tłumaczy decyzję. **Alternatywa:** jeśli Plan 03-04 Task 2 ustawił działający `vi.mock('../src/SceneSetup.js')`, executor MOŻE uprościć i `import { Application } from '../src/main.js'` zamiast wirePhase3 — obie ścieżki są poprawne.
3. Helper `simulateClickOnMesh` używa `mockReturnValueOnce` — każde kliknięcie wymaga osobnego mockReturnValueOnce.
4. Intent shape spójny z reszta Phase 3:
   - manipulation/visual-target: `{kind:'click', meshId}` (literał 'click', emitowany przez RaycastController._handlePointerUp)
   - visual-attest: `{kind:'check', stepId}` (Pitfall 2 Opcja A, emitowany przez button click handler w wirePhase3)
  </action>
  <verify>
    <automated>npm test -- --run tests/phase3.e2e.test.js</automated>
    <acceptance_criteria>
      - Plik `tests/phase3.e2e.test.js` istnieje
      - `grep -c "8" tests/phase3.e2e.test.js` zwraca ≥2 (8 kroków sanity check + 8 done events)
      - `grep -c "happy path" tests/phase3.e2e.test.js` zwraca ≥1
      - `grep -c "advanceTimersByTime" tests/phase3.e2e.test.js` zwraca ≥1 (spin-up timer)
      - `grep -c "phase3-attest-check" tests/phase3.e2e.test.js` zwraca ≥1 (button selector)
      - `grep -c "Procedura zakończona" tests/phase3.e2e.test.js` zwraca ≥1 (final readout assert)
      - `grep -c "W cyklu" tests/phase3.e2e.test.js` zwraca ≥1
      - `grep -cE "kind:\s*['\"]check['\"]" tests/phase3.e2e.test.js` zwraca ≥1 (visual-attest intent literal)
      - `grep -cE "ŚWIADOMY TRADE-OFF|świadomy trade-off" tests/phase3.e2e.test.js` zwraca ≥1 (warning #7 — JSDoc trade-off doc)
      - `npm test -- --run tests/phase3.e2e.test.js` — 2 testy zielone
      - Pełna suite `npm test -- --run` zielona, ≥177 testów łącznie
    </acceptance_criteria>
  </verify>
  <done>
    `tests/phase3.e2e.test.js` przechodzi 8/8 kroków uruchomienia z asercją finalnego state + DOM. Helper wirePhase3 ma JSDoc po polsku tłumaczący trade-off (kopia logiki vs vi.mock SceneSetup). Pełna suite zielona.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MANUAL CHECKPOINT — przeglądarka 60 FPS + visual hover hint + happy path uruchomienia</name>
  <what-built>
    Phase 3 kompletna: RaycastController, store refactor, 5 store subscribers w Application, DOM scaffolding + CSS, 8-krokowy happy path uruchomienia testowany E2E w jsdom (177+ testów zielonych).
  </what-built>
  <how-to-verify>
    Uruchom serwer dev i ręcznie zweryfikuj funkcjonalność, której jsdom nie odda:

    1. **Uruchom dev server:** `npm run dev` → otwórz http://localhost:5173/
    2. **Sprawdź initial render:**
       - DisclaimerBanner widoczny u góry
       - Status: "Oczekiwanie na inspekcję — 100/100"
       - Step readout: "Krok 1/8: Sprawdź tabliczkę znamionową" (lub analogiczne polskie label dla pierwszego kroku)
       - Brak visual-attest button (krok 1 jest visual-target, nie visual-attest)
    3. **60 FPS performance check:**
       - Otwórz DevTools → Performance → Record 10s w trybie idle (kursor poza canvasem)
       - Asercja: ≥60 FPS, ZERO wywołań `Raycaster.intersectObjects` w idle (bo brak pointermove)
       - Record 10s podczas aktywnego ruszania myszką nad meshami
       - Asercja: ≥60 FPS, raycast wywołany maksymalnie 1×/tick (16ms)
    4. **Visual hover hint:**
       - Najedź kursorem na koło zamachowe → potwierdź subtelny szary lift (`#222222` emissive)
       - Cursor zmienia się na `pointer`
       - Zjedź z mesha → emissive wraca do poprzedniego stanu (czarny), cursor `default`
       - Najedź na inny mesh (np. estop) → A leave + B enter z hysteresis ~33ms (≥2 ticki)
    5. **Click-vs-drag:**
       - Krótki click (bez ruchu) na tabliczce znamionowej → step advance, readout zmienia się na "Krok 2/8"
       - Drag (przytrzymanie + ruch >5px) → orbit kamery, BRAK step advance
    6. **Happy path 8/8 kroków:**
       - Wykonaj scenariusz: tabliczka (klik) → kontrola narzędzia (button "Potwierdź:") → kontrola wzrokowa (button) → wziernik (klik) → osłona przednia (klik) → estop (klik) → wyłącznik główny (klik) → POCZEKAJ 3s → dźwignia sprzęgła (klik)
       - Po każdym kroku: readout aktualizuje się, score pozostaje 100/100
       - Po ostatnim: readout = "Procedura zakończona", status = "W cyklu — 100/100"
    7. **Wrong-mesh test:**
       - Reset (refresh strony)
       - Klik estop NA POCZĄTKU (zamiast tabliczki) → status text zmienia się na format z violations w score (np. "Oczekiwanie na inspekcję — 90/100"), readout wciąż "Krok 1/8"
    8. **HMR check:**
       - Edytuj `src/main.js` (zmień jakiś komentarz) → save → Vite HMR
       - DevTools console: brak warningów "uncaught" / "leak"
       - Aplikacja restartuje czysto

    Jeśli wszystko OK → odpowiedź "approved".
    Jeśli któryś krok failuje → opisz dokładnie co nie działa (krok numer + obserwowane vs oczekiwane).
  </how-to-verify>
  <resume-signal>Type "approved" lub opisz konkretne issues z każdego kroku</resume-signal>
</task>

</tasks>

<verification>
- E2E test pokrywa Phase 3 SC3 (happy path uruchomienia w pełni "playable").
- Manual checkpoint pokrywa SC1 (60 FPS, zero raycast w idle), SC5 (visual hover hint, hysteresis bounded).
- Wszystkie 5 INTERACT-XX wymagań mappowane na plany (01: store/lock; 02: RaycastController + intent; 03: hover; 04: visual-attest button; 05: TEST-04).
- Intent shape spójność: manipulation/visual-target → {kind:'click', meshId}; visual-attest → {kind:'check', stepId}.
- wirePhase3 helper trade-off udokumentowany JSDoc po polsku (warning #7).
</verification>

<success_criteria>
- 8/8 kroków uruchomienia E2E test zielony
- Final state: machineState='w-cyklu', score=100/100, currentStepId=null, 0 violations, 8 step.done
- Manual checkpoint approved przez deweloperа
- Pełna suite ≥177 testów zielona
</success_criteria>

<output>
Po zakończeniu utwórz `.planning/phases/03-click-to-state-pipeline/03-05-SUMMARY.md`:
- Wynik E2E (faktyczne liczby: step.done, violations, final score, machineState)
- Manual checkpoint sign-off — co potwierdzono w przeglądarce, jakie ewentualne quirks
- Pełna lista testów Phase 3 (RaycastController + trainingStore + uruchomienie.integration + application + phase3.e2e)
- Final test count Phase 3 (oczekiwanie ≥177)
- Decyzja wirePhase3 trade-off — która ścieżka wybrana (kopia helper vs vi.mock + Application import)
- Następne kroki: Phase 4 może budować HighlightManager na fundamencie hover R-M-R z Phase 3
</output>
