---
phase: 03-click-to-state-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/main.js
  - src/i18n/pl.js
  - tests/application.test.js
autonomous: true
requirements: [INTERACT-02, INTERACT-04]
tags: [application, wiring, subscribers, dispose, integration]
must_haves:
  truths:
    - "Application.constructor auto-startuje scenariusz uruchomienie (D-Phase3-01)"
    - "Application instancjuje RaycastController z DI (renderer, camera, interactables, store)"
    - "RaycastController._runHysteresis(dt) zarejestrowany w Application.tickables (GSAP single source of timing)"
    - "3 store subscribery: machineState, scoring.score, currentStepId — wszystkie zwracają unsubscribe wpinane w _unsubscribers"
    - "Subscriber currentStepId aktualizuje #phase3-step-readout TextContent + render visual-attest button przez {kind:'check', stepId} (Opcja A z Pitfall 2)"
    - "Subscriber machineState/score aktualizuje #status-text format '{Polski state} — {score}/100' (D-Phase3-10/11)"
    - "Application.dispose() wywołuje raycastController.dispose() przed sceneSetup.dispose()"
    - "src/i18n/pl.js zawiera nowe klucze pl.ui.{attestPrefix, attestAriaPrefix, procedureComplete, stepFormatPrefix} (UI-06 boundary compliance)"
    - "HMR safe — brak leaków subscriberów ani event listenerów"
  artifacts:
    - path: "src/main.js"
      provides: "Application z RaycastController DI + 3 store subscribers + visual-attest button rendering + dispose hook"
      contains: "RaycastController"
    - path: "src/i18n/pl.js"
      provides: "Phase 3 ui keys: attestPrefix, attestAriaPrefix, procedureComplete, stepFormatPrefix"
      contains: "attestPrefix"
    - path: "tests/application.test.js"
      provides: "Test integracyjny — Application konstruktor wpina 3 subscribers + raycastController dispose"
      contains: "raycastController"
  key_links:
    - from: "Application.constructor"
      to: "store.startScenario(uruchomienie)"
      via: "import uruchomienie + this.store.getState().startScenario"
      pattern: "startScenario\\(uruchomienie\\)"
    - from: "Application.tickables"
      to: "raycastController._runHysteresis"
      via: "this.tickables.push((dt) => this.raycastController._runHysteresis(dt))"
      pattern: "_runHysteresis"
    - from: "store.subscribe(currentStepId)"
      to: "DOM #phase3-step-readout + visual-attest button"
      via: "subscriber callback → textContent + container.appendChild"
      pattern: "phase3-step-readout"
    - from: "Application.dispose"
      to: "raycastController.dispose"
      via: "this._unsubscribers lub direct call"
      pattern: "raycastController\\.dispose"
    - from: "_renderStepAndAttest visual-attest button click"
      to: "store.attemptStep"
      via: "intent {kind:'check', stepId: currentStepId} — Opcja A z Pitfall 2 (NIE 'visual-attest')"
      pattern: "kind:\\s*['\"]check['\"]"
---

<objective>
Wiring Phase 3 w `src/main.js` (Application composition root):

1. **D-Phase3-01:** auto-start scenariusza `uruchomienie` w konstruktorze (po `createTrainingStore()`).
2. **DI RaycastController:** instancjacja z `pressModel.getInteractables()`, `sceneSetup.renderer`, `sceneSetup.camera`, `store`.
3. **Tickable registration:** `this.tickables.push((dt) => this.raycastController._runHysteresis(dt))`.
4. **3 store subscribery (D-Phase3-10/11/12):**
   - `machineState` → aktualizuje `#status-text` w formacie `{Polski label} — {score}/100`
   - `scoring.score` → re-renders `#status-text` z aktualnym score (compound z machineState)
   - `currentStepId` → aktualizuje `#phase3-step-readout` ("Krok N/M: {labelPL}" lub "Procedura zakończona") + dynamicznie wstrzykuje/usuwa visual-attest button (D-Phase3-09)
5. **Visual-attest button intent:** Per RESEARCH Pitfall 2 / Opcja A: button emituje `store.getState().attemptStep({kind:'check', stepId: currentStepId})` (NIE `{kind:'visual-attest'}` — ProcedureEngine waliduje `intent.kind === 'check'`). Komentarz JSDoc po polsku wyjaśnia dlaczego.
6. **dispose() hook:** RaycastController.dispose() + unsubscribery zwalnia wszystko na HMR.
7. **`src/i18n/pl.js` extension:** dodanie 4 kluczy `pl.ui.{attestPrefix, attestAriaPrefix, procedureComplete, stepFormatPrefix}` żeby `src/main.js` nie zawierał polskich literałów (UI-06 boundary compliance).

Test integracyjny w `tests/application.test.js` weryfikuje wiring. Plik MA już header `// @vitest-environment jsdom` (potwierdzone) — Task 2 rozszerza istniejący plik bez nowego pliku.

Purpose: Domykna pętla 3D event → store → DOM. Plan 03-05 dostarczy pełen E2E happy-path test.

Output: Application kompletna; uruchomienie aplikacji w przeglądarce powinno pozwolić na klik tabliczki → step.done → score 100/100 (manual QA w Plan 03-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-click-to-state-pipeline/03-CONTEXT.md
@.planning/phases/03-click-to-state-pipeline/03-RESEARCH.md
@.planning/phases/03-click-to-state-pipeline/03-PATTERNS.md
@.planning/phases/03-click-to-state-pipeline/03-01-SUMMARY.md
@.planning/phases/03-click-to-state-pipeline/03-02-SUMMARY.md
@.planning/phases/03-click-to-state-pipeline/03-03-SUMMARY.md

@src/main.js
@src/RaycastController.js
@src/state/trainingStore.js
@src/i18n/pl.js
@src/training/scenarios/uruchomienie.js
@tests/application.test.js
@index.html

<interfaces>
Konsumowane API:

```js
// Plan 03-01: store.attemptStep(intent) 1-arg + state.activeScenario + state.isAnimating
// Plan 03-02: RaycastController({renderer, camera, interactables, store}) + _runHysteresis(dt) + dispose()
//   UWAGA: RaycastController._handlePointerUp emituje intent {kind:'click', meshId} (literał 'click'
//   per D-Phase3-03 Opcja A) — Application NIE musi już mapować userData.kind.
// Plan 03-03: DOM #phase3-step-readout + #phase3-attest-container

// Existing (Phase 2):
// pressModel.getInteractables(): Map<string, THREE.Mesh>
// sceneSetup.renderer: THREE.WebGLRenderer (sceneSetup.camera: THREE.PerspectiveCamera)

// pl.machineState (singular!) — Pitfall 5: sekcja istnieje, NIE tworzymy duplikatu
// pl.machineState['oczekiwanie-na-inspekcje'] === 'Oczekiwanie na inspekcję'
```

Po Plan 03-04 Application.constructor shape (przykładowe miejsce wstawek):

```js
constructor() {
  this.sceneSetup = new SceneSetup('three-canvas');
  this.pressModel = new PressModel(this.sceneSetup.scene);
  this.ui = new UI();
  this.disclaimerBanner = new DisclaimerBanner();
  this.store = createTrainingStore();
  this.currentAngle = 0;

  this.tickables = [(dt) => this.simulationTick(dt)];
  // ... GSAP ticker setup ...
  this._unsubscribers = [];

  // ▼ NOWE Plan 03-04 wstawia po _unsubscribers init ▼
  this.store.getState().startScenario(uruchomienie);  // D-Phase3-01

  this.raycastController = new RaycastController({
    renderer: this.sceneSetup.renderer,
    camera: this.sceneSetup.camera,
    interactables: this.pressModel.getInteractables(),
    store: this.store,
  });
  this.tickables.push((dt) => this.raycastController._runHysteresis(dt));

  this._wireStoreSubscribers();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/main.js + src/i18n/pl.js — auto-start scenariusza, DI RaycastController, 3 store subscribers, visual-attest button rendering, dispose hook, UI keys</name>
  <read_first>
    - src/main.js (cały — pattern istniejącego konstruktora i dispose)
    - src/RaycastController.js (Plan 03-02 — API: konstruktor, _runHysteresis, dispose; intent.kind to literał 'click')
    - src/state/trainingStore.js (Plan 03-01 — startScenario, attemptStep)
    - src/i18n/pl.js (linie 35-44 — pl.machineState singular, klucze 7 stanów; linie z `ui:` sekcją — gdzie dodać nowe klucze)
    - src/training/scenarios/uruchomienie.js (struktura steps, kind, labelPL — pierwsze kilka kroków)
    - index.html (Plan 03-03 — id="phase3-step-readout" i id="phase3-attest-container" istnieją)
    - .planning/phases/03-click-to-state-pipeline/03-RESEARCH.md (Pattern 4 fine-grained subscribers, Pattern 5 visual-attest button, Pitfall 2 intent.kind)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `src/main.js` — constructor extension, store subscriber registration, dispose extension)
  </read_first>
  <behavior>
    (Implementacja w action; testy w Task 2.)
    - **`src/i18n/pl.js`** — rozszerz sekcję `ui:` o 4 nowe klucze Phase 3 PRZED edycją src/main.js (deterministyczna kolejność)
    - Konstruktor po `this._unsubscribers = []`:
      1. Auto-start `store.startScenario(uruchomienie)`
      2. Instancjacja RaycastController z DI
      3. Push `_runHysteresis` do tickables
      4. Wywołanie `this._wireStoreSubscribers()` (helper)
    - `_wireStoreSubscribers()`: 3 subscribers (machineState, scoring.score, currentStepId)
    - Subscribery 1+2 (machineState/score) — wspólny helper `_renderStatusText()` używający compound (czyta oba ze state)
    - Subscriber 3 (currentStepId) — re-renders #phase3-step-readout + zarządza #phase3-attest-container (clear + opcjonalnie wstrzyknij button)
    - Visual-attest button kliknięcie: `store.getState().attemptStep({kind:'check', stepId: currentStepId})` — Opcja A z Pitfall 2
    - dispose: `this.raycastController.dispose()` przed `this.sceneSetup.dispose()`
    - Initial render: po startScenario subscribery są wpięte ale nie odpalą się dla wartości początkowej — wywołać `_renderStatusText()` i subscriber-callback dla currentStepId raz po wpięciu
  </behavior>
  <action>
**KROK A — Modyfikacja `src/i18n/pl.js`:**

W sekcji `ui:` (znajdź `ui: {` w pliku) dodaj 4 nowe klucze Phase 3:

```js
ui: {
  // ... istniejące klucze (statusRunning, statusStopped itp.)
  // Phase 3 — Plan 03-04 (UI-06 boundary compliance: zero polskich literałów w src/main.js)
  attestPrefix: 'Potwierdź: ',
  attestAriaPrefix: 'Potwierdź krok: ',
  procedureComplete: 'Procedura zakończona',
  stepFormatPrefix: 'Krok ',
},
```

**KROK B — Modyfikacje w `src/main.js`:**

1. **Imports** — dodaj na początku (po linii 8 z `DisclaimerBanner`):
```js
import { RaycastController } from './RaycastController.js';
import uruchomienie from './training/scenarios/uruchomienie.js';
import { pl } from './i18n/pl.js';
```

2. **Konstruktor** — po linii `this._unsubscribers = [];` (linia 30) dodaj:
```js

    // D-Phase3-01: auto-start scenariusza uruchomienie (Phase 6 doda dropdown wyboru).
    this.store.getState().startScenario(uruchomienie);

    // INTERACT-01..05: RaycastController jako DI z pressModel.getInteractables() (Phase 2 contract).
    // Tickable rejestrowany w GSAP ticker (single source of timing — INTERACT-01).
    this.raycastController = new RaycastController({
      renderer: this.sceneSetup.renderer,
      camera: this.sceneSetup.camera,
      interactables: this.pressModel.getInteractables(),
      store: this.store,
    });
    this.tickables.push((dt) => this.raycastController._runHysteresis(dt));

    // Plan 03-04: store subscribery dla DOM (status text + step readout + visual-attest button)
    this._wireStoreSubscribers();
    // Initial render — subscribery odpalają się dopiero przy CHANGE; renderujemy stan początkowy ręcznie.
    this._renderStatusText();
    this._renderStepAndAttest(this.store.getState().currentStepId);
```

3. **Nowe metody** — dodaj PRZED `dispose()`:
```js
  /**
   * Rejestruje 3 store subscribery (D-Phase3-10/11/12) — wszystkie wpinane w _unsubscribers (STATE-03).
   * Fine-grained przez subscribeWithSelector (już aktywne w trainingStore.js middleware).
   */
  _wireStoreSubscribers() {
    const unsub1 = this.store.subscribe(
      (s) => s.machineState,
      () => this._renderStatusText(),
    );
    const unsub2 = this.store.subscribe(
      (s) => s.scoring.score,
      () => this._renderStatusText(),
    );
    const unsub3 = this.store.subscribe(
      (s) => s.currentStepId,
      (next) => this._renderStepAndAttest(next),
    );
    this._unsubscribers.push(unsub1, unsub2, unsub3);
  }

  /**
   * Aktualizuje #status-text do formatu "{Polski state} — {score}/100" (D-Phase3-10/11).
   * Reuse istniejącego elementu (Phase 4 wymieni na pełny StatusPanel).
   * Pitfall 5: pl.machineState (singular) — sekcja istnieje od Phase 1 D-09.
   */
  _renderStatusText() {
    const state = this.store.getState();
    const label = pl.machineState[state.machineState] ?? state.machineState;
    const text = `${label} — ${state.scoring.score}/100`;
    if (this.ui?.elements?.statusText) {
      this.ui.elements.statusText.textContent = text;
    }
  }

  /**
   * Renderuje step readout "Krok N/M: {labelPL}" + dynamicznie wstrzykuje visual-attest
   * button gdy aktywny step jest typu 'visual-attest' (D-Phase3-09/12).
   *
   * Pitfall 2 / Opcja A: button emituje intent {kind:'check', stepId} — NIE 'visual-attest'.
   * ProcedureEngine Branch 3 oczekuje kind:'check' dla visual-attest kroków; D-Phase3-03
   * (Update 2026-05-06) opisuje pełny mapping. Zachowuje ProcedureEngine pure.
   *
   * UWAGA: wszystkie polskie stringi z pl.ui.* — UI-06 boundary scanner egzekwuje zero
   * polskich literałów w src/main.js.
   */
  _renderStepAndAttest(currentStepId) {
    const readoutEl = document.getElementById('phase3-step-readout');
    const containerEl = document.getElementById('phase3-attest-container');
    if (!readoutEl || !containerEl) return; // DOM nie zamontowany (test bez DOM)

    const activeScenario = this.store.getState().activeScenario;
    if (!activeScenario || !currentStepId) {
      readoutEl.textContent = pl.ui.procedureComplete;
      containerEl.replaceChildren(); // clear (XSS-safe)
      return;
    }

    const steps = activeScenario.steps;
    const idx = steps.findIndex((s) => s.id === currentStepId);
    const step = idx >= 0 ? steps[idx] : null;
    if (!step) {
      readoutEl.textContent = pl.ui.procedureComplete;
      containerEl.replaceChildren();
      return;
    }
    readoutEl.textContent = `${pl.ui.stepFormatPrefix}${idx + 1}/${steps.length}: ${step.labelPL}`;

    // Clear + opcjonalnie wstrzyknij visual-attest button
    containerEl.replaceChildren();
    if (step.kind === 'visual-attest') {
      const btn = document.createElement('button');
      btn.className = 'phase3-attest-check';
      btn.textContent = `${pl.ui.attestPrefix}${step.labelPL}`;
      btn.setAttribute('aria-label', `${pl.ui.attestAriaPrefix}${step.labelPL}`);
      btn.addEventListener('click', () => {
        // Opcja A z Pitfall 2: intent.kind 'check' (NIE 'visual-attest') — kompatybilne z istniejącym ProcedureEngine.
        this.store.getState().attemptStep({ kind: 'check', stepId: currentStepId });
      });
      containerEl.appendChild(btn);
    }
  }
```

4. **dispose()** — zmodyfikuj istniejącą metodę. PRZED `this.sceneSetup.dispose();` dodaj:
```js
    if (this.raycastController) this.raycastController.dispose();
```

Pełny dispose() po zmianie:
```js
  dispose() {
    gsap.ticker.remove(this._tickerCallback);
    for (const unsub of this._unsubscribers) unsub();
    this._unsubscribers = [];
    if (this.disclaimerBanner) this.disclaimerBanner.dispose();
    this.pressModel.disposeMaterials();
    if (this.raycastController) this.raycastController.dispose();
    this.sceneSetup.dispose();
  }
```

**XSS / Polish literal compliance:**
- `_renderStatusText` używa `textContent` (NIE `innerHTML`) — defense-in-depth XSS per Security Domain w RESEARCH.md
- `_renderStepAndAttest` używa `replaceChildren()` zamiast `innerHTML = ''` (modern, XSS-safe)
- Polskie literały w komentarzach (linie komentarza `//`) są OK — boundaries scanner sprawdza tylko string literals
- Wszystkie user-visible polskie stringi pochodzą z `pl.ui.*` lub `pl.machineState.*` — zero polskich literałów w `src/main.js`
  </action>
  <verify>
    <automated>npm test -- --run tests/boundaries.test.js tests/uruchomienie.integration.test.js</automated>
    <acceptance_criteria>
      - `grep -c "import { RaycastController }" src/main.js` zwraca 1
      - `grep -c "import uruchomienie" src/main.js` zwraca 1
      - `grep -c "import { pl }" src/main.js` zwraca 1
      - `grep -c "startScenario(uruchomienie)" src/main.js` zwraca 1
      - `grep -c "new RaycastController" src/main.js` zwraca 1
      - `grep -c "_runHysteresis" src/main.js` zwraca 1
      - `grep -c "this.store.subscribe" src/main.js` zwraca 3 (machineState, score, currentStepId)
      - `grep -c "phase3-step-readout" src/main.js` zwraca 1
      - `grep -c "phase3-attest-container" src/main.js` zwraca 1
      - `grep -c "kind: 'check'" src/main.js` zwraca 1 (Opcja A — visual-attest button intent)
      - `grep -c "raycastController.dispose" src/main.js` zwraca 1
      - `grep -c "innerHTML" src/main.js` zwraca 0 (XSS — używamy textContent + replaceChildren)
      - `grep -nE "['\"\`].*[ąćęłńóśźżĄĆĘŁŃÓŚŹŻ].*['\"\`]" src/main.js | grep -v '^[^:]*:\s*//' | grep -v '^[^:]*:\s*\*' | grep -c .` zwraca 0 (zero polskich literałów w main.js — wszystko z pl.ui)
      - `grep -c "attestPrefix\|attestAriaPrefix\|procedureComplete\|stepFormatPrefix" src/i18n/pl.js` zwraca ≥4 (4 nowe klucze ui Phase 3)
      - `grep -c "pl.ui.attestPrefix\|pl.ui.procedureComplete\|pl.ui.stepFormatPrefix" src/main.js` zwraca ≥3 (uzycie nowych kluczy)
      - `npm test -- --run tests/boundaries.test.js` zielony (Polish literal scanner zielony — zero literałów w main.js)
      - `npm test -- --run tests/uruchomienie.integration.test.js` zielony (regresji nie ma)
    </acceptance_criteria>
  </verify>
  <done>
    `src/main.js` ma kompletny wiring Phase 3: auto-start, DI RaycastController, 3 subscribers, visual-attest rendering z Opcji A, dispose hook. `src/i18n/pl.js` ma 4 nowe klucze ui. Boundaries.test.js zielony.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/application.test.js — rozszerzenie istniejącego pliku o describe Phase 3 wiring (jsdom potwierdzony)</name>
  <read_first>
    - tests/application.test.js (cały — header POTWIERDZA `// @vitest-environment jsdom` na linii 2; istniejące pattern setup z mock canvas)
    - src/main.js (po Task 1 — Application constructor i nowe metody)
    - tests/PressModel.smoke.test.js (linie 1-30 — mock setup canvas)
    - .planning/phases/03-click-to-state-pipeline/03-PATTERNS.md (sekcja `tests/RaycastController.test.js` — mock pattern dla RaycastController)
  </read_first>
  <behavior>
    - Test 1: Application konstruktor wpina RaycastController (`app.raycastController` instanceof RaycastController)
    - Test 2: Application konstruktor wpina 3 subscribers — `app._unsubscribers.length >= 3` (poprzednie + 3 nowe)
    - Test 3: store ma `activeScenario === uruchomienie` po Application konstruktorze (auto-start)
    - Test 4: tickables zawiera 2 callbacki (simulationTick + raycastController._runHysteresis)
    - Test 5: dispose() wywołuje `raycastController.dispose()` (spy) — restore listenery removed
    - Test 6: dispose() wywołuje wszystkie unsubscribers — po dispose `app._unsubscribers === []`
    - Test 7: Initial render — po konstruktorze `#phase3-step-readout.textContent` zawiera "Krok 1/" (pierwszy krok scenariusza)
    - Test 8: Subscriber currentStepId reaguje — wywołanie `store.getState().attemptStep({kind:'click', meshId:'tabliczka-znamionowa'})` przesuwa krok i `#phase3-step-readout` aktualizuje się do "Krok 2/..."
  </behavior>
  <action>
**Plik `tests/application.test.js` MA już header `// @vitest-environment jsdom`** (potwierdzone w revision context — linia 2 zaczyna się od `// @vitest-environment jsdom`). Rozszerz istniejący plik bez tworzenia nowego.

Dodaj nowy `describe` block na końcu pliku:

```js
describe('Application — Phase 3 wiring (Plan 03-04)', () => {
  let app;

  beforeEach(() => {
    // Setup DOM dla Application (włącz #phase3-step-readout + #phase3-attest-container z Plan 03-03)
    document.body.innerHTML = `
      <div id="three-canvas"></div>
      <span id="status-dot" class="dot"></span>
      <span id="status-text"></span>
      <input type="range" id="speed-slider" min="10" max="120" value="30">
      <span id="speed-value">30</span>
      <button id="btn-toggle">Start/Stop</button>
      <span id="val-angle">0</span>
      <span id="val-displacement">0</span>
      <div id="phase3-step-readout"></div>
      <div id="phase3-attest-container"></div>
    `;
    app = new Application();
  });

  afterEach(() => {
    if (app) app.dispose();
    app = null;
  });

  it('konstruktor wpina raycastController (instance of RaycastController)', () => {
    expect(app.raycastController).toBeDefined();
    expect(app.raycastController._raycaster).toBeDefined(); // RaycastController._raycaster instance check
  });

  it('konstruktor auto-startuje scenariusz uruchomienie (D-Phase3-01)', () => {
    const s = app.store.getState();
    expect(s.activeScenario).toBeDefined();
    expect(s.activeScenario.id).toBe('uruchomienie');
    expect(s.currentStepId).toBe('sprawdz-tabliczke'); // pierwszy krok
  });

  it('konstruktor wpina ≥3 store subscribers (machineState/score/currentStepId)', () => {
    expect(app._unsubscribers.length).toBeGreaterThanOrEqual(3);
  });

  it('tickables zawiera simulationTick + raycastController._runHysteresis (2 callbacks)', () => {
    expect(app.tickables.length).toBeGreaterThanOrEqual(2);
  });

  it('initial render — #phase3-step-readout zawiera "Krok 1/" po konstruktorze', () => {
    const readout = document.getElementById('phase3-step-readout').textContent;
    expect(readout).toMatch(/Krok 1\//);
  });

  it('subscriber currentStepId reaguje — kliknięcie tabliczki przesuwa readout do "Krok 2/"', () => {
    // intent.kind LITERAŁ 'click' (D-Phase3-03 Opcja A) — kompatybilne z ProcedureEngine Branch 3
    app.store.getState().attemptStep({ kind: 'click', meshId: 'tabliczka-znamionowa' });
    const readout = document.getElementById('phase3-step-readout').textContent;
    expect(readout).toMatch(/Krok 2\//);
  });

  it('subscriber machineState aktualizuje #status-text z formatem "{label} — {score}/100"', () => {
    // initial machineState dla uruchomienie === 'oczekiwanie-na-inspekcje'
    const statusText = document.getElementById('status-text').textContent;
    expect(statusText).toContain('Oczekiwanie na inspekcję');
    expect(statusText).toContain('100/100');
  });

  it('dispose() wywołuje raycastController.dispose() i czyści _unsubscribers', () => {
    const spy = vi.spyOn(app.raycastController, 'dispose');
    app.dispose();
    expect(spy).toHaveBeenCalled();
    expect(app._unsubscribers).toEqual([]);
    app = null; // afterEach nie wywoła ponownie
  });
});
```

**Mock SceneSetup (jeśli wymagany):** Sprawdź jak istniejący `tests/application.test.js` obsługuje WebGLRenderer w jsdom. Jeśli plik używa `vi.mock('../src/SceneSetup.js', ...)` lub canvas-mock w beforeAll — zachowaj/wykorzystaj ten sam pattern. Jeśli istniejący plik tylko skanuje statycznie (jak header pliku sugeruje "statycznie sprawdza obecność wzorców w pliku zamiast ładować WebGLRenderer w jsdom"), MUSISZ dodać `vi.mock('../src/SceneSetup.js', () => ({ SceneSetup: vi.fn().mockImplementation(() => ({ scene: new THREE.Scene(), camera: new THREE.PerspectiveCamera(), renderer: { domElement: { addEventListener: vi.fn(), removeEventListener: vi.fn(), getBoundingClientRect: () => ({left:0,top:0,width:800,height:600}), style:{} } }, dispose: vi.fn(), render: vi.fn() })) }))` na początku pliku PRZED wszystkimi describe.

Decyzja deterministyczna: **rozszerzasz istniejący `tests/application.test.js`** (header jsdom potwierdzony). Jeśli istniejący plik nie ma vi.mock dla SceneSetup, dodaj go — to single point of change. NIE twórz `tests/application.phase3.test.js`.
  </action>
  <verify>
    <automated>npm test -- --run tests/application.test.js</automated>
    <acceptance_criteria>
      - Plik `tests/application.test.js` zawiera describe block "Phase 3 wiring (Plan 03-04)" — `grep -c "Phase 3 wiring" tests/application.test.js` zwraca ≥1
      - `grep -c "raycastController" tests/application.test.js` zwraca ≥3 (3 testy dotyczące raycastController)
      - `grep -c "phase3-step-readout" tests/application.test.js` zwraca ≥2
      - `grep -c "Krok 1/" tests/application.test.js` zwraca ≥1
      - `grep -c "kind: 'click'" tests/application.test.js` zwraca ≥1 (intent literal compliance)
      - Plik zawiera dokładnie jeden header `@vitest-environment jsdom` na linii 2 (bez nowego pliku phase3.test.js)
      - `test ! -f tests/application.phase3.test.js` (warunkowy plik NIE powstaje)
      - `npm test -- --run tests/application.test.js` — wszystkie testy zielone (włącznie z 8 nowymi Phase 3)
      - Pełna suite `npm test -- --run` zielona (≥175 testów łącznie)
    </acceptance_criteria>
  </verify>
  <done>
    `tests/application.test.js` ma 8 nowych testów Phase 3 weryfikujących wiring konstruktor + initial render + subscriber reakcji + dispose. Wszystkie zielone. Brak osobnego `tests/application.phase3.test.js` — rozszerzenie istniejącego pliku.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenariusz `uruchomienie` (JSON data) → DOM | step.labelPL renderowane w textContent (nie innerHTML) — XSS-safe |
| store events → DOM subscribers | Subscribery odczytują state, nie zapisują z user-controlled stringów |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (XSS) | _renderStepAndAttest button.textContent | mitigate | `textContent` + `replaceChildren()` zamiast `innerHTML`. Grep gate w verify potwierdza zero `innerHTML` użyć. |
| T-03-06 | Tampering | visual-attest intent shape | mitigate | Per Pitfall 2 / Opcja A: button emituje `{kind:'check', stepId}` — bezpieczna ścieżka przez ProcedureEngine Branch 3. ProcedureEngine pozostaje pure. |
| T-03-07 | DoS (memory leak) | subscribery + listener leaks na HMR | mitigate | Wszystkie 3 subscribers wpięte w _unsubscribers; raycastController.dispose() w Application.dispose(). Test 8 weryfikuje. |
| T-03-08 | Repudiation | wrong-mesh = engine-side recording | accept | D-Phase3-04: każde kliknięcie ZAWSZE przez store; engine recordsuje violation z errorCode + Polish reason. |
</threat_model>

<verification>
- Application konstruktor jest jedynym miejscem orchestracji 5 klas (SceneSetup, PressModel, UI, DisclaimerBanner, RaycastController) + store + subscribers.
- Visual-attest button używa Opcji A z Pitfall 2 (intent.kind:'check') — jasno udokumentowane w komentarzach JSDoc po polsku.
- pl.machineState (singular) — żadnych duplikatów `machineStates`.
- pl.ui rozszerzone o 4 klucze Phase 3 (UI-06 boundary compliance).
- Dispose pełen — RaycastController + unsubs + ticker + DisclaimerBanner + materiały + sceneSetup.
- HMR safe (`import.meta.hot.dispose`).
- Testy w tests/application.test.js (jsdom potwierdzony) — bez warunkowego nowego pliku.
</verification>

<success_criteria>
- INTERACT-02 ✓ (klik 3D → store.attemptStep przez RaycastController; visual-attest checkbox → store.attemptStep z {kind:'check'})
- INTERACT-04 ✓ (manipulation = klik 3D, visual-attest = button w panelu — D-Phase3-09 + Opcja A)
- D-Phase3-01..14 wszystkie wpięte
- 8 nowych testów aplikacji + pełna suite ≥175 zielona
</success_criteria>

<output>
Po zakończeniu utwórz `.planning/phases/03-click-to-state-pipeline/03-04-SUMMARY.md`:
- Strukturę zmian w `src/main.js` (3 nowe importy, ~25 linii w konstruktorze, 3 nowe metody, dispose hook)
- Zmiany w `src/i18n/pl.js` (4 nowe klucze ui Phase 3: attestPrefix, attestAriaPrefix, procedureComplete, stepFormatPrefix)
- Decyzję Opcji A z Pitfall 2 zatwierdzoną — udokumentowaną w JSDoc po polsku
- Listę 8 nowych testów application + ich status (rozszerzenie istniejącego pliku, bez nowego phase3.test.js)
- Rezultat pełnej suite (oczekiwane ≥175)
</output>
