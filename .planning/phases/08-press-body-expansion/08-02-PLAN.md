---
phase: 08-press-body-expansion
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/PressModel.js
  - tests/PressModel.worktable.test.js
autonomous: true
task_count: 2
requirements: [GEO-02, GEO-05]
tags: [decoration, worktable, GEO-02, D-Phase8-02]
must_haves:
  truths:
    - "Pod suwakiem widoczny jest stół roboczy (industrial steel grey płyta)"
    - "Stół NIE koliduje z suwakiem w żadnej fazie obrotu wału (clearance > 0 zachowany przez pełny cykl 8 kątów)"
    - "Stół statyczny (KIN-01) — nie dziecko shaftAxis, nie zmienia pozycji pod update(angle)"
    - "Stół nie pojawia się w getInteractables() ani getMeshDictionary()"
  artifacts:
    - path: "src/PressModel.js"
      provides: "_buildWorktable() metoda — 1× BoxGeometry stół"
      contains: "_buildWorktable"
    - path: "tests/PressModel.worktable.test.js"
      provides: "Testy decoration + clearance vs slider min Y (KIN-aware via PhysicsEngine)"
  key_links:
    - from: "stół.position.y + stół.height/2"
      to: "min(slider.y) over full rotation"
      via: "PhysicsEngine.calculateSliderPosition + shaftY - sliderHalfHeight"
      pattern: "stol\\.position\\.y.*<.*sliderMin"
---

<objective>
Dodać stół roboczy (worktable) jako decoration mesh pod suwakiem — industrial steel grey płyta na której teoretycznie ląduje sztanca tłocząca. Pozycja **wyliczona z PhysicsEngine** żeby zagwarantować że stół nie koliduje z animacją suwaka w żadnej fazie obrotu.

Purpose: GEO-02 — wizualne uzupełnienie press body, target pozycyjny dla dolnej martwej strefy suwaka. Bez stołu suwak "uderza w nic".

Output: 1 nowy decoration mesh. Total decoration meshes po tym planie: 8 (2 łożyska + 5 fundament + 1 stół).

**KRYTYCZNA UWAGA O STAŁYCH:** CONTEXT używa stałych `r=0.5, l=2.0` (stare wartości). Live code w `src/PressModel.js` linie 19-20: **`this.r = 0.8, this.l = 4.0`**. Z PhysicsEngine wynika:
- `currentY_max @ angle=0: r + l = 0.8 + 4.0 = 4.8` → najniższa pozycja suwaka: `slider.center.y = shaftY - 4.8 = 3.2`
- `currentY_min @ angle=π: -r + l = 3.2` → najwyższa pozycja: `slider.center.y = 4.8`
- Suwak BoxGeometry(2, **1.5**, 1.5) → half-height = 0.75 → **dolna krawędź suwaka @ y_min = 3.2 - 0.75 = 2.45**

Fallback z CONTEXT (stół @ y≈5.0) jest **NIEPRAWIDŁOWY** — kolidowałby z suwakiem. Stół musi być pod y=2.45.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-press-body-expansion/08-CONTEXT.md
@.planning/phases/08-press-body-expansion/08-01-SUMMARY.md
@.planning/phases/07-kinematic-fix-anchoring/07-02-SUMMARY.md

<interfaces>
From src/PressModel.js (LIVE constants — NIE z CONTEXT):
```javascript
this.r = 0.8;       // linia 19
this.l = 4.0;       // linia 20
this.shaftY = 8.0;  // linia 21
// slider geometry, linia 136:
const sliderGeo = new THREE.BoxGeometry(2, 1.5, 1.5);  // half-height = 0.75
// slider position dynamic, in update():
//   const currentY = PhysicsEngine.calculateSliderPosition(angle, this.r, this.l);
//   this.slider.position.y = this.shaftY - currentY;
```

From src/PhysicsEngine.js:
```javascript
static calculateSliderPosition(angle, r, l)
  // y = r*cos(α) + √(l² - (r·sin(α))²)
  // Max currentY @ α=0: r+l. Min currentY @ α=π: -r+l (gdy r<l).
```

Computed slider bottom edge bounds (r=0.8, l=4.0, shaftY=8.0, half=0.75):
- min slider center y: 3.2 (gdy α=0)
- min slider bottom edge: 2.45
- → stół top edge MUSI być < 2.45 (z clearance ≥ 0.15)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — testy stół roboczy + KIN-aware clearance</name>
  <files>tests/PressModel.worktable.test.js</files>
  <behavior>
    Stworzyć `tests/PressModel.worktable.test.js` (wzór: bearings/foundation testy).

    Cases:
    1. Total decoration meshes count: traverse + filter → length === 8 (po 08-01 było 7, teraz +1 stół).
    2. Stół: dokładnie 1 mesh z `geometry.type === 'BoxGeometry'`, `parameters.width === 3`, `height === 0.3`, `depth === 2.5`. (D-Phase8-02 fallback wymiary; executor może doprecyzować po review, ale test trzyma kontrakt z planu.)
    3. World position centrowana: `position.x === 0`, `position.z === 0`, tolerance 1e-6. `position.y` — patrz test 4.
    4. **KIN-aware clearance (CORE):** Dla 16 kątów rotacji `α ∈ [0, 2π)` co π/8:
       ```js
       const sliderHalfH = 0.75;
       const tableHalfH = 0.3 / 2;
       for (α of angles) {
         pressModel.update(α);
         pressModel.group.updateMatrixWorld(true);
         const sliderBottom = pressModel.slider.position.y - sliderHalfH;
         const tableTop = stol.position.y + tableHalfH;
         expect(tableTop, `α=${α} kolizja: tableTop=${tableTop} sliderBottom=${sliderBottom}`).toBeLessThan(sliderBottom);
       }
       ```
       Z r=0.8, l=4.0, shaftY=8.0: min slider.center.y=3.2 → min sliderBottom=2.45.
    5. Stół statyczny (KIN-01): world position niezmieniona między update(0) i update(π/2) (tolerance 1e-6).
    6. Stół dzieckiem `this.group` (NIE `this.shaftAxis`).
    7. `userData.kind === 'decoration'`, `getInteractables().size === 15`, `getMeshDictionary().size === 15`.
    8. Forbidden IDs: `stol`, `stol-roboczy`, `worktable`, `table` → interactables.has(...) === false.
  </behavior>
  <action>
    Skopiować wzór z `tests/PressModel.foundation.test.js`. Dodać 7-8 testów. Kąty: `Array.from({length: 16}, (_, i) => i * Math.PI / 8)`.

    Uruchomić `npm test -- tests/PressModel.worktable.test.js` — testy MUSZĄ failować (brak `_buildWorktable`, count=7 nie 8). Commit: `test(08-02): add failing tests for worktable + KIN-aware clearance`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test -- tests/PressModel.worktable.test.js 2>&amp;1 | tail -20</automated>
    <expected>≥6 failures (no _buildWorktable yet)</expected>
  </verify>
  <done>Plik istnieje, ≥7 testów, run pokazuje failures, commit RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — _buildWorktable() z auto-fit do slider min Y</name>
  <files>src/PressModel.js</files>
  <behavior>
    Po implementacji 7-8 testów stół + pełna regresja zielona (≥690 testów po 08-01).
  </behavior>
  <action>
    Dodać metodę `_buildWorktable()` po `_buildFoundation()` per D-Phase8-02:

    - **JSDoc:** GEO-02 / D-Phase8-02 kontrakt, derywacja pozycji z PhysicsEngine, clearance rationale (slider min Y - tableTop ≥ 0.15), boundary (decoration, this.group), materiał placeholder (Phase 9 MAT-04).
    - **Derywacja pozycji (in-code, deterministyczna):**
      ```js
      // Min slider center Y over full rotation (PhysicsEngine-derived).
      // Dla r<l zawsze: max(currentY) = r + l @ angle=0.
      const sliderMinCenterY = this.shaftY - (this.r + this.l);  // = 3.2 dla r=0.8, l=4.0
      const sliderHalfH = 0.75;  // BoxGeometry(2, 1.5, 1.5) → 1.5/2
      const sliderMinBottom = sliderMinCenterY - sliderHalfH;     // = 2.45
      const tableHeight = 0.3;
      const clearance = 0.2;                                       // user choice z CONTEXT
      const tableTopY = sliderMinBottom - clearance;               // = 2.25
      const tableCenterY = tableTopY - tableHeight / 2;            // = 2.10
      ```
    - **Stół:** `new THREE.BoxGeometry(3, 0.3, 2.5)`, materiał lokalny `new THREE.MeshStandardMaterial({ color: 0x5a5a5a, roughness: 0.5 })` (steel grey placeholder per D-Phase8-06), pozycja `(0, tableCenterY, 0)`, `castShadow = receiveShadow = true`, `userData = { kind: 'decoration' }`, dziecko `this.group`.

    Wywołanie w `buildPress()`: `this._buildWorktable();` po `this._buildFoundation();` (przed `this.update(0)`).

    Uruchomić `npm test -- tests/PressModel.worktable.test.js` → 7-8/8 PASS. Następnie `npm test` (full) → ≥690 PASS. Commit: `feat(08-02): add worktable with KIN-aware clearance (GEO-02)`.

    **NIE hardkodować y=2.10** — derywacja z this.r/this.l/this.shaftY jest **wymagana** (gdy user zmieni r/l w PressModel.js, stół auto-dopasuje się). Test #4 i tak chroni inwariant kolizji.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test 2>&amp;1 | tail -15</automated>
    <expected>All tests pass (≥690 total)</expected>
  </verify>
  <done>
    - `_buildWorktable()` w PressModel.js (grep -nc → 2)
    - Stół @ y obliczone z PhysicsEngine (NIE hardcoded 5.0 z CONTEXT fallback)
    - KIN-aware clearance test #4 PASS dla 16 kątów
    - getInteractables().size === 15 niezmienione
    - Full regression zielona
  </done>
</task>

</tasks>

<verification>
- `grep -nc "_buildWorktable" src/PressModel.js` → 2
- `grep -nc "this.shaftY - (this.r + this.l)" src/PressModel.js` → 1 (auto-fit formula)
- `npm test` → ≥690/690 PASS
- `npm run build` → <800KB main bundle
</verification>

<success_criteria>
1. GEO-02 spełniony: stół widoczny pod suwakiem, KIN-aware position
2. KIN-01 invariant: stół statyczny dla pełnego cyklu obrotu
3. NO collision: stół.top + 0.15 ≤ slider.bottom dla każdego kąta
4. D-Phase8-05: stół poza getInteractables/getMeshDictionary
5. Build <800KB
</success_criteria>

<output>
Create `.planning/phases/08-press-body-expansion/08-02-SUMMARY.md` — opisując derywację pozycji (z r=0.8, l=4.0 → y_center=2.10), różnice względem CONTEXT fallback (skorygowano z y≈5.0 do y≈2.10 bo r/l są inne niż w CONTEXT), bundle delta.
</output>
