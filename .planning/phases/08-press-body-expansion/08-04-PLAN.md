---
phase: 08-press-body-expansion
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - tests/PressModel.phase8.integration.test.js
  - tests/PressModel.anchoring.test.js
autonomous: true
task_count: 2
requirements: [GEO-05, MAT-04, "TEST-06 (partial)", "TEST-08 (partial)"]
tags: [integration, regression, decoration-audit, build-budget, D-Phase8-05, D-Phase8-07]
must_haves:
  truths:
    - "Wszystkie 11 decoration meshes z Phase 8 + Phase 7 obecne i policzalne"
    - "getInteractables().size === 15 niezmienione (Phase 2 baseline preserved)"
    - "getMeshDictionary().size === 15 niezmienione"
    - "PressModel.js imports niezmienione (boundary D-Phase7-05)"
    - "Build <800KB main bundle (TEST-08 partial, Phase 9 final)"
    - "Floor invariant rozszerzony do `>= -0.8` (uwzględnia fundament)"
  artifacts:
    - path: "tests/PressModel.phase8.integration.test.js"
      provides: "Suma assertion: 11 decoration meshes, 15 interactables, boundary, build budget metadata"
    - path: "tests/PressModel.anchoring.test.js"
      provides: "Floor invariant test #3 rozszerzony z 2.0 → -0.8 (Phase 8 floor)"
  key_links:
    - from: "Phase 8 plans 08-01..08-03"
      to: "phase8.integration test"
      via: "verify aggregate state"
      pattern: "decoration meshes === 11"
---

<objective>
Plan zamykający Phase 8: integracyjny test który weryfikuje że plany 08-01, 08-02, 08-03 zmergeowane razem dają spójny stan (11 decoration meshes, 15 interactables niezmienione, build <800KB, anchoring test rozszerzony o nowy floor).

Purpose: TEST-06/08 partial — gate przed Phase 9. Jeden punkt kontroli całej fazy zamiast wierzyć że per-plan testy złożone razem dają completeness.

Output: 1 nowy plik testowy (integration) + 1 zmodyfikowany plik testowy (anchoring floor invariant update).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-press-body-expansion/08-CONTEXT.md
@.planning/phases/08-press-body-expansion/08-01-SUMMARY.md
@.planning/phases/08-press-body-expansion/08-02-SUMMARY.md
@.planning/phases/08-press-body-expansion/08-03-SUMMARY.md
@.planning/phases/07-kinematic-fix-anchoring/07-03-SUMMARY.md

<interfaces>
Expected decoration meshes po Phase 8 (cumulative):
| Source plan | Mesh | Geometry | World pos |
|---|---|---|---|
| 07-02 | bearing-left | Cylinder R=0.6 H=0.8 | (-2, 8, 0) |
| 07-02 | bearing-right | Cylinder R=0.6 H=0.8 | (+2, 8, 0) |
| 08-01 | foundation | Box(6, 0.8, 4) | (0, -0.4, 0) |
| 08-01 | anchor-bolt × 4 | Cylinder R=0.1 H=0.3 | (±2.8, -0.15, ±1.8) |
| 08-02 | worktable | Box(3, 0.3, 2.5) | (0, ~2.10, 0) |
| 08-03 | bracket-left | Box(0.4, 1.0, 1.0) | (-2, 8, -0.5) |
| 08-03 | bracket-right | Box(0.4, 1.0, 1.0) | (+2, 8, -0.5) |
| 08-03 | mid-brace | Box(4, 0.4, 0.4) | (0, 4, -1) |

**Total: 11 decoration meshes.**

From tests/PressModel.anchoring.test.js (linia 71-79, Phase 7 baseline):
```javascript
it('każdy interactable mesh ma worldPosition.y > 0 (strict — Phase 7 baseline; podstawa @ y=0 w Phase 8)', () => {
  // Najniższy obecnie interactable: panel-oburezny @ y=2.0
  ...
  expect(v.y).toBeGreaterThan(0);
});
```

Phase 8 floor invariant: fundament siedzi @ y=-0.8..0. Test floor MUSI być rozszerzony — interactables nadal >= 0 (żaden interactable nie zszedł poniżej; fundament to decoration, nie interactable). Update komentarza + opcjonalnie dodać secondary assertion: decoration meshes y_min >= -0.8.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test — Phase 8 aggregate decoration audit</name>
  <files>tests/PressModel.phase8.integration.test.js</files>
  <action>
    Stworzyć `tests/PressModel.phase8.integration.test.js`. Wzór canvas mock + import THREE + PressModel.

    Testy (≥8):
    1. **Total decoration count:** `pressModel.group.traverse` filter `userData.kind === 'decoration'` → length === 11 (per tabela w `<interfaces>`).
    2. **Per-mesh inventory:** breakdown by geometry signature:
       - Cylinder R=0.6 H=0.8 → 2 (łożyska Phase 7)
       - Box 6×0.8×4 → 1 (fundament)
       - Cylinder R=0.1 H=0.3 → 4 (śruby kotwowe)
       - Box 3×0.3×2.5 → 1 (stół)
       - Box 0.4×1.0×1.0 → 2 (brackets)
       - Box 4×0.4×0.4 → 1 (mid-brace)
       - Total: 2+1+4+1+2+1 = 11 ✓
    3. **getInteractables invariant:** `size === 15` niezmienione (Phase 2 baseline, preserved przez 8 planów v1.0 + 4 plany Phase 7 + 4 plany Phase 8).
    4. **getMeshDictionary invariant:** `size === 15`.
    5. **Boundary:** wczytać `src/PressModel.js` jako tekst (fs.readFileSync), regex `/^import .+ from/gm`, assertion że wszystkie imports są w whitelist `['three', './PhysicsEngine', './i18n/pl.js', './MaterialRegistry.js']`. Per D-Phase7-05.
    6. **KIN-01 static under rotation:** snapshot world positions wszystkich 11 decoration meshes przed `update(0)` i po `update(π)`. Wszystkie 11 niezmienione tolerance 1e-6.
    7. **Forbidden interactable IDs (Phase 8 surface):** `fundament`, `foundation`, `stol`, `stol-roboczy`, `worktable`, `sruba-kotwowa`, `anchor-bolt`, `wspornik-lewy`, `wspornik-prawy`, `bracket-left`, `bracket-right`, `cross-brace`, `mid-brace` → wszystkie `interactables.has(...) === false`.
    8. **Decoration floor invariant:** `decoration.y_world.min >= -0.8` (fundament center y=-0.4, half=0.4 → bottom y=-0.8; wszystko inne wyżej).
    9. **Materials check:** wszystkie decoration meshes mają instancję `MeshStandardMaterial` (Phase 9 PBR upgrade-ready).
    10. **Build budget gate (metadata-only):** asercja dummy `expect(true).toBe(true)` + komentarz że human verify gate na `npm run build`. (Vitest nie odpala build; pełna verify w `<verify>`.)

    Uruchomić `npm test -- tests/PressModel.phase8.integration.test.js` — wszystko zielone (zakłada że 08-01..08-03 zmergeowane przed Wave 3). Commit: `test(08-04): add Phase 8 integration audit (11 decoration meshes, boundary, KIN-01)`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test -- tests/PressModel.phase8.integration.test.js 2>&amp;1 | tail -20 &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
    <expected>Integration test 10/10 PASS; build success; main bundle <800KB</expected>
  </verify>
  <done>
    - Plik istnieje (≥10 testów)
    - Wszystkie testy PASS
    - `npm run build` < 800KB main bundle (logged w SUMMARY)
    - Commit w git log
  </done>
</task>

<task type="auto">
  <name>Task 2: Update floor invariant w anchoring test — Phase 8 baseline</name>
  <files>tests/PressModel.anchoring.test.js</files>
  <action>
    Otworzyć `tests/PressModel.anchoring.test.js` (273 linii, Phase 7-03). Zmodyfikować test #3 (linia 71-79 obecny "worldPosition.y > 0 strict — Phase 7 baseline").

    **Zmiany:**
    1. **Update komentarz:** zaktualizować z "Phase 7 baseline; podstawa @ y=0 w Phase 8" na "Phase 8 baseline: fundament decoration @ y=-0.8..0, ale wszystkie interactables nadal y > 0 (najniższy: panel-oburezny @ y=2.0). Floor invariant interactables nieaffected by fundament (decoration ≠ interactable)."
    2. **Asercja niezmieniona:** nadal `expect(v.y).toBeGreaterThan(0)` (interactables NIE schodzą do fundamentu — to dla nich kontrakt: każdy interactable >0).
    3. **NOWY test #4** dodać: `it('Phase 8 decoration meshes nie powodują że jakikolwiek interactable spadł poniżej fundamentu (y_min >= -0.8 - EPSILON)', ...)` — defensywne: iteruje interactables, weryfikuje `y > -0.8 - EPSILON` (czyli żaden interactable nie wpadł do fundamentu).
    4. **Komentarz top-of-file:** dopisać linijkę nad linia 9 ANCHOR-02: `// Phase 8: floor invariant rozszerzony o decoration fundament @ y=-0.8 (test #4). Interactables NADAL >0 — fundament to pure visual layer.`

    Uruchomić `npm test -- tests/PressModel.anchoring.test.js` → ≥14/14 PASS (13 baseline + 1 new). Pełna regresja `npm test` → wszystko zielone. Commit: `test(08-04): extend anchoring floor invariant for Phase 8 foundation (-0.8 baseline)`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test 2>&amp;1 | tail -10</automated>
    <expected>All tests pass (≥700 total — 676 Phase 7 + ~8 foundation + ~8 worktable + ~8 brackets + ~10 integration + 1 anchoring extension)</expected>
  </verify>
  <done>
    - anchoring.test.js zmodyfikowany (komentarz + nowy test #4)
    - 14+ testy passing w anchoring suite
    - Full regression zielona
    - Commit w git log
  </done>
</task>

</tasks>

<verification>
- `npm test` → ≥700/700 PASS
- `npm run build` → main bundle <800KB (D-Phase8-07 hard limit; TEST-08 final w Phase 9 z 850KB)
- `grep -nc "kind: 'decoration'" src/PressModel.js` → 11 (2 łożyska + 1 fundament + 4 śruby + 1 stół + 2 brackets + 1 mid-brace)
- `grep -c "^import " src/PressModel.js` → 4 (THREE + PhysicsEngine + i18n/pl + MaterialRegistry — boundary preserved)
- `pressModel.getInteractables().size === 15` (asserted w integration test)
</verification>

<success_criteria>
1. Wszystkie 6 Phase 8 success criteria z ROADMAP.md zaspokojone:
   - ✅ Fundament + 4 śruby (08-01)
   - ✅ Stół roboczy KIN-aware (08-02)
   - ✅ Wsporniki łożysk (08-03)
   - ✅ Cross-brace minimal (08-03)
   - ✅ getInteractables().size === 13... ZWERYFIKOWAĆ: ROADMAP mówi 13 ale Phase 2 baseline + Phase 7 audit dało 15. Test używa 15 (live state). Update ROADMAP w SUMMARY jeśli rozbieżność wpłynie na audit.
   - ✅ Boundary (PressModel.js imports niezmienione)
2. TEST-06 partial: 683→~700 testów, brak regresji
3. TEST-08 partial: <800KB main bundle (Phase 9 weźmie do 850KB final budget)
4. Phase 8 GEO-01..05 wszystkie spełnione i przetestowane
</success_criteria>

<output>
Create `.planning/phases/08-press-body-expansion/08-04-SUMMARY.md` + Phase 8 closing notes:
- Per-plan diff (linki do 08-01/02/03 SUMMARY)
- Bundle size delta total (Phase 7 770.62 KB → Phase 8 ??? KB)
- Decoration meshes inventory (11 total)
- ROADMAP discrepancy note: success criterion #5 mówi "size===13" ale faktyczna baseline od Phase 2 to 15 (Phase 7-03 audit). Rekomendacja: update ROADMAP w STATE.md (out of scope tego planu, ale udokumentować).
- Phase 9 readiness: materials są placeholder MeshStandardMaterial z color only — PBR upgrade-ready.
</output>
