---
phase: 08-press-body-expansion
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/PressModel.js
  - tests/PressModel.bracketsBrace.test.js
autonomous: true
task_count: 2
requirements: [GEO-03, GEO-04, GEO-05]
tags: [decoration, bearing-brackets, cross-brace, GEO-03, GEO-04, D-Phase8-03, D-Phase8-04]
must_haves:
  truths:
    - "Łożyska Phase 7 (lewe @ x=-2, prawe @ x=+2, y=8) mają widoczne wsporniki łączące je z kolumnami ramy"
    - "Między kolumnami ramy widoczna jest środkowa belka (mid-brace) na wysokości y≈4 (między fundamentem a topFrame)"
    - "Wsporniki + cross-brace statyczne (KIN-01), dzieci this.group"
    - "Żadne nowe meshy nie pojawiają się w getInteractables() ani getMeshDictionary()"
  artifacts:
    - path: "src/PressModel.js"
      provides: "_buildBearingBrackets() + _buildCrossBrace() metody"
      contains: "_buildBearingBrackets"
    - path: "tests/PressModel.bracketsBrace.test.js"
      provides: "Testy 2 wsporników + 1 mid-brace decoration meshes"
  key_links:
    - from: "bearing brackets"
      to: "bearings @ (±2, 8, 0)"
      via: "współdzielona oś x i shaftY"
      pattern: "_buildBearingBrackets"
    - from: "mid-brace"
      to: "leftFrame, rightFrame columns"
      via: "łączy x=-2 do x=+2 na y≈4"
      pattern: "_buildCrossBrace"
---

<objective>
Dodać (a) **wsporniki łożysk** (brackets) łączące Phase 7 łożyska wału z kolumnami ramy oraz (b) **środkową belkę poprzeczną** (mid-brace) między kolumnami. Wszystkie meshy decoration.

Purpose:
- GEO-03: wsporniki eliminują wizualną lukę między łożyskiem (worldX=±2, z=0) a kolumną (worldX=±2, z=-1) — łożyska wyglądają jak naprawdę przykręcone do ramy
- GEO-04: cross-brace dodaje industrial-press feel bez przeglądu chamfered corners (D-Phase8-04 minimum subtelne)

**WAŻNE (D-Phase8-04 audit):** `topFrame` już istnieje (PressModel.js linia 88-93, BoxGeometry(6,1,2) @ y=shaftY+1.5=9.5) — łączy obie kolumny u góry. Phase 8 dodaje **TYLKO środkową belkę** (mid-brace), żadnych chamfers, żadnych diagonalnych X-cross.

Output: 3 nowe decoration meshes (2 brackets + 1 mid-brace). Total decoration meshes po tym planie + 08-01 + 08-02: 11 (2 łożyska + 5 fundament + 1 stół + 2 brackets + 1 mid-brace).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-press-body-expansion/08-CONTEXT.md
@.planning/phases/08-press-body-expansion/08-01-SUMMARY.md
@.planning/phases/07-kinematic-fix-anchoring/07-02-SUMMARY.md

<interfaces>
From src/PressModel.js (Phase 7-02 bearings @ linie 759-777):
```javascript
// Łożyska Phase 7:
bearingLeft.position.set(-2.0, this.shaftY, 0);   // worldX=-2, z=0
bearingRight.position.set(2.0, this.shaftY, 0);   // worldX=+2, z=0
```

Frame columns (linie 76-86):
```javascript
// leftFrame: BoxGeometry(2, shaftY+2, 2), position (-2, (shaftY+2)/2, -1)  → world x=-2, z=-1, y=0..shaftY+2=10
// rightFrame: same, x=+2
// → Z-gap between bearing (z=0) and column (z=-1) = 1.0 unit → bracket BoxGeometry(0.4, 1.0, 1.0) @ (-2, 8, -0.5) wypełnia tę lukę
```

topFrame (linie 88-93) — już istnieje, łączy kolumny u góry:
```javascript
// topFrame: BoxGeometry(6, 1, 2), position (0, shaftY+1.5, -1) → y=9..10, x=-3..+3, z=-2..0
```

Mid-brace target: y między fundamentem (y=0) a topFrame (y=9.5) — wybierz y=4 (środek roboczy, nad osłonami z y=4-5 ale poniżej wału).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — testy 2 wsporniki łożysk + 1 mid-brace</name>
  <files>tests/PressModel.bracketsBrace.test.js</files>
  <behavior>
    Stworzyć `tests/PressModel.bracketsBrace.test.js`. 

    Cases:
    1. Total decoration count: traverse + filter → length === 11 (jeśli plan 08-02 zmerge wcześniej; jeśli nie — test używa **różnicy**: liczy decoration meshes które są BoxGeometry z width=0.4 → 2 brackets, ORAZ box z width=4 → 1 mid-brace).
    
    Lepsze podejście: testy specyficzne per-mesh, NIE total count (żeby 08-02 i 08-03 mogły mergeować w dowolnej kolejności).
    
    2. **Wsporniki (2 meshes):** filter decoration meshes po `geometry.type==='BoxGeometry'` i `parameters.width === 0.4 && parameters.height === 1.0 && parameters.depth === 1.0` → length === 2. World positions sortowane po x: `(-2, 8, -0.5)` i `(+2, 8, -0.5)` tolerance 1e-6.
    3. **Mid-brace (1 mesh):** filter po `geometry.type==='BoxGeometry'` i `parameters.width === 4 && parameters.height === 0.4 && parameters.depth === 0.4` → length === 1. World position `(0, 4, -1)` tolerance 1e-6 (z=-1 zgodnie z kolumną).
    4. Wszystkie 3 mają `userData.kind === 'decoration'`.
    5. Wszystkie 3 są dziećmi `this.group` (NIE shaftAxis).
    6. Wszystkie 3 statyczne pod update(π/2) — world position niezmieniona tolerance 1e-6.
    7. `getInteractables().size === 15` niezmienione.
    8. `getMeshDictionary().size === 15` niezmienione.
    9. Forbidden IDs: `wspornik-lewy`, `wspornik-prawy`, `bracket-left`, `bracket-right`, `cross-brace`, `mid-brace` → interactables.has(...) === false.
  </behavior>
  <action>
    Skopiować strukturę z `tests/PressModel.bearings.test.js`. Filtry per-geometry-params dla niezależności od kolejności merge z 08-02. Uruchomić `npm test -- tests/PressModel.bracketsBrace.test.js` — failuje. Commit: `test(08-03): add failing tests for bearing brackets + mid-brace`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test -- tests/PressModel.bracketsBrace.test.js 2>&amp;1 | tail -20</automated>
    <expected>≥5 failures (no _buildBearingBrackets / _buildCrossBrace yet)</expected>
  </verify>
  <done>Plik istnieje, ≥8 testów, failures udokumentowane, commit RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — _buildBearingBrackets() + _buildCrossBrace()</name>
  <files>src/PressModel.js</files>
  <behavior>
    Po implementacji wszystkie testy bracketsBrace zielone + full regresja.
  </behavior>
  <action>
    Dodać 2 metody po `_buildWorktable()` (lub po `_buildFoundation()` jeśli 08-02 nie scalony jeszcze).

    **`_buildBearingBrackets()` per D-Phase8-03:**
    - JSDoc: GEO-03, łączą łożyska z kolumnami, materiał `this.matBody` (wizualna spójność ze szkieletem), boundary.
    - Współdzielona `new THREE.BoxGeometry(0.4, 1.0, 1.0)` (waga: szerokość X=0.4 cienka, wysokość Y=1.0 obejmuje łożysko h=0.8 z 0.1 margin top/bottom, głębokość Z=1.0 wypełnia od bearing.z=0 do kolumna.z=-0.5 center).
    - `bracketLeft` @ `(-2, this.shaftY, -0.5)` (między bearing @ z=0 i kolumna @ z=-1)
    - `bracketRight` @ `(2, this.shaftY, -0.5)`
    - Każdy: `castShadow = receiveShadow = true`, `userData = { kind: 'decoration' }`, `this.group.add(...)`.

    **`_buildCrossBrace()` per D-Phase8-04 (minimal — TYLKO mid-brace, NIE chamfers, NIE X-cross):**
    - JSDoc: GEO-04 minimum subtelne, `topFrame` już łączy u góry, dodajemy tylko środkową belkę. Defer chamfered corners do v1.2+ (CONTEXT Deferred Ideas).
    - Pre-check (komentarz w kodzie): `// AUDIT D-Phase8-04: topFrame @ linia 88-93 już łączy kolumny u góry (y=9.5). Phase 8 dodaje TYLKO mid-brace.`
    - `midBraceGeo = new THREE.BoxGeometry(4, 0.4, 0.4)` (X=4 łączy x=-2 do x=+2, Y=0.4 cienka, Z=0.4 cienka)
    - Materiał: `this.matBody` (reuse — D-Phase8-06)
    - Pozycja: `(0, 4, -1)` — środek roboczy, na z=-1 zgodnie z kolumnami
    - `castShadow = receiveShadow = true`, `userData = { kind: 'decoration' }`, `this.group.add(midBrace)`.

    Wywołanie w `buildPress()` po `this._buildWorktable();`:
    ```js
    this._buildBearingBrackets();   // Phase 8 GEO-03 — D-Phase8-03
    this._buildCrossBrace();        // Phase 8 GEO-04 — D-Phase8-04 (minimal mid-brace)
    ```

    Uruchomić `npm test -- tests/PressModel.bracketsBrace.test.js` → PASS. Następnie `npm test` full → ≥697 PASS. Commit: `feat(08-03): add bearing brackets + mid-brace (GEO-03, GEO-04)`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test 2>&amp;1 | tail -15</automated>
    <expected>All tests pass (≥697 total)</expected>
  </verify>
  <done>
    - `_buildBearingBrackets`, `_buildCrossBrace` methods exist (grep -nc → 2 + 2)
    - 3 decoration meshes added: 2 brackets + 1 mid-brace
    - getInteractables().size === 15
    - Full regression zielona
  </done>
</task>

</tasks>

<verification>
- `grep -nc "_buildBearingBrackets\|_buildCrossBrace" src/PressModel.js` → 4
- Audit topFrame istnieje (nie duplikujemy): `grep -nc "topFrame" src/PressModel.js` ≥ 5 (z Phase 1)
- `npm test` → ≥697/697 PASS
- `npm run build` → <800KB
</verification>

<success_criteria>
1. GEO-03: 2 brackets widoczne między łożyskami a kolumnami
2. GEO-04 minimum: 1 mid-brace dodany (TYLKO — żadnych chamfers/X-cross per D-Phase8-04)
3. KIN-01 invariant zachowany
4. D-Phase8-05: wszystkie 3 meshe poza interactables
5. Build <800KB
</success_criteria>

<output>
Create `.planning/phases/08-press-body-expansion/08-03-SUMMARY.md` — opisując 2 brackets + 1 mid-brace, audit topFrame (potwierdzić w kodzie że już istnieje, nie duplikować), defer chamfers (Deferred Ideas).
</output>
