---
phase: 08-press-body-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PressModel.js
  - tests/PressModel.foundation.test.js
autonomous: true
task_count: 2
requirements: [GEO-01, GEO-05]
tags: [decoration, foundation, anchor-bolts, GEO-01, D-Phase8-01]
must_haves:
  truths:
    - "Pod istniejącą podstawą (base @ y=0..1) widoczny jest fundament (foundation) jako szeroka płyta"
    - "4 śruby kotwowe widoczne w narożnikach fundamentu (czarne, matowe walce)"
    - "Fundament nie koliduje z żadnym istniejącym meshem (siedzi y=-0.8..0, poniżej base)"
    - "Hierarchia obecna (rama, wał, suwak, panel) NIETKNIĘTA — żaden Y nie zmieniony"
    - "Fundament + śruby nie pojawiają się w getInteractables() ani getMeshDictionary()"
  artifacts:
    - path: "src/PressModel.js"
      provides: "_buildFoundation() metoda dodająca 1× foundation BoxGeometry + 4× anchor bolt CylinderGeometry"
      contains: "_buildFoundation"
    - path: "tests/PressModel.foundation.test.js"
      provides: "Testy decoration meshes count, geometry, world positions, interactables niezmienione"
  key_links:
    - from: "src/PressModel.js buildPress()"
      to: "_buildFoundation()"
      via: "wywołanie po _buildBearings() przed this.update(0)"
      pattern: "_buildFoundation\\(\\)"
    - from: "foundation + 4 anchor bolts"
      to: "this.group.add(...)"
      via: "userData.kind='decoration'"
      pattern: "kind: 'decoration'"
---

<objective>
Dodać fundament (podstawę przemysłową) jako szeroką płytę pod istniejącym `base` meshem, plus 4 śruby kotwowe widoczne w narożnikach. Wszystkie meshy `userData.kind='decoration'`, dzieci `this.group`, NIE rejestrowane w interactables.

Purpose: GEO-01 — prasa wygląda jak przykręcona do podłogi (industrial install), nie unosi się w powietrzu. Fundament + śruby = wizualna kontynuacja anchor narrative z Phase 7 (łożyska dla wału, fundament dla całej prasy).

Output: 5 nowych decoration meshes (1 fundament + 4 śruby). Total decoration meshes po tym planie: 7 (2 łożyska + 5 fundament).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-press-body-expansion/08-CONTEXT.md
@.planning/phases/07-kinematic-fix-anchoring/07-02-SUMMARY.md

<interfaces>
<!-- Decoration mesh wzór z Phase 7-02 (_buildBearings) — replicate -->

From src/PressModel.js (Phase 7-02 _buildBearings, linie 759-777):
```javascript
_buildBearings() {
  const bearingGeo = new THREE.CylinderGeometry(0.6, 0.6, 0.8, 32);
  bearingGeo.rotateZ(Math.PI / 2);
  const bearingLeft = new THREE.Mesh(bearingGeo, this.matBody);
  bearingLeft.position.set(-2.0, this.shaftY, 0);
  bearingLeft.castShadow = true;
  bearingLeft.receiveShadow = true;
  bearingLeft.userData = { kind: 'decoration' };  // <-- minimalny kontrakt
  this.group.add(bearingLeft);                     // <-- NIE shaftAxis
  // (right symmetric)
}
```

Existing `base` mesh (linie 69-73):
```javascript
const baseGeo = new THREE.BoxGeometry(6, 1, 4);
const base = new THREE.Mesh(baseGeo, this.matBase);  // matBase = 0x333333
base.position.y = 0.5;  // siedzi y=0..1
```

PressModel boundary (D-Phase7-05): importy TYLKO THREE + PhysicsEngine + i18n/pl + MaterialRegistry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — testy fundament + 4 śruby kotwowe</name>
  <files>tests/PressModel.foundation.test.js</files>
  <behavior>
    Stworzyć `tests/PressModel.foundation.test.js` (wzór: `tests/PressModel.bearings.test.js`). Testy MUSZĄ failować przed Task 2.

    Cases:
    1. Total decoration meshes count: `pressModel.group.traverse` filter `userData.kind === 'decoration'` → length === 7 (2 łożyska Phase 7 + 1 fundament + 4 śruby = 7).
    2. Fundament: dokładnie 1 mesh z `geometry.type === 'BoxGeometry'`, `parameters.width === 6`, `height === 0.8`, `depth === 4`. World position (0, -0.4, 0) tolerance 1e-6 (środek bryły; y=-0.8..0).
    3. Śruby kotwowe: dokładnie 4 meshy z `geometry.type === 'CylinderGeometry'`, `radiusTop === radiusBottom === 0.1`, `height === 0.3`. World positions w 4 narożnikach fundamentu: (±2.8, -0.15, ±1.8) tolerance 1e-6 (sort po x+z).
    4. Fundament + śruby są dziećmi `this.group` (NIE `this.shaftAxis`) — KIN-01 invariant.
    5. Fundament + śruby statyczne pod `update(π/2)` (snapshot world position przed/po, tolerance 1e-6).
    6. `getInteractables().size === 15` NIEZMIENIONE (Phase 2 baseline).
    7. `getMeshDictionary().size === 15` NIEZMIENIONE.
    8. Forbidden IDs: `interactables.has('fundament')`, `'foundation'`, `'sruba-kotwowa'`, `'anchor-bolt'` → false dla wszystkich.
    9. Floor invariant rozszerzony: `tests/PressModel.anchoring.test.js` test #1 (`worldPosition.y >= 0 - EPSILON`) NADAL zielony po dodaniu fundamentu (fundament nie jest interactable, więc nie wpływa, ale verify że żaden interactable nie wpadł poniżej; Task 4 plan-04 rozszerzy floor o nowy baseline `>= -0.8`).
  </behavior>
  <action>
    Skopiować strukturę z `tests/PressModel.bearings.test.js` (canvas mock + describe + beforeEach). Dodać 8-9 testów per <behavior>. Sortowanie 4 śrub po `x` then `z`. Uruchomić `npm test -- tests/PressModel.foundation.test.js` — testy MUSZĄ failować (no `_buildFoundation` jeszcze). Commit: `test(08-01): add failing tests for foundation + 4 anchor bolts`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test -- tests/PressModel.foundation.test.js 2>&amp;1 | tail -20</automated>
    <expected>≥6 failures (no _buildFoundation yet); reszta failuje na count !== 7</expected>
  </verify>
  <done>Plik istnieje, ≥8 testów, run pokazuje failures, commit RED w git log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — _buildFoundation() implementacja</name>
  <files>src/PressModel.js</files>
  <behavior>
    Po implementacji wszystkie testy z Task 1 zielone, oraz 683/683 pełna regresja (676 Phase 7 baseline + 7-8 nowych foundation testów).
  </behavior>
  <action>
    Dodać metodę `_buildFoundation()` po `_buildBearings()` (po linii 777) per D-Phase8-01:

    - **JSDoc:** opis GEO-01 / D-Phase8-01, geometria, boundary (decoration, this.group, brak _registerInteractable), materiał placeholder (Phase 9 PBR), referencja do Phase 9 MAT-03.
    - **Fundament:** `new THREE.BoxGeometry(6, 0.8, 4)`, materiał lokalny `new THREE.MeshStandardMaterial({ color: 0x3a3a3a })` (placeholder per D-Phase8-06; Phase 9 MAT-03 dorobi PBR), pozycja `(0, -0.4, 0)` (środek bryły, y=-0.8..0), `castShadow = receiveShadow = true`, `userData = { kind: 'decoration' }`, dziecko `this.group`.
    - **4 śruby kotwowe:** współdzielona `new THREE.CylinderGeometry(0.1, 0.1, 0.3, 16)`, materiał lokalny `new THREE.MeshStandardMaterial({ color: 0x1a1a1a, roughness: 0.9 })` (czarny matowy per D-Phase8-06), pozycje narożników: `(-2.8, -0.15, -1.8)`, `(2.8, -0.15, -1.8)`, `(-2.8, -0.15, 1.8)`, `(2.8, -0.15, 1.8)` (top śruby na poziomie y=0 = górna powierzchnia fundamentu; head wystaje 0.15 nad fundament wewnątrz negatywnego Y poniżej linii base). Dla każdej śruby: `castShadow = true`, `userData = { kind: 'decoration' }`, `this.group.add(bolt)`.

    Wywołanie w `buildPress()`: dodać `this._buildFoundation();` w linii 170 (po `this._buildBearings()`, przed `// Inicjalizacja położenia`).

    NIE wywoływać `_registerInteractable` dla żadnego z 5 meshy (D-Phase8-05). NIE dodawać wpisów w `src/i18n/pl.js`.

    Per D-Phase8-01: NIE przesuwać `this.group` o `+y=0.8` (cascade ryzyko 683 testów) — fundament adoptuje y=-0.8..0 poniżej istniejącej hierarchii.

    Uruchomić `npm test -- tests/PressModel.foundation.test.js` — 8/8 PASS. Następnie `npm test` (full regression) — 683+/683+ PASS. Commit: `feat(08-01): add foundation + 4 anchor bolts (GEO-01)`.
  </action>
  <verify>
    <automated>cd "C:/Users/mparol/Desktop/Dokumenty/Projekty/HydraulicPress" &amp;&amp; npm test 2>&amp;1 | tail -15</automated>
    <expected>All tests pass (≥683 total). Full regression zielona.</expected>
  </verify>
  <done>
    - `_buildFoundation()` istnieje w PressModel.js (grep -nc → ≥2: declaration + call)
    - 5 nowych meshes utworzonych (1 fundament + 4 śruby), wszystkie `userData.kind='decoration'`
    - `getInteractables().size === 15` niezmienione
    - npm test zielony (≥683 testów)
    - Commit GREEN w git log
  </done>
</task>

</tasks>

<verification>
- `grep -nc "_buildFoundation" src/PressModel.js` → 2 (declaration + call)
- `grep -nc "kind: 'decoration'" src/PressModel.js` → 7 (2 łożyska + 1 fundament + 4 śruby)
- `npm test` → ≥683/683 PASS
- `npm run build` → main bundle <800KB (foundation +material adds ~1-2KB)
</verification>

<success_criteria>
1. GEO-01 spełniony: fundament + 4 śruby kotwowe widoczne jako decoration meshes
2. D-Phase8-05 invariant: getInteractables/getMeshDictionary nadal size===15
3. KIN-01 invariant: wszystkie 5 nowych meshes statyczne pod update(angle)
4. PressModel boundary preserved (D-Phase7-05): importy bez zmian
5. Build budget: <800KB main bundle
6. Full regression: 683+ tests zielone
</success_criteria>

<output>
Create `.planning/phases/08-press-body-expansion/08-01-SUMMARY.md` when done, opisując zmiany w PressModel.js (linie + diff metryki), liczbę testów, bundle delta, decyzje implementacyjne (materiał lokalny vs reused matBase — czemu lokalny: D-Phase8-06 placeholder że Phase 9 dorobi PBR per group).
</output>
