---
phase: 12-data-foundations
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03"]
files_modified: []
autonomous: false
requirements: [EDU-03]
must_haves:
  truths:
    - "Full Vitest suite is green (≥903 baseline + new Phase 12 tests)"
    - "npm run build main bundle < 850 KB (text data not embedded as blobs)"
    - "All bhp strings and normRef citations reviewed by a domain expert for norm accuracy"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 12: run the full test suite + bundle gate, then pause for a domain-expert review of all BHP content (bhp strings in elementInfo.js + normRef/explanation in quizData.js) against the cited norms. The review is the STATE.md phase-close gate for EDU-03 content integrity — it does NOT block code execution of earlier plans, but the phase is not DONE until the gate is cleared.

Purpose: Guarantee no regression (TEST-09/10 spirit) and verified BHP factual accuracy (EDU-03) before downstream phases build on this data contract.
Output: Confirmed green suite + bundle, and a recorded domain-expert sign-off (or a logged list of corrections to apply).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-data-foundations/12-RESEARCH.md
@.planning/phases/12-data-foundations/12-VALIDATION.md

<!-- EDU-02 topic→norm mapping (review reference) is in 12-RESEARCH.md "EDU-02 Topic Group Mapping"
     and the Assumptions Log (A1-A3, A7) — those are the [ASSUMED] clause numbers to verify. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + bundle regression gate</name>
  <files></files>
  <read_first>
    - .planning/phases/12-data-foundations/12-VALIDATION.md (Sampling Rate + Per-Task Verification Map cross rows)
    - .planning/STATE.md (Performance Metrics: 903 green baseline, 809.94 KB bundle)
  </read_first>
  <action>
    Run the full Vitest suite and the production build. Confirm: (a) the full suite is green with NO regressions to the 903 baseline plus the new Phase 12 tests (elementInfo additive, quizData, quizSelection, boundaries), and (b) `npm run build` reports a main bundle < 850 KB. If the bundle grew meaningfully, confirm the new data is plain JS strings, not asset imports / base64 blobs (CRIT-V12-1). Record the exact test count and bundle KB in the summary.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -15 && npm run build 2>&1 | grep -iE "kb|dist/assets" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0; total passing ≥ 903 + new Phase 12 tests; zero failures
    - `npm run build` main bundle < 850 KB
    - No data file is imported as an asset (data remains plain string content)
  </acceptance_criteria>
  <done>Full suite green, bundle < 850 KB, no blob embedding; counts recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Domain-expert BHP / norm-citation review gate (EDU-03)</name>
  <action>Pause for a domain-expert review of all bhp strings (elementInfo.js) and normRef/explanation values (quizData.js) against the cited standards. This is the STATE.md phase-close content-integrity gate; it does not block earlier code execution but Phase 12 is not DONE until cleared.</action>
  <what-built>
    All 15 elementInfo `bhp` strings (Polish, norm-cited per EDU-02 topic group) and the quizData.js `normRef` + `explanation` values. Norm clause numbers were authored at SECTION level from training knowledge and flagged [ASSUMED] in 12-RESEARCH.md (Assumptions A1-A3, A7).
  </what-built>
  <how-to-verify>
    1. Open `src/data/elementInfo.js` and review each `bhp` string against its EDU-02 topic group and cited norm (mapping in 12-RESEARCH.md "EDU-02 Topic Group Mapping").
    2. Open `src/data/quizData.js` and review each question's `normRef` (correct standard + section) and `explanation` (factually correct, matches the correct answer).
    3. Verify against the actual standards: ISO 16092-1:2017, ISO 16092-2:2019, Dyrektywa 2006/42/EC, OSHA 1910.217, IEC 60204-1.
    4. For any incorrect citation or BHP statement, list the exact field (entry id / question id) and the correction needed.
    Per STATE.md, this is a phase-close gate, not a post-phase action. If a domain expert is unavailable, confirm explicitly that section-level citations are acceptable to ship as-is and that corrections are deferred to a tracked follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 12, or paste the list of bhp/normRef corrections to apply (an executor will fix them in elementInfo.js / quizData.js and re-run the affected tests before approval).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No runtime input; this plan runs tests/build and a human content review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information (content integrity) | bhp / normRef BHP content shown to trainees | mitigate | Blocking domain-expert review gate verifies norm accuracy before phase close (non-code mitigation, per STATE.md) |

Attack surface remains static authored data + a pure function. The sole material risk is content-integrity of safety/norm text; this plan's blocking human gate is its mitigation.
</threat_model>

<verification>
- `npm test` full suite green (≥903 + Phase 12 tests)
- `npm run build` < 850 KB
- Domain-expert sign-off recorded (or corrections applied and re-verified)
</verification>

<success_criteria>
- No regression to the inherited 903-test / <850 KB baseline (TEST-09/10 spirit)
- EDU-03 content integrity gate cleared: bhp + normRef reviewed and approved (or corrections applied)
</success_criteria>

<output>
Create `.planning/phases/12-data-foundations/12-04-SUMMARY.md` when done, recording the final test count, bundle KB, and the domain-expert review outcome.
</output>
