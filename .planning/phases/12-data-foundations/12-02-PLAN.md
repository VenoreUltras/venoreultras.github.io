---
phase: 12-data-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/quizData.js
  - tests/quizData.test.js
autonomous: true
requirements: [EDU-03, EXAM-01]
must_haves:
  truths:
    - "quizData.js exports a frozen quizBank array of ≥32 BHP questions"
    - "Each question has id, type, scenarioIds, category, question, normRef, explanation"
    - "Each of the 4 scenarios (uruchomienie, cykl-pracy, zatrzymanie, awaria) has ≥8 questions"
    - "Question types mix mc / tf / sequence; mc/tf carry options+correctIdx, sequence carries steps+correctOrder"
    - "Every question links to an EDU-02 topic group via its category field"
  artifacts:
    - path: "src/data/quizData.js"
      provides: "Frozen quizBank: QuizQuestion[] (≥32) covering 4 scenarios, zero imports"
      contains: "export const quizBank = Object.freeze"
    - path: "tests/quizData.test.js"
      provides: "Shape, freeze, ≥32 total, ≥8/scenario, per-type field assertions"
      exports: []
  key_links:
    - from: "tests/quizData.test.js"
      to: "src/data/quizData.js"
      via: "import { quizBank }"
      pattern: "import.*quizBank"
---

<objective>
Create `src/data/quizData.js` — a zero-import frozen `quizBank` array of ≥32 Polish BHP quiz questions covering the 4 real scenario IDs (`uruchomienie`, `cykl-pracy`, `zatrzymanie`, `awaria`), with a mix of `mc` / `tf` / `sequence` types. Each question carries `explanation` (Polish per-question feedback) and `normRef` (norm citation). Question content links to EDU-02 topic groups via `category`. Create `tests/quizData.test.js` mirroring `tests/faultRules.test.js`.

Purpose: The BHP exam question bank (EXAM-01) that Plan 03's `selectQuizQuestions` filters and that Phase 17's QuizController consumes.
Output: `src/data/quizData.js` + `tests/quizData.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-data-foundations/12-RESEARCH.md
@.planning/phases/12-data-foundations/12-PATTERNS.md

<interfaces>
<!-- QuizQuestion schema (JSDoc typedef to author at top of quizData.js). -->
QuizQuestion:
  id: string            — 'q-{scenarioId}-{nn}', kebab-case, nn zero-padded
  type: 'mc'|'tf'|'sequence'
  scenarioIds: string[] — subset of ['uruchomienie','cykl-pracy','zatrzymanie','awaria']
  category: string      — EDU-02 topic group key
  question: string      — Polish
  normRef: string       — section-level citation, e.g. 'ISO 16092-1:2017 §5.4'
  explanation: string   — Polish per-question feedback
  options?: string[]    — mc: 4 options; tf: ['Prawda','Fałsz']  (mc/tf only)
  correctIdx?: number   — 0-based index into options                (mc/tf only)
  steps?: string[]      — sequence only
  correctOrder?: number[] — sequence only, same length as steps

<!-- EDU-02 topic groups for `category` (link EXAM-01 → EDU-02):
     inspekcja-przedrozruchowa, oslony-interlock, sterowanie-oburezne,
     e-stop, energia-kola-zamachowego, sprzeglo-hamulec, loto -->

<!-- 4 scenarioIds verified from src/training/scenarios/index.js REGISTRY:
     uruchomienie | cykl-pracy | zatrzymanie | awaria. ≥8 questions per scenario. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/quizData.test.js (shape + freeze + coverage)</name>
  <files>tests/quizData.test.js</files>
  <read_first>
    - tests/faultRules.test.js (primary analog — frozen-array data-integrity test structure)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "tests/quizData.test.js — CREATE" has the full assertion blocks)
    - .planning/phases/12-data-foundations/12-RESEARCH.md (Phase Requirements → Test Map rows for EDU-03 / EXAM-01)
  </read_first>
  <behavior>
    - quizBank is a non-empty array and Object.isFrozen
    - quizBank.length >= 32
    - every question has id/type/scenarioIds(≥1)/category/question/normRef(len>5)/explanation(len>10)
    - type is one of mc/tf/sequence
    - mc & tf questions have options (≥2) + numeric correctIdx in [0, options.length)
    - sequence questions have steps[] + correctOrder[] of equal length
    - every scenarioId across all questions is in the allowed set of 4
    - each of the 4 scenarios has ≥8 questions (filter scenarioIds.includes)
  </behavior>
  <action>
    Header: `// tests/quizData.test.js`, `// @vitest-environment node`, Polish one-liner citing EXAM-01/EDU-03; import `{ quizBank }` from `../src/data/quizData.js`. Author the describe block `quizBank — data integrity (EXAM-01)` with the assertions listed in behavior, exactly per the PATTERNS.md template. This is the RED step — the import fails until Task 2 creates quizData.js.
  </action>
  <verify>
    <automated>npm test -- tests/quizData.test.js 2>&1 | grep -E "Cannot find module|quizData|EXAM-01" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/quizData.test.js exists with describe `quizBank — data integrity (EXAM-01)`
    - Test fails on missing module before Task 2 (RED confirmed)
    - Includes the ≥32-total and ≥8-per-scenario assertions and the per-type field assertions
  </acceptance_criteria>
  <done>Test file present; all required assertions written; red because src/data/quizData.js absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/data/quizData.js (≥32 questions, 4 scenarios, mixed types)</name>
  <files>src/data/quizData.js</files>
  <read_first>
    - src/training/faultRules.js (analog — frozen typed-object array, header + @typedef style)
    - src/data/elementInfo.js (analog — zero-import frozen export, Polish content, header convention)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "src/data/quizData.js — CREATE" has header, typedef, export pattern)
    - .planning/phases/12-data-foundations/12-RESEARCH.md ("The 4 Scenario Sets", "EDU-02 Topic Group Mapping", "Common Pitfalls" Pitfall 3, Pitfall 5)
  </read_first>
  <action>
    Author the JSDoc `@typedef QuestionType` and `@typedef QuizQuestion` per the interfaces block. Export `quizBank = Object.freeze([...])` with ≥32 questions: each of the 4 scenarioIds (`uruchomienie`, `cykl-pracy`, `zatrzymanie`, `awaria`) must be covered by ≥8 questions (a question may appear in multiple scenarios via its `scenarioIds` array, but reach ≥8 per scenario without forcing artificial overlap — aim for ~32-40 distinct). Mix all three `type` values across the bank; mc questions get 4 `options` + `correctIdx`; tf questions get `options: ['Prawda','Fałsz']` + `correctIdx`; sequence questions get `steps[]` + `correctOrder[]` of equal length. Set `category` to one of the EDU-02 topic group keys to link EXAM-01 content to EDU-02 (cover guards+interlock, two-hand control, E-stop, flywheel energy, clutch-brake, LOTO, pre-start inspection). Polish `question`/`explanation`; `normRef` at SECTION level only (domain-expert gate is Plan 04). `id` format `q-{scenarioId}-{nn}`. Top-level shallow `Object.freeze` only (do NOT freeze inner objects). ZERO import statements. This is the GREEN step.
  </action>
  <verify>
    <automated>npm test -- tests/quizData.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/quizData.test.js` fully green
    - `grep -c "^import" src/data/quizData.js` returns 0 (zero imports)
    - `grep -c "id: 'q-" src/data/quizData.js` returns ≥32
    - File contains `export const quizBank = Object.freeze([`
    - At least one question of each type (mc, tf, sequence) present
    - Every category value is one of the 7 EDU-02 topic-group keys
  </acceptance_criteria>
  <done>quizBank exports ≥32 frozen questions, ≥8 per scenario, mixed types, EDU-02-linked categories, zero imports; quizData.test.js green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static authored data, no runtime input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information (content integrity) | normRef / explanation text in quizData.js | mitigate | Section-level citations only; full accuracy verified by domain-expert review gate in Plan 04 (non-code mitigation) |

Attack surface is a static frozen data export — no network, no user input, no auth, no DB. Only concern is content-integrity of BHP/norm text, handled via the Plan 04 domain-expert gate.
</threat_model>

<verification>
- `npm test -- tests/quizData.test.js` fully green
- Zero imports in src/data/quizData.js
- `npm test` full suite stays ≥903 green
</verification>

<success_criteria>
- quizBank: ≥32 frozen Polish questions, ≥8 per scenario, mc/tf/sequence mix (EXAM-01)
- Each question carries normRef + explanation; category links to EDU-02 topic groups (EDU-03 + EXAM-01)
- Pure zero-import frozen data module
</success_criteria>

<output>
Create `.planning/phases/12-data-foundations/12-02-SUMMARY.md` when done.
</output>
