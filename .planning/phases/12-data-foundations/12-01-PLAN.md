---
phase: 12-data-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/elementInfo.js
  - tests/elementInfo.test.js
autonomous: true
requirements: [EDU-01, EDU-02, EDU-03]
must_haves:
  truths:
    - "Each of the 15 elementInfo entries has a bhp string and a media array"
    - "Existing consumers reading entry.safety/sopSteps/function work unchanged"
    - "elementInfo stays Object.frozen after extension"
    - "Existing elementInfo.test.js 5-field + frozen assertions stay green without modification"
  artifacts:
    - path: "src/data/elementInfo.js"
      provides: "bhp (Polish norm-cited string) + media: [] on all 15 entries"
      contains: "bhp:"
    - path: "tests/elementInfo.test.js"
      provides: "Additive assertions for bhp (string, length>20) and media (empty array)"
      contains: "Phase 12 extensions"
  key_links:
    - from: "tests/elementInfo.test.js"
      to: "src/data/elementInfo.js"
      via: "import { elementInfo }"
      pattern: "import.*elementInfo"
---

<objective>
Extend `src/data/elementInfo.js` additively: every one of the 15 entries gains a `bhp: string` (Polish, norm-cited per its EDU-02 topic group) and a `media: []` (empty in Phase 12; Phase 16 populates it). Extend `tests/elementInfo.test.js` with new additive assertions in a NEW describe block — the existing 5-field and `Object.isFrozen` assertions are NOT touched (they are the backward-compatibility contract).

Purpose: Provide the BHP content contract every v1.2 consumer (overlay Phase 14, quiz Phase 12) depends on, without breaking the 903-test baseline.
Output: Extended `elementInfo.js` (15 entries × +2 fields) + extended `elementInfo.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-data-foundations/12-RESEARCH.md
@.planning/phases/12-data-foundations/12-PATTERNS.md

<interfaces>
<!-- The 15 entry keys (meshIds) — bhp + media MUST be added to each, in this order. -->
kolo-zamachowe, hamulec, wziernik-smarowania, oslona-tylna, kurtyna-lewa,
kurtyna-prawa, tabliczka-znamionowa, panel-oburezny, przycisk-start-lewy,
przycisk-start-prawy, lampka-gotowosci, estop, oslona-przednia,
wylacznik-glowny, dzwignia-sprzegla

<!-- EDU-02 topic group → applicable elements → primary norm(s) (drives bhp content + normRef).
     Clause numbers cited at SECTION level only until domain-expert review (Plan 04 gate). -->
Osłony + interlock: oslona-przednia, oslona-tylna, kurtyna-lewa, kurtyna-prawa — ISO 16092-1:2017 §5.4, EN ISO 14120, Dyrektywa 2006/42/EC §1.4
Sterowanie oburęczne: panel-oburezny, przycisk-start-lewy, przycisk-start-prawy — ISO 16092-1:2017 §5.5, EN ISO 13851
E-stop: estop — IEC 60204-1:2016 §9.2, EN ISO 13850
Energia koła zamachowego: kolo-zamachowe — ISO 16092-2:2019 §5.2, OSHA 1910.217(b)
Sprzęgło-hamulec: dzwignia-sprzegla, hamulec — ISO 16092-2:2019 §5.3, OSHA 1910.217(b)
LOTO: wylacznik-glowny — OSHA 1910.147, Dyrektywa 2006/42/EC §1.6.3
Inspekcja przedrozruchowa: wziernik-smarowania, tabliczka-znamionowa, lampka-gotowosci — ISO 16092-1:2017 §6, OSHA 1910.217(e)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/elementInfo.test.js with additive bhp/media assertions</name>
  <files>tests/elementInfo.test.js</files>
  <read_first>
    - tests/elementInfo.test.js (the file being extended — read all of it; the existing describe block at lines ~27-70 must stay byte-identical)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "tests/elementInfo.test.js — MODIFY" has the exact new describe block to append)
    - tests/faultRules.test.js (analog: Polish describe/it naming + Object.isFrozen assertion style)
  </read_first>
  <behavior>
    - For every entry in elementInfo: entry.bhp is typeof 'string'
    - For every entry: entry.bhp.length > 20 (rejects placeholders)
    - For every entry: Array.isArray(entry.media) === true
    - For every entry: entry.media.length === 0 (Phase 12 keeps media empty)
    - elementInfo is still Object.isFrozen after extension
  </behavior>
  <action>
    Append a NEW describe block titled `elementInfo — Phase 12 extensions (EDU-01/EDU-02)` AFTER the existing block. Do NOT modify any existing `it(...)` (the "5 pól" test, the "length > 10" test, the existing `Object.isFrozen` test stay byte-identical — they are the backward-compat contract). Iterate `Object.entries(elementInfo)` in each new assertion, passing the entry id as the assertion label. Write Polish `it(...)` titles. Polish literals are allowed here (test file, not src/data scanner scope). This is the RED step — these tests will fail until Task 2 adds the fields.
  </action>
  <verify>
    <automated>npm test -- tests/elementInfo.test.js 2>&1 | grep -E "Phase 12 extensions|bhp|media" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - New describe block `elementInfo — Phase 12 extensions (EDU-01/EDU-02)` exists in tests/elementInfo.test.js
    - The 4 new bhp/media assertions FAIL before Task 2 (RED confirmed)
    - The pre-existing assertions in the original describe block are unchanged (no diff in lines ~27-70)
  </acceptance_criteria>
  <done>New describe block present; new assertions red; existing assertions untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bhp + media to all 15 elementInfo entries</name>
  <files>src/data/elementInfo.js</files>
  <read_first>
    - src/data/elementInfo.js (the file being extended — read all 15 entries; preserve every existing field verbatim)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "src/data/elementInfo.js — MODIFY": Object.freeze top-level only, bhp+media AFTER safety, media:[])
    - .planning/phases/12-data-foundations/12-RESEARCH.md (section "EDU-02 Topic Group Mapping" + "Common Pitfalls" Pitfall 1 and Pitfall 5)
  </read_first>
  <action>
    To each of the 15 entries, add two fields immediately AFTER the existing `safety` field (preserve insertion order): `bhp` and `media`. `bhp` is a Polish norm-cited BHP instruction string (>20 chars, distinct from the shorter `safety` warning) whose content and cited norm match the entry's EDU-02 topic group from the interfaces block above; cite norms at SECTION level only (e.g. `ISO 16092-1:2017 §5.4`), never sub-clause, until the Plan 04 domain-expert gate. `media` is an empty array `[]`. Keep the top-level `Object.freeze(...)` wrapper; do NOT add inner Object.freeze on entries. Do NOT remove or alter the existing 5 fields (name, function, parameters, sopSteps, safety) — `safety` stays. Add NO import statements (zero-import data module, enforced by boundaries.test.js). Update the file header comment "Każdy wpis ma 5 pól" to mention the new bhp + media fields. This is the GREEN step.
  </action>
  <verify>
    <automated>npm test -- tests/elementInfo.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/elementInfo.test.js` passes ALL assertions (old 5-field/frozen + new Phase 12 bhp/media)
    - `grep -c "bhp:" src/data/elementInfo.js` returns 15
    - `grep -c "media:" src/data/elementInfo.js` returns 15
    - `grep -c "^import" src/data/elementInfo.js` returns 0 (zero imports preserved)
    - Each bhp string references a norm matching its EDU-02 topic group; citations are section-level only
  </acceptance_criteria>
  <done>All 15 entries have bhp (>20 chars, norm-cited) + media:[]; elementInfo.test.js fully green; zero imports; still frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static authored data, no runtime input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information (content integrity) | bhp norm citations in elementInfo.js | mitigate | Cite norms at section level only; full accuracy verified by domain-expert review gate in Plan 04 (non-code mitigation) |

Attack surface is static authored data + a pure data export — no network, no user input, no auth, no DB. The only real concern is content-integrity (incorrect BHP/norm text misleading a trainee), handled via the domain-expert review gate, not a code mitigation.
</threat_model>

<verification>
- `npm test -- tests/elementInfo.test.js` fully green (old + new assertions)
- `npm test` full suite stays ≥903 green (no regression)
- Zero imports in src/data/elementInfo.js
</verification>

<success_criteria>
- All 15 entries carry bhp (Polish, norm-cited, >20 chars) + media:[] (EDU-01, EDU-02, EDU-03 content layer)
- Backward compatibility: existing 5-field + Object.isFrozen tests pass without modification
- No new imports; file remains a pure zero-import frozen data module
</success_criteria>

<output>
Create `.planning/phases/12-data-foundations/12-01-SUMMARY.md` when done.
</output>
