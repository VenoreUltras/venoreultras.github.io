---
phase: 12-data-foundations
plan: 03
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - src/training/quizSelection.js
  - tests/quizSelection.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [EXAM-01]
must_haves:
  truths:
    - "selectQuizQuestions(scenarioId) returns ≥8 questions for each of the 4 valid scenarios"
    - "It returns only questions whose scenarioIds include the requested scenario"
    - "It returns a NEW array each call and is deterministic (stable order)"
    - "It throws a Polish error on unknown/empty/undefined scenarioId"
    - "boundaries.test.js enforces zero-forbidden-imports for quizData.js and quizSelection.js"
  artifacts:
    - path: "src/training/quizSelection.js"
      provides: "Pure selectQuizQuestions(scenarioId): QuizQuestion[]; only imports ../data/quizData.js"
      exports: ["selectQuizQuestions"]
    - path: "tests/quizSelection.test.js"
      provides: "Happy-path (4 scenarios, isolation, new-array, determinism) + throw cases"
      exports: []
    - path: "tests/boundaries.test.js"
      provides: "FORBIDDEN_PAIRS entries for quizData.js and quizSelection.js"
      contains: "quizSelection.js"
  key_links:
    - from: "src/training/quizSelection.js"
      to: "src/data/quizData.js"
      via: "import { quizBank }"
      pattern: "import.*quizBank.*data/quizData"
    - from: "tests/quizSelection.test.js"
      to: "src/training/quizSelection.js"
      via: "import { selectQuizQuestions }"
      pattern: "import.*selectQuizQuestions"
---

<objective>
Create `src/training/quizSelection.js` exporting the pure function `selectQuizQuestions(scenarioId)` — no side effects, no store mutation, throws a Polish error on unknown scenarioId, only import allowed is `../data/quizData.js` (mirrors `ScoringService.js` + `loadScenario`). Create `tests/quizSelection.test.js`. Extend `tests/boundaries.test.js` FORBIDDEN_PAIRS with entries for BOTH `src/data/quizData.js` and `src/training/quizSelection.js`.

Purpose: The pure selection layer (EXAM-01) called by the Phase 13 store `finishedAt` subscriber and by tests — keeps quiz selection isolated and testable (CRIT-V12-5).
Output: `src/training/quizSelection.js` + `tests/quizSelection.test.js` + extended `tests/boundaries.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-data-foundations/12-RESEARCH.md
@.planning/phases/12-data-foundations/12-PATTERNS.md

<!-- Depends on Plan 02: imports { quizBank } from ../data/quizData.js. Sequential after 12-02. -->

<interfaces>
<!-- Pure function signature (mirror ScoringService.calculate + scenarios/index.js loadScenario throw). -->
selectQuizQuestions(scenarioId: string): QuizQuestion[]
  valid = new Set(['uruchomienie','cykl-pracy','zatrzymanie','awaria'])
  throws Error (Polish) if scenarioId not in valid
  returns quizBank.filter(q => q.scenarioIds.includes(scenarioId))  // new array each call

<!-- boundaries.test.js FORBIDDEN_PAIRS entries to ADD (existsSync guard makes pre-creation-safe):
  { file: 'src/data/quizData.js', mustNotImport: [three, gsap, ../state/, ../training/, ./state/,
      ./training/, ../RaycastController, ../ui/, ./ui/, ../highlight/, ./highlight/, ../education/, ./education/] }
  { file: 'src/training/quizSelection.js', mustNotImport: [three, gsap, ../state/, ./state/] }
  Note: src/data/ is ALREADY in ALLOWED_PATHS (UI-06 diacritics scanner) — no scanner change. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/quizSelection.test.js (happy path + throws)</name>
  <files>tests/quizSelection.test.js</files>
  <read_first>
    - tests/scoringService.test.js (primary analog — pure-function test: happy path + throw)
    - tests/faultRules.test.js (lines ~63-87 — throw/defensive test style)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "tests/quizSelection.test.js — CREATE" has full happy-path + throw blocks)
  </read_first>
  <behavior>
    - for each of the 4 scenarios: returns an array of ≥8 questions
    - returns only questions whose scenarioIds include the requested scenario
    - two calls return different array instances (not ===) but equal content
    - results are deterministic: same id order across calls
    - throws on 'nieznany' / arbitrary unknown string; error message contains the passed id
    - throws on '' (empty) and on undefined
  </behavior>
  <action>
    Header: `// tests/quizSelection.test.js`, `// @vitest-environment node`, Polish one-liner citing EXAM-01; import `{ selectQuizQuestions }` from `../src/training/quizSelection.js`. Author two describe blocks — `selectQuizQuestions — happy path (EXAM-01)` (use `it.each` over the 4 scenarioIds for the ≥8 assertion, plus isolation, new-array, determinism) and `selectQuizQuestions — throw on unknown scenarioId (EXAM-01)`. This is the RED step; import resolves only after Task 2.
  </action>
  <verify>
    <automated>npm test -- tests/quizSelection.test.js 2>&1 | grep -E "Cannot find module|selectQuizQuestions|EXAM-01" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/quizSelection.test.js exists with both describe blocks
    - Covers all 4 scenarios (≥8 each), scenario isolation, new-array, determinism, and 4 throw cases
    - Red before Task 2 (module missing)
  </acceptance_criteria>
  <done>Test file written with happy-path + throw coverage; red because quizSelection.js absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/training/quizSelection.js (pure function)</name>
  <files>src/training/quizSelection.js</files>
  <read_first>
    - src/training/ScoringService.js (primary analog — pure named export, Polish JSDoc, throw on invalid input)
    - src/training/scenarios/index.js (lines ~29-33 — loadScenario allowlist + Polish throw message)
    - src/data/quizData.js (the only allowed import — confirm quizBank export name; created in Plan 02)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "src/training/quizSelection.js — CREATE")
  </read_first>
  <action>
    Header comment per PATTERNS.md. Single import: `import { quizBank } from '../data/quizData.js'` (the ONLY import). Export named function `selectQuizQuestions(scenarioId)` with Polish JSDoc (@param/@returns/@throws). Validate against `new Set(['uruchomienie','cykl-pracy','zatrzymanie','awaria'])`; on miss, `throw new Error` with Polish message containing the passed scenarioId and the list of valid IDs (mirror loadScenario). Return `quizBank.filter(q => q.scenarioIds.includes(scenarioId))` (new array each call, no mutation, no caching). Named export only (no default). Do NOT import three/gsap/state/. This is the GREEN step.
  </action>
  <verify>
    <automated>npm test -- tests/quizSelection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/quizSelection.test.js` fully green
    - `grep -c "^import" src/training/quizSelection.js` returns 1 (only quizBank)
    - File exports `selectQuizQuestions` (named, not default)
    - Throws Polish error whose message includes the unknown scenarioId
  </acceptance_criteria>
  <done>Pure selectQuizQuestions implemented, single allowed import, Polish throw; quizSelection.test.js green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/boundaries.test.js FORBIDDEN_PAIRS for quizData.js + quizSelection.js</name>
  <files>tests/boundaries.test.js</files>
  <read_first>
    - tests/boundaries.test.js (the file being extended — read FORBIDDEN_PAIRS array ~lines 24-135, the elementInfo.js entry ~line 116, the ScoringService.js entry, the existsSync guard ~line 155, ALLOWED_PATHS ~line 172)
    - .planning/phases/12-data-foundations/12-PATTERNS.md (section "tests/boundaries.test.js — MODIFY" has the exact two entries to append)
  </read_first>
  <action>
    Append two FORBIDDEN_PAIRS entries (after the existing elementInfo/data block): one for `src/data/quizData.js` mirroring the `src/data/elementInfo.js` mustNotImport list verbatim, and one for `src/training/quizSelection.js` with `mustNotImport: ['three','gsap','../state/','./state/']` (training-layer; `../data/` allowed for the quizBank import). Add a short Polish comment tagging each as Phase 12. Do NOT touch ALLOWED_PATHS — `src/data/` is already listed (line ~172); the UI-06 diacritics scanner already covers quizData.js. The existsSync guard means these entries are valid regardless of run order.
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/boundaries.test.js` fully green
    - `grep -c "src/data/quizData.js\|src/training/quizSelection.js" tests/boundaries.test.js` returns ≥2
    - No edit to ALLOWED_PATHS (src/data/ already present)
    - boundaries test confirms quizData.js and quizSelection.js have zero forbidden imports
  </acceptance_criteria>
  <done>Two FORBIDDEN_PAIRS entries added; boundaries.test.js green; quizData/quizSelection import boundaries enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selectQuizQuestions(scenarioId) arg | scenarioId is the only input; validated against a 4-item allowlist, throws otherwise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / Elevation | quizSelection import surface | mitigate | boundaries.test.js FORBIDDEN_PAIRS forbids three/gsap/state imports; keeps quiz selection isolated (CRIT-V12-5) |
| T-12-04 | Denial (invalid input) | selectQuizQuestions arg | mitigate | Explicit allowlist validation throws Polish Error on unknown/empty/undefined scenarioId (ASVS V5 input validation) |

No packages installed in this phase (no T-12-SC supply-chain entry needed). Attack surface is a pure function with a single validated string argument — no network, no auth, no DB.
</threat_model>

<verification>
- `npm test -- tests/quizSelection.test.js tests/boundaries.test.js` fully green
- Exactly one import in src/training/quizSelection.js (quizBank)
- `npm test` full suite stays ≥903 green
</verification>

<success_criteria>
- selectQuizQuestions is pure, deterministic, returns ≥8 per scenario, throws Polish error on unknown id (EXAM-01)
- Import boundaries enforced in boundaries.test.js for both new files (CRIT-V12-5 isolation)
</success_criteria>

<output>
Create `.planning/phases/12-data-foundations/12-03-SUMMARY.md` when done.
</output>
