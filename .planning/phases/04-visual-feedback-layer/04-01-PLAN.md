---
phase: 04-visual-feedback-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/pl.js
  - src/state/trainingStore.js
  - tests/i18n.pl.test.js
  - tests/trainingStore.test.js
autonomous: true
requirements: [UI-02, FEEDBACK-04, FEEDBACK-05]
must_haves:
  truths:
    - "src/i18n/pl.js eksportuje pl.stepStates z 4 kluczami: oczekuje/aktywny/poprawny/blad"
    - "src/i18n/pl.js eksportuje pl.stepStateIcons (4 emoji) i pl.machineStateIcons (7 emoji)"
    - "src/i18n/pl.js eksportuje pl.ui.scorePrefix, pl.ui.hcToggleOn, pl.ui.hcToggleOff"
    - "trainingStore initial state zawiera hcOutlineMode: false"
    - "store.setState({ hcOutlineMode: true }) propaguje do subskryberów"
  artifacts:
    - path: "src/i18n/pl.js"
      provides: "stepStates + stepStateIcons + machineStateIcons + ui.scorePrefix/hcToggle*"
      contains: "stepStates"
    - path: "src/state/trainingStore.js"
      provides: "hcOutlineMode flag w initial state"
      contains: "hcOutlineMode"
    - path: "tests/i18n.pl.test.js"
      provides: "asercje obecności wszystkich nowych kluczy"
    - path: "tests/trainingStore.test.js"
      provides: "asercja initial hcOutlineMode === false"
  key_links:
    - from: "src/i18n/pl.js"
      to: "StatusPanel/StepPanel/EmissiveController consumers"
      via: "named exports"
      pattern: "pl\\.(stepStates|stepStateIcons|machineStateIcons)"
    - from: "src/state/trainingStore.js"
      to: "EdgeOutlineController + StatusPanel"
      via: "subscribeWithSelector"
      pattern: "hcOutlineMode"
---

<objective>
Rozszerzyć fundament i18n + store dla Phase 4. Dodać polskie etykiety + emoji ikony stanów kroku i stanów maszyny do pl.js (single source per Phase 1 D-04 i UI-06) oraz boolean hcOutlineMode do trainingStore (D-Phase4-09 single runtime source dla high-contrast outline mode).

Purpose: Wszystkie kolejne plany Phase 4 (EmissiveController, HighlightManager, EdgeOutlineController, StatusPanel, StepPanel) konsumują te klucze i ten flag — bez nich plany Wave 2+ nie zbudują się.
Output: pl.js + trainingStore.js rozszerzone o sekcje udokumentowane w D-Phase4-05 i D-Phase4-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-feedback-layer/04-CONTEXT.md
@.planning/phases/04-visual-feedback-layer/04-PATTERNS.md
@src/i18n/pl.js
@src/state/trainingStore.js

<interfaces>
<!-- Istniejące klucze pl.js, które ROZSZERZAMY (nie nadpisujemy): -->
- pl.machineState: { 'oczekiwanie-na-inspekcje', 'gotowa-do-pracy', 'rozpedzanie', 'w-cyklu', 'zatrzymana', 'awaria', 'tryb-wolny' } — istnieje od Phase 1 D-09; Phase 4 dorzuca pl.machineStateIcons z TYMI SAMYMI 7 kluczami.
- pl.ui: { statusRunning, statusStopped, attestPrefix, attestAriaPrefix, procedureComplete, stepFormatPrefix } — Phase 4 dorzuca scorePrefix/hcToggleOn/hcToggleOff jako kolejne klucze.
- trainingStore initial state ma już: session, currentStepId, steps, machineState, meshStates, events, scoring, activeScenario, isAnimating. Phase 4 dorzuca hcOutlineMode: false jako 10. klucz w obiekcie initial.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rozszerzyć src/i18n/pl.js o stepStates, stepStateIcons, machineStateIcons, ui.scorePrefix, ui.hcToggleOn, ui.hcToggleOff</name>
  <files>src/i18n/pl.js, tests/i18n.pl.test.js</files>
  <read_first>
    - src/i18n/pl.js (cały plik — 130 linii) by zachować strukturę i porządek sekcji
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja "Polish strings via pl.js" (linie 822-843) — gotowa lista wartości
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-05 (rekomendowane glify), D-Phase4-06 (Wong palette skojarzone z stepStates blad/poprawny), D-Phase4-08 (toggle label format)
  </read_first>
  <action>
    Edytuj `src/i18n/pl.js` — dodaj DOKŁADNIE te sekcje wewnątrz eksportowanego obiektu `pl` (per D-04 single source, UI-06 boundary scanner):

    1. Pod istniejącą `pl.machineState` (linia 49) dodaj `pl.machineStateIcons` z 7 kluczami z DOKŁADNIE tymi emoji (D-Phase4-05):
       ```javascript
       machineStateIcons: {
         'oczekiwanie-na-inspekcje': '🔍',
         'gotowa-do-pracy': '🟢',
         'rozpedzanie': '🔄',
         'w-cyklu': '⚙️',
         'zatrzymana': '⏸️',
         'awaria': '⚠️',
         'tryb-wolny': '🆓',
       },
       ```
       UWAGA: 7 kluczy MUSI dokładnie pokrywać klucze `pl.machineState` (zero rozbieżności — test to wymusi).

    2. Dodaj nową sekcję `pl.stepStates` (4 etykiety statusu kroku per D-Phase4-04/05):
       ```javascript
       stepStates: {
         oczekuje: 'Oczekuje',
         aktywny: 'Aktywny',
         poprawny: 'Poprawny',
         blad: 'Błąd',
       },
       ```

    3. Dodaj `pl.stepStateIcons` (4 emoji per D-Phase4-05):
       ```javascript
       stepStateIcons: {
         oczekuje: '⏳',
         aktywny: '▶️',
         poprawny: '✅',
         blad: '❌',
       },
       ```

    4. Rozszerz istniejącą sekcję `pl.ui` o 3 nowe klucze (D-Phase4-08, D-Phase4-16):
       ```javascript
       scorePrefix: 'Wynik: ',
       hcToggleOn: 'Wysoki kontrast: WŁ',
       hcToggleOff: 'Wysoki kontrast: WYŁ',
       ```

    NIE modyfikuj istniejących wartości `pl.machineState`, `pl.parts`, `pl.errors`, `pl.disclaimer`, `pl.webgl`, `pl.physics` — Phase 1-3 lock-in.

    Stwórz `tests/i18n.pl.test.js` (NEW) z asercjami:
       - `pl.stepStates` ma DOKŁADNIE 4 klucze: oczekuje, aktywny, poprawny, blad
       - `pl.stepStateIcons` ma DOKŁADNIE 4 klucze pokrywające się z `pl.stepStates`
       - `pl.machineStateIcons` ma DOKŁADNIE 7 kluczy pokrywających się z `pl.machineState` (`Object.keys(pl.machineStateIcons).sort()` === `Object.keys(pl.machineState).sort()`)
       - `pl.ui.scorePrefix === 'Wynik: '`
       - `pl.ui.hcToggleOn === 'Wysoki kontrast: WŁ'` i `pl.ui.hcToggleOff === 'Wysoki kontrast: WYŁ'`
       - Wszystkie wartości w `stepStates` to non-empty stringi (smoke).

    Per D-04: wszystkie polskie literały dla nowych Phase 4 stringów ŻYJĄ TUTAJ — kolejne plany NIE wolno wstawiać literałów polskich w src/highlight/* ani src/ui/* (UI-06 boundary scanner egzekwuje przy okazji).
  </action>
  <verify>
    <automated>npx vitest run tests/i18n.pl.test.js</automated>
  </verify>
  <done>
    - `tests/i18n.pl.test.js` zielony (5+ asercji)
    - `grep -n "stepStates\|stepStateIcons\|machineStateIcons\|scorePrefix\|hcToggleOn\|hcToggleOff" src/i18n/pl.js` pokazuje 6+ wystąpień
    - Cały dotychczasowy test suite (`npm test`) zielony — żadna sekcja Phase 1-3 nie regresowała
  </done>
</task>

<task type="auto">
  <name>Task 2: Dodać hcOutlineMode: false do initial state trainingStore + test subscription</name>
  <files>src/state/trainingStore.js, tests/trainingStore.test.js</files>
  <read_first>
    - src/state/trainingStore.js linie 27-80 (initial state object + startScenario reset) — zlokalizować gdzie wstawić nowy klucz oraz czy startScenario powinno resetować flag (NIE — flag persist między scenariuszami per D-Phase4-09)
    - tests/trainingStore.test.js (cały) by zachować styl testów store
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-09 (klucz, default false, single runtime source nad localStorage)
  </read_first>
  <action>
    Edytuj `src/state/trainingStore.js`:
       1. W initial state object (linie ~28-42, wewnątrz `subscribeWithSelector((set, get) => ({ ... }))`) dodaj nowy klucz po `isAnimating: false,`:
          ```javascript
          // D-Phase4-09: single runtime source dla high-contrast outline mode.
          // Persist warstwa to localStorage 'pm300:hc-outline:v1' — bootstrap w Application
          // (Plan 04-06) odczytuje persist i wywołuje setState({ hcOutlineMode: ... }).
          // Default false (per D-Phase4-09 — większość użytkowników bez wymagania HC).
          hcOutlineMode: false,
          ```
       2. NIE modyfikuj `startScenario` ani `attemptStep` — flag jest user-preference, nie scenario state. Zmiana scenariusza NIE resetuje hcOutlineMode (intencja: ustawienie persistuje przez całą sesję).

    Edytuj `tests/trainingStore.test.js` — dodaj describe block `describe('hcOutlineMode (Phase 4 D-Phase4-09)', ...)` z testami:
       1. `initial state ma hcOutlineMode === false` (`expect(store.getState().hcOutlineMode).toBe(false)`)
       2. `setState({ hcOutlineMode: true }) zmienia wartość` (`store.setState({ hcOutlineMode: true }); expect(store.getState().hcOutlineMode).toBe(true)`)
       3. `subscribeWithSelector na hcOutlineMode emituje na zmianę` — register subscriber via `store.subscribe((s) => s.hcOutlineMode, listener)`, wywołaj `setState({ hcOutlineMode: true })`, expect listener wywołany 1×
       4. `startScenario(uruchomienie) NIE resetuje hcOutlineMode` — set true, then startScenario, expect nadal true (user preference persist)

    Per D-Phase4-09: trainingStore nie zna localStorage — Application robi bootstrap. Test używa store bezpośrednio bez DOM/localStorage.
  </action>
  <verify>
    <automated>npx vitest run tests/trainingStore.test.js</automated>
  </verify>
  <done>
    - `tests/trainingStore.test.js` zielony, wszystkie 4 nowe asercje pass
    - `grep -n "hcOutlineMode" src/state/trainingStore.js` pokazuje 1+ wystąpień (initial state)
    - Pełny `npm test` zielony — żadna istniejąca asercja Phase 1-3 nie regresowała
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brak (czysto in-process) | pl.js + trainingStore — zero zewnętrznych wejść w tym planie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | pl.js (stałe stringi) | accept | LOW — moduł ESM bez side-effectów; jeśli atakujący ma write na disk to całe app jest skompromitowane |
| T-04-02 | Information Disclosure | trainingStore.hcOutlineMode | accept | LOW — pure UI preference, brak PII, brak network |
</threat_model>

<verification>
- `npx vitest run tests/i18n.pl.test.js tests/trainingStore.test.js` — oba pliki zielone
- `npm test` — pełny suite zielony, brak regresji Phase 1-3
- `grep -n "pl\.stepStates\|pl\.stepStateIcons\|pl\.machineStateIcons" src/i18n/pl.js | grep -v '^[^:]*:[[:space:]]*//' | wc -l` — co najmniej 3 (3 sekcje)
</verification>

<success_criteria>
- pl.js eksportuje 3 nowe sekcje (stepStates, stepStateIcons, machineStateIcons) + 3 nowe klucze w pl.ui
- trainingStore initial state ma hcOutlineMode: false
- pl.machineStateIcons klucze === pl.machineState klucze (test wymusza)
- Wszystkie testy zielone (133+ tests sprzed Phase 4 + nowe asercje)
</success_criteria>

<output>
Po completion stwórz `.planning/phases/04-visual-feedback-layer/04-01-SUMMARY.md` zgodny z templates/summary.md.
</output>
