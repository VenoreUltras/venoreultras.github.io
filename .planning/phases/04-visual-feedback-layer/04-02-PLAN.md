---
phase: 04-visual-feedback-layer
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/highlight/EmissiveController.js
  - tests/EmissiveController.test.js
autonomous: true
requirements: [FEEDBACK-01, FEEDBACK-02, FEEDBACK-03]
must_haves:
  truths:
    - "EmissiveController posiada per-mesh stack warstw (hover, state) i potrafi je niezależnie ustawiać/czyścić"
    - "Wartwa state wygrywa nad hover (D-Phase4-13 priority order)"
    - "setLayer('state', mesh, {color, pulse:true}) startuje GSAP timeline yoyo na material.emissiveIntensity (numerami, nie Color object)"
    - "setLayer('state', mesh, {color, flash:true}) startuje 800ms ease-out flash (0.05+0.75s)"
    - "clearLayer kończy timeline (tl.kill()) i przywraca top warstwę poniżej"
    - "dispose() zabija wszystkie timelines + przywraca baseline emissive=0x000000, intensity=0 na wszystkich meshach"
  artifacts:
    - path: "src/highlight/EmissiveController.js"
      provides: "EmissiveController class z setLayer/clearLayer/applyTopLayer/dispose API"
      exports: ["EmissiveController"]
      min_lines: 80
    - path: "tests/EmissiveController.test.js"
      provides: "Testy stack priority + GSAP timeline lifecycle + dispose"
      min_lines: 100
  key_links:
    - from: "src/highlight/EmissiveController.js"
      to: "mesh.material.emissive (THREE.Color) + mesh.material.emissiveIntensity (number)"
      via: "setHex + gsap.to numerami"
      pattern: "emissive\\.setHex|emissiveIntensity"
    - from: "EmissiveController._timelines"
      to: "gsap.timeline"
      via: "create + kill lifecycle"
      pattern: "gsap\\.timeline|\\.kill\\(\\)"
---

<objective>
Stworzyć `EmissiveController` — pojedynczy controller (singleton w Application) zarządzający stackiem warstw emissive (hover < state) per interactable mesh. Read-modify-restore z Phase 3 (D-Phase3-05) zostaje TUTAJ przebudowane: zamiast `_hoverPrev = mesh.material.emissive.getHex()` mamy `setLayer('hover', mesh, {color})` / `clearLayer('hover', mesh)` z deterministycznym priority resolver.

Per D-Phase4-13/14: `EmissiveController` jest fundamentem Wave 3 (HighlightManager pisze do warstwy 'state', RaycastController port do warstwy 'hover'). Bez tego planu żaden konsument nie może działać.

Per FEEDBACK-02 + CRIT-5: GSAP target = NUMBER na `material.emissiveIntensity`, NIE Color object — zero GC churn na każdy tick GSAP.
Per FEEDBACK-03: zero `OutlinePass` w pipeline (outline rezerwowany dla EdgeOutlineController w Plan 04-04 jako EdgesGeometry+LineSegments).

Output: `src/highlight/EmissiveController.js` + testy unit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-feedback-layer/04-CONTEXT.md
@.planning/phases/04-visual-feedback-layer/04-PATTERNS.md
@src/RaycastController.js
@src/MaterialRegistry.js
@tests/RaycastController.test.js

<interfaces>
<!-- API EmissiveController jakie konsumują kolejne plany Wave 3+: -->
```javascript
class EmissiveController {
  constructor({ interactables: Map<string, THREE.Mesh> });
  setLayer(layerName: 'hover'|'state', mesh: THREE.Mesh, params: { color: number, pulse?: boolean, flash?: boolean }): void;
  clearLayer(layerName: 'hover'|'state', mesh: THREE.Mesh): void;
  dispose(): void;
}
```

<!-- Priority order (D-Phase4-13): -->
state > hover > baseline(0x000000)

<!-- Pulse params (D-Phase4-11): -->
{ color: 0xD55E00, pulse: true } → emissive=#D55E00, gsap timeline yoyo na emissiveIntensity (0→0.8→0...) ease='sine.inOut', repeat:-1, overwrite:'auto'

<!-- Flash params (D-Phase4-12): -->
{ color: 0x009E73, flash: true } → emissive=#009E73, sekwencja 0.05s ease='power1.in' do 0.6 → 0.75s ease='power2.out' do 0; brak yoyo, brak repeat
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementacja src/highlight/EmissiveController.js</name>
  <files>src/highlight/EmissiveController.js</files>
  <read_first>
    - src/RaycastController.js linie 1-50 (header + ctor + _hoverPrevEmissive pattern do PRZEBUDOWY) i linie 110-125 (_commitHover/_commitLeave read-modify-restore)
    - src/MaterialRegistry.js linie 1-72 (per-mesh Map + disposeAll registry pattern)
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja "src/highlight/EmissiveController.js" linie 29-132 — gotowy szkielet kodu z _applyTopLayer + GSAP timeline lifecycle
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-11 (pulse semantics), D-Phase4-12 (flash 800ms), D-Phase4-13 (priority order), D-Phase4-14 (singleton w Application)
  </read_first>
  <behavior>
    - Test 1: setLayer('hover', mesh, {color: 0x222222}) → mesh.material.emissive.getHex() === 0x222222, emissiveIntensity === 1
    - Test 2: setLayer('state', mesh, {color: 0xD55E00}) NA TYM SAMYM meshu → emissive === 0xD55E00 (state wygrywa nad hover)
    - Test 3: clearLayer('state', mesh) po teście 2 → emissive === 0x222222 (hover znowu top)
    - Test 4: clearLayer('hover', mesh) → emissive === 0x000000, intensity === 0 (baseline)
    - Test 5: setLayer('state', mesh, {color: 0xD55E00, pulse: true}) → gsap.timeline utworzony i przechowywany w _timelines.get(mesh) (truthy)
    - Test 6: clearLayer('state', mesh) gdy aktywny pulse → tl.kill() wywołany ZANIM zmiana warstwy (timeline usunięty z _timelines)
    - Test 7: setLayer dla nieznanego mesha (nie w interactables) → graceful no-op (zero throw)
    - Test 8: dispose() → wszystkie timelines killed, wszystkie meshe mają emissive=0x000000+intensity=0, _layers wyczyszczone
  </behavior>
  <action>
    Stwórz `src/highlight/EmissiveController.js` zgodnie ze szkieletem z 04-PATTERNS.md sekcja EmissiveController (linie 35-132). Kluczowe szczegóły:

    Header (per CONVENTIONS — Polish JSDoc, English identifiers):
    ```javascript
    // src/highlight/EmissiveController.js
    // Phase 4 — FEEDBACK-01..03: stack warstw emissive (hover < state) per interactable.
    // D-Phase4-13/14: pojedyncza instancja w Application. RaycastController pisze 'hover',
    // HighlightManager pisze 'state'. Read-modify-restore z Phase 3 D-Phase3-05 jest TUTAJ.
    // Boundary (boundaries.test.js): może importować THREE+gsap; NIE store/training/DOM.
    ```

    Importy: `import * as THREE from 'three'; import { gsap } from 'gsap';` — NIC z `../state/`, `../training/`, ani DOM.

    Constructor:
    ```javascript
    constructor({ interactables }) {
      this._meshes = Array.from(interactables.values()); // snapshot raz, zero alokacji per-tick
      this._layers = new Map(); // mesh → { hover: ?, state: ? }
      this._timelines = new Map(); // mesh → gsap.core.Timeline (aktywne)
      for (const mesh of this._meshes) {
        this._layers.set(mesh, { hover: null, state: null });
      }
    }
    ```

    Public API:
    - `setLayer(layerName, mesh, params)` — pobiera slot z `_layers.get(mesh)`. Jeśli slot null (mesh nieznany) → return (graceful no-op). W przeciwnym razie: `slot[layerName] = params;` + wywołaj `_applyTopLayer(mesh)`.
    - `clearLayer(layerName, mesh)` — analogicznie, ustawia `slot[layerName] = null` + `_applyTopLayer(mesh)`.

    Private `_applyTopLayer(mesh)`:
    ```javascript
    _applyTopLayer(mesh) {
      const slot = this._layers.get(mesh);
      // ZAWSZE kill aktualny timeline przed zmianą warstwy (D-Phase4-13 cleanup discretion)
      const tl = this._timelines.get(mesh);
      if (tl) { tl.kill(); this._timelines.delete(mesh); }

      // Priority: state > hover > baseline
      if (slot.state) {
        mesh.material.emissive.setHex(slot.state.color);
        if (slot.state.pulse) {
          // D-Phase4-11: yoyo repeat -1, ease sine.inOut, overwrite auto
          const newTl = gsap.timeline({ overwrite: 'auto' })
            .to(mesh.material, {
              emissiveIntensity: 0.8,
              duration: 0.4, yoyo: true, repeat: -1, ease: 'sine.inOut',
            });
          this._timelines.set(mesh, newTl);
        } else if (slot.state.flash) {
          // D-Phase4-12: 0.05s power1.in do 0.6 → 0.75s power2.out do 0 (~800ms total)
          const newTl = gsap.timeline()
            .to(mesh.material, { emissiveIntensity: 0.6, duration: 0.05, ease: 'power1.in' })
            .to(mesh.material, { emissiveIntensity: 0,   duration: 0.75, ease: 'power2.out' });
          this._timelines.set(mesh, newTl);
        } else {
          // state bez pulse/flash — solid bez animacji (rzadko, ale defensywnie)
          mesh.material.emissiveIntensity = 1;
        }
      } else if (slot.hover) {
        mesh.material.emissive.setHex(slot.hover.color);
        mesh.material.emissiveIntensity = 1; // hover bez animacji
      } else {
        mesh.material.emissive.setHex(0x000000); // baseline
        mesh.material.emissiveIntensity = 0;
      }
    }
    ```

    `dispose()`:
    ```javascript
    dispose() {
      for (const tl of this._timelines.values()) tl.kill();
      this._timelines.clear();
      for (const mesh of this._meshes) {
        mesh.material.emissive.setHex(0x000000);
        mesh.material.emissiveIntensity = 0;
      }
      this._layers.clear();
    }
    ```

    KRYTYCZNE INVARIANTY:
    - GSAP target to `mesh.material` (obiekt) z polem `emissiveIntensity` (number) — NIE `mesh.material.emissive` (Color obj). Per FEEDBACK-02 + CRIT-5 — komentarz inline w kodzie.
    - Zero importów z `../state/`, `../training/`, ani DOM. Boundary egzekwowane w Plan 04-06 (boundaries.test.js update).
    - Phase 2 cloned-material invariant (TWIN-11): pisanie do `mesh.material.emissive` per-mesh jest bezpieczne tylko bo materials są clone-ami; NIE modyfikuj registry.
  </action>
  <verify>
    <automated>node -e "import('./src/highlight/EmissiveController.js').then(m => console.log(typeof m.EmissiveController))"</automated>
  </verify>
  <done>
    - Plik `src/highlight/EmissiveController.js` istnieje, eksportuje `class EmissiveController`
    - `grep -c "import" src/highlight/EmissiveController.js` zwraca dokładnie 2 (THREE + gsap), zero `state/training/DOM`
    - `grep -n "gsap.to\|gsap.timeline" src/highlight/EmissiveController.js` pokazuje że targety to `mesh.material` (obiekt), pole `emissiveIntensity` — NIGDY `mesh.material.emissive` jako target
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/EmissiveController.test.js — stack priority + GSAP timeline lifecycle + dispose</name>
  <files>tests/EmissiveController.test.js</files>
  <read_first>
    - tests/RaycastController.test.js linie 1-50 (setup + makeMesh helper) i linie 81-101 (emissive readout assertion + dispose smoke) — analog setup
    - tests/MaterialRegistry.smoke.test.js (cały) — analog per-mesh map size + disposeAll
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja EmissiveController test (linie 670-702) — szkielet stack priority test
    - src/highlight/EmissiveController.js (właśnie utworzony w Task 1) — by testy znały dokładne API
  </read_first>
  <action>
    Stwórz `tests/EmissiveController.test.js` z `@vitest-environment node` (analog RaycastController.test.js — bez DOM, używa THREE bezpośrednio).

    Helper:
    ```javascript
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import * as THREE from 'three';
    import { EmissiveController } from '../src/highlight/EmissiveController.js';

    function makeMesh(id) {
      const mat = new THREE.MeshStandardMaterial({ emissive: 0x000000, emissiveIntensity: 0 });
      const mesh = new THREE.Mesh(new THREE.BoxGeometry(1,1,1), mat);
      mesh.userData = { id, kind: 'manipulation' };
      return mesh;
    }
    ```

    Implementuj 4 describe bloki z 8+ testami:

    1. **`describe('stack priority — D-Phase4-13')`**:
       - `it('hover layer ustawia emissive i intensity=1')` — setLayer('hover', m, {color: 0x222222}); expect getHex === 0x222222, emissiveIntensity === 1
       - `it('state layer wygrywa nad hover')` — setLayer hover, potem state → state widoczne
       - `it('clearLayer state przywraca hover')` — po setLayer hover+state, clearLayer state → hover znowu top
       - `it('clearLayer wszystkich → baseline 0x000000 intensity=0')`
       - `it('setLayer dla nieznanego mesha graceful no-op')` — meshNotInMap, brak throw, brak side-effect

    2. **`describe('GSAP timeline lifecycle — D-Phase4-11/12')`**:
       - `it('pulse params startuje timeline w _timelines')` — setLayer('state', m, {color, pulse:true}); expect ctrl._timelines.get(m) truthy (uzyskaj przez `Object.getOwnPropertyDescriptor` jeśli prywatne — albo dodaj test-only getter; rekomendacja: `it` sprawdza `mesh.material.emissive.getHex()` i symulowany advanceTimers wykrywa zmianę intensity)
       - `it('flash params startuje sekwencję dwóch tweenów (~800ms)')` — z `vi.useFakeTimers()` advanceTimersByTime(50) → intensity bliskie 0.6; advanceTimersByTime(750) → intensity bliskie 0
       - `it('clearLayer state podczas pulse killuje timeline (kolejny setLayer hover czyni emissive===hover.color, intensity===1)')` — start pulse, clear state → emissive ustawia się na baseline (lub hover) i timeline jest dead
       - `it('setLayer drugi raz tej samej warstwy nadpisuje (idempotency)')` — setLayer state pulse, setLayer state flash → poprzedni timeline killed, nowy aktywny

    3. **`describe('dispose — STATE-03')`**:
       - `it('dispose() killuje wszystkie timelines i przywraca baseline na meshach')` — start pulse na 2 meshach, dispose, expect emissive===0 + intensity===0 dla obu, timelines puste

    4. **`describe('CRIT-5 GSAP target = number nie Color (FEEDBACK-02)')`** — smoke regex test:
       - `it('plik EmissiveController.js NIE wywołuje gsap.to na obiekcie Color')` — odczyt sourcefile, regex `/gsap\.(to|timeline\(\)).*?\.to\(\s*[a-zA-Z_$][\w.]*\.emissive\s*,/` musi NIE match'ować (brak target=emissive). Pattern `gsap.to(mesh.material, { emissiveIntensity` MUSI match (pozytyw).

    Użyj `vi.useFakeTimers()` + `vi.advanceTimersByTime` dla testów flash duration (analog tests/uruchomienie.integration.test.js linie 21-22). GSAP używa rzeczywistego clocku — gdy fake timers nie wystarczą, zaakceptuj test heurystyczny (`expect(mesh.material.emissiveIntensity).not.toBe(0)` po kicku timeline, bez precyzyjnego pomiaru).
  </action>
  <verify>
    <automated>npx vitest run tests/EmissiveController.test.js</automated>
  </verify>
  <done>
    - Wszystkie testy w `tests/EmissiveController.test.js` zielone (8+ asercji)
    - Pełny `npm test` zielony
    - Test "GSAP target = number" wykrywa anti-pattern (regex check w sourcefile)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brak (in-process Three.js writes) | EmissiveController pisze tylko do mesh.material — zero zewnętrznych granic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | GSAP timeline leak | mitigate | dispose() killuje wszystkie timelines (test wymusza); applyTopLayer killuje stary timeline przed nowym |
| T-04-04 | Tampering | Phase 2 invariant cloned materials | mitigate | EmissiveController NIE klonuje materiałów ani nie modyfikuje registry — tylko pisze do `mesh.material.emissive` (Phase 2 zapewniła że są clone-ami, TWIN-11) |
</threat_model>

<verification>
- `npx vitest run tests/EmissiveController.test.js` zielony
- `grep -n "import" src/highlight/EmissiveController.js | grep -v '^#' | wc -l` === 2 (tylko THREE + gsap)
- `grep -E "gsap\.(to|timeline)" src/highlight/EmissiveController.js | grep -v '^#'` — wszystkie matche mają target `mesh.material` (nie `mesh.material.emissive`)
- Pełny `npm test` zielony
</verification>

<success_criteria>
- EmissiveController.js eksportuje class EmissiveController z setLayer/clearLayer/dispose
- Stack priority deterministyczny: state > hover > baseline
- GSAP timeline lifecycle: create na pulse/flash, kill na warstwa change i dispose
- Zero polskich literałów w pliku (UI-06 boundary)
- Zero importów z store/training/DOM
</success_criteria>

<output>
Po completion stwórz `.planning/phases/04-visual-feedback-layer/04-02-SUMMARY.md` zgodny z templates/summary.md.
</output>
