---
phase: 04-visual-feedback-layer
plan: 05
type: execute
wave: 4
depends_on: [04-02, 04-04]
files_modified:
  - index.html
  - style.css
  - src/RaycastController.js
  - tests/RaycastController.test.js
autonomous: true
requirements: [FEEDBACK-04, FEEDBACK-05, UI-01, UI-02]
must_haves:
  truths:
    - "index.html NIE zawiera #phase3-step-readout, #phase3-attest-container, #phase3-panel"
    - "index.html zawiera #status-panel (top bar) i #step-panel (lewa kolumna)"
    - "style.css NIE zawiera reguł .phase3-* (block usunięty)"
    - "style.css zawiera .status-panel, .step-panel, .step-item, .step-item--{oczekuje,aktywny,poprawny,blad}, .phase4-attest-check, .status-panel__hc-toggle"
    - "step-item--blad używa Wong #D55E00; step-item--poprawny używa Wong #009E73"
    - "RaycastController._commitHover używa EmissiveController.setLayer('hover', mesh, {color: HOVER_HINT_HEX})"
    - "RaycastController._commitLeave używa EmissiveController.clearLayer('hover', mesh)"
    - "RaycastController NIE odczytuje już mesh.material.emissive bezpośrednio (port D-Phase4-13 zakończony)"
  artifacts:
    - path: "index.html"
      provides: "DOM struktura Phase 4 (status-panel + step-panel)"
      contains: "id=\"status-panel\""
    - path: "style.css"
      provides: "Style glassmorphism + Wong palette dla nowych paneli"
      contains: ".step-item--blad"
    - path: "src/RaycastController.js"
      provides: "Port hover read-modify-restore na EmissiveController API"
    - path: "tests/RaycastController.test.js"
      provides: "Testy update — hover wywołuje EmissiveController.setLayer/clearLayer zamiast bezpośredniej manipulacji emissive"
  key_links:
    - from: "src/RaycastController.js"
      to: "EmissiveController.setLayer/clearLayer"
      via: "DI emissive w konstruktorze"
      pattern: "this\\._emissive\\.(setLayer|clearLayer)"
    - from: "style.css"
      to: "DOM struktura StatusPanel/StepPanel"
      via: "klasy CSS"
      pattern: "\\.step-item--(oczekuje|aktywny|poprawny|blad)"
---

<objective>
Brownfield-restruktura DOM + CSS + Phase 3 → Phase 4 port. Trzy operacje:

1. **`index.html` restructure (D-Phase4-02):** Usuń `#phase3-panel` blok (linie 59-63 z istniejącej struktury), dorzuć `#status-panel` (top bar) i `#step-panel` (lewa kolumna jako `<aside>`). Phase 3 placeholders są w pełni zastąpione.

2. **`style.css` restructure:** Usuń wszystkie reguły `.phase3-*` i `#phase3-*` (cleanup). Dorzuć `.status-panel`, `.step-panel`, `.step-item`, 4 wariacje `.step-item--{oczekuje,aktywny,poprawny,blad}` z Wong palette dla blad/poprawny, `.phase4-attest-check` (przemianowany z `.phase3-attest-check`), `.status-panel__hc-toggle`. Reuse istniejących CSS variables z `:root` (glassmorphism + colors) — zero nowych top-level zmiennych poza Wong palette jeśli jeszcze nie ma.

3. **`src/RaycastController.js` port (D-Phase4-13):** Konstruktor przyjmuje `emissive: EmissiveController` jako DI; `_commitHover` woła `this._emissive.setLayer('hover', mesh, {color: HOVER_HINT_HEX})`; `_commitLeave` woła `this._emissive.clearLayer('hover', this._committedTarget)`. Pole `_hoverPrevEmissive` USUNIĘTE — warstwa 'hover' w EmissiveController trzyma stan. Test update: spy na `emissive.setLayer/clearLayer` zamiast assertion na `mesh.material.emissive`.

Output: 4 zmodyfikowane pliki. Phase 3 brownfield-port zamknięty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-feedback-layer/04-CONTEXT.md
@.planning/phases/04-visual-feedback-layer/04-PATTERNS.md
@index.html
@style.css
@src/RaycastController.js
@tests/RaycastController.test.js
@src/highlight/EmissiveController.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html — usuń #phase3-panel, dorzuć #status-panel + #step-panel</name>
  <files>index.html</files>
  <read_first>
    - index.html cały (68 linii) — by zlokalizować dokładną pozycję `#phase3-panel` i `#ui-layer`
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja "index.html + style.css" (linie 593-607) — gotowy patch
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-01 (layout — top bar + left column + right telemetry istniejąca)
  </read_first>
  <action>
    Edit `index.html`:

    1. USUŃ blok linii 59-63 (`<div id="phase3-panel" class="glass-panel">...</div>`) w całości.

    2. DORZUĆ `<div id="status-panel">` jako pierwsze dziecko `<div id="ui-layer">` (przed `<header class="header">`):
       ```html
       <div id="status-panel" class="status-panel glass-panel" role="region" aria-label="Status maszyny"></div>
       ```
       Ten kontener jest top-bar — pozioma belka z 4 elementami (icon+state+score+HC toggle), wypełniana przez `StatusPanel` z Plan 04-04 (innerHTML w `_build()`).

    3. DORZUĆ `<aside id="step-panel">` jako rodzeństwo (lewa kolumna; per D-Phase4-01 ma być widoczne równolegle ze sceną i prawą kolumną telemetrii). Wstaw po `<div class="info-panel glass-panel">` (czyli na końcu `#ui-layer`, gdzie był `#phase3-panel`):
       ```html
       <aside id="step-panel" class="step-panel glass-panel" role="region" aria-label="Procedura szkoleniowa"></aside>
       ```

    4. NIE modyfikuj: `<div id="three-canvas">`, `<header>`, `.control-panel`, `.info-panel`, `<script>` — wszystko inne istnieje od Phase 1-3 i ma działać dalej (slider RPM, btn-toggle — D-Phase4-17 zostawia tor RPM).

    5. UI-05 disclaimer banner: Phase 1 wstawia go runtime przez `new DisclaimerBanner()` z JS (sprawdź `src/DisclaimerBanner.js` — banner sticky tworzony w body z Application). Phase 4 NIE modyfikuje tego — banner sticky pozostaje na top page niezależnie od top-bar `#status-panel`. Layout CSS rozwiązuje co jest "wyżej" (banner sticky vs status panel).

    Verification:
    - `grep -c "phase3-" index.html` zwraca 0
    - `grep -c "id=\"status-panel\"" index.html` zwraca 1
    - `grep -c "id=\"step-panel\"" index.html` zwraca 1
    - `grep -c "id=\"three-canvas\"" index.html` zwraca 1 (NIE usunięte)
    - `grep -c "id=\"ui-layer\"" index.html` zwraca 1 (NIE usunięte)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const a=h.match(/phase3-/g)||[];const b=h.match(/id=\"status-panel\"/g)||[];const c=h.match(/id=\"step-panel\"/g)||[];if(a.length!==0||b.length!==1||c.length!==1)process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>
    - `index.html` zawiera `#status-panel` + `#step-panel`, NIE zawiera żadnego `phase3-` (id ani klasy)
    - `npm run dev` (manual) — strona ładuje się bez 404/console errors w Chrome
  </done>
</task>

<task type="auto">
  <name>Task 2: style.css — usuń .phase3-*, dorzuć .status-panel, .step-panel, .step-item, .step-item--*, .phase4-attest-check, .status-panel__hc-toggle</name>
  <files>style.css</files>
  <read_first>
    - style.css cały (najpierw `wc -l style.css`, potem Read pierwszych 200 linii dla `:root` + glass-panel patterns; jeśli plik większy — zaplanuj kolejne offsety)
    - Grep `\.phase3-` w style.css by znaleźć linie do usunięcia
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-03 (glassmorphism), D-Phase4-06 (Wong palette #D55E00 / #009E73)
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja style.css patch (linie 605-607)
  </read_first>
  <action>
    Edit `style.css`:

    1. USUŃ wszystkie reguły dla selektorów `.phase3-*` i `#phase3-*` w CAŁOŚCI (np. `.phase3-readout`, `.phase3-attest-container`, `.phase3-attest-check`, `#phase3-panel`, `#phase3-step-readout`, `#phase3-attest-container`). Użyj grep żeby zlokalizować bloki, potem usuń całe reguły (od selektora do zamykającego `}`).

    2. DORZUĆ na końcu pliku nowy blok `/* Phase 4: visual-feedback-layer (FEEDBACK-04/05, UI-01/02) */`:

    ```css
    /* === Phase 4 — top bar StatusPanel === */
    .status-panel {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 50;
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 0.75rem 1.5rem;
    }
    .status-panel__bar {
      display: flex;
      align-items: center;
      gap: 1rem;
      width: 100%;
    }
    .status-panel__icon {
      font-size: 1.5rem;
    }
    .status-panel__state {
      font-weight: 600;
      color: var(--text-main);
    }
    .status-panel__score {
      font-family: var(--font-mono);
      color: var(--text-muted);
    }
    .status-panel__hc-toggle {
      margin-left: auto;
      padding: 0.4rem 0.8rem;
      background: transparent;
      border: 1px solid var(--glass-border);
      color: var(--text-main);
      cursor: pointer;
      border-radius: 6px;
      font-family: var(--font-sans);
    }
    .status-panel__hc-toggle[aria-pressed="true"] {
      background: var(--accent-primary);
      color: var(--text-main);
    }

    /* === Phase 4 — left column StepPanel === */
    .step-panel {
      position: absolute;
      top: 5rem; /* poniżej #status-panel */
      left: 1rem;
      bottom: 1rem;
      width: 22rem;
      overflow-y: auto;
      padding: 1rem;
    }
    .step-panel__list {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }
    .step-item {
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      border-left: 3px solid var(--glass-border);
      color: var(--text-muted);
      transition: border-color 0.2s, color 0.2s;
    }
    .step-item--oczekuje {
      border-left-color: var(--glass-border);
    }
    .step-item--aktywny {
      border-left-color: var(--accent-primary);
      color: var(--text-main);
      font-weight: 600;
    }
    .step-item--poprawny {
      border-left-color: #009E73; /* Wong success — D-Phase4-06 */
      color: var(--text-main);
    }
    .step-item--blad {
      border-left-color: #D55E00; /* Wong error — D-Phase4-06 */
      color: var(--text-main);
    }

    /* === Phase 4 — visual-attest inline button === */
    .phase4-attest-check {
      margin-top: 0.5rem;
      padding: 0.4rem 0.8rem;
      background: var(--accent-primary);
      color: var(--text-main);
      border: none;
      cursor: pointer;
      border-radius: 6px;
      font-family: var(--font-sans);
    }
    .phase4-attest-check:disabled {
      background: var(--text-muted);
      cursor: not-allowed;
      opacity: 0.5;
    }
    ```

    3. NIE modyfikuj `:root`, `.control-panel`, `.info-panel`, `.glass-panel`, `#three-canvas` ani innych istniejących reguł — Phase 1-3 lock-in.

    Verification:
    - `grep -c "\\.phase3-\\|#phase3-" style.css` zwraca 0
    - `grep -c "\\.step-item--blad" style.css` zwraca 1+
    - `grep -c "\\.step-item--poprawny" style.css` zwraca 1+
    - `grep -c "#D55E00" style.css` zwraca 1+ (Wong error)
    - `grep -c "#009E73" style.css` zwraca 1+ (Wong success)
    - `grep -c "\\.status-panel__hc-toggle" style.css` zwraca 1+
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('style.css','utf8');const a=(c.match(/\\.phase3-|#phase3-/g)||[]).length;const b=(c.match(/\\.step-item--blad|\\.step-item--poprawny/g)||[]).length;const d=(c.match(/#D55E00|#009E73/g)||[]).length;if(a!==0||b<2||d<2)process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>
    - `style.css` nie zawiera już żadnych reguł `.phase3-*` / `#phase3-*`
    - Dorzucone reguły dla `.status-panel*`, `.step-panel*`, `.step-item--*`, `.phase4-attest-check`
    - Wong palette (#D55E00, #009E73) użyta dla blad/poprawny
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: src/RaycastController.js — port _commitHover/_commitLeave na EmissiveController + tests update</name>
  <files>src/RaycastController.js, tests/RaycastController.test.js</files>
  <read_first>
    - src/RaycastController.js cały plik (przeczytaj OFFSET=1 LIMIT=200) — by zlokalizować _commitHover, _commitLeave, _hoverPrevEmissive, dispose
    - tests/RaycastController.test.js cały (jeśli >200 linii, przeczytaj fragmentami) — by zaktualizować assertion `mesh.material.emissive.getHex()` na spy `emissive.setLayer/clearLayer`
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja RaycastController MODIFY (linie 458-499) — gotowy diff przed/po
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-13 (channel layer port)
    - src/highlight/EmissiveController.js (Plan 04-02) — by znać API setLayer('hover', ...) / clearLayer('hover', ...)
  </read_first>
  <behavior>
    - Test update 1: po pointermove + hysteresis commit, `emissive.setLayer('hover', mesh, {color: HOVER_HINT_HEX})` wywołany 1×
    - Test update 2: po pointermove odjeżdżający z mesha, `emissive.clearLayer('hover', mesh)` wywołany 1×
    - Test update 3: dispose RaycastController + brak hover-target → brak throw, brak `emissive.clearLayer` na null
    - Test update 4: dispose RaycastController z aktywnym committedTarget → `emissive.clearLayer('hover', target)` wywołany defensywnie
    - Smoke: pole `_hoverPrevEmissive` USUNIĘTE — `grep` w sourcefile zwraca 0
  </behavior>
  <action>
    Edit `src/RaycastController.js`:

    1. **Konstruktor** (linie 19-50): zmień sygnaturę DI — dorzuć `emissive`:
       ```javascript
       constructor({ renderer, camera, interactables, store, emissive }) {
         this._renderer = renderer;
         this._camera = camera;
         this._meshes = Array.from(interactables.values());
         this._store = store;
         this._emissive = emissive; // EmissiveController instance (Plan 04-02)
         // ... existing fields ...
         // USUŃ pole `this._hoverPrevEmissive = 0;` — warstwa 'hover' w EmissiveController trzyma stan
       }
       ```
       Dorzuć JSDoc do `@param`: `@param {import('./highlight/EmissiveController.js').EmissiveController} deps.emissive`.

    2. **`_commitHover(mesh)`** (linie 110-115 sprzed):
       ```javascript
       // PRZED:
       _commitHover(mesh) {
         this._committedTarget = mesh;
         this._hoverPrevEmissive = mesh.material.emissive.getHex();
         mesh.material.emissive.setHex(HOVER_HINT_HEX);
         this._renderer.domElement.style.cursor = 'pointer';
       }

       // PO (D-Phase4-13):
       _commitHover(mesh) {
         this._committedTarget = mesh;
         this._emissive.setLayer('hover', mesh, { color: HOVER_HINT_HEX });
         this._renderer.domElement.style.cursor = 'pointer';
       }
       ```

    3. **`_commitLeave()`**:
       ```javascript
       // PRZED (przykładowe):
       _commitLeave() {
         if (this._committedTarget) {
           this._committedTarget.material.emissive.setHex(this._hoverPrevEmissive);
           this._committedTarget = null;
         }
         this._renderer.domElement.style.cursor = 'default';
         this._pendingCount = 0;
         this._pendingTarget = null;
       }

       // PO:
       _commitLeave() {
         if (this._committedTarget) {
           this._emissive.clearLayer('hover', this._committedTarget);
           this._committedTarget = null;
         }
         this._renderer.domElement.style.cursor = 'default';
         this._pendingCount = 0;
         this._pendingTarget = null;
       }
       ```

    4. **`dispose()`** — jeśli istnieje branch `if (this._committedTarget) restore emissive...`, zamień analogicznie na `this._emissive.clearLayer('hover', this._committedTarget)`. Jeśli `dispose()` po prostu wywołuje `this._commitLeave()` to OK — restore odbędzie się przez warstwę.

    5. UPEWNIJ SIĘ że `HOVER_HINT_HEX` constant pozostaje (linia 15).

    Edit `tests/RaycastController.test.js`:

    Każdy test który asercjuje `mesh.material.emissive.getHex() === HOVER_HINT_HEX` musi się zmienić na spy `emissive.setLayer`. Pattern:
    ```javascript
    // PRZED:
    expect(mesh.material.emissive.getHex()).toBe(HOVER_HINT_HEX);

    // PO:
    expect(setLayerSpy).toHaveBeenCalledWith('hover', mesh, { color: HOVER_HINT_HEX });
    ```

    Setup w beforeEach (analog tests/HighlightManager.test.js z Plan 04-03):
    ```javascript
    import { EmissiveController } from '../src/highlight/EmissiveController.js';
    // ... w beforeEach:
    emissive = new EmissiveController({ interactables });
    setLayerSpy = vi.spyOn(emissive, 'setLayer');
    clearLayerSpy = vi.spyOn(emissive, 'clearLayer');
    controller = new RaycastController({ renderer, camera, interactables, store, emissive });
    ```

    Pamiętaj: tests/RaycastController.test.js już istnieje z Phase 3 — to MODYFIKACJA, nie nowy plik. Zachowaj wszystkie pozostałe testy (hysteresis, click-vs-drag, attemptStep flow); zmień TYLKO te które dotykają emissive directly.

    Verification:
    - `grep -c "_hoverPrevEmissive" src/RaycastController.js` zwraca 0
    - `grep -c "this._emissive\\.setLayer\\|this._emissive\\.clearLayer" src/RaycastController.js` >= 2
    - `grep -c "mesh.material.emissive.setHex" src/RaycastController.js` zwraca 0 (wszystkie writes idą przez emissive controller)
  </action>
  <verify>
    <automated>npx vitest run tests/RaycastController.test.js</automated>
  </verify>
  <done>
    - `tests/RaycastController.test.js` zielony — wszystkie testy Phase 3 nadal pass + zmienione asercje na spy
    - `grep -c "_hoverPrevEmissive" src/RaycastController.js` === 0
    - `grep -c "this._emissive\\." src/RaycastController.js` >= 2
    - Pełny `npm test` zielony
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset (style.css, index.html) | Bez user input — czysto statyczne |
| input event → store via RaycastController | Granica istniejąca z Phase 3, port nie zmienia trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | RaycastController port — regresja Phase 3 | mitigate | Pełny test suite RaycastController.test.js musi pass + integration test uruchomienie.integration.test.js (zostanie zaktualizowany w Plan 04-06) |
| T-04-12 | Information Disclosure | XSS via index.html | mitigate | Tylko statyczne ID/atrybuty — żadnego user content w nowych elementach (StatusPanel/StepPanel używają textContent w runtime — Plan 04-04) |
</threat_model>

<verification>
- `grep -c "phase3-" index.html style.css` === 0 (oba pliki czyste)
- `grep -c "_hoverPrevEmissive" src/RaycastController.js` === 0
- `grep -c "this._emissive\\." src/RaycastController.js` >= 2
- `npx vitest run tests/RaycastController.test.js` zielony
- `npm test` pełny suite zielony
</verification>

<success_criteria>
- DOM zrestrukturyzowane (#status-panel + #step-panel zamiast #phase3-*)
- CSS czysty z .phase3-* i ma nowe reguły z Wong palette
- RaycastController port D-Phase4-13 zakończony — hover idzie przez EmissiveController.setLayer/clearLayer
- Wszystkie testy Phase 3 RaycastController nadal zielone (regresja-free)
</success_criteria>

<output>
Po completion stwórz `.planning/phases/04-visual-feedback-layer/04-05-SUMMARY.md` zgodny z templates/summary.md.
</output>
