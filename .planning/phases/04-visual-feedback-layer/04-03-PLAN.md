---
phase: 04-visual-feedback-layer
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/highlight/HighlightManager.js
  - src/highlight/EdgeOutlineController.js
  - tests/HighlightManager.test.js
  - tests/EdgeOutlineController.test.js
autonomous: true
requirements: [FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-04, FEEDBACK-05]
must_haves:
  truths:
    - "HighlightManager subskrybuje state.steps i mapuje status → emissive layer (error→pulse czerwony, done→flash zielony)"
    - "Mapowanie krok→mesh używa activeScenario.steps[].targetMeshId (single source per Phase 1 D-06)"
    - "Cleared status (oczekuje/aktywny) wywołuje EmissiveController.clearLayer('state', mesh)"
    - "EdgeOutlineController prebuilduje EdgesGeometry+LineSegments per interactable RAZ w konstruktorze"
    - "Subscriber na state.hcOutlineMode toggluje LineSegments.visible all-at-once"
    - "Zero OutlinePass w pipeline (FEEDBACK-03)"
    - "dispose() obu klas zwalnia subskrypcje + GPU buffers (geometries + materials)"
  artifacts:
    - path: "src/highlight/HighlightManager.js"
      provides: "subscriber state.steps → EmissiveController.setLayer('state', ...)"
      exports: ["HighlightManager"]
      min_lines: 60
    - path: "src/highlight/EdgeOutlineController.js"
      provides: "high-contrast outline mode via EdgesGeometry+LineSegments"
      exports: ["EdgeOutlineController"]
      min_lines: 60
    - path: "tests/HighlightManager.test.js"
      provides: "Testy error pulse + done flash + clear lifecycle + dispose"
    - path: "tests/EdgeOutlineController.test.js"
      provides: "Testy prebuild + toggle + dispose GPU buffers"
  key_links:
    - from: "src/highlight/HighlightManager.js"
      to: "EmissiveController.setLayer('state', ...)"
      via: "DI emissive instance"
      pattern: "this\\._emissive\\.(setLayer|clearLayer)"
    - from: "src/highlight/EdgeOutlineController.js"
      to: "store.subscribe((s) => s.hcOutlineMode)"
      via: "subscribeWithSelector"
      pattern: "hcOutlineMode"
---

<objective>
Stworzyć dwa controllery Wave 3 — `HighlightManager` (FEEDBACK-01/02/03/04) i `EdgeOutlineController` (FEEDBACK-05). Oba konsumują EmissiveController/store fundament z 04-02 i 04-01.

`HighlightManager` to subskryber `state.steps` mapujący każdy step na mesh przez `activeScenario.steps[].targetMeshId` i delegujący do `EmissiveController.setLayer('state', mesh, {color, pulse|flash})`. `EdgeOutlineController` to prebuild EdgesGeometry+LineSegments per interactable z toggle `visible` napędzanym subskrypcją `state.hcOutlineMode`.

Per FEEDBACK-04: redundantne kodowanie — HighlightManager dostarcza warstwę KOLOR (#D55E00 error / #009E73 success) w 3D; StepPanel/StatusPanel (Plan 04-04) dostarczają TEKST + IKONA w DOM. Razem tworzą color+icon+text projekcję.
Per FEEDBACK-05: HC toggle przełącza prezentację z emissive na outline-shader (LineSegments.visible) — colorblind-safe alternatywa.

Output: 2 NEW src plików w `src/highlight/` + 2 NEW test plików.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-feedback-layer/04-CONTEXT.md
@.planning/phases/04-visual-feedback-layer/04-PATTERNS.md
@src/highlight/EmissiveController.js
@src/state/trainingStore.js
@src/training/scenarios/uruchomienie.js
@src/main.js
@src/PressModel.js

<interfaces>
<!-- HighlightManager API: -->
```javascript
class HighlightManager {
  constructor({ store, emissive: EmissiveController, interactables: Map<string, THREE.Mesh> });
  dispose(): void;
}
```

<!-- EdgeOutlineController API: -->
```javascript
class EdgeOutlineController {
  constructor({ interactables: Map<string, THREE.Mesh>, store });
  dispose(): void;
}
```

<!-- Store flag konsumowany: -->
state.hcOutlineMode: boolean (z Plan 04-01)
state.steps: { [stepId]: { status: 'pending'|'active'|'done'|'error' } }
state.activeScenario: { steps: Array<{ id, kind, labelPL, targetMeshId? }> }

<!-- EmissiveController API konsumowany: -->
emissive.setLayer('state', mesh, { color: 0xRRGGBB, pulse?: boolean, flash?: boolean })
emissive.clearLayer('state', mesh)

<!-- Wong palette (D-Phase4-06, Phase 1 UI-06 lock): -->
ERROR_HEX = 0xD55E00
SUCCESS_HEX = 0x009E73
HC_LINE_DEFAULT = 0xFFFFFF
EDGES_THRESHOLD_DEG = 15
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/highlight/HighlightManager.js + tests/HighlightManager.test.js</name>
  <files>src/highlight/HighlightManager.js, tests/HighlightManager.test.js</files>
  <read_first>
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja HighlightManager (linie 138-202) — gotowy szkielet ctor + _wireSubscribers + _projectStepsToMeshes
    - src/main.js linie 59-73 (_wireStoreSubscribers analog) i linie 100-135 (_renderStepAndAttest — pattern szukania kroku w activeScenario)
    - src/training/scenarios/uruchomienie.js — by zobaczyć structure step.id, step.kind, step.targetMeshId
    - tests/RaycastController.test.js linie 1-50 (makeMesh helper analog)
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-15 (HighlightManager subscribe contract), D-Phase4-11 (pulse), D-Phase4-12 (flash)
    - src/highlight/EmissiveController.js (z 04-02) — by znać API setLayer/clearLayer
  </read_first>
  <behavior>
    - Test 1: gdy startScenario(uruchomienie) i `steps['odblokuj-estop'] = { status: 'error' }`, EmissiveController.setLayer wywołany z ('state', estopMesh, {color: 0xD55E00, pulse: true})
    - Test 2: gdy `steps['odblokuj-estop'] = { status: 'done' }`, setLayer wywołany z ('state', estopMesh, {color: 0x009E73, flash: true})
    - Test 3: gdy `steps['odblokuj-estop'] = { status: 'pending' }`, EmissiveController.clearLayer wywołany z ('state', estopMesh)
    - Test 4: krok bez `targetMeshId` (np. visual-attest) NIE wywołuje setLayer (graceful skip)
    - Test 5: krok z `targetMeshId` którego mesha nie ma w interactables (mismatch) — graceful skip, brak throw
    - Test 6: dispose() zwalnia subscribers (po dispose, kolejne setState NIE wywołuje EmissiveController)
    - Test 7: initial render — subskryber odpala się dopiero przy CHANGE; HighlightManager w konstruktorze MUSI wywołać _projectStepsToMeshes raz na initial state (analog main.js linia 51-52)
  </behavior>
  <action>
    Stwórz `src/highlight/HighlightManager.js`:

    Header:
    ```javascript
    // src/highlight/HighlightManager.js
    // Phase 4 — FEEDBACK-01/02/03/04: subskrypcja state.steps, mapowanie krok→mesh
    // przez activeScenario.steps[].targetMeshId, wywołanie EmissiveController.setLayer('state', ...).
    // D-Phase4-15. Boundary: importuje store; THREE/gsap pośrednio przez EmissiveController; NIE training/, NIE DOM.
    ```

    Importy: tylko store types (komentarzem JSDoc) — runtime import to TYLKO `EmissiveController` (DI), `pl` (jeśli używany — nie jest tutaj). Per Plan 04-06 boundaries.test.js: `src/highlight/HighlightManager.js` może `from '../state/'` przez DI tylko, ale aktualnie nie potrzebujemy bezpośredniego importu — store przychodzi przez konstruktor DI.

    Klasa zgodnie z 04-PATTERNS.md (linie 147-201):
    ```javascript
    export class HighlightManager {
      constructor({ store, emissive, interactables }) {
        this._store = store;
        this._emissive = emissive;
        this._interactables = interactables;
        this._unsubscribers = [];
        this._wireSubscribers();
      }

      _wireSubscribers() {
        // D-Phase4-15: subscribe na state.steps
        const unsub = this._store.subscribe(
          (s) => s.steps,
          (steps) => this._projectStepsToMeshes(steps),
        );
        this._unsubscribers.push(unsub);
        // Initial render — analog main.js linia 51 (store.subscribe odpala się dopiero przy CHANGE)
        this._projectStepsToMeshes(this._store.getState().steps);
      }

      _projectStepsToMeshes(steps) {
        const scenario = this._store.getState().activeScenario;
        if (!scenario) return;
        for (const step of scenario.steps) {
          const mesh = step.targetMeshId ? this._interactables.get(step.targetMeshId) : null;
          if (!mesh) continue;
          const status = steps[step.id]?.status;
          if (status === 'error') {
            this._emissive.setLayer('state', mesh, { color: 0xD55E00, pulse: true });
          } else if (status === 'done') {
            this._emissive.setLayer('state', mesh, { color: 0x009E73, flash: true });
          } else {
            // status === 'pending' | 'active' | undefined → clear
            this._emissive.clearLayer('state', mesh);
          }
        }
      }

      dispose() {
        for (const u of this._unsubscribers) u();
        this._unsubscribers = [];
      }
    }
    ```

    KRYTYCZNE: brak polskich literałów (UI-06). Brak `import` z `../training/` (boundary). Hex literały OK (kolory są data, nie polski tekst).

    Stwórz `tests/HighlightManager.test.js` (`@vitest-environment node`):
    - import EmissiveController + HighlightManager + createTrainingStore + uruchomienie scenario
    - helper `makeInteractables(ids[])` → Map<id, mockMesh>
    - 7 testów (lista w `<behavior>`)
    - Użyj `vi.spyOn(EmissiveController.prototype, 'setLayer')` lub bezpośrednio mockuj instance metody by asertować call args (zalecany: prawdziwy EmissiveController + sprawdzanie efektu na mesh.material.emissive — bardziej integration-y, ale zaufanie wzrasta)

    Pattern testu (analog 04-PATTERNS.md linie 644-661):
    ```javascript
    it('step.error → EmissiveController.setLayer(state, mesh, {color: 0xD55E00, pulse: true})', () => {
      const store = createTrainingStore();
      store.getState().startScenario(uruchomienie);
      const stepWithMesh = uruchomienie.steps.find(s => s.targetMeshId);
      const mesh = makeMesh(stepWithMesh.targetMeshId);
      const interactables = new Map([[stepWithMesh.targetMeshId, mesh]]);
      const emissive = new EmissiveController({ interactables });
      const setLayerSpy = vi.spyOn(emissive, 'setLayer');
      const hm = new HighlightManager({ store, emissive, interactables });
      setLayerSpy.mockClear(); // initial _projectStepsToMeshes pewnie wywołał clearLayer

      store.setState((s) => ({ steps: { ...s.steps, [stepWithMesh.id]: { status: 'error' } } }));

      expect(setLayerSpy).toHaveBeenCalledWith('state', mesh, { color: 0xD55E00, pulse: true });
      hm.dispose();
      emissive.dispose();
    });
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/HighlightManager.test.js</automated>
  </verify>
  <done>
    - `tests/HighlightManager.test.js` zielony (7+ asercji)
    - `grep -c "import" src/highlight/HighlightManager.js` zwraca 0 lub 1 (najwyżej JSDoc-only types nie liczą jako runtime; runtime import: 0 jeśli wszystko przez DI)
    - `grep -E "import.*from.*\\.\\./training" src/highlight/HighlightManager.js` zwraca pusto
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/highlight/EdgeOutlineController.js + tests/EdgeOutlineController.test.js</name>
  <files>src/highlight/EdgeOutlineController.js, tests/EdgeOutlineController.test.js</files>
  <read_first>
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja EdgeOutlineController (linie 207-266) — gotowy szkielet prebuild + dispose
    - src/MaterialRegistry.js linie 60-80 (disposeAll pattern — geometry.dispose() + material.dispose())
    - src/PressModel.js _buildLightCurtain (jeśli dostępne) — analog LineSegments+EdgesGeometry usage
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-10 (implementacja outline = EdgesGeometry threshold 15deg + LineSegments per interactable, prebuild raz, toggle visible)
    - tests/MaterialRegistry.smoke.test.js — analog dispose smoke
  </read_first>
  <behavior>
    - Test 1: ctor prebuilduje LineSegments per mesh w interactables (15 mocked → 15 LineSegments dodanych jako dzieci meshy, wszystkie .visible === false na starcie gdy hcOutlineMode === false)
    - Test 2: gdy ctor odpalony z store.hcOutlineMode === true, wszystkie LineSegments mają .visible === true natychmiast (initial render)
    - Test 3: store.setState({ hcOutlineMode: true }) → wszystkie LineSegments.visible === true
    - Test 4: store.setState({ hcOutlineMode: false }) z true → wszystkie .visible === false
    - Test 5: dispose() — geometry.dispose() wywołane na każdym EdgesGeometry (spy/mock); material.dispose() wywołane raz; LineSegments removed from parent mesh; subscriber unsubscribed
  </behavior>
  <action>
    Stwórz `src/highlight/EdgeOutlineController.js`:

    Header:
    ```javascript
    // src/highlight/EdgeOutlineController.js
    // Phase 4 — FEEDBACK-05: high-contrast outline mode. Prebuild EdgesGeometry+LineSegments
    // per interactable RAZ w konstruktorze; toggle visible przez subscriber state.hcOutlineMode.
    // D-Phase4-10. SC1 wyklucza OutlinePass — używamy tylko THREE.EdgesGeometry + LineSegments.
    // Boundary: importuje THREE + store (DI); NIE training/, NIE DOM.
    ```

    Importy: `import * as THREE from 'three';` — store przez DI.

    Stałe:
    ```javascript
    const EDGES_THRESHOLD_DEG = 15;
    const HC_LINE_COLOR_DEFAULT = 0xFFFFFF; // białe linie — kontrast bezpieczny dla deuteranopii
    ```

    Klasa zgodnie z 04-PATTERNS.md linie 222-265:
    ```javascript
    export class EdgeOutlineController {
      constructor({ interactables, store }) {
        this._store = store;
        this._lines = new Map();
        this._geometries = [];
        this._material = new THREE.LineBasicMaterial({ color: HC_LINE_COLOR_DEFAULT });

        for (const mesh of interactables.values()) {
          const edges = new THREE.EdgesGeometry(mesh.geometry, EDGES_THRESHOLD_DEG);
          const segs = new THREE.LineSegments(edges, this._material);
          segs.visible = false;
          mesh.add(segs);
          this._lines.set(mesh, segs);
          this._geometries.push(edges);
        }

        this._unsub = this._store.subscribe(
          (s) => s.hcOutlineMode,
          (on) => this._toggleAll(on),
        );
        this._toggleAll(this._store.getState().hcOutlineMode);
      }

      _toggleAll(on) {
        for (const segs of this._lines.values()) segs.visible = !!on;
      }

      dispose() {
        if (this._unsub) { this._unsub(); this._unsub = null; }
        for (const geo of this._geometries) geo.dispose();
        this._geometries.length = 0;
        this._material.dispose();
        for (const segs of this._lines.values()) {
          if (segs.parent) segs.parent.remove(segs);
        }
        this._lines.clear();
      }
    }
    ```

    Stwórz `tests/EdgeOutlineController.test.js` (`@vitest-environment node`):
    - import THREE, EdgeOutlineController, createTrainingStore
    - helper `makeMesh(id, geo = new THREE.BoxGeometry())` — Three.js geom + Material + Mesh
    - Test 1: prebuild — `expect(mesh.children.filter(c => c.isLineSegments)).toHaveLength(1)` na każdym z 3 mocked meshach; każdy z `.visible === false` na initial gdy hcOutlineMode === false
    - Test 2: store z hcOutlineMode initial true → po ctor `.visible === true` na wszystkich
    - Test 3: toggle dynamiczny store.setState({hcOutlineMode:true}) → wszystkie visible=true; setState false → wszystkie false
    - Test 4: dispose — `vi.spyOn(geo, 'dispose')` na każdej EdgesGeometry; po dispose wszystkie wywołane; subscriber zwolniony (kolejny setState NIE wywołuje _toggleAll)
    - Test 5: zero OutlinePass — sourcefile regex check: `expect(fileContents).not.toMatch(/OutlinePass/)` (FEEDBACK-03 explicit assertion)
  </action>
  <verify>
    <automated>npx vitest run tests/EdgeOutlineController.test.js</automated>
  </verify>
  <done>
    - `tests/EdgeOutlineController.test.js` zielony (5+ asercji)
    - `grep -c "OutlinePass" src/highlight/EdgeOutlineController.js` zwraca 0 (FEEDBACK-03)
    - `grep -E "import.*from.*\\.\\./training" src/highlight/EdgeOutlineController.js` zwraca pusto
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store→scene write | HighlightManager + EdgeOutlineController czytają state, piszą do mesh.material/visible — one-way data flow (CRIT-7 invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | One-way data flow | mitigate | Żaden handler w HighlightManager/EdgeOutlineController NIE pisze do store (tylko subscribe + read) — code review checklist + boundary update w Plan 04-06 |
| T-04-06 | Denial of Service | GPU memory leak (EdgesGeometry+LineSegments) | mitigate | dispose() zwalnia _geometries[].dispose() + _material.dispose() + remove segs from parent (test wymusza spies) |
| T-04-07 | Information Disclosure | brak | accept | LOW — żadnego PII, czysto wizualne |
</threat_model>

<verification>
- `npx vitest run tests/HighlightManager.test.js tests/EdgeOutlineController.test.js` — oba zielone
- `grep -c "OutlinePass" src/highlight/` zwraca 0 across all files (FEEDBACK-03 explicit)
- `grep -rE "import.*from.*\\.\\./training" src/highlight/` zwraca pusto (boundary)
- `grep -rn "0xD55E00\|0x009E73" src/highlight/HighlightManager.js` pokazuje 2 (Wong palette w użyciu)
- Pełny `npm test` zielony
</verification>

<success_criteria>
- HighlightManager mapuje status step → emissive layer (error pulse czerwony / done flash zielony / cleared) per FEEDBACK-01/02
- EdgeOutlineController prebuilduje LineSegments raz, toggluje visible per state.hcOutlineMode (FEEDBACK-05)
- Zero OutlinePass w pipeline (FEEDBACK-03)
- Wong palette w użyciu (FEEDBACK-04 kolor warstwa)
- dispose obu klas zwalnia subscribers + GPU buffers
</success_criteria>

<output>
Po completion stwórz `.planning/phases/04-visual-feedback-layer/04-03-SUMMARY.md` zgodny z templates/summary.md.
</output>
