---
phase: 04-visual-feedback-layer
plan: 04
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/ui/StatusPanel.js
  - src/ui/StepPanel.js
  - tests/StatusPanel.test.js
  - tests/StepPanel.test.js
autonomous: true
requirements: [UI-01, UI-02, FEEDBACK-04, FEEDBACK-05]
must_haves:
  truths:
    - "StatusPanel renderuje top-bar z emoji + Polish state label + 'Wynik: N/100' + HC toggle button (UI-02 + D-Phase4-03)"
    - "StatusPanel HC toggle persistuje do localStorage 'pm300:hc-outline:v1' i piszę state.hcOutlineMode (D-Phase4-09)"
    - "StatusPanel używa aria-pressed na HC button (a11y)"
    - "StepPanel renderuje listę kroków activeScenario z auto-scroll do aktywnego (UI-01 + D-Phase4-04)"
    - "StepPanel każdy item ma emoji + numer + labelPL + klasę step-item--{stateKey}"
    - "Visual-attest button disabled gdy state.isAnimating=true LUB status==='done' (CRIT-8 affordance)"
    - "Wszystkie polskie stringi z pl.* — zero literałów w *.js (UI-06 boundary)"
  artifacts:
    - path: "src/ui/StatusPanel.js"
      provides: "DOM top-bar: state icon+label, score, HC toggle"
      exports: ["StatusPanel"]
      min_lines: 80
    - path: "src/ui/StepPanel.js"
      provides: "DOM lewa kolumna z listą kroków + visual-attest inline button"
      exports: ["StepPanel"]
      min_lines: 80
    - path: "tests/StatusPanel.test.js"
      provides: "Testy render + HC toggle + localStorage persist + ARIA"
    - path: "tests/StepPanel.test.js"
      provides: "Testy render 8 kroków + auto-scroll + visual-attest button + isAnimating disable"
  key_links:
    - from: "src/ui/StatusPanel.js"
      to: "localStorage 'pm300:hc-outline:v1'"
      via: "click handler na HC button"
      pattern: "pm300:hc-outline:v1"
    - from: "src/ui/StepPanel.js"
      to: "store.attemptStep({ kind:'check', stepId })"
      via: "visual-attest inline button click"
      pattern: "attemptStep"
---

<objective>
Stworzyć dwa komponenty DOM Phase 4 — `StatusPanel` (top bar UI-02) i `StepPanel` (lewa kolumna UI-01). Oba subskrybują store i renderują redundantne kodowanie kolor+ikona+tekst (FEEDBACK-04). HC toggle button w StatusPanel zapisuje wybór do localStorage (D-Phase4-09) i propaguje do `state.hcOutlineMode` — zasilając EdgeOutlineController z 04-03.

`StepPanel` zastępuje Phase 3 placeholder `_renderStepAndAttest` z `src/main.js` (linie 100-135) — przenosi visual-attest button jako inline element przy danym kroku z `kind === 'visual-attest'` i status `aktywny`. Auto-scroll smooth do active step.

Oba pliki muszą: (a) używać `textContent` (XSS-safe), (b) wszystkie polskie stringi z `pl.*` (UI-06), (c) NIE importować THREE/gsap (boundary egzekwowane w Plan 04-06).

Output: 2 NEW src plików w `src/ui/` + 2 NEW test plików (jsdom).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-feedback-layer/04-CONTEXT.md
@.planning/phases/04-visual-feedback-layer/04-PATTERNS.md
@src/i18n/pl.js
@src/DisclaimerBanner.js
@src/main.js
@tests/disclaimerBanner.test.js
@src/training/scenarios/uruchomienie.js

<interfaces>
<!-- StatusPanel API: -->
```javascript
class StatusPanel {
  constructor({ store, rootElementId?: string = 'status-panel' });
  dispose(): void;
}
```

<!-- StepPanel API: -->
```javascript
class StepPanel {
  constructor({ store, rootElementId?: string = 'step-panel' });
  dispose(): void;
}
```

<!-- Klucze pl.js (z 04-01) jakie konsumujemy: -->
pl.machineState[stateKey] (Polish full label)
pl.machineStateIcons[stateKey] (emoji)
pl.stepStates[stateKey]
pl.stepStateIcons[stateKey]
pl.ui.scorePrefix ('Wynik: ')
pl.ui.hcToggleOn ('Wysoki kontrast: WŁ')
pl.ui.hcToggleOff ('Wysoki kontrast: WYŁ')
pl.ui.attestPrefix ('Potwierdź: ')
pl.ui.attestAriaPrefix ('Potwierdź krok: ')
pl.ui.procedureComplete ('Procedura zakończona')

<!-- localStorage key (D-Phase4-09): -->
HC_STORAGE_KEY = 'pm300:hc-outline:v1'

<!-- Store fields konsumowane: -->
state.machineState, state.scoring.score, state.hcOutlineMode (StatusPanel)
state.activeScenario, state.steps, state.currentStepId, state.isAnimating (StepPanel)
state.attemptStep ({kind:'check', stepId}) (StepPanel button)

<!-- Wymagana strukturę DOM root (Plan 04-06 wstawi do index.html): -->
<div id="status-panel"></div>
<aside id="step-panel"></aside>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/ui/StatusPanel.js + tests/StatusPanel.test.js</name>
  <files>src/ui/StatusPanel.js, tests/StatusPanel.test.js</files>
  <read_first>
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja StatusPanel (linie 364-451) — gotowy szkielet ctor + _build + _render + _wireSubscribers + localStorage + dispose
    - src/DisclaimerBanner.js (cały plik) — analog DOM mount + ARIA + dispose + textContent
    - tests/disclaimerBanner.test.js — analog jsdom + button click + localStorage
    - src/main.js linie 80-87 (`_renderStatusText` — pattern Polish state label + score)
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-03 (4 elementy belki), D-Phase4-08 (HC button), D-Phase4-09 (localStorage), D-Phase4-16 (osobny score readout)
  </read_first>
  <behavior>
    - Test 1: render — gdy machineState='gotowa-do-pracy', icon textContent === '🟢', state textContent === 'Gotowa do pracy', score textContent === 'Wynik: 100/100', HC button textContent === 'Wysoki kontrast: WYŁ', aria-pressed === 'false'
    - Test 2: store.setState({machineState:'awaria'}) → re-render: icon === '⚠️', state === 'Awaria — błąd procedury'
    - Test 3: store.setState scoring.score=75 → score textContent === 'Wynik: 75/100'
    - Test 4: HC button click — store.hcOutlineMode flip true, localStorage 'pm300:hc-outline:v1' === 'true', button textContent === 'Wysoki kontrast: WŁ', aria-pressed === 'true'
    - Test 5: dispose() — odpinacze odpięte (subsequent setState NIE re-renderuje), removeEventListener wywołany na HC button
    - Test 6: rzuca jeśli #status-panel nie istnieje (`throw new Error('StatusPanel: brak #status-panel w DOM')` analog DisclaimerBanner) — sanity test
    - Test 7: localStorage write w private mode rzuca DOMException — silent catch (try/catch); store.hcOutlineMode wciąż się ustawia (D-Phase4-09 graceful)
  </behavior>
  <action>
    Stwórz `src/ui/StatusPanel.js`:

    Header:
    ```javascript
    // src/ui/StatusPanel.js
    // Phase 4 — UI-02 + FEEDBACK-04/05: top bar z 6 polskimi stanami maszyny + score + HC toggle.
    // D-Phase4-03 (4 elementy belki), D-Phase4-08 (HC button), D-Phase4-09 (localStorage persist),
    // D-Phase4-16 (score osobny element). Boundary: importuje DOM + store + pl; NIE THREE/gsap.
    ```

    Importy: `import { pl } from '../i18n/pl.js';` — nic więcej (DOM globalny, store przez DI).

    Stałe:
    ```javascript
    const HC_STORAGE_KEY = 'pm300:hc-outline:v1';
    ```

    Klasa zgodnie z 04-PATTERNS.md linie 378-449 — szkielet:
    - constructor: pobierz #status-panel, throw jeśli brak; _build() (statyczna struktura DOM); _wireSubscribers(); _render() initial
    - _readPersisted/_writePersisted: try/catch wokół localStorage (private mode/quota)
    - _build(): innerHTML JEDEN RAZ ze szkieletem (4 spans + button); zapisz handle do .icon/.state/.score/.hcBtn; podepnij click listener wywołujący `store.setState({hcOutlineMode: !current})` + `_writePersisted(next)`
    - _render(): textContent na każdym z 4 elementów (XSS-safe); `pl.machineStateIcons[stateKey] ?? ''`, `pl.machineState[stateKey] ?? stateKey` (fallback na key by NIGDY nie pokazać `undefined`); aria-pressed na button (string!)
    - _wireSubscribers(): 3 subscribery (machineState, scoring.score, hcOutlineMode) — każdy wywołuje _render(); push do this._unsubscribers
    - dispose(): removeEventListener + odpinacze

    Stwórz `tests/StatusPanel.test.js` (`@vitest-environment jsdom`):
    - import { StatusPanel }, createTrainingStore
    - beforeEach: `document.body.innerHTML = '<div id="status-panel"></div>';` + nowy store
    - afterEach: panel.dispose() + clear DOM + clear localStorage
    - 7 testów (lista w `<behavior>`)
    - Test 7 (localStorage failure): mock `Storage.prototype.setItem` żeby rzucało; assert że click NIE rzuca (silent catch); state.hcOutlineMode wciąż się zmienia
  </action>
  <verify>
    <automated>npx vitest run tests/StatusPanel.test.js</automated>
  </verify>
  <done>
    - `tests/StatusPanel.test.js` zielony (7+ asercji)
    - `grep -E "import.*['\"]three" src/ui/StatusPanel.js` zwraca pusto (boundary)
    - `grep -E "import.*['\"]gsap" src/ui/StatusPanel.js` zwraca pusto (boundary)
    - `grep -nE "innerHTML\\s*=\\s*['\\\"]" src/ui/StatusPanel.js` — tylko 1 wystąpienie (statyczny szkielet w _build), nigdy z user input
    - `grep -c "pm300:hc-outline:v1" src/ui/StatusPanel.js` >= 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/ui/StepPanel.js + tests/StepPanel.test.js</name>
  <files>src/ui/StepPanel.js, tests/StepPanel.test.js</files>
  <read_first>
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja StepPanel (linie 273-358) — gotowy szkielet ctor + _wireSubscribers + _render + _mapStatusToStateKey + dispose
    - src/main.js linie 100-135 (`_renderStepAndAttest` — pełna analogia 1:1 dla pojedynczego kroku, Phase 4 rozszerza do listy)
    - tests/disclaimerBanner.test.js — analog jsdom + button click
    - src/training/scenarios/uruchomienie.js — by zobaczyć structure step.id, step.kind, step.labelPL, step.targetMeshId
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-04 (auto-scroll smooth, double-click protection)
  </read_first>
  <behavior>
    - Test 1: po startScenario(uruchomienie), `#step-panel ol.step-panel__list li.step-item` ma 8 elementów (uruchomienie ma 8 kroków)
    - Test 2: pierwszy krok (currentStepId pierwszego) ma klasę `step-item--aktywny`; reszta ma `step-item--oczekuje`
    - Test 3: textContent pierwszego li zawiera emoji '▶️' + '1.' + step.labelPL (z `pl.stepStateIcons.aktywny`)
    - Test 4: po setState steps[stepId] = {status:'error'} li otrzymuje klasę `step-item--blad` i ikona zmienia się na ❌
    - Test 5: po setState steps[stepId] = {status:'done'} klasa `step-item--poprawny` + ikona ✅
    - Test 6: visual-attest krok aktywny → button `.phase4-attest-check` widoczny pod tym li
    - Test 7: setState({isAnimating:true}) → button `.phase4-attest-check` ma `disabled === true`
    - Test 8: button click przy isAnimating=false → `store.getState().attemptStep` wywołany z `{kind:'check', stepId: <step.id>}`
    - Test 9: scrollIntoView smooth wywołany z {behavior:'smooth', block:'center'} przy zmianie currentStepId — spy na Element.prototype.scrollIntoView
    - Test 10: status === 'done' nawet gdy step jest current → klasa --poprawny (NIE --aktywny — done wins per _mapStatusToStateKey)
    - Test 11: dispose — kolejny setState nie re-renderuje (subscribery odpięte)
  </behavior>
  <action>
    Stwórz `src/ui/StepPanel.js` zgodnie z 04-PATTERNS.md linie 282-358. Kluczowe szczegóły:

    Header + import:
    ```javascript
    // src/ui/StepPanel.js
    // Phase 4 — UI-01: panel boczny lewa kolumna z listą kroków scenariusza, auto-scroll
    // do aktywnego, inline visual-attest button. Zastępuje Phase 3 _renderStepAndAttest
    // z src/main.js. D-Phase4-04 (auto-scroll smooth, double-click protection).
    // Boundary: importuje DOM + store + pl; NIE THREE/gsap/training.

    import { pl } from '../i18n/pl.js';
    ```

    Klasa:
    - constructor: pobierz #step-panel, throw jeśli brak; _wireSubscribers(); _render() initial
    - _wireSubscribers(): 3 subscribery (currentStepId, steps, isAnimating) — wszystkie wywołują _render()
    - _render():
      1. state = store.getState(); scenario = state.activeScenario; jeśli null → root.replaceChildren() i return
      2. utwórz `<ol class="step-panel__list">`
      3. iteruj scenario.steps z idx — dla każdego:
         - status = state.steps[step.id]?.status ?? 'pending'
         - stateKey = _mapStatusToStateKey(status, step.id === state.currentStepId)
         - li.className = `step-item step-item--${stateKey}`
         - icon = pl.stepStateIcons[stateKey] ?? ''
         - **TEXTCONTENT** (XSS-safe): `li.textContent = \`${icon} ${idx+1}. ${step.labelPL}\`;`
         - jeśli step.kind === 'visual-attest' && step.id === state.currentStepId && status !== 'done': dodaj button
            - btn.className = 'phase4-attest-check'
            - btn.textContent = pl.ui.attestPrefix + step.labelPL
            - btn.setAttribute('aria-label', pl.ui.attestAriaPrefix + step.labelPL)
            - btn.disabled = state.isAnimating
            - btn.addEventListener('click', () => store.getState().attemptStep({kind:'check', stepId: step.id}))
            - li.appendChild(btn)
         - jeśli step.id === currentStepId → zapamiętaj activeEl
      4. root.replaceChildren(list)
      5. jeśli activeEl: `activeEl.scrollIntoView({behavior:'smooth', block:'center'})`
    - _mapStatusToStateKey(status, isCurrent):
        ```javascript
        if (status === 'done')  return 'poprawny';
        if (status === 'error') return 'blad';
        if (isCurrent)          return 'aktywny';
        return 'oczekuje';
        ```
    - dispose(): odpinacze + this._unsubscribers = []

    KRYTYCZNE:
    - Brak `innerHTML =` z user content — tylko `textContent` (XSS, analog DisclaimerBanner.js linia 65)
    - Brak `import` z `three`, `gsap`, `../training/`. Jedyny import: `pl` + DOM globalny (document)
    - Klucze stateKey: 'oczekuje'|'aktywny'|'poprawny'|'blad' — DOKŁADNIE zgodne z `pl.stepStates` z Plan 04-01
    - 'blad' (bez ł) — match polskim klasom CSS BEM-friendly (Plan 04-06 doda `.step-item--blad` w style.css)

    Stwórz `tests/StepPanel.test.js` (`@vitest-environment jsdom`):
    - beforeEach: `document.body.innerHTML = '<aside id="step-panel"></aside>';` + store + startScenario(uruchomienie)
    - 11 testów (lista w `<behavior>`)
    - Test 9 scrollIntoView spy: `vi.spyOn(Element.prototype, 'scrollIntoView')` przed `new StepPanel`; po setState({currentStepId: stepId2}) expect spy wywołany z {behavior:'smooth', block:'center'}
    - Test 8 attemptStep spy: `vi.spyOn(store.getState(), 'attemptStep')` LUB sprawdź side-effect (po click, jeśli krok ma kind='visual-attest', state.steps[stepId].status zmienił się — bardziej end-to-end)
  </action>
  <verify>
    <automated>npx vitest run tests/StepPanel.test.js</automated>
  </verify>
  <done>
    - `tests/StepPanel.test.js` zielony (11+ asercji)
    - `grep -E "import.*['\"]three|import.*['\"]gsap|from.*\\.\\./training" src/ui/StepPanel.js` zwraca pusto (boundary)
    - `grep -nE "innerHTML\\s*=" src/ui/StepPanel.js` zwraca pusto (TYLKO textContent + appendChild + replaceChildren — XSS-safe)
    - `grep -c "step-item--" src/ui/StepPanel.js` >= 1 (klasy stanowe)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pl.js → DOM (textContent) | Polskie stringi z pl.machineState/stepStates wstawiane przez textContent — XSS-safe by default |
| localStorage ↔ runtime | Klucz pm300:hc-outline:v1 czytany/pisany przez StatusPanel (LOW trust — czysto kosmetyczny toggle) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | localStorage 'pm300:hc-outline:v1' | accept | LOW — czysto cosmetic toggle; tampering = "user lubi/nie lubi outlinów". Brak PII, brak auth, brak ścieżki escalacji |
| T-04-09 | Information Disclosure (XSS) | pl.* → DOM render | mitigate | textContent everywhere (NIGDY innerHTML z user/dynamic content); test grep `innerHTML\s*=` w StepPanel zwraca pusto, w StatusPanel tylko 1× statyczny szkielet w _build |
| T-04-10 | Denial of Service | DOM event listener leak | mitigate | dispose() removeEventListener + odpinacze subscriberów (test wymusza) |
</threat_model>

<verification>
- `npx vitest run tests/StatusPanel.test.js tests/StepPanel.test.js` — oba zielone
- `grep -rE "import.*['\"]three" src/ui/` zwraca pusto
- `grep -rE "import.*['\"]gsap" src/ui/` zwraca pusto
- `grep -rnE "innerHTML\\s*=" src/ui/` — tylko 1 wystąpienie (StatusPanel _build statyczny szkielet)
- Pełny `npm test` zielony
</verification>

<success_criteria>
- StatusPanel renderuje 4 elementy (icon+state+score+HC button) z polskim tekstem z pl.* (UI-02 SC3)
- StepPanel renderuje 8 kroków uruchomienia z auto-scroll smooth + visual-attest button (UI-01)
- HC button persistuje do localStorage 'pm300:hc-outline:v1' i pisze state.hcOutlineMode (D-Phase4-09)
- Visual-attest button disabled gdy isAnimating=true LUB status='done' (D-Phase4-04 affordance, defense-in-depth nad CRIT-8)
- Wszystkie polskie stringi z pl.* (UI-06)
- Zero THREE/gsap importów (boundary)
</success_criteria>

<output>
Po completion stwórz `.planning/phases/04-visual-feedback-layer/04-04-SUMMARY.md` zgodny z templates/summary.md.
</output>
