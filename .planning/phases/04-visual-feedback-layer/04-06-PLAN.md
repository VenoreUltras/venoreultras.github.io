---
phase: 04-visual-feedback-layer
plan: 06
type: execute
wave: 5
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified:
  - src/main.js
  - src/UI.js
  - tests/boundaries.test.js
  - tests/uruchomienie.integration.test.js
  - tests/application.test.js
autonomous: false
requirements: [FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-04, FEEDBACK-05, UI-01, UI-02]
must_haves:
  truths:
    - "Application konstruktor instancjuje EmissiveController PRZED RaycastController, potem HighlightManager + EdgeOutlineController + StatusPanel + StepPanel"
    - "Application bootstrap-uje state.hcOutlineMode z localStorage 'pm300:hc-outline:v1' PRZED instancjacją EdgeOutlineController/StatusPanel"
    - "Application.dispose() chain zwalnia wszystkie 5 nowych instancji + RaycastController + emissiveController PO RaycastController (clearLayer hover może wywołać setHex na zdisposowanym mesh inaczej)"
    - "src/UI.js NIE zawiera już updateStatus() projektującego isRunning → #status-text — StatusPanel jest single source"
    - "src/main.js NIE zawiera już _wireStoreSubscribers, _renderStatusText, _renderStepAndAttest (zastąpione przez StepPanel + StatusPanel)"
    - "tests/boundaries.test.js zawiera 5 nowych entries: src/highlight/EmissiveController.js, HighlightManager.js, EdgeOutlineController.js, src/ui/StepPanel.js, StatusPanel.js z poprawnymi forbidden imports"
    - "tests/uruchomienie.integration.test.js przechodzi happy-path 8/8 kroków z nowymi panelami (StepPanel + StatusPanel) i potwierdza emissive #D55E00 na error step"
    - "Manual checkpoint: deuteranopia simulator pokazuje że error/success są jednoznaczne"
  artifacts:
    - path: "src/main.js"
      provides: "Application z 5 nowymi controllerami + bootstrap localStorage"
      contains: "EmissiveController"
    - path: "src/UI.js"
      provides: "UI bez updateStatus() projekcji"
    - path: "tests/boundaries.test.js"
      provides: "5 nowych entries dla src/highlight/* i src/ui/*"
    - path: "tests/uruchomienie.integration.test.js"
      provides: "Integration assert end-to-end z Phase 4 panelami"
  key_links:
    - from: "src/main.js Application constructor"
      to: "5 nowych controllerów (EmissiveController, HighlightManager, EdgeOutlineController, StatusPanel, StepPanel)"
      via: "instantiation order"
      pattern: "new (EmissiveController|HighlightManager|EdgeOutlineController|StatusPanel|StepPanel)"
    - from: "Application.dispose"
      to: "wszystkie 5 nowych instancji + RaycastController + emissiveController"
      via: "dispose chain"
      pattern: "\\.dispose\\(\\)"
---

<objective>
Final wiring Phase 4. Spinamy 5 nowych klas (EmissiveController/HighlightManager/EdgeOutlineController/StatusPanel/StepPanel) w `Application`, usuwamy zastąpione metody Phase 3 (`_renderStatusText`, `_renderStepAndAttest`, `_wireStoreSubscribers`) z `src/main.js` i `updateStatus()` z `src/UI.js` (D-Phase4-17).

Bootstrap localStorage (D-Phase4-09) — odczyt `'pm300:hc-outline:v1'` PRZED konstruktorami subscriberów; `store.setState({ hcOutlineMode: persisted })` ustawia initial state. EdgeOutlineController/StatusPanel czytają już ustawioną wartość.

Update `tests/boundaries.test.js` — 5 nowych entries (src/highlight/* mogą importować THREE+gsap+store, NIE training/DOM; src/ui/* mogą importować DOM+store+pl, NIE THREE/gsap/training).

Update `tests/uruchomienie.integration.test.js` — refaktor selektorów (jeśli używa #phase3-*) na #step-panel/#status-panel + dorzuć asercje że error step daje emissive=#D55E00 + ikona ❌ + tekst "Błąd" w DOM (FEEDBACK-04 redundant encoding test).

Manual checkpoint na końcu (D-Phase4-NA — SC5 deuteranopia simulator) — Phase 4 SC5: "Pełny Phase 3 happy-path produkuje widoczne green/red feedback; viewed through deuteranopia simulator distinguishable". Ten checkpoint jest BLOKUJĄCY przed `/gsd-verify-work`.

Output: 5 zmodyfikowanych plików + 1 manual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-feedback-layer/04-CONTEXT.md
@.planning/phases/04-visual-feedback-layer/04-PATTERNS.md
@src/main.js
@src/UI.js
@tests/boundaries.test.js
@tests/uruchomienie.integration.test.js
@tests/application.test.js
@src/highlight/EmissiveController.js
@src/highlight/HighlightManager.js
@src/highlight/EdgeOutlineController.js
@src/ui/StatusPanel.js
@src/ui/StepPanel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/main.js — wire 5 controllerów + bootstrap localStorage + usuń legacy renderery</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js cały (186 linii) — by zobaczyć dokładną strukturę i co usuwamy
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja main.js MODIFY (linie 504-565) — gotowy diff wiring + dispose chain
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-09 (localStorage bootstrap), D-Phase4-13/14 (EmissiveController PRZED RaycastController), D-Phase4-15/16 (StatusPanel score osobny od status badge), D-Phase4-17 (UI.updateStatus usuniecie)
    - src/highlight/* i src/ui/* (Plan 04-02..04-04) — by potwierdzić sygnatury konstruktorów
  </read_first>
  <action>
    Edit `src/main.js` — pełna restruktura `Application.constructor`:

    1. Dorzuć importy na top:
    ```javascript
    import { EmissiveController } from './highlight/EmissiveController.js';
    import { HighlightManager } from './highlight/HighlightManager.js';
    import { EdgeOutlineController } from './highlight/EdgeOutlineController.js';
    import { StatusPanel } from './ui/StatusPanel.js';
    import { StepPanel } from './ui/StepPanel.js';
    ```

    2. W `constructor()`, PO `this.store = createTrainingStore()` ale PRZED `startScenario`, dorzuć bootstrap:
    ```javascript
    // D-Phase4-09: bootstrap hcOutlineMode z localStorage przed instancjacją subscriberów
    const hcInitial = (() => {
      try { return localStorage.getItem('pm300:hc-outline:v1') === 'true'; }
      catch { return false; }
    })();
    this.store.setState({ hcOutlineMode: hcInitial });
    ```

    3. Po `this.store.getState().startScenario(uruchomienie)` (linia 36), zmień instantiację Phase 3 na Phase 4 sequence (kolejność krytyczna — D-Phase4-13/14):
    ```javascript
    // D-Phase4-14: EmissiveController PRZED RaycastController (RaycastController potrzebuje go w DI)
    this.emissiveController = new EmissiveController({
      interactables: this.pressModel.getInteractables(),
    });

    // RaycastController z DI emissive (Plan 04-05 brownfield port)
    this.raycastController = new RaycastController({
      renderer: this.sceneSetup.renderer,
      camera: this.sceneSetup.camera,
      interactables: this.pressModel.getInteractables(),
      store: this.store,
      emissive: this.emissiveController,
    });
    this.tickables.push((dt) => this.raycastController._runHysteresis(dt));

    // D-Phase4-15: HighlightManager subscribuje state.steps → emissive layer 'state'
    this.highlightManager = new HighlightManager({
      store: this.store,
      emissive: this.emissiveController,
      interactables: this.pressModel.getInteractables(),
    });

    // FEEDBACK-05: EdgeOutlineController prebuilduje LineSegments + subskrybuje hcOutlineMode
    this.edgeOutlineController = new EdgeOutlineController({
      interactables: this.pressModel.getInteractables(),
      store: this.store,
    });

    // UI-01/02: DOM panele (rootElementId domyślny match'uje index.html z Plan 04-05)
    this.statusPanel = new StatusPanel({ store: this.store });
    this.stepPanel = new StepPanel({ store: this.store });
    ```

    4. USUŃ stare metody i wywołania (Phase 3 placeholdery — D-Phase4-02/D-Phase4-17):
    - USUŃ wywołanie `this._wireStoreSubscribers();` (linia 49)
    - USUŃ wywołania `this._renderStatusText();` i `this._renderStepAndAttest(...)` (linia 51-52)
    - USUŃ całe metody `_wireStoreSubscribers()` (linie 59-73), `_renderStatusText()` (linie 80-87), `_renderStepAndAttest(currentStepId)` (linie 100-135)
    - USUŃ import `pl` jeśli nie jest już używany w main.js (sprawdź — może być potrzebny gdzie indziej)

    5. Update `dispose()` — chain zgodnie z 04-PATTERNS.md linie 552-565:
    ```javascript
    dispose() {
      gsap.ticker.remove(this._tickerCallback);
      for (const unsub of this._unsubscribers) unsub();
      this._unsubscribers = [];
      if (this.disclaimerBanner) this.disclaimerBanner.dispose();
      if (this.stepPanel) this.stepPanel.dispose();           // NEW Phase 4
      if (this.statusPanel) this.statusPanel.dispose();       // NEW Phase 4
      if (this.highlightManager) this.highlightManager.dispose(); // NEW Phase 4
      if (this.edgeOutlineController) this.edgeOutlineController.dispose(); // NEW Phase 4
      if (this.raycastController) this.raycastController.dispose(); // RaycastController PRZED emissive
      if (this.emissiveController) this.emissiveController.dispose(); // PO RaycastController (raycast.dispose woła clearLayer hover, emissive musi jeszcze żyć)
      this.pressModel.disposeMaterials();
      this.sceneSetup.dispose();
    }
    ```

    KOLEJNOŚĆ DISPOSE KRYTYCZNA: RaycastController → emissiveController. RaycastController.dispose woła `_commitLeave()` który woła `this._emissive.clearLayer('hover', target)` — emissive musi żyć w tym momencie. Dodaj komentarz inline.

    6. NIE modyfikuj `simulationTick`, `tickables`, `_tickerCallback` — tor RPM/animacja zostaje (D-Phase4-17).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('src/main.js','utf8');['_wireStoreSubscribers','_renderStatusText','_renderStepAndAttest'].forEach(s=>{if(m.includes(s+'(')&&!m.includes('// USUNIĘTY')&&!m.includes('// removed')){if(m.match(new RegExp('^[^/]*'+s+'\\\\(','m')))process.exit(1)}});['EmissiveController','HighlightManager','EdgeOutlineController','StatusPanel','StepPanel'].forEach(s=>{if(!m.includes('new '+s))process.exit(1)});console.log('OK')"</automated>
  </verify>
  <done>
    - `grep -c "new EmissiveController\\|new HighlightManager\\|new EdgeOutlineController\\|new StatusPanel\\|new StepPanel" src/main.js` === 5
    - `grep -c "_renderStatusText\\|_renderStepAndAttest\\|_wireStoreSubscribers" src/main.js` === 0 (metody usunięte)
    - `grep -c "pm300:hc-outline:v1" src/main.js` >= 1 (bootstrap)
    - Pełny `npm test` zielony
  </done>
</task>

<task type="auto">
  <name>Task 2: src/UI.js — usuń updateStatus() projekcji isRunning → #status-text + tests/application.test.js update</name>
  <files>src/UI.js, tests/application.test.js</files>
  <read_first>
    - src/UI.js cały — by zlokalizować updateStatus() i btnToggle event listener
    - tests/application.test.js cały (lub fragmenty z `updateStatus`/#status-text/#status-dot) — by zaktualizować testy
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md D-Phase4-17 (UI.updateStatus usuniecie, slider RPM tor zostaje)
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja UI.js MODIFY (linie 572-585)
  </read_first>
  <action>
    Edit `src/UI.js`:

    1. ZNAJDŹ event listener na `this.elements.btnToggle` (linie ~22-26):
    ```javascript
    this.elements.btnToggle.addEventListener('click', () => {
      this.isRunning = !this.isRunning;
      this.updateStatus(); // <- USUŃ to wywołanie
    });
    ```
    Usuń wywołanie `this.updateStatus()`. Pozostaw `this.isRunning = !this.isRunning;` — slider RPM tor zostaje (D-Phase4-17).

    2. USUŃ całą metodę `updateStatus()` (linie ~34-44 — projekcja isRunning → #status-text/#status-dot textContent/className). StatusPanel jest single source dla `#status-text` (Plan 04-04 — chociaż StatusPanel używa innych ID `.status-panel__state`).

    3. USUŃ jakiekolwiek pole `this.elements.statusText` / `this.elements.statusDot` jeśli było używane TYLKO przez updateStatus — w przeciwnym razie zostaw (może być czytane skądinąd; sprawdź grep).

    4. NIE usuwaj: `getAngularVelocity()`, `updateTelemetry(angle, displacement)`, slider RPM listener, btn-toggle event handler (samego eventu) — Phase 1-3 lock-in.

    Uwaga `pl.ui.statusRunning` / `pl.ui.statusStopped` — pozostają w pl.js (niewykorzystane v1, mogą być reused w Phase 5; nie usuwamy by nie regresować Phase 1-3).

    Edit `tests/application.test.js`:

    Sprawdź czy testy używają `#status-text`/`#status-dot`/`updateStatus`:
    - Jeśli tak: zamień asercje na `#status-panel .status-panel__state` (StatusPanel jest renderowane przez Application via `new StatusPanel`).
    - Jeśli test asertował że click `btn-toggle` zmienia `#status-text` na "Praca ciągła"/"Zatrzymana" — USUŃ ten test lub zamień na assertion że `this.isRunning` flip'uje. Status maszyny w StatusPanel pochodzi ze `state.machineState`, nie z `isRunning` — to dwa ortogonalne kanały (D-Phase4-17).
    - Dorzuć (jeśli brak): test smoke że Application instantiacja tworzy `app.statusPanel`, `app.stepPanel`, `app.highlightManager`, `app.edgeOutlineController`, `app.emissiveController` jako pola.

    Verification:
    - `grep -c "updateStatus" src/UI.js` === 0
    - `grep -c "this\\.isRunning" src/UI.js` >= 1 (flag wciąż istnieje, używany przez RPM tor)
  </action>
  <verify>
    <automated>npx vitest run tests/application.test.js</automated>
  </verify>
  <done>
    - `grep -c "updateStatus" src/UI.js` === 0
    - `tests/application.test.js` zielony
    - Pełny `npm test` zielony — żaden test Phase 1-3 nie regresował (z wyjątkiem zmienionych asercji DOM `#status-text` → `.status-panel__state`)
  </done>
</task>

<task type="auto">
  <name>Task 3: tests/boundaries.test.js — 5 nowych entries dla src/highlight/* i src/ui/*</name>
  <files>tests/boundaries.test.js</files>
  <read_first>
    - tests/boundaries.test.js cały — by zobaczyć FORBIDDEN_PAIRS pattern + jak dodać nowe entries
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja boundaries.test.js MODIFY (linie 776-782) — gotowe 5 entries
    - .planning/phases/04-visual-feedback-layer/04-CONTEXT.md sekcja "Test boundaries dla nowych plików" (linia 83)
  </read_first>
  <action>
    Edit `tests/boundaries.test.js` — dorzuć 5 nowych entries do `FORBIDDEN_PAIRS` (lub równoważnej struktury):

    ```javascript
    // Phase 4 entries (D-Phase4-NA — boundary scanner update):
    // src/highlight/* mogą THREE+gsap+store; NIE training/, NIE DOM bezpośrednio (komunikują się przez store)
    {
      file: 'src/highlight/EmissiveController.js',
      mustNotImport: ['../state/', '../training/', './state/', './training/'],
    },
    {
      file: 'src/highlight/HighlightManager.js',
      mustNotImport: ['../training/', './training/'],
    },
    {
      file: 'src/highlight/EdgeOutlineController.js',
      mustNotImport: ['../training/', './training/'],
    },
    // src/ui/* mogą DOM+store+pl; NIE THREE/gsap/training
    {
      file: 'src/ui/StepPanel.js',
      mustNotImport: ['three', 'gsap', '../training/', './training/'],
    },
    {
      file: 'src/ui/StatusPanel.js',
      mustNotImport: ['three', 'gsap', '../training/', './training/'],
    },
    ```

    UWAGA o EmissiveController: per 04-CONTEXT linia 83 — "src/highlight/EmissiveController.js może importować THREE i GSAP, ale nie store bezpośrednio (komunikuje się przez DI z HighlightManager/RaycastController)". Stąd `mustNotImport: ['../state/', '../training/', ...]`. HighlightManager dostaje store DI ale CZYTA store — może `from '../state/'` jeśli faktycznie importuje (ale Plan 04-03 nie importuje runtime, używa DI), więc forbid TYLKO `../training/`. Dostosuj zgodnie z faktycznymi importami w plikach.

    Sprawdź dokładnie istniejący format FORBIDDEN_PAIRS w pliku — może być inna struktura (np. tablica obiektów, mapa, plik per file). Dostosuj składnię.

    Verification:
    - `grep -c "src/highlight/EmissiveController.js\\|src/highlight/HighlightManager.js\\|src/highlight/EdgeOutlineController.js\\|src/ui/StepPanel.js\\|src/ui/StatusPanel.js" tests/boundaries.test.js` === 5
  </action>
  <verify>
    <automated>npx vitest run tests/boundaries.test.js</automated>
  </verify>
  <done>
    - `tests/boundaries.test.js` zielony — wszystkie 5 nowych plików spełnia boundary
    - `grep -c "src/highlight/" tests/boundaries.test.js` >= 3
    - `grep -c "src/ui/" tests/boundaries.test.js` >= 2
  </done>
</task>

<task type="auto">
  <name>Task 4: tests/uruchomienie.integration.test.js — refaktor selektorów + assert FEEDBACK-04 redundant encoding</name>
  <files>tests/uruchomienie.integration.test.js</files>
  <read_first>
    - tests/uruchomienie.integration.test.js cały — by zlokalizować `#phase3-*` selektory (jeśli są) i głowne asercje happy-path
    - src/training/scenarios/uruchomienie.js — by znać step.id z `targetMeshId` na potrzeby asercji emissive
    - .planning/phases/04-visual-feedback-layer/04-PATTERNS.md sekcja uruchomienie.integration.test.js MODIFY (linie 789-792)
    - src/highlight/EmissiveController.js + HighlightManager.js — by wiedzieć jak emissive jest pisane na error/done
  </read_first>
  <action>
    Edit `tests/uruchomienie.integration.test.js`:

    1. Jeśli test używa `@vitest-environment node` (bez DOM): asercje DOM są poza zakresem — dorzuć TYLKO scene-side asercje:
       ```javascript
       it('error step → mesh.material.emissive === #D55E00 + state.steps[stepId].status === error (FEEDBACK-04 color encoding)', () => {
         // setup: app/store/HighlightManager + 1 mocked mesh dla 'estop' lub innego step.targetMeshId
         // wywołaj attemptStep który wywoła error effect
         // expect mesh.material.emissive.getHex() === 0xD55E00
       });
       ```

    2. Jeśli test mountuje DOM (`@vitest-environment jsdom`):
       - Refaktor selektorów: `#phase3-step-readout` → `#step-panel .step-item--aktywny`; `#phase3-attest-container button` → `#step-panel .phase4-attest-check`
       - Setup DOM: `document.body.innerHTML = '<div id="status-panel"></div><aside id="step-panel"></aside>';` PRZED instantiation Application
       - Dorzuć asercję redundant encoding (FEEDBACK-04):
         ```javascript
         // FEEDBACK-04 redundant encoding: kolor + ikona + tekst
         expect(mesh.material.emissive.getHex()).toBe(0xD55E00); // KOLOR
         const errorItem = document.querySelector('.step-item--blad');
         expect(errorItem).not.toBeNull();
         expect(errorItem.textContent).toContain('❌'); // IKONA
         // TEKST już jest w textContent (labelPL) — assert że klasa ma wariant blad
         ```

    3. Happy-path 8 kroków: zachowaj istniejące asercje przejść scenariusza; jedyna zmiana to selektory DOM (jeśli były) i dorzucenie scene-side emissive checks dla error/done branches.

    4. NIE wymagamy pełnej deuteranopia QA w teście — to manual checkpoint w Task 5. Tu tylko automatyczne sanity że redundant encoding fizycznie działa (3 niezależne kanały: kolor 3D + ikona DOM + tekst DOM).

    Sprawdź czy test obecnie przechodzi PRZED zmianami; jeśli przechodzi — wprowadź minimum zmian (zachowuj backward compat). Jeśli nie przechodzi z powodu Phase 4 wiring — naprawiaj selektory.
  </action>
  <verify>
    <automated>npx vitest run tests/uruchomienie.integration.test.js</automated>
  </verify>
  <done>
    - `tests/uruchomienie.integration.test.js` zielony
    - `grep -c "phase3-step-readout\\|phase3-attest-container" tests/uruchomienie.integration.test.js` === 0 (jeśli były tam selektory — zostały zmigrowane)
    - Co najmniej 1 nowa asercja redundant encoding (kolor 3D + ikona DOM)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual checkpoint — Phase 4 SC5 deuteranopia simulator + happy path w przeglądarce</name>
  <what-built>
    Phase 4 visual feedback layer kompletny:
    - 5 nowych klas (EmissiveController, HighlightManager, EdgeOutlineController, StatusPanel, StepPanel)
    - 4 nowe sekcje w pl.js (stepStates/stepStateIcons/machineStateIcons + ui.scorePrefix/hcToggleOn/hcToggleOff)
    - DOM zrestrukturyzowane (#status-panel + #step-panel)
    - CSS Wong palette (#D55E00 / #009E73) na blad/poprawny
    - HC outline toggle persistowany do localStorage
  </what-built>
  <how-to-verify>
    1. Uruchom `npm run dev`. Otwórz `http://localhost:5173/`.
    2. Sprawdź że na top bar widać `🔍 Oczekiwanie na inspekcję — Wynik: 100/100 — Wysoki kontrast: WYŁ`.
    3. Sprawdź że lewa kolumna (`#step-panel`) renderuje 8 kroków uruchomienia, pierwszy z `▶️` (aktywny), reszta z `⏳` (oczekuje).
    4. Wykonaj happy path 8/8 kroków klikając meshe w 3D. Po każdym poprawnym kroku:
       - DOM: ikona kroku zmienia się na `✅` (klasa `.step-item--poprawny`, zielona krawędź lewa)
       - 3D: krótki zielony flash (`#009E73`) ~800ms na meshu
       - Score: rośnie/utrzymuje 100
    5. Wymuś błąd: w pierwszym kroku kliknij niewłaściwy mesh (np. tabliczkę). Sprawdź:
       - DOM: aktywny krok dostaje klasę `.step-item--blad`, ikona `❌`, czerwona krawędź lewa
       - 3D: czerwone pulsowanie (`#D55E00`) na meshu, kontynuuje aż do następnej akcji
       - Score: spadek (-10/-25)
    6. Kliknij `Wysoki kontrast: WYŁ` button. Sprawdź:
       - Wszystkie 15 interactables dostają białe krawędzie (LineSegments visible)
       - Button label zmienia się na `Wysoki kontrast: WŁ`, aria-pressed=true
       - localStorage: w DevTools → Application → Local Storage → `pm300:hc-outline:v1` === `"true"`
       - Reload page → tryb HC nadal aktywny (persist)
    7. **Deuteranopia simulator** — użyj DevTools → Rendering → "Emulate vision deficiencies" → "Deuteranopia". Wymuś error step ponownie. Sprawdź że:
       - Czerwony pulse jest wciąż wizualnie odróżnialny od zielonego (pulse intensity oscylacja vs flash 800ms)
       - Ikona ❌ vs ✅ jest jednoznaczna
       - Tekst klasy `Błąd` vs `Poprawny` jest jednoznaczny
       - **Włączenie trybu HC** — krawędzie dają jeszcze jeden orthogonalny kanał (białe krawędzie + emissive odpadający)
    8. Sprawdź FPS w DevTools Performance — idle hover nad meshem nie zwiększa frame time o >2ms (CRIT-5 budget).
    9. Sprawdź `console` — zero errors/warnings przy load i podczas happy-path.
  </how-to-verify>
  <resume-signal>
    Wpisz "approved" jeśli wszystkie 9 kontroli zielone (deuteranopia QA pass + 60 FPS hold + console clean) — Phase 4 zamknięty, można odpalić `/gsd-verify-work 4`.

    Jeśli problem: opisz konkretnie który punkt zawiódł (np. "punkt 5: error pulse niewidoczny w trybie HC"), planner stworzy gap closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application bootstrap → store | localStorage read → setState({hcOutlineMode}); silent fallback false na exception |
| dispose chain order | RaycastController.dispose woła emissive.clearLayer — emissive musi przeżyć RaycastController.dispose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | localStorage 'pm300:hc-outline:v1' bootstrap | accept | LOW — czysto cosmetic; tampering = "user lubi outline". Bootstrap silent catch zapewnia że quota/private mode nie crashuje app |
| T-04-14 | Denial of Service | dispose chain misorder → leaked timeline lub use-after-free | mitigate | Komentarz inline w dispose() o kolejności RaycastController PRZED emissive; integration test smoke + manual checkpoint #9 (console clean) |
| T-04-15 | Information Disclosure | brak | accept | LOW — czysto wizualne, brak PII |
</threat_model>

<verification>
- `npm test` pełny suite zielony (wszystkie testy Phase 1-3 + nowe Phase 4 testy)
- `grep -c "new EmissiveController\\|new HighlightManager\\|new EdgeOutlineController\\|new StatusPanel\\|new StepPanel" src/main.js` === 5
- `grep -c "_renderStatusText\\|_renderStepAndAttest\\|_wireStoreSubscribers\\|updateStatus" src/main.js src/UI.js` === 0 (legacy methods removed)
- `grep -c "src/highlight/\\|src/ui/" tests/boundaries.test.js` >= 5
- Manual checkpoint: human approval (deuteranopia QA + 60 FPS + console clean)
</verification>

<success_criteria>
- Application instantiacja w poprawnej kolejności (Emissive → Raycast → Highlight → Edge → Status → Step)
- Bootstrap localStorage hcOutlineMode (D-Phase4-09)
- Phase 3 legacy renderery usunięte z main.js i UI.js (D-Phase4-02/D-Phase4-17)
- 5 boundaries entries dodane (egzekwują architektoniczne invarianty Phase 4)
- Integration test pokrywa happy-path Phase 4 z redundant encoding asercjami
- Manual checkpoint deuteranopia QA pass (Phase 4 SC5)
</success_criteria>

<output>
Po completion + human approval stwórz `.planning/phases/04-visual-feedback-layer/04-06-SUMMARY.md` zgodny z templates/summary.md. Phase 4 zamknięty — kolejny krok: `/gsd-verify-work 4`.
</output>
