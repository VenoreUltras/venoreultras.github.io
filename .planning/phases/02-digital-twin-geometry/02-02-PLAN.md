---
phase: 02-digital-twin-geometry
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/PressModel.js
autonomous: true
requirements: [TWIN-01, TWIN-03, TWIN-04, TWIN-06]
must_haves:
  truths:
    - "Po buildPress() pressModel.getInteractables() zawiera 6 ID: kolo-zamachowe, hamulec, wziernik-smarowania, oslona-tylna, kurtyna-lewa, kurtyna-prawa"
    - "Każdy z tych 6 meshów ma sklonowany MeshStandardMaterial (różne instancje per mesh)"
    - "Koło zamachowe rotuje razem z this.shaftAxis (jest jego dzieckiem) bez zmiany w update(angle)"
    - "Koło zamachowe zachowuje pozycję (-2.5, 0, 0) w lokalnym frame'a shaftAxis (świat -2.5, 8, 0) — D-Phase2-04 zachowane"
    - "Klocek hamulca jest dzieckiem this.group (statyczny, nie rotuje), pozycja (2.5, 8, 0) ±0.5 — D-Phase2-04 ZACHOWANE (HIGH-2)"
    - "Tarcza hamulcowa jest dzieckiem this.shaftAxis na PRAWYM końcu wału (x>0), Claude's Discretion z CONTEXT §'Oddzielna tarcza hamulcowa' — visual-only, nie interactable"
    - "Każdy mesh ma userData = { id, kind, restPosition, labelPL, descriptionPL } z kind 'visual-target' (kolo-zamachowe/wziernik/oslona-tylna/kurtyna-*) lub 'manipulation' (hamulec)"
  artifacts:
    - path: "src/PressModel.js"
      provides: "Metody _buildFlywheel, _buildBrake, _buildOilSightGlass, _buildRearGuard, _buildLightCurtain wywołane z buildPress()"
      contains: "_buildFlywheel"
  key_links:
    - from: "_buildFlywheel()"
      to: "this.shaftAxis"
      via: "this.shaftAxis.add(flywheelGroup) + this.shaftAxis.add(brakeDisc) — auto-rotacja przez scene graph"
      pattern: "shaftAxis\\.add"
    - from: "_buildBrake/_buildOilSightGlass/_buildRearGuard/_buildLightCurtain"
      to: "this.group"
      via: "this.group.add(...) — statyczne dzieci top-level grupy prasy"
      pattern: "this\\.group\\.add"
---

<objective>
Wave 2: dokleja 6 statycznych meshów do PressModel — wszystkie z prymitywów core Three.js (BoxGeometry / CylinderGeometry), bez pivot-grup ruchomych, bez CanvasTexture. Demonstruje pełny kontrakt _registerInteractable (TWIN-11/12/13) na realnych meshach. Adresuje wymagania TWIN-01 (koło zamachowe), TWIN-03 (hamulec — klocek, kind manipulation ale brak poses w Phase 2 per D-Phase2-06), TWIN-04 (wziernik), TWIN-06 (osłona tylna + 2 kolumny kurtyny).

**Kluczowe** (revision HIGH-2):
- Koło zamachowe @ świat `(-2.5, 8, 0)` — LEWA strona wału (D-Phase2-04).
- Tarcza hamulcowa @ świat `(+1.7, 8, 0)` — PRAWA strona wału (przeciwna do koła zamachowego). Claude's Discretion z CONTEXT.
- Klocek hamulca @ świat `(2.5, 8, 0)` — PRAWA strona, dokładnie jak D-Phase2-04 cementuje. Dotyka tarczy hamulcowej z prawej z odstępem ~0.1.
- Koło zamachowe = dziecko `this.shaftAxis` → auto-rotuje za darmo przez istniejący `update(angle)`.
- Tarcza hamulcowa = dziecko `this.shaftAxis` → rotuje razem z wałem.
- Pozostałe 4 (hamulec/wziernik/oslona-tylna/kurtyna-*) = dzieci `this.group` (statyczne).

Atomic commit: "feat(02): static meshes — flywheel + brake + oil sight + rear guard + light curtain (TWIN-01/03/04/06)".
Output: 5 nowych prywatnych metod w PressModel; 6 wpisów w `_interactables` po buildPress(); zero zmian w `update(angle)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-digital-twin-geometry/02-CONTEXT.md
@.planning/phases/02-digital-twin-geometry/02-RESEARCH.md
@.planning/phases/02-digital-twin-geometry/02-UI-SPEC.md
@.planning/phases/02-digital-twin-geometry/02-01-SUMMARY.md
@src/PressModel.js
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _buildFlywheel() (TWIN-01) — koło zamachowe (LEWA strona wału) + tarcza hamulcowa (PRAWA strona wału)</name>
  <files>src/PressModel.js</files>
  <action>
Dodaj prywatną metodę `_buildFlywheel()` po `buildPress()` (przed `_registerInteractable`). Wywołaj ją z `buildPress()` PRZED `this.update(0)`.

**Layout (HIGH-2 — D-Phase2-04 ZACHOWANE):**
- Koło zamachowe → `flywheelGroup.position.set(-2.5, 0, 0)` w lokalnym frame'a `shaftAxis` (shaftAxis ma `position.y = 8`, więc świat = (-2.5, 8, 0)). To **dokładnie D-Phase2-04**.
- Tarcza hamulcowa → `brakeDisc.position.set(+1.7, 0, 0)` w lokalnym frame'a `shaftAxis` (świat = (1.7, 8, 0)). PRAWA strona wału, w pobliżu prawej krawędzi (klocek będzie na x=2.5).

Geometria koła zamachowego (per RESEARCH §"Koło zamachowe jako dziecko shaftAxis" + CONTEXT Claude's Discretion §"6 szprych" + UI-SPEC visual fidelity):
- `flywheelGroup = new THREE.Group()` — KONTENER dla obwodu + szprych. Pozycja: `flywheelGroup.position.set(-2.5, 0, 0)` (lokalnie w shaftAxis).
- `this.shaftAxis.add(flywheelGroup)` — KRYTYCZNE: dziecko shaftAxis (D-Phase2-05).
- Obwód: `rimGeo = new THREE.CylinderGeometry(1.5, 1.5, 0.3, 32); rimGeo.rotateZ(Math.PI / 2)` — koło ustawione pionowo (oś obrotu wzdłuż X, czyli wzdłuż wału). `rim = new THREE.Mesh(rimGeo, this.matFlywheel)`.
- `flywheelGroup.add(rim)`.
- 6 szprych (CONTEXT discretion): `for (let i = 0; i < 6; i++) { const spokeGeo = new THREE.BoxGeometry(0.1, 2.8, 0.1); const spoke = new THREE.Mesh(spokeGeo, this.matFlywheel); spoke.rotation.x = (Math.PI / 6) * i; flywheelGroup.add(spoke); }` — 6 prostokątnych szprych co 60° wokół osi X.
- Cienie: `rim.castShadow = true; rim.receiveShadow = true`; szprychy `castShadow = true`.

**Tarcza hamulcowa** (CONTEXT Claude's Discretion §"Oddzielna tarcza hamulcowa" — udokumentowane wykorzystanie discretion, MEDIUM-3):
- Tarcza jest osobnym `CylinderGeometry` jako dziecko `shaftAxis`, na PRAWYM końcu wału (x>0).
- Visual-only: NIE jest interactable (brak userData / brak _registerInteractable). Jej rola: target wizualny dla klocka hamulca (Task 2).
- Rotuje razem z wałem (dziecko shaftAxis) — wizualnie spójne z realnymi prasami gdzie tarcza hamulcowa siedzi na wale głównym.

```js
const brakeDiscGeo = new THREE.CylinderGeometry(0.9, 0.9, 0.15, 32);
brakeDiscGeo.rotateZ(Math.PI / 2);  // pionowo, oś obrotu wzdłuż X (jak koło zamachowe)
const brakeDisc = new THREE.Mesh(brakeDiscGeo, this.matBrakeSteel);
brakeDisc.position.set(1.7, 0, 0);  // PRAWA strona wału (lokalnie w shaftAxis); świat = (1.7, 8, 0)
brakeDisc.castShadow = true;
this.shaftAxis.add(brakeDisc);
// NIE _registerInteractable — visual-only (CONTEXT Claude's Discretion).
```

**Komentarz JSDoc** nad tarczą explicitnie odwołujący się do CONTEXT:
```js
// Tarcza hamulcowa: Claude's Discretion z CONTEXT §"Oddzielna tarcza hamulcowa wbudowana w wał".
// Wizualnie ważna jako target dla klocka hamulca (TWIN-03). Visual-only — brak interactable
// rejestracji (CRIT-7: tylko mesze klikalne mają userData kontrakt).
// Pozycja x=+1.7 lokalnie: PRAWA strona wału, blisko prawej krawędzi ramki, gdzie klocek
// hamulca (D-Phase2-04 @ x=2.5) ma do niej dostęp z prawej strony.
```

Rejestracja interactable koła zamachowego (PRIMARY mesh = `rim`):
```js
this._registerInteractable({
  mesh: rim,
  id: 'kolo-zamachowe',
  kind: 'visual-target',
  baseMaterial: this.matFlywheel,
});
```

Komentarz JSDoc po polsku nad metodą:
```js
/**
 * TWIN-01: Koło zamachowe (LEWA strona wału) + tarcza hamulcowa (PRAWA strona wału, visual-only).
 * Oba dzieci this.shaftAxis — auto-rotują przez istniejący update(angle), bez rozszerzania update().
 *
 * Layout (D-Phase2-04 + Claude's Discretion):
 *   - Koło zamachowe: lokalnie (-2.5, 0, 0) → świat (-2.5, 8, 0). Cementowane przez D-Phase2-04.
 *   - Tarcza hamulcowa: lokalnie (1.7, 0, 0) → świat (1.7, 8, 0). Claude's Discretion (CONTEXT
 *     §"Oddzielna tarcza hamulcowa"). PRAWA strona wału, by klocek hamulca (D-Phase2-04 @ x=2.5)
 *     miał wizualny target.
 *
 * Koło zamachowe: 6 szprych, obwód r=1.5, materiał matFlywheel (metaliczność 0.85 — UI-SPEC).
 * Tarcza hamulcowa: cylinder r=0.9, h=0.15, matBrakeSteel.
 */
```
  </action>
  <verify>
    <automated>npm test</automated>
    Manual: `npm run dev` — koło zamachowe (LEWA strona wału) ORAZ tarcza hamulcowa (PRAWA strona wału) obracają się razem z wałem przy slider'u prędkości.
    Konsola:
    - `pressModel.getInteractables().get('kolo-zamachowe').material !== pressModel.matFlywheel` → true (cloned).
    - Tarcza hamulcowa NIE jest w interactables (`pressModel.getInteractables().has('tarcza-hamulcowa')` → false — bo to nie jest interactable; visual-only).
  </verify>
  <done>
    `_buildFlywheel()` istnieje i jest wołany z `buildPress()`. Po starcie `getInteractables().has('kolo-zamachowe')` → true. Wizualnie: koło z 6 szprychami po LEWEJ stronie wału + tarcza hamulcowa po PRAWEJ stronie wału, oba rotujące razem z wałem.
  </done>
</task>

<task type="auto">
  <name>Task 2: _buildBrake() (TWIN-03, HIGH-2 fix) + _buildOilSightGlass() (TWIN-04) + _buildRearGuard() (TWIN-06a) + _buildLightCurtain() (TWIN-06b)</name>
  <files>src/PressModel.js</files>
  <action>
Cztery prywatne metody, wszystkie wywoływane z `buildPress()` PO `_buildFlywheel()` i przed `this.update(0)`.

### `_buildBrake()` — TWIN-03 klocek hamulca (HIGH-2: D-Phase2-04 ZACHOWANE — PRAWA strona wału)

Per CONTEXT D-Phase2-04 (pos `(2.5, 8, 0)` — PRAWA strona wału, dozwolone ±0.5 dostrojenie) + D-Phase2-05 (statyczny, dziecko `this.group`) + UI-SPEC negative criteria ("klocek w pozycji released NIE dotyka tarczy hamulcowej, ~0.1 jednostki odstępu").

**Layout (revision HIGH-2):** klocek na x=2.5 dotyka tarczy hamulcowej (z Task 1, świat x=1.7, r=0.9 → prawa krawędź tarczy x=1.7+0.9=2.6) z prawej strony z odstępem ~0.1 (klocek o szerokości 0.4, środek na x=2.8 → lewa krawędź klocka 2.6+0.0... ale chcemy odstęp). Korekta: środek klocka na **x=2.9** dla odstępu ~0.1 między prawą krawędzią tarczy (2.6) a lewą krawędzią klocka (2.9-0.2=2.7). To w obrębie ±0.5 od D-Phase2-04 (2.5±0.5 = [2.0, 3.0]). ✓

```js
const brakeGeo = new THREE.BoxGeometry(0.4, 0.4, 0.4);
const brake = new THREE.Mesh(brakeGeo, this.matBrakeSteel);
// Tarcza hamulcowa @ świat (1.7, 8, 0), r=0.9 → prawa krawędź tarczy x=2.6.
// Klocek docika z prawej; środek klocka x=2.9 (lewa krawędź 2.7), odstęp od tarczy 0.1. ✓
// D-Phase2-04 @ (2.5, 8, 0) ±0.5 dozwolone → 2.9 mieści się w range [2.0, 3.0]. ✓
brake.position.set(2.9, this.shaftY, 0);  // y=shaftY=8 (na wysokości tarczy)
brake.castShadow = true;
this.group.add(brake);  // STATYCZNY — D-Phase2-05; NIE shaftAxis.

this._registerInteractable({
  mesh: brake,
  id: 'hamulec',
  kind: 'manipulation',  // klikalny, ale brak poses (D-Phase2-06)
  baseMaterial: this.matBrakeSteel,
});
```

**Komentarz inline** (HIGH-2 — explicit reference do D-Phase2-04):
```js
// HIGH-2 / D-Phase2-04: klocek hamulca po PRAWEJ stronie wału (x≈2.9, w range 2.5±0.5).
// Tarcza hamulcowa (Task 1, x=1.7, r=0.9) jest po prawej stronie wału — klocek docika z prawej
// z odstępem ~0.1 (UI-SPEC negative criteria: "klocek w pozycji released NIE dotyka tarczy").
// STATYCZNY (D-Phase2-05) — dziecko this.group, nie shaftAxis. Phase 4 wizualizuje stan
// hamulca przez przesuwanie klocka o ~0.1 jednostki (czyta state.meshStates['hamulec']).
```

### `_buildOilSightGlass()` — TWIN-04 wziernik smarowania
Per CONTEXT D-Phase2-04 (pos `(0, 7, 1.1)` — front korpusu) + UI-SPEC ("mała okrągła szybka, kolor bursztynowy"). Statyczny, dziecko `this.group`.

```js
const sightGeo = new THREE.CylinderGeometry(0.15, 0.15, 0.05, 24);
sightGeo.rotateX(Math.PI / 2);
const sight = new THREE.Mesh(sightGeo, this.matOilSightYellow);
sight.position.set(0, 7, 1.1);
sight.castShadow = true;
this.group.add(sight);

this._registerInteractable({
  mesh: sight,
  id: 'wziernik-smarowania',
  kind: 'visual-target',
  baseMaterial: this.matOilSightYellow,
});
```

### `_buildRearGuard()` — TWIN-06 osłona tylna stała
Per CONTEXT D-Phase2-04 (pos `(0, 4, -1.5)`) + UI-SPEC (`#2a2a2a`).

```js
const rearGuardGeo = new THREE.BoxGeometry(2.5, 3, 0.05);
const rearGuard = new THREE.Mesh(rearGuardGeo, this.matGuardRearBlack);
rearGuard.position.set(0, 4, -1.5);
rearGuard.castShadow = true;
rearGuard.receiveShadow = true;
this.group.add(rearGuard);

this._registerInteractable({
  mesh: rearGuard,
  id: 'oslona-tylna',
  kind: 'visual-target',
  baseMaterial: this.matGuardRearBlack,
});
```

### `_buildLightCurtain()` — TWIN-06 dwie kolumny kurtyny świetlnej
Per CONTEXT D-Phase2-04 (`(±1.7, 4, 1.5)`).

```js
const curtainGeo = new THREE.CylinderGeometry(0.1, 0.1, 3, 16);

const curtainLeft = new THREE.Mesh(curtainGeo, this.matLightCurtainBlack);
curtainLeft.position.set(-1.7, 4, 1.5);
curtainLeft.castShadow = true;
this.group.add(curtainLeft);
this._registerInteractable({
  mesh: curtainLeft,
  id: 'kurtyna-lewa',
  kind: 'visual-target',
  baseMaterial: this.matLightCurtainBlack,
});

const curtainRight = new THREE.Mesh(curtainGeo, this.matLightCurtainBlack);
curtainRight.position.set(1.7, 4, 1.5);
curtainRight.castShadow = true;
this.group.add(curtainRight);
this._registerInteractable({
  mesh: curtainRight,
  id: 'kurtyna-prawa',
  kind: 'visual-target',
  baseMaterial: this.matLightCurtainBlack,
});
```

UWAGA: oba meshe używają **tej samej `curtainGeo`** (geometria shared) ale dostają **różne sklonowane materiały** (CRIT-6).

### Wywołanie z buildPress()
Po `_buildFlywheel()` i przed `this.update(0)`:
```js
this._buildBrake();
this._buildOilSightGlass();
this._buildRearGuard();
this._buildLightCurtain();
```
  </action>
  <verify>
    <automated>npm test</automated>
    Manual `npm run dev`:
    - Klocek hamulca widoczny po PRAWEJ stronie wału (x≈2.9), dotykający (z odstępem ~0.1) tarczy hamulcowej.
    - Wziernik widoczny na froncie korpusu, mały bursztynowy "okrąg".
    - Osłona tylna ciemna, tył strefy roboczej.
    - Dwie czarne kolumny po bokach strefy roboczej.

    Konsola:
    - `pressModel.getInteractables().size === 6`.
    - `pressModel.getInteractables().get('hamulec').position.x` mieści się w [2.0, 3.0] (range D-Phase2-04 ±0.5). ✓ HIGH-2.
    - `pressModel.getInteractables().get('kurtyna-lewa').material !== pressModel.getInteractables().get('kurtyna-prawa').material` → true.
  </verify>
  <done>
    Po buildPress():
    - getInteractables() ma 6 wpisów: kolo-zamachowe, hamulec, wziernik-smarowania, oslona-tylna, kurtyna-lewa, kurtyna-prawa.
    - hamulec.position.x ∈ [2.0, 3.0] (HIGH-2: D-Phase2-04 zachowane).
    - hamulec.kind === 'manipulation', reszta 'visual-target'.
    - 6 osobnych instancji material.
    - Manual: scena renderuje wszystkie 6 nowych meshów; klocek hamulca po PRAWEJ stronie wału dotyka tarczy hamulcowej z prawej.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (rotation) | Klocek hamulca jako dziecko shaftAxis przez pomyłkę | mitigate | Explicit `this.group.add(brake)`; JSDoc komentarz "STATYCZNY — D-Phase2-05" + Wave 5 smoke test asertuje że hamulec.parent === pressModel.group. |
| T-02-05 | DoS (cloning) | _registerInteractable wołane wielokrotnie z tym samym id | accept | Wave 5 smoke test asertuje `interactables.size === 15`. |
| T-02-22 | Locked decision violation | Klocek hamulca poza range D-Phase2-04 (HIGH-2) | mitigate | Explicit pozycja x=2.9 w range 2.5±0.5; verify konsoli sprawdza `position.x ∈ [2.0, 3.0]`; smoke test 02-06 może dodać assertion. |
</threat_model>

<verification>
1. `npm test` — wszystkie testy zielone.
2. `pressModel.getInteractables().size === 6`.
3. `pressModel.getInteractables().get('hamulec').position.x ∈ [2.0, 3.0]` (HIGH-2 — D-Phase2-04 zachowane).
4. Manual: koło zamachowe LEWA + tarcza hamulcowa PRAWA rotują z wałem; klocek hamulca PRAWA strona dotyka tarczy z prawej.
5. UI-SPEC §Visual Fidelity: koło reads as massive flywheel ✓; klocek z odstępem od tarczy ✓; kurtyna 2 symetryczne kolumny ✓.
</verification>

<success_criteria>
- ROADMAP Phase 2 SC1: 6/15 nowych meshów.
- ROADMAP Phase 2 SC2: getInteractables/getMeshDictionary mają 6 wpisów.
- ROADMAP Phase 2 SC3: 6 cloned materials.
- ROADMAP Phase 2 SC4: userData kontrakt.
- TWIN-01, TWIN-03, TWIN-04, TWIN-06 zaimplementowane.
- HIGH-2 (D-Phase2-04 ZACHOWANE): klocek hamulca po PRAWEJ stronie wału, x∈[2.0, 3.0]; koło zamachowe po LEWEJ x=-2.5.
- MEDIUM-3 (Claude's Discretion documented): tarcza hamulcowa explicit JSDoc reference do CONTEXT §"Oddzielna tarcza hamulcowa".
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-digital-twin-geometry/02-02-SUMMARY.md` z:
- Lista 6 zarejestrowanych ID.
- **Explicit notatka HIGH-2**: "Layout zachowuje D-Phase2-04 dokładnie — klocek hamulca po PRAWEJ stronie wału (x=2.9 ∈ [2.0, 3.0]), koło zamachowe po LEWEJ (x=-2.5). Tarcza hamulcowa po PRAWEJ (x=1.7) jako Claude's Discretion (CONTEXT §'Oddzielna tarcza hamulcowa') — visual-only, dziecko shaftAxis dla rotacji z wałem."
- **Explicit notatka MEDIUM-3**: "Tarcza hamulcowa wykorzystuje Claude's Discretion z CONTEXT — visual-only mesh (brak userData / brak interactable), dziecko shaftAxis dla rotacji razem z kołem zamachowym. Funkcjonalnie służy jako wizualny target dla klocka hamulca w Phase 4 (przesuwanie klocka o ~0.1 jednostki na podstawie state.meshStates['hamulec'])."

Atomic commit:
```
feat(02): static meshes — flywheel + brake + oil sight + rear guard + light curtain (TWIN-01/03/04/06)

- _buildFlywheel: koło zamachowe (LEWA, x=-2.5, D-Phase2-04) + tarcza hamulcowa (PRAWA, x=1.7,
  Claude's Discretion z CONTEXT). Oba dzieci shaftAxis → auto-rotacja przez update(angle).
- _buildBrake: klocek (kind manipulation, PRAWA strona, x=2.9 ∈ [2.0, 3.0] = D-Phase2-04 ±0.5),
  statyczny, odstęp ~0.1 od tarczy hamulcowej.
- _buildOilSightGlass: bursztynowa szybka na froncie korpusu.
- _buildRearGuard: ciemna ścianka tylna (visual-target).
- _buildLightCurtain: kurtyna-lewa + kurtyna-prawa, shared geometry, cloned materials.
- 6 wpisów w getInteractables() po buildPress().
- HIGH-2 fix: D-Phase2-04 zachowane — klocek hamulca po PRAWEJ stronie wału.
- MEDIUM-3: tarcza hamulcowa udokumentowana jako Claude's Discretion z CONTEXT.
```
</output>
