---
phase: 02-digital-twin-geometry
plan: 06
type: execute
wave: 6
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/main.js
  - tests/PressModel.smoke.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [TWIN-11, TWIN-12, TWIN-13]
must_haves:
  truths:
    - "Application.dispose() w main.js wywołuje pressModel.disposeMaterials() przed sceneSetup.dispose()"
    - "tests/PressModel.smoke.test.js zawiera 7 asercji TWIN-11/12/13: size=15, IDs, dictionary contract, userData identity-only, paranoid 105-pair material identity, dispose spy, HMR cycle"
    - "tests/PressModel.smoke.test.js zawiera asercję dla CanvasTexture dispose (tabliczka-znamionowa)"
    - "tests/boundaries.test.js zawiera entry dla src/MaterialRegistry.js z mustNotImport ['../state/', '../training/', './state/', './training/']"
    - "Wszystkie testy zielone (133 z Phase 1 + nowe smoke testy ≥ 7 + 1 boundaries entry)"
  artifacts:
    - path: "src/main.js"
      provides: "Wpięcie disposeMaterials w Application.dispose"
      contains: "pressModel.disposeMaterials"
    - path: "tests/PressModel.smoke.test.js"
      provides: "Smoke test TWIN-11/12/13 contract enforcement"
      contains: "@vitest-environment jsdom"
    - path: "tests/boundaries.test.js"
      provides: "Boundary entry dla MaterialRegistry"
      contains: "MaterialRegistry"
  key_links:
    - from: "src/main.js Application.dispose()"
      to: "pressModel.disposeMaterials()"
      via: "explicit method call przed sceneSetup.dispose()"
      pattern: "disposeMaterials"
    - from: "tests/PressModel.smoke.test.js"
      to: "PressModel + MaterialRegistry contract"
      via: "import { PressModel } + assertions na cloned material identity + dispose spy"
      pattern: "MaterialRegistry|disposeMaterials"
---

<objective>
Wave 6: wire-up + verification. Wpięcie `pressModel.disposeMaterials()` do `Application.dispose()` (TWIN-11 SC5 HMR cycle), nowy smoke test pokrywający TWIN-11/12/13 contract enforcement (cloned materials identity, getInteractables/getMeshDictionary contract, userData identity-only, dispose path), oraz update `boundaries.test.js` o entry dla nowego `src/MaterialRegistry.js`.

Po tym planie Phase 2 jest **funkcjonalnie ukończona**: wszystkie 15 meshów obecne, wszystkie 13 wymagań TWIN-XX zaimplementowane i przetestowane, dispose path działa.

Atomic commit: "test(02): smoke tests + dispose wire + boundaries (TWIN-11/12/13 enforcement)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-digital-twin-geometry/02-RESEARCH.md
@.planning/phases/02-digital-twin-geometry/02-05-SUMMARY.md
@src/main.js
@src/PressModel.js
@src/MaterialRegistry.js
@tests/boundaries.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wpięcie disposeMaterials w Application.dispose (src/main.js)</name>
  <files>src/main.js</files>
  <action>
W metodzie `Application.dispose()` w `src/main.js` (linie 59-65) dodaj wywołanie `this.pressModel.disposeMaterials()` PRZED `this.sceneSetup.dispose()`.

Final stan dispose():
```js
dispose() {
  gsap.ticker.remove(this._tickerCallback);
  for (const unsub of this._unsubscribers) unsub();
  this._unsubscribers = [];
  if (this.disclaimerBanner) this.disclaimerBanner.dispose();
  this.pressModel.disposeMaterials();  // TWIN-11 SC5 — release GPU buffers (materials + textures) na HMR
  this.sceneSetup.dispose();
}
```

Komentarz inline: `// TWIN-11 SC5 — release GPU buffers (materials + textures) na HMR`.

NIE TYKAJ żadnego innego kodu w main.js.
  </action>
  <verify>
    <automated>npm test</automated>
    Manual: `npm run dev`, edytuj plik PressModel.js (np. dodaj komentarz), zapisz → Vite HMR; sprawdź konsolę przeglądarki — brak warnings o leaking materials.
  </verify>
  <done>
    Application.dispose() wywołuje pressModel.disposeMaterials() przed sceneSetup.dispose(). Manual HMR test passes (brak warnings).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke test tests/PressModel.smoke.test.js (TWIN-11/12/13)</name>
  <files>tests/PressModel.smoke.test.js</files>
  <behavior>
    Test cases (RESEARCH §"Vitest jsdom smoke test patterns"):
    1. TWIN-12: getInteractables() ma 15 wpisów z poprawnymi ID (lista 15 polskich kebab-case IDs zgodna z CONTEXT specifics).
    2. TWIN-12: getMeshDictionary() ma 15 wpisów z {labelPL, descriptionPL, kind} — wszystkie non-empty, kind ∈ {'manipulation','visual-target'}.
    3. TWIN-13: userData kontrakt identity-only — id matches key, kind enum, labelPL/descriptionPL non-empty, restPosition plain object {pos:{x,y,z}, rot:{x,y,z}}, NO state/isOpen/value/status/currentPose/isHighlighted.
    4. TWIN-11 SC3: cloned materials — `flywheel.material !== eStop.material` (CRIT-6 enforcement).
    5. TWIN-11 paranoid: żadne dwa interactable nie współdzielą material reference (105-pair test for 15 meshes — 15*14/2=105).
    6. TWIN-11 SC5 dispose: disposeMaterials() woła dispose() na każdym sklonowanym MeshStandardMaterial (≥14 spy calls — 15 meshów minus tabliczka która ma MeshBasicMaterial; spy.toHaveBeenCalledTimes(1) per spy).
    7. TWIN-10/11: tabliczka znamionowa CanvasTexture dispose path — texture.dispose() wołany przez disposeMaterials().
    8. TWIN-11 SC5 HMR cycle: rebuild po dispose — registry.size() === 15, NIE 30 (idempotent).
    9. UI-SPEC negative criteria: lampka-gotowosci.material.emissiveIntensity === 0 (Phase 4 territory check).
    10. Pivot-group readiness: oslona-przednia / wylacznik-glowny / dzwignia-sprzegla mają userData.poses jako plain object z keys per UI-SPEC tabela.
  </behavior>
  <action>
Utwórz `tests/PressModel.smoke.test.js` z dyrektywą `// @vitest-environment jsdom` w pierwszej linii (RESEARCH §"Brownfield integration"). Importy: vitest, three, PressModel.

Pełny test file (use this exact structure):

```js
// @vitest-environment jsdom
// Phase 2 smoke: TWIN-11/12/13 contract enforcement.
// Bez WebGLRenderer (PITFALLS MOD-6); pure scene graph + identity assertions.

import { describe, it, expect, vi, beforeEach } from 'vitest';
import * as THREE from 'three';
import { PressModel } from '../src/PressModel.js';

const EXPECTED_IDS = [
  'kolo-zamachowe', 'dzwignia-sprzegla', 'hamulec', 'wziernik-smarowania',
  'oslona-przednia', 'oslona-tylna', 'kurtyna-lewa', 'kurtyna-prawa',
  'panel-oburezny', 'przycisk-start-lewy', 'przycisk-start-prawy',
  'lampka-gotowosci', 'estop', 'wylacznik-glowny', 'tabliczka-znamionowa',
];

const MANIPULATION_IDS = new Set([
  'dzwignia-sprzegla', 'hamulec', 'oslona-przednia', 'przycisk-start-lewy',
  'przycisk-start-prawy', 'estop', 'wylacznik-glowny',
]);

const VISUAL_TARGET_IDS = new Set([
  'kolo-zamachowe', 'wziernik-smarowania', 'oslona-tylna', 'kurtyna-lewa',
  'kurtyna-prawa', 'panel-oburezny', 'lampka-gotowosci', 'tabliczka-znamionowa',
]);

const POSES_IDS = new Set(['oslona-przednia', 'wylacznik-glowny', 'dzwignia-sprzegla']);

describe('PressModel — Phase 2 smoke (TWIN-11/12/13)', () => {
  let scene;
  let pressModel;

  beforeEach(() => {
    scene = new THREE.Scene();
    pressModel = new PressModel(scene);
  });

  it('TWIN-12: getInteractables() zwraca 15 wpisów z poprawnymi ID', () => {
    const interactables = pressModel.getInteractables();
    expect(interactables.size).toBe(15);
    for (const id of EXPECTED_IDS) {
      expect(interactables.has(id), `missing mesh: ${id}`).toBe(true);
    }
  });

  it('TWIN-12: getMeshDictionary() ma 15 wpisów z {labelPL, descriptionPL, kind}', () => {
    const dict = pressModel.getMeshDictionary();
    expect(dict.size).toBe(15);
    for (const [id, entry] of dict) {
      expect(entry.labelPL, `${id} labelPL empty`).toBeTruthy();
      expect(entry.descriptionPL, `${id} descriptionPL empty`).toBeTruthy();
      expect(['manipulation', 'visual-target']).toContain(entry.kind);
    }
  });

  it('TWIN-12: stable references — getInteractables() zwraca to samo Map przy wielokrotnych wywołaniach', () => {
    const a = pressModel.getInteractables();
    const b = pressModel.getInteractables();
    expect(a).toBe(b);
    const da = pressModel.getMeshDictionary();
    const db = pressModel.getMeshDictionary();
    expect(da).toBe(db);
  });

  it('TWIN-13: userData kontrakt identity-only (CRIT-7)', () => {
    for (const [id, mesh] of pressModel.getInteractables()) {
      // identity fields
      expect(mesh.userData.id).toBe(id);
      expect(['manipulation', 'visual-target']).toContain(mesh.userData.kind);
      expect(mesh.userData.labelPL).toBeTruthy();
      expect(mesh.userData.descriptionPL).toBeTruthy();

      // kind correctness per D-Phase2-09
      if (MANIPULATION_IDS.has(id)) expect(mesh.userData.kind).toBe('manipulation');
      if (VISUAL_TARGET_IDS.has(id)) expect(mesh.userData.kind).toBe('visual-target');

      // restPosition plain object format (CONTEXT specifics)
      expect(mesh.userData.restPosition).toBeDefined();
      expect(mesh.userData.restPosition.pos).toEqual(
        expect.objectContaining({ x: expect.any(Number), y: expect.any(Number), z: expect.any(Number) })
      );
      expect(mesh.userData.restPosition.rot).toEqual(
        expect.objectContaining({ x: expect.any(Number), y: expect.any(Number), z: expect.any(Number) })
      );

      // CRIT-7: NO live status keys (paranoid enumerate)
      for (const forbidden of ['state', 'isOpen', 'value', 'status', 'currentPose', 'isHighlighted']) {
        expect(mesh.userData, `${id} userData has forbidden key '${forbidden}'`).not.toHaveProperty(forbidden);
      }
    }
  });

  it('TWIN-13: ruchome interactable mają userData.poses jako plain object definicję', () => {
    const interactables = pressModel.getInteractables();
    for (const id of POSES_IDS) {
      const mesh = interactables.get(id);
      expect(mesh.userData.poses, `${id} brak poses`).toBeDefined();
      // każdy pose ma rot.x/y/z (plain numbers)
      for (const poseName of Object.keys(mesh.userData.poses)) {
        const pose = mesh.userData.poses[poseName];
        expect(pose.rot).toEqual(
          expect.objectContaining({ x: expect.any(Number), y: expect.any(Number), z: expect.any(Number) })
        );
      }
    }
    // Specific pose names per UI-SPEC §Default Pose
    expect(Object.keys(interactables.get('oslona-przednia').userData.poses).sort()).toEqual(['closed', 'open']);
    expect(Object.keys(interactables.get('wylacznik-glowny').userData.poses).sort()).toEqual(['off', 'on']);
    expect(Object.keys(interactables.get('dzwignia-sprzegla').userData.poses).sort()).toEqual(['engaged', 'released']);
  });

  it('TWIN-11 SC3: cloned materials — flywheel.material !== estop.material', () => {
    const flywheel = pressModel.getInteractables().get('kolo-zamachowe');
    const eStop = pressModel.getInteractables().get('estop');
    expect(flywheel.material).not.toBe(eStop.material);
  });

  it('TWIN-11 paranoid: żadne dwa interactable nie współdzielą material reference (105-par)', () => {
    const meshes = [...pressModel.getInteractables().values()];
    expect(meshes.length).toBe(15);
    for (let i = 0; i < meshes.length; i++) {
      for (let j = i + 1; j < meshes.length; j++) {
        expect(
          meshes[i].material,
          `${meshes[i].userData.id} ↔ ${meshes[j].userData.id} share material reference`
        ).not.toBe(meshes[j].material);
      }
    }
  });

  it('TWIN-11 SC5: disposeMaterials() woła dispose() na każdym sklonowanym materiale', () => {
    const materials = [...pressModel.getInteractables().values()].map(m => m.material);
    const spies = materials.map(m => vi.spyOn(m, 'dispose'));

    pressModel.disposeMaterials();

    for (const spy of spies) expect(spy).toHaveBeenCalledTimes(1);
    expect(pressModel.materialRegistry.size()).toBe(0);
  });

  it('TWIN-10/11: tabliczka znamionowa CanvasTexture dispose path', () => {
    const nameplate = pressModel.getInteractables().get('tabliczka-znamionowa');
    expect(nameplate.material).toBeInstanceOf(THREE.MeshBasicMaterial);
    const texture = nameplate.material.map;
    expect(texture).toBeDefined();
    expect(texture.colorSpace).toBe(THREE.SRGBColorSpace);

    const texSpy = vi.spyOn(texture, 'dispose');
    pressModel.disposeMaterials();
    expect(texSpy).toHaveBeenCalledTimes(1);
  });

  it('TWIN-11 SC5: HMR cycle — rebuild po dispose nie rośnie material count', () => {
    pressModel.disposeMaterials();
    const newPress = new PressModel(scene);
    expect(newPress.getInteractables().size).toBe(15);
    expect(newPress.materialRegistry.size()).toBe(15);
  });

  it('UI-SPEC negative criteria: lampka-gotowosci nie świeci w Phase 2 (Phase 4 territory)', () => {
    const lamp = pressModel.getInteractables().get('lampka-gotowosci');
    expect(lamp.material.emissiveIntensity ?? 0).toBe(0);
    // emissive #000000 → getHex() === 0
    expect(lamp.material.emissive.getHex()).toBe(0x000000);
  });

  it('CRIT-7: Phase 1 brownfield meshes (shaft/eccentric/rod/slider) zostały niezmienione (nie są w interactables)', () => {
    const interactables = pressModel.getInteractables();
    // Phase 1 meshes NIE powinny być rejestrowane (są non-interactable structural)
    expect(interactables.has('shaft')).toBe(false);
    expect(interactables.has('eccentric')).toBe(false);
    expect(interactables.has('rod')).toBe(false);
    expect(interactables.has('slider')).toBe(false);
  });
});
```

**WAŻNE techniczne**:
- Plik MUSI mieć `// @vitest-environment jsdom` w PIERWSZEJ linii — `_buildNameplate()` woła `document.createElement('canvas')` co wymaga DOM.
- `MANIPULATION_IDS` + `VISUAL_TARGET_IDS` per D-Phase2-09 mapping (CONTEXT decisions).
- Test "ruchome poses" sprawdza dokładnie 3 nazwy poses (closed/open, off/on, engaged/released) — UI-SPEC §Default Pose tabela jako kontrakt.
- Test "shared material" sprawdza wszystkie 105 par — to "paranoid" assertion z RESEARCH §"CRIT-6 Enforcement".
- Test HMR cycle używa nowego `scene` (jsdom-safe — brak WebGL).

**NIE testujemy** (świadomie out-of-scope, RESEARCH §"Co NIE testować"):
- WebGL render (jsdom nie obsługuje, MOD-6).
- Visual fidelity ("czy E-stop wygląda jak grzybek") — manual QA per UI-SPEC.
- `renderer.info.memory.materials` count — proxy przez `registry.size()`.
  </action>
  <verify>
    <automated>npm test -- tests/PressModel.smoke.test.js</automated>
    Wszystkie 11 testów (10 nowych + 1 brownfield-protection) zielonych.

    Pełny suite: `npm test` — 133 (Phase 1) + ≥11 (smoke) testów zielonych.
  </verify>
  <done>
    `tests/PressModel.smoke.test.js` istnieje, ma `@vitest-environment jsdom` w pierwszej linii, zawiera ≥11 asercji TWIN-11/12/13. `npm test` zielony. Sumarycznie testów po Phase 2: ≥144 (133 brownfield + 11 nowych).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update tests/boundaries.test.js o entry dla MaterialRegistry</name>
  <files>tests/boundaries.test.js</files>
  <action>
W tablicy `FORBIDDEN_PAIRS` (linie 24-42) dodaj NOWY entry dla `src/MaterialRegistry.js`. Wstaw bezpośrednio po entry dla `PressModel.js` (linia 31), żeby zachować logiczną kolejność (scene resources together):

```js
  // MaterialRegistry — scene resources layer; nie wolno importować store/training
  { file: 'src/MaterialRegistry.js', mustNotImport: ['../state/', '../training/', './state/', './training/'] },
```

NIE TYKAJ żadnych innych entries. Zachowaj istniejące pary dla ProcedureEngine/PressModel/PhysicsEngine/etc.

UWAGA: MaterialRegistry NIE używa THREE wprost (woła tylko `.dispose()` jako method na przekazanych obiektach), ale **dozwolone jest dla niego importowanie `three`** w przyszłości jeśli zajdzie potrzeba (np. THREE.SRGBColorSpace constants). Dlatego `mustNotImport` NIE zawiera `'three'` ani `'gsap'`. To jest zgodne z RESEARCH §"Brownfield integration": "MaterialRegistry to layer 'scene resources' jak SceneSetup".
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
    - Test "src/MaterialRegistry.js must not import ../state/, ../training/, ./state/, ./training/" — zielony (MaterialRegistry nie importuje żadnego z tych path'ów).
    - Wszystkie pozostałe boundaries entries — zielone (no regression).
    - Polish-literal scanner — zielony.
  </verify>
  <done>
    boundaries.test.js ma entry dla MaterialRegistry. `npm test` zielony cały suite.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | DoS (HMR leak) | Brak disposeMaterials wpięcia → leak GPU buffers przy hot reload | mitigate | Task 1 wpina; Task 2 smoke test SC5 wymusza idempotency. |
| T-02-16 | Tampering (test cheat) | Smoke test pasuje przy brakujących meshach (size assertion za luźna) | mitigate | EXPECTED_IDS lista dokładnie 15 elementów; `for...of` iteracja sprawdza KAŻDE id z osobna; paranoid 105-pair test wymusza unique materials. |
| T-02-17 | Boundary violation | MaterialRegistry przypadkowo importuje store przez transitive dep | mitigate | Task 3 dodaje boundary entry; CI failuje przy violation. |
</threat_model>

<verification>
1. `npm test` — wszystkie testy zielone (≥144).
2. `npm run dev` — strona renderuje się; manual edycja PressModel.js → HMR bez warnings o leakach.
3. `npm run build` — production build przechodzi.
4. Code review checklist (RESEARCH §"Code review checklist") — wszystkie 11 punktów ✓.
</verification>

<success_criteria>
- ROADMAP Phase 2 SC1-5 wszystkie spełnione i przetestowane.
- TWIN-11 enforcement: cloned materials weryfikowane paranoid 105-pair testem.
- TWIN-12 enforcement: getInteractables/getMeshDictionary mają 15 wpisów z stable references.
- TWIN-13 enforcement: userData identity-only (NO state/isOpen/value/status/currentPose/isHighlighted).
- Boundary scanner ma entry dla MaterialRegistry.
- Phase 2 funkcjonalnie ukończona — gotowa do Phase 3.
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-digital-twin-geometry/02-06-SUMMARY.md` z:
- Final liczba testów (133 + 11 = 144 minimum).
- Lookup table PIVOT_TARGET dla Phase 3 animator (oslona-przednia/dzwignia-sprzegla → 'parent', wylacznik-glowny → 'self').
- Code review checklist 11/11 ✓.
- Gotowość do Phase 3 (Click-to-State Pipeline): wszystkie interactable z userData kontraktem.

Atomic commit:
```
test(02): smoke tests + dispose wire + boundaries (TWIN-11/12/13 enforcement)

- src/main.js: Application.dispose() woła pressModel.disposeMaterials() (TWIN-11 SC5).
- tests/PressModel.smoke.test.js: 11 asercji TWIN-11/12/13:
  * size=15, IDs, dictionary contract, stable references
  * userData identity-only (paranoid 6-key forbidden enumeration)
  * userData.poses dla 3 ruchomych z dokładnymi nazwami pose'ów
  * cloned materials (paranoid 105-par)
  * dispose spy + HMR cycle idempotency
  * tabliczka CanvasTexture dispose path
  * UI-SPEC negative: lampka emissiveIntensity=0 (Phase 4 territory)
  * brownfield protection: shaft/eccentric/rod/slider NIE w interactables.
- tests/boundaries.test.js: entry dla src/MaterialRegistry.js (no store/training import).

Phase 2 funkcjonalnie ukończona. Gotowa do Phase 3.
```
</output>
