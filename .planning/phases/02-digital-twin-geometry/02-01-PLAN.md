---
phase: 02-digital-twin-geometry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MaterialRegistry.js
  - src/i18n/pl.js
  - src/PressModel.js
  - tests/MaterialRegistry.smoke.test.js
autonomous: true
requirements: [TWIN-11, TWIN-12, TWIN-13]
must_haves:
  truths:
    - "Klasa MaterialRegistry istnieje z metodami getCloned/trackTexture/disposeAll/size"
    - "src/i18n/pl.js eksportuje pl.parts z 15 wpisami { label, description } (kebab-case polskie ID jako klucze)"
    - "PressModel ma w konstruktorze this.materialRegistry, this._interactables (Map), this._meshDictionary (Map)"
    - "PressModel.getInteractables() i getMeshDictionary() zwracają stable references do tych samych Map"
    - "PressModel ma metodę _registerInteractable({mesh, id, kind, baseMaterial, poses?, pivotTarget?}) — jedyne miejsce mutujące Mapy + userData interactable meshów"
    - "_registerInteractable obsługuje baseMaterial===null path (pomija getCloned, mesh już ma własny material — używane przez tabliczka-znamionowa MeshBasicMaterial w 02-03)"
    - "_registerInteractable akceptuje opcjonalny pivotTarget ∈ {'self', 'parent'} i zapisuje do mesh.userData.pivotTarget — kontrakt dla Phase 3 animator (HIGH-1)"
    - "_registerInteractable akceptuje opcjonalny poses (plain object) i zapisuje do mesh.userData.poses jako definicję (CRIT-7 — definicja, NIE aktywny pose name)"
    - "Allowed userData key set: {id, kind, restPosition, labelPL, descriptionPL, poses?, pivotTarget?} — żaden inny klucz nie powinien się pojawić"
    - "PressModel.disposeMaterials() deleguje do materialRegistry.disposeAll()"
    - "buildMaterials() ma wszystkie 12 nowych base materials z UI-SPEC §Color (matFlywheel, matBrakeSteel, matNameplateSilver, matLightCurtainBlack, matEStopRed, matSafetyButtonGreen, matReadyLamp, matGuardOrange, matGuardRearBlack, matOilSightYellow, matSafetyPanelGray, matSwitchBody)"
  artifacts:
    - path: "src/MaterialRegistry.js"
      provides: "Centralny rejestr sklonowanych materiałów + textures z disposeAll"
      contains: "export class MaterialRegistry"
    - path: "src/i18n/pl.js"
      provides: "Sekcja pl.parts z 15 wpisami { label, description }"
      contains: "parts:"
    - path: "src/PressModel.js"
      provides: "Scaffolding: constructor + _registerInteractable + getInteractables + getMeshDictionary + disposeMaterials + rozszerzony buildMaterials"
      contains: "_registerInteractable"
    - path: "tests/MaterialRegistry.smoke.test.js"
      provides: "Smoke test getCloned + disposeAll + null baseMaterial path w _registerInteractable"
      contains: "MaterialRegistry"
  key_links:
    - from: "src/PressModel.js"
      to: "src/MaterialRegistry.js"
      via: "import { MaterialRegistry } from './MaterialRegistry.js'"
      pattern: "import.*MaterialRegistry"
    - from: "src/PressModel.js"
      to: "src/i18n/pl.js"
      via: "import { pl } from './i18n/pl.js' i resolve labelPL/descriptionPL z pl.parts[id]"
      pattern: "pl\\.parts\\["
---

<objective>
Wave 1 fundament Phase 2: tworzy MaterialRegistry, rozszerza pl.js o sekcję `pl.parts` z 15 wpisami (kontrakt UI-SPEC §Copywriting), oraz dodaje do PressModel.js scaffolding rejestru interactable: konstruktor (Mapy + registry instance), centralną metodę `_registerInteractable()` egzekwującą CRIT-6 (cloned material) + CRIT-7 (userData identity-only) + obsługującą `pivotTarget` semantyką Phase 3 animatora (HIGH-1) + null baseMaterial dla CanvasTexture path (MEDIUM-5), publiczne accessory `getInteractables()` / `getMeshDictionary()`, dispose path `disposeMaterials()`, oraz rozszerzony `buildMaterials()` o 12 nowych base materials z palety UI-SPEC. Dodatkowo: smoke test `tests/MaterialRegistry.smoke.test.js` weryfikujący kontrakt registry + null-baseMaterial path. **Zero nowych meshów w tym planie** — to fundament, na którym Wave 2-5 buduje 15 meshów.

Purpose: bez tego fundamentu kolejne plany nie mają gdzie rejestrować interactable ani gdzie klonować materials. Atomic commit: "feat(02): MaterialRegistry + pl.parts + interactable scaffolding (TWIN-11/12/13 base, D-Phase2-07/08)".
Output: 2 nowe pliki (MaterialRegistry.js + smoke test), 2 zmodyfikowane pliki (i18n/pl.js + PressModel.js), zero behavior change w renderze (PressModel buildPress() nadal buduje tylko Phase 1 meshe — frame/shaft/eccentric/rod/slider).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-digital-twin-geometry/02-CONTEXT.md
@.planning/phases/02-digital-twin-geometry/02-RESEARCH.md
@.planning/phases/02-digital-twin-geometry/02-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/PressModel.js
@src/i18n/pl.js

<interfaces>
<!-- Z research §"MaterialRegistry — rekomendowana implementacja" — interfejs wymagany przez Wave 2-5 -->

```javascript
// src/MaterialRegistry.js — public surface
export class MaterialRegistry {
  constructor()
  /** @returns {THREE.Material} clone of baseMaterial, idempotent per meshId */
  getCloned(baseMaterial, meshId)
  /** Track texture for dispose (CanvasTexture, Wave 3 tabliczka-znamionowa) */
  trackTexture(meshId, texture)
  /** Dispose all materials + textures, clear maps. Wired in main.js w 02-06. */
  disposeAll()
  /** Diagnostic — used by smoke test (registry.size() === 15 after build) */
  size()
}
```

```javascript
// src/PressModel.js — public surface ADDED w tym planie
class PressModel {
  // istniejące: constructor(scene), buildMaterials(), buildPress(), update(angle)
  // NOWE w tym planie:
  /** @returns {Map<string, THREE.Mesh>} stable reference, immutable po buildPress() */
  getInteractables()
  /** @returns {Map<string, {labelPL: string, descriptionPL: string, kind: string}>} */
  getMeshDictionary()
  /** Wire'd przez main.js w 02-06; deleguje do this.materialRegistry.disposeAll() */
  disposeMaterials()
  // PRYWATNE:
  /** Jedyne miejsce mutujące this._interactables/this._meshDictionary + ustawiające userData.
   *  Sygnatura:
   *    _registerInteractable({ mesh, id, kind, baseMaterial, poses?, pivotTarget? })
   *  - baseMaterial===null → pomija getCloned (CanvasTexture path, mesh już ma material)
   *  - poses?: plain object — definicja (CRIT-7), zapisana do userData.poses
   *  - pivotTarget?: 'self' | 'parent' — kontrakt dla Phase 3 animator (HIGH-1) */
  _registerInteractable({ mesh, id, kind, baseMaterial, poses, pivotTarget })
}
```

```javascript
// src/i18n/pl.js — public surface ADDED
export const pl = {
  // ... istniejące disclaimer/webgl/ui/physics/machineState/errors
  parts: {
    'kolo-zamachowe':       { label: 'Koło zamachowe', description: '...' },
    // ... 14 więcej, klucze = mesh ID kebab-case polskie
  }
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utwórz src/MaterialRegistry.js + smoke test (D-Phase2-07)</name>
  <files>src/MaterialRegistry.js, tests/MaterialRegistry.smoke.test.js</files>
  <behavior>
    - Test: `new MaterialRegistry()` ma `size() === 0` po construct.
    - Test: `getCloned(baseMat, 'foo')` zwraca instancję ≠ baseMat (clone). `getCloned(baseMat, 'foo')` drugi raz zwraca tę samą instancję (idempotent — cached per meshId). `getCloned(baseMat, 'bar')` zwraca trzecią instancję (różną od 'foo' clone).
    - Test: po dwóch różnych `meshId` → `size() === 2`.
    - Test: `trackTexture('foo', fakeTexture)` rejestruje texture; po `disposeAll()` `fakeTexture.dispose` został wywołany 1×.
    - Test: `disposeAll()` woła `.dispose()` na wszystkich sklonowanych materiałach + texturach, czyści Mapy (`size() === 0` po).
    - Fake material/texture: plain object `{ dispose: vi.fn(), clone() { return { dispose: vi.fn() }; } }` — bez Three.js zależności.
  </behavior>
  <action>
**Plik 1: `src/MaterialRegistry.js`** — utwórz nowy plik zgodnie z research §"MaterialRegistry — rekomendowana implementacja".

Wymagania:
- Klasa `MaterialRegistry` z konstruktorem inicjalizującym `this._materials = new Map()` i `this._textures = new Map()`.
- Metoda `getCloned(baseMaterial, meshId)`: idempotentna — pierwsza prośba klonuje base przez `baseMaterial.clone()` i zapisuje pod `meshId`; kolejne wołania z tym samym `meshId` zwracają zacachowany clone (NIE klonują ponownie).
- Metoda `trackTexture(meshId, texture)`: zapisuje texture w mapie `_textures` pod kluczem `meshId`. Używana w Wave 3 dla CanvasTexture tabliczki znamionowej.
- Metoda `disposeAll()`: iteruje `_materials` i `_textures`, woła `.dispose()` na każdym, czyści obie Mapy.
- Metoda `size()`: zwraca `this._materials.size` — używana w smoke test (Wave 5).
- JSDoc po polsku per CONVENTIONS.
- Brak zewnętrznych importów (czysty ES module).
- Komentarz nagłówka: "CRIT-6 invariant: per-mesh cloned MeshStandardMaterial. Każdy interactable dostaje swój clone — bez tego HighlightManager (Phase 4) zaświeciłby wszystko naraz. Dispose path zapobiega leakowi GPU buffers przy Vite HMR (TWIN-11 SC5)."

**Plik 2: `tests/MaterialRegistry.smoke.test.js`** — utwórz nowy smoke test (MEDIUM-4 fix: realny check zamiast bezsensownego boundaries-test).

Test cases (vitest, NIE wymaga jsdom — pure-JS):
```js
import { describe, it, expect, vi } from 'vitest';
import { MaterialRegistry } from '../src/MaterialRegistry.js';

function makeFakeMaterial() {
  // Każdy clone() zwraca nowy obiekt z własnym dispose spy
  return {
    dispose: vi.fn(),
    clone() { return { dispose: vi.fn(), _isClone: true }; },
  };
}

function makeFakeTexture() {
  return { dispose: vi.fn() };
}

describe('MaterialRegistry — smoke (TWIN-11)', () => {
  it('konstruktor: size() === 0', () => {
    const r = new MaterialRegistry();
    expect(r.size()).toBe(0);
  });

  it('getCloned: zwraca clone ≠ base, idempotent per meshId', () => {
    const r = new MaterialRegistry();
    const base = makeFakeMaterial();
    const c1 = r.getCloned(base, 'foo');
    expect(c1).not.toBe(base);
    expect(c1._isClone).toBe(true);
    const c2 = r.getCloned(base, 'foo');
    expect(c2).toBe(c1); // cached
    const c3 = r.getCloned(base, 'bar');
    expect(c3).not.toBe(c1);
    expect(r.size()).toBe(2);
  });

  it('disposeAll: woła dispose na materiałach + texturach, czyści mapy', () => {
    const r = new MaterialRegistry();
    const base = makeFakeMaterial();
    const c1 = r.getCloned(base, 'foo');
    const c2 = r.getCloned(base, 'bar');
    const tex = makeFakeTexture();
    r.trackTexture('foo', tex);

    r.disposeAll();

    expect(c1.dispose).toHaveBeenCalledTimes(1);
    expect(c2.dispose).toHaveBeenCalledTimes(1);
    expect(tex.dispose).toHaveBeenCalledTimes(1);
    expect(r.size()).toBe(0);
  });
});
```
  </action>
  <verify>
    <automated>npm test -- tests/MaterialRegistry.smoke.test.js</automated>
    Wszystkie 3 testy zielone. Pełny suite `npm test` — Phase 1 (133) + 3 nowe = ≥136 zielonych.
  </verify>
  <done>
    Plik `src/MaterialRegistry.js` istnieje, eksportuje `MaterialRegistry` class z metodami getCloned/trackTexture/disposeAll/size. Smoke test `tests/MaterialRegistry.smoke.test.js` zielony (3 testy). Pełny suite zielony.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rozszerz src/i18n/pl.js o sekcję pl.parts (D-Phase2-08, UI-SPEC §Copywriting Contract)</name>
  <files>src/i18n/pl.js</files>
  <action>
Po sekcji `errors:` w `pl` object dodaj sekcję `parts:` z 15 wpisami. Klucze = mesh ID kebab-case polskie (per CONTEXT specifics §"Lista finalnych id"). Każdy wpis: `{ label: string, description: string }`. Treść EXACT z UI-SPEC §"Copywriting Contract — pl.parts.* (Polish Part Names)" tabela — 15 wierszy. NIE zmieniaj treści.

Klucze (kolejność per UI-SPEC tabela):
1. `kolo-zamachowe`
2. `dzwignia-sprzegla`
3. `hamulec`
4. `wziernik-smarowania`
5. `oslona-przednia`
6. `oslona-tylna`
7. `kurtyna-lewa`
8. `kurtyna-prawa`
9. `panel-oburezny`
10. `przycisk-start-lewy`
11. `przycisk-start-prawy`
12. `lampka-gotowosci`
13. `estop`
14. `wylacznik-glowny`
15. `tabliczka-znamionowa`

Format (przykład pierwszego wpisu — pełna treść per UI-SPEC):
```js
parts: {
  'kolo-zamachowe': {
    label: 'Koło zamachowe',
    description: 'Magazynuje energię obrotową napędu i oddaje ją w momencie sprzęgnięcia, dostarczając moment do uderzenia suwakiem.',
  },
  // ... 14 więcej dokładnie wg UI-SPEC tabela
},
```

Po dodaniu przeczytaj UI-SPEC §"Copywriting Contract" i przepisz każdą `description` znak-po-znaku — żaden re-wording. Te stringi są kontraktem.
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
    Test §"Polish string literal scanner (UI-06, MOD-3)" musi być zielony — `pl.js` jest na ALLOWED_PATHS (`src/i18n/`), więc polskie literały są dozwolone.
  </verify>
  <done>
    `src/i18n/pl.js` ma `parts:` sekcję z 15 wpisami. Każdy `label` jest niepusty Polish string. Każdy `description` ma 80-160 znaków per UI-SPEC. Klucze są dokładnie 15 mesh ID z CONTEXT specifics. `npm test` zielony.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffolding w src/PressModel.js — registry + accessory + _registerInteractable (z pivotTarget + null guard) + extended buildMaterials</name>
  <files>src/PressModel.js</files>
  <behavior>
    - Test (Wave 5 02-06 dopisze): `new PressModel(scene)` nie throwuje + `getInteractables() instanceof Map` + `getInteractables().size === 0` przed wywołaniem buildPress() rozszerzonego.
    - Test (Wave 5): `pressModel.materialRegistry instanceof MaterialRegistry`.
    - Test (Wave 5): `disposeMaterials()` woła `materialRegistry.disposeAll()`.
    - **Test inline (ten plan, Task 3 dodatkowy unit)**: wywołanie `_registerInteractable({mesh: fakeMesh, id: 'test', kind: 'visual-target', baseMaterial: null})` na fake meshu z istniejącym `material` NIE woła `getCloned` i pozostawia `mesh.material` niezmienione. (MEDIUM-5 explicit guard verification.)
  </behavior>
  <action>
1. **Import**: dodaj na górze pliku:
   ```js
   import { pl } from './i18n/pl.js';
   import { MaterialRegistry } from './MaterialRegistry.js';
   ```

2. **Konstruktor** — po `this.scene = scene;` (przed `this.r = 0.8`) dodaj:
   ```js
   this.materialRegistry = new MaterialRegistry();
   /** @type {Map<string, THREE.Mesh>} stable reference dla Phase 3 RaycastController */
   this._interactables = new Map();
   /** @type {Map<string, {labelPL: string, descriptionPL: string, kind: string}>} */
   this._meshDictionary = new Map();
   ```

3. **buildMaterials()** — po istniejących 6 base materials dodaj 12 nowych (kolejność per UI-SPEC §Color tabele 60/30/10):

   **60% Dominant** (poza istniejącymi matBody/matBase):
   - `this.matSafetyPanelGray = new THREE.MeshStandardMaterial({ color: 0x6b6b6b, metalness: 0.2, roughness: 0.6 })`
   - `this.matSwitchBody = new THREE.MeshStandardMaterial({ color: 0x404040, metalness: 0.3, roughness: 0.5 })`

   **30% Secondary** (poza istniejącym matShaft):
   - `this.matFlywheel = new THREE.MeshStandardMaterial({ color: 0x7a7a7a, metalness: 0.85, roughness: 0.35 })`
   - `this.matBrakeSteel = new THREE.MeshStandardMaterial({ color: 0x5a5a5a, metalness: 0.7, roughness: 0.45 })`
   - `this.matNameplateSilver = new THREE.MeshStandardMaterial({ color: 0xc8c8c8, metalness: 0.6, roughness: 0.4 })`
   - `this.matLightCurtainBlack = new THREE.MeshStandardMaterial({ color: 0x1a1a1a, metalness: 0.4, roughness: 0.5 })`

   **10% Accent** (paleta Wong):
   - `this.matEStopRed = new THREE.MeshStandardMaterial({ color: 0xD55E00, metalness: 0.1, roughness: 0.55 })`
   - `this.matSafetyButtonGreen = new THREE.MeshStandardMaterial({ color: 0x009E73, metalness: 0.1, roughness: 0.55 })`
   - `this.matReadyLamp = new THREE.MeshStandardMaterial({ color: 0x009E73, metalness: 0.0, roughness: 0.3 })`
   - `this.matGuardOrange = new THREE.MeshStandardMaterial({ color: 0xE07A1F, metalness: 0.05, roughness: 0.7 })`
   - `this.matGuardRearBlack = new THREE.MeshStandardMaterial({ color: 0x2a2a2a, metalness: 0.0, roughness: 0.8 })`
   - `this.matOilSightYellow = new THREE.MeshStandardMaterial({ color: 0xd4a017, metalness: 0.1, roughness: 0.4 })`

   **NIE TYKAJ** istniejących `matBody`, `matShaft`, `matEccentric`, `matRod`, `matSlider`, `matBase`.

4. **Nowa metoda `_registerInteractable({ mesh, id, kind, baseMaterial, poses = null, pivotTarget = null })`** — wstaw PRZED `update(angle)` (po `buildPress()`). Komentarz nagłówka EXACTLY:

   ```js
   // === CRIT-6 + CRIT-7 INVARIANT (Phase 1 lock-in, Phase 2 enforcement) ===
   // Jedyne miejsce w PressModel mutujące this._interactables / this._meshDictionary
   // i ustawiające userData interactable mesh'a.
   //
   // CRIT-6: każdy interactable dostaje sklonowany MeshStandardMaterial (lub Basic dla
   //         tabliczki) przez MaterialRegistry — bez tego highlight w Phase 4 zaświeci wszystko.
   // CRIT-7: userData = TYLKO tożsamość. NIGDY status. Pose'y to definicja (poses dict, identity).
   //         Active pose name żyje w state.meshStates[id].pose w store (Phase 3+), NIE tutaj.
   //
   // Allowed userData keys: { id, kind, restPosition, labelPL, descriptionPL, poses?, pivotTarget? }
   // Forbidden userData keys: state, isOpen, value, status, currentPose, isHighlighted.
   //
   // pivotTarget (HIGH-1, kontrakt dla Phase 3 animator):
   //   'self'   → Phase 3 rotuje sam mesh (np. wylacznik-glowny — pokrętło rotuje wokół własnej centerline).
   //   'parent' → Phase 3 rotuje mesh.parent (np. oslona-przednia, dzwignia-sprzegla — pivot-grupy).
   //   null/undefined → mesh nie ma poses (statyczny lub visual-target).
   //
   // baseMaterial===null path: dla CanvasTexture (tabliczka-znamionowa) — mesh już ma swój
   //   MeshBasicMaterial przed wywołaniem; pomijamy getCloned (mesh.material zostaje untouched).
   //
   // Mapowanie kind (D-Phase2-09):
   //   manipulation: dzwignia-sprzegla, hamulec, oslona-przednia, przycisk-start-lewy,
   //                 przycisk-start-prawy, estop, wylacznik-glowny
   //   visual-target: kolo-zamachowe, wziernik-smarowania, oslona-tylna, kurtyna-lewa,
   //                  kurtyna-prawa, panel-oburezny, lampka-gotowosci, tabliczka-znamionowa
   ```

   Implementacja:
   ```js
   _registerInteractable({ mesh, id, kind, baseMaterial, poses = null, pivotTarget = null }) {
     // 1. Cloned material przez registry (CRIT-6) — z guard'em na null path (MEDIUM-5).
     //    baseMaterial===null → tabliczka-znamionowa CanvasTexture path, mesh.material już ustawiony.
     if (baseMaterial !== null) {
       mesh.material = this.materialRegistry.getCloned(baseMaterial, id);
     }

     // 2. Resolve labelPL/descriptionPL z pl.parts (D-Phase2-08)
     const partCopy = pl.parts[id];
     if (!partCopy) {
       throw new Error(`PressModel._registerInteractable: brak pl.parts['${id}'] — dodaj wpis w src/i18n/pl.js`);
     }

     // 3. userData = identity only (CRIT-7) — restPosition jako plain object
     mesh.userData = {
       id,
       kind,
       restPosition: {
         pos: { x: mesh.position.x, y: mesh.position.y, z: mesh.position.z },
         rot: { x: mesh.rotation.x, y: mesh.rotation.y, z: mesh.rotation.z },
       },
       labelPL: partCopy.label,
       descriptionPL: partCopy.description,
     };

     // 4. Optional poses (definicja, nie aktywny pose name — CRIT-7)
     if (poses) mesh.userData.poses = poses;

     // 5. Optional pivotTarget (HIGH-1 — kontrakt dla Phase 3 animator)
     //    Walidacja enum żeby Phase 3 nie dostał śmiecia.
     if (pivotTarget !== null) {
       if (pivotTarget !== 'self' && pivotTarget !== 'parent') {
         throw new Error(`PressModel._registerInteractable: pivotTarget='${pivotTarget}' nie jest 'self' ani 'parent' (id='${id}')`);
       }
       mesh.userData.pivotTarget = pivotTarget;
     }

     // 6. Stable references po buildPress()
     this._interactables.set(id, mesh);
     this._meshDictionary.set(id, {
       labelPL: partCopy.label,
       descriptionPL: partCopy.description,
       kind,
     });

     return mesh;
   }
   ```

5. **Publiczne accessory**:
   ```js
   getInteractables() { return this._interactables; }
   getMeshDictionary() { return this._meshDictionary; }
   disposeMaterials() { this.materialRegistry.disposeAll(); }
   ```

6. **NIE ROZSZERZAJ `update(angle)`** — D-Phase2-05.

7. **NIE WYWOŁUJ `_registerInteractable()` w buildPress() w tym planie.** buildPress() zostaje IDENTYCZNY funkcjonalnie.

**MEDIUM-5 — explicit unit test inline w smoke test z Task 1**: rozszerz `tests/MaterialRegistry.smoke.test.js` o dodatkowy describe block testujący null-baseMaterial path w `_registerInteractable`. Aby uniknąć importu PressModel (który wymaga jsdom dla CanvasTexture w Wave 3), test wyizoluj na poziomie kontraktu — wystarczy że Wave 5 02-06 smoke test po pełnym buildPress() asertuje że tabliczka.material instanceof MeshBasicMaterial (już jest planowane w 02-06 Task 2 Test 7). Tutaj wystarczy że logika `if (baseMaterial !== null)` jest obecna w kodzie + manualny review.

ALTERNATIVE (preferowane jeśli executor ma czas): dodaj `tests/PressModel.registerInteractable.test.js` (jsdom envir.) z fake mesh + fake baseMaterial sprawdzający 4 ścieżki:
  - baseMaterial valid → mesh.material = clone
  - baseMaterial null → mesh.material untouched
  - poses passed → userData.poses set
  - pivotTarget invalid → throws
Jeśli zbyt dużo dla planu, zaadresuje to 02-06 smoke test (już zawiera test 4: tabliczka.material instanceof MeshBasicMaterial co implicite weryfikuje null path).

**Decyzja dla executora**: pozostań przy MaterialRegistry smoke test (Task 1) + 02-06 integracja jako pokrycie. Nie tworzymy osobnego pliku _registerInteractable test — kontrakt egzekwowany cumulative.
  </action>
  <verify>
    <automated>npm test</automated>
    Cały istniejący suite musi pozostać zielony (133 testy z Phase 1 + 3 z MaterialRegistry smoke). Brak nowych testów w tym task — Wave 5 (02-06) dopisuje smoke test który asertuje contract w pełni.

    Manual check: `npm run dev` — strona renderuje istniejącą prasę bez wizualnej regresji.
  </verify>
  <done>
    PressModel.js ma: import pl + MaterialRegistry; konstruktor inicjalizujący registry+Mapy; buildMaterials() z 18 base materials (6 starych + 12 nowych); _registerInteractable() z guard'em na null baseMaterial + optional poses + optional pivotTarget z walidacją enum; getInteractables/getMeshDictionary/disposeMaterials. update() niezmieniony. buildPress() niezmieniony funkcjonalnie. ≥136 testy zielone. Manual: scena renderuje się jak przed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pl.js` → PressModel runtime | Nieistniejący klucz `pl.parts[id]` → exception przy `_registerInteractable`. Mitigation: explicit throw z nazwą brakującego klucza. Wave 5 smoke test wymusza wszystkie 15 obecnych. |
| MaterialRegistry → GPU | Niewywołane `disposeAll()` przy HMR → leak GPU buffers. Mitigation: Wave 5 (02-06) wpina do Application.dispose(). |
| `_registerInteractable` → userData | Przypadkowe ustawienie state/isOpen/value łamie CRIT-7. Mitigation: komentarz nagłówka explicit list zabronionych kluczy + Wave 5 smoke test asertuje paranoid forbidden-keys + allowed-keys whitelist. |
| Phase 3 animator → poses semantyka | Phase 3 nie wie czy rotować mesh czy mesh.parent (HIGH-1). Mitigation: pivotTarget enum w userData; smoke test asertuje obecność dla wszystkich ruchomych. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (state) | userData mutacja przez Phase 3+ kod | mitigate | CRIT-7 invariant w komentarzu + Wave 5 smoke test (paranoid forbidden-keys + allowed-keys whitelist). |
| T-02-02 | DoS (memory) | HMR cycle leak materials/textures | mitigate | MaterialRegistry.disposeAll() + Wave 5 wire do Application.dispose(); MaterialRegistry smoke test (Task 1) asertuje `size() === 0` po dispose. |
| T-02-03 | Information Disclosure | n/a | accept | Phase 2 nie operuje na PII. |
| T-02-21 | Pivot semantics ambiguity | Phase 3 animator silently psuje któryś z 3 ruchomych przez błędne założenie pivota (HIGH-1) | mitigate | pivotTarget enum w userData + walidacja w _registerInteractable + Wave 5 smoke test asertuje obecność dla wszystkich 3 ruchomych. |
</threat_model>

<verification>
Po wszystkich 3 taskach:
1. `npm test` — 133 testy Phase 1 + 3 MaterialRegistry smoke = ≥136 zielone.
2. `npm run build` — production build przechodzi.
3. `npm run dev` — scena renderuje się identycznie jak przed.
4. Manual: `pressModel.getInteractables().size === 0` w konsoli (Wave 2-5 dopiero dopiszą).
5. Manual: `pressModel.materialRegistry instanceof MaterialRegistry` w konsoli — true.
</verification>

<success_criteria>
- [x] ROADMAP Phase 2 SC2 (getInteractables/getMeshDictionary scaffolding).
- [x] ROADMAP Phase 2 SC3 (cloned-material registry istnieje).
- [x] ROADMAP Phase 2 SC4 (userData kontrakt zacementowany w komentarzu i implementacji).
- [x] ROADMAP Phase 2 SC5 (dispose path istnieje).
- [x] D-Phase2-07 (MaterialRegistry pattern): zaimplementowany.
- [x] D-Phase2-08 (pl.js single source dla labelPL/descriptionPL): pl.parts dodane.
- [x] D-Phase2-09 (kind enum + mapping): mapowanie w komentarzu.
- [x] CRIT-6 invariant: scaffolding zacementowany.
- [x] CRIT-7 invariant: scaffolding zacementowany.
- [x] HIGH-1 (pivotTarget kontrakt): sygnatura + walidacja + komentarz dokumentujący semantyką.
- [x] MEDIUM-4 (realny verify dla Task 1): smoke test MaterialRegistry zamiast bezsensownego boundaries-test.
- [x] MEDIUM-5 (explicit null-baseMaterial guard): kod ma `if (baseMaterial !== null)` + komentarz objaśniający + Wave 5 02-06 integracja weryfikuje (tabliczka MeshBasicMaterial).
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-digital-twin-geometry/02-01-SUMMARY.md` z: lista zmodyfikowanych plików; potwierdzenie 12 nowych base materials; potwierdzenie 15 wpisów `pl.parts`; treść `_registerInteractable` (snippet) z nową sygnaturą (poses + pivotTarget); potwierdzenie ≥136 testów zielonych.

Atomic commit:
```
feat(02): MaterialRegistry + pl.parts + interactable scaffolding (TWIN-11/12/13)

- src/MaterialRegistry.js: cloned material + texture registry z disposeAll (D-Phase2-07).
- tests/MaterialRegistry.smoke.test.js: 3 testy (getCloned idempotent, disposeAll, size).
- src/i18n/pl.js: sekcja pl.parts z 15 wpisami {label, description} (D-Phase2-08).
- src/PressModel.js: konstruktor inicjalizuje registry + Mapy; _registerInteractable
  egzekwuje CRIT-6 + CRIT-7 + obsługuje pivotTarget (HIGH-1) + null baseMaterial path
  (MEDIUM-5); getInteractables / getMeshDictionary / disposeMaterials; buildMaterials
  rozszerzony o 12 base materials (paleta UI-SPEC). Zero behavior change w renderze.
```
</output>
