---
phase: 02-digital-twin-geometry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MaterialRegistry.js
  - src/i18n/pl.js
  - src/PressModel.js
autonomous: true
requirements: [TWIN-11, TWIN-12, TWIN-13]
must_haves:
  truths:
    - "Klasa MaterialRegistry istnieje z metodami getCloned/trackTexture/disposeAll/size"
    - "src/i18n/pl.js eksportuje pl.parts z 15 wpisami { label, description } (kebab-case polskie ID jako klucze)"
    - "PressModel ma w konstruktorze this.materialRegistry, this._interactables (Map), this._meshDictionary (Map)"
    - "PressModel.getInteractables() i getMeshDictionary() zwracają stable references do tych samych Map"
    - "PressModel ma metodę _registerInteractable({mesh, id, kind, baseMaterial}) — jedyne miejsce mutujące Mapy + userData interactable meshów"
    - "PressModel.disposeMaterials() deleguje do materialRegistry.disposeAll()"
    - "buildMaterials() ma wszystkie 12 nowych base materials z UI-SPEC §Color (matFlywheel, matBrakeSteel, matNameplateSilver, matLightCurtainBlack, matEStopRed, matSafetyButtonGreen, matReadyLamp, matGuardOrange, matGuardRearBlack, matOilSightYellow, matSafetyPanelGray, matSwitchBody)"
  artifacts:
    - path: "src/MaterialRegistry.js"
      provides: "Centralny rejestr sklonowanych materiałów + textures z disposeAll"
      contains: "export class MaterialRegistry"
    - path: "src/i18n/pl.js"
      provides: "Sekcja pl.parts z 15 wpisami { label, description }"
      contains: "parts:"
    - path: "src/PressModel.js"
      provides: "Scaffolding: constructor + _registerInteractable + getInteractables + getMeshDictionary + disposeMaterials + rozszerzony buildMaterials"
      contains: "_registerInteractable"
  key_links:
    - from: "src/PressModel.js"
      to: "src/MaterialRegistry.js"
      via: "import { MaterialRegistry } from './MaterialRegistry.js'"
      pattern: "import.*MaterialRegistry"
    - from: "src/PressModel.js"
      to: "src/i18n/pl.js"
      via: "import { pl } from './i18n/pl.js' i resolve labelPL/descriptionPL z pl.parts[id]"
      pattern: "pl\\.parts\\["
---

<objective>
Wave 1 fundament Phase 2: tworzy MaterialRegistry, rozszerza pl.js o sekcję `pl.parts` z 15 wpisami (kontrakt UI-SPEC §Copywriting), oraz dodaje do PressModel.js scaffolding rejestru interactable: konstruktor (Mapy + registry instance), centralną metodę `_registerInteractable()` egzekwującą CRIT-6 (cloned material) + CRIT-7 (userData identity-only), publiczne accessory `getInteractables()` / `getMeshDictionary()`, dispose path `disposeMaterials()`, oraz rozszerzony `buildMaterials()` o 12 nowych base materials z palety UI-SPEC. **Zero nowych meshów w tym planie** — to fundament, na którym Wave 2-5 buduje 15 meshów.

Purpose: bez tego fundamentu kolejne plany nie mają gdzie rejestrować interactable ani gdzie klonować materials. Atomic commit: "feat(02): MaterialRegistry + pl.parts + interactable scaffolding (TWIN-11/12/13 base, D-Phase2-07/08)".
Output: 1 nowy plik, 2 zmodyfikowane pliki, zero behavior change w renderze (PressModel buildPress() nadal buduje tylko Phase 1 meshe — frame/shaft/eccentric/rod/slider).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-digital-twin-geometry/02-CONTEXT.md
@.planning/phases/02-digital-twin-geometry/02-RESEARCH.md
@.planning/phases/02-digital-twin-geometry/02-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/PressModel.js
@src/i18n/pl.js

<interfaces>
<!-- Z research §"MaterialRegistry — rekomendowana implementacja" — interfejs wymagany przez Wave 2-5 -->

```javascript
// src/MaterialRegistry.js — public surface
export class MaterialRegistry {
  constructor()
  /** @returns {THREE.Material} clone of baseMaterial, idempotent per meshId */
  getCloned(baseMaterial, meshId)
  /** Track texture for dispose (CanvasTexture, Wave 3 tabliczka-znamionowa) */
  trackTexture(meshId, texture)
  /** Dispose all materials + textures, clear maps. Wired in main.js w 02-06. */
  disposeAll()
  /** Diagnostic — used by smoke test (registry.size() === 15 after build) */
  size()
}
```

```javascript
// src/PressModel.js — public surface ADDED w tym planie
class PressModel {
  // istniejące: constructor(scene), buildMaterials(), buildPress(), update(angle)
  // NOWE w tym planie:
  /** @returns {Map<string, THREE.Mesh>} stable reference, immutable po buildPress() */
  getInteractables()
  /** @returns {Map<string, {labelPL: string, descriptionPL: string, kind: string}>} */
  getMeshDictionary()
  /** Wire'd przez main.js w 02-06; deleguje do this.materialRegistry.disposeAll() */
  disposeMaterials()
  // PRYWATNE:
  /** Jedyne miejsce mutujące this._interactables/this._meshDictionary + ustawiające userData */
  _registerInteractable({ mesh, id, kind, baseMaterial })
}
```

```javascript
// src/i18n/pl.js — public surface ADDED
export const pl = {
  // ... istniejące disclaimer/webgl/ui/physics/machineState/errors
  parts: {
    'kolo-zamachowe':       { label: 'Koło zamachowe', description: '...' },
    // ... 14 więcej, klucze = mesh ID kebab-case polskie
  }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utwórz src/MaterialRegistry.js (D-Phase2-07)</name>
  <files>src/MaterialRegistry.js</files>
  <action>
Utwórz nowy plik `src/MaterialRegistry.js` zgodnie z research §"MaterialRegistry — rekomendowana implementacja".

Wymagania:
- Klasa `MaterialRegistry` z konstruktorem inicjalizującym `this._materials = new Map()` i `this._textures = new Map()`.
- Metoda `getCloned(baseMaterial, meshId)`: idempotentna — pierwsza prośba klonuje base przez `baseMaterial.clone()` i zapisuje pod `meshId`; kolejne wołania z tym samym `meshId` zwracają zacachowany clone (NIE klonują ponownie).
- Metoda `trackTexture(meshId, texture)`: zapisuje texture w mapie `_textures` pod kluczem `meshId`. Używana w Wave 3 dla CanvasTexture tabliczki znamionowej.
- Metoda `disposeAll()`: iteruje `_materials` i `_textures`, woła `.dispose()` na każdym, czyści obie Mapy.
- Metoda `size()`: zwraca `this._materials.size` — używana w smoke test (Wave 5).
- JSDoc po polsku per CONVENTIONS (krótkie zdania, nazwy parametrów po angielsku — Three.js terms).
- Brak zewnętrznych importów (czysty ES module — `Material.dispose` woła się przez instance method).
- Komentarz nagłówka: "CRIT-6 invariant: per-mesh cloned MeshStandardMaterial. Każdy interactable dostaje swój clone — bez tego HighlightManager (Phase 4) zaświeciłby wszystko naraz. Dispose path zapobiega leakowi GPU buffers przy Vite HMR (TWIN-11 SC5)."
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
    Po dodaniu boundaries entry w 02-06 ten test będzie sprawdzał że MaterialRegistry NIE importuje store/training. W tym planie wystarczy że plik istnieje i parsuje się jako ES module — boundaries scanner pomija nieistniejące entries.
  </verify>
  <done>
    Plik `src/MaterialRegistry.js` istnieje, eksportuje `MaterialRegistry` class z metodami getCloned/trackTexture/disposeAll/size. Plik ma JSDoc po polsku. `npm test` nie regresuje (zielony jak przed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rozszerz src/i18n/pl.js o sekcję pl.parts (D-Phase2-08, UI-SPEC §Copywriting Contract)</name>
  <files>src/i18n/pl.js</files>
  <action>
Po sekcji `errors:` w `pl` object dodaj sekcję `parts:` z 15 wpisami. Klucze = mesh ID kebab-case polskie (per CONTEXT specifics §"Lista finalnych id"). Każdy wpis: `{ label: string, description: string }`. Treść EXACT z UI-SPEC §"Copywriting Contract — pl.parts.* (Polish Part Names)" tabela — 15 wierszy. NIE zmieniaj treści (UI-SPEC §Checker Sign-Off Dimension 1 wymaga PASS na 80–160 chars per description; UI-SPEC zacementował dokładne stringi).

Klucze (kolejność per UI-SPEC tabela):
1. `kolo-zamachowe`
2. `dzwignia-sprzegla`
3. `hamulec`
4. `wziernik-smarowania`
5. `oslona-przednia`
6. `oslona-tylna`
7. `kurtyna-lewa`
8. `kurtyna-prawa`
9. `panel-oburezny`
10. `przycisk-start-lewy`
11. `przycisk-start-prawy`
12. `lampka-gotowosci`
13. `estop`
14. `wylacznik-glowny`
15. `tabliczka-znamionowa`

Format (przykład pierwszego wpisu — pełna treść per UI-SPEC):
```js
parts: {
  'kolo-zamachowe': {
    label: 'Koło zamachowe',
    description: 'Magazynuje energię obrotową napędu i oddaje ją w momencie sprzęgnięcia, dostarczając moment do uderzenia suwakiem.',
  },
  // ... 14 więcej dokładnie wg UI-SPEC tabela
},
```

Po dodaniu przeczytaj UI-SPEC §"Copywriting Contract" i przepisz każdą `description` znak-po-znaku — żaden re-wording. Te stringi są kontraktem.
  </action>
  <verify>
    <automated>npm test -- tests/boundaries.test.js</automated>
    Test §"Polish string literal scanner (UI-06, MOD-3)" musi być zielony — `pl.js` jest na ALLOWED_PATHS (`src/i18n/`), więc polskie literały są dozwolone.
  </verify>
  <done>
    `src/i18n/pl.js` ma `parts:` sekcję z 15 wpisami. Każdy `label` jest niepusty Polish string. Każdy `description` ma 80-160 znaków per UI-SPEC. Klucze są dokładnie 15 mesh ID z CONTEXT specifics. `npm test` zielony.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffolding w src/PressModel.js — registry + accessory + _registerInteractable + extended buildMaterials</name>
  <files>src/PressModel.js</files>
  <behavior>
    - Test (Wave 5 02-06 dopisze): `new PressModel(scene)` nie throwuje + `getInteractables() instanceof Map` + `getInteractables().size === 0` przed wywołaniem buildPress() rozszerzonego (na razie buildPress() nie woła _registerInteractable, więc po construct'cie size=0).
    - Test (Wave 5 02-06 dopisze): `pressModel.materialRegistry instanceof MaterialRegistry` (i odpowiednio rozszerzone Map'y).
    - Test (Wave 5): `disposeMaterials()` woła `materialRegistry.disposeAll()` — spy na `disposeAll`.
  </behavior>
  <action>
1. **Import**: dodaj na górze pliku:
   ```js
   import { pl } from './i18n/pl.js';
   import { MaterialRegistry } from './MaterialRegistry.js';
   ```

2. **Konstruktor** — po `this.scene = scene;` (przed `this.r = 0.8`) dodaj:
   ```js
   this.materialRegistry = new MaterialRegistry();
   /** @type {Map<string, THREE.Mesh>} stable reference dla Phase 3 RaycastController */
   this._interactables = new Map();
   /** @type {Map<string, {labelPL: string, descriptionPL: string, kind: string}>} */
   this._meshDictionary = new Map();
   ```

3. **buildMaterials()** — po istniejących 6 base materials dodaj 12 nowych (kolejność per UI-SPEC §Color tabele 60/30/10):

   **60% Dominant** (poza istniejącymi matBody/matBase):
   - `this.matSafetyPanelGray = new THREE.MeshStandardMaterial({ color: 0x6b6b6b, metalness: 0.2, roughness: 0.6 })`
   - `this.matSwitchBody = new THREE.MeshStandardMaterial({ color: 0x404040, metalness: 0.3, roughness: 0.5 })`

   **30% Secondary** (poza istniejącym matShaft):
   - `this.matFlywheel = new THREE.MeshStandardMaterial({ color: 0x7a7a7a, metalness: 0.85, roughness: 0.35 })`
   - `this.matBrakeSteel = new THREE.MeshStandardMaterial({ color: 0x5a5a5a, metalness: 0.7, roughness: 0.45 })`
   - `this.matNameplateSilver = new THREE.MeshStandardMaterial({ color: 0xc8c8c8, metalness: 0.6, roughness: 0.4 })` (rezerwa — w Wave 3 użyjemy MeshBasicMaterial z CanvasTexture, ale base zostawiamy dla ewentualnego fallbacku oraz spójności palety)
   - `this.matLightCurtainBlack = new THREE.MeshStandardMaterial({ color: 0x1a1a1a, metalness: 0.4, roughness: 0.5 })`

   **10% Accent** (paleta Wong):
   - `this.matEStopRed = new THREE.MeshStandardMaterial({ color: 0xD55E00, metalness: 0.1, roughness: 0.55 })`
   - `this.matSafetyButtonGreen = new THREE.MeshStandardMaterial({ color: 0x009E73, metalness: 0.1, roughness: 0.55 })`
   - `this.matReadyLamp = new THREE.MeshStandardMaterial({ color: 0x009E73, metalness: 0.0, roughness: 0.3 })` (emissive zostaje #000000 — Phase 4 territory; UI-SPEC §Default Pose Visual State)
   - `this.matGuardOrange = new THREE.MeshStandardMaterial({ color: 0xE07A1F, metalness: 0.05, roughness: 0.7 })`
   - `this.matGuardRearBlack = new THREE.MeshStandardMaterial({ color: 0x2a2a2a, metalness: 0.0, roughness: 0.8 })`
   - `this.matOilSightYellow = new THREE.MeshStandardMaterial({ color: 0xd4a017, metalness: 0.1, roughness: 0.4 })`

   **NIE TYKAJ** istniejących `matBody`, `matShaft`, `matEccentric`, `matRod`, `matSlider`, `matBase` — Phase 1 brownfield kontrakt zachowany (D-Phase2-07: cloned materials dotyczy NOWYCH interactable; istniejące non-interactable shaft/eccentric/rod używają shared materials, OK bo nie są highlighted).

4. **Nowa metoda `_registerInteractable({ mesh, id, kind, baseMaterial })`** — wstaw PRZED `update(angle)` (po `buildPress()`). Komentarz nagłówka EXACTLY:

   ```js
   // === CRIT-6 + CRIT-7 INVARIANT (Phase 1 lock-in, Phase 2 enforcement) ===
   // Jedyne miejsce w PressModel mutujące this._interactables / this._meshDictionary
   // i ustawiające userData interactable mesh'a.
   //
   // CRIT-6: każdy interactable dostaje sklonowany MeshStandardMaterial (lub Basic dla
   //         tabliczki) przez MaterialRegistry — bez tego highlight w Phase 4 zaświeci wszystko.
   // CRIT-7: userData = TYLKO tożsamość. NIGDY status. Pose'y to definicja (poses dict, identity).
   //         Active pose name żyje w state.meshStates[id].pose w store (Phase 3+), NIE tutaj.
   //
   // Zabronione klucze w userData: state, isOpen, value, status, currentPose, isHighlighted.
   // Dozwolone klucze: id, kind, restPosition, labelPL, descriptionPL, poses (definicja, nie aktywny).
   //
   // Mapowanie kind (D-Phase2-09):
   //   manipulation: dzwignia-sprzegla, hamulec, oslona-przednia, przycisk-start-lewy,
   //                 przycisk-start-prawy, estop, wylacznik-glowny
   //   visual-target: kolo-zamachowe, wziernik-smarowania, oslona-tylna, kurtyna-lewa,
   //                  kurtyna-prawa, panel-oburezny, lampka-gotowosci, tabliczka-znamionowa
   ```

   Implementacja:
   ```js
   _registerInteractable({ mesh, id, kind, baseMaterial }) {
     // 1. Cloned material przez registry (CRIT-6)
     mesh.material = this.materialRegistry.getCloned(baseMaterial, id);

     // 2. Resolve labelPL/descriptionPL z pl.parts (D-Phase2-08)
     const partCopy = pl.parts[id];
     if (!partCopy) {
       throw new Error(`PressModel._registerInteractable: brak pl.parts['${id}'] — dodaj wpis w src/i18n/pl.js`);
     }

     // 3. userData = identity only (CRIT-7) — restPosition jako plain object
     mesh.userData = {
       id,
       kind,
       restPosition: {
         pos: { x: mesh.position.x, y: mesh.position.y, z: mesh.position.z },
         rot: { x: mesh.rotation.x, y: mesh.rotation.y, z: mesh.rotation.z },
       },
       labelPL: partCopy.label,
       descriptionPL: partCopy.description,
     };

     // 4. Stable references po buildPress()
     this._interactables.set(id, mesh);
     this._meshDictionary.set(id, {
       labelPL: partCopy.label,
       descriptionPL: partCopy.description,
       kind,
     });

     return mesh;
   }
   ```

   **UWAGA**: dla pivot-grup (Wave 5: oslona-przednia/wylacznik-glowny/dzwignia-sprzegla) `mesh` to GRUPA (nie sam mesh) — userData rejestrujemy na grupie. `restPosition` zaczyta `group.position` i `group.rotation` (group ma te same pola co Mesh). Wave 5 dopisze do userData dodatkowy klucz `poses` PO wywołaniu _registerInteractable (lub przekaże poses jako optional argument — patrz §"Pivot pre-translate gotcha").

   Aby ułatwić Wave 5: rozszerz signature opcjonalnie:
   ```js
   _registerInteractable({ mesh, id, kind, baseMaterial, poses = null }) {
     // ... jak wyżej ...
     if (poses) mesh.userData.poses = poses;  // definicja, nie aktywny pose name (CRIT-7)
     // ...
   }
   ```

   Dla tabliczki znamionowej (Wave 3) `baseMaterial` może być `null` — w tym przypadku _registerInteractable NIE woła `getCloned` (mesh już ma swój `MeshBasicMaterial` z CanvasTexture). Dodaj guard:
   ```js
   if (baseMaterial !== null) {
     mesh.material = this.materialRegistry.getCloned(baseMaterial, id);
   }
   ```

5. **Publiczne accessory**:
   ```js
   /** @returns {Map<string, THREE.Mesh>} stable reference dla Phase 3 RaycastController */
   getInteractables() { return this._interactables; }

   /** @returns {Map<string, {labelPL: string, descriptionPL: string, kind: string}>} */
   getMeshDictionary() { return this._meshDictionary; }

   /** Dispose path — wywoływany z Application.dispose() (main.js, plan 02-06). TWIN-11 SC5. */
   disposeMaterials() { this.materialRegistry.disposeAll(); }
   ```

6. **NIE ROZSZERZAJ `update(angle)`** — D-Phase2-05 zacementował: koło zamachowe rotuje za darmo jako dziecko shaftAxis (Wave 2); inne nowe meshe albo statyczne, albo z pivot-grupą animowaną Phase 3+.

7. **NIE WYWOŁUJ `_registerInteractable()` w buildPress() w tym planie.** buildPress() zostaje IDENTYCZNY funkcjonalnie — dodajemy TYLKO scaffolding. Wave 2-5 dopiszą wywołania `this._buildXxx()` które rejestrują meshe.
  </action>
  <verify>
    <automated>npm test</automated>
    Cały istniejący suite musi pozostać zielony (133 testy z Phase 1). Brak nowych testów w tym planie — Wave 5 (02-06) dopisuje smoke test który asertuje contract w pełni.

    Manual check: `npm run dev` — strona renderuje istniejącą prasę bez wizualnej regresji.
  </verify>
  <done>
    PressModel.js ma: import pl + MaterialRegistry; konstruktor inicjalizujący registry+Mapy; buildMaterials() z 18 base materials (6 starych + 12 nowych); _registerInteractable() z guard'em na null baseMaterial i optional poses; getInteractables/getMeshDictionary/disposeMaterials. update() niezmieniony. buildPress() niezmieniony funkcjonalnie. 133 istniejące testy zielone. Manual: scena renderuje się jak przed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pl.js` → PressModel runtime | Nieistniejący klucz `pl.parts[id]` → exception przy `_registerInteractable`. Mitigation: explicit throw z nazwą brakującego klucza (Task 3.4). Wave 5 smoke test wymusza wszystkie 15 obecnych. |
| MaterialRegistry → GPU | Niewywołane `disposeAll()` przy HMR → leak GPU buffers. Mitigation: Wave 5 (02-06) wpina do Application.dispose(); Phase 1 ma już istniejący kanał `import.meta.hot.dispose`. |
| `_registerInteractable` → userData | Przypadkowe ustawienie state/isOpen/value łamie CRIT-7. Mitigation: komentarz nagłówka explicit list zabronionych kluczy + Wave 5 smoke test asertuje `not.toHaveProperty('state'/'isOpen'/'value')`. |

## STRIDE Threat Register

Phase 2 jest scenecomp / non-network — STRIDE głównie nieaplikowalne. Notuję jeden istotny wektor:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (state) | userData mutacja przez Phase 3+ kod | mitigate | CRIT-7 invariant w komentarzu + smoke test (Wave 5) — `not.toHaveProperty('state')`. RaycastController (Phase 3) czyta tylko `userData.id/kind`, store mutuje `state.meshStates[id]`, nie userData. |
| T-02-02 | DoS (memory) | HMR cycle leak materials/textures | mitigate | MaterialRegistry.disposeAll() + Wave 5 wire do Application.dispose(); smoke test SC5 asertuje `registry.size() === 0` po dispose. |
| T-02-03 | Information Disclosure | n/a | accept | Phase 2 nie operuje na PII ani secrets — czysta geometria + Polish copy z UI. |
</threat_model>

<verification>
Po wszystkich 3 taskach:
1. `npm test` — 133 testy Phase 1 zielone (no regression).
2. `npm run build` — production build przechodzi bez warnings o nieużywanych eksportach (MaterialRegistry zaimportowany w PressModel, używany w konstruktorze).
3. `npm run dev` — scena renderuje się identycznie jak przed (brak nowych meshów; existing prasa kinetycznie poprawna).
4. Manual: `pressModel.getInteractables().size === 0` w konsoli przeglądarki (zerowy rejestr — Wave 2-5 dopiero dopiszą).
5. Manual: `pressModel.materialRegistry instanceof MaterialRegistry` w konsoli — true.
</verification>

<success_criteria>
- [x] Mapuje na ROADMAP Phase 2 SC2 (getInteractables/getMeshDictionary scaffolding) — Map'y istnieją, gotowe do wypełnienia.
- [x] Mapuje na ROADMAP Phase 2 SC3 (cloned-material registry istnieje) — registry działa, ale jest puste do Wave 2.
- [x] Mapuje na ROADMAP Phase 2 SC4 (userData kontrakt zacementowany w komentarzu i implementacji `_registerInteractable`) — egzekucja w Wave 2-5.
- [x] Mapuje na ROADMAP Phase 2 SC5 (dispose path istnieje przez `disposeMaterials()` → `disposeAll()`) — wire'd w 02-06.
- [x] D-Phase2-07 (MaterialRegistry pattern): zaimplementowany.
- [x] D-Phase2-08 (pl.js single source dla labelPL/descriptionPL): pl.parts dodane.
- [x] D-Phase2-09 (kind enum + mapping): mapowanie w komentarzu nad `_registerInteractable`.
- [x] CRIT-6 invariant: scaffolding zacementowany.
- [x] CRIT-7 invariant: scaffolding zacementowany.
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-digital-twin-geometry/02-01-SUMMARY.md` z: lista zmodyfikowanych plików; potwierdzenie 12 nowych base materials; potwierdzenie 15 wpisów `pl.parts`; treść `_registerInteractable` (snippet) dla referencji Wave 2-5; potwierdzenie 133 testy zielone.

Atomic commit:
```
feat(02): MaterialRegistry + pl.parts + interactable scaffolding (TWIN-11/12/13)

- src/MaterialRegistry.js: cloned material + texture registry z disposeAll (D-Phase2-07).
- src/i18n/pl.js: sekcja pl.parts z 15 wpisami {label, description} (D-Phase2-08, UI-SPEC).
- src/PressModel.js: konstruktor inicjalizuje registry + Mapy; _registerInteractable
  egzekwuje CRIT-6 (cloned material) + CRIT-7 (userData identity-only); getInteractables /
  getMeshDictionary / disposeMaterials; buildMaterials rozszerzony o 12 base materials
  (paleta UI-SPEC 60/30/10). Zero behavior change w renderze.
```
</output>
