---
phase: 02-digital-twin-geometry
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/PressModel.js
autonomous: true
requirements: [TWIN-10]
must_haves:
  truths:
    - "Po buildPress() pressModel.getInteractables() zawiera tabliczka-znamionowa (cumulative 7 wpisów)"
    - "Tabliczka renderuje CanvasTexture z napisami: 'PM-300', 'Nr ser. 2025/0042', 'Producent: Demo Sp. z o.o.' (UI-SPEC §Typography exact strings)"
    - "Tabliczka używa MeshBasicMaterial (NIE MeshStandardMaterial) — tekst nie powinien być oświetlony scenicznie"
    - "Texture jest tracked w MaterialRegistry przez trackTexture('tabliczka-znamionowa', texture)"
    - "texture.colorSpace === THREE.SRGBColorSpace (Three.js r0.184 explicit zgodnie z UI-SPEC §Typography)"
  artifacts:
    - path: "src/PressModel.js"
      provides: "Metoda _buildNameplate() z CanvasTexture + tracked texture"
      contains: "_buildNameplate"
  key_links:
    - from: "_buildNameplate"
      to: "this.materialRegistry.trackTexture"
      via: "trackTexture('tabliczka-znamionowa', texture) — dispose path"
      pattern: "trackTexture"
---

<objective>
Wave 3: dodaje tabliczkę znamionową (TWIN-10) z `CanvasTexture` renderowaną raz w buildzie. Tabliczka jest wyizolowana w osobnym planie ze względu na specjalny lifecycle: `MeshBasicMaterial` (nie Standard), `CanvasTexture` osobno trackowany w registry, polskie diakrytyki w stringach (test boundary scanner).

Atomic commit: "feat(02): nameplate with CanvasTexture (TWIN-10)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-digital-twin-geometry/02-CONTEXT.md
@.planning/phases/02-digital-twin-geometry/02-RESEARCH.md
@.planning/phases/02-digital-twin-geometry/02-UI-SPEC.md
@.planning/phases/02-digital-twin-geometry/02-02-SUMMARY.md
@src/PressModel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: _buildNameplate() (TWIN-10)</name>
  <files>src/PressModel.js</files>
  <action>
Dodaj prywatną metodę `_buildNameplate()` po `_buildLightCurtain()`. Wywołaj z `buildPress()` po `_buildLightCurtain()` i przed `this.update(0)`.

**Implementacja per RESEARCH §"CanvasTexture — tabliczka znamionowa" + UI-SPEC §Typography (LITERALNE wartości):**

```js
/**
 * TWIN-10: Tabliczka znamionowa z CanvasTexture renderowaną raz w buildzie.
 * MeshBasicMaterial (nie Standard) — tekst nie powinien być oświetlony scenicznie.
 * Texture trackowana osobno w registry dla dispose path (TWIN-11 SC5).
 *
 * Treść tabliczki (CONTEXT specifics: placeholder inline w PressModel, ASCII-clean
 * pure żeby boundary scanner nie wykrył polskich diakrytyk poza i18n/scenarios):
 *   Linia 1: PM-300        (96px, weight 700)
 *   Linia 2: Nr ser. 2025/0042   (56px, weight 600)
 *   Linia 3: Producent: Demo Sp. z o.o.  (44px, weight 500)
 */
_buildNameplate() {
  // 1. Canvas + 2D context — render once at build time
  const canvas = document.createElement('canvas');
  canvas.width = 512;
  canvas.height = 320;
  const ctx = canvas.getContext('2d');

  // Background — matt silver per UI-SPEC §Typography
  ctx.fillStyle = '#c8c8c8';
  ctx.fillRect(0, 0, 512, 320);

  // Border — engraved bezel
  ctx.strokeStyle = '#3a3a3a';
  ctx.lineWidth = 4;
  ctx.strokeRect(2, 2, 508, 316);

  // Text rendering — system-ui font fallback chain (UI-SPEC §Typography)
  ctx.fillStyle = '#1a1a1a';
  ctx.textBaseline = 'alphabetic';
  ctx.imageSmoothingEnabled = true;

  // Line 1 — model
  ctx.font = 'bold 96px system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
  ctx.fillText('PM-300', 32, 130);

  // Line 2 — serial
  ctx.font = '600 56px system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
  ctx.fillText('Nr ser. 2025/0042', 32, 200);

  // Line 3 — producer (ASCII-clean — boundaries scanner OK)
  ctx.font = '500 44px system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
  ctx.fillText('Producent: Demo Sp. z o.o.', 32, 260);

  // 2. Texture — Three.js r0.184 default colorSpace = SRGB-aware via explicit set
  const texture = new THREE.CanvasTexture(canvas);
  texture.colorSpace = THREE.SRGBColorSpace;
  texture.minFilter = THREE.LinearFilter;
  texture.magFilter = THREE.LinearFilter;
  // anizotropia: renderer NIE jest dostępny w PressModel ctor — accepting default 1.
  // RESEARCH otwarte pytanie #1: rekomendacja (b) {maxAnisotropy} — ZACHOWANE NA PHASE 5/QA
  // ze względu na drobny visual-only wpływ; smoke test nie zależy od tego.

  // 3. Material + Mesh — MeshBasicMaterial (NIE Standard — tekst nie ma być oświetlony)
  const material = new THREE.MeshBasicMaterial({ map: texture });

  // 4. Geometry per UI-SPEC §Color tabela (0.4 × 0.25 × 0.02)
  const plateGeo = new THREE.BoxGeometry(0.4, 0.25, 0.02);
  const plate = new THREE.Mesh(plateGeo, material);

  // 5. Pozycja per CONTEXT D-Phase2-04 + RESEARCH §"Layout collision risk":
  //    Lewy bok korpusu (x=-3) koliduje z lewą ramką (x=-3..-1, y=0..10). Mitigation: przesuń Z na +0.05
  //    żeby tabliczka wystawała przed front ramki (jak realne maszyny).
  plate.position.set(-3.05, 5.5, 0.05);
  // Obrót: tabliczka czytelna z przodu maszyny — lekkie skierowanie do kamery (rotacja Y)
  plate.rotation.y = Math.PI * 0.05;  // ~9° w prawo, czytelność dla kursanta z pozycji frontalnej
  plate.castShadow = false;  // płaska powierzchnia, cienie niepotrzebne
  this.group.add(plate);

  // 6. Track texture w registry — disposeAll() Wave 5 ją domknie
  this.materialRegistry.trackTexture('tabliczka-znamionowa', texture);

  // 7. Register interactable — UWAGA: baseMaterial = null bo mesh JUŻ ma swoje material
  //    (MeshBasicMaterial z CanvasTexture, nie ze sklonowanego MeshStandardMaterial).
  //    _registerInteractable z guard'em na null pominie wywołanie getCloned (plan 02-01 Task 3.4).
  this._registerInteractable({
    mesh: plate,
    id: 'tabliczka-znamionowa',
    kind: 'visual-target',
    baseMaterial: null,  // CanvasTexture path — material już ustawiony explicit
  });
}
```

**Edge case do zweryfikowania w `_registerInteractable`** (plan 02-01 Task 3.4): guard na `baseMaterial !== null` musi pominąć wywołanie `getCloned`. Jeśli plan 02-01 dostarczył guard zgodnie z opisem, to działa. Jeśli nie, plan 02-03 zatrzymuje się na regression test i fixujemy `_registerInteractable` (commit-amendable, ale lepiej zauważone teraz niż w Wave 5).

**Smoke test (Wave 5 02-06 dopisze)** — assertion że `nameplate.material instanceof MeshBasicMaterial` i `nameplate.material.map === <jakaś texture>` — TWIN-10 contract.

Komentarz w _buildNameplate o boundaries.test.js scanner: "Treść tabliczki ASCII-clean — bez polskich diakrytyk żeby boundary scanner (UI-06) nie failował na literałach Polish poza i18n/scenarios. CONTEXT deferred ideas: jeśli BHP review wymaga producenta z `ż`/`ł`, migracja do `pl.parts['tabliczka-znamionowa'].canvasContent` (osobny commit, deferred Phase 2)."
  </action>
  <verify>
    <automated>npm test</automated>
    Manual `npm run dev`:
    - Tabliczka widoczna na lewym boku korpusu, na wysokości oczu, lekko skręcona do kamery.
    - Czytelne napisy "PM-300", "Nr ser. 2025/0042", "Producent: Demo Sp. z o.o.".
    - Przy ruchach kamery tekst nie miga / nie blurruje (UI-SPEC negative criteria).

    Konsola:
    - `pressModel.getInteractables().size === 7`.
    - `pressModel.getInteractables().get('tabliczka-znamionowa').material instanceof THREE.MeshBasicMaterial` → true.
    - `pressModel.getInteractables().get('tabliczka-znamionowa').material.map.colorSpace === THREE.SRGBColorSpace` → true.
    - `pressModel.materialRegistry._textures.has('tabliczka-znamionowa')` → true.

    Boundaries:
    - `npm test -- tests/boundaries.test.js` — Polish-literal scanner zielony (treść ASCII).
  </verify>
  <done>
    `_buildNameplate()` istnieje i wołany z `buildPress()`. Tabliczka renderuje się z CanvasTexture, MeshBasicMaterial, texture trackowana w registry. `getInteractables().size === 7` cumulative. Boundaries scanner OK.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | DoS (texture leak) | `texture.dispose()` NIE jest wołany przez `material.dispose()` automatycznie | mitigate | Osobny `_textures` Map w MaterialRegistry; `disposeAll()` woła `tex.dispose()` na każdej; smoke test (Wave 5) asertuje spy.toHaveBeenCalledTimes(1) na texture.dispose. |
| T-02-07 | Information Disclosure | "Producent: Demo Sp. z o.o." ASCII placeholder — w realnym wdrożeniu mogłaby zawierać dane wrażliwe | accept | Tabliczka jest fikcyjna (Demo); CONTEXT deferred ideas dopuszcza migrację do pl.parts dla realnych danych. Phase 2 nie ma realnych klientów. |
| T-02-08 | UI-06 boundary violation | Polski diakrytyk w treści tabliczki (np. nazwa producenta `Łomża Sp. z o.o.`) | mitigate | Explicit komentarz nakazujący ASCII-clean; `npm test boundaries` w verify. Migracja do pl.parts gdy potrzebne. |
</threat_model>

<verification>
1. `npm test` — 133 testy + boundaries Polish-literal scanner zielone.
2. Manual: tabliczka czytelna z dystansu kamery (UI-SPEC visual fidelity).
3. `pressModel.getInteractables().size === 7`.
4. Texture trackowana w registry (Wave 5 dispose path zweryfikuje).
</verification>

<success_criteria>
- ROADMAP Phase 2 SC1: 7/15 cumulative.
- TWIN-10 zaimplementowane.
- UI-SPEC §Typography contract: dokładnie te 3 stringi, fonty, kolory, contrast 7:1 (#1a1a1a na #c8c8c8 = ~9:1 → ✓ AAA).
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-digital-twin-geometry/02-03-SUMMARY.md` z: potwierdzenie 7/15 ID; literalna treść tabliczki (ASCII-clean); pozycja x=-3.05 / z=0.05 (mitigation kolizji z lewą ramką).

Atomic commit:
```
feat(02): nameplate with CanvasTexture (TWIN-10)

- _buildNameplate: 512x320 canvas, MeshBasicMaterial, SRGBColorSpace, system-ui fonts.
- Treść ASCII-clean (boundary scanner UI-06 OK): PM-300 / Nr ser. 2025/0042 / Producent: Demo Sp. z o.o.
- Texture trackowana osobno w MaterialRegistry._textures dla dispose path (TWIN-11 SC5).
- Pozycja (-3.05, 5.5, 0.05) — mitigation kolizji z lewą ramką (RESEARCH §Layout collision risk).
- Po buildPress() getInteractables() ma 7 wpisów cumulative.
```
</output>
