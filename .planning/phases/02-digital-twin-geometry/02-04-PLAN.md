---
phase: 02-digital-twin-geometry
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/PressModel.js
autonomous: true
requirements: [TWIN-07, TWIN-08]
must_haves:
  truths:
    - "Po buildPress() pressModel.getInteractables() zawiera 5 nowych ID: panel-oburezny, przycisk-start-lewy, przycisk-start-prawy, lampka-gotowosci, estop (cumulative 12)"
    - "panel-oburezny jest grupą-rodzicem dla pozostałych 4 — przesunięcie panelu propaguje na dzieci przez scene graph"
    - "estop ma kształt grzybka (LatheGeometry profile head + stem) — czerwony grzybek na czarnym walcu, dziecko grupy panelu"
    - "2 zielone przyciski po bokach E-stopa, lampka gotowości wyżej, panel jako visual-target z własnym wpisem w registry"
    - "panel-oburezny.kind === 'visual-target'; przycisk-start-lewy/prawy/estop.kind === 'manipulation'; lampka-gotowosci.kind === 'visual-target'"
  artifacts:
    - path: "src/PressModel.js"
      provides: "Metody _buildSafetyPanel() (panel + dzieci) + _buildEStop() (LatheGeometry grzybek)"
      contains: "_buildSafetyPanel"
  key_links:
    - from: "_buildEStop"
      to: "safetyPanel group"
      via: "safetyPanel.add(estopGroup) — E-stop dziecko panelu, nie this.group"
      pattern: "safetyPanel\\.add"
---

<objective>
Wave 4: dodaje klaster panelu oburęcznego (TWIN-07) + E-stop (TWIN-08) jako 5 nowych interactable. Panel jest grupą-rodzicem dla 4 dzieci; przesunięcie panelu propaguje. E-stop używa LatheGeometry (RESEARCH §"E-stop" recipe) — pierwszy mesh w fazie używający Lathe. 5 nowych meshów cumulative → 12/15.

Atomic commit: "feat(02): safety panel cluster + E-stop (TWIN-07/08)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-digital-twin-geometry/02-CONTEXT.md
@.planning/phases/02-digital-twin-geometry/02-RESEARCH.md
@.planning/phases/02-digital-twin-geometry/02-UI-SPEC.md
@.planning/phases/02-digital-twin-geometry/02-03-SUMMARY.md
@src/PressModel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: _buildSafetyPanel() (TWIN-07) — panel + 2 zielone przyciski + lampka</name>
  <files>src/PressModel.js</files>
  <action>
Dodaj prywatną metodę `_buildSafetyPanel()` po `_buildNameplate()`. Wywołaj z `buildPress()` po `_buildNameplate()` przed `this.update(0)`.

**Implementacja per CONTEXT D-Phase2-04 (panel @ (0, 2, 2.5)) + RESEARCH §"Layout collision risk" (children positions: lewy/prawy ±0.4, lampka y+0.2) + UI-SPEC visual fidelity:**

```js
/**
 * TWIN-07: Panel oburęczny (pulpit) + 4 dzieci klikalnych:
 * - przycisk-start-lewy (manipulation, zielony)
 * - przycisk-start-prawy (manipulation, zielony)
 * - lampka-gotowosci (visual-target, zielona; emissive #000000 w Phase 2 — Phase 4 zaświeca)
 * - estop (manipulation) — dodawany w _buildEStop() (osobna metoda, ten sam plan).
 *
 * Panel = grupa-rodzic; dzieci pozycjonowane w lokalnym frame'a panelu (przesunięcie panelu propaguje).
 * Sam pulpit (panel) = osobny visual-target wpis w registry — kursant może najechać i dostać tooltip
 * "Panel oburęczny" z opisem dydaktycznym (Phase 5 EDU-*).
 */
_buildSafetyPanel() {
  // 1. Grupa panelu — kontener dla pulpitu + 4 dzieci. Pozycja w świecie per D-Phase2-04.
  this.safetyPanel = new THREE.Group();
  this.safetyPanel.position.set(0, 2, 2.5);
  this.group.add(this.safetyPanel);

  // 2. Sam pulpit (mesh) — wystarczająco duży żeby pomieścić E-stop + 2 buttony + lampkę
  //    Skośna powierzchnia jak realne pulpity? — Phase 2 prosty BoxGeometry (CONTEXT D-Phase2-02)
  //    Dimensions: szerokość ~1.6, głębokość ~0.7, grubość ~0.1 (widoczna z pozycji frontalnej kamery)
  const pulpitGeo = new THREE.BoxGeometry(1.6, 0.1, 0.7);
  const pulpit = new THREE.Mesh(pulpitGeo, this.matSafetyPanelGray);
  pulpit.position.set(0, 0, 0);  // origin grupy panelu = środek pulpitu
  pulpit.castShadow = true;
  pulpit.receiveShadow = true;
  this.safetyPanel.add(pulpit);

  this._registerInteractable({
    mesh: pulpit,
    id: 'panel-oburezny',
    kind: 'visual-target',
    baseMaterial: this.matSafetyPanelGray,
  });

  // 3. Lewy zielony przycisk startu — walec 0.08r, h=0.06, na pulpicie po lewej
  const buttonGeo = new THREE.CylinderGeometry(0.08, 0.08, 0.06, 24);
  // Geometria: oś walca po Y (default Cylinder) → przycisk wystaje "do góry" z pulpitu = +Y
  const startLeft = new THREE.Mesh(buttonGeo, this.matSafetyButtonGreen);
  startLeft.position.set(-0.5, 0.08, 0.15);  // lewa strona pulpitu, lekko z przodu
  startLeft.castShadow = true;
  this.safetyPanel.add(startLeft);
  this._registerInteractable({
    mesh: startLeft,
    id: 'przycisk-start-lewy',
    kind: 'manipulation',
    baseMaterial: this.matSafetyButtonGreen,
  });

  // 4. Prawy zielony przycisk startu — symetryczny do lewego
  const startRight = new THREE.Mesh(buttonGeo, this.matSafetyButtonGreen);
  startRight.position.set(0.5, 0.08, 0.15);
  startRight.castShadow = true;
  this.safetyPanel.add(startRight);
  this._registerInteractable({
    mesh: startRight,
    id: 'przycisk-start-prawy',
    kind: 'manipulation',
    baseMaterial: this.matSafetyButtonGreen,
  });

  // 5. Lampka gotowości — mała półsfera/walec, wyżej na pulpicie (UI-SPEC: "wyżej niż przyciski")
  //    Phase 2: emissive zostaje #000000 / emissiveIntensity=0 (UI-SPEC negative criteria — Phase 4 territory)
  const lampGeo = new THREE.SphereGeometry(0.05, 16, 12);
  const lamp = new THREE.Mesh(lampGeo, this.matReadyLamp);
  lamp.position.set(0, 0.1, -0.2);  // środek pulpitu, dalej (wyżej w lokalnym Z na panelu = bliżej tyłu pulpitu)
  lamp.castShadow = true;
  this.safetyPanel.add(lamp);
  this._registerInteractable({
    mesh: lamp,
    id: 'lampka-gotowosci',
    kind: 'visual-target',
    baseMaterial: this.matReadyLamp,
  });

  // E-stop dodawany w _buildEStop() — Task 2.
}
```

**UWAGA o `this.safetyPanel`**: zostaje jako instance field bo `_buildEStop()` (Task 2) potrzebuje referencji żeby dodać E-stop jako dziecko panelu (RESEARCH boundary contract: "E-stop dziecko grupy panelu, NIE this.group, by przemieszczanie panelu nie wymagało update'u E-stopa").
  </action>
  <verify>
    <automated>npm test</automated>
    Manual `npm run dev`:
    - Pulpit panelu widoczny przed prasą (z dodatnim Z=2.5), na wysokości y=2.
    - Dwa zielone walce przycisków po bokach pulpitu.
    - Mała zielona kuleczka lampki na środku pulpitu (Phase 2: NIE świeci, czarny emissive).

    Konsola:
    - `pressModel.getInteractables().size === 11` (7 + 4 z tego task = 11; E-stop w Task 2 → 12).
    - `pressModel.getInteractables().get('lampka-gotowosci').material.emissive.getHex() === 0x000000` (UI-SPEC negative criteria).
    - `pressModel.getInteractables().get('panel-oburezny').material !== pressModel.getInteractables().get('przycisk-start-lewy').material` → true.
  </verify>
  <done>
    Po Task 1: 4 wpisy w registry (panel-oburezny + 2 przyciski + lampka), wszystkie dzieci `this.safetyPanel`. Wizualnie pulpit z 3 elementami widoczny.
  </done>
</task>

<task type="auto">
  <name>Task 2: _buildEStop() (TWIN-08) — czerwony grzybek z LatheGeometry</name>
  <files>src/PressModel.js</files>
  <action>
Dodaj prywatną metodę `_buildEStop()` po `_buildSafetyPanel()`. Wywołaj z `buildPress()` PO `_buildSafetyPanel()` (E-stop wymaga `this.safetyPanel` istniejącego) przed `this.update(0)`.

**Implementacja per RESEARCH §"LatheGeometry — E-stop" + UI-SPEC §"E-stop reads as a red mushroom button":**

```js
/**
 * TWIN-08: E-stop — czerwony grzybek wyłącznika awaryjnego.
 * Dwa LatheGeometry meshe (stem + head) pod jedną grupą; head jest PRIMARY (klikalny),
 * stem jest decorative.
 *
 * Pozycja: dziecko this.safetyPanel @ lokalna pozycja (0, 0.1, 0) — środek pulpitu, lekko wyżej niż base.
 * Kombinacja UI-SPEC ("wyraźnie większy niż zielone przyciski, na środkowej osi panelu") + CONTEXT D-Phase2-04
 * (świat (0, 2.3, 2.5) → panel @ (0, 2, 2.5) → E-stop lokalnie y=0.3, ale my dajemy 0.1 bo
 * stem startuje od y=0 lokalnie, head do 0.55, więc top E-stopa @ panel.y + 0.55 = 2.55 + offset,
 * UI-SPEC ma wystarczający margin).
 */
_buildEStop() {
  if (!this.safetyPanel) {
    throw new Error('_buildEStop wymaga _buildSafetyPanel uprzednio (this.safetyPanel undefined)');
  }

  const estopGroup = new THREE.Group();
  estopGroup.position.set(0, 0.05, 0);  // środek pulpitu, tuż nad powierzchnią
  this.safetyPanel.add(estopGroup);

  // 1. Stem — czarny walec u dołu (RESEARCH profile)
  const stemPoints = [
    new THREE.Vector2(0.0,  0.0),
    new THREE.Vector2(0.06, 0.0),
    new THREE.Vector2(0.06, 0.18),
    new THREE.Vector2(0.0,  0.18),
  ];
  const stemGeo = new THREE.LatheGeometry(stemPoints, 24);
  const stem = new THREE.Mesh(stemGeo, this.matSwitchBody);
  stem.castShadow = true;
  estopGroup.add(stem);
  // Stem NIE jest interactable — używa shared this.matSwitchBody (visual-only, decorative).

  // 2. Head — czerwony grzybek (PRIMARY mesh, interactable)
  const headPoints = [
    new THREE.Vector2(0.0,  0.18),
    new THREE.Vector2(0.06, 0.18),  // nasada grzybka, wąska
    new THREE.Vector2(0.13, 0.21),  // rozszerza się
    new THREE.Vector2(0.13, 0.25),  // płasko-cylindryczny rant
    new THREE.Vector2(0.10, 0.27),  // zaokrąglona kopuła
    new THREE.Vector2(0.0,  0.27),
  ];
  const headGeo = new THREE.LatheGeometry(headPoints, 32);
  const head = new THREE.Mesh(headGeo, this.matEStopRed);
  head.castShadow = true;
  estopGroup.add(head);

  this._registerInteractable({
    mesh: head,
    id: 'estop',
    kind: 'manipulation',
    baseMaterial: this.matEStopRed,
  });

  // UWAGA: head.position = (0,0,0) w lokalnym frame'a estopGroup; estopGroup @ (0, 0.05, 0) w panelu;
  //        panel @ (0, 2, 2.5) w świecie. Końcowa pozycja head w świecie: (0, 2.05, 2.5) + lathe profile y.
  //        UI-SPEC "wyraźnie większy niż zielone przyciski (r=0.08)" — head r=0.13 → ✓ większy.
  //        UI-SPEC "na środkowej osi panelu" — head x=0 (jest na osi). ✓
  //        UI-SPEC negative criteria: estop.kind === 'manipulation', NIE 'visual-target' (UI-SPEC nie cementuje, ale
  //        D-Phase2-09 cementuje: estop ∈ manipulation). ✓
}
```

**Pierwszy `LatheGeometry` w fazie**: weryfikacja per RESEARCH §"Krytyczne uwagi":
- ✓ Pierwszy/ostatni point z x=0 (zamyka cap top/bottom).
- ✓ `segments=24/32` daje gładką krzywiznę.
- ✓ Lathe rotuje wokół Y w lokalnym frame'a — grzybek stoi pionowo bez dodatkowych rotacji.

**RESEARCH boundary contract** (potwierdzone tutaj): _registerInteractable rejestruje TYLKO `head` (PRIMARY); `stem` jest visual-only z shared material `this.matSwitchBody`. Smoke test (Wave 5) asertuje `interactables.size === 15` — gdyby przypadkowo zarejestrowano stem, size byłby 16.
  </action>
  <verify>
    <automated>npm test</automated>
    Manual `npm run dev`:
    - Czerwony grzybek E-stop widoczny na środku pulpitu, wyraźnie większy niż zielone przyciski po bokach.
    - Wizualnie czytelny jako "mushroom button" (kopuła + cylindryczna rant + cienki stem).

    Konsola:
    - `pressModel.getInteractables().size === 12` (cumulative).
    - `pressModel.getInteractables().get('estop').material !== pressModel.getInteractables().get('przycisk-start-lewy').material` → true.
    - E-stop head jest dzieckiem `pressModel.safetyPanel` (przesunięcie panelu propaguje).
  </verify>
  <done>
    `_buildEStop()` istnieje i wołany po `_buildSafetyPanel()` z `buildPress()`. Czerwony grzybek widoczny na panelu. `getInteractables().size === 12` cumulative.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering (parent reference) | _buildEStop wołany przed _buildSafetyPanel → this.safetyPanel undefined → silent failure (E-stop dodany do this.group) | mitigate | Explicit guard `if (!this.safetyPanel) throw` w _buildEStop; kolejność wywołania w buildPress() udokumentowana komentarzem. |
| T-02-10 | DoS (size mismatch) | Przypadkowa rejestracja stem'a E-stopa jako interactable | mitigate | Wave 5 smoke test asertuje size === 15; ten plan dodaje 5, cumulative 12 — Wave 5 (planów 04+05) dorzucają resztę do 15. |
| T-02-11 | UI-SPEC violation | Lampka z emissiveIntensity > 0 w Phase 2 | mitigate | Komentarz w _buildSafetyPanel: "Phase 4 territory"; UI-SPEC negative criteria → smoke test (Wave 5) asertuje `lamp.material.emissiveIntensity === 0`. |
</threat_model>

<verification>
1. `npm test` — wszystkie testy zielone.
2. `pressModel.getInteractables().size === 12` (cumulative).
3. Wszystkie 5 ID obecne; każdy material unique cloned instance.
4. Manual: panel z grzybkiem + 2 zielonymi + lampką widoczny przed prasą; E-stop wyraźnie większy.
5. Lampka emissive #000000 (Phase 2 lock-in).
</verification>

<success_criteria>
- ROADMAP Phase 2 SC1: 12/15 cumulative.
- ROADMAP Phase 2 SC2: getInteractables ma 12 wpisów.
- TWIN-07 zaimplementowane (panel + 2 buttony + lampka).
- TWIN-08 zaimplementowane (E-stop z LatheGeometry).
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-digital-twin-geometry/02-04-SUMMARY.md` z: 5 nowych ID; potwierdzenie hierarchii (panel = parent dla 4 dzieci); wybrane offsety w lokalnym frame'a panelu.

Atomic commit:
```
feat(02): safety panel cluster + E-stop (TWIN-07/08)

- _buildSafetyPanel: pulpit (visual-target) + przycisk-start-lewy/prawy (manipulation) +
  lampka-gotowosci (visual-target, emissive #000000 — Phase 4 territory).
  Panel = grupa-rodzic; this.safetyPanel zapisana jako instance field dla _buildEStop.
- _buildEStop: LatheGeometry stem (decorative) + head (PRIMARY, manipulation, czerwony grzybek).
  Head dziecko this.safetyPanel — przesunięcie panelu propaguje.
- 5 nowych interactables; cumulative 12/15.
```
</output>
