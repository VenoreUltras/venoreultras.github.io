---
phase: 17-quizcontroller-application-wiring
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - src/main.js
  - tests/application.test.js
autonomous: false
requirements: [EXAM-04, TEST-09, TEST-10]
must_haves:
  truths:
    - "Application instantiates this.quizController after examPromptModal"
    - "Application.dispose() disposes quizController BEFORE examPromptModal (reverse construction order)"
    - "Application.dispose() covers startMenuOverlay, elementInfoOverlay, mediaManager, quizController with no leaks"
    - "Full test suite is green at >=903 baseline (986+ incl. new QuizController/export tests)"
    - "getInteractables().size === 15 (no regression)"
    - "npm run build main bundle < 850 KB"
  artifacts:
    - path: "src/main.js"
      provides: "QuizController instantiation + dispose chain entry"
      contains: "QuizController"
  key_links:
    - from: "src/main.js"
      to: "QuizController"
      via: "new QuizController({store}) after examPromptModal + dispose before examPromptModal"
      pattern: "new QuizController|quizController.dispose"
---

<objective>
Final integration + milestone gate for v1.2. Wire QuizController into the Application constructor (after examPromptModal, DI store) and into the dispose chain (before examPromptModal, reverse construction order). Then run the full milestone gate: complete suite green at >=903 baseline, getInteractables().size===15, mode state machine no regression, and the FINAL bundle gate npm run build < 850 KB.

Purpose: main.js is the single integration point and the riskiest edit (CONTEXT: "wiring ostatni"). This plan touches it minimally and verifies the whole milestone after every change. A human checkpoint confirms the end-to-end smoke (cross-system flow a browser is needed for) and the bundle gate.
Output: wired src/main.js, updated tests/application.test.js, and a passed milestone gate closing v1.2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-quizcontroller-application-wiring/17-CONTEXT.md
@.planning/phases/17-quizcontroller-application-wiring/17-PATTERNS.md
@.planning/phases/17-quizcontroller-application-wiring/17-RESEARCH.md
@CLAUDE.md
@src/main.js

<interfaces>
main.js wiring points (VERIFIED line numbers):
  line 26:  import { ExamPromptModal } from './ui/ExamPromptModal.js';  → add QuizController import after
  line 357: this.elementInfoOverlay = new ElementInfoOverlay({...});
  line 366-369: this.examPromptModal = new ExamPromptModal({store, scenarios});  → instantiate quizController AFTER this block
  Dispose chain (Application.dispose, lines 494-525):
    line 504: if (this.tooltipManager) this.tooltipManager.dispose();
    line 505: if (this.examPromptModal) this.examPromptModal.dispose();   ← insert quizController BEFORE this line
    line 506: if (this.elementInfoOverlay) this.elementInfoOverlay.dispose();
    line 508: if (this.mediaManager) this.mediaManager.dispose?.();
  startMenuOverlay disposed at line 494 (VERIFIED present), elementInfoOverlay 506, mediaManager 508.

QuizController (Plan 02): new QuizController({ store: this.store }) — uses #modal-container root.

Dispose-order rule: quizController constructed AFTER examPromptModal → must be disposed BEFORE it
(reverse construction order). application.test.js W6 uses invocationCallOrder to assert this.

getInteractables().size===15 invariant — tests already cover it (tests/PressModel.*.phase9.test.js).
Bundle baseline: 826.74 KB main; gate < 850 KB; no new packages this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire QuizController into Application ctor + dispose chain</name>
  <read_first>
    - src/main.js lines 26 (imports), 357-369 (elementInfoOverlay + examPromptModal ctor block), 494-525 (dispose chain)
    - 17-PATTERNS.md "src/main.js (config/wiring)" (import + instantiation + dispose insertion patterns)
    - src/ui/ExamPromptModal.js (the adjacent wiring analog)
  </read_first>
  <files>src/main.js</files>
  <action>
    Add `import { QuizController } from './ui/QuizController.js';` adjacent to the ExamPromptModal import (after line 26). After the examPromptModal instantiation block (ends line 369), add a Polish comment block ((d.5) QuizController — Phase 17: modal quizu BHP, wyzwalany przez activeModal='bhp-quiz' ustawiany przez subscriber finishedAt w trybie egzamin) and `this.quizController = new QuizController({ store: this.store });`. In Application.dispose(), insert `if (this.quizController) this.quizController.dispose();` BETWEEN the tooltipManager.dispose() line (504) and the examPromptModal.dispose() line (505) — reverse construction order. Do NOT reorder or touch any other dispose line. Minimal, targeted edit only.
  </action>
  <verify>
    <automated>grep -q "new QuizController" src/main.js && grep -q "import { QuizController }" src/main.js && node -e "const s=require('fs').readFileSync('src/main.js','utf8');const q=s.indexOf('this.quizController.dispose');const e=s.indexOf('this.examPromptModal.dispose');if(q<0||e<0||q>e)process.exit(1);console.log('dispose order OK')"</automated>
  </verify>
  <acceptance_criteria>
    - QuizController imported and instantiated as this.quizController after examPromptModal
    - quizController.dispose() appears BEFORE examPromptModal.dispose() in source order (reverse ctor order)
    - No other dispose line moved
  </acceptance_criteria>
  <done>QuizController wired into ctor + dispose chain with correct order.</done>
</task>

<task type="auto">
  <name>Task 2: Extend application tests for QuizController wiring + dispose order</name>
  <read_first>
    - tests/application.test.js (Phase 5 wiring describe ~lines 240-255; W6 dispose-order test ~lines 450-479; beforeEach DOM templates)
    - 17-PATTERNS.md "tests/application.test.js (test — additive)"
  </read_first>
  <files>tests/application.test.js</files>
  <action>
    Append (do not modify existing cases): an `it` asserting `app.quizController` is defined after Application construction. Extend the dispose-order coverage (W6 or a new W6b `it`) with `vi.spyOn(app.quizController, 'dispose')` and `vi.spyOn(app.examPromptModal, 'dispose')`, call `app.dispose()`, and assert `quizSpy.mock.invocationCallOrder[0] < examPromptSpy.mock.invocationCallOrder[0]`. Add (or assert in an existing dispose-coverage test) that startMenuOverlay, elementInfoOverlay, mediaManager, and quizController dispose are all invoked (spies called once). The existing beforeEach DOM templates already include #modal-container — no template change needed.
  </action>
  <verify>
    <automated>npm test -- tests/application.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/application.test.js` exits 0
    - A test asserts app.quizController defined
    - A test asserts quizController disposed before examPromptModal (invocationCallOrder)
    - Dispose-coverage asserts startMenuOverlay/elementInfoOverlay/mediaManager/quizController all disposed
  </acceptance_criteria>
  <done>Application wiring + dispose-order + leak-coverage tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MILESTONE GATE — full suite + bundle + getInteractables + dispose leak + e2e smoke</name>
  <what-built>
    QuizController wired into Application; full v1.2 feature set integrated. This is the final v1.2
    milestone gate. Claude has run the automated gate below; the human confirms the browser-only
    end-to-end smoke and the bundle gate.
  </what-built>
  <how-to-verify>
    Claude runs FIRST and reports results inline:
    1. Full suite: `npm test` — MUST be all green, >=903 baseline (expected 986+ passed, 1 skipped, plus new QuizController + export tests). Report the passed/failed/skipped counts.
    2. Invariant: confirm getInteractables().size===15 tests pass (tests/PressModel.bolts-welds.phase9.test.js #5, tests/PressModel.cables.phase9.test.js) — no regression.
    3. Mode state machine: confirm mode-transition tests (free/nauka/egzamin) green — no regression.
    4. Dispose leak check: `npm test -- tests/application.test.js` — dispose chain covers startMenuOverlay, elementInfoOverlay, mediaManager, quizController; quizController disposed before examPromptModal.
    5. FINAL bundle gate: `npm run build` — main bundle MUST be < 850 KB. Report the exact KB of the main `dist/assets/index-*.js` chunk (baseline 826.74 KB; quiz-data stays a separate chunk).

    THEN the human verifies the end-to-end smoke via `npm run dev` (http://localhost:5173):
    a. Cold start → StartMenu appears.
    b. Tryb swobodny: click an element → overlay opens → media tab → ESC closes. No JS console errors.
    c. Tryb nauka: complete the SOP → ExamPromptModal appears.
    d. Tryb egzamin: complete the SOP → BHP quiz opens, 3D sim pauses → answer mc/tf/sequence questions → per-question feedback shows explanation + norm citation → score screen shows quiz.score/100 + pass/fail → "Zakończ" closes quiz and returns to free.
    e. Export: trigger PDF and JSON export → both contain BOTH the procedural (SOP) section AND the BHP section/field.
    Confirm zero JS console errors throughout.
  </how-to-verify>
  <resume-signal>Type "approved" if all automated checks pass AND the e2e smoke is clean (no console errors, both export sections present, bundle < 850 KB). Otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application ctor → subsystem lifecycle | main.js owns construction + dispose of all subsystems. Incorrect dispose order leaks listeners/subscriptions across HMR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Denial of service (resource leak) | Application.dispose() chain | mitigate | quizController added to dispose chain in reverse ctor order; application.test.js asserts dispose called + ordering; HMR re-init verified leak-free in e2e smoke. |
| T-17-08 | Tampering (regression) | main.js minimal edit destabilizing running app | mitigate | Full suite run after the wiring edit; getInteractables().size===15 + mode-machine tests guard no regression; bundle gate guards size. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; bundle gate (<850 KB) is the supply-size check. |
</threat_model>

<verification>
- `npm test` full suite green, >=903 baseline (expected 986+ plus new tests).
- `npm test -- tests/application.test.js` green (wiring + dispose order + leak coverage).
- getInteractables().size===15 tests green.
- `npm run build` main bundle < 850 KB (FINAL milestone gate).
- e2e smoke clean (human checkpoint): menu → free → nauka → egzamin quiz → export with both sections.
</verification>

<success_criteria>
- QuizController instantiated + disposed (before examPromptModal) in Application
- Application.dispose() leak-free covering startMenuOverlay/elementInfoOverlay/mediaManager/quizController
- Full suite green at >=903 baseline, no regression
- getInteractables().size===15 holds; mode state machine no regression
- npm run build < 850 KB (v1.2 milestone gate closed)
- e2e smoke approved by human
</success_criteria>

<output>
Create `.planning/phases/17-quizcontroller-application-wiring/17-04-SUMMARY.md` when done
</output>
