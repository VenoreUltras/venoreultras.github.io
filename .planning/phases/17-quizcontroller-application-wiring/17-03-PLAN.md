---
phase: 17-quizcontroller-application-wiring
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/export/JsonExporter.js
  - src/export/PdfExporter.js
  - tests/jsonExporter.test.js
  - tests/pdfExporter.test.js
autonomous: true
requirements: [EXAM-04]
must_haves:
  truths:
    - "JSON export includes a quiz field (score, correct, total, passed, finishedAt) when quiz.finishedAt is set"
    - "JSON export omits the quiz field when quiz absent or finishedAt is null (nauka mode)"
    - "PDF export renders a 'Wynik BHP' section when quiz.finishedAt is set, after the History section, before the footer"
    - "PDF omits the BHP section when quiz not completed"
    - "Existing scoring.procedure JSON/PDF output is unchanged (additive only)"
  artifacts:
    - path: "src/export/JsonExporter.js"
      provides: "buildJsonPayload additive quiz field"
      contains: "quiz"
    - path: "src/export/PdfExporter.js"
      provides: "generatePdf BHP section (sectionBhpResult) after History, before footer"
      contains: "sectionBhpResult"
  key_links:
    - from: "src/export/JsonExporter.js"
      to: "state.quiz"
      via: "conditional quiz field on finishedAt guard"
      pattern: "state\\.quiz"
    - from: "src/export/PdfExporter.js"
      to: "state.quiz + pl.pdf.sectionBhpResult"
      via: "doc.text BHP section guarded by finishedAt"
      pattern: "sectionBhpResult"
---

<objective>
Extend the two exporters with quiz (BHP) result data — strictly additive, non-breaking. JSON gains a conditional `quiz` field; PDF gains a "Wynik BHP" section after the History section and before the footer. Both are guarded so nauka-mode exports (quiz never completed) and quiz-less mock states produce identical output to today.

Purpose: EXAM-04 requires the hybrid exam result (3D SOP + BHP quiz) in PDF/JSON export. The procedural (scoring) sections stay untouched — quiz and scoring are separate, isolated sections (CRIT-V12-5).
Output: extended JsonExporter.js + PdfExporter.js with additive tests in both test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-quizcontroller-application-wiring/17-CONTEXT.md
@.planning/phases/17-quizcontroller-application-wiring/17-PATTERNS.md
@.planning/phases/17-quizcontroller-application-wiring/17-RESEARCH.md
@CLAUDE.md
@src/export/JsonExporter.js
@src/export/PdfExporter.js

<interfaces>
JsonExporter (src/export/JsonExporter.js:25):
  buildJsonPayload(state, scenarioTitlePL)  // currently returns {version, session, metadata} directly
  state.quiz available from full store state: { questions, currentIndex, answers, score, finishedAt }

PdfExporter (src/export/PdfExporter.js:145):
  generatePdf({ state, scenarioTitlePL, metrics, allAttemptsMetrics=[] })  // async
  Section 4 (Historia prób) ends at line 260; footer loop starts line 262 — insert BHP section between.
  Helpers in scope: _ensureSpace(doc, y, mm, header), MARGIN_L, doc.setFontSize/doc.text
  pl.pdf.sectionBhpResult/bhpScore/bhpPassed/bhpFailed added in Plan 01.

Pass threshold: 80. Do NOT import QUIZ_PASS_THRESHOLD into exporters (research note: avoid store
import from export utils to keep them pure data transforms) — use literal 80 with a `// QUIZ_PASS_THRESHOLD`
comment, consistent with RESEARCH Pattern 3/4. correct = round((score/100)*total).

Guard for both: `state.quiz?.finishedAt !== null && state.quiz?.finishedAt !== undefined`
(handles nauka mode where quiz.finishedAt===null, and mock states with no quiz key).

Existing tests use property-level assertions (NOT toStrictEqual) — additive fields are non-breaking
(RESEARCH Assumption A3, VERIFIED). Do not change existing assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive quiz field to buildJsonPayload</name>
  <read_first>
    - src/export/JsonExporter.js lines 25-46 (current direct return)
    - 17-PATTERNS.md "src/export/JsonExporter.js (utility, transform — additive)"
    - tests/jsonExporter.test.js (existing mockState + assertion style)
  </read_first>
  <files>src/export/JsonExporter.js, tests/jsonExporter.test.js</files>
  <behavior>
    - quiz present (finishedAt set): result.quiz = {score, correct, total, passed, finishedAt}, passed = score>=80, correct = round((score/100)*total), total = questions.length
    - quiz absent OR finishedAt===null: result has no quiz field
    - existing version/session/metadata unchanged
  </behavior>
  <action>
    Refactor buildJsonPayload to assign the existing return object to a `const result` variable, then conditionally add `result.quiz` when `state.quiz?.finishedAt !== null && state.quiz?.finishedAt !== undefined`, then `return result`. Compute total=state.quiz.questions.length, correct=Math.round((state.quiz.score/100)*total), passed=state.quiz.score >= 80 (comment `// QUIZ_PASS_THRESHOLD`). Do not change existing keys. In tests/jsonExporter.test.js append (do not modify existing cases): a mockStateWithQuiz fixture (5 questions, score 80, finishedAt set) asserting result.quiz defined with score/passed(true)/total(5)/finishedAt; a case asserting result.quiz undefined for the existing quiz-less mockState; a case asserting result.quiz undefined when quiz.finishedAt===null.
  </action>
  <verify>
    <automated>npm test -- tests/jsonExporter.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/jsonExporter.test.js` exits 0 (new + all existing cases green)
    - `grep -c "result.quiz" src/export/JsonExporter.js` >= 1
    - Existing version/session/metadata assertions unchanged
  </acceptance_criteria>
  <done>JSON export additive quiz field green; nauka mode omits it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BHP section to generatePdf</name>
  <read_first>
    - src/export/PdfExporter.js lines 145-273 (generatePdf body, Section 4 end at 260, footer at 262)
    - 17-PATTERNS.md "src/export/PdfExporter.js (utility, transform — additive)" + "PDF Section Spacing"
    - tests/pdfExporter.test.js (mockDoc.text spy pattern)
  </read_first>
  <files>src/export/PdfExporter.js, tests/pdfExporter.test.js</files>
  <behavior>
    - quiz completed: BHP section rendered after Historia prób (line 260), before footer loop (line 262)
    - section: heading pl.pdf.sectionBhpResult; line "{bhpScore}: correct/total (score%)"; pass/fail line bhpPassed/bhpFailed
    - quiz not completed (finishedAt null/undefined): no BHP section, footer position unchanged
    - existing procedural sections unchanged
  </behavior>
  <action>
    Insert the BHP section between line 260 (end of Sekcja 4 block) and line 262 (footer loop start), guarded by `if (state.quiz?.finishedAt !== null && state.quiz?.finishedAt !== undefined)`. Use the section spacing pattern: setFontSize(12); y = _ensureSpace(doc, y, 8, header); doc.text(pl.pdf.sectionBhpResult, MARGIN_L, y); y += 7; setFontSize(10); compute total = state.quiz.questions?.length ?? 0, correct = total>0 ? Math.round((state.quiz.score/100)*total) : 0, passed = state.quiz.score >= 80 (comment `// QUIZ_PASS_THRESHOLD`); doc.text(`${pl.pdf.bhpScore}: ${correct}/${total} (${state.quiz.score}%)`, MARGIN_L, y); y += 6; doc.text(passed ? pl.pdf.bhpPassed : pl.pdf.bhpFailed, MARGIN_L, y); y += 10. Procedural scoring section unchanged. In tests/pdfExporter.test.js append (do not modify existing): a mockStateWithQuiz case asserting mockDoc.text was called with pl.pdf.sectionBhpResult; a case with quiz.finishedAt===null asserting pl.pdf.sectionBhpResult NOT among text calls. Polish comments.
  </action>
  <verify>
    <automated>npm test -- tests/pdfExporter.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/pdfExporter.test.js` exits 0 (new + existing cases green)
    - `grep -c "sectionBhpResult" src/export/PdfExporter.js` >= 1
    - BHP section inserted after Sekcja 4, before the footer loop (verify by surrounding context, not line number alone)
    - Existing PDF section tests unchanged and green
  </acceptance_criteria>
  <done>PDF BHP section green; procedural section untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store state → export payload (JSON/PDF) | Quiz + scoring data serialized for download. Isolation must hold: quiz and scoring are separate fields/sections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering (data integrity) | additive export breaking existing payload shape | mitigate | Additions are conditional (finishedAt guard) and use `result.quiz`/new PDF section only; existing keys/sections untouched; existing property-level tests assert unchanged shape. |
| T-17-06 | Repudiation | quiz vs scoring conflation in export | mitigate | Quiz rendered as a distinct field (JSON) / section (PDF) labelled "Wynik BHP"; procedural scoring stays separate (CRIT-V12-5). |
</threat_model>

<verification>
- `npm test -- tests/jsonExporter.test.js tests/pdfExporter.test.js` exits 0.
- Full suite: no regression in any previously-passing export test.
</verification>

<success_criteria>
- buildJsonPayload emits quiz field only when quiz completed; nauka/quiz-less omit it
- generatePdf renders the BHP section only when quiz completed, after History, before footer
- Procedural scoring output byte-identical to before (additive)
- Both export test files green
</success_criteria>

<output>
Create `.planning/phases/17-quizcontroller-application-wiring/17-03-SUMMARY.md` when done
</output>
