---
phase: 06-scenarios-replay-retry-export
plan: 07
type: execute
wave: 4
depends_on: [06-04, 06-05, 06-06]
files_modified:
  - src/export/PdfExporter.js
  - src/ui/SessionOverlay.js
  - src/ui/StatusPanel.js
  - index.html
  - style.css
  - public/fonts/NotoSans-Regular.ttf
  - package.json
  - tests/pdfExporter.test.js
  - tests/sessionOverlay.test.js
  - tests/StatusPanel.test.js
  - tests/boundaries.test.js
autonomous: true
requirements: [SCORE-05, SCORE-06]
tags: [pdf, jsPDF, ui, session-overlay, scenario-selector]
user_setup:
  - service: jspdf-npm
    why: "PDF generation client-side dla raportu sesji szkoleniowej (D-Phase6-16, SCORE-05)"
    env_vars: []
    dashboard_config: []
    additional_steps:
      - "Plan instaluje `jspdf@^4.2.1` przez npm. Brak dashboard config."
  - service: noto-sans-ttf
    why: "Polskie diakrytyki w PDF (D-Phase6-16)"
    env_vars: []
    additional_steps:
      - "Plik `public/fonts/NotoSans-Regular.ttf` musi być pobrany ręcznie z https://fonts.google.com/noto/specimen/Noto+Sans (SIL Open Font License). Plan zatrzymuje się na blocking-human checkpoint przed Task 1."
must_haves:
  truths:
    - "PdfExporter.generatePdf(state, scenarioTitle) async function dynamicznie importuje jspdf, osadza Noto Sans TTF chunk-based base64, generuje 5-sekcyjny raport (D-Phase6-17), zwraca Blob (SCORE-05)"
    - "PDF zawiera tytuł 'RAPORT SESJI SZKOLENIOWEJ', stopka z disclaimer + numerem strony + appVersion; ZERO seal/signature line (CRIT-1, AF-9)"
    - "SessionOverlay pojawia się gdy session.finishedAt !== null, expone'uje 4 przyciski: Otwórz replay / Spróbuj ponownie (Nauka only) / Eksportuj JSON / Eksportuj PDF (UI-SPEC §3)"
    - "StatusPanel rozszerzony o ScenarioSelector (4 buttony 1-4 / pl.scenarios klucze) z confirmModal mid-run (UI-SPEC §1, D-Phase5-07 reuse)"
    - "pluralPL używany w SessionOverlay (Wynik: N błędów) i w PDF lista błędów (SCORE-06)"
  artifacts:
    - path: "src/export/PdfExporter.js"
      provides: "async generatePdf"
      exports: ["generatePdf", "generateFilename"]
    - path: "src/ui/SessionOverlay.js"
      provides: "class SessionOverlay"
    - path: "public/fonts/NotoSans-Regular.ttf"
      provides: "TTF font asset"
  key_links:
    - from: "SessionOverlay button Eksportuj PDF"
      to: "PdfExporter.generatePdf"
      via: "async onclick handler"
      pattern: "await generatePdf"
    - from: "PdfExporter loadFont"
      to: "/fonts/NotoSans-Regular.ttf"
      via: "fetch + arrayBuffer + chunk-base64 + addFileToVFS + addFont + setFont"
      pattern: "addFileToVFS"
    - from: "StatusPanel ScenarioSelector"
      to: "store.openConfirmModal (Plan 05) lub direct startScenario"
      via: "klik scenario-btn"
      pattern: "scenario-btn"
---

<objective>
Ostatni warstwowy plan: PDF exporter z dynamic import('jspdf') + Noto Sans TTF embed,
SessionOverlay UI (4 buttony export/replay/retry), oraz brownfield rozszerzenie StatusPanel
o ScenarioSelector dla wszystkich 4 scenariuszy.

Purpose: domyka SCORE-05 (PDF) i SCORE-06 (pluralPL faktycznie używany w UI/PDF). To także
ostatni kawałek UI Phase 6 wymagany do user-facing kompletu (overlay z exportami).

Output: 2 nowe pliki produkcyjne + 1 brownfield (StatusPanel) + index.html + style.css +
package.json (jspdf dependency) + 3 pliki testów rozszerzone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-scenarios-replay-retry-export/06-CONTEXT.md
@.planning/phases/06-scenarios-replay-retry-export/06-RESEARCH.md
@.planning/phases/06-scenarios-replay-retry-export/06-UI-SPEC.md

@src/ui/StatusPanel.js
@src/training/ScoringService.js
@src/export/JsonExporter.js
@src/persistence/sessionPersistence.js

@.planning/phases/06-scenarios-replay-retry-export/06-04-PLAN.md
@.planning/phases/06-scenarios-replay-retry-export/06-05-PLAN.md
@.planning/phases/06-scenarios-replay-retry-export/06-06-PLAN.md

<interfaces>
<!-- jsPDF 4.2.1 API verified in RESEARCH.md §Code Examples -->
const { jsPDF } = await import('jspdf');
const doc = new jsPDF({orientation:'p', unit:'mm', format:'a4'});
doc.addFileToVFS('NotoSans-Regular.ttf', base64);
doc.addFont('NotoSans-Regular.ttf', 'NotoSans', 'normal');
doc.setFont('NotoSans', 'normal');
doc.setFontSize(18);
doc.text('text', x, y, {align?, maxWidth?});
doc.setDrawColor(r,g,b); doc.line(x1,y1,x2,y2);
doc.addPage(); doc.setPage(N);
doc.splitTextToSize(text, maxWidth);  // → string[]
doc.internal.getNumberOfPages(); doc.internal.pageSize.getWidth(); getHeight();
doc.output('blob');  // → Blob

<!-- pl.js helpers (Plan 06-01) -->
pluralPL(n, forms)
pl.plurals.blad / pl.plurals.proba
pl.pdf.* / pl.overlay.* / pl.replay.*
pl.disclaimer.full
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 0 (checkpoint): Pobranie Noto Sans TTF do public/fonts/</name>
  <what-required>
    Plan wymaga pliku TTF Noto Sans Latin Extended w `public/fonts/NotoSans-Regular.ttf`.
    Plik NIE jest dostępny w repozytorium i NIE może być pobrany przez Claude (brak dostępu
    do sieci dla download binarny; oraz problem checksum verification).
  </what-required>
  <how-to-action>
    1. Otwórz https://fonts.google.com/noto/specimen/Noto+Sans
    2. Kliknij "Get font" → "Download all"
    3. Z ZIP wyciągnij `NotoSans-Regular.ttf` (lub `NotoSans/static/NotoSans-Regular.ttf`).
    4. Skopiuj do `C:\Users\mparol\Desktop\Dokumenty\Projekty\HydraulicPress\public\fonts\NotoSans-Regular.ttf`.
    5. Plik ma rozmiar 100-250 KB.
    6. Verify: `ls public/fonts/NotoSans-Regular.ttf` zwraca file.
    7. Licencja: SIL Open Font License — kompatybilna z projektem (commercial-friendly).
  </how-to-action>
  <resume-signal>Napisz "font-pobrany" lub "approved" gdy plik na miejscu.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: PdfExporter — dynamic import jspdf + Noto Sans embed + 5-sekcyjny layout</name>
  <files>src/export/PdfExporter.js, package.json, tests/pdfExporter.test.js, tests/boundaries.test.js</files>
  <action>
    1. **Install jspdf**: `npm install jspdf@^4.2.1` (zmiana w package.json + package-lock.json). Pin minor by uniknąć regression z 4.x→5.x API breaks. Slopcheck verdict OK z RESEARCH.md.

    2. **Utwórz `src/export/PdfExporter.js`**:

    Eksporty:
    ```
    export async function generatePdf(state, scenario, allAttemptsMetrics): Promise<Blob>
    export function generateFilename(scenarioId, date = new Date()): string  // pm300_raport_X_YYYYMMDD-HHMM.pdf
    export async function downloadPdf(state, scenario, allAttemptsMetrics, scenarioTitlePL): Promise<void>
    ```

    Implementation `generatePdf`:
    - `const { jsPDF } = await import('jspdf');` — dynamic import (Pitfall 4 z RESEARCH).
    - `const doc = new jsPDF({orientation:'p', unit:'mm', format:'a4'});`
    - `await _loadFont(doc)` — chunked base64 z fetch '/fonts/NotoSans-Regular.ttf'. Pattern z RESEARCH.md §Pattern 4 (chunk 0x8000, Pitfall 5).
    - Header function `_writePageHeader(doc, pageNum, scenarioTitlePL)`:
      - `doc.setFont('NotoSans', 'normal'); doc.setFontSize(18);`
      - `doc.text(pl.pdf.reportTitle, 105, 25, {align:'center'});` — 'RAPORT SESJI SZKOLENIOWEJ' (NIE 'Certyfikat')
      - `doc.setFontSize(10); doc.text(date ISO, 20, 35);`
      - `doc.text(pl.pdf.scenarioLabel + ' ' + scenarioTitlePL, 20, 41);`
      - `doc.setDrawColor(204,204,204); doc.line(20, 48, 190, 48);`
    - Sekcja 1 Podsumowanie (y od 52mm):
      - `doc.setFontSize(12); doc.text(pl.pdf.sectionSummary, 20, 52);`
      - `doc.setFontSize(10);` 4 wiersze z metryk (errorCount, completionTimeMs MM:SS, attempts.length, score).
      - Liczby mnogie przez pluralPL.
    - Sekcja 2 Lista błędów: tabela 4 kolumny (#, Czas, Krok, Powaga) z events filtrowanych do severity. Helper `_addTableRow(doc, y, columns, widths)`. Page-break helper `_ensureSpace(doc, y, lineHeight) → newY` (RESEARCH §Pattern 4). Każdy nowy page → re-emit header.
    - Sekcja 3 Pominięte kroki + naruszenia: dwie listy zwięzłe z computeMetrics.missedSteps i computeMetrics.sequenceViolations.
    - Sekcja 4 Historia prób (tylko gdy attempts.length > 1): tabela per attempt z score.
    - Footer każdej strony (loop after generation):
      - `for (let p=1; p<=doc.internal.getNumberOfPages(); p++) { doc.setPage(p); doc.setFontSize(8); doc.text(pl.disclaimer.full, 20, 277, {maxWidth:170}); doc.text(pl.pdf.appVersion, 20, 290); doc.text(pl.pdf.pageLabel(p, total), 190, 290, {align:'right'}); }`
    - `return doc.output('blob');`

    `_loadFont(doc)` — implementacja per RESEARCH §Pattern 4:
    - `const resp = await fetch('/fonts/NotoSans-Regular.ttf');`
    - Jeśli `!resp.ok` → throw `'Nie można załadować czcionki Noto Sans'` (consumer w SessionOverlay wyłapie i pokaże alert).
    - `const buf = await resp.arrayBuffer();`
    - Chunk-base64 (CHUNK=0x8000) per RESEARCH chunked base64 snippet.
    - `doc.addFileToVFS('NotoSans-Regular.ttf', b64); doc.addFont('NotoSans-Regular.ttf', 'NotoSans', 'normal'); doc.setFont('NotoSans', 'normal');`

    `downloadPdf` wrapper: try → blob = generatePdf → URL.createObjectURL → anchor click → revokeObjectURL. Catch → alert pl polish: 'Nie można załadować modulu PDF. Sprawdź połączenie.' (Pitfall 8 fallback path).

    3. **Testy** (`tests/pdfExporter.test.js`):
    - generateFilename('uruchomienie', new Date('2026-05-27T14:30:00Z')) → 'pm300_raport_uruchomienie_20260527-1430.pdf'.
    - generatePdf — mock jsPDF moduł całkowicie (`vi.mock('jspdf', () => ({ jsPDF: vi.fn().mockImplementation(() => mockDoc) }))`). Mock doc z spy'ami na addFileToVFS, addFont, setFont, text, addPage, output, internal.getNumberOfPages.
    - generatePdf wywołuje addFileToVFS z 'NotoSans-Regular.ttf' i base64 string.
    - generatePdf wywołuje setFont('NotoSans', 'normal').
    - generatePdf wywołuje doc.text('RAPORT SESJI SZKOLENIOWEJ', ...). Sanity: literał obecny w PDF.
    - generatePdf NIE wywołuje doc.text z tekstem zawierającym 'Certyfikat' (anty-AF-9 regex assertion).
    - generatePdf wywołuje doc.text z pl.disclaimer.full (footer).
    - Mock fetch globalThis: vi.stubGlobal('fetch', vi.fn(() => Promise.resolve({ok:true, arrayBuffer: () => Promise.resolve(new ArrayBuffer(1024))}))).
    - generatePdf zwraca Blob (mock doc.output('blob') → fake Blob).

    ~8 asercji.

    Boundary entry: `src/export/PdfExporter.js` → `{ mustNotImport: ['three', 'gsap', '../training/', '../state/', '../highlight/', '../ui/', '@floating-ui/dom'] }`. UWAGA: PdfExporter MOŻE importować `pl` z i18n + `computeMetrics` z `../training/ScoringService.js`? NIE — ScoringService boundary zabrania nikomu z UI go importować ALE PdfExporter też nie powinien (utility). KOMPROMIS: PdfExporter dostaje już-policzone metryki przez argument (jak w sygnaturze `allAttemptsMetrics`). Consumer w Plan 06-07 Task 2 (SessionOverlay) policzy metrics i przekaże.

    Boundary refinement: PdfExporter import: tylko `import { pl, pluralPL } from '../i18n/pl.js';`.
  </action>
  <verify>
    <automated>npm test -- tests/pdfExporter.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>8 asercji zielone. jspdf zainstalowany. PdfExporter boundary-clean. PDF zawiera 'RAPORT SESJI SZKOLENIOWEJ', NIE zawiera 'Certyfikat'.</done>
</task>

<task type="auto">
  <name>Task 2: SessionOverlay UI + StatusPanel ScenarioSelector + DOM/CSS</name>
  <files>src/ui/SessionOverlay.js, src/ui/StatusPanel.js, index.html, style.css, tests/sessionOverlay.test.js, tests/StatusPanel.test.js, tests/boundaries.test.js</files>
  <action>
    **SessionOverlay (nowa klasa)**:

    Konstruktor: `new SessionOverlay({ store, scenarios })`.

    Render warunek (subscriber): `state.session.finishedAt !== null && state.overlayOpen !== false` (overlay otwiera się automatycznie po finishedAt; user może zamknąć → `state.overlayOpen = false`, ale po retry/openReplay overlayOpen się nie zmienia explicite).

    Dorzuć do storu (`src/state/trainingStore.js`):
    - Initial `overlayOpen: false` (start sesji).
    - Akcja `closeOverlay() => set({overlayOpen: false})`.
    - Subscriber wewnętrzny (store-level): gdy `session.finishedAt` zmienia z null → not-null, `set({overlayOpen: true})`.

    DOM (per UI-SPEC §3):
    - Mount #session-overlay (style.display='none' initial).
    - Struktura: backdrop + card. Card zawiera: header (h2 title + close X), score block (28px display number + label), metrics (3 wiersze pluralPL), errors table (warunkowy gdy errorCount > 0; max-height 200px scroll gdy > 10 rows), actions (4 przyciski).

    Score variant CSS:
    - score >= 80 → `.session-overlay__score-value--good` (zielony tint).
    - score < 50 → `.session-overlay__score-value--bad` (czerwony tint).
    - inny → bez modyfikatora.

    Actions buttons:
    - "Otwórz replay" (secondary): `store.openReplay(state.session.attempts.length)` — ostatni attempt (current). UWAGA: current attempt nie jest w `attempts[]`; w Plan 06-04 ReplayDrawer.loadAttempt brał `session.attempts[idx]`. Aby replay obejmował też current finished attempt, store musi push current do attempts gdy finishSession nastąpi. WSTECZNA POPRAWKA Plan 06-02 finishSession: niech push current attempt do attempts[]:
      ```
      finishSession: () => set(s => {
        const currentAttempt = { attemptIdx: s.session.attempts.length, startedAt: s.session.startedAt, finishedAt: now(), events:[...s.events], scoring:{...s.scoring} };
        return { session: { ...s.session, attempts:[...s.session.attempts, currentAttempt], finishedAt: now() } };
      })
      ```
      To zachowuje konsystencję: po finishSession każdy "completed" attempt jest w attempts[]. Plan 06-03 retry() już push'uje current przed reset; finishSession też powinien dla last attempt. **Edytuj Plan 06-02 trainingStore.js w tym task** (cross-plan brownfield).
    - "Spróbuj ponownie" (secondary, tylko Nauka): `state.difficulty === 'nauka'` ? render : skip. Klik → `store.retry()`. RetryButton w StepPanel jest dla mid-run; ten w overlay dla post-completion.
    - "Eksportuj JSON" (primary accent): klik → `downloadJson(buildJsonPayload(store.getState(), pl.scenarios[state.session.scenarioId].title), generateFilename(state.session.scenarioId))`.
    - "Eksportuj PDF" (primary accent): klik async → `downloadPdf(...)`. Disable button + spinner podczas async.

    XSS safety: skeleton innerHTML literal, dane z storu przez textContent / replaceChildren.

    Metric row helpers: `pluralPL(errorCount, pl.plurals.blad)`, `pluralPL(attempts.length, pl.plurals.proba)`.

    **ScenarioSelector (extension StatusPanel)**:

    StatusPanel._build() rozszerz o `.scenario-selector` element z 4 buttonami (id-y z pl.scenarios keys). Pattern z UI-SPEC §1.

    Subscriber na `state.session.scenarioId` → toggle `.scenario-btn--active` na właściwym buttonie.

    Klik handler scenario-btn:
    - Jeśli `current === clicked` → no-op.
    - Jeśli `state.currentStepId === null` (nie ma aktywnej procedury) lub `state.session.finishedAt !== null` (sesja zakończona) → bezpośredni `store.startScenario(scenarios[clickedId])`.
    - Else (mid-run) → `store.openConfirmModal({current: pl.scenarios[currentId].title, next: pl.scenarios[clickedId].title})`. ConfirmModal Phase 5 (reuse) handluje confirmation + delegate do startScenario.

    UWAGA Phase 5 KeyboardController 1-4: obecnie tylko klawisz '1' = uruchomienie. Plan 06-08 (wiring) zaktualizuje KeyboardController by ładował wszystkie 4 scenariusze przy klawiszach 2/3/4. Ten plan (06-07) NIE edytuje KeyboardController — to Plan 06-08 odpowiedzialność.

    DOM/CSS:
    - index.html dorzuć `#session-overlay` z UI-SPEC §3 strukturą (PRZED #replay-drawer).
    - style.css dorzuć bloki: `.session-overlay`, `.session-overlay__backdrop`, `.session-overlay__card`, header/title/close/score-block/score-value (good/bad warianty), metrics/metric-row, errors/error-table, error-row--critical/--medium, actions. Plus `.scenario-selector`, `.scenario-btn`, `.scenario-btn--active`. Wszystko per UI-SPEC sekcje 1 i 3.

    Testy SessionOverlay (`tests/sessionOverlay.test.js`):
    - new SessionOverlay mountuje DOM, display:none initial.
    - session.finishedAt zmienia → overlayOpen=true → overlay visible.
    - Render: score-value variant 'good' dla score 95, 'bad' dla score 40.
    - errorCount > 0 → tabela errors renderowana; errorCount === 0 → noErrors message.
    - "Otwórz replay" click → store.openReplay(attemptIdx) called.
    - "Spróbuj ponownie" widoczny tylko w Nauka.
    - "Spróbuj ponownie" click → store.retry() called.
    - "Eksportuj JSON" click → downloadJson called z poprawnym payload + filename.
    - "Eksportuj PDF" click async → downloadPdf called; button disabled w trakcie loading.
    - Close X click → store.closeOverlay called (overlayOpen=false).
    - dispose cleanup.

    ~12 asercji jsdom.

    Testy StatusPanel rozszerzone (`tests/StatusPanel.test.js` — dodaj `describe('Phase 6 — scenario selector')`):
    - .scenario-selector ma 4 .scenario-btn (uruchomienie, cykl-pracy, zatrzymanie, awaria).
    - .scenario-btn--active na buttonie dla state.session.scenarioId.
    - Klik na inny scenario gdy session.finishedAt === null AND currentStepId !== null → openConfirmModal called.
    - Klik na inny gdy session.finishedAt !== null → startScenario called bezpośrednio.
    - Klik na ten sam → no-op (żaden side effect).

    ~5 asercji rozszerzające istniejące.

    Boundary entries:
    - `src/ui/SessionOverlay.js` → `{ mustNotImport: ['three', 'gsap', '../training/', '../state/', '../highlight/', '../replay/'] }`. UWAGA: SessionOverlay importuje JsonExporter, PdfExporter (z `../export/`) i ScoringService.computeMetrics — dorzuć wyjątek dla `../export/` i `../training/ScoringService.js` (ale NIE ogólny `../training/`).

    Plan-level decyzja: SessionOverlay potrzebuje computeMetrics, więc albo (a) importuje ScoringService bezpośrednio i boundary pozwala na to, albo (b) Plan 06-08 Application woła computeMetrics i wstrzykuje wynik do SessionOverlay przez DI.

    REKOMENDACJA: opcja (b) — DI. SessionOverlay konstruktor: `{store, scenarios, computeMetrics: importedScoringService.computeMetrics, jsonExporter: {build, download}, pdfExporter: {generate, download}}`. Boundary pozostaje czysty: SessionOverlay nie importuje nic z training/state/highlight/replay. Application Plan 06-08 wstrzykuje wszystko.

    Boundary entry: `{ mustNotImport: ['three', 'gsap', '../training/', '../state/', '../highlight/', '../replay/', '../export/'] }` (export też DI). Tylko `pl` z `../i18n/pl.js` + DOM.
  </action>
  <verify>
    <automated>npm test -- tests/sessionOverlay.test.js tests/StatusPanel.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>17 asercji łącznie. SessionOverlay i ScenarioSelector renderują się. Wszystkie 4 przyciski overlay funkcjonalne. Boundary entries dopisane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click "Eksportuj PDF" → fetch('/fonts/NotoSans-Regular.ttf') | trusted same-origin |
| user click "Eksportuj JSON" → Blob download | outbound; brak PII |
| PDF doc.text z state.events.errorCode | tampering — but events to ze store (controlled flow) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Tampering | PDF wygenerowany z 'Certyfikat' (np. accidental refactor literal) | mitigate | Task 1 test: assertion że doc.text NIE wywoływane z stringiem zawierającym 'Certyfikat' (regex) |
| T-06-18 | Info Disclosure | export JSON zawiera scenarioId + events — bez PII; trainee identity NIE w payload (D-Phase6-15) | accept | Schema verify in Plan 06-06 (Task 3) — payload nie ma fieldów PII |
| T-06-19 | DoS | downloadPdf wywołane wielokrotnie w trakcie loading TTF | mitigate | Button disabled atrybut + flaga `_pdfLoading` w SessionOverlay |
| T-06-20 | Tampering | fetch('/fonts/NotoSans-Regular.ttf') zwraca 404 (plik brakuje) | mitigate | _loadFont throws polish error message; downloadPdf catch + alert; Helvetica fallback NIE (zbyt brzydki dla polskich diakrytyk — fail loudly) |
| T-06-SC | Tampering | npm install jspdf (supply chain) | mitigate | slopcheck verified [OK] (RESEARCH.md); pin `^4.2.1`; pre-install legitimacy checkpoint blocking-human poniżej |
</threat_model>

<verification>
- `npm test` pełen suite zielony.
- Grep: `src/export/PdfExporter.js` zawiera literały `'RAPORT SESJI SZKOLENIOWEJ'` i NIE zawiera `'Certyfikat'` ani `'certyfikat'` (grep -v '^#' | grep -ic certyfikat == 0).
- Grep: `src/ui/SessionOverlay.js` zawiera `closeOverlay` i `downloadJson`.
- Grep: `index.html` zawiera `id="session-overlay"`.
- Grep: `style.css` zawiera `.session-overlay__card` i `.scenario-btn--active`.
- File exists: `public/fonts/NotoSans-Regular.ttf` (rozmiar > 50KB).
- `package.json` zawiera `"jspdf"` w dependencies.
</verification>

<success_criteria>
- SCORE-05 zamknięty (PDF generation działa; jednak runtime test wymaga manualnego smoke w Plan 06-08).
- SCORE-06 zamknięty (pluralPL faktycznie używany w SessionOverlay + PDF).
- Plan 06-08 (wiring) instancjuje SessionOverlay i podpina dispose chain.
- ROADMAP Phase 6 SC4 spełniony: PDF "titled 'Raport sesji szkoleniowej', carries the disclaimer in its footer, contains zero certificate-styled elements".
</success_criteria>

<output>
Napisz `.planning/phases/06-scenarios-replay-retry-export/06-07-SUMMARY.md`. Wymień:
- 2 nowe klasy (PdfExporter, SessionOverlay) + brownfield StatusPanel.
- Cross-plan edit Plan 06-02 (finishSession push current attempt do attempts[]).
- jspdf wersja zainstalowana.
- Noto Sans TTF rozmiar pliku.
- Liczba asercji per moduł.
- Test "PDF nie zawiera 'Certyfikat'" przeszedł (CRIT-1 lock confirmed).
</output>
