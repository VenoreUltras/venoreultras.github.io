---
phase: 06-scenarios-replay-retry-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/training/scenarios/validateScenario.js
  - src/training/ProcedureEngine.js
  - src/training/faultRules.js
  - src/training/scoringWeights.js
  - src/i18n/pl.js
  - tests/scenarioShape.test.js
  - tests/procedureEngine.test.js
  - tests/faultRules.test.js
  - tests/i18n.pl.test.js
autonomous: true
requirements: [SOP-04, SOP-05, SOP-06, SCORE-06]
tags: [foundation, pure-logic, i18n, validator]
must_haves:
  truths:
    - "validateScenario akceptuje bimanual i machineStateAttest jako prawidłowe step kinds (D-Phase6-04/05)"
    - "ProcedureEngine.validateStep zwraca poprawny rezultat dla nowych branchy bimanual i machineStateAttest"
    - "faultRules zawiera brak-cisnienia-oleju i awaryjne-zatrzymanie (D-Phase6-03)"
    - "pluralPL(n, forms) zwraca poprawne formy PL dla n=0/1/2/3/5/11/22 (D-Phase6-18, SCORE-06)"
    - "pl.machineState/Icons rozszerzone o 4 nowe stany (D-Phase6-05); pl.scenarios + pl.plurals + pl.overlay + pl.replay + pl.pdf istnieją z kompletem kluczy"
  artifacts:
    - path: "src/training/scenarios/validateScenario.js"
      contains: "bimanual"
    - path: "src/training/ProcedureEngine.js"
      contains: "machineStateAttest"
    - path: "src/training/faultRules.js"
      contains: "brak-cisnienia-oleju"
    - path: "src/i18n/pl.js"
      contains: "pluralPL"
  key_links:
    - from: "src/training/ProcedureEngine.js"
      to: "src/training/scenarios/validateScenario.js"
      via: "validateScenario rzuca przed dotarciem do ProcedureEngine — kontrakt wsparty testem shape"
      pattern: "VALID_KINDS"
    - from: "src/i18n/pl.js"
      to: "Intl.PluralRules"
      via: "cached _pluralRules instance"
      pattern: "Intl\\.PluralRules\\('pl-PL'\\)"
---

<objective>
Fundament Phase 6 — czyste rozszerzenia logiki: validator scenariuszy akceptuje 2 nowe step kindy
(`bimanual`, `machineStateAttest`), ProcedureEngine dostaje 2 nowe branche walidacji, faultRules
dorzuca 2 nowe reguły (`brak-cisnienia-oleju`, `awaryjne-zatrzymanie`), pl.js rozszerzony o
~50 kluczy + helper `pluralPL`. Wszystko pure, testowalne w Node bez DOM/THREE.

Purpose: Wave 1 dostarcza kontrakty, na których Wave 2 (scenariusze) i Wave 3 (store +
replay + UI) bazują. Brak tego planu = scenarioShape.test rzuca przy pierwszym imporcie
`cykl-pracy.js` (Pitfall 3).

Output: 4 pliki produkcyjne zmodyfikowane + 4 pliki testowe rozszerzone; pełen suite zielony.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scenarios-replay-retry-export/06-CONTEXT.md
@.planning/phases/06-scenarios-replay-retry-export/06-RESEARCH.md
@.planning/phases/06-scenarios-replay-retry-export/06-UI-SPEC.md

@src/training/scenarios/validateScenario.js
@src/training/ProcedureEngine.js
@src/training/faultRules.js
@src/i18n/pl.js
@tests/scenarioShape.test.js

<interfaces>
<!-- Bieżący kontrakt validateScenario (Phase 1 Plan 01-02) -->
- VALID_KINDS = new Set(['manipulation', 'visual-target', 'visual-attest'])
- VALID_EFFECT_TYPES zawiera: setMachineState, setMeshState, appendEvent, advanceStep, startSpinUpTimer, playAudio
- needsTarget = kind in ('manipulation', 'visual-target')
- visual-attest NIE może mieć targetMeshId
- step opcjonalne pola: validateBefore (function), effectsOnSuccess[], effectsOnError[]

<!-- ProcedureEngine.validateStep — 4 branche (Phase 1 Plan 01-03) -->
Branch 1: brak aktywnego kroku → {ok:false, reason:'no-active-step'}
Branch 2: forbidden-state (validateBefore false) → setStepStatus error + appendEvent + effectsOnError
Branch 3: kind matching — manipulation/visual-target = click+meshId; visual-attest = check+stepId
Branch 4: success → appendEvent step.done + effectsOnSuccess + advanceStep

<!-- faultRules — istniejący kontrakt (Phase 1 Plan 01-02) -->
type FaultRule = { id, when:(state)=>boolean, then:{effects[]}, severity }
evaluateFaultRulesData(state, rules) → effects[]; defensive try/catch wokół rule.when

<!-- ScoringService.calculate (Phase 1) — Phase 6 NIE zmienia tego (computeMetrics dorobimy w 06-06) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rozszerzenie validateScenario.js — VALID_KINDS i shape constraints dla nowych kindów</name>
  <files>src/training/scenarios/validateScenario.js, tests/scenarioShape.test.js</files>
  <behavior>
    - VALID_KINDS dorzuca 'bimanual' i 'machineStateAttest' (D-Phase6-04/05).
    - Krok kind='bimanual' MUSI mieć `targetMeshIds: string[]` długości dokładnie 2; pojedyncze `targetMeshId` jest błędem.
    - Krok kind='machineStateAttest' MUSI mieć `targetMachineState: string` (niepusty); `targetMeshId` i `targetMeshIds` zabronione.
    - Krok kind='bimanual' opcjonalnie ma pole `windowMs: number` (domyślnie 500 w ProcedureEngine; jeśli obecne, musi być number > 0).
    - Pole `rationalePL` (string, max 200 znaków) — opcjonalne dla każdego stepu, ale gdy obecne — walidowane (typeof string, length ≤ 200, throw przy naruszeniu). D-Phase6-06.
    - Wszystkie błędy w komunikatach po polsku, prefiks `Scenariusz "{id}": krok "{stepId}": ...`.
    - Test: rozszerz `scenarioShape.test.js` o 8 nowych asercji: poprawny bimanual (2 meshe), poprawny machineStateAttest, oraz 6 fail-cases (bimanual bez targetMeshIds, targetMeshIds length 1/3, machineStateAttest z targetMeshId, machineStateAttest bez targetMachineState, rationalePL > 200 znaków).
  </behavior>
  <action>
    Edytuj VALID_KINDS by zawierał `'bimanual'` i `'machineStateAttest'`. Dorzuć branch walidacji po `if (step.kind === 'visual-attest' ...)`:

    - Dla bimanual: sprawdź `Array.isArray(step.targetMeshIds) && step.targetMeshIds.length === 2 && step.targetMeshIds.every(s => typeof s === 'string' && s.length>0)`; sprawdź że `step.targetMeshId` jest undefined; sprawdź że `step.windowMs` jest undefined LUB (typeof number i > 0).
    - Dla machineStateAttest: sprawdź `typeof step.targetMachineState === 'string' && step.targetMachineState.length>0`; sprawdź że `targetMeshId` i `targetMeshIds` są undefined.
    - Po pętli per-step: jeśli `step.rationalePL !== undefined` → `typeof === 'string' && length <= 200` (D-Phase6-06).

    NIE ruszaj istniejących branchy dla manipulation/visual-target/visual-attest — to brownfield extend, nie refactor.

    Testy: dodaj `describe('Phase 6 — nowe step kindy')` z 8 asercjami w `tests/scenarioShape.test.js`. Reuse `tests/fixtures/scenario.fixture.js` jako baseline, override per case.

    NIE dodawaj `targetMeshIds` do `needsTarget` — istniejący guard pozostaje dla manipulation/visual-target.
  </action>
  <verify>
    <automated>npm test -- tests/scenarioShape.test.js</automated>
  </verify>
  <done>VALID_KINDS = 5 elementów; nowe scenariusze (Plan 06-03) importują się bez throw; 8 nowych asercji zielone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProcedureEngine — branche bimanual i machineStateAttest + faultRules +2</name>
  <files>src/training/ProcedureEngine.js, src/training/faultRules.js, tests/procedureEngine.test.js, tests/faultRules.test.js</files>
  <behavior>
    ProcedureEngine.validateStep — nowe branche (po istniejących Branch 1-4):

    - Branch 5 (bimanual): gdy `expectedStep.kind === 'bimanual'`. Intent ma kształt `{kind:'bimanual', firstMeshId, firstTimestamp, secondMeshId, secondTimestamp}`.
      - `bothPresent = expectedStep.targetMeshIds.includes(firstMeshId) && expectedStep.targetMeshIds.includes(secondMeshId) && firstMeshId !== secondMeshId`
      - `windowMs = expectedStep.windowMs ?? 500`
      - `windowOk = Math.abs(secondTimestamp - firstTimestamp) <= windowMs`
      - Sukces → standardowy zestaw effects {appendEvent step.done, ...effectsOnSuccess, advanceStep}.
      - Błąd → `setStepStatus error` + `appendEvent step.violation` z errorCode 'E-BIMANUAL-TIMEOUT' (gdy !windowOk) lub 'E-BIMANUAL-WRONG-TARGET' (gdy !bothPresent), severity 'medium'.

    - Branch 6 (machineStateAttest): gdy `expectedStep.kind === 'machineStateAttest'`. Intent ma kształt `{kind:'machineStateCheck'}` (no meshId).
      - `ok = state.machineState === expectedStep.targetMachineState`
      - Sukces → standardowy zestaw effects + advanceStep.
      - Brak match — NIE error (różnica od pozostałych branchy): zwraca `{ok:false, reason:'machine-state-not-matching', effects:[]}` (no-op, czeka na zmianę machineState).

    faultRules.js — dorzuć 2 reguły (Object.freeze zachowany):

    - `brak-cisnienia-oleju`: `when = state.meshStates?.['wziernik-smarowania'] === 'pusty' && state.machineState !== 'oczekiwanie-na-inspekcje'`. then.effects = appendEvent fault.triggered + setMachineState 'awaria-brak-oleju'. severity 'critical'.
    - `awaryjne-zatrzymanie`: `when = state.meshStates?.['estop'] === 'pressed' && state.machineState === 'w-cyklu'`. then.effects = appendEvent fault.triggered + setMachineState 'awaria'. severity 'critical'.

    Testy procedureEngine: 6 nowych asercji (bimanual happy, bimanual timeout, bimanual wrong target, machineStateAttest happy, machineStateAttest waiting, machineStateAttest po effect setMachineState target).
    Testy faultRules: 4 nowe asercji (brak-cisnienia gdy pusty + w-cyklu → fires; gdy oczekiwanie-na-inspekcje → no-fire; awaryjne-zatrzymanie gdy estop+w-cyklu → fires; gdy estop+gotowa → no-fire).
  </behavior>
  <action>
    ProcedureEngine.validateStep — dodaj branche PO branchu kind matching (po obecnej linii ~71 wrong-target), PRZED Branch 4 success:

    ```
    if (expectedStep.kind === 'bimanual') { ... }
    if (expectedStep.kind === 'machineStateAttest') { ... }
    ```

    Uważaj: bimanual i machineStateAttest NIE przechodzą przez istniejący Branch 3 (kind matching dla manipulation/visual-target/visual-attest) — wstaw nowe branche jako early-return PRZED Branchem 3 lub PRZESTRUKTURYZUJ Branch 3 na switch by handlować wszystkie 5 kindów. Wybierz strukturalne podejście: PRZED Branch 3 wstaw `if (expectedStep.kind === 'bimanual') return validateBimanual(...)` analogicznie dla machineStateAttest. To eliminuje zagnieżdżenie i zachowuje istniejący kontrakt 4-branchy w jasnej hierarchii.

    Helper functions w tym samym module (poniżej validateStep): `validateBimanual(intent, expectedStep, state, now)`, `validateMachineStateAttest(intent, expectedStep, state, now)`. Każda zwraca `{ok, reason, effects}` w identycznym kształcie co główna funkcja.

    faultRules.js — w istniejącej `Object.freeze([...])` dorzuć 2 nowe wpisy ZA `oslona-otwarta-w-cyklu`. Zachowaj wzorzec: `when` jako pure predicate, `then.effects` z appendEvent + setMachineState. Severity 'critical'.

    Testy: w `tests/procedureEngine.test.js` dodaj `describe('Phase 6 — bimanual')` i `describe('Phase 6 — machineStateAttest')`. Konstruuj minimalny scenariusz inline (1-2 steps) by izolować testy od fixture'u uruchomienie. W `tests/faultRules.test.js` dodaj `describe('Phase 6 — nowe reguły')`.
  </action>
  <verify>
    <automated>npm test -- tests/procedureEngine.test.js tests/faultRules.test.js</automated>
  </verify>
  <done>10 nowych asercji zielone. ProcedureEngine NIE importuje THREE/DOM/store/gsap (pure, granica boundaries.test.js bez zmian). faultRules.length === 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pl.js — Phase 6 keys + pluralPL helper + nowe machineState wpisy</name>
  <files>src/i18n/pl.js, tests/i18n.pl.test.js</files>
  <behavior>
    Dorzuć do eksportu `pl` (na końcu obiektu, przed `}`):

    - `pl.machineState`: 4 nowe wpisy `cykl-zakonczony` / `awaria-os-otwarta` / `awaria-brak-oleju` / `lockout` z tekstami z UI-SPEC §6.
    - `pl.machineStateIcons`: 4 nowe wpisy z emoji ✔️ / 🚨 / ⛽ / 🔒 (1:1 z machineState).
    - `pl.scenarios`: nowa sekcja z 4 wpisami `{[id]: { title }}` — uruchomienie/cykl-pracy/zatrzymanie/awaria.
    - `pl.plurals`: `{ blad: {one,few,many}, proba: {one,few,many} }` jak w UI-SPEC §Copywriting.
    - `pl.overlay`: pełen blok kluczy z UI-SPEC §Copywriting (sessionComplete, scoreLabel, openReplay, retry, exportJson, exportPdf, closeAria, errorsSectionTitle, noErrors, viewLastSession + funkcje metricErrors/metricTime/metricAttempts).
    - `pl.replay`: drawerLabel, playAria, pauseAria, speedNormal, speedSlow, closeAria, infoFormat (funkcja).
    - `pl.pdf`: reportTitle, scenarioLabel, sectionSummary/Errors/Missed/Attempts, colNum/Time/Step/Severity, severityCritical/Medium/Minor, pageLabel (funkcja), appVersion.
    - Eksport nazwany: `export function pluralPL(n, forms)` — pure, używa cached `_pluralRules = new Intl.PluralRules('pl-PL')` (module-level const). Fallback: `forms[_pluralRules.select(n)] ?? forms.many`.

    Test (tests/i18n.pl.test.js): dorzucenie ~20 asercji:
    - pl.machineState i pl.machineStateIcons mają identyczny zestaw kluczy po sortowaniu (Object.keys(...).sort()).
    - pl.scenarios zawiera 4 wpisy.
    - pluralPL(0/1/2/3/4/5/11/12/22/100, pl.plurals.blad) zwraca odpowiednio many/one/few/few/few/many/many/many/few/many.
    - pluralPL(1/2, pl.plurals.proba) zwraca 'próba' i 'próby'.
    - pl.overlay.metricErrors(3) zwraca string zawierający '3' i 'błędy'.
    - pl.pdf.reportTitle === 'RAPORT SESJI SZKOLENIOWEJ' (literal anti-certyfikat; CRIT-1 lock).
  </behavior>
  <action>
    Dodaj na końcu modułu `src/i18n/pl.js` (PO definicji `export const pl = {...}`):

    ```
    const _pluralRules = new Intl.PluralRules('pl-PL');
    export function pluralPL(n, forms) {
      return forms[_pluralRules.select(n)] ?? forms.many;
    }
    ```

    W obiekcie `pl`, ROZSZERZ `pl.machineState` o 4 wpisy i `pl.machineStateIcons` o 4 wpisy (zachowując istniejące 7). Dodaj `pl.scenarios = { ... }`, `pl.plurals = { ... }`, `pl.overlay = { ... }`, `pl.replay = { ... }`, `pl.pdf = { ... }` jako oddzielne sekcje na końcu obiektu.

    Funkcje (metricErrors, metricTime, metricAttempts, infoFormat, pageLabel) zapisz jako arrow function fields — będą wywołane przez konsumentów w Plan 06-07. metricErrors(n) używa `pluralPL(n, pl.plurals.blad)` (cykl: deklaruj pluralPL w tym samym module, więc funkcje overlay/replay/pdf mogą go używać przez lokalną referencję LUB pozostaw funkcje "leniwymi" — wykonają lookup w czasie wywołania, więc OK).

    Klucze 1:1 z UI-SPEC §Copywriting Contract (kompletna tabela ~50 wpisów). Polskie diakrytyki bez ASCII fallback ("ę", "ą", "ł", "ó", "ś", "ż", "ź", "ć", "ń").

    Test: dorzuć `describe('Phase 6 — i18n extensions')` i `describe('Phase 6 — pluralPL')`.
  </action>
  <verify>
    <automated>npm test -- tests/i18n.pl.test.js</automated>
  </verify>
  <done>20 nowych asercji zielone. pl.machineState.size === 11 (7 baseline + 4 nowe). pluralPL eksportowany; Intl.PluralRules cached na module-level (nie wewnątrz funkcji — perf).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 6 Plan 06-01 to czyste rozszerzenie pure logic; brak nowych untrusted-input boundaries. validateScenario czyta tylko statyczne moduły scenariuszy. faultRules ewaluuje store snapshot — store boundary już zabezpieczony (Phase 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | validateScenario.js — malformacja `targetMeshIds` (np. zduplikowane id) | mitigate | walidator wymusza `firstMeshId !== secondMeshId` w validateBimanual w ProcedureEngine (Task 2); validateScenario sprawdza długość 2 i typy stringów |
| T-06-02 | Denial of Service | pluralPL(n) z n=NaN/Infinity | accept | Intl.PluralRules.select rzuca dla nie-numerów; defaultowy `forms.many` fallback przez nullish coalescing minimizuje wpływ; consumer scope ograniczony do liczników błędów (≤100) |
| T-06-03 | Information Disclosure | brak | n/a | Pure logic, no I/O |
</threat_model>

<verification>
1. `npm test` — pełen suite zielony (434 baseline + ~38 nowych asercji = ~472).
2. `npm test -- tests/scenarioShape.test.js tests/procedureEngine.test.js tests/faultRules.test.js tests/i18n.pl.test.js` — wszystkie 4 pliki zielone.
3. Grep verification: `validateScenario.js` zawiera literał `'bimanual'` i `'machineStateAttest'`.
4. Grep: `ProcedureEngine.js` zawiera funkcje `validateBimanual` i `validateMachineStateAttest`.
5. Grep: `pl.js` zawiera literały `'RAPORT SESJI SZKOLENIOWEJ'`, `pluralPL`, `'cykl-zakonczony'`.
6. `tests/boundaries.test.js` — bez zmian; pure logic nie wymaga nowych entries.
</verification>

<success_criteria>
- Plan 06-02 (store) może użyć nowych branchy ProcedureEngine bez dodatkowych zmian w pure layer.
- Plan 06-03 (scenariusze) może importować cykl-pracy/zatrzymanie/awaria scenariusze i validateScenario je zaakceptuje.
- Plan 06-06 (export) może wywołać `pluralPL(n, pl.plurals.blad)` z pl.js bez dodatkowego setupu.
- Plan 06-07 (PDF/overlay) może wywołać `pl.overlay.metricErrors(n)`, `pl.pdf.reportTitle` itp. — wszystkie klucze obecne w pl.js.
- Wszystkie 11 wymagań Phase 6 nadal "Pending" — ten plan dostarcza foundation, nie zamyka żadnego wymagania samodzielnie (SCORE-06 zostanie domknięty dopiero gdy pluralPL będzie konsumowany w UI w Plan 06-07).
</success_criteria>

<output>
Po ukończeniu napisz `.planning/phases/06-scenarios-replay-retry-export/06-01-SUMMARY.md` używając szablonu summary.md. Wymień rozszerzone moduły, nowe testy (liczba), kluczowe decyzje (jeśli rozstrzygnięto coś podczas implementacji).
</output>
