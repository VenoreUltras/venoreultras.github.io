---
phase: 06-scenarios-replay-retry-export
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/training/scenarios/cykl-pracy.js
  - src/training/scenarios/zatrzymanie.js
  - src/training/scenarios/awaria.js
  - src/training/scenarios/index.js
  - tests/integration/cykl-pracy.test.js
  - tests/integration/zatrzymanie.test.js
  - tests/integration/awaria.test.js
autonomous: true
requirements: [SOP-04, SOP-05, SOP-06, TEST-05]
tags: [scenarios, sop, integration-tests]
must_haves:
  truths:
    - "cykl-pracy.js eksportuje scenariusz z 5-6 krokami, krok 5 kind='bimanual', krok 6 kind='machineStateAttest' z targetMachineState='cykl-zakonczony' (D-Phase6-01)"
    - "zatrzymanie.js eksportuje scenariusz z 5 krokami w kolejności sprzęgło→hamulec→attest omega=0→wyłącznik→attest LOTO (D-Phase6-02)"
    - "awaria.js eksportuje scenariusz z 3 krokami dydaktycznymi, initialMachineState='w-cyklu' (D-Phase6-03)"
    - "Każdy z 4 scenariuszy (uruchomienie + 3 nowe) ma integration test pokrywający happy path + ≥2 failure paths (TEST-05)"
    - "scenarios/index.js eksportuje wszystkie 4 scenariusze pod kluczami zgodnymi z pl.scenarios"
  artifacts:
    - path: "src/training/scenarios/cykl-pracy.js"
      provides: "scenario module"
      contains: "bimanual"
    - path: "src/training/scenarios/zatrzymanie.js"
      provides: "scenario module"
      contains: "machineStateAttest"
    - path: "src/training/scenarios/awaria.js"
      provides: "scenario module"
    - path: "tests/integration/cykl-pracy.test.js"
      provides: "integration test happy + 2 failure paths"
    - path: "tests/integration/zatrzymanie.test.js"
      provides: "integration test happy + 2 failure paths"
    - path: "tests/integration/awaria.test.js"
      provides: "integration test happy + 2 failure paths"
  key_links:
    - from: "cykl-pracy.js krok 5 (oburęczny-start)"
      to: "store.attemptBimanualStep"
      via: "kind=bimanual + targetMeshIds:['przycisk-start-lewy','przycisk-start-prawy']"
      pattern: "kind: 'bimanual'"
    - from: "awaria.js krok 1"
      to: "faultRules brak-cisnienia-oleju"
      via: "meshStates['wziernik-smarowania'] === 'pusty' triggers fault"
      pattern: "wziernik-smarowania"
---

<objective>
Trzy deklaratywne moduły JSON scenariuszy SOP (cykl-pracy, zatrzymanie, awaria) zgodne z
D-Phase6-01/02/03 + 3 pliki integration tests pokrywające happy path + ≥2 failure paths each
(TEST-05). Wzorzec: `uruchomienie.js` + `tests/uruchomienie.integration.test.js`.

Purpose: domykają SOP-04/05/06; uruchamiają silnik na nowych branchach z Plan 06-01/02.

Output: 4 pliki produkcyjne (3 scenariusze + index extension) + 3 pliki testów integracji.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-scenarios-replay-retry-export/06-CONTEXT.md
@.planning/phases/06-scenarios-replay-retry-export/06-RESEARCH.md

@src/training/scenarios/uruchomienie.js
@src/training/scenarios/index.js
@tests/uruchomienie.integration.test.js
@.planning/phases/06-scenarios-replay-retry-export/06-01-PLAN.md
@.planning/phases/06-scenarios-replay-retry-export/06-02-PLAN.md

<interfaces>
<!-- Wzorzec scenariusza (uruchomienie.js) — KOPIUJ -->
export default {
  id: 'string',
  titlePL: 'string',
  descriptionPL: 'string',
  initialMachineState: 'oczekiwanie-na-inspekcje' | ...,
  steps: [
    { id, kind, targetMeshId?, targetMeshIds?, targetMachineState?, windowMs?,
      labelPL, descriptionPL, rationalePL,
      validateBefore?, effectsOnSuccess?, effectsOnError? }
  ]
}

<!-- Lista istniejących meshes Phase 2 (15 sztuk; getInteractables) -->
'kolo-zamachowe', 'hamulec', 'wziernik-smarowania', 'oslona-tylna', 'kurtyna-lewa',
'kurtyna-prawa', 'tabliczka-znamionowa', 'panel-oburezny', 'przycisk-start-lewy',
'przycisk-start-prawy', 'lampka-gotowosci', 'estop', 'oslona-przednia',
'wylacznik-glowny', 'dzwignia-sprzegla'

<!-- Wzorzec integration test (uruchomienie.integration.test.js):
     - createTrainingStore({ now: vi.fn(() => 1000), scheduleTimer: ... })
     - store.startScenario(scenario)
     - sekwencja attemptStep / attemptBimanualStep z minimalnymi intent shape
     - asserty: currentStepId, steps[id].status, machineState, events.length, scoring.score -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cykl-pracy.js scenario + integration test</name>
  <files>src/training/scenarios/cykl-pracy.js, tests/integration/cykl-pracy.test.js</files>
  <action>
    Utwórz `src/training/scenarios/cykl-pracy.js` jako default export zgodny z wzorcem `uruchomienie.js`. ID='cykl-pracy', titlePL='Cykl pracy', initialMachineState='gotowa-do-pracy'. 6 kroków per D-Phase6-01:

    1. `zamknij-oslone-przednia` — kind='manipulation', targetMeshId='oslona-przednia', effectsOnSuccess `setMeshState oslona-przednia closed`. RationalePL ≤200 znaków (np. "Otwarta osłona w cyklu pracy = ryzyko amputacji ręki — gating ruchu suwaka.").
    2. `sprawdz-panel-oburezny` — kind='visual-attest', labelPL='Sprawdź panel oburęczny i lampkę gotowości'. RationalePL np. "Lampka zielona = system bezpieczeństwa gotowy; brak lampki = nie startuj.".
    3. `zaladuj-material` — kind='visual-attest', labelPL='Materiał ułożony na osi tłocznika'. RationalePL.
    4. `wyjdz-ze-strefy` — kind='visual-attest', labelPL='Obie ręce wycofane ze strefy tłocznika'. RationalePL.
    5. `oburezny-start` — kind='bimanual', targetMeshIds=['przycisk-start-lewy','przycisk-start-prawy'], windowMs=500. effectsOnSuccess: `setMachineState 'w-cyklu'`. RationalePL "Oburęczne sterowanie — obie ręce poza strefą = niemożliwe przygniecenie." (PN-EN 574 normy).
    6. `obserwuj-cykl` — kind='machineStateAttest', targetMachineState='cykl-zakonczony'. RationalePL "Czekaj na zatrzymanie wału przed kolejnym cyklem.".

    UWAGA implementacja effectsOnSuccess dla kroku 5: setMachineState 'w-cyklu' wystarczy do uruchomienia; jednak krok 6 czeka na `cykl-zakonczony`. Jak machine wskoczy z 'w-cyklu' do 'cykl-zakonczony'? OPCJE:
    - Opcja A: krok 5 emituje również effect `startSpinUpTimer` lub analog `startCycleTimer` które po ~3s ustawi 'cykl-zakonczony'.
    - Opcja B (RECOMMENDED): krok 5 effectsOnSuccess to tylko `setMachineState 'w-cyklu'` + appendEvent. Plan 06-08 doda subscriber w Application który po N sekundach w 'w-cyklu' wywoła `store.setState({machineState:'cykl-zakonczony'})` (cycle-end timer). To timer pozostaje out-of-store (Application warstwa, jak istniejący scheduleTimer dla rozpędzania).

    DECYZJA dla tego planu: użyj Opcji B. effectsOnSuccess kroku 5 = `[setMachineState 'w-cyklu', appendEvent step.done]`. Komentarz JSDoc w pliku: "Krok 6 wymaga że Application doda subscriber na machineState='w-cyklu' z 3s timer → 'cykl-zakonczony'. Wiring w Plan 06-08."

    Test (`tests/integration/cykl-pracy.test.js`):
    - Happy path: startScenario(cyklPracy) → attemptStep click oslona-przednia → 4× attemptStep check (visual-attest) → attemptBimanualStep(both, same timestamp) → setState machineState='cykl-zakonczony' (manualnie w teście — symuluje cycle timer; Plan 06-08 doda do Application) → currentStepId === null → scoring.score === 100, attempts: 0.
    - Failure 1: attemptStep z wrong meshId na kroku 1 (np. click 'kolo-zamachowe' zamiast 'oslona-przednia') → step.violation z 'E-NIEPRAWIDLOWY-MESH', severity medium → score 90.
    - Failure 2: attemptBimanualStep z firstTimestamp=0, secondTimestamp=1000 (windowMs=500 → timeout) → step.violation z errorCode='E-BIMANUAL-TIMEOUT', severity medium → score 90.

    Reuse `vi.useFakeTimers()` i fake `now()` injected do createTrainingStore. Pattern z uruchomienie.integration.test.js.
  </action>
  <verify>
    <automated>npm test -- tests/integration/cykl-pracy.test.js tests/scenarioShape.test.js</automated>
  </verify>
  <done>3 testy zielone (happy + 2 failure). validateScenario nie rzuca przy imporcie. scenarioShape baseline nadal zielony.</done>
</task>

<task type="auto">
  <name>Task 2: zatrzymanie.js + awaria.js scenarios + integration tests</name>
  <files>src/training/scenarios/zatrzymanie.js, src/training/scenarios/awaria.js, tests/integration/zatrzymanie.test.js, tests/integration/awaria.test.js</files>
  <action>
    **zatrzymanie.js**: id='zatrzymanie', titlePL='Zatrzymanie prasy', initialMachineState='w-cyklu'. 5 kroków per D-Phase6-02:

    1. `rozsprzegnij` — kind='manipulation', targetMeshId='dzwignia-sprzegla', effectsOnSuccess `setMeshState dzwignia-sprzegla disengaged`. RationalePL.
    2. `zacisnij-hamulec` — kind='manipulation', targetMeshId='hamulec', effectsOnSuccess `setMeshState hamulec engaged` + `setMachineState 'zatrzymana'`. RationalePL.
    3. `czekaj-zatrzymanie` — kind='machineStateAttest', targetMachineState='zatrzymana'. Po effect z kroku 2 machineState już === 'zatrzymana', więc store-subscriber automatycznie advansuje (Plan 06-02 Task 1). RationalePL "Czekamy aż wał całkowicie się zatrzyma — moment bezwładności koła zamachowego.".
    4. `wylacz-zasilanie` — kind='manipulation', targetMeshId='wylacznik-glowny', effectsOnSuccess `setMeshState wylacznik-glowny off`. RationalePL.
    5. `loto-attest` — kind='visual-attest', labelPL='Kłódka założona, klucz odebrany (LOTO)', effectsOnSuccess `setMachineState 'lockout'`. RationalePL "Lockout-Tagout: nawet jeśli ktoś nieuprawniony spróbuje uruchomić, brak klucza blokuje napęd.".

    **awaria.js**: id='awaria', titlePL='Awaria — reakcja operatora', initialMachineState='w-cyklu'. 3 kroki per D-Phase6-03:

    1. `reakcja-na-otwarcie-oslony` — kind='manipulation', targetMeshId='estop'. validateBefore: `(state) => state.machineState === 'awaria-os-otwarta'`. RationalePL "Otwarcie osłony w cyklu = automatyczna awaria; jedyny prawidłowy ruch to E-stop, NIE rozsprzęganie ani wyłącznik główny.". effectsOnSuccess: `setMeshState estop pressed` + `setMachineState 'awaria'`.
       - Aby ten krok mógł być aktywny, scenariusz potrzebuje pretextu wywołania faultRule. Initialny machineState='w-cyklu' + faultRule oslona-otwarta-w-cyklu wymaga że meshStates['oslona-przednia'] !== 'closed'. Domyślnie meshStates={} (puste). Aby faultRule odpalił automatycznie po startScenario, Application w Plan 06-08 lub test setupy MUSI ustawić meshStates['oslona-przednia']='open'. ALTERNATYWA: scenariusz emituje effect `setMeshState oslona-przednia open` jako część session.start.
       - DECYZJA: dorzuć do scenariusza pole `effectsOnStart: [{type:'setMeshState', meshId:'oslona-przednia', value:'open'}, {type:'appendEvent', event:{type:'session.start.faulted', faultId:'oslona-otwarta-w-cyklu', severity:'critical'}}]`. To wymaga rozszerzenia validateScenario.js i store.startScenario.

    UWAGA: `effectsOnStart` nie był w Plan 06-01 zakresie. Aby nie escalować, użyj ALTERNATYWY 2: krok 0 visual-attest "Zobacz: osłona otwarta w cyklu" z effectsOnSuccess `setMeshState oslona-przednia open`. Ale to dodaje czwarty krok, a CONTEXT mówi 3 kroki. KOMPROMIS:

    **Final decision**: Awaria scenariusz ma DOKŁADNIE 3 kroki dydaktyczne, ale awaria-os-otwarta wprowadzona jest przez initial `meshStates: {'oslona-przednia':'open'}` w startScenario. Aby store.startScenario obsłużył ten optional initial meshStates, ROZSZERZ scenario schema: opcjonalne pole `initialMeshStates: {meshId: value}` walidowane przez validateScenario.js (jeśli nie dodane wcześniej w Plan 06-01 — dodaj w THIS task jako quick extension).

    **EXTEND Plan 06-01 validateScenario** w tym task (krytyczna brama dla awaria scenariusza):
    - Dorzuć walidację pola `initialMeshStates`: opcjonalne, gdy obecne — Object o keys=string i values=string.
    - W tests/scenarioShape.test.js dodaj 1 asercję dla initialMeshStates.

    **EXTEND trainingStore.startScenario** by zaaplikował `initialMeshStates` jeśli obecne:
    ```
    meshStates: scenario.initialMeshStates ?? {},
    ```

    1. `reakcja-na-otwarcie-oslony` (jak wyżej, validateBefore zapewnia że może być wciśnięty tylko gdy machineState='awaria-os-otwarta' — który zostanie ustawiony przez faultRule po startScenario gdy meshStates['oslona-przednia']='open' i machineState='w-cyklu').
       UWAGA: istniejący faultRule `oslona-otwarta-w-cyklu` ustawia machineState='awaria' (Phase 1 lock), NIE 'awaria-os-otwarta'. ZMIENIAĆ? NIE — to wprowadziłoby breaking change. Zamiast: dorzuć NOWY faultRule 'awaria-os-otwarta-granular' w Plan 06-01 lub… OPTYMALNA DECYZJA: ZMIEŃ existing rule w Plan 06-01 Task 2 by emitował `setMachineState 'awaria-os-otwarta'`. To wymaga edycji Plan 06-01.

    **WSTECZNA POPRAWKA Plan 06-01 Task 2**: w faultRules.js zmień existing `oslona-otwarta-w-cyklu.then.effects` by drugi effect był `setMachineState 'awaria-os-otwarta'` (NIE 'awaria'). Update test w faultRules.test.js assercji która sprawdzała 'awaria' → zmień na 'awaria-os-otwarta'.

    Patrz <implementation_note> niżej dla obsługi tych cross-plan zmian.

    2. `reakcja-na-brak-oleju` — kind='manipulation', targetMeshId='dzwignia-sprzegla'. validateBefore: `(state) => state.machineState === 'awaria-brak-oleju'`. Pretext: krok poprzedni emituje `effectsOnSuccess` które resetuje meshStates['oslona-przednia']='closed' + setMeshState 'wziernik-smarowania' 'pusty' → faultRule brak-cisnienia-oleju odpala → setMachineState 'awaria-brak-oleju' → krok 2 aktywuje validateBefore.

    Aby uprościć: effectsOnSuccess kroku 1: `[{type:'setMeshState', meshId:'estop', value:'pressed'}, {type:'setMachineState', value:'oczekiwanie-na-inspekcje'}, {type:'setMeshState', meshId:'wziernik-smarowania', value:'pusty'}, {type:'setMachineState', value:'w-cyklu'}]`. Następnie faultRule brak-cisnienia-oleju odpala (machineState='w-cyklu' + meshStates['wziernik-smarowania']='pusty') → setMachineState='awaria-brak-oleju'.

    UWAGA: applyEffects iteruje przez effects sekwencyjnie, więc setMachineState 'oczekiwanie-na-inspekcje' → setMeshState wziernik 'pusty' → setMachineState 'w-cyklu' → faultRules ewaluują się PO completion attemptStep cycle (Plan 06-02 attemptStep linia 95-98). To zadziała.

    3. `reset-po-awarii` — kind='machineStateAttest', targetMachineState='oczekiwanie-na-inspekcje'. effectsOnSuccess kroku 2 ustawia machineState='oczekiwanie-na-inspekcje' bezpośrednio (np. po user-action E-stop). Hmm — pętla circular. POPRAWKA: krok 2 effectsOnSuccess = `setMachineState 'oczekiwanie-na-inspekcje'`. Wtedy krok 3 machineStateAttest auto-advansuje przez subscriber Plan 06-02.

    Awaria scenariusz jest złożony — dokumentuj wszystkie te tricks w obfitych komentarzach JSDoc w awaria.js.

    Test (`tests/integration/zatrzymanie.test.js`):
    - Happy: 5 kroków → currentStepId null → score 100.
    - Failure 1: krok 2 wrong-mesh (klik 'wylacznik-glowny' zamiast 'hamulec') → step.violation severity medium.
    - Failure 2: krok 5 attemptStep check z wrong stepId → no-op (silnie, krok visual-attest wymaga matching stepId) lub step.violation. Sprawdź dokładnie ProcedureEngine branch 3.

    Test (`tests/integration/awaria.test.js`):
    - Happy: startScenario z initialMeshStates {'oslona-przednia':'open'} → po startScenario faultRule odpala → machineState='awaria-os-otwarta' → attemptStep click estop → krok 1 done → emit effect ladujący stan brak-cisnienia-oleju → krok 2 active → attemptStep click dzwignia-sprzegla → krok 2 done → machineState='oczekiwanie-na-inspekcje' → store-subscriber machineStateAttest advansuje krok 3 → currentStepId null.

    UWAGA: po startScenario events.length === 1 (session.start). PO faultRules ewaluacja (która odpala się w attemptStep, NIE w startScenario) — faultRule nie odpali się bezpośrednio po startScenario. PROBLEM. Plan 06-08 musi dodać manualny `evaluateFaultRules` call po startScenario w Application. Lub: test wywołuje `store.getState().attemptStep({kind:'click', meshId:'estop'})` od razu po startScenario — co validateBefore zablokuje bo machineState='w-cyklu' nie === 'awaria-os-otwarta'. ZAGADKA.

    **PRAGMATYCZNE rozwiązanie**: dorzuć do trainingStore.startScenario PO `set({...})` ten kawałek: `const faultEffects = evaluateFaultRules(get(), faultRules); if (faultEffects.length>0) applyEffects(set, get, faultEffects, scheduleTimer);`. To eval-once po setup. EDYTUJ Plan 06-02 (lub zaplanuj jako kolejny task tutaj jako "Task 3 Hot-fix").

    Aby uniknąć rozlewania cross-plan, ZADANIE: W TYM PLANIE (06-03) Task 2 dorzuca tę linię do startScenario w trainingStore.js + dorzuca test do trainingStore.test.js sprawdzający że startScenario z initialMeshStates wywołującym faultRule ustawia machineState. To brownfield edit zachowujący spójność testu integracyjnego awaria.
    - Failure 1 awaria: attemptStep click 'kolo-zamachowe' zamiast 'estop' w kroku 1 → step.violation medium.
    - Failure 2 awaria: attemptStep click 'estop' PRZED tym jak machineState='awaria-os-otwarta' (zmienić initialMeshStates by nie wywoływać faultRule) → forbidden-state branch z validateBefore.
  </action>
  <verify>
    <automated>npm test -- tests/integration/zatrzymanie.test.js tests/integration/awaria.test.js tests/scenarioShape.test.js tests/trainingStore.test.js tests/faultRules.test.js</automated>
  </verify>
  <done>6 testów integracji zielone (3 happy + ~6 failure paths). validateScenario zwalidował 3 nowe scenariusze. trainingStore.startScenario ewaluuje faultRules na initial state. initialMeshStates field obsługiwany. faultRule oslona-otwarta-w-cyklu emituje 'awaria-os-otwarta' (granular).</done>
</task>

<task type="auto">
  <name>Task 3: scenarios/index.js + integration test "smoke" wszystkich 4 scenariuszy</name>
  <files>src/training/scenarios/index.js, tests/scenarios.index.test.js</files>
  <action>
    Rozszerz `src/training/scenarios/index.js` by eksportował 4 scenariusze pod kluczami zgodnymi z pl.scenarios:

    ```
    import uruchomienie from './uruchomienie.js';
    import cyklPracy from './cykl-pracy.js';
    import zatrzymanie from './zatrzymanie.js';
    import awaria from './awaria.js';

    export const scenarios = {
      'uruchomienie': uruchomienie,
      'cykl-pracy': cyklPracy,
      'zatrzymanie': zatrzymanie,
      'awaria': awaria,
    };

    export { uruchomienie, cyklPracy, zatrzymanie, awaria };
    ```

    UWAGA: zachowaj backward compatibility z istniejącym importem `import uruchomienie from './training/scenarios/uruchomienie.js'` w main.js (Phase 5 line 15) — to bezpośredni file import, NIE zostanie złamany.

    Test (`tests/scenarios.index.test.js`):
    - scenarios object ma dokładnie 4 klucze: 'uruchomienie', 'cykl-pracy', 'zatrzymanie', 'awaria'.
    - Każdy scenariusz przechodzi validateScenario bez throw.
    - Każdy scenariusz ma `scenarios[k].id === k`.
    - Każdy scenariusz ma niepuste `steps[]`.
    - Klucze scenarios object są równe (sorted) z Object.keys(pl.scenarios).sort().

    ~6 asercji.
  </action>
  <verify>
    <automated>npm test -- tests/scenarios.index.test.js</automated>
  </verify>
  <done>Test zielony. scenarios.index.js eksportuje 4 scenariusze. Plan 06-07 KeyboardController + ScenarioSelector mogą importować z jednego punktu.</done>
</task>

</tasks>

<implementation_note>
Ten plan zawiera cross-plan modyfikacje:

1. Task 2 ROZSZERZA Plan 06-01:
   - validateScenario dorzuca pole `initialMeshStates`.
   - faultRule `oslona-otwarta-w-cyklu` zmienia `setMachineState` z 'awaria' na 'awaria-os-otwarta'.
   - Test scenarioShape +1 asercja, test faultRules: zmień existing asercję na 'awaria-os-otwarta'.

2. Task 2 ROZSZERZA Plan 06-02:
   - trainingStore.startScenario obsługuje `initialMeshStates` (już opisane w schema; tylko zmień `meshStates: scenario.initialMeshStates ?? {}`).
   - trainingStore.startScenario ewaluuje faultRules PO initial set (by initialMeshStates triggujące fault rules natychmiast się rozwiało).
   - Test trainingStore: +1 asercja dla initialMeshStates faultRule chain.

Te cross-plan edits są MAŁE i lokalne. Decyzja: jeśli Plan 06-01 i 06-02 jeszcze nie zostały wykonane, ten plan dorzuca te kawałki w Task 2. Jeśli już wykonane, ten plan robi brownfield edits. Executor musi rozpoznać i zachować idempotency (sprawdzić czy zmiana już jest obecna w pliku — jeśli tak, pomiń).

REKOMENDACJA wykonania: Plan 06-01 → 06-02 → 06-03 sekwencyjnie (Wave 1 → Wave 1 → Wave 2). Cross-plan editsy w Task 2 traktuj jako "Wave 1 cleanup before Wave 2 commit".
</implementation_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenarios/*.js → validateScenario | static module load; trusted |
| awaria.js initialMeshStates → faultRules.when predicates | trusted (scenariusz to konfiguracja autora; nie user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | scenariusz cykl-pracy z niedopasowanym targetMeshIds (nieistniejący mesh) | mitigate | Plan 06-08 Application sanity check: `getInteractables().has(meshId)` for każdy targetMeshId/targetMeshIds w startScenario; warning gdy mismatch |
| T-06-08 | DoS | awaria scenariusz infinite-loop faultRule chain (np. setMeshState→faultRule→setMachineState→faultRule) | mitigate | faultRules predicate defensive try/catch; advanceStep idempotency Phase 3; Plan 06-02 finishSession idempotency guard; TEST: integration test awaria wymaga że events.length < 50 po happy path (sanity bound) |
</threat_model>

<verification>
1. `npm test` — pełen suite + nowe testy zielone.
2. `npm test tests/integration/` — wszystkie 4 pliki (uruchomienie istniejący + 3 nowe).
3. Grep: `src/training/scenarios/cykl-pracy.js` zawiera `kind: 'bimanual'` i `kind: 'machineStateAttest'`.
4. Grep: `src/training/scenarios/awaria.js` zawiera `initialMeshStates`.
5. Grep: `src/training/faultRules.js` zawiera `'awaria-os-otwarta'`.
6. Grep: `src/state/trainingStore.js` zawiera `initialMeshStates`.
7. `validateScenario` import-time check: każdy z 3 nowych scenariuszy nie rzuca przy `import x from './scenarios/X.js'`.
</verification>

<success_criteria>
- SOP-04 zamknięty (cykl-pracy w pełni grywalny, happy + failure paths covered).
- SOP-05 zamknięty (zatrzymanie w pełni grywalny).
- SOP-06 zamknięty (awaria — 3 fault events grywalne, deklaratywne w scenariuszu, faultRules consumed).
- TEST-05 częściowo zamknięty (3 z 4 scenariuszy mają integration tests; uruchomienie był z Phase 1 — łącznie 4/4).
- ROADMAP Phase 6 SC1 spełniony: "3 nowe declarative scenario JSON modules drop into `src/training/scenarios/` and play end-to-end through the existing engine without engine changes" — POPRAWKA: w tym planie ROZSZERZAMY validateScenario o initialMeshStates pole. Komunikat w SUMMARY ma to udokumentować jako brownfield edit, nie zmiana engine'u.
</success_criteria>

<output>
Po ukończeniu napisz `.planning/phases/06-scenarios-replay-retry-export/06-03-SUMMARY.md`. Dokumentuj:
- 3 utworzone scenariusze (krótki spec każdego).
- Cross-plan edits (faultRule oslona granular, initialMeshStates field, store.startScenario faultRule eval).
- Liczba integration tests + happy/failure breakdown.
- Decyzje dot. awaria scenariusza (initialMeshStates pretext, cykliczne effects).
</output>
