---
phase: 20-gate-testy-bundle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/quizModalDisplay.regression.test.js
autonomous: true
requirements: [TEST-11, TEST-12]

must_haves:
  truths:
    - "Pełny suite testów zielony (0 failed)"
    - "Main bundle < 834.98 KB baseline"
    - "Regresja widoczności modala quizu pokryta testem (source-guard [open])"
    - "Brak referencji NotoSans/FONT_URL/jspdf/html2canvas"
  artifacts:
    - path: "tests/quizModalDisplay.regression.test.js"
      provides: "Source-guard CSS: .modal-card--bhp-quiz display bramkowany [open]"
      contains: "modal-card--bhp-quiz"
---

# Phase 20 Plan 01 — Gate testów i bundle + regresja modala

Faza gate'owa milestone v1.3. Konsoliduje pokrycie testowe (TEST-11) i potwierdza
gate bundla (TEST-12). Dodaje test regresyjny dla bugu widoczności modala quizu
wykrytego w UAT fazy 19. Wykonana inline w autonomous workflow — szczegóły w SUMMARY.

## Tasks

### Task 1 — Test regresyjny widoczności modala quizu
<read_first>
- style.css (reguła .modal-card--bhp-quiz)
- src/ui/QuizController.js (logika _render show/hide)
</read_first>
<action>
Dodaj tests/quizModalDisplay.regression.test.js — source-guard na style.css: reguła
display dla .modal-card--bhp-quiz musi być bramkowana [open]; brak bare bloku z display.
</action>
<acceptance_criteria>
- npx vitest run tests/quizModalDisplay.regression.test.js → 2 passed
- npm test → 0 failed
</acceptance_criteria>

### Task 2 — Potwierdzenie gate'ów
<read_first>
- package.json
</read_first>
<action>
Potwierdź: npm test zielony; npm run build < 834.98 KB; brak NotoSans/FONT_URL w src/;
brak jspdf/html2canvas w package.json.
</action>
<acceptance_criteria>
- npm run build < 834.98 KB (faktycznie 827.96 KB)
- grep NotoSans|FONT_URL src/ → 0; grep jspdf|html2canvas package.json → 0
</acceptance_criteria>
