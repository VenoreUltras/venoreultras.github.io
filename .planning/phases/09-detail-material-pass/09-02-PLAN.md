---
phase: 09-detail-material-pass
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/PressModel.js
  - tests/PressModel.bolts-welds.phase9.test.js
autonomous: true
task_count: 3
requirements: [DEC-01]
tags: [decoration, instanced-mesh, bolts, welds, D-Phase9-02]

must_haves:
  truths:
    - "Trzy InstancedMesh nodes śrub dodane do PressModel.group: rama-podstawa (8), brackets-shaft (8), safetyPanel-mount (4) — total 20 instances"
    - "Spawy: 4 na cross-brace + 4 na bracket-łożysko = 8 osobnych Cylinder meshy (małe, nie InstancedMesh — count<10)"
    - "Wszystkie nowe meshy (3 InstancedMesh + 8 spawy) mają userData.kind='decoration' i NIE pojawiają się w getInteractables()"
    - "getInteractables().size === 15 niezmienione; getMeshDictionary().size === 15 niezmienione"
    - "InstancedMesh używają matAnchorBolt (Phase 8 baseline upgraded przez 09-01 PBR); spawy używają matBody"
    - "KIN-01 preserved: wszystkie nowe meshy direct children this.group (nie shaftAxis) — statyczne pod rotacją"
    - "Bundle <820KB hard (estimate +5-8KB)"
  artifacts:
    - path: "src/PressModel.js"
      provides: "_buildBoltsAndWelds() helper tworzący 3 InstancedMesh + 8 spawów"
      contains: "InstancedMesh"
    - path: "tests/PressModel.bolts-welds.phase9.test.js"
      provides: "Count + InstancedMesh.count + decoration kind + boundary asserts"
  key_links:
    - from: "PressModel.buildPress()"
      to: "_buildBoltsAndWelds()"
      via: "method call after existing decoration builds"
      pattern: "_buildBoltsAndWelds"
    - from: "InstancedMesh.userData.kind"
      to: "'decoration'"
      via: "assignment"
      pattern: "kind: ['\"]decoration['\"]"
---

<objective>
Industrial decoration pass: śruby (InstancedMesh dla performance — 20 śrub w 3 grupach = 3 draw calls zamiast 20) + spawy (8 małych Cylinder na łączeniach rama/bracket/łożysko). Wszystkie decorative, nie klikalne, KIN-static.

Purpose: DEC-01 z REQUIREMENTS.md — drobne meshy na kluczowych łączeniach. Industrial feel przez widoczne mocowania.
Output: PressModel.group ma +3 InstancedMesh + 8 weld Cylinders. Decoration count: 11 (Phase 8) + 3 InstancedMesh nodes + 8 weld meshes = 22 children (ale 3 z nich to InstancedMesh z 20 instances ≡ 20 wizualnych śrub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-detail-material-pass/09-CONTEXT.md
@.planning/phases/09-detail-material-pass/09-01-PLAN.md
@.planning/phases/08-press-body-expansion/08-04-SUMMARY.md
@src/PressModel.js

<interfaces>
<!-- THREE.InstancedMesh API -->
- `new THREE.InstancedMesh(geometry, material, count)` — shares geom+mat, count instances
- `instancedMesh.setMatrixAt(index, Matrix4)` — per-instance transform
- `instancedMesh.instanceMatrix.needsUpdate = true` po wszystkich setMatrixAt

<!-- Existing PressModel decoration position references (Phase 8 -- z 08-04-SUMMARY.md) -->
- Fundament: Box(6, 0.8, 4) @ (0, -0.4, 0)
- 4 anchor-bolts existing: Cylinder R=0.1 H=0.3 @ (±2.8, -0.15, ±1.8)
- Bearings: Cylinder R=0.6 H=0.8 @ (±2, 8, 0)
- Brackets: Box(0.4, 1.0, 1.0) @ (±2, 8, -0.5)
- Mid-brace: Box(4, 0.4, 0.4) @ (0, 4, -1)
- Frame columns: BoxGeometry(2, 10, 2) @ (±2, 5, -1)

<!-- Bolt geometry per D-Phase9-02 -->
- Śruba: Cylinder R=0.12 H=0.3 (large industrial M16-class)
- Material: this.matAnchorBolt (Phase 8 baseline → Phase 9 inherits PBR Grupa A jeśli ma metalness 0.8, otherwise keep current)

<!-- Weld geometry per D-Phase9-02 -->
- Spaw: Cylinder R=0.05 H=0.3 (mała "linia spawu")
- Material: this.matBody (Grupa A z 09-01, "wytopiony" look)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test suite bolts + welds (RED)</name>
  <files>tests/PressModel.bolts-welds.phase9.test.js</files>
  <behavior>
    Vitest @vitest-environment node. Importuje PressModel + mock Scene.

    Test #1 — InstancedMesh count:
    - `pressModel.group.children.filter(c => c instanceof THREE.InstancedMesh).length === 3`
    - Trzy nodes: rama-podstawa (count=8), brackets-shaft (count=8), safetyPanel-mount (count=4)
    - Total instances sum: 8+8+4 = 20

    Test #2 — Per-InstancedMesh count asserts:
    - Find by `userData.id` lub `userData.kind === 'decoration'` filter + sort by name
    - Assert każdy ma poprawny `instancedMesh.count` (8/8/4)

    Test #3 — Spawy count:
    - Filter Cylinder R=0.05 H=0.3 children → assert length === 8 (4 cross-brace + 4 bracket)

    Test #4 — Decoration kind:
    - Wszystkie 3 InstancedMesh + 8 spawy mają `userData.kind === 'decoration'`

    Test #5 — Interactables nietknięte:
    - `pressModel.getInteractables().size === 15`
    - `pressModel.getMeshDictionary().size === 15`

    Test #6 — Forbidden IDs nie w interactables:
    - `interactables.has('sruba-rama-podstawa')`, `has('spaw-bracket')`, `has('sruba-bracket')`, `has('sruba-panel')` → wszystkie false (8 wariantów)

    Test #7 — Material per group:
    - InstancedMesh.material === this.matAnchorBolt (lub equivalent metallic)
    - Weld Cylinder.material === this.matBody

    Test #8 — KIN-01 preservation:
    - Snapshot worldPosition wszystkich nowych meshy przed update(0), po update(π) — drift < 1e-6

    Test #9 — Boundary:
    - `fs.readFileSync` PressModel.js → regex `^import` → 4 matches (THREE + PhysicsEngine + i18n/pl + MaterialRegistry)

    Total: 9 testów (część z parametrized expects).
  </behavior>
  <action>Stwórz tests/PressModel.bolts-welds.phase9.test.js z 9 testami. Uruchom `npx vitest run tests/PressModel.bolts-welds.phase9.test.js` — wszystkie RED (źródło bez implementacji). Commit: `test(09-02): add bolts+welds InstancedMesh asserts (RED)`.</action>
  <verify>
    <automated>npx vitest run tests/PressModel.bolts-welds.phase9.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>9 testów RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _buildBoltsAndWelds() — 3 InstancedMesh + 8 welds (GREEN)</name>
  <files>src/PressModel.js</files>
  <behavior>
    Dodaj prywatną metodę `_buildBoltsAndWelds()` wywoływaną z buildPress() po istniejących Phase 8 decoration buildach.

    Geometry (shared):
    - `const boltGeo = new THREE.CylinderGeometry(0.12, 0.12, 0.3, 12);` (12 radial segments — performance compromise)
    - `const weldGeo = new THREE.CylinderGeometry(0.05, 0.05, 0.3, 8);`

    Grupa 1 — Śruby rama-podstawa (8 sztuk):
    - 4 narożniki: (±2.5, 0, ±1.5)
    - 4 środkowe boki: (0, 0, ±1.5), (±2.5, 0, 0)
    - InstancedMesh(boltGeo, this.matAnchorBolt, 8)
    - Per instance: Matrix4().setPosition(x, 0, z) z rotation X=π/2 (bolt head zwrócony do góry — orientacja head Y axis, ale CylinderGeometry default Y, więc bez rotacji jeśli pozycja Y na powierzchni). Use setMatrixAt(i, matrix).
    - userData: { id: 'bolts-frame-base', kind: 'decoration' }
    - Dodaj do this.group

    Grupa 2 — Śruby brackets-shaft (8 sztuk):
    - 4 na lewy bracket, 4 na prawy bracket — pozycje na licu bracket'a frontem do widza
    - Lewy: (-1.8, 7.5+offset, 0) × 4 (corners bracket'a; bracket @ (-2, 8, -0.5) Box(0.4,1,1) — corners @ (-2±0.2, 8±0.5, 0))
    - Prawy: analogicznie +2.0
    - Suggested positions (każda Z slightly w przód = 0): top-left, top-right, bottom-left, bottom-right per bracket
    - InstancedMesh count=8, matAnchorBolt
    - userData: { id: 'bolts-brackets', kind: 'decoration' }

    Grupa 3 — Śruby safetyPanel-mount (4 sztuki):
    - Locate safetyPanel pozycję (Phase 2 — sprawdź w PressModel; default panel ~(0, 2-3, 2-3) front)
    - 4 corners panel mount frame
    - InstancedMesh count=4, matAnchorBolt
    - userData: { id: 'bolts-safety-panel', kind: 'decoration' }

    Spawy (8 osobnych Cylinder, NIE InstancedMesh bo count < 10):
    - 4 na mid-brace ↔ kolumna (po 2 z każdej strony, gdzie mid-brace łączy się z lewą/prawą kolumną). Mid-brace @ (0,4,-1) ends at x=±2. Spawy @ (±1.95, 4±0.2, -1).
    - 4 na bracket ↔ łożysko (po 2 per bracket). Bracket @ (±2, 8, -0.5), bearing @ (±2, 8, 0). Spawy @ (±2±0.1, 8±0.2, -0.25).
    - Każdy: `new THREE.Mesh(weldGeo, this.matBody)`, userData: { id: `spaw-${idx}`, kind: 'decoration' }
    - Dodaj do this.group osobno

    Wywołanie:
    - W buildPress(), po istniejących Phase 8 decoration buildach (po _buildBearingBrackets/foundation/worktable), dodaj `this._buildBoltsAndWelds();`

    Verify: testy 1-9 powinny PASS.
  </behavior>
  <action>Implementuj `_buildBoltsAndWelds()` w PressModel.js per behavior. Wywołaj z buildPress(). Uruchom `npx vitest run tests/PressModel.bolts-welds.phase9.test.js` — oczekuj 9/9 PASS. Następnie pełny suite `npx vitest run` — oczekuj 720+17+9=746/746 PASS (zero regresji). Sprawdź też że Phase 8 integration test #1 (`count === 11` decoration filtered by Box/Cyl signatures) NADAL PASS — InstancedMesh ma inną geometry signature (instanceMatrix), nie powinien się dopisać do listy 11. Jeśli wpadnie do count: update Phase 8 test #1 do `>= 11 && treat InstancedMesh osobno` lub ekskluduj InstancedMesh z Phase 8 signature inventory (test #1 Phase 8 czyta children.filter(c => c.userData?.kind === 'decoration') — InstancedMesh tu wpadnie i policzy się jako +3, naruszając Phase 8 == 11 invariant). **Critical: musisz zaktualizować tests/PressModel.phase8.integration.test.js test #1 z `=== 11` na `>= 11` LUB filtruj InstancedMesh — wybierz: zaktualizuj test #1 Phase 8 do filter z dodatkowym `!c.isInstancedMesh` warunkiem, zachowując semantykę "tylko regular Mesh decoration".** Commit: `feat(09-02): bolts InstancedMesh (3 groups, 20 instances) + 8 welds (DEC-01 GREEN)`.</action>
  <verify>
    <automated>npx vitest run tests/PressModel.bolts-welds.phase9.test.js 2>&1 | tail -3 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <done>9/9 bolts-welds testów PASS, pełny suite 746/746 PASS (Phase 8 test #1 zaktualizowany z `!isInstancedMesh` filter komentarzem dlaczego — Phase 9 wprowadza InstancedMesh osobnym audit testem #1 w bolts-welds).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bundle gate + final regression sweep</name>
  <files>tests/PressModel.bolts-welds.phase9.test.js</files>
  <behavior>
    Run `npm run build` — sprawdź main bundle:
    - Pre-09-02 baseline (po 09-01): ~773 KB (estimated +1-2KB z 09-01)
    - Post-09-02 expected: ~778-781 KB (+5-8 KB delta z InstancedMesh + 8 welds; shared geometry oszczędza vs 20 osobnych meshy)
    - Hard gate: < 800 KB (jeszcze 09-03 cables i 09-04 highlight backup będą dodawać)

    Dodaj komentarz / stub test #10 do tests/PressModel.bolts-welds.phase9.test.js:
    - `it('build budget — main bundle < 800 KB checked externally via npm run build', () => { expect(true).toBe(true); });`
    - Komentarz w pliku: konkretne wartości po build (zapisz w SUMMARY).
  </behavior>
  <action>Uruchom `npm run build`. Zapisz output bundle size do task notes. Dodaj test #10 stub do bolts-welds.test.js z komentarzem aktualnego rozmiaru. Final sweep: `npx vitest run` → 747/747 PASS. Commit: `chore(09-02): bundle verify <800KB (DEC-01 complete)`.</action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(main|index).*kB" && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <done>Bundle < 800 KB potwierdzone, 747/747 testów PASS.</done>
</task>

</tasks>

<verification>
- `npx vitest run` → 747/747 PASS
- `npm run build` → main bundle < 800 KB (room dla 09-03 + 09-04)
- `grep -c "InstancedMesh" src/PressModel.js` → ≥ 3 (3 grupy)
- `grep -c "kind: 'decoration'" src/PressModel.js` → ≥ 10 (8 Phase 8 + spawy + InstancedMesh)
- `grep -c "^import " src/PressModel.js` → 4 (boundary preserved)
</verification>

<success_criteria>
1. 3 InstancedMesh dodane: rama-podstawa (8 instances), brackets-shaft (8), safetyPanel-mount (4) — total 20 śrub jako 3 draw calls
2. 8 spawów (Cylinder R=0.05) na cross-brace i bracket-łożysko
3. Wszystkie nowe meshy decoration, nie klikalne, KIN-static
4. getInteractables().size === 15, getMeshDictionary().size === 15 niezmienione
5. Bundle < 800 KB
6. 747/747 testów PASS (720 baseline + 17 09-01 + 9+1 09-02)
</success_criteria>

<output>
Stwórz `.planning/phases/09-detail-material-pass/09-02-SUMMARY.md`.
</output>
