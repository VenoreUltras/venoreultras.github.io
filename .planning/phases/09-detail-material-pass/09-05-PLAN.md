---
phase: 09-detail-material-pass
plan: 05
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-04]
files_modified:
  - tests/PressModel.phase9.integration.test.js
autonomous: true
task_count: 2
requirements: [TEST-06, TEST-07, TEST-08, MAT-04, DEC-01, DEC-02]
tags: [integration, phase-close, milestone-close, build-budget, regression, D-Phase9-06, D-Phase9-07]

must_haves:
  truths:
    - "Single integration test file aggreguje wszystkie Phase 9 invariants (PBR per grupa, InstancedMesh, kable, pre-flash backup, bundle, getInteractables count)"
    - "npm run build main bundle < 850 KB HARD gate (D-Phase9-06)"
    - "Łączny test count >= 720 baseline + 33 Phase 9 (17 materials + 9+1 bolts-welds + 8 cables + 8 preflash) = 763 PASS"
    - "getInteractables().size === 15 niezmienione (Phase 2 baseline kept przez całą fazę)"
    - "Boundary D-Phase7-05 preserved (PressModel.js 4 imports; EmissiveController.js 2 imports)"
    - "Phase 9 + Milestone v1.1 oba COMPLETE po merge tego planu"
  artifacts:
    - path: "tests/PressModel.phase9.integration.test.js"
      provides: "Aggregate audit suite — 10+ testów weryfikujących cross-plan invariants Phase 9"
  key_links:
    - from: "Plany 09-01..09-04 zmergeowane"
      to: "Phase 9 close + v1.1 milestone close"
      via: "integration test gate"
      pattern: "PASS"
---

<objective>
Plan zamykający Phase 9 + Milestone v1.1. Integracyjny test suite weryfikujący że plany 09-01 (PBR), 09-02 (śruby+spawy), 09-03 (kable), 09-04 (pre-flash backup) zmergeowane razem spełniają wszystkie Phase 9 success criteria + hard bundle gate < 850 KB.

Purpose: Single gate przed Phase 9 close + Milestone v1.1 close. Wzorowane na Plan 08-04 integration audit pattern.
Output: tests/PressModel.phase9.integration.test.js + final bundle verification + ROADMAP/STATE update note dla orchestratora.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-detail-material-pass/09-CONTEXT.md
@.planning/phases/09-detail-material-pass/09-01-PLAN.md
@.planning/phases/09-detail-material-pass/09-02-PLAN.md
@.planning/phases/09-detail-material-pass/09-03-PLAN.md
@.planning/phases/09-detail-material-pass/09-04-PLAN.md
@.planning/phases/08-press-body-expansion/08-04-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test suite Phase 9 aggregate</name>
  <files>tests/PressModel.phase9.integration.test.js</files>
  <action>
    Stwórz tests/PressModel.phase9.integration.test.js (Vitest @vitest-environment node). 10+ testów aggregate audit:

    1. **PBR Grupa A homogeneous** — iteruj [matBody, matShaft, matEccentric, matSlider, matFlywheel, matBrakeSteel]; assert dla każdego `metalness === 0.8 && roughness === 0.5 && color.getHex() === 0x4a4a4a` (parametrized 6 asercji w jednym teście dla compact reporting).
    2. **PBR Grupa B homogeneous + BHP** — [matSafetyPanelGray, matSwitchBody, matGuardOrange, matGuardRearBlack]; assert `metalness === 0.1 && roughness === 0.85`; dodatkowo `matGuardOrange.color.getHex() === 0xC8B400` (BHP yellow).
    3. **PBR Grupa C concrete** — matFoundation.metalness===0, roughness===0.95, color===0x808080, normalMap instanceof DataTexture, normalScale.x===0.3 && normalScale.y===0.3.
    4. **DataTexture w MaterialRegistry** — `materialRegistry._textures` ma entry dla concrete normal.
    5. **InstancedMesh count === 3** + sum instances === 20 (8+8+4 śruby).
    6. **8 spawów obecne** — filter Cylinder R=0.05 H=0.3 z group.children === 8.
    7. **Kable obecne** — `kabel-pneumatyczny` (TubeGeometry) + `kabel-estop-segment-*` (3-4 Box segments) — assert obecność i kind='decoration'.
    8. **Interactables niezmienione** — `getInteractables().size === 15`, `getMeshDictionary().size === 15`.
    9. **Boundary preserved** — `fs.readFileSync('src/PressModel.js')` + regex `/^import .+ from/gm` → 4 imports; `fs.readFileSync('src/highlight/EmissiveController.js')` → 2 imports (THREE + gsap).
    10. **KIN-01 dla Phase 9 decoration** — snapshot worldPosition wszystkich Phase 9-added children (InstancedMesh + welds + cables) przed update(0), po update(π) → drift < 1e-6.
    11. **Forbidden Phase 9 IDs nie w interactables** — `bolts-frame-base`, `bolts-brackets`, `bolts-safety-panel`, `spaw-*`, `kabel-pneumatyczny`, `kabel-estop-segment-*` → wszystkie `interactables.has(id) === false`.
    12. **EmissiveController._preFlashBackups infrastructure** — import EmissiveController, mock interactables, assert `instance._preFlashBackups instanceof Map` i methods `_savePreFlash`/`_restorePreFlash` istnieją.
    13. **Build budget HARD gate stub** — `it('build budget < 850 KB checked externally via npm run build', () => { expect(true).toBe(true); });` z komentarzem aktualnej wartości po Task 2 sweep.

    Commit: `test(09-05): Phase 9 integration audit (PBR + bolts + cables + preflash + boundary + KIN-01)`.
  </action>
  <verify>
    <automated>npx vitest run tests/PressModel.phase9.integration.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>13 integration testów napisanych, wszystkie PASS (testy weryfikatorskie po merge 09-01..09-04 — nie TDD).</done>
</task>

<task type="auto">
  <name>Task 2: Final bundle gate < 850 KB + milestone close summary</name>
  <files>tests/PressModel.phase9.integration.test.js</files>
  <action>
    1. Uruchom `npm run build`. Capture main bundle size. Hard gate < 850 KB.
    2. Update komentarz w test #13 z konkretnym KB value (np. "Phase 9 close: 823.4 KB main — headroom 26.6 KB do 850 KB v1.1 final budget").
    3. Final test sweep: `npx vitest run` — oczekuj ≥ 763/763 PASS (720 Phase 8 baseline + 17 PBR + 9+1 bolts + 8 cables + 8 preflash + 13 integration = ~776/776; faktyczna liczba zależy od finalnej decyzji o liczbie testów w 09-02/03 z poprawkami Phase 8 #1).
    4. Jeśli bundle > 850 KB: STOP — wróć do orchestrator z raportem o overrunie, zgłoś defer DEC-02 concrete normalMap (D-Phase9-06 fallback). Jeśli < 850 KB → close phase.
    5. Sprawdź ROADMAP.md Phase 9 success criteria (7 items):
       1. ✅ Decorative draw call delta < 20 (3 InstancedMesh + 8 welds + 5 cables = ~16 nowych draw calls, instancing oszczędza 17)
       2. ✅ Kable obecne (TubeGeometry + box segments)
       3. ✅ PBR per grupa (Grupa A/B/C)
       4. ✅ HighlightManager pre-flash MaterialState backup pełen
       5. ✅ 642+ tests zielone (faktycznie 776+)
       6. ✅ Bundle < 850 KB
       7. Manual smoke 60 FPS — DEFERRED do user manual smoke (executor headless, brak WebGL)
    6. Commit: `chore(09-05): Phase 9 close + v1.1 milestone close (bundle XYZ KB, 776/776 tests)`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "kB" && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <done>npm run build success + main bundle < 850 KB; pełny suite GREEN; bundle KB value zapisana w teście #13 i SUMMARY; ROADMAP Phase 9 success criteria 1-6 spełnione (7 deferred do manual smoke).</done>
</task>

</tasks>

<verification>
- `npx vitest run` → ≥ 776/776 PASS (zero regresji)
- `npm run build` → main bundle < 850 KB (HARD gate D-Phase9-06)
- `grep -c "^import " src/PressModel.js` → 4 (boundary)
- `grep -c "^import " src/highlight/EmissiveController.js` → 2 (boundary)
- `getInteractables().size === 15` (zaszczepione w integration test #8)
- 3 InstancedMesh + 20 instances + 8 spawy + 5 kable + DataTexture concrete + pre-flash backup infrastructure — wszystkie weryfikowane w jednym test file
</verification>

<success_criteria>
1. Integration test file 13 testów PASS — single audit gate Phase 9
2. npm run build main bundle < 850 KB (D-Phase9-06 hard gate met)
3. Pełny test suite ≥ 776/776 PASS (zero regresji v1.0 642 + Phase 7+8 78 + Phase 9 ~35)
4. Boundary preserved (PressModel 4 imports, EmissiveController 2 imports)
5. getInteractables().size === 15 niezmienione przez całą Phase 9
6. ROADMAP Phase 9 success criteria 1-6 met (7 deferred do manual smoke)
7. **Phase 9 ZAMKNIĘTA + Milestone v1.1 ZAMKNIĘTY** (wszystkie 18 v1.1 requirements DONE: KIN×3 + ANCHOR×3 + GEO×5 + DEC×2 + MAT×4 + TEST×3)
</success_criteria>

<output>
Stwórz `.planning/phases/09-detail-material-pass/09-05-SUMMARY.md` + milestone close note: zaktualizuj `.planning/ROADMAP.md` Phase 9 status z `[ ]` na `✅` + zaznacz v1.1 milestone jako shipped. STATE.md update (jeśli pattern z poprzednich faz). Powiadom orchestratora o milestone close — sugeruj `/gsd-new-milestone v2` lub manual smoke session.
</output>
