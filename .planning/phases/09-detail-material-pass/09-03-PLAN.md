---
phase: 09-detail-material-pass
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/PressModel.js
  - tests/PressModel.cables.phase9.test.js
autonomous: true
task_count: 2
requirements: [DEC-02]
tags: [decoration, cables, tube-geometry, catmull-rom, D-Phase9-03]

must_haves:
  truths:
    - "Kabel pneumatyczny panel-oburezny→rama: TubeGeometry z CatmullRomCurve3 (32 segments, radius 0.05, czarny matCable)"
    - "Kabel E-stop→rama: 3-4 Box segmenty w łuku, czarny matCable"
    - "matCable to MeshBasicMaterial color 0x0a0a0a (performance — bez PBR)"
    - "Wszystkie kable mają userData.kind='decoration', NIE klikalne"
    - "getInteractables().size === 15 niezmienione"
    - "Bundle <830KB hard"
    - "KIN-01 preserved (kable direct children this.group, statyczne)"
  artifacts:
    - path: "src/PressModel.js"
      provides: "_buildCables() helper, matCable material, kabel-pneumatyczny TubeGeometry, kabel-estop box segments"
      contains: "CatmullRomCurve3"
    - path: "tests/PressModel.cables.phase9.test.js"
      provides: "Cable presence + geometry signature + decoration + boundary asserts"
  key_links:
    - from: "TubeGeometry kabel-pneumatyczny"
      to: "CatmullRomCurve3"
      via: "constructor curve arg"
      pattern: "TubeGeometry.*Curve"
    - from: "matCable"
      to: "MeshBasicMaterial 0x0a0a0a"
      via: "constructor (NIE MeshStandardMaterial — performance)"
      pattern: "MeshBasicMaterial.*0x0a0a0a"
---

<objective>
Dodaj kable / przewody pneumatyczne jako wizualną sugestię infrastruktury między panelem oburęcznym a ramą + E-stop a ramą. Per D-Phase9-03: mix TubeGeometry (gruby kabel pneumatyczny — wizualnie ważny) + Box segmenty (cienki kabel E-stop — performance saver). Materiał czarny MeshBasicMaterial (kable nie potrzebują PBR — matowy czarny wystarczy).

SEQUENCE z 09-02: oba plany modyfikują PressModel.js → file overlap → Wave 3.

Purpose: DEC-02 z REQUIREMENTS.md — sugestia okablowania bez modelowania faktycznego rozkładu instalacji.
Output: 2 kable widoczne w scenie, ~5 nowych meshy (1 TubeGeometry + ~4 Box segments E-stop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-detail-material-pass/09-CONTEXT.md
@.planning/phases/09-detail-material-pass/09-02-PLAN.md
@src/PressModel.js

<interfaces>
<!-- THREE.CatmullRomCurve3 + TubeGeometry -->
- `new THREE.CatmullRomCurve3([Vector3, Vector3, ...])` — smooth curve through control points
- `new THREE.TubeGeometry(curve, tubularSegments, radius, radialSegments, closed)`
  - per D-Phase9-03: tubularSegments=32, radius=0.05, radialSegments=8, closed=false

<!-- Pozycje endpoints (lookup w PressModel — sprawdź w runtime/source) -->
- Panel oburezny (mesh id 'panel-oburezny'): pozycja TBD (sprawdź getInteractables lub buildPress source)
- E-stop (mesh id 'estop'): pozycja TBD
- Rama (target attachment): np. lewa kolumna front face @ (-2, 4, 0) lub przy ramce dolnej @ (-1.5, 1, 0.5)

<!-- MeshBasicMaterial — NIE MeshStandardMaterial -->
- `new THREE.MeshBasicMaterial({ color: 0x0a0a0a })` — bez PBR, bez świateł, najtańszy shader
- Rationale per D-Phase9-03: kable matowy czarny, brak shading important
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cables tests (RED) + matCable material</name>
  <files>tests/PressModel.cables.phase9.test.js, src/PressModel.js</files>
  <behavior>
    **Tests** (tests/PressModel.cables.phase9.test.js):

    Test #1 — matCable istnieje:
    - `pressModel.matCable instanceof THREE.MeshBasicMaterial`
    - `pressModel.matCable.color.getHex() === 0x0a0a0a`

    Test #2 — Kabel pneumatyczny (TubeGeometry):
    - Filter children: znajdź mesh z `userData.id === 'kabel-pneumatyczny'`
    - `mesh.geometry instanceof THREE.TubeGeometry`
    - `mesh.material === pressModel.matCable`
    - `mesh.userData.kind === 'decoration'`

    Test #3 — Kabel E-stop (Box segments):
    - Filter children: `userData.id` matching `/kabel-estop-segment-\d/` LUB `userData.id === 'kabel-estop'` (jeśli grupowane)
    - Count between 3 i 4 segments per D-Phase9-03
    - Każdy material === matCable, kind === 'decoration'

    Test #4 — Total cable mesh count:
    - Cables w hierarchii: 1 TubeGeometry + 3-4 Box = 4-5 nowych meshy
    - `pressModel.group.children.filter(c => c.userData?.id?.startsWith('kabel-'))` length 4 lub 5

    Test #5 — Interactables nietknięte:
    - `pressModel.getInteractables().size === 15`
    - `interactables.has('kabel-pneumatyczny') === false`
    - `interactables.has('kabel-estop') === false`

    Test #6 — KIN-01 (sample 2 random cable meshes, snapshot pozycja przed/po update(π), drift < 1e-6)

    Test #7 — Boundary:
    - 4 imports w PressModel.js (THREE wystarcza dla TubeGeometry/CatmullRomCurve3)

    Test #8 — Material economy:
    - Kabel meshy używają MeshBasicMaterial (NIE MeshStandardMaterial) — sprawdź `instanceof THREE.MeshBasicMaterial`

    Total: 8 testów. Uruchom — wszystkie RED.

    **Implementation step 1** (matCable only):
    - W buildMaterials() dodaj: `this.matCable = new THREE.MeshBasicMaterial({ color: 0x0a0a0a });`
    - Po dodaniu — test #1 PASS, reszta RED.
  </behavior>
  <action>Stwórz tests/PressModel.cables.phase9.test.js z 8 testami. Dodaj `matCable` w buildMaterials() (rejestracja w MaterialRegistry przez trackMaterial nie wymagana — wystarczy że dispose path PressModel pokrywa via group traversal lub explicit dispose; verify w 09-05 final integration). Uruchom `npx vitest run tests/PressModel.cables.phase9.test.js` — oczekuj 1 PASS (#1) + 7 RED. Commit: `test(09-03): cables tests (RED) + matCable MeshBasicMaterial`.</action>
  <verify>
    <automated>npx vitest run tests/PressModel.cables.phase9.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>matCable utworzony, 8 testów napisanych, 1 PASS + 7 RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _buildCables() — TubeGeometry pneumatyczny + Box segments E-stop (GREEN)</name>
  <files>src/PressModel.js</files>
  <behavior>
    Dodaj `_buildCables()` wywoływane z buildPress() po _buildBoltsAndWelds().

    **Krok A — Identify endpoints:**
    - Przeczytaj source PressModel.js (lub użyj this.X position fields). Panel oburezny i E-stop są w hierarchii. Endpoint na ramie: użyj front-bottom kolumny lewej: ~(-2, 1.5, 0.5).
    - Jeśli panel-oburezny pozycja w runtime jest np. (1.5, 3, 1.5), E-stop może być (-1.5, 3.5, 1.5) (uściśl z PressModel source — to są Phase 2 baseline pozycje).

    **Krok B — Kabel pneumatyczny (TubeGeometry):**
    ```
    const controlPoints = [
      new THREE.Vector3(panelX, panelY, panelZ),     // start: panel oburezny back
      new THREE.Vector3(panelX, panelY - 1, panelZ - 0.5),  // sag down
      new THREE.Vector3(midX, frameY + 0.5, frameZ),  // approach frame
      new THREE.Vector3(frameX, frameY, frameZ),     // end: rama mount
    ];
    const curve = new THREE.CatmullRomCurve3(controlPoints);
    const tubeGeo = new THREE.TubeGeometry(curve, 32, 0.05, 8, false);
    const tubeMesh = new THREE.Mesh(tubeGeo, this.matCable);
    tubeMesh.userData = { id: 'kabel-pneumatyczny', kind: 'decoration' };
    this.group.add(tubeMesh);
    ```

    **Krok C — Kabel E-stop (4 Box segments w łuku):**
    - Endpoints: estop pos → rama lewa front
    - 4 small Box segments: każdy `BoxGeometry(0.04, 0.04, segmentLen)` rotated/positioned wzdłuż łuku
    - Interpoluj 5 punktów (start + 3 mid + end), tworząc 4 segmenty
    - Każdy: `new THREE.Mesh(segmentGeo, this.matCable)`, userData `{ id: 'kabel-estop-segment-${i}', kind: 'decoration' }`
    - Dodaj do this.group (NIE grupuj w Object3D — flat dla prostszego raycaster ignore i KIN-01)

    **Krok D — Verify:**
    - `npx vitest run tests/PressModel.cables.phase9.test.js` → 8/8 PASS
    - Pełny suite: `npx vitest run` → 747 (po 09-02) + 8 = 755/755 PASS
    - `npm run build` → bundle < 830 KB (estimate +15-20 KB z TubeGeometry buffer + 4 Box buffers; TubeGeometry samples generuje sporo vertices ale buffer raczej kompresuje OK)
    - Phase 8 integration test #1 (count === 11) — kable wpadają do `userData.kind === 'decoration'` filter! Zaktualizuj Phase 8 test #1 zgodnie z 09-02 wcześniej (już dodany `!isInstancedMesh` filter) → teraz dodaj filtr `!c.userData?.id?.startsWith('kabel-')` lub po prostu zmień invariant na `>= 11` z komentarzem "Phase 9 dodaje InstancedMesh + cables osobnymi auditami".
  </behavior>
  <action>Zaimplementuj `_buildCables()`. Update Phase 8 integration test #1 invariant: filter dodatkowo wyklucza `c.userData?.id?.startsWith('kabel-')` LUB zmień asercję na `>= 11` z komentarzem. Preferowane: jawne filtrowanie żeby Phase 8 invariant pozostał `=== 11 regular decoration meshes` (nie InstancedMesh, nie cables). Uruchom pełny suite. Commit: `feat(09-03): pneumatic TubeGeometry cable + E-stop box segments (DEC-02 GREEN)`.</action>
  <verify>
    <automated>npx vitest run tests/PressModel.cables.phase9.test.js 2>&1 | tail -3 && npx vitest run 2>&1 | tail -3 && npm run build 2>&1 | grep -E "kB"</automated>
  </verify>
  <done>8/8 cables tests PASS, 755/755 pełny suite, bundle < 830 KB.</done>
</task>

</tasks>

<verification>
- `npx vitest run` → 755/755 PASS
- `npm run build` → main bundle < 830 KB
- `grep "TubeGeometry" src/PressModel.js` → 1 match
- `grep "CatmullRomCurve3" src/PressModel.js` → 1 match
- `grep -c "kabel-" src/PressModel.js` → ≥ 2 (pneumatyczny + estop segments)
- `grep -c "^import " src/PressModel.js` → 4 (boundary preserved)
</verification>

<success_criteria>
1. Kabel pneumatyczny TubeGeometry między panelem oburęcznym a ramą (32 segments, radius 0.05, czarny)
2. Kabel E-stop 3-4 Box segments w łuku, czarny
3. matCable = MeshBasicMaterial 0x0a0a0a (performance — bez PBR)
4. Wszystkie cable meshy decoration + KIN-static
5. getInteractables().size === 15 niezmienione
6. Bundle < 830 KB
7. 755/755 testów PASS
</success_criteria>

<output>
Stwórz `.planning/phases/09-detail-material-pass/09-03-SUMMARY.md`.
</output>
