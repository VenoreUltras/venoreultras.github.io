---
phase: 09-detail-material-pass
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/highlight/EmissiveController.js
  - tests/EmissiveController.preflash.phase9.test.js
autonomous: true
task_count: 2
requirements: [MAT-04]
tags: [highlight, emissive, pre-flash-backup, pbr-compat, D-Phase9-05]

must_haves:
  truths:
    - "EmissiveController._savePreFlash(mesh) backupuje pełny MaterialState: color (hex), emissive (hex), metalness, roughness"
    - "EmissiveController._restorePreFlash(mesh) przywraca color, emissive, metalness, roughness do wartości pre-flash"
    - "Po flash + restore: material.metalness i material.roughness są BIT-EXACT identyczne z wartościami przed flash (PBR Grupa A/B/C z 09-01 nie zostaje zmieniony)"
    - "Hover layer i hint layer NIE wymagają pełnego backup (one tylko piszą emissive — pre-flash to state-flash specific)"
    - "Wszystkie istniejące HighlightManager.test.js testy (24 testy) NADAL PASS — semantyka emissive bez zmian"
    - "Boundary EmissiveController preserved: imports tylko THREE + gsap"
  artifacts:
    - path: "src/highlight/EmissiveController.js"
      provides: "_savePreFlash + _restorePreFlash methods, _preFlashBackups Map"
      contains: "_savePreFlash"
    - path: "tests/EmissiveController.preflash.phase9.test.js"
      provides: "Pre-flash backup capture + restore asserts dla pełnego MaterialState"
  key_links:
    - from: "EmissiveController.setLayer('state', mesh, { flash: true })"
      to: "_savePreFlash(mesh) before gsap timeline"
      via: "explicit call w _applyTopLayer flash branch"
      pattern: "_savePreFlash"
    - from: "gsap timeline onComplete / clearLayer('state', mesh)"
      to: "_restorePreFlash(mesh)"
      via: "callback chain"
      pattern: "_restorePreFlash"
---

<objective>
Rozszerz EmissiveController pre-flash backup tak aby zapamiętywał pełen MaterialState (color + emissive + metalness + roughness) przed flash animation i przywracał wszystkie 4 pola po. Phase 4 baseline backupuje (implicitly przez gsap target=emissiveIntensity i hardcoded BASELINE_HEX) tylko emissive — Phase 9 PBR per grupa wprowadza zróżnicowane metalness/roughness które mogą być modyfikowane runtime przez przyszłe rozszerzenia flash → backup musi je objąć.

Note: 09-CONTEXT referuje "HighlightManager.js" jako target — w rzeczywistości flash logic mieszka w `src/highlight/EmissiveController.js`. Plan adresuje EmissiveController.

PARALLEL z 09-02 i 09-03 (zero file overlap — EmissiveController.js osobno od PressModel.js). Wave 2.

Purpose: MAT-04 z REQUIREMENTS.md — materiały zachowują compat z HighlightManager (Phase 4); pre-flash MaterialState backup pełen.
Output: EmissiveController z `_preFlashBackups` Map + `_savePreFlash`/`_restorePreFlash` methods + 8 testów asercjujących backup/restore round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-detail-material-pass/09-CONTEXT.md
@src/highlight/EmissiveController.js
@tests/HighlightManager.test.js
@CLAUDE.md

<interfaces>
<!-- Existing EmissiveController API (src/highlight/EmissiveController.js linie 31-152) -->

Constructor: `new EmissiveController({ interactables })` — Map<id, Mesh>
- `_meshes: Mesh[]` — snapshot z interactables.values()
- `_layers: Map<Mesh, {hover, hint, state}>` — slots
- `_timelines: Map<Mesh, gsap.Timeline>` — active animations

`setLayer(layerName, mesh, params)`:
- layerName: 'hover' | 'hint' | 'state'
- params for flash: `{ color: hex, flash: true }`

`clearLayer(layerName, mesh)` — resets slot, recomputes top

`_applyTopLayer(mesh)`:
- state > hint > hover > baseline
- flash branch (linie 111-116): gsap timeline 0→FLASH_PEAK→0 w 0.8s na material.emissiveIntensity
- restore baseline branch (linie 130-134): emissive.setHex(0x000000), emissiveIntensity = 0

<!-- Critical Phase 9 insight -->
Phase 4 baseline NIE backupuje material.color/metalness/roughness bo flash nigdy ich nie modyfikował (tylko emissive intensity). Phase 9 PBR per grupa (09-01) wprowadza zróżnicowane metalness/roughness, ale flash JESZCZE ich nie modyfikuje. D-Phase9-05 jest **defensywnym backup** — zabezpiecza że przyszłe rozszerzenia flash mogą mutować PBR fields bez ryzyka leak'u do baseline. Implementation: dodaj infrastructure (Map + save + restore methods), wywoływaj save w flash branch i restore w clearLayer / timeline.onComplete. **Save MUSI być wywołany PRZED gsap timeline tworzenia — żeby snapshot capture pre-flash state nawet jeśli następnie gsap modyfikuje pola.**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-flash backup tests (RED)</name>
  <files>tests/EmissiveController.preflash.phase9.test.js</files>
  <behavior>
    Vitest @vitest-environment node. Importuje EmissiveController, THREE.

    Helper `makeMesh(id, { metalness = 0.5, roughness = 0.5, color = 0x444444 } = {})`:
    - Tworzy MeshStandardMaterial z explicit metalness/roughness/color
    - Tworzy Mesh z BoxGeometry(1,1,1)
    - mesh.userData = { id, kind: 'manipulation' }

    Test #PF1 — _savePreFlash istnieje (API surface):
    - `typeof emissive._savePreFlash === 'function'`
    - `typeof emissive._restorePreFlash === 'function'`
    - `emissive._preFlashBackups instanceof Map`

    Test #PF2 — _savePreFlash captures pełen state:
    - Mesh z metalness=0.8, roughness=0.5, color=0x4a4a4a, emissive=0x000000
    - Wywołaj `emissive._savePreFlash(mesh)`
    - `emissive._preFlashBackups.get(mesh)` zawiera `{ color: 0x4a4a4a, emissive: 0x000000, metalness: 0.8, roughness: 0.5 }`

    Test #PF3 — _restorePreFlash bit-exact restore:
    - Mesh z metalness=0.8, roughness=0.5, color=0x4a4a4a
    - Save → mutuj wszystko: `mesh.material.metalness = 0.0; mesh.material.roughness = 0.99; mesh.material.color.setHex(0xFF0000); mesh.material.emissive.setHex(0xD55E00);`
    - Restore → assert metalness === 0.8, roughness === 0.5, color.getHex() === 0x4a4a4a, emissive.getHex() === 0x000000

    Test #PF4 — Flash flow integration:
    - Mesh z PBR Grupa A (metalness 0.8 / roughness 0.5 / color 0x4a4a4a)
    - `emissive.setLayer('state', mesh, { color: 0xD55E00, flash: true })`
    - Sprawdź że `_preFlashBackups.get(mesh)` istnieje po setLayer (snapshot taken)
    - Symulacja "flash zakończony" przez `emissive.clearLayer('state', mesh)`
    - Po clearLayer: metalness === 0.8, roughness === 0.5 (PBR preserved)

    Test #PF5 — Rapid retry (re-flash before completion):
    - setLayer flash, _savePreFlash captures original
    - setLayer flash ponownie (override) — _savePreFlash NIE powinien overwrite original snapshot (rapid retry safety)
    - Strategy: save tylko jeśli `!_preFlashBackups.has(mesh)` (idempotent save until restore)
    - Assert: po dwóch setLayer + jeden clearLayer → restore wraca do ORIGINAL pre-pierwszego-flash state

    Test #PF6 — clearLayer non-state nie triggeruje restore:
    - setLayer flash na state → save captured
    - setLayer hint → save niemodyfikowany
    - clearLayer('hint', mesh) → backup nadal istnieje (hint layer nie dotyka state-backup)

    Test #PF7 — Backup cleanup po restore:
    - Po _restorePreFlash → `_preFlashBackups.has(mesh) === false` (cleared żeby kolejny flash mógł snapshot fresh state)

    Test #PF8 — Dispose czyści backupy:
    - emissive.dispose() → `_preFlashBackups.size === 0`

    Total: 8 testów. Uruchom — wszystkie RED (API jeszcze nie istnieje).
  </behavior>
  <action>Stwórz tests/EmissiveController.preflash.phase9.test.js z 8 testami. Uruchom `npx vitest run tests/EmissiveController.preflash.phase9.test.js` — oczekuj 8 RED. Commit: `test(09-04): pre-flash backup MaterialState tests (RED)`.</action>
  <verify>
    <automated>npx vitest run tests/EmissiveController.preflash.phase9.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>8 testów RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _savePreFlash + _restorePreFlash + integration w _applyTopLayer (GREEN)</name>
  <files>src/highlight/EmissiveController.js</files>
  <behavior>
    **Dodaj infrastructure:**

    W constructor: `this._preFlashBackups = new Map();  // Map<Mesh, {color, emissive, metalness, roughness}>`

    **_savePreFlash(mesh) method:**
    ```
    _savePreFlash(mesh) {
      if (!mesh.material || !mesh.material.emissive) return;
      // Idempotent — nie overwrite jeśli już jest backup (rapid retry safety, Test #PF5)
      if (this._preFlashBackups.has(mesh)) return;
      const m = mesh.material;
      this._preFlashBackups.set(mesh, {
        color: m.color ? m.color.getHex() : 0x000000,
        emissive: m.emissive.getHex(),
        metalness: typeof m.metalness === 'number' ? m.metalness : 0,
        roughness: typeof m.roughness === 'number' ? m.roughness : 1,
      });
    }
    ```

    **_restorePreFlash(mesh) method:**
    ```
    _restorePreFlash(mesh) {
      const backup = this._preFlashBackups.get(mesh);
      if (!backup) return;
      const m = mesh.material;
      if (m.color) m.color.setHex(backup.color);
      if (m.emissive) m.emissive.setHex(backup.emissive);
      if (typeof m.metalness === 'number') m.metalness = backup.metalness;
      if (typeof m.roughness === 'number') m.roughness = backup.roughness;
      this._preFlashBackups.delete(mesh);  // cleared po restore (Test #PF7)
    }
    ```

    **Integration w _applyTopLayer (modify existing):**

    Flash branch (linie 111-116) — przed gsap timeline:
    ```
    } else if (slot.state.flash) {
      this._savePreFlash(mesh);  // <-- NEW: capture pre-flash state
      const newTl = gsap.timeline({
        onComplete: () => this._restorePreFlash(mesh),  // <-- NEW: restore po flash końcu
      })
        .to(mesh.material, { emissiveIntensity: FLASH_PEAK, duration: FLASH_RISE_S, ease: 'power1.in' })
        .to(mesh.material, { emissiveIntensity: 0, duration: FLASH_FALL_S, ease: 'power2.out' });
      this._timelines.set(mesh, newTl);
    }
    ```

    Baseline branch (linie 130-134) — gdy clearLayer('state') powoduje powrót do baseline:
    ```
    } else {
      // baseline
      this._restorePreFlash(mesh);  // <-- NEW: restore PBR fields jeśli był pre-flash backup
      mesh.material.emissive.setHex(BASELINE_HEX);
      mesh.material.emissiveIntensity = BASELINE_INTENSITY;
    }
    ```

    **Update dispose():**
    ```
    dispose() {
      for (const tl of this._timelines.values()) tl.kill();
      this._timelines.clear();
      for (const mesh of this._meshes) { /* existing */ }
      this._layers.clear();
      this._preFlashBackups.clear();  // <-- NEW
    }
    ```

    Verify: testy 8/8 PF PASS. **Critical regression check:** pełny suite `npx vitest run` musi pokazać że istniejące 24 HighlightManager.test.js testy NADAL PASS — pre-flash backup infrastructure NIE może zmienić observable behavior dla baseline (color/metalness/roughness NIE są mutated by flash, więc save+restore to no-op dla wartości; tylko snapshot+cleanup różnica). Test #PF7 zapewnia że backup jest cleared po restore żeby kolejne setLayer flash mogły fresh snapshot.

    Verify też że `npm run build` bundle < 840 KB (delta < 1 KB — tylko Map + 2 methods).
  </behavior>
  <action>Implementuj `_savePreFlash`, `_restorePreFlash`, integration w `_applyTopLayer` flash i baseline branches, cleanup w dispose. Uruchom `npx vitest run tests/EmissiveController.preflash.phase9.test.js` → 8/8 PASS. Pełny suite `npx vitest run` → 755+8=763/763 PASS (po 09-03). `npm run build` → < 840 KB. Commit: `feat(09-04): pre-flash MaterialState backup (color+emissive+metalness+roughness) (MAT-04 GREEN)`.</action>
  <verify>
    <automated>npx vitest run tests/EmissiveController.preflash.phase9.test.js 2>&1 | tail -3 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <done>8/8 PF testów PASS, 763/763 pełny suite, zero regresji HighlightManager.test.js (24 testów Phase 4/5 PASS), bundle < 840 KB.</done>
</task>

</tasks>

<verification>
- `npx vitest run` → 763/763 PASS (lub 763 jeśli 09-04 idzie po 09-03; jeśli parallel z 09-02/03 — niezależnie 720+17+8=745)
- `npm run build` → < 840 KB
- `grep "_savePreFlash" src/highlight/EmissiveController.js` → ≥ 3 matches (definition + call w flash + call w baseline branch lub onComplete)
- `grep "_preFlashBackups" src/highlight/EmissiveController.js` → ≥ 4 matches (constructor + save + restore + dispose)
- `grep -c "^import " src/highlight/EmissiveController.js` → 2 (THREE + gsap — boundary preserved)
</verification>

<success_criteria>
1. EmissiveController._savePreFlash captures color+emissive+metalness+roughness do Map
2. _restorePreFlash bit-exact przywraca wszystkie 4 pola + czyści backup entry
3. Flash flow: setLayer('state', flash:true) → save → animate → onComplete:restore
4. Baseline flow: clearLayer('state') → baseline branch → restore (jeśli backup istnieje)
5. Idempotent save (rapid retry safety)
6. Dispose czyści _preFlashBackups Map
7. 24 istniejących HighlightManager.test.js testów PASS (zero regresji)
8. 8 nowych pre-flash testów PASS
9. Bundle < 840 KB
</success_criteria>

<output>
Stwórz `.planning/phases/09-detail-material-pass/09-04-SUMMARY.md`.
</output>
