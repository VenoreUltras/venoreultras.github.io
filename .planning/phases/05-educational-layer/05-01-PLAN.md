---
phase: 05-educational-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/trainingStore.js
  - src/i18n/pl.js
  - index.html
  - style.css
  - package.json
  - tests/trainingStore.test.js
  - tests/i18n.pl.test.js
autonomous: true
requirements: [EDU-01, EDU-02, INTERACT-06, UI-04]
tags: [phase-5, foundation, store, i18n, css, dependencies]

must_haves:
  truths:
    - "Store posiada 6 pól (difficulty, freeRoam, activeModal, _confirmPayload, audioMuted, labelsVisible) i 8 akcji zgodnych z D-Phase5-01..28"
    - "pl.keymap (11 wpisów) i pl.modals (help + confirmScenarioSwitch) są jedynym źródłem polskich stringów dla skrótów i modali"
    - "index.html zawiera #label-overlay-container (z-index:8) i #modal-container (z-index:300) z pointer-events:none na kontenerach"
    - "style.css zawiera klasy Phase 5 (.tooltip, .label-3d, .modal-overlay, .modal-card, .difficulty-badge, .free-roam-indicator, .step-item__rationale)"
    - "@floating-ui/dom ~1.7.6 zainstalowane (devDeps unchanged; dependencies extended)"
  artifacts:
    - path: "src/state/trainingStore.js"
      provides: "Phase 5 store fields + actions"
      contains: "difficulty: 'nauka'"
    - path: "src/i18n/pl.js"
      provides: "pl.keymap + pl.modals + pl.ui.difficulty*"
      contains: "keymap:"
    - path: "index.html"
      provides: "DOM mount points dla LabelOverlay + Modal"
      contains: "label-overlay-container"
    - path: "package.json"
      provides: "@floating-ui/dom dependency"
      contains: "@floating-ui/dom"
  key_links:
    - from: "package.json"
      to: "@floating-ui/dom"
      via: "dependencies"
      pattern: "@floating-ui/dom.*1\\.7"
    - from: "src/state/trainingStore.js"
      to: "Plan 05-02..07 controllers"
      via: "store.getState()/subscribe"
      pattern: "difficulty.*nauka"
---

<objective>
Fundament Phase 5: rozszerzenie trainingStore o 5 ortogonalnych flag dydaktycznych + akcje, rozszerzenie pl.js o keymap/modals/badges, dorzucenie DOM mount points (label-overlay-container, modal-container) do index.html, dorzucenie kompletu klas CSS Phase 5 do style.css, instalacja @floating-ui/dom. Wszystkie kolejne plany (05-02..07) konsumują artefakty z tego planu.

Purpose: Bez tego fundamentu żaden controller Phase 5 nie ma jak czytać difficulty/freeRoam, żaden modal nie ma kontenera, żaden tooltip nie ma stylów ani biblioteki pozycjonującej.
Output: Store gotowy do subskrypcji, i18n gotowy do konsumpcji, DOM gotowy do montowania, CSS gotowy do klas Phase 5, dep zainstalowany.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@src/state/trainingStore.js
@src/i18n/pl.js
@index.html
@src/training/scenarios/uruchomienie.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rozszerz trainingStore o 5 pól + 8 akcji Phase 5</name>
  <read_first>
    - src/state/trainingStore.js (cały — wzór initial state + akcje)
    - tests/trainingStore.test.js (cały — pattern asercji)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-01..07, D-Phase5-10, D-Phase5-18)
    - .planning/phases/05-educational-layer/05-RESEARCH.md (sekcja "Store extension — trainingStore z Phase 5 polami" linie 769-799)
  </read_first>
  <behavior>
    - Test 1: createTrainingStore() initial state ma difficulty==='nauka', freeRoam===false, activeModal===null, audioMuted===false, labelsVisible===false (D-Phase5-04).
    - Test 2: setDifficulty('egzamin') → state.difficulty==='egzamin'; setDifficulty('nauka') → 'nauka'.
    - Test 3: toggleFreeRoam() flipuje state.freeRoam (false→true→false).
    - Test 4: toggleHelp() z null → 'help'; ponowne wywołanie z 'help' → null (toggle).
    - Test 5: closeModal() z 'help' → null; z 'confirm-scenario-switch' → null; z null → null (no-op).
    - Test 6: openConfirmModal({current,next}) → activeModal==='confirm-scenario-switch'; state._confirmPayload zachowany.
    - Test 7: toggleMute() flipuje audioMuted (D-Phase5-18). Akcja NIE pisze do localStorage (persist warstwa = Application bootstrap, analog hcOutlineMode D-Phase4-09).
    - Test 8: toggleLabels() flipuje labelsVisible.
    - Test 9: resetScenario() — gdy activeScenario null → no-op; gdy ustawione → wywołuje startScenario(activeScenario) (state.steps zresetowane do 'pending', currentStepId do pierwszego, scoring.score===100).
    - Test 10: subscribeWithSelector na każdym z 5 nowych slice'ów wywołuje callback PRZY zmianie (nie przy każdej akcji innego pola — sanity dla fine-grained sub Phase 4 pattern).
  </behavior>
  <action>
    Dorzucić w `createTrainingStore` initial state PO `hcOutlineMode: false` (linia 46 trainingStore.js) pięć pól zgodnie z D-Phase5-01..18: `difficulty: 'nauka'`, `freeRoam: false`, `activeModal: null`, `audioMuted: false`, `labelsVisible: false`, oraz `_confirmPayload: null` (internal, dla ConfirmModal payload). Dorzucić 8 akcji w returnowanym obiekcie store (po `_onSpinUpComplete`): `setDifficulty`, `toggleFreeRoam`, `toggleHelp`, `closeModal`, `openConfirmModal`, `toggleMute`, `toggleLabels`, `resetScenario`. `resetScenario` wewnętrznie woła `get().startScenario(get().activeScenario)` jeśli activeScenario truthy. Boundary: store NIE może importować localStorage ani DOM — persist robi Application (Plan 05-07, analog hcOutlineMode Plan 04-06).

    Komentarze JSDoc po polsku, identyfikatory ENG. Polskie literały TYLKO w komentarzach (boundary scanner blokuje stringi z diakrytykami w `src/` poza `i18n/`+`scenarios/`).

    Plik testu: dorzucić nowy describe `Phase 5 — flagi dydaktyczne (D-Phase5-01..18)` do `tests/trainingStore.test.js` z 10 asercjami opisanymi w `<behavior>`. RED najpierw, GREEN po dorzuceniu kodu.
  </action>
  <verify>
    <automated>npm test -- --run tests/trainingStore.test.js</automated>
  </verify>
  <done>
    - 10 nowych asercji Phase 5 PASS w tests/trainingStore.test.js.
    - Cały suite 267+10 testów green (nic z Phase 1-4 nie złamane).
    - boundaries.test.js dla src/state/trainingStore.js zostaje green (brak nowych zakazanych importów).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rozszerz pl.js o keymap + modals + difficulty/free-roam labels</name>
  <read_first>
    - src/i18n/pl.js (cały — wzór struktur pl.ui/pl.machineState)
    - tests/i18n.pl.test.js (cały — pattern shape asercji)
    - .planning/phases/05-educational-layer/05-UI-SPEC.md sekcja "Copywriting Contract" (linie 363-405) i "Keymap — 11 wpisow" (linie 389-403)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-19, D-Phase5-23, D-Phase5-24)
    - .planning/phases/05-educational-layer/05-PATTERNS.md sekcja "Brownfield: src/i18n/pl.js" (linie 636-688)
  </read_first>
  <behavior>
    - Test 1: pl.keymap jest Array o długości 11 (R/T/1/2/3/4/Space/Esc/H/L/M — UI-SPEC wskazuje 11, korekta z D-Phase5-19).
    - Test 2: każdy wpis pl.keymap ma kształt {key: string, descriptionPL: string, group: 'sterowanie'|'tryby'|'pomoc'}.
    - Test 3: pl.keymap zawiera klucze (lista oczekiwana): R, T, 1, 2, 3, 4, Space, Esc, H, L, M.
    - Test 4: pl.modals.help zawiera title, sectionKeymap, sectionColors, sectionIcons, sectionDisclaimer, keyHeader, actionHeader, groupHeader, colorError, colorSuccess, colorHint, colorHC (12 kluczy łącznie — keyHeader/actionHeader/groupHeader są nagłówkami tabeli keymap w HelpModal Plan 05-03 Task 2).
    - Test 5: pl.modals.confirmScenarioSwitch zawiera title (string), body (function — `typeof === 'function'`), confirm (string), cancel (string). body('uruchomienie','awaria') zwraca string zawierający obie nazwy.
    - Test 6: pl.modals.closeAria === 'Zamknij'.
    - Test 7: pl.ui zawiera difficultyNauka, difficultyEgzamin, freeRoamActive, setDifficultyNauka, setDifficultyEgzamin (5 nowych kluczy).
    - Test 8: pl.ui.difficultyNauka === '📚 Nauka'; pl.ui.difficultyEgzamin === '📝 Egzamin'; pl.ui.freeRoamActive === '🆓 Tryb wolny'.
  </behavior>
  <action>
    Dorzucić do obiektu `pl` w `src/i18n/pl.js` (po `pl.parts`):
    1. `keymap` — Array 11 wpisów zgodnie z UI-SPEC §"Keymap — 11 wpisow" linie 389-403 (wartości descriptionPL skopiowane DOKŁADNIE z UI-SPEC, group ∈ {sterowanie, tryby, pomoc}).
    2. `modals` — obiekt `{closeAria, help, confirmScenarioSwitch}`. `help` zawiera 12 kluczy: title, 4 section* (sectionKeymap/sectionColors/sectionIcons/sectionDisclaimer), 3 header* (keyHeader: "Klawisz", actionHeader: "Akcja", groupHeader: "Grupa" — nagłówki tabeli keymap renderowane przez HelpModal Plan 05-03 Task 2; dorzucone TU by uniknąć defensywnego patcha w Plan 05-03), 4 color* (colorError/colorSuccess/colorHint/colorHC). `confirmScenarioSwitch` zawiera title, `body: (current, next) => 'Przerwiesz postęp w "' + current + '". Załadować "' + next + '"?'`, confirm, cancel — wartości jak w UI-SPEC linie 374-377.
    3. Dorzucić do `pl.ui` (po `freeModeAria`) 5 nowych kluczy: difficultyNauka, difficultyEgzamin, freeRoamActive, setDifficultyNauka, setDifficultyEgzamin (UI-SPEC linie 378-382 + 05-PATTERNS.md linie 681-685).

    NIE dorzucać `pl.interactableDescriptions` — RESEARCH §1042 + 05-PATTERNS.md linia 688 potwierdzają, że TooltipManager czyta `pl.parts[id].description` (już istnieje, 15 wpisów verified).

    Plik testu: rozszerzyć `tests/i18n.pl.test.js` o describe `Phase 5 — keymap + modals + difficulty labels` z 8 asercjami z `<behavior>`. RED → GREEN.
  </action>
  <verify>
    <automated>npm test -- --run tests/i18n.pl.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>
    - 8 nowych asercji pl.js shape PASS.
    - boundaries.test.js zielony (i18n jest allowed-path, dorzucone polskie literały OK).
    - pl.keymap.length === 11; pl.modals.help to obiekt; pl.modals.confirmScenarioSwitch.body to funkcja.
  </done>
</task>

<task type="auto">
  <name>Task 3: Instalacja @floating-ui/dom + DOM mount points + Phase 5 CSS</name>
  <read_first>
    - package.json (verify cur deps + GSAP pin pattern)
    - index.html (cały — UI layer struktura, gdzie wstawić kontenery)
    - style.css (przeczytaj ostatnie 100 linii — sekcja Phase 4 → na końcu dorzucimy Phase 5 blok)
    - .planning/phases/05-educational-layer/05-UI-SPEC.md sekcje "DOM Mount Points" (linie 458-477) + "CSS — Nowe klasy" (linie 480-521) + spacing/typography/color (linie 49-122)
    - .planning/phases/05-educational-layer/05-RESEARCH.md sekcja "Package Legitimacy Audit" (linie 152-163) + "Pitfall 4" (linie 645-651)
  </read_first>
  <action>
    1. `npm install @floating-ui/dom@~1.7.6` (pin minor zgodnie z Phase Z policy + D-Phase5-27). Po instalacji zweryfikować że `package.json` ma `"@floating-ui/dom": "~1.7.6"` w `dependencies` (NIE devDependencies — runtime dep).

    2. W `index.html` dorzucić DOKŁADNIE jako UI-SPEC §458-474:
       - PRZED `<div id="ui-layer">` (sibling #three-canvas, z-index:8): `<div id="label-overlay-container" style="position:absolute;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:8;"></div>`
       - PRZED `<script type="module">` (po `</div>` zamykającym ui-layer; z-index:300): `<div id="modal-container" style="position:fixed;inset:0;z-index:300;pointer-events:none;"></div>`

    3. W `style.css` dorzucić NA KOŃCU pliku nowy blok z komentarzem-headerem zgodnie z UI-SPEC §480-490:
       ```
       /* ──────────────────────────────────────────────────────────
          Phase 5 — educational-layer (UI-03, FEEDBACK-06, INTERACT-06, EDU-01..03)
          ────────────────────────────────────────────────────────── */
       ```
       Klasy do zaimplementowania (pełen zestaw z UI-SPEC §492-521 table; każda klasa zgodnie ze szczegółowymi wymiarami z sekcji 1-6 UI-SPEC):
       - `.tooltip` + `.tooltip--visible` + `.tooltip--hidden` (UI-SPEC §126-156: max-width 240px, padding 8px 12px, border-radius 8px, font 14px/400/1.5, bg rgba(16,24,39,0.92) + backdrop-filter blur 8px, border rgba(255,255,255,0.15), opacity transition 120ms ease-out)
       - `.label-3d` (UI-SPEC §161-181: font 13px/600/1.2, padding 4px 8px, border-radius 4px, bg rgba(11,15,25,0.78) + blur 4px, white-space nowrap, pointer-events none, user-select none)
       - `.modal-overlay` + `.modal-overlay--visible` (UI-SPEC §209-213: position fixed inset 0, bg rgba(11,15,25,0.85), z-index 300)
       - `.modal-card` + `.modal-card--confirm` (UI-SPEC §214-225 + 502: position fixed translate(-50%,-50%), z-index 301, width min(640px,92vw), max-height 80vh, overflow-y auto, bg var(--glass-bg) + blur 12px, border var(--glass-border), border-radius 16px, padding 24px, box-shadow 0 25px 50px -12px rgba(0,0,0,0.6); confirm wariant max-width 440px)
       - `.modal-card__header` + `.modal-card__title` + `.modal-card__close` + `.modal-card__body` + `.modal-card__actions` (UI-SPEC §228-244: header flex space-between + border-bottom; title 16px/600; close 44x44 WCAG + focus-visible outline 2px var(--accent-primary); body padding default; actions flex justify-end gap 12px margin-top 24px)
       - `.help-section` + `.help-section__heading` (UI-SPEC §502-509 listed in tabeli; heading 13px/600, sekcja border-bottom 1px var(--glass-border) padding-bottom 16px margin-bottom 16px)
       - `.keymap-table` + `.keymap-table kbd` (UI-SPEC §249-254: kbd font 13px monospace, padding 2px 6px, border 1px var(--glass-border), border-radius 4px, bg rgba(255,255,255,0.05))
       - `.color-legend` + `.color-swatch` (UI-SPEC §256-263: swatch inline-block 16x16px border-radius 3px border rgba(255,255,255,0.2))
       - `.icon-legend` (lista emoji + opis 14px)
       - `.disclaimer-repeat` (UI-SPEC §289-294: font 13px/400 var(--text-muted), bg rgba(230,159,0,0.08), border-left 3px solid #E69F00, padding 12px 16px)
       - `.difficulty-badge` + `.difficulty-badge--nauka` + `.difficulty-badge--egzamin` (UI-SPEC §326-334: pill inline-flex gap 4px padding 4px 8px border-radius 12px font 13px/600 + variant border/bg/text Wong colors)
       - `.free-roam-indicator` (UI-SPEC §336-341: zolty wariant; ukrycie visibility:hidden width:0 overflow:hidden gdy nieaktywny — nie display:none by uniknąć reflowu)
       - `.step-item__rationale` (UI-SPEC §350-358: font 14px/400/1.5 var(--text-muted), margin-top 4px, padding-left 8px, border-left 2px solid rgba(240,228,66,0.4), font-style italic)

    Wszystkie kolory hardkodowane lokalnie w blokach Phase 5 (NIE dodawać nowych custom properties do `:root` — UI-SPEC §44-45). Wszystkie odstępy z 8-point grid.
  </action>
  <verify>
    <automated>npm test -- --run tests/boundaries.test.js &amp;&amp; node -e "const pkg=require('./package.json'); if(!pkg.dependencies['@floating-ui/dom']) process.exit(1)" &amp;&amp; grep -q "label-overlay-container" index.html &amp;&amp; grep -q "modal-container" index.html &amp;&amp; grep -q "step-item__rationale" style.css &amp;&amp; grep -q "modal-card" style.css &amp;&amp; grep -q "difficulty-badge" style.css</automated>
  </verify>
  <done>
    - @floating-ui/dom ~1.7.6 obecny w package.json dependencies; node_modules ma zainstalowany pakiet.
    - index.html zawiera #label-overlay-container i #modal-container jako siblingi z poprawnym z-indeksami + pointer-events:none.
    - style.css zawiera komplet klas Phase 5 (>= 20 nowych klas z UI-SPEC §492-521 table).
    - boundaries.test.js i pełny suite zielone (nic nie złamane).
    - Polski-literal scanner zielony (style.css/HTML nie skanowane; pl.js zostaje single source).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → store actions | KeyboardController + DOM clicks emitują akcje (Plan 05-03+); store akceptuje tylko whitelist akcji |
| localStorage → store bootstrap | Application odczytuje pm300:difficulty:v1 i pm300:audio-mute:v1 (Plan 05-07); store NIE zna localStorage |
| npm registry → @floating-ui/dom | nowa runtime dep — slopcheck [OK] verified w 05-RESEARCH.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | npm install @floating-ui/dom | mitigate | Package Legitimacy Audit w 05-RESEARCH.md potwierdza [OK]: ~3.5 lata na rynku, >10M downloads/tydz, brak postinstall scripts, github.com/floating-ui/floating-ui (oficjalne repo). Pin `~1.7.6` blokuje minor bumpy. NIE wymaga blocking-human checkpoint (verdict [OK], nie [ASSUMED]/[SUS]). |
| T-05-01-XSS | Tampering | pl.modals.confirmScenarioSwitch.body(current, next) — string interpolation z user-niezależnymi nazwami scenariuszy | accept | Argumenty `current`/`next` pochodzą z `pl.scenarios.{id}.titlePL` (statyczne dane z pl.js i scenario JSON, nie user input). Konsumpcja w HelpModal/ConfirmModal przez textContent (Plan 05-03), nie innerHTML — XSS-safe by construction. |
| T-05-01-INFO | Information Disclosure | localStorage `pm300:difficulty:v1` i `pm300:audio-mute:v1` | accept | Non-sensitive user preferences (analog hcOutlineMode Phase 4). Brak PII. Aplikacja offline. |
</threat_model>

<verification>
- `npm test` — pełny suite zielony (267 baseline + ~18 nowych asercji Task 1+2).
- `npm install` clean (no postinstall scripts, no warnings dla @floating-ui/dom).
- `grep -c "step-item__rationale\|modal-card\|difficulty-badge\|tooltip\|label-3d" style.css` ≥ 5 (klasy obecne).
- `node -e "import('./src/i18n/pl.js').then(m=>{ if(m.pl.keymap.length!==11) process.exit(1); if(typeof m.pl.modals.confirmScenarioSwitch.body !== 'function') process.exit(1)})"` exit 0.
</verification>

<success_criteria>
- Store: 5 nowych pól + 8 nowych akcji testowanych w tests/trainingStore.test.js (10 asercji PASS).
- pl.js: pl.keymap (11), pl.modals (help+confirmScenarioSwitch+closeAria), pl.ui (+5) testowane w tests/i18n.pl.test.js (8 asercji PASS).
- index.html: 2 nowe sibling kontenery (#label-overlay-container, #modal-container).
- style.css: pełny komplet 20+ klas Phase 5 zgodnie z UI-SPEC §492-521 table.
- package.json: @floating-ui/dom ~1.7.6 w dependencies.
- Boundary scanner + Polish-literal scanner: zielone.
- Pełny suite: 267 baseline + ~18 nowych = 285+ green.
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-01-SUMMARY.md` when done — opisz: nowe pola store (lista 5), nowe akcje (lista 8), shape pl.keymap (11 wpisów lista keys), shape pl.modals, dodane mount points, dodane klasy CSS (lista). Mapuj każdą zmianę na D-Phase5-01..28.
</output>
