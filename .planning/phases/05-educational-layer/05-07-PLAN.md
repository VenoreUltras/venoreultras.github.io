---
phase: 05-educational-layer
plan: 07
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - src/main.js
  - tests/boundaries.test.js
  - tests/application.test.js
  - tests/uruchomienie.integration.test.js
autonomous: false
requirements: [FEEDBACK-06, UI-03, UI-04, INTERACT-06, EDU-01, EDU-02, EDU-03]
tags: [phase-5, wiring, boundaries, integration, manual-checkpoint]

must_haves:
  truths:
    - "Application bootstrap czyta pm300:difficulty:v1 i pm300:audio-mute:v1 z localStorage PRZED instancjonowaniem kontrolerów Phase 5"
    - "Wiring kolejność: SceneSetup → PressModel → store + bootstrap → EmissiveController → RaycastController (z onHoverChange DI=null tymczasowo) → HighlightManager → EdgeOutlineController → TooltipManager (po RaycastController by wpiąć onHoverChange) → AudioController → KeyboardController → LabelOverlay → HelpModal → StatusPanel → StepPanel"
    - "LabelOverlay.update() wpięte do Application.tickables (per-frame CSS2DRenderer.render)"
    - "AudioController.updateHum(rpmEffective) wpięte do simulationTick (per-tick freq/gain ramp)"
    - "Modal-aware physics pause: gdy state.activeModal !== null → simulationTick skip currentAngle integration (D-Phase5-28)"
    - "Dispose chain w odwrotnej kolejności tworzenia; HelpModal/LabelOverlay przed RaycastController; AudioController przed EmissiveController"
    - "boundaries.test.js zawiera 5 nowych entries (TooltipManager/AudioController/KeyboardController/LabelOverlay/HelpModal) z D-Phase5-26 forbidden imports"
    - "Toggle audioMuted i difficulty piszą do localStorage (Application bootstrap subskruje na te 2 slice'y po startScenario)"
  artifacts:
    - path: "src/main.js"
      provides: "Application z 5 nowymi kontrolerami Phase 5 + bootstrap + dispose chain"
      min_lines: 200
    - path: "tests/boundaries.test.js"
      provides: "5 nowych FORBIDDEN_PAIRS entries (Phase 5)"
      contains: "src/education/TooltipManager.js"
    - path: "tests/uruchomienie.integration.test.js"
      provides: "+3 asercje Phase 5 (rationale Nauka, free-roam pauza, modal Esc precedence)"
  key_links:
    - from: "src/main.js bootstrap"
      to: "localStorage pm300:difficulty:v1 + pm300:audio-mute:v1"
      via: "localStorage.getItem + setState"
      pattern: "pm300:difficulty:v1|pm300:audio-mute:v1"
    - from: "src/main.js"
      to: "5 Phase 5 controllers"
      via: "new TooltipManager/AudioController/KeyboardController/LabelOverlay/HelpModal"
      pattern: "new (TooltipManager|AudioController|KeyboardController|LabelOverlay|HelpModal)"
---

<objective>
Finalna integracja Phase 5: wpiąć 5 nowych kontrolerów do Application.constructor zgodnie z bootstrap kolejnością i dispose chain (D-Phase5-25), dorzucić modal-aware physics pause do simulationTick (D-Phase5-23/28), localStorage persist dla difficulty+audioMuted, rozszerzyć boundaries.test.js o 5 nowych entries (D-Phase5-26), zaktualizować integration test, przeprowadzić manual checkpoint (SC1-SC5 Phase 5).

Purpose: Bez Plan 05-07 wszystkie klasy z Plan 05-02..06 są disconnected — Plan 05-07 jest "klejem" integracyjnym + bramą weryfikacyjną na całą fazę.
Output: Działający produkt Phase 5 z 7 wymaganiami spełnionymi w przeglądarce + manual QA sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@.planning/phases/05-educational-layer/05-VALIDATION.md
@src/main.js
@tests/boundaries.test.js
@tests/application.test.js
@tests/uruchomienie.integration.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Application bootstrap + 5 controller wiring + modal-aware tick + dispose chain</name>
  <read_first>
    - src/main.js (cały — verify Plan 04-06 bootstrap pattern linie 42-49 + Plan 4 wiring + dispose chain T-04-14)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "Brownfield: src/main.js" (linie 571-632) + "Integration Points" w 05-CONTEXT linie 162-174
    - .planning/phases/05-educational-layer/05-RESEARCH.md "Application.constructor — localStorage bootstrap ordering" (linie 802-826) + "Pattern 4: Modal-aware physics pause" (linie 437-460)
    - tests/application.test.js (cały — wzór wiring describe + dispose order spy)
    - src/education/* (Plan 05-02..05 — verify konstruktor signatures)
    - src/ui/HelpModal.js (Plan 05-03 — verify signature)
  </read_first>
  <action>
    Edytować `src/main.js`:

    1. **Imports** (po linii 15 import uruchomienie): dodać:
       ```javascript
       import { TooltipManager } from './education/TooltipManager.js';
       import { AudioController } from './education/AudioController.js';
       import { KeyboardController } from './education/KeyboardController.js';
       import { LabelOverlay } from './education/LabelOverlay.js';
       import { HelpModal } from './ui/HelpModal.js';
       ```

    2. **Storage key consts** (po `HC_STORAGE_KEY` linia 17):
       ```javascript
       const DIFFICULTY_KEY = 'pm300:difficulty:v1';      // D-Phase5-04
       const AUDIO_MUTE_KEY = 'pm300:audio-mute:v1';       // D-Phase5-18
       ```

    3. **Bootstrap** (po `hcInitial` setState linia 49, PRZED `startScenario`):
       ```javascript
       // D-Phase5-04 + D-Phase5-18 bootstrap. Graceful catch (analog hcInitial pattern T-04-13).
       const difficultyInitial = (() => {
         try {
           const v = localStorage.getItem(DIFFICULTY_KEY);
           return (v === 'nauka' || v === 'egzamin') ? v : 'nauka';
         } catch { return 'nauka'; }
       })();
       const audioMutedInitial = (() => {
         try { return localStorage.getItem(AUDIO_MUTE_KEY) === 'true'; }
         catch { return false; }
       })();
       this.store.setState({ difficulty: difficultyInitial, audioMuted: audioMutedInitial });
       ```

    4. **Persist subscribers** (po setState bootstrap, PRZED startScenario): wpiąć subscribery zapisujące do localStorage (analog StatusPanel HC pattern Plan 04-04, ale w Application — bo trainingStore boundary-clean nie zna localStorage; D-Phase5-26):
       ```javascript
       // Persist warstwa — store-side toggleMute/setDifficulty zmienia state, Application
       // zapisuje do localStorage. trainingStore zachowuje boundary clean (D-Phase5-26).
       this._unsubscribers.push(
         this.store.subscribe((s) => s.difficulty, (v) => {
           try { localStorage.setItem(DIFFICULTY_KEY, v); } catch { /* silent */ }
         }),
         this.store.subscribe((s) => s.audioMuted, (v) => {
           try { localStorage.setItem(AUDIO_MUTE_KEY, String(v)); } catch { /* silent */ }
         }),
       );
       ```

    5. **Phase 5 controllers** (po `this.stepPanel = new StepPanel(...)` linia 87) — kolejność z 05-CONTEXT §162-174:
       ```javascript
       // Phase 5 D-Phase5-25 — kolejność tworzenia:
       // (a) AudioController (boundary-clean, store-only — niezależny)
       this.audioController = new AudioController({ store: this.store });

       // (b) KeyboardController (window listener)
       this.keyboardController = new KeyboardController({
         store: this.store,
         scenarios: { uruchomienie },
       });

       // (c) LabelOverlay (per-frame tickable)
       this.labelOverlay = new LabelOverlay({
         scene: this.sceneSetup.scene,
         camera: this.sceneSetup.camera,
         renderer: this.sceneSetup.renderer,
         interactables: this.pressModel.getInteractables(),
         store: this.store,
       });
       this.tickables.push(() => this.labelOverlay.update());  // FEEDBACK-06 per-frame

       // (d) HelpModal (DOM modal blocker)
       this.helpModal = new HelpModal({ store: this.store });

       // (e) TooltipManager — PO RaycastController, by wpiąć onHoverChange callback
       this.tooltipManager = new TooltipManager({
         store: this.store,
         raycastController: this.raycastController,
       });
       // Hook callback: RaycastController.constructor wcześniej (linia 61) NIE dostał onHoverChange
       // — wpinamy je tutaj po-hoc (planner alternatywa: rebuild RaycastController z DI w ctor).
       // PREFEROWANE: po-hoc setter, bo TooltipManager wymaga raycastController instancji.
       this.raycastController._onHoverChange = (id, mesh) => {
         if (id) this.tooltipManager.onHoverEnter(id, this.sceneSetup.renderer.domElement);
         else this.tooltipManager.onHoverLeave();
       };
       ```
       UWAGA: Plan 05-04 dorzucił `onHoverChange` jako optional DI param do RaycastController ctor. Plan 05-07 może albo: (i) wstrzyknąć w ctor (wymaga utworzenia TooltipManager PRZED RaycastController — circular dep bo TooltipManager wymaga raycastController), albo (ii) po-hoc assign po obu konstruktorach. PREFERED: po-hoc assign (jak wyżej) — TooltipManager wymaga raycastController jako reference (nie dla bezpośredniego użycia, tylko by zapamiętać kto je wstrzykuje); RaycastController._onHoverChange jest mutable polem. Test application.test.js weryfikuje że callback wpięty + woła tooltip metody.

    6. **simulationTick modal-aware pause** (linia 90-124): rozszerzyć:
       ```javascript
       simulationTick(deltaTime) {
         const dtSeconds = deltaTime / 1000;
         const state = this.store.getState();
         const { machineState, activeModal } = state;

         // D-Phase5-23 + D-Phase5-28: pauza integration gdy modal otwarty.
         // Rendering + raycaster + label overlay nadal działają (poza tym predicatem).
         const integrationPaused = activeModal !== null;

         const targetRpm = this.ui.speed;
         const targetOmega = (targetRpm / 60) * Math.PI * 2;
         const spinActive = machineState === 'rozpedzanie' || machineState === 'gotowa-do-pracy' || machineState === 'w-cyklu';
         const desiredOmega = spinActive ? targetOmega : this.ui.getAngularVelocity();
         const rampRate = targetOmega / 3.0;
         const maxStep = rampRate * dtSeconds;
         const delta = desiredOmega - this._omega;
         this._omega += Math.sign(delta) * Math.min(Math.abs(delta), maxStep);

         if (!integrationPaused && this._omega > 0) {
           this.currentAngle = (this.currentAngle + this._omega * dtSeconds) % (Math.PI * 2);
         }

         this.pressModel.update(this.currentAngle);
         const displacement = PhysicsEngine.calculateSliderPosition(this.currentAngle, this.pressModel.r, this.pressModel.l);
         this.ui.updateTelemetry(this.currentAngle, displacement);

         // D-Phase5-17: AudioController hum freq/gain proporcjonalny do effective RPM.
         // Nie wpinamy do osobnego ticka — piggy-back na simulationTick (jeden punkt timing).
         const rpmEffective = (this._omega / (Math.PI * 2)) * 60;
         this.audioController?.updateHum(rpmEffective);
       }
       ```

    7. **Dispose chain** (linia 136-149): rozszerzyć w odwrotnej kolejności tworzenia, ZACHOWUJĄC T-04-14 invariant (RaycastController PRZED EmissiveController):
       ```javascript
       dispose() {
         gsap.ticker.remove(this._tickerCallback);
         for (const unsub of this._unsubscribers) unsub();
         this._unsubscribers = [];
         if (this.disclaimerBanner) this.disclaimerBanner.dispose();
         // Phase 4
         if (this.stepPanel) this.stepPanel.dispose();
         if (this.statusPanel) this.statusPanel.dispose();
         // Phase 5 — odwrotna kolejność tworzenia
         if (this.tooltipManager) this.tooltipManager.dispose();
         if (this.helpModal) this.helpModal.dispose();
         if (this.labelOverlay) this.labelOverlay.dispose();              // PRZED sceneSetup.dispose (Discretion CSS2DRenderer order)
         if (this.keyboardController) this.keyboardController.dispose();
         if (this.audioController) this.audioController.dispose();        // PRZED emissive (audio nie zależy od emissive, ale logiczna kolejność)
         // Phase 4 cd.
         if (this.highlightManager) this.highlightManager.dispose();
         if (this.edgeOutlineController) this.edgeOutlineController.dispose();
         if (this.raycastController) this.raycastController.dispose();    // PRZED emissive — _commitLeave woła clearLayer (T-04-14)
         if (this.emissiveController) this.emissiveController.dispose();
         this.pressModel.disposeMaterials();
         this.sceneSetup.dispose();
       }
       ```

    8. **Update tests/application.test.js**: dorzucić describe `Phase 5 — wiring` z asercjami:
       - Test W1: po `new Application()`, app ma pola: tooltipManager, audioController, keyboardController, labelOverlay, helpModal (verify instanceof).
       - Test W2: tickables zawiera funkcję wołającą `this.labelOverlay.update()` (spy + dispatch one tick).
       - Test W3: bootstrap localStorage — mock `localStorage.getItem` zwracający 'egzamin' dla DIFFICULTY_KEY + 'true' dla AUDIO_MUTE_KEY; new Application → store.difficulty==='egzamin', audioMuted===true.
       - Test W4: bootstrap graceful — localStorage throw → difficulty=='nauka' (default), audioMuted===false.
       - Test W5: persist subscriber — store.setState({difficulty:'egzamin'}) → localStorage.setItem wywołane z (DIFFICULTY_KEY, 'egzamin'). store.setState({audioMuted:true}) → setItem z (AUDIO_MUTE_KEY, 'true').
       - Test W6: dispose order — spy na każdy dispose() przez `vi.spyOn` + `mock.invocationCallOrder` (analog Plan 04-06 dispose order assertion T-04-14). Weryfikuj że: tooltipManager.dispose < helpModal.dispose < labelOverlay.dispose < keyboardController.dispose < audioController.dispose < highlightManager.dispose < edgeOutlineController.dispose < raycastController.dispose < emissiveController.dispose.
       - Test W7: modal-aware pause — store.setState({activeModal:'help'}); simulationTick(16) → currentAngle nie zmienia się (vs control gdy activeModal=null currentAngle rośnie). Test wpisany inline z assert na currentAngle.
       - Test W8: onHoverChange wired — spy na `tooltipManager.onHoverEnter` i `onHoverLeave`; trigger `raycastController._onHoverChange('kolo-zamachowe', meshFixture)` → onHoverEnter wywołane z ('kolo-zamachowe', renderer.domElement).
  </action>
  <verify>
    <automated>npm test -- --run tests/application.test.js</automated>
  </verify>
  <done>
    - 8 nowych asercji W1-W8 PASS.
    - Pre-existing tests/application.test.js (Plan 04-06 Phase 4 wiring asercje + ewentualne Phase 3) PASS.
    - 5 nowych kontrolerów instancjonowanych w prawidłowej kolejności.
    - Bootstrap localStorage przed startScenario.
    - Dispose order verifikowane przez invocationCallOrder.
  </done>
</task>

<task type="auto">
  <name>Task 2: boundaries.test.js +5 entries Phase 5</name>
  <read_first>
    - tests/boundaries.test.js (cały — verify FORBIDDEN_PAIRS shape linie 24-66)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-26 — allowed/forbidden imports per nowa klasa)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "Brownfield: tests/boundaries.test.js" (linie 692-709)
    - src/education/* + src/ui/HelpModal.js (verify aktualne imports — sanity przed dorzuceniem entries)
  </read_first>
  <action>
    Edytować tests/boundaries.test.js. Dorzucić do FORBIDDEN_PAIRS array (po linii 66, przed close `]`):

    ```javascript
    // Phase 5 (Plan 05-07): educational layer boundaries (D-Phase5-26).
    // TooltipManager — store + DOM + @floating-ui/dom + pl.js; NIE THREE/gsap/training/highlight.
    { file: 'src/education/TooltipManager.js',
      mustNotImport: ['three', 'gsap', '../training/', './training/', '../highlight/', './highlight/'] },
    // AudioController — TYLKO store przez DI; NIE THREE/gsap/DOM/floating-ui/training.
    { file: 'src/education/AudioController.js',
      mustNotImport: ['three', 'gsap', '@floating-ui/dom', '../training/', './training/', '../highlight/', './highlight/'] },
    // KeyboardController — store + window; NIE THREE/gsap/training/highlight/floating-ui.
    { file: 'src/education/KeyboardController.js',
      mustNotImport: ['three', 'gsap', '@floating-ui/dom', '../training/', './training/', '../highlight/', './highlight/'] },
    // LabelOverlay — THREE (CSS2DRenderer) + store; NIE gsap/training/floating-ui.
    // DOM ograniczony do #label-overlay-container mount + per-label createElement.
    { file: 'src/education/LabelOverlay.js',
      mustNotImport: ['gsap', '@floating-ui/dom', '../training/', './training/', '../highlight/', './highlight/'] },
    // HelpModal — store + DOM + pl; NIE THREE/gsap/training/highlight/floating-ui.
    { file: 'src/ui/HelpModal.js',
      mustNotImport: ['three', 'gsap', '@floating-ui/dom', '../training/', './training/', '../highlight/', './highlight/'] },
    ```

    Zachowaj komentarz nagłówkowy nad blokiem Phase 5 entries: `// Phase 5 (Plan 05-07): educational layer boundaries (D-Phase5-26).` (analog Phase 4 komentarz linia 51).
  </action>
  <verify>
    <automated>npm test -- --run tests/boundaries.test.js</automated>
  </verify>
  <done>
    - 5 nowych entries w FORBIDDEN_PAIRS.
    - boundaries.test.js zielony (wszystkie 5 nowych plików Phase 5 spełniają boundary).
    - Polish-literal scanner zielony.
    - Negative sanity test (linie 135-148) zielony.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration test rozszerzenie — rationale + free-roam + modal-pause</name>
  <read_first>
    - tests/uruchomienie.integration.test.js (cały — verify Plan 04-06 +2 testy FEEDBACK-04 redundant encoding linie ~koniec)
    - .planning/phases/05-educational-layer/05-VALIDATION.md (Wave 0 Requirements + sampling rate)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-05/09/11/23)
    - src/training/scenarios/uruchomienie.js (verify rationale obecne 8/8)
  </read_first>
  <action>
    Dorzucić do tests/uruchomienie.integration.test.js nowy describe `Phase 5 — integration end-to-end`:

    1. Test I1 (rationale Nauka happy path): pełny scenariusz uruchomienie w Nauka difficulty (default). Po startScenario+ render → DOM zawiera `<p class="step-item__rationale">` z textContent zaczynającym się "Każda maszyna..." (rationalePL kroku #1). Po sukcesie kroku #1 (klik na tabliczce — symulacja z store.attemptStep) → rationale dla kroku #1 znika (status=done), rationale dla kroku #2 pojawia się.

    2. Test I2 (Egzamin no-rationale): store.setState({difficulty:'egzamin'}); startScenario → DOM NIE zawiera żadnego `.step-item__rationale`.

    3. Test I3 (free-roam pauza SOP): startScenario; currentStepId='sprawdz-tabliczke'; store.setState({freeRoam:true}); symuluj click na meshu (ale RaycastController._handlePointerUp bezpośrednio przez test) → store.currentStepId NADAL 'sprawdz-tabliczke' (attemptStep NIE wywołane); steps[].status NIE zmienione. setState({freeRoam:false}); ten sam click → advance.

    4. Test I4 (modal Esc precedence): store.setState({activeModal:'help'}); dispatch window KeyboardEvent('keydown', {key:'Escape'}) → store.activeModal===null AND store.machineState NIE jest 'awaria' (E-stop NIE odpalone). Z activeModal=null; dispatch Escape → triggerEStop? wywołane (jeśli store ma triggerEStop akcję — opcjonalny optional chain).

    Setup integration testu: pełen `new Application()` z mock document (jsdom) — analog Plan 04-06 integration. Mock AudioContext (Plan 05-02 pattern) + mock @floating-ui/dom (Plan 05-04 pattern). Wszystkie 5 kontrolerów Phase 5 wpinanych.

    Wszystkie asercje używają real store + real subscribery (NIE mockować klas Phase 5 — testujemy faktyczne wiring).
  </action>
  <verify>
    <automated>npm test -- --run tests/uruchomienie.integration.test.js</automated>
  </verify>
  <done>
    - 4 nowe asercje I1-I4 PASS.
    - Pre-existing testy integration (Plan 03 happy path 8/8 + Plan 04-06 redundant encoding) PASS.
    - Phase 5 end-to-end weryfikowane: rationale, free-roam pauza, Esc precedence.
  </done>
</task>

<task type="auto">
  <name>Task 4: Pełna walidacja przed manual checkpoint</name>
  <read_first>
    - .planning/phases/05-educational-layer/05-VALIDATION.md (sekcje Manual-Only Verifications + Sampling Rate)
    - package.json (verify @floating-ui/dom w deps)
    - tests/boundaries.test.js (verify 5 Phase 5 entries dodane Task 2)
  </read_first>
  <action>
    1. Uruchomić pełny suite: `npm test -- --run --reporter=dot`.
    2. Zweryfikować coverage: `npm run test:coverage` — coverage dla `src/training/**` i `src/state/**` nie spadł poniżej baseline (95/95/90/95).
    3. Sprawdzić, że ŻADEN plik Phase 5 nie naruszył boundary:
       - `grep -E "^import" src/education/*.js src/ui/HelpModal.js` — verify ręcznie że tylko D-Phase5-26 allowed imports.
    4. Sprawdzić Polish-literal scanner: `npm test -- --run tests/boundaries.test.js` zawiera describe 'Polish string literal scanner' — zielony.
    5. Sprawdzić package.json: `@floating-ui/dom ~1.7.6` w `dependencies` (nie `devDependencies`).
    6. Sprawdzić index.html: ma `#label-overlay-container` i `#modal-container`.
    7. Sprawdzić style.css: ma sekcję Phase 5 z klasami `.tooltip`, `.label-3d`, `.modal-card`, `.difficulty-badge`, `.free-roam-indicator`, `.step-item__rationale`.

    Sumaryczny stan oczekiwany:
    - Tests: 267 baseline + ~80 nowych Phase 5 = ~345+ green.
    - Coverage: thresholds zachowane.
    - Boundary: 5 Phase 5 entries + Polish scanner green.
    - Brak nowych zakazanych importów we wszystkich plikach Phase 5.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - Pełny suite zielony (~345+ testów).
    - Coverage thresholds zachowane.
    - boundaries.test.js + Polish-literal scanner zielone.
    - Wszystkie kontrolery Phase 5 instancjonowane w Application; dispose chain działa.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 — Educational Layer kompletny (7 wymagań: FEEDBACK-06, UI-03, UI-04, INTERACT-06, EDU-01, EDU-02, EDU-03):
    - **TooltipManager** (@floating-ui/dom 600ms hover) — UI-03.
    - **LabelOverlay** (CSS2DRenderer L-toggle 15 labelek + camera-facing + declutter) — FEEDBACK-06.
    - **AudioController** (WebAudio synthesis: alarm 600Hz×2, confirm 880Hz/200ms, hum freq=80+1.2·RPM, M mute persist) — EDU-03.
    - **KeyboardController** (11 klawiszy R/T/1-4/Space/Esc/H/L/M + Esc precedence + modal-blocking) — INTERACT-06.
    - **HelpModal** (H-toggle + keymap + legenda kolorów/ikon + disclaimer) — INTERACT-06 SC5.
    - **EmissiveController stack 3-warstwowy** + **HighlightManager hint warstwa** (Nauka next-step Wong yellow #F0E442) — D-Phase5-03.
    - **StepPanel rationale inline** (Nauka pod aktywnym non-done krokiem; Egzamin ukryte) — UI-04.
    - **StatusPanel difficulty badge + toggle + free-roam indicator** — EDU-02.
    - **RaycastController free-roam click bypass** + onHoverChange DI dla tooltipa — EDU-01.
    - **Application bootstrap** localStorage `pm300:difficulty:v1` + `pm300:audio-mute:v1` + dispose chain rozszerzony.
    - **Modal-aware physics pause** (activeModal!==null → currentAngle integration skipped) — D-Phase5-23/28.
    - **boundaries.test.js +5 entries** Phase 5 — D-Phase5-26 enforced.
  </what-built>
  <how-to-verify>
    Uruchom `npm run dev` i otwórz http://localhost:5173/. Procedura QA (9 punktów):

    1. **Tooltip 600ms (UI-03 SC1):**
       - Hover na "Koło zamachowe" — po 600ms tooltip z opisem "Magazynuje energię obrotową napędu...".
       - Resize okno (DevTools mobile preset) — tooltip pozostaje w viewport (flip+shift).
       - Hover na element przy krawędzi okna — tooltip flipuje (top→bottom).

    2. **Etykiety 3D L-toggle (FEEDBACK-06 SC1):**
       - Naciśnij `L` — 15 polskich etykiet pojawia się nad meshami.
       - Orbituj kamerą — etykiety zasłoniętych elementów znikają (camera-facing filter); żadne dwie etykiety nie nachodzą wizualnie (declutter offset).
       - Naciśnij `L` ponownie — wszystkie etykiety znikają.

    3. **Rationale inline Nauka (UI-04 SC2):**
       - Domyślnie Nauka — pod aktywnym krokiem widoczna kursywa: "Każda maszyna ma osobną instrukcję...".
       - Klik na StatusPanel "Przełącz na Egzamin" — rationale znika (i tooltipy znikają, etykiety znikają).
       - Klik "Przełącz na Naukę" — rationale wraca.

    4. **Free-roam (EDU-01 SC3):**
       - Naciśnij `T` — StatusPanel pokazuje "🆓 Tryb wolny"; klik w meshe NIE advance'uje kroków (StepPanel pozostaje na bieżącym).
       - Naciśnij `T` ponownie — wracasz do procedury w tym samym kroku.
       - Hover w free-roam: tooltipy + etykiety nadal działają (Nauka).

    5. **Audio (EDU-03 SC4):**
       - Wymuś awarię: pomiń krok (klik w niewłaściwy mesh w trybie procedury) lub w DevTools `app.store.setState({machineState:'awaria'})` — usłysz alarm 2× burst 600Hz.
       - Wykonaj poprawny krok — confirm 880Hz/200ms (delikatny "dink").
       - Podkręć slider RPM — hum częstotliwość rośnie liniowo (proporcjonalne do RPM); RPM<5 → cisza.
       - Naciśnij `M` — wszystko wyciszone. Odśwież stronę — `M` persist (mute zachowane z localStorage pm300:audio-mute:v1).

    6. **Skróty + Help overlay (INTERACT-06 SC5):**
       - Naciśnij `H` — modal pomocy otwarty: tabela 11 klawiszy z polskimi opisami, legenda 4 kolorów (#D55E00/#009E73/#F0E442/#FFFFFF), legenda 4 step states + 7 machine states + 2 difficulty modes, disclaimer powtórzony.
       - Modal otwarty: slider RPM nadal updateuje UI ale wał NIE przyspiesza (modal-aware pause). Renderer + raycast działają.
       - Naciśnij `H` — modal zamknięty, wał kontynuuje rozpędzanie.
       - Otwórz modal → naciśnij `Esc` — modal się zamyka (NIE odpala E-stop).
       - Bez modalu → naciśnij `Esc` — symulacja zatrzymana/E-stop (Phase 1 SOP-09 behavior).

    7. **WebGL context-loss (Phase Z constraint):**
       - DevTools → Rendering → "Lose context" — hum cichnie (AudioController nie zawiesza się); GSAP ticker pauzuje (Phase 1).
       - "Restore context" — hum powraca przy RPM > 5.

    8. **Difficulty persist (D-Phase5-04):**
       - Przełącz na Egzamin → odśwież stronę — initial difficulty='egzamin' (badge "📝 Egzamin"); tooltipy/etykiety/rationale OFF od startu.
       - Przełącz z powrotem na Nauka → odśwież — persist 'nauka'.

    9. **FPS check (CLAUDE.md preservation):**
       - DevTools → Performance → record 5s session z `L` ON + free-roam + hover po meshach + rotacja kamerą.
       - Sprawdź FPS — powinno być 60 (cel CLAUDE.md "preserve it"). Akceptowalny minimal 55 FPS na zintegrowanej grafice.

    Console: BRAK nowych errorów ani warning (poza ewentualnymi WebAudio autostart warnings z browsera — akceptowalne; resume na pierwszy klik dismissuje).

    **Zaakceptuj** wpisaniem "approved" gdy wszystkie 9 punktów spełnione. Jeśli któryś failuje — opisz konkretny problem (numer punktu, oczekiwane vs faktyczne zachowanie). Lista znanych acceptable issues: camera-facing dot product może wymagać tuningu konwencji (Assumption A2) — jeśli etykiety zachowują się odwrotnie (zasłonięte widoczne, widoczne zasłonięte), to fix w follow-up (zmiana w LabelOverlay.js linia z `dot < 0` na `dot > 0` lub `worldNormal(0,1,0)` zamiast `(0,0,1)`).
  </how-to-verify>
  <resume-signal>Wpisz "approved" gdy wszystkie 9 punktów spełnione, lub opisz problem (numer punktu + obserwowane zachowanie).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application bootstrap → localStorage | 2 nowe versionowane klucze (pm300:difficulty:v1, pm300:audio-mute:v1); graceful catch |
| store akcje → 5 Phase 5 kontrolerów | DI-only; każdy kontroler dostaje store przez ctor, NIE importuje go runtime'em |
| Application.tickables → LabelOverlay.update + simulationTick → AudioController.updateHum | per-frame integration; GSAP ticker single source timing |
| dispose chain | odwrotna kolejność tworzenia; T-04-14 invariant zachowany (RaycastController PRZED EmissiveController) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-RACE | Tampering | onHoverChange po-hoc assign vs ctor DI | mitigate | Po utworzeniu TooltipManager, `raycastController._onHoverChange = ...` (mutowalne pole). Test W8 weryfikuje że callback jest wpięty I że woła tooltip.onHoverEnter/Leave. Alternatywa: rebuild RaycastController (overhead) — odrzucone. |
| T-05-07-LEAK | Denial of Service | persist subscribery leak przy HMR | mitigate | 2 nowe subskrybery w `Application._unsubscribers` — istniejący dispose chain (linia 138) zwalnia automatycznie. |
| T-05-07-AUDIOSTART | Tampering | AudioContext autoblock browser policy | accept | AudioController._getOrCreateContext jest lazy — context tworzony przy pierwszym audio triggerze (= zwykle po user-gesture: klik canvas lub naciśnięcie klawisza). Browser może pokazać warning przed pierwszym kliknięciem — manualny QA punkt 5 weryfikuje że audio działa po pierwszej interakcji. |
| T-05-07-INTEG | Tampering | Test integration end-to-end może być flaky przez async @floating-ui | mitigate | Plan 05-04 mockuje @floating-ui/dom w tests/TooltipManager.test.js. Integration test (Task 3) używa tego samego mock + AudioContext mock — deterministyczne. |
| T-05-07-FOCUS | Information Disclosure | focus trap w modal a11y | accept | `<dialog>` native + showModal — browser zarządza. Manual QA punkt 6 weryfikuje Tab/Shift+Tab nie wycieka poza modal. Pełny aria-live region dla tooltip = Phase 7 (deferred). |
</threat_model>

<verification>
- `npm test -- --run` — pełny suite ~345+ testów green.
- `npm run test:coverage` — coverage thresholds zachowane (95/95/90/95 dla src/training i src/state).
- `grep -c "education/TooltipManager\|education/AudioController\|education/KeyboardController\|education/LabelOverlay\|ui/HelpModal" tests/boundaries.test.js` ≥ 5.
- `grep -c "new TooltipManager\|new AudioController\|new KeyboardController\|new LabelOverlay\|new HelpModal" src/main.js` ≥ 5.
- `grep -c "DIFFICULTY_KEY\|AUDIO_MUTE_KEY" src/main.js` ≥ 4 (2 const def + 2 użycia w bootstrap + 2 w persist subscriberach).
- `grep -c "integrationPaused\|activeModal" src/main.js` ≥ 2 (modal-aware pause).
- Manual QA: 9/9 punktów approved (lub follow-up plan zaadresowuje konkretne issues).
</verification>

<success_criteria>
**Wszystkie 5 Success Criteria Phase 5 z ROADMAP.md spełnione:**
1. ✓ Hover 600ms → @floating-ui/dom tooltip z `pl.parts[id].description`; toggle `L` → 15 CSS2DRenderer labels z declutter.
2. ✓ Rationale "po co ten krok" — inline w Nauka, ukryte w Egzamin; z scenario rationalePL (NIE hardcode).
3. ✓ Free-roam (`T`) + difficulty Nauka/Egzamin — store flag = single source of truth.
4. ✓ WebAudio: alarm ~600Hz×2, confirm ~880Hz/200ms, hum proporcjonalny do RPM; `M` mute global + persist localStorage.
5. ✓ Keyboard `R`/`T`/`1-4`/`Space`/`Esc`/`H`/`L`/`M` + `H`-toggleable help overlay z keymap+legendami+disclaimer.

**7 wymagań Phase 5 zaadresowane** (FEEDBACK-06, UI-03, UI-04, INTERACT-06, EDU-01, EDU-02, EDU-03).

**Cross-cutting invariants utrzymane:**
- Zustand store jedyne mutowalne shared state (CRIT-7).
- boundaries.test.js zielony z 5 nowymi entries (D-Phase5-26).
- Polish-literal scanner zielony (wszystkie polskie literały w pl.js).
- Wong palette + redundant encoding (Wong yellow #F0E442 dodany jako hint warstwa).
- gsap.ticker single source of timing.
- Every subscriber returns unsub; Application.dispose chain zachowany (T-04-14 + 5 nowych klas).
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-07-SUMMARY.md` — opisz: bootstrap order (localStorage → setState → controllers), wiring order (5 controllerów per D-Phase5-25), tickables additions (LabelOverlay.update), simulationTick changes (modal-aware pause + updateHum), persist subscribers, dispose chain order, boundaries 5 entries, integration test +4 asercji, manual QA 9-point result. Mapuj na każdą z 5 SC ROADMAP + 7 wymagań.
</output>
