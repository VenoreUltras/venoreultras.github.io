---
phase: 05-educational-layer
plan: 08
type: tdd
wave: 4
depends_on: ["05-01", "05-03"]
files_modified:
  - src/ui/ConfirmModal.js
  - src/education/KeyboardController.js
  - tests/ConfirmModal.test.js
  - tests/KeyboardController.test.js
autonomous: true
requirements: [INTERACT-06]
tags: [phase-5, modal, confirm, scenario-switch, dom]

must_haves:
  truths:
    - "ConfirmModal renderuje pl.modals.confirmScenarioSwitch.title + body(current,next) + 2 przyciski (Anuluj / Załaduj scenariusz) gdy state.activeModal === 'confirm-scenario-switch' (D-Phase5-07)"
    - "ConfirmModal czyta state._confirmPayload (z openConfirmModal) by zrenderować nazwy bieżącego i nowego scenariusza w body template"
    - "Klik 'Załaduj scenariusz' → store.closeModal({confirm:true}) → KeyboardController obserwuje payload + woła startScenario(scenarios[next]); klik 'Anuluj' → store.closeModal({confirm:false}) → currentStepId NIEZMIENIONY"
    - "KeyboardController._loadScenario(id) gating: gdy state.currentStepId && nie wszystkie kroki done → state.openConfirmModal({current, next}) zamiast natychmiast startScenario (D-Phase5-07)"
    - "Bezwarunkowo dozwolone (brak gating): currentStepId === null OR wszystkie state.steps[*].status === 'done'"
    - "Esc / klik overlay / klik 'Anuluj' = anulacja (NIE ładuje); klik 'Załaduj scenariusz' = potwierdzenie + load"
  artifacts:
    - path: "src/ui/ConfirmModal.js"
      provides: "Modal blokujący dla potwierdzenia zmiany scenariusza mid-run"
      min_lines: 100
    - path: "src/education/KeyboardController.js"
      provides: "Gating _loadScenario przez ConfirmModal mid-run (brownfield edit Plan 05-03)"
      contains: "openConfirmModal"
    - path: "tests/ConfirmModal.test.js"
      provides: "ConfirmModal lifecycle + payload render + close handlers coverage"
      min_lines: 120
    - path: "tests/KeyboardController.test.js"
      provides: "+4 testy gating + close payload handling"
  key_links:
    - from: "src/ui/ConfirmModal.js"
      to: "pl.modals.confirmScenarioSwitch + state._confirmPayload"
      via: "import pl + store.getState()"
      pattern: "confirmScenarioSwitch"
    - from: "src/education/KeyboardController.js _loadScenario"
      to: "state.openConfirmModal({current, next})"
      via: "store.getState().openConfirmModal"
      pattern: "openConfirmModal"
---

<objective>
D-Phase5-07: ConfirmModal blokujący przy zmianie scenariusza w trakcie aktywnej procedury. Klawisz `1-4` mid-run otwiera modal "Przerwiesz postęp w '{current}'. Załadować '{next}'?" z przyciskami Anuluj/Załaduj. Pierwotnie brakujący element Phase 5 — wprowadzony post-revision iteration 2 (plan-checker B3).

Purpose: Bezpieczeństwo dla kursantów — redukuje przypadkowy reset procedury naciśnięciem klawisza skrótu. Modal pauzuje SOP (analogicznie do HelpModal — `activeModal !== null` triggeruje physics pause D-Phase5-23/28).
Output: Nowa klasa ConfirmModal + brownfield-edit KeyboardController (gating w _loadScenario + close-payload handling). Plan 05-07 wpina ConfirmModal do Application dispose chain + bootstrap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@src/ui/HelpModal.js
@src/education/KeyboardController.js
@src/state/trainingStore.js
@src/i18n/pl.js
@src/training/scenarios/uruchomienie.js

<interfaces>
Store (post Plan 05-01):
- state.activeModal: null | 'help' | 'confirm-scenario-switch'
- state._confirmPayload: null | {current: string, next: string, scenarioId: string} (internal)
- state.openConfirmModal(payload) → state.activeModal='confirm-scenario-switch'; state._confirmPayload=payload
- state.closeModal({confirm?: boolean} = {}) → state.activeModal=null; state._confirmPayload zachowany do następnego openConfirmModal (lub Plan 05-08 dodaje cleanup w akcji — patrz decyzja Task 1)

UWAGA do Plan 05-01: oryginalna sygnatura `closeModal()` (bez args) — Plan 05-08 wymaga rozszerzenia o opcjonalny `{confirm: boolean}` LUB innego mechanizmu (np. osobna akcja `confirmScenarioSwitch()` która woła closeModal + emituje event). 
DECYZJA Plan 05-08 (minimal coupling): NIE rozszerzać `closeModal` o argumenty. Zamiast tego:
- ConfirmModal sam wywołuje `state.closeModal()` (na Esc/Anuluj/X) — payload pozostaje w `state._confirmPayload` (no-op load).
- ConfirmModal "Załaduj" button: wywołuje `state.startScenario(scenarios[payload.scenarioId])` BEZPOŚREDNIO + następnie `state.closeModal()`. Wymaga DI `scenarios: {uruchomienie}` w ConfirmModal ctor (analog KeyboardController).
- KeyboardController._loadScenario: TYLKO opens confirm modal gdy mid-run; nie obsługuje payload close (ConfirmModal robi load samodzielnie).
- Cleanup _confirmPayload: po `closeModal()` Plan 05-08 dorzuca w trainingStore.closeModal action → `_confirmPayload: null` (drobny brownfield edit pl.modals działa bez tego, ale czyściej z cleanup; alternative — Plan 05-08 nie rusza store, _confirmPayload zostaje stale ale jest sprawdzany TYLKO gdy activeModal==='confirm-scenario-switch').

KeyboardController._loadScenario (Plan 05-03 obecny, linia ~28):
```javascript
_loadScenario(id) {
  if (this._scenarios[id]) {
    this._store.getState().startScenario(this._scenarios[id]);
  } else {
    console.warn(`[KeyboardController] scenariusz ${id} — Phase 6`);
  }
}
```

Plan 05-08 brownfield:
```javascript
_loadScenario(id) {
  if (!this._scenarios[id]) {
    console.warn(`[KeyboardController] scenariusz ${id} — Phase 6`);
    return;
  }
  const state = this._store.getState();
  // D-Phase5-07 gating: mid-run wymaga potwierdzenia.
  const hasActiveStep = state.currentStepId !== null;
  const allDone = state.steps && Object.values(state.steps).every(s => s.status === 'done');
  if (hasActiveStep && !allDone) {
    // Pauza SOP + delegacja do ConfirmModal (który sam wywoła startScenario po potwierdzeniu).
    state.openConfirmModal({
      current: state.activeScenario?.id ?? '',
      next: id,
      scenarioId: id,  // duplikat dla jasności — payload niezmienny
    });
    return;
  }
  // Bezwarunkowo dozwolone: brak aktywnej procedury lub wszystkie kroki done.
  state.startScenario(this._scenarios[id]);
}
```

pl.js (Plan 05-01):
- pl.modals.confirmScenarioSwitch.title = "Zmiana scenariusza"
- pl.modals.confirmScenarioSwitch.body = (current, next) => `Przerwiesz postęp w "${current}". Załadować "${next}"?`
- pl.modals.confirmScenarioSwitch.confirm = "Załaduj scenariusz"
- pl.modals.confirmScenarioSwitch.cancel = "Anuluj"
- pl.modals.closeAria = "Zamknij"
- pl.scenarios[id].titlePL (planner verify obecne dla 'uruchomienie' w pl.js; jeśli brak, ConfirmModal używa raw scenarioId — fallback)

DOM mount (Plan 05-01):
- #modal-container istnieje (z-index:300, pointer-events:none); ConfirmModal mountuje overlay + dialog jako children (analog HelpModal).

CSS (Plan 05-01):
- `.modal-card--confirm` wariant max-width:440px (UI-SPEC §214-225 + 502)
- `.btn.primary` i `.btn.secondary` istnieją (UI-SPEC §316-317)
- `.modal-card__actions` flex justify-end gap:12px margin-top:24px (UI-SPEC §313-315)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED + GREEN — ConfirmModal (DOM + payload render + close handlers)</name>
  <read_first>
    - src/ui/HelpModal.js (post-Plan 05-03 — wzór mount + subscriber + dispose; ConfirmModal = analogiczna struktura z innym body)
    - tests/HelpModal.test.js (post-Plan 05-03 — wzór 12 asercji DOM lifecycle)
    - src/state/trainingStore.js (post Plan 05-01 — verify openConfirmModal + _confirmPayload + closeModal akcje)
    - .planning/phases/05-educational-layer/05-UI-SPEC.md sekcja "4. ConfirmModal" (linie 299-317) + "Copywriting Contract" rows linie 374-377
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-07, D-Phase5-23 modal pause)
    - src/i18n/pl.js (verify pl.modals.confirmScenarioSwitch shape — Plan 05-01)
    - src/training/scenarios/uruchomienie.js (verify scenario.id === 'uruchomienie' + ewentualny scenario.titlePL)
  </read_first>
  <behavior>
    - Test C1 (DOM mount): `new ConfirmModal({store, scenarios:{uruchomienie}})` mountuje 2 elementy do #modal-container: `.modal-overlay` + `dialog.modal-card.modal-card--confirm` (wariant z max-width:440px). Throw gdy #modal-container brakuje.
    - Test C2 (initial hidden): activeModal===null → .modal-overlay NIE ma klasy `--visible`; dialog nieotwarty.
    - Test C3 (open via store + payload render): store.openConfirmModal({current:'uruchomienie', next:'awaria', scenarioId:'awaria'}); store.setState({activeModal:'confirm-scenario-switch'}) (akcja Plan 05-01 robi oba w jednym setState) → .modal-overlay ma `--visible`; dialog open; body zawiera tekst z `pl.modals.confirmScenarioSwitch.body('uruchomienie', 'awaria')` (verify substring 'uruchomienie' AND substring 'awaria' AND substring 'Przerwiesz postęp').
    - Test C4 (close via Esc / store): activeModal:'confirm-scenario-switch' → null → klasa --visible usunięta.
    - Test C5 (content title): body header zawiera h2 z textContent === pl.modals.confirmScenarioSwitch.title ('Zmiana scenariusza').
    - Test C6 (confirm button): .modal-card__actions zawiera `<button class="btn primary">` z textContent === pl.modals.confirmScenarioSwitch.confirm ('Załaduj scenariusz').
    - Test C7 (cancel button): .modal-card__actions zawiera `<button class="btn secondary">` z textContent === pl.modals.confirmScenarioSwitch.cancel ('Anuluj').
    - Test C8 (confirm click → load + close): otwórz modal z payload {current:'uruchomienie', next:'uruchomienie', scenarioId:'uruchomienie'}; spy na store.startScenario; klik .btn.primary → startScenario wywołane 1× z scenarios.uruchomienie; activeModal → null.
    - Test C9 (cancel click → close ONLY, no load): otwórz z payload; spy na startScenario; klik .btn.secondary → activeModal===null; startScenario NIE wywołane.
    - Test C10 (overlay click → close ONLY, no load): klik na .modal-overlay (poza dialogiem) → activeModal===null; startScenario NIE wywołane.
    - Test C11 (Esc → close ONLY, no load): symulowany store.setState({activeModal:null}) (KeyboardController robi to przez Esc precedencję D-Phase5-20 — testowane Plan 05-03) → modal się chowa; startScenario NIE wywołane.
    - Test C12 (subscriber re-render na zmianę payload): open z payload A, sprawdź body; bez zamykania modify _confirmPayload z payload B (real test: closeModal → openConfirmModal({...newPayload}) — ConfirmModal subscriber wykrywa zmianę activeModal back to 'confirm-scenario-switch' → re-render z nowymi nazwami).
    - Test C13 (graceful brak payload): activeModal='confirm-scenario-switch' AND _confirmPayload===null (edge case) → body renderuje fallback (np. textContent='Zmień scenariusz?') LUB modal pozostaje hidden (planner ocenia; PREFEROWANE: log console.warn + render z fallback ('?', '?'); modal pozostaje funkcjonalny).
    - Test C14 (dispose): dispose() — removeEventListener (confirm + cancel + overlay), unsubscribe, .modal-overlay i dialog usunięte z DOM.
    - Test C15 (XSS safety): grep src/ui/ConfirmModal.js — `innerHTML` max 1 (skeleton statyczny analog HelpModal Test 12). Wszystkie dynamiczne stringi (payload.current, payload.next, button labels) przez textContent.
    - Test C16 (boundary smoke): readFileSync src/ui/ConfirmModal.js — regex `/import .* from ['"](three|gsap|@floating-ui|\.\.\/training\/|\.\.\/highlight\/|\.\.\/education\/)/` MUST NOT match. Tylko `../i18n/pl.js` dozwolone w imports.
  </behavior>
  <action>
    **RED:** Stwórz `tests/ConfirmModal.test.js` z 16 asercjami C1-C16. Setup beforeEach analog HelpModal.test.js: `document.body.innerHTML = '<div id="modal-container"></div>'`. Real store z `createTrainingStore()` (post Plan 05-01). DI `scenarios: {uruchomienie}` (import z `../src/training/scenarios/uruchomienie.js`).

    **GREEN:** Stwórz `src/ui/ConfirmModal.js` zgodnie z UI-SPEC §299-317 + PATTERNS HelpModal §326-408 (kopia struktury):

    1. Header komentarz: Phase 5 D-Phase5-07; boundary D-Phase5-26 (DOM + store + pl; NIE THREE/gsap/training/highlight/floating-ui/education).

    2. `import { pl } from '../i18n/pl.js';`

    3. `class ConfirmModal`:
       - `constructor({store, scenarios, rootElementId='modal-container'})`: pola `_store`, `_scenarios`, `_root` (throw gdy brak), `_unsubscribers=[]`. Wołaj `_build()`, `_wireSubscribers()`, `_render()`.
       - `_build()`:
         - `_overlay = createElement('div')` + className='modal-overlay' + aria-hidden='true'.
         - `_dialog = createElement('dialog')` + className='modal-card modal-card--confirm' + role='dialog' + aria-modal='true' + aria-labelledby='confirm-modal-title'.
         - Statyczny innerHTML szkielet (XSS-safe — bez user content):
           ```
           <header class="modal-card__header">
             <h2 id="confirm-modal-title" class="modal-card__title"></h2>
             <button class="modal-card__close" type="button"></button>
           </header>
           <div class="modal-card__body"></div>
           <div class="modal-card__actions">
             <button class="btn secondary" data-action="cancel" type="button"></button>
             <button class="btn primary" data-action="confirm" type="button"></button>
           </div>
           ```
         - Wypełnij textContent: title = pl.modals.confirmScenarioSwitch.title; close.aria-label = pl.modals.closeAria + textContent='✕'; cancel.textContent = pl.modals.confirmScenarioSwitch.cancel; confirm.textContent = pl.modals.confirmScenarioSwitch.confirm.
         - Body element (`.modal-card__body`) ma 1 `<p class="confirm-modal__body-text">` element (textContent ustawiany w _render z payload).
         - `_root.appendChild(_overlay); _root.appendChild(_dialog);`.
         - Bound handlers:
           - `_onCancel = () => { this._store.getState().closeModal(); };`
           - `_onConfirm = () => { const state = this._store.getState(); const payload = state._confirmPayload; if (payload && this._scenarios[payload.scenarioId]) { state.startScenario(this._scenarios[payload.scenarioId]); } state.closeModal(); };`
           - `_onOverlayClick = () => { this._store.getState().closeModal(); };`
         - Wpięcie listenerów: cancel button, confirm button, overlay.
       - `_wireSubscribers()`: `this._unsubscribers.push(this._store.subscribe(s => s.activeModal, () => this._render()));` + (opcjonalnie subscribe na `s => s._confirmPayload` gdyby planner chciał odpowiadać na zmianę payload bez open/close — verify Test C12 czy wymagane; analog HelpModal: pojedynczy sub na activeModal wystarcza bo openConfirmModal zmienia OBA pola jednym setState).
       - `_render()`:
         - `const state = this._store.getState();`
         - `const isOpen = state.activeModal === 'confirm-scenario-switch';`
         - `if (isOpen) { const p = state._confirmPayload ?? {current:'?', next:'?'}; this._dialog.querySelector('.confirm-modal__body-text').textContent = pl.modals.confirmScenarioSwitch.body(p.current, p.next); }` (textContent XSS-safe; fallback dla Test C13).
         - Toggle `modal-overlay--visible` na overlay.
         - Dialog open/close: `if (isOpen) (this._dialog.showModal?.() ?? this._dialog.setAttribute('open','')); else (this._dialog.close?.() ?? this._dialog.removeAttribute('open'));`.
       - `dispose()`: removeEventListener × 3 (cancel/confirm/overlay); for u of _unsubscribers u(); _overlay.remove(); _dialog.remove(). Idempotent.

    4. JSDoc po polsku, identyfikatory ENG. Boundary: tylko `import { pl } from '../i18n/pl.js';` — zero THREE/gsap/floating-ui/training/highlight/education.

    Po RED → GREEN: `npm test -- --run tests/ConfirmModal.test.js` PASS 16/16. Commit `feat(05-08): ConfirmModal — D-Phase5-07 scenario switch confirmation`.
  </action>
  <verify>
    <automated>npm test -- --run tests/ConfirmModal.test.js</automated>
  </verify>
  <done>
    - 16/16 testów ConfirmModal PASS.
    - Plik src/ui/ConfirmModal.js istnieje, max 1 innerHTML (skeleton), 1 import (pl).
    - XSS-safe: payload current/next przez textContent.
    - Boundary smoke (Test C16) zielony.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED + GREEN — KeyboardController._loadScenario gating</name>
  <read_first>
    - src/education/KeyboardController.js (post Plan 05-03 — verify obecna implementacja _loadScenario linie ~28-35)
    - tests/KeyboardController.test.js (post Plan 05-03 — wzór 13 testów; verify że Test 9 obecnie testuje natychmiast startScenario przy '1' bez gating)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-07 + interfaces sekcja powyżej)
    - src/state/trainingStore.js (post Plan 05-01 — verify openConfirmModal + startScenario sygnatury)
  </read_first>
  <behavior>
    Dorzucone testy do tests/KeyboardController.test.js w nowym describe `Phase 5 — _loadScenario gating (D-Phase5-07, Plan 05-08)`:

    - Test G1 (mid-run gating): store.startScenario(uruchomienie); state.currentStepId !== null AND co najmniej 1 krok status !== 'done'; spy na store.openConfirmModal; spy na store.startScenario; dispatch '1' → openConfirmModal wywołane 1× z {current:'uruchomienie', next:'uruchomienie', scenarioId:'uruchomienie'} (lub current=activeScenario.id); startScenario NIE wywołane DRUGI raz (pierwsze wywołanie z startScenario w setup — verify że count NIE wzrasta po dispatch '1').

    - Test G2 (no active scenario bypass): store.activeScenario === null; state.currentStepId === null; dispatch '1' → startScenario wywołane 1× z scenarios.uruchomienie; openConfirmModal NIE wywołane (spy 0).

    - Test G3 (all done bypass): store.startScenario(uruchomienie); MOCK state.steps wszystkie status='done' (np. setState({steps: Object.fromEntries(allStepIds.map(id=>[id,{...,status:'done'}]))})); currentStepId może być null lub last step; dispatch '1' → startScenario wywołane (reset scenariusza); openConfirmModal NIE wywołane.

    - Test G4 (modal-aware blocking nie omija gating): state.activeModal='help'; dispatch '1' → KeyboardController D-Phase5-21 modal-aware blocking ⇒ '1' jest no-op (Plan 05-03 Test 6); openConfirmModal NIE wywołane; startScenario NIE wywołane. (Sanity — Plan 05-08 gating jest WEWNĄTRZ _loadScenario, ale modal blocking dzieje się WCZEŚNIEJ w _handleKeyDown.)

    Update src/education/KeyboardController.js _loadScenario (linie ~28-35):
    ```javascript
    _loadScenario(id) {
      if (!this._scenarios[id]) {
        console.warn(`[KeyboardController] scenariusz ${id} — Phase 6 (placeholder, brak danych)`);
        return;
      }
      const state = this._store.getState();
      // D-Phase5-07 gating: jeśli kursant ma aktywną procedurę (currentStepId !== null)
      // I nie wszystkie kroki są done — wymagaj potwierdzenia przez ConfirmModal.
      const hasActiveStep = state.currentStepId !== null;
      const stepsObj = state.steps || {};
      const stepValues = Object.values(stepsObj);
      const allDone = stepValues.length > 0 && stepValues.every(s => s.status === 'done');
      if (hasActiveStep && !allDone) {
        state.openConfirmModal({
          current: state.activeScenario?.id ?? '',
          next: id,
          scenarioId: id,
        });
        return;
      }
      // Bezwarunkowo dozwolone: brak aktywnej procedury lub wszystkie kroki done.
      state.startScenario(this._scenarios[id]);
    }
    ```

    Zerowa zmiana w `_handleKeyDown` — gating jest WEWNĄTRZ _loadScenario, więc istniejące Plan 05-03 testy (Test 6 modal-blocking, Test 9 '1' loads) zachowują semantykę z drobną korektą: Test 9 w Plan 05-03 zakładał że '1' bezwarunkowo woła startScenario. Plan 05-08 modyfikuje TEN test: '1' woła startScenario TYLKO gdy brak gating warunków (lub openConfirmModal gdy gating spełniony). Plan 05-03 Test 9 setup: store w stanie initial (currentStepId===null, activeScenario===null) → spełnia Test G2 → startScenario wywołane. Plan 05-03 Test 9 NIE wymaga aktualizacji jeśli setup był initial state; verify w Task 2.
  </behavior>
  <action>
    1. Dorzucić 4 testy G1-G4 do tests/KeyboardController.test.js (nowy describe na końcu pliku).
    2. RED → GREEN edycja src/education/KeyboardController.js._loadScenario per `<behavior>` snippet.
    3. Zweryfikować: pre-existing Plan 05-03 Test 9 (dispatch '1' → startScenario) nadal PASS — setup używa initial state (currentStepId===null lub activeScenario===null), więc bypass aktywuje się i startScenario wywołane bez gating.
    4. Verify: zero nowych importów w KeyboardController.js (boundary unchanged — D-Phase5-26).
    5. `npm test -- --run tests/KeyboardController.test.js` PASS (13 baseline Plan 05-03 + 4 nowe = 17/17).

    Commit `feat(05-08): KeyboardController._loadScenario gating przez ConfirmModal (D-Phase5-07)`.
  </action>
  <verify>
    <automated>npm test -- --run tests/KeyboardController.test.js tests/ConfirmModal.test.js</automated>
  </verify>
  <done>
    - 4 nowe asercje G1-G4 PASS.
    - 13 baseline Plan 05-03 PASS (backward compat).
    - _loadScenario rozszerzony o gating bez nowych importów.
    - Boundary i Polish-literal scanner zielone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KeyboardController._loadScenario → store.openConfirmModal | DI store; payload to plain object z id-ami (nie user input) |
| ConfirmModal.btn.primary click → store.startScenario | bezpośredni call ze scenario z DI `scenarios` map; ConfirmModal nie zna THREE |
| ConfirmModal close (cancel/overlay/Esc) → store.closeModal | brak destrukcji currentStepId — payload zachowany w state aż do następnego openConfirmModal |
| pl.modals.confirmScenarioSwitch.body(payload) → DOM textContent | payload current/next są scenario id (statyczne dane Phase 5/6); XSS-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-XSS | Tampering | payload.current/payload.next renderowane do DOM | mitigate | textContent (Test C3/C12 weryfikuje przez substring check; Test C15 weryfikuje max 1 innerHTML). Payload pochodzi z KeyboardController._loadScenario gdzie current=state.activeScenario.id (statyczne id) i next=id (literał klawisza, lookup w this._scenarios). |
| T-05-08-RACE | Tampering | rapid '1' dispatch z aktywną procedurą | mitigate | openConfirmModal jest idempotentny — drugi dispatch '1' przy activeModal='confirm-scenario-switch' jest blokowany przez D-Phase5-21 modal-aware blocking (Plan 05-03 Test 6). Zustand subscribeWithSelector zapewnia CHANGE-only re-render w ConfirmModal._render. |
| T-05-08-BYPASS | Elevation of Privilege | ConfirmModal "Załaduj" wywołuje startScenario z DOWOLNYM scenarioId | accept | Payload.scenarioId pochodzi z KeyboardController._loadScenario(id), gdzie id ∈ {'uruchomienie','2','3','4'} (klawisze 1-4) + walidacja `if (!this._scenarios[id]) console.warn + return`. ConfirmModal._onConfirm dodatkowo guarduje `if (this._scenarios[payload.scenarioId])` przed startScenario. Phase 6 dorzuca scenarios 2-4. Brak path-traversal — keys są literałami klawiszy mapowanymi w KeyboardController. |
| T-05-08-LEAK | Denial of Service | ConfirmModal listener leak po HMR | mitigate | Test C14: dispose usuwa 3 listenery (cancel/confirm/overlay) + unsubscribe. Plan 05-07 wpina dispose do Application chain (przed HelpModal, po TooltipManager — odwrotna kolejność tworzenia). |
| T-05-08-FOCUS | Information Disclosure | focus trap w modal a11y | accept | `<dialog>` native + showModal — browser zarządza focus trap. jsdom fallback (Test C2/C3 używają attribute 'open' check). Pełna a11y QA = Plan 05-07 Manual QA punkt 6. |
</threat_model>

<verification>
- `npm test -- --run tests/ConfirmModal.test.js tests/KeyboardController.test.js` — 16+17 = 33 PASS.
- `grep -c "^import" src/ui/ConfirmModal.js` === 1 (tylko `import { pl }`).
- `grep -c "innerHTML" src/ui/ConfirmModal.js` ≤ 1 (skeleton XSS-safe).
- `grep -c "^import" src/education/KeyboardController.js` === 0 (boundary unchanged — DI only).
- `grep -c "openConfirmModal" src/education/KeyboardController.js` === 1 (gating call).
- `grep -c "modal-card--confirm" src/ui/ConfirmModal.js` ≥ 1.
- Pełny suite zielony (Plan 05-08 nie łamie Plan 05-01/05-03 baseline).
</verification>

<success_criteria>
- D-Phase5-07 spełnione: ConfirmModal pojawia się mid-run przy '1-4'; modal pauzuje SOP (activeModal !== null → D-Phase5-23/28 trigger w Plan 05-07).
- Bezwarunkowo dozwolone (no gating): currentStepId === null OR wszystkie steps done.
- ConfirmModal: title + body(current,next) + 2 przyciski (Anuluj/Załaduj); XSS-safe; lifecycle czysty.
- KeyboardController._loadScenario rozszerzony bez złamania boundary; 13 baseline Plan 05-03 testów PASS.
- LOCKED decyzja D-Phase5-07 zaimplementowana w pełni (post-revision iteration 2).
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-08-SUMMARY.md` — opisz: ConfirmModal lifecycle (build + 3 close handlers + payload subscriber), KeyboardController._loadScenario gating decision tree (active + !allDone → confirm; else → direct startScenario), payload shape, decision NIE rozszerzać closeModal o args (ConfirmModal sam dispatch startScenario), backward compat z Plan 05-03 (13 baseline PASS + 17 total po +4 G-testy). Mapuj na D-Phase5-07.
</output>
