---
phase: 05-educational-layer
plan: 03
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/education/KeyboardController.js
  - src/ui/HelpModal.js
  - tests/KeyboardController.test.js
  - tests/HelpModal.test.js
autonomous: true
requirements: [INTERACT-06]
tags: [phase-5, keyboard, modal, dom]

must_haves:
  truths:
    - "Single window.addEventListener('keydown') mapuje 11 klawiszy (R/T/1-4/Space/Esc/H/L/M) na store akcje (D-Phase5-19)"
    - "Esc precedencja: activeModal!==null → closeModal(); inaczej → triggerEStop (D-Phase5-20)"
    - "Modal-aware blocking: gdy activeModal!==null, R/T/1-4/Space/L/M no-op; H toggle'uje (D-Phase5-21)"
    - "L no-op gdy difficulty==='egzamin'; M zawsze działa (D-Phase5-22)"
    - "HelpModal renderuje keymap (z pl.keymap) + legendę kolorów + legendę ikon + disclaimer (D-Phase5-23 SC5)"
    - "HelpModal otwiera/zamyka się przez subscriber na state.activeModal === 'help' (close: H/Esc/X button/klik overlay)"
  artifacts:
    - path: "src/education/KeyboardController.js"
      provides: "Globalny keyboard mapping → store"
      min_lines: 80
    - path: "src/ui/HelpModal.js"
      provides: "Modal blokujący z keymap + legendami"
      min_lines: 120
    - path: "tests/KeyboardController.test.js"
      provides: "INTERACT-06 + Esc precedencja coverage"
    - path: "tests/HelpModal.test.js"
      provides: "Modal lifecycle + content coverage"
  key_links:
    - from: "src/education/KeyboardController.js"
      to: "store akcje (toggleHelp/toggleFreeRoam/toggleMute/toggleLabels/resetScenario/closeModal)"
      via: "store.getState().action()"
      pattern: "store\\.getState\\(\\)"
    - from: "src/ui/HelpModal.js"
      to: "pl.keymap + pl.modals.help + pl.modals.closeAria"
      via: "import pl"
      pattern: "pl\\.keymap|pl\\.modals"
---

<objective>
INTERACT-06: globalny `KeyboardController` mapujący 11 klawiszy na akcje store + `HelpModal` modalny renderujący keymap z `pl.keymap` jako single source. Razem spełniają SC5 Phase 5 (skróty + H-toggleable help overlay).

Purpose: Plan 05-03 dostarcza warstwę input (keyboard) i informacyjny modal jako parę: KeyboardController otwiera HelpModal przez `store.toggleHelp()`, HelpModal czyta `state.activeModal` i renderuje siebie. Oba używają pl.keymap/pl.modals (Plan 05-01).
Output: 2 nowe klasy + 2 nowe pliki testów; reszta wiringu (Application bootstrap + dispose) idzie w Plan 05-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@src/RaycastController.js
@src/DisclaimerBanner.js
@src/ui/StatusPanel.js
@src/i18n/pl.js

<interfaces>
Store akcje (Plan 05-01 dorzuca):
- toggleHelp(), closeModal(), toggleFreeRoam(), toggleMute(), toggleLabels(), resetScenario(), setDifficulty(...), openConfirmModal(payload)
- triggerEStop() — istniejący Phase 1 invariant SOP-09 (planner: jeśli store nie ma tej akcji explicit, KeyboardController woła `state.triggerEStop?.()` defensywnie — Phase 6 dopina pełną semantykę; Phase 5 nie wymaga aby Esc faktycznie zatrzymywał maszynę gdy modal zamknięty, wystarczy że Esc z modalem → closeModal, Esc bez modalu → wywołuje triggerEStop jeśli istnieje, inaczej no-op).
- toggleSimulation() — RESEARCH Open Question #2: planner zostawia jako optional chain `state.toggleSimulation?.()` LUB dorzuca cienki adapter w Plan 05-07 (Application wpina DI z `ui.toggleBtn()`). Plan 05-03 zostawia `state.toggleSimulation?.()` — jeśli undefined, Space no-op (testy weryfikują że wywołanie NIE rzuca).

DOM mount:
- #modal-container istnieje w index.html (Plan 05-01); HelpModal mountuje overlay + dialog jako children.

pl.js (Plan 05-01):
- pl.keymap: Array<{key, descriptionPL, group}> (11 wpisów)
- pl.modals.help: {title, sectionKeymap, sectionColors, sectionIcons, sectionDisclaimer, colorError, colorSuccess, colorHint, colorHC}
- pl.modals.closeAria: 'Zamknij'
- pl.stepStates, pl.stepStateIcons, pl.machineState, pl.machineStateIcons (legenda ikon)
- pl.disclaimer.full (legenda sekcji disclaimer)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED + GREEN — KeyboardController</name>
  <read_first>
    - .planning/phases/05-educational-layer/05-RESEARCH.md "KeyboardController — Esc precedencja" (linie 829-873)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "src/education/KeyboardController.js" (linie 192-245)
    - src/RaycastController.js (cały — wzór bound listener + dispose pattern)
    - tests/RaycastController.test.js (wzór window event dispatch + spy)
    - src/state/trainingStore.js (Plan 05-01 baseline — verify akcje)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-19/20/21/22)
  </read_first>
  <behavior>
    - Test 1 (rejestracja): `new KeyboardController({store})` wywołuje `window.addEventListener('keydown', boundHandler)` 1×. Spy na `window.addEventListener`.
    - Test 2 (dispose): `controller.dispose()` wywołuje `window.removeEventListener('keydown', tenSamReference)`.
    - Test 3 (H toggle help): dispatchEvent KeyboardEvent('keydown',{key:'h'}) gdy activeModal===null → store.activeModal==='help'. Drugie dispatch → null (toggle).
    - Test 4 (Esc precedencja w modalu): activeModal='help'; dispatch Escape → activeModal===null. `triggerEStop` NIE wywołane (spy zero).
    - Test 5 (Esc bez modalu): activeModal===null; dispatch Escape → wywołane `state.triggerEStop?.()` jeśli istnieje (mock akcji w store), albo no-op gdy undefined (test nie rzuca). Test ze stubem `store.setState({triggerEStop: vi.fn()})` weryfikuje 1 wywołanie.
    - Test 6 (modal-aware blocking): activeModal='help'; dispatch 'r' / 't' / '1' / ' ' / 'l' / 'm' → żadna akcja store NIE wywołana (spy na resetScenario/toggleFreeRoam/toggleMute/toggleLabels zero). H i Esc działają (Test 3+4).
    - Test 7 (R reset): activeModal===null; dispatch 'r' → `resetScenario` wywołane 1×. Z activeScenario null → no-op (resetScenario sam guarduje, Plan 05-01).
    - Test 8 (T free-roam): dispatch 't' → state.freeRoam flip.
    - Test 9 (1 load scenario): dispatch '1' → ładuje scenariusz 'uruchomienie' (przez DI `scenarios: {uruchomienie: <obj>}` w konstruktorze; controller woła `store.getState().startScenario(scenarios.uruchomienie)`). 2/3/4 — Phase 6 stub: console.warn (nie throw); test weryfikuje że NIE rzuca i NIE zmienia activeScenario.
    - Test 10 (Space): dispatch ' ' (event.key===' ') → `state.toggleSimulation?.()` wywołane jeśli istnieje (mock); brak akcji → no-op (nie rzuca). RESEARCH Open Question #2 + correction A5.
    - Test 11 (L w Egzaminie): setState({difficulty:'egzamin'}); dispatch 'l' → toggleLabels NIE wywołane (no-op per D-Phase5-22). Z difficulty='nauka' → toggleLabels wywołane.
    - Test 12 (M zawsze): difficulty='egzamin'; dispatch 'm' → toggleMute wywołane (M ignoruje difficulty per D-Phase5-22).
    - Test 13 (boundary smoke): readFileSync 'src/education/KeyboardController.js' — regex `/import .* from ['"](three|gsap|@floating-ui|\.\.\/training\/|\.\.\/highlight\/)/` MUST NOT match.
  </behavior>
  <action>
    RED: stworzyć `tests/KeyboardController.test.js` z 13 asercjami. Real store `createTrainingStore()` z Plan 05-01 (post-Wave-1). Helper `dispatch(key)` =`window.dispatchEvent(new KeyboardEvent('keydown', {key}))`. Spy na akcje store przez `vi.spyOn(store.getState(), 'toggleHelp')` PRZED `new KeyboardController(...)`.

    GREEN: stworzyć `src/education/KeyboardController.js` zgodnie z 05-PATTERNS.md §192-245 + 05-RESEARCH.md §829-873:

    1. Header komentarz: Phase 5 INTERACT-06; D-Phase5-19/20/21/22; boundary D-Phase5-26 (store+window; NIE THREE/gsap/training/highlight/floating-ui).

    2. `class KeyboardController`:
       - `constructor({store, scenarios})`: pola `_store`, `_scenarios` (Map id→scenario module — Plan 05-07 wstrzyknie `{uruchomienie}`), `_onKeyDown = this._handleKeyDown.bind(this)`. `window.addEventListener('keydown', this._onKeyDown)`.
       - `_handleKeyDown(event)`:
         - `const key = event.key === ' ' ? 'space' : event.key.toLowerCase();` (RESEARCH correction A5).
         - `const state = this._store.getState();`
         - `if (key === 'escape') { if (state.activeModal !== null) state.closeModal(); else state.triggerEStop?.(); return; }` (D-Phase5-20).
         - `if (key === 'h') { state.toggleHelp(); return; }` (H zawsze działa per D-Phase5-21).
         - `if (state.activeModal !== null) return;` (D-Phase5-21 modal blocking).
         - Lookup actions: `{r: ()=>state.resetScenario(), t: ()=>state.toggleFreeRoam(), '1': ()=>this._loadScenario('uruchomienie'), '2'/'3'/'4': ()=>console.warn('[KeyboardController] scenariusz 2/3/4 — Phase 6'), space: ()=>state.toggleSimulation?.(), l: ()=>{ if(state.difficulty!=='egzamin') state.toggleLabels(); }, m: ()=>state.toggleMute()}`.
         - `actions[key]?.();`
       - `_loadScenario(id)`: jeśli `this._scenarios[id]` → `this._store.getState().startScenario(this._scenarios[id])`. Else console.warn.
       - `dispose()`: `window.removeEventListener('keydown', this._onKeyDown)`.

    3. JSDoc po polsku. Boundary: ZERO importów (DI store + scenarios; window jest globalny).

    Po RED → GREEN: `npm test -- --run tests/KeyboardController.test.js` MUST PASS. Commit `feat(05-03): KeyboardController — INTERACT-06 9-klawiszowy mapping + Esc precedence`.
  </action>
  <verify>
    <automated>npm test -- --run tests/KeyboardController.test.js</automated>
  </verify>
  <done>
    - 13/13 testów PASS.
    - Plik `src/education/KeyboardController.js` istnieje, zero importów (`grep -c "^import" → 0`).
    - boundary smoke test (Test 13) zielony — żadnego z listy zakazanych importów.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED + GREEN — HelpModal (DOM build + subscriber + render)</name>
  <read_first>
    - .planning/phases/05-educational-layer/05-UI-SPEC.md sekcja "3. HelpModal" (linie 184-296) + "DOM Mount Points" (linie 458-477)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "src/ui/HelpModal.js" (linie 326-408)
    - src/ui/StatusPanel.js (wzór _build static innerHTML skeleton + textContent dla user content + 3 subscribers + dispose)
    - src/DisclaimerBanner.js (wzór single DOM element + role='dialog' + close listener + dispose)
    - tests/StatusPanel.test.js (wzór describe DOM render + subscriber re-render)
    - src/i18n/pl.js (Plan 05-01 verify pl.keymap.length===11 + pl.modals.help shape)
    - index.html (Plan 05-01 — verify #modal-container)
  </read_first>
  <behavior>
    - Test 1 (constructor mount): `new HelpModal({store})` mountuje 2 elementy do #modal-container: `.modal-overlay` + `dialog.modal-card` (lub div fallback gdy jsdom nie wspiera). Throw gdy #modal-container brakuje.
    - Test 2 (initial state hidden): activeModal===null → .modal-overlay NIE ma klasy `--visible` (lub aria-hidden=true); dialog.open===false (lub brak atrybutu 'open').
    - Test 3 (open via store): store.setState({activeModal:'help'}) → .modal-overlay ma klasę `--visible`; dialog ma atrybut open (lub showModal wywołane).
    - Test 4 (close via store): activeModal:'help' → null → klasa --visible usunięta; dialog zamknięty.
    - Test 5 (content keymap): po otwarciu modal-card__body zawiera tabelę z 11 wierszami (po jednym na pl.keymap entry). Każdy wiersz ma `<kbd>` z key + `<td>` z descriptionPL (textContent — XSS-safe). Test: `body.querySelectorAll('.keymap-table tr').length === 11` (lub 12 jeśli liczy header).
    - Test 6 (content color legend): body zawiera .color-legend z 4 wierszami; każdy ma .color-swatch (style.background) + text z pl.modals.help.colorError/Success/Hint/HC.
    - Test 7 (content icon legend): body zawiera .icon-legend z 4 stepStates (pl.stepStates × pl.stepStateIcons) + 7 machineStates (pl.machineState × pl.machineStateIcons) + 2 trybów difficulty (📚 Nauka, 📝 Egzamin z pl.ui).
    - Test 8 (content disclaimer): body zawiera .disclaimer-repeat z pl.disclaimer.full jako textContent.
    - Test 9 (close button click): klik na .modal-card__close → store.activeModal===null. removeEventListener wpięty w dispose.
    - Test 10 (overlay click): klik na .modal-overlay (poza .modal-card) → store.activeModal===null.
    - Test 11 (dispose): dispose() — listener close usunięty, listener overlay usunięty, subskryber unsub, elementy usunięte z DOM (.modal-overlay && .modal-card NIE są dziećmi #modal-container).
    - Test 12 (XSS safety): grep w src/ui/HelpModal.js — `innerHTML` użyte CO NAJWYŻEJ na statycznym szkielecie (wzór StatusPanel._build); dynamiczne stringi (pl.keymap entries, pl.disclaimer.full) MUSZĄ używać textContent (nie innerHTML). Asercja: `(src.match(/innerHTML/g) || []).length <= 1` (max 1 — initial skeleton).
  </behavior>
  <action>
    RED: stworzyć `tests/HelpModal.test.js` z 12 asercjami. Setup beforeEach: stwórz `document.body.innerHTML = '<div id="modal-container"></div>'` (analog tests/StatusPanel.test.js). Real store, wstrzyknij. Wymuś otwarcie `store.setState({activeModal:'help'})`; pomiar DOM.

    GREEN: stworzyć `src/ui/HelpModal.js` zgodnie z 05-PATTERNS.md §326-408 + UI-SPEC §184-296:

    1. Header: Phase 5 INTERACT-06 SC5; boundary D-Phase5-26 (DOM+store+pl; NIE THREE/gsap/training).

    2. `import { pl } from '../i18n/pl.js';`

    3. `class HelpModal`:
       - `constructor({store, rootElementId='modal-container'})`: pola `_store`, `_root` (z `document.getElementById(rootElementId)`), throw gdy brak. `_unsubscribers=[]`. Wołaj `_build()`, `_wireSubscribers()`, `_render()` (initial — analog StatusPanel).
       - `_build()`:
         - Stwórz `_overlay = document.createElement('div')` z `className='modal-overlay'`, `aria-hidden='true'`.
         - Stwórz `_dialog = document.createElement('dialog')` z `className='modal-card'`, `role='dialog'`, `aria-modal='true'`, `aria-labelledby='modal-title'`.
         - **Statyczny innerHTML szkielet** dialoga (XSS-safe — tylko literały HTML, brak user content): `<header class="modal-card__header"><h2 id="modal-title" class="modal-card__title"></h2><button class="modal-card__close" type="button"></button></header><div class="modal-card__body"></div>` (analog StatusPanel._build linie 59-67).
         - Wypełnij title przez `textContent = pl.modals.help.title`; close button: `textContent='✕'`, `aria-label=pl.modals.closeAria`.
         - Build body przez createElement (NIE innerHTML):
           - Sekcja 1: `<section class="help-section">` + `<h3 class="help-section__heading">` z `textContent=pl.modals.help.sectionKeymap` + `<table class="keymap-table">` z `<thead><tr><th></th><th></th><th></th></tr></thead>` z textContent ustawianym przez `pl.modals.help.keyHeader` / `actionHeader` / `groupHeader` (te 3 klucze dorzucone w Plan 05-01 Task 2 — XSS-safe przez textContent na pustych <th>). Każdy wiersz: `<tr><td><kbd>${entry.key}</kbd></td><td>${entry.descriptionPL}</td><td>${entry.group}</td></tr>` — przez createElement i textContent.
           - Sekcja 2: `<section class="help-section">` + heading sectionColors + `<ul class="color-legend">` z 4 `<li>`. Każdy li: `<span class="color-swatch" style="background:${HEX}"></span> ${pl.modals.help.colorError/Success/Hint/HC}` przez createElement (NIE innerHTML, style przez element.style.background = '#D55E00').
           - Sekcja 3: heading sectionIcons + 3 podsekcje (stepStates z pl.stepStateIcons[k]+' '+pl.stepStates[k]; machineState analogicznie; difficulty 2 entries z pl.ui).
           - Sekcja 4: heading sectionDisclaimer + `<p class="disclaimer-repeat">` z textContent = pl.disclaimer.full.
         - `_root.appendChild(this._overlay); _root.appendChild(this._dialog);`
         - Bound handlers: `this._onClose = () => this._store.getState().closeModal();`. `this._dialog.querySelector('.modal-card__close').addEventListener('click', this._onClose);`. `this._overlay.addEventListener('click', this._onClose);`. (Klik w środku dialog — NIE bubbluje do overlay bo dialog jest siblingiem nie dzieckiem overlay; planner verify.)
       - `_wireSubscribers()`: `this._unsubscribers.push(this._store.subscribe(s => s.activeModal, () => this._render()));`
       - `_render()`: `const isOpen = this._store.getState().activeModal === 'help';` → toggle `modal-overlay--visible` na overlay; dialog: `if(isOpen) (this._dialog.showModal?.() ?? this._dialog.setAttribute('open',''))` else `(this._dialog.close?.() ?? this._dialog.removeAttribute('open'))`. (Fallback gdy jsdom nie wspiera HTMLDialogElement — UI-SPEC §207.)
       - `dispose()`: removeEventListener close + overlay; for u of _unsubscribers u(); _overlay.remove(); _dialog.remove().

    4. JSDoc po polsku, identyfikatory ENG.

    Po RED → GREEN: `npm test -- --run tests/HelpModal.test.js` PASS. Commit `feat(05-03): HelpModal — INTERACT-06 SC5 keymap + legendy + disclaimer`.

    UWAGA: Plan 05-01 (post-revision) dorzucił do pl.modals.help klucze keyHeader ("Klawisz"), actionHeader ("Akcja"), groupHeader ("Grupa") — Test 4 Plan 05-01 obejmuje już te 3 klucze. Plan 05-03 KONSUMUJE je read-only przez import `pl.modals.help`. NIE modyfikujemy src/i18n/pl.js (nie ma w files_modified) — jeśli któryś klucz brakuje, oznacza to że Plan 05-01 nie został wykonany zgodnie z planem; ZGŁOSIĆ regresję, NIE patchować defensywnie.
  </action>
  <verify>
    <automated>npm test -- --run tests/HelpModal.test.js tests/i18n.pl.test.js</automated>
  </verify>
  <done>
    - 12/12 testów HelpModal PASS.
    - HelpModal renderuje 4 sekcje z pl.keymap (11 wierszy) + 4 kolory + 13 ikon (4 step + 7 machine + 2 difficulty) + disclaimer.
    - XSS-safe: max 1 innerHTML w pliku (skeleton).
    - Boundary: importuje tylko `../i18n/pl.js` (zero THREE/gsap/training).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.keydown → store actions | KeyboardController jest jedyną bramą; nie omija store |
| pl.* → HelpModal DOM | dane statyczne; textContent (XSS-safe) |
| DOM click events → store.closeModal | overlay/close button → kontrolowane actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-XSS | Tampering | pl.keymap entries renderowane do DOM | mitigate | Wszystkie dynamiczne stringi przez textContent (Test 12: max 1 innerHTML w HelpModal — statyczny szkielet bez user content). Pattern z StatusPanel/StepPanel (T-04-09 mitigation). |
| T-05-03-FOCUS | Information Disclosure | focus trap w modal (a11y) | accept | Plan 05-03 używa `<dialog>` natywnego + showModal() — przeglądarka sama zarządza focus trap. jsdom fallback: dialog.open attribute (test env only). Pełny focus trap behavior verify w manual QA Plan 05-07 SC5. |
| T-05-03-RACE | Tampering | rapid keydown przy modal toggle | mitigate | Subscriber CHANGE-only (zustand subscribeWithSelector) — duplikatowe activeModal:'help' nie odpalają re-render. Test 3+4 weryfikuje pojedyncze re-render. |
| T-05-03-LEAK | Denial of Service | KeyboardController leak po HMR | mitigate | Test 2: dispose woła removeEventListener z bound reference. Plan 05-07 wpina dispose do Application.dispose chain (STATE-03 invariant). |
</threat_model>

<verification>
- `npm test -- --run tests/KeyboardController.test.js tests/HelpModal.test.js` — 25/25 PASS.
- `grep -c "^import" src/education/KeyboardController.js` === 0 (zero imports — DI only).
- `grep -c "^import" src/ui/HelpModal.js` === 1 (tylko `import { pl }`).
- `grep -c "innerHTML" src/ui/HelpModal.js` ≤ 1 (XSS-safe).
- Smoke: pełny suite `npm test` zielony.
</verification>

<success_criteria>
- KeyboardController obsługuje 11 klawiszy zgodnie z mapowaniem D-Phase5-19; Esc precedencja D-Phase5-20 spełniona; modal-aware blocking D-Phase5-21; L/M asymetria D-Phase5-22.
- HelpModal renderuje wszystkie 4 sekcje z UI-SPEC §246-296 jako single source z pl.keymap/pl.modals; XSS-safe; lifecycle czysty.
- Brak złamań boundary; Polish-literal scanner zielony (wszystkie polskie stringi czytane z pl.js).
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-03-SUMMARY.md` — opisz: mapping 11 klawiszy → akcje, Esc precedencja matrix (3 przypadki), modal-aware actions blocked list (6 klawiszy), HelpModal 4 sekcje content sources, XSS-safe pattern (max 1 innerHTML), dispose chain. Mapuj na D-Phase5-19..24.
</output>
