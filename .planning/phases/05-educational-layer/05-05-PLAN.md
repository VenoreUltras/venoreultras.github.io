---
phase: 05-educational-layer
plan: 05
type: tdd
wave: 3
depends_on: ["05-01"]
files_modified:
  - src/highlight/EmissiveController.js
  - src/highlight/HighlightManager.js
  - src/education/LabelOverlay.js
  - tests/EmissiveController.test.js
  - tests/HighlightManager.test.js
  - tests/LabelOverlay.test.js
autonomous: true
requirements: [FEEDBACK-06]
tags: [phase-5, labels, css2drenderer, highlight, hint-layer]

must_haves:
  truths:
    - "EmissiveController stack rozszerzony z 2 do 3 warstw (hover < hint < state) — D-Phase5-03"
    - "HighlightManager projektuje hint warstwę gdy difficulty='nauka' AND currentStepId AND nie freeRoam (D-Phase5-03/12)"
    - "Hint warstwa: kolor #F0E442 (Wong yellow), emissiveIntensity 0.3, STATYCZNA (bez pulse/flash)"
    - "LabelOverlay tworzy CSS2DObject per interactable (15 labelek) jako children meshy"
    - "L toggle: state.labelsVisible flipuje widoczność wszystkich labelek; difficulty='egzamin' force-hide (D-Phase5-22)"
    - "Camera-facing filter per-frame: dot(worldNormal, cameraDir) < 0 = facing camera"
    - "Declutter sort-by-Z + offset +20px Y gdy screen distance < 40px (O(n²) acceptable n=15)"
  artifacts:
    - path: "src/highlight/EmissiveController.js"
      provides: "Stack 3-warstwowy (hover/hint/state)"
      contains: "hint:"
    - path: "src/highlight/HighlightManager.js"
      provides: "hint projector + ERROR/SUCCESS"
      contains: "HINT_HEX"
    - path: "src/education/LabelOverlay.js"
      provides: "CSS2DRenderer + 15 CSS2DObject labels"
      min_lines: 120
  key_links:
    - from: "src/highlight/HighlightManager.js"
      to: "EmissiveController.setLayer('hint', mesh, {color, intensity})"
      via: "DI emissive"
      pattern: "setLayer\\(.'hint'"
    - from: "src/education/LabelOverlay.js"
      to: "three/addons/renderers/CSS2DRenderer.js"
      via: "import"
      pattern: "CSS2DRenderer"
---

<objective>
FEEDBACK-06: warstwa etykiet 3D (CSS2DRenderer) toggleowana klawiszem L, oraz rozszerzenie istniejącego stacku emissive o czwartą warstwę `hint` (D-Phase5-03) projektowaną przez HighlightManager. Razem domykają warstwę edukacyjno-wizualną Phase 5 (poza tooltipem — Plan 05-04).

Purpose: Spełnia FEEDBACK-06 (etykiety 3D L-toggleable z `userData.labelPL`) i D-Phase5-03 (hint następnego kroku w Nauka). Wszystkie zmiany w istniejących klasach Phase 4 są STRICT extensions API — backward compat zachowany.
Output: 1 nowa klasa (LabelOverlay) + 2 brownfield edits (EmissiveController, HighlightManager) + 3 test files (1 new, 2 updates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@src/highlight/EmissiveController.js
@src/highlight/HighlightManager.js
@src/highlight/EdgeOutlineController.js
@src/state/trainingStore.js

<interfaces>
EmissiveController obecne (Plan 04-02 + 04-06):
- Slot: `{hover: null, state: null}` per mesh
- Priority: state > hover > baseline (linia 92-125)
- setLayer(layerName: 'hover'|'state', mesh, params)
- clearLayer(layerName, mesh)
- `_applyTopLayer` graceful skip dla materiałów bez `emissive` field (MeshBasicMaterial — tabliczka)

Po Plan 05-05:
- Slot: `{hover: null, hint: null, state: null}` per mesh
- Priority: state > hint > hover > baseline (D-Phase5-03)
- setLayer('hover'|'hint'|'state', ...) — hint nie wspiera pulse/flash (statyczny per D-Phase5-03; setLayer ignore pulse/flash dla hint, lub po prostu nie używamy)
- _applyTopLayer branch dodatkowy dla hint: `mesh.material.emissive.setHex(slot.hint.color); mesh.material.emissiveIntensity = slot.hint.intensity ?? 0.3;`

HighlightManager obecne (Plan 04-03):
- _wireSubscribers: subscribe na state.steps → _projectStepsToMeshes + state.events → _flashNewViolations
- _projectStepsToMeshes: error → setLayer('state', ERROR_HEX, pulse); done → setLayer('state', SUCCESS_HEX, flash); inne → clearLayer('state')

Po Plan 05-05:
- Dorzucony subscriber na [currentStepId, difficulty, freeRoam] → _projectHint()
- _projectHint(): clear wszystkich 'hint'; jeśli difficulty==='nauka' AND !freeRoam AND currentStepId AND activeScenario → znajdź step.targetMeshId; setLayer('hint', mesh, {color: 0xF0E442, intensity: 0.3}).

LabelOverlay (NEW) — interface:
- constructor({scene, camera, renderer, interactables, store})
- Tworzy CSS2DRenderer, mountuje domElement do #label-overlay-container (Plan 05-01)
- Buduje 15 CSS2DObject — po jednym per interactable, child mesha
- update() — wpinane do Application.tickables (Plan 05-07)
- dispose() — usuwa CSS2DObject z meshy, remove domElement

three/addons (RESEARCH §700-724):
- import { CSS2DRenderer, CSS2DObject } from 'three/addons/renderers/CSS2DRenderer.js';
- CSS2DRenderer.setSize(w, h)
- CSS2DRenderer.render(scene, camera)
- CSS2DObject — Object3D subclass, .element = div, .visible = boolean
- BRAK metody dispose() w three 0.184 — cleanup manualny

Store flags Plan 05-01: labelsVisible, difficulty, freeRoam, currentStepId.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: EmissiveController stack 3-warstwowy + test update</name>
  <read_first>
    - src/highlight/EmissiveController.js (cały — verify _layers init linia 41, _applyTopLayer linie 80-126)
    - tests/EmissiveController.test.js (cały — 13 testów Plan 04-02; verify stack priority asercje)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "Brownfield: src/highlight/EmissiveController.js" (linie 448-471)
    - .planning/phases/05-educational-layer/05-RESEARCH.md "Pattern 3: EmissiveController stack extension" (linie 405-435) + "Pitfall 5" (linie 652-654)
  </read_first>
  <behavior>
    Update tests/EmissiveController.test.js — dorzucić describe `Phase 5 — warstwa hint (D-Phase5-03)`:
    - Test A (hint init slot): _layers po ctor zawiera `{hover: null, hint: null, state: null}` dla każdego mesha — verify przez `controller._layers.get(mesh).hasOwnProperty('hint')` (lub w hasOwn check).
    - Test B (hint solid): setLayer('hint', mesh, {color: 0xF0E442, intensity: 0.3}) — gdy hover=null, state=null: mesh.material.emissive.getHex() === 0xF0E442, emissiveIntensity === 0.3. Bez GSAP timeline (this._timelines.get(mesh) === undefined — statyczny).
    - Test C (state > hint priority): setLayer('hint', mesh, {color:0xF0E442, intensity:0.3}); setLayer('state', mesh, {color: 0xD55E00, pulse: true}) → emissive na czerwony, pulse timeline aktywny. ClearLayer('state', mesh) → wraca do hint (żółty 0xF0E442, intensity 0.3, no timeline).
    - Test D (hint > hover priority): setLayer('hover', mesh, {color: 0x222222}); setLayer('hint', mesh, {color:0xF0E442}) → emissive żółty (hint wygrywa). ClearLayer('hint') → wraca do hover (szary 0x222222, intensity HOVER_INTENSITY=1).
    - Test E (clearLayer hint): setLayer('hint'); clearLayer('hint', mesh) → baseline 0x000000 (gdy brak innych warstw).
    - Test F (graceful skip dla MeshBasicMaterial): mesh z material bez `emissive` field — setLayer('hint', mesh, ...) NIE rzuca (analog Plan 04-06 Rule 1 fix).
    - Test G (existing tests intact): wszystkie 13 testów Plan 04-02 PASS — backward compat (stack rozszerzony, nie zamieniony).

    Update src/highlight/EmissiveController.js:
    - Linia 14-26 const stack: dorzuć `const HINT_INTENSITY_DEFAULT = 0.3;` (D-Phase5-03).
    - Linia 41: zmień `this._layers.set(mesh, { hover: null, state: null });` na `this._layers.set(mesh, { hover: null, hint: null, state: null });`.
    - JSDoc setLayer (linia 47-50): zmień `@param {'hover'|'state'} layerName` na `@param {'hover'|'hint'|'state'} layerName`. Analogicznie clearLayer.
    - `_applyTopLayer` (linia 80): branch hint MIĘDZY state a hover:
      ```
      } else if (slot.hint) {
        // D-Phase5-03: statyczny żółty hint (Wong #F0E442), bez pulse/flash.
        mesh.material.emissive.setHex(slot.hint.color);
        mesh.material.emissiveIntensity = slot.hint.intensity ?? HINT_INTENSITY_DEFAULT;
      } else if (slot.hover) {
      ```
      (Pomiędzy `slot.state` branch a `slot.hover` branch.)
    - Komentarz w nagłówku klasy: zaktualizuj priority order do "state > hint > hover > baseline".

    NIE łamać backward compat z Plan 04-02..06 — istniejące testy MUSZĄ przejść.
  </behavior>
  <action>
    1. Dorzucić 6 nowych asercji (A-F) do tests/EmissiveController.test.js w nowym describe.
    2. RED: `npm test -- --run tests/EmissiveController.test.js` → FAIL na nowych testach (hint slot/branch nie ma).
    3. GREEN: edycje w src/highlight/EmissiveController.js zgodnie z `<behavior>`. Komentarze JSDoc po polsku.
    4. `npm test -- --run tests/EmissiveController.test.js` PASS (13 baseline + 6 nowych = 19).

    Commit `feat(05-05): EmissiveController stack 3-warstwowy z hint (D-Phase5-03)`.
  </action>
  <verify>
    <automated>npm test -- --run tests/EmissiveController.test.js</automated>
  </verify>
  <done>
    - 6 nowych asercji A-F PASS.
    - 13 baseline asercji PASS (backward compat).
    - Priority order state > hint > hover > baseline weryfikowana.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: HighlightManager hint subscriber + test update</name>
  <read_first>
    - src/highlight/HighlightManager.js (cały — _wireSubscribers linie 48-63, _projectStepsToMeshes linie 106-122)
    - tests/HighlightManager.test.js (cały — 15 testów Plan 04-03)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "Brownfield: src/highlight/HighlightManager.js" (linie 475-505)
    - src/training/scenarios/uruchomienie.js (verify step[*].targetMeshId obecność dla manipulation/visual-target)
  </read_first>
  <behavior>
    Update tests/HighlightManager.test.js — describe `Phase 5 — warstwa hint (D-Phase5-03)`:
    - Test H1 (initial render Nauka): store.startScenario(uruchomienie); difficulty='nauka' (default Plan 05-01); freeRoam=false; currentStepId === 'sprawdz-tabliczke' (pierwszy krok, targetMeshId='tabliczka-znamionowa') → po `new HighlightManager(...)` emissive.setLayer wywołane z ('hint', meshFor('tabliczka-znamionowa'), {color: 0xF0E442, intensity: 0.3}) — spy na emissive.setLayer.
    - Test H2 (Egzamin OFF): store.setState({difficulty:'egzamin'}); _projectHint wywołane → emissive.clearLayer('hint', any mesh) wywołane dla wszystkich interactables. NIE wywołuje setLayer('hint',...).
    - Test H3 (freeRoam OFF): difficulty='nauka', freeRoam=true → clear hint dla wszystkich.
    - Test H4 (currentStepId null OFF): currentStepId=null → clear hint.
    - Test H5 (step bez targetMeshId graceful): visual-attest krok bez targetMeshId — clear hint (no-op nie rzuca).
    - Test H6 (advance step → przesunięcie hint): trigger advance (po sukcesie step) → currentStepId zmienia się na 'kontrola-narzedzia'; _projectHint wywołane: clearLayer('hint', tabliczka), żaden setLayer('hint') (bo kontrola-narzedzia to visual-attest bez targetMeshId).
    - Test H7 (advance do step z targetMeshId): currentStepId='sprawdz-olej' (visual-target, targetMeshId='wziernik-smarowania') → setLayer('hint', wziernik, {color:0xF0E442, intensity:0.3}); poprzedni hint na tabliczce wyczyszczony.
    - Test H8 (constructor initial baseline): nowy HighlightManager skonstruowany PO startScenario z difficulty='nauka' wywołuje _projectHint w ctor — pierwszy step hint emitowany (analog Plan 04-03 initial render).
    - Test H9 (boundary unchanged): plik nadal nie ma runtime importów (poza tym co już ma).

    Update src/highlight/HighlightManager.js:
    1. Linia 19-20 const stack: dorzuć `const HINT_HEX = 0xF0E442;` (Wong yellow, D-Phase5-03) i `const HINT_INTENSITY = 0.3;`.
    2. `_wireSubscribers` (linia 48): dorzuć trzecią subskrypcję (po istniejących steps + events):
       ```
       const unsubHint = this._store.subscribe(
         (s) => [s.currentStepId, s.difficulty, s.freeRoam],
         () => this._projectHint(),
         { equalityFn: (a, b) => a[0]===b[0] && a[1]===b[1] && a[2]===b[2] },
       );
       this._unsubscribers.push(unsubHint);
       this._projectHint();  // initial render — analog _projectStepsToMeshes linia 55
       ```
    3. Nowa metoda `_projectHint()`:
       ```
       _projectHint() {
         // Clear na wszystkich (idempotent)
         for (const mesh of this._interactables.values()) this._emissive.clearLayer('hint', mesh);
         const {currentStepId, difficulty, freeRoam, activeScenario} = this._store.getState();
         if (difficulty !== 'nauka' || freeRoam || !currentStepId || !activeScenario) return;
         const step = activeScenario.steps.find((s) => s.id === currentStepId);
         if (!step?.targetMeshId) return;
         const mesh = this._interactables.get(step.targetMeshId);
         if (mesh) this._emissive.setLayer('hint', mesh, { color: HINT_HEX, intensity: HINT_INTENSITY });
       }
       ```
    4. Komentarz JSDoc po polsku: cel hint warstwy, gating (Nauka+!freeRoam+currentStep+targetMeshId).
  </behavior>
  <action>
    1. RED: dorzucić 9 testów do tests/HighlightManager.test.js w nowym describe.
    2. GREEN: edycje w src/highlight/HighlightManager.js zgodnie z `<behavior>`.
    3. `npm test -- --run tests/HighlightManager.test.js tests/EmissiveController.test.js` PASS.

    Commit `feat(05-05): HighlightManager warstwa hint (D-Phase5-03)`.
  </action>
  <verify>
    <automated>npm test -- --run tests/HighlightManager.test.js</automated>
  </verify>
  <done>
    - 9 nowych asercji H1-H9 PASS.
    - 15 baseline asercji Plan 04-03 PASS.
    - hint warstwa aktywna tylko w difficulty='nauka' + !freeRoam + currentStepId + targetMeshId.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: LabelOverlay (CSS2DRenderer + 15 etykiet) + nowy test file</name>
  <read_first>
    - .planning/phases/05-educational-layer/05-RESEARCH.md "Pattern 5: CSS2DRenderer + LabelOverlay" (linie 463-573) + "Pitfall 3" (linie 635-643) + "Pitfall 4" (linie 645-650)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "src/education/LabelOverlay.js" (linie 251-321)
    - src/highlight/EdgeOutlineController.js (analog: per-mesh prebuild + store subscriber + child-of-mesh + dispose pattern)
    - tests/EdgeOutlineController.test.js (wzór tests z 17 asercji)
    - src/i18n/pl.js (verify pl.parts[id].label dla 15 interactables)
  </read_first>
  <behavior>
    Stworzyć tests/LabelOverlay.test.js z 14 asercjami w 5 describe:

    Describe 1 — DOM mount + prebuild:
    - Test L1: konstruktor wymaga #label-overlay-container w DOM — throw `LabelOverlay: brak #label-overlay-container` gdy nie istnieje.
    - Test L2: konstruktor mountuje CSS2DRenderer.domElement jako child #label-overlay-container.
    - Test L3: prebuild — dla każdego z 15 interactables tworzy CSS2DObject (planner: w jsdom CSS2DObject może być mockowany lub real — three/addons działa bez WebGL dla CSS2D). Verify: this._labels.size === 15 (Map size).
    - Test L4: textContent każdego label.element === mesh.userData.labelPL (czytane z Phase 2 invariant D-Phase2-04; testy używają fixture interactables Map z 15 wpisami).

    Describe 2 — Visibility toggle:
    - Test L5: initial state labelsVisible=false → wszystkie label.visible === false po ctor (lub po pierwszej `update()`).
    - Test L6: store.setState({labelsVisible:true}); update() → label.visible === true dla każdego z 15 (uproszczone: bez camera-facing test w jsdom, planner stub-uje camera-facing przez fake camera direction OR test sprawdza setVisible).
    - Test L7: difficulty='egzamin'; labelsVisible=true; update() → label.visible === false (D-Phase5-22 force-hide).

    Describe 3 — Camera-facing filter:
    - Test L8: mock camera + mesh z konkretną pozycją + quaternion; update() → planner weryfikuje, że `_applyCameraFacing` ustawia visible na podstawie dot product. Z stub camera.getWorldDirection ustawiona, weryfikuje label.visible dla 2 meshy: jeden "z przodu", jeden "z tyłu". UWAGA: konwencja dot < 0 może być orient-dependent (RESEARCH Assumption A2); test asercjonuje że metoda jest WYWOŁANA, nie konkretną wartością visible per mesh (manual QA Plan 05-07 weryfikuje correctness w 3D).

    Describe 4 — Declutter:
    - Test L9: jsdom getBoundingClientRect zwraca 0×0 (Pitfall 3) — stub getBoundingClientRect na każdym label.element z konkretnymi pozycjami: 2 labels w odległości < 40px → drugi (dalszy) ma style.transform === 'translateY(-20px)'; 2 labels > 40px → ŻADEN nie ma offsetu.

    Describe 5 — Dispose:
    - Test L10: dispose() — wszystkie 15 label.element usunięte z DOM (każde `label.element.parentNode === null`).
    - Test L11: dispose() — every mesh.children NIE zawiera już label (`mesh.remove(label)` wywołane).
    - Test L12: dispose() — `_css2dRenderer.domElement` usunięty z DOM.
    - Test L13: dispose() idempotent — drugie wywołanie nie rzuca.
    - Test L14 (boundary smoke): readFileSync 'src/education/LabelOverlay.js' — regex `/import .* from ['"](\.\.\/training\/|gsap|@floating-ui)/` MUST NOT match. Tylko 'three' + 'three/addons/...' dozwolone (boundary D-Phase5-26: THREE + store + DOM mount).

    Stworzyć src/education/LabelOverlay.js zgodnie z 05-PATTERNS.md §251-321 + 05-RESEARCH.md §463-573:

    1. Header: Phase 5 FEEDBACK-06; D-Phase5-10/22; boundary D-Phase5-26 (THREE + store przez DI; ograniczony DOM dla mount + label.element creation; NIE @floating-ui/dom, NIE training/).

    2. Imports: `import * as THREE from 'three';` + `import { CSS2DRenderer, CSS2DObject } from 'three/addons/renderers/CSS2DRenderer.js';`.

    3. `class LabelOverlay`:
       - `constructor({scene, camera, renderer, interactables, store})`: pola j.w. (zachować referencje). `this._css2dRenderer = new CSS2DRenderer();`. mount = `document.getElementById('label-overlay-container')`, throw gdy brak (UI-SPEC §Pitfall 4). `mount.appendChild(this._css2dRenderer.domElement);`. `this._css2dRenderer.setSize(renderer.domElement.clientWidth, renderer.domElement.clientHeight);`. Prebuild: `this._labels = new Map();` dla każdego (id, mesh) z interactables: createElement('div'), `className='label-3d'`, `textContent=mesh.userData.labelPL`; new CSS2DObject(div); `label.visible = false`; `mesh.add(label)`; `this._labels.set(mesh, label)`. Subskrypcja: `this._unsub = this._store.subscribe(s => [s.labelsVisible, s.difficulty], () => {/* re-evaluation per tick; subscriber tylko po to by triggerować render gdy flag flipuje */}, {equalityFn: (a,b)=>a[0]===b[0]&&a[1]===b[1]});`. (Planner: subscriber może być no-op bo `update()` per-tick i tak czyta state; alternatywnie subscriber kasuje `label.visible=false` natychmiast gdy flag false by uniknąć latencji. Wybrać natychmiastową reakcję — set all visible=false w callback gdy `!labelsVisible || difficulty==='egzamin'`.)
       - `update()` (called per-frame z Application.tickables):
         - `const {labelsVisible, difficulty} = this._store.getState();`
         - `const visible = labelsVisible && difficulty !== 'egzamin';`
         - Jeśli `!visible`: for each label set visible=false. render. return.
         - Else: `_applyCameraFacing()` per mesh; render; `_declutter()`.
       - `_applyCameraFacing()`: `const cameraDir = new THREE.Vector3(); this._camera.getWorldDirection(cameraDir);`. For each [mesh, label]: `const worldNormal = new THREE.Vector3(0, 0, 1).applyQuaternion(mesh.quaternion);` (uproszczona normalna — boxGeometry forward; RESEARCH Assumption A2). `const dot = worldNormal.dot(cameraDir);`. `label.visible = dot < 0;` (camera patrzy "w stronę" mesha gdy dot < 0).
       - `_declutter()`: Pattern z 05-RESEARCH.md §535-562: zbierz rects (getBoundingClientRect tylko dla label.visible), sortuj po zIndex desc, podwójna pętla i<j: jeśli dist < 40px → `rects[j].label.element.style.transform = 'translateY(-20px)'`. PRZED dwukrotnym wywołaniem update: clear transform na wszystkich `label.element.style.transform = ''`.
       - `dispose()`: `if (this._unsub) { this._unsub(); this._unsub = null; }`. For each [mesh, label]: `mesh.remove(label); label.element.remove();`. `this._labels.clear();`. `this._css2dRenderer.domElement.remove();` (CSS2DRenderer NIE ma dispose() w three 0.184 — RESEARCH §724). Idempotent: drugie wywołanie — guard `if (this._labels.size === 0) return;` na początku.

    4. Per-frame update wpinany przez Plan 05-07 do `Application.tickables.push((dt) => this.labelOverlay.update())`.
  </behavior>
  <action>
    1. RED: stworzyć tests/LabelOverlay.test.js z 14 asercjami. Setup beforeEach: `document.body.innerHTML = '<div id="label-overlay-container"></div>'`. Fixture interactables: Map z 15 fake meshami (`{userData: {id, labelPL}, quaternion: new THREE.Quaternion(), add: vi.fn(), remove: vi.fn()}`) — lub real THREE.Mesh stubs.
    2. GREEN: implementacja src/education/LabelOverlay.js.
    3. `npm test -- --run tests/LabelOverlay.test.js` PASS.

    Commit `feat(05-05): LabelOverlay — FEEDBACK-06 CSS2DRenderer + 15 labelek + declutter`.
  </action>
  <verify>
    <automated>npm test -- --run tests/LabelOverlay.test.js tests/EmissiveController.test.js tests/HighlightManager.test.js</automated>
  </verify>
  <done>
    - 14 asercji LabelOverlay PASS.
    - Pełny suite zielony.
    - LabelOverlay tworzy 15 CSS2DObject (jeden per interactable z labelPL).
    - L toggle + Egzamin force-hide weryfikowane.
    - Declutter offset +20px Y weryfikowany z stub getBoundingClientRect.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mesh.userData.labelPL → DOM textContent | identity field (Phase 2 D-Phase2-04), XSS-safe |
| EmissiveController.setLayer('hint',...) → mesh.material.emissive | stack invariant; nie omija EmissiveController |
| CSS2DRenderer → DOM | three addons; bezpieczny |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-LEAK | Denial of Service | CSS2DObject dispose path (brak dispose() w three 0.184) | mitigate | RESEARCH §724: cleanup manualny — Test L10-L13 weryfikuje 3-stopniowy cleanup (mesh.remove + element.remove + domElement.remove). `_labels.clear()` zwalnia mapę. |
| T-05-05-PERF | Denial of Service | O(n²) declutter at n=15 = 105 par/tick | accept | RESEARCH §134-135: O(n²) acceptable dla n=15 (≈100 operacji/tick na 60 FPS = 6000/s, trywialne). Manual QA Plan 05-07 weryfikuje 60 FPS. |
| T-05-05-STACK | Tampering | EmissiveController stack breakage przy rozszerzeniu | mitigate | Test A (init slot includes hint) + Test G (existing tests intact) — backward compat egzekwowany. Stack jest extension, nie replacement. |
| T-05-05-NORMAL | Information Disclosure | Camera-facing dot product konwencja zależna od geometrii (RESEARCH Assumption A2) | accept | Test L8 testuje WYWOŁANIE metody, nie konkretną wartość per mesh. Manual QA Plan 05-07 (SC1: orbituj kamerą, ETIKIETY zasłoniętych elementów znikają) weryfikuje correctness. Jeśli QA pokaże regresję, fix w follow-up (Phase 5 może mieć drobny patch — alternatywnie quaternion(0,1,0) zamiast (0,0,1) — planner ocenia per orientation prasy). |
</threat_model>

<verification>
- `npm test -- --run tests/EmissiveController.test.js tests/HighlightManager.test.js tests/LabelOverlay.test.js` — wszystkie zielone.
- `grep -c "hint" src/highlight/EmissiveController.js` ≥ 4 (slot init + applyTopLayer branch + JSDoc + komentarz).
- `grep -c "HINT_HEX.*F0E442" src/highlight/HighlightManager.js` === 1 (Wong yellow const).
- `grep -c "CSS2DRenderer\|CSS2DObject" src/education/LabelOverlay.js` ≥ 2.
- `grep -E "^import" src/education/LabelOverlay.js` — TYLKO `import * as THREE from 'three'` + `import { CSS2DRenderer, CSS2DObject } from 'three/addons/...'` (zero training/gsap/floating-ui).
- Pełny suite zielony.
</verification>

<success_criteria>
- EmissiveController stack 3-warstwowy (D-Phase5-03); priority state > hint > hover > baseline.
- HighlightManager projektuje hint w Nauka + !freeRoam; clear w Egzamin/freeRoam.
- LabelOverlay tworzy 15 CSS2DObject z labelPL z userData; L toggle + Egzamin force-hide.
- Camera-facing + declutter implementowane (testable in jsdom + manual QA gate w Plan 05-07).
- Pełny pre-existing suite green (backward compat Plan 04 weryfikowany).
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-05-SUMMARY.md` — opisz: stack extension (slot init + applyTopLayer branch + priority), hint projector gating matrix (Nauka+!freeRoam+currentStep+targetMesh), LabelOverlay 15 prebuild + camera-facing dot + declutter algorithm + dispose 3-step (mesh.remove → element.remove → domElement.remove). Mapuj na D-Phase5-03/10/12/22.
</output>
