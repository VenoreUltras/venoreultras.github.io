---
phase: 05-educational-layer
plan: 06
type: tdd
wave: 3
depends_on: ["05-01"]
files_modified:
  - src/ui/StepPanel.js
  - src/ui/StatusPanel.js
  - src/RaycastController.js
  - tests/StepPanel.test.js
  - tests/StatusPanel.test.js
  - tests/RaycastController.test.js
autonomous: true
requirements: [UI-04, EDU-01, EDU-02]
tags: [phase-5, ui-brownfield, panels, free-roam]

must_haves:
  truths:
    - "StepPanel renderuje rationalePL pod aktywnym krokiem TYLKO gdy difficulty='nauka' AND status!=='done' AND step.rationalePL istnieje (D-Phase5-11)"
    - "StatusPanel renderuje difficulty badge (📚 Nauka / 📝 Egzamin) z 4-stanową stylizacją + toggle button przełączający tryb (D-Phase5-01)"
    - "StatusPanel renderuje .free-roam-indicator (🆓 Tryb wolny) gdy state.freeRoam=true (visibility:hidden gdy false — bez reflowu)"
    - "RaycastController _handlePointerUp guard: jeśli state.freeRoam=true → no-op (NIE wywołuje attemptStep) — D-Phase5-05"
    - "Wszystkie polskie stringi z pl.ui (difficultyNauka/difficultyEgzamin/freeRoamActive/setDifficulty*); pl.disclaimer.full reusowane"
  artifacts:
    - path: "src/ui/StepPanel.js"
      provides: "rationale inline render w Nauka"
      contains: "step-item__rationale"
    - path: "src/ui/StatusPanel.js"
      provides: "difficulty badge + free-roam indicator + difficulty toggle"
      contains: "difficulty-badge"
    - path: "src/RaycastController.js"
      provides: "freeRoam branch przed attemptStep"
      contains: "freeRoam"
  key_links:
    - from: "src/ui/StepPanel.js"
      to: "scenario.steps[].rationalePL"
      via: "state.activeScenario lookup"
      pattern: "rationalePL"
    - from: "src/RaycastController.js"
      to: "state.freeRoam"
      via: "store.getState()"
      pattern: "freeRoam"
---

<objective>
Brownfield-edit 3 plików Phase 1-4 dla zachowania semantyki Phase 5: StepPanel branchuje na difficulty (rationale w Nauka, ukryte w Egzamin); StatusPanel dorzuca difficulty badge + free-roam indicator + toggle button difficulty; RaycastController bypasuje attemptStep gdy freeRoam=true. Razem spełniają UI-04 (rationale display) + EDU-01 (free-roam interaction model) + EDU-02 (difficulty UI projection).

Purpose: Te 3 edycje są punktem styku Phase 5 z istniejącym pipeline'em Phase 3+4. Bez nich store-side flagi nie projektują się na UI ani interakcję.
Output: 3 brownfield edits + 3 test updates. Plan 05-07 wpina dispose/wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@src/ui/StepPanel.js
@src/ui/StatusPanel.js
@src/RaycastController.js
@src/training/scenarios/uruchomienie.js
@src/i18n/pl.js

<interfaces>
StepPanel obecny (Plan 04-04+05):
- _render() linie 57-116 — per-step <li.step-item.step-item--{stateKey}>
- Active step (currentStepId) ma `<li>` z textContent `${icon} ${idx+1}. ${step.labelPL}`
- Visual-attest button conditionally appended pod aktywnym non-done krokiem

Po Plan 05-06: po linii `li.textContent = ...` (linia 83) i PRZED visual-attest if-block:
```
if (state.difficulty === 'nauka' && step.id === state.currentStepId && status !== 'done' && step.rationalePL) {
  const rationale = document.createElement('p');
  rationale.className = 'step-item__rationale';
  rationale.textContent = step.rationalePL;
  li.appendChild(rationale);
}
```
Subskrypcja: dorzucić sub na `s => s.difficulty` żeby zmiana difficulty triggerowała re-render (analog 3 subscriberów linie 50-53).

StatusPanel obecny (Plan 04-04):
- _build() linie 59-67 — innerHTML szkielet z 4 elementami (icon/state/score/HC button)
- _render() linie 89-98 — textContent dla każdego

Po Plan 05-06: rozszerz szkielet `_build()` o 3 nowe elementy w `.status-panel__bar`:
- `<span class="difficulty-badge">` (variant klasa --nauka/--egzamin w _render)
- `<button class="status-panel__difficulty-toggle" type="button" aria-label="">` (cyklotwarczo nauka↔egzamin)
- `<span class="free-roam-indicator">` (visibility:hidden gdy freeRoam=false)

Subscribery: dorzucić sub na `s => s.difficulty` i `s => s.freeRoam`.

RaycastController obecny (Plan 04-05+ 05-04):
- _handlePointerUp linia 146-163 — wywołuje store.attemptStep(intent) gdy click+hit
- Po Plan 05-04: konstruktor ma optional `onHoverChange` (DI dla tooltipa); _commitHover/_commitLeave wołają callback

Po Plan 05-06: dorzucić guard na początku `_handlePointerUp` PRZED raycast (lub po raycast, przed attemptStep — planner wybiera; PREFEROWANE: PRZED raycast, by oszczędzić CPU):
```
const { freeRoam } = this._store.getState();
if (freeRoam) return;  // D-Phase5-05: free-roam pauzuje SOP; klik = no-op
```

scenario.steps[].rationalePL — verified obecne dla wszystkich 8 kroków uruchomienie.js (verified w RESEARCH §886).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: StepPanel rationale inline (UI-04)</name>
  <read_first>
    - src/ui/StepPanel.js (cały — verify _render linie 57-116, _wireSubscribers linie 48-55)
    - tests/StepPanel.test.js (cały — pattern asercji)
    - .planning/phases/05-educational-layer/05-UI-SPEC.md sekcja "6. Rationale inline" (linie 346-359)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "Brownfield: src/ui/StepPanel.js" (linie 509-527)
    - src/training/scenarios/uruchomienie.js (verify rationalePL we wszystkich 8 krokach)
  </read_first>
  <behavior>
    Dorzucone testy do tests/StepPanel.test.js w nowym describe `Phase 5 — rationale inline (UI-04, D-Phase5-11)`:
    - Test R1 (Nauka render): store.startScenario(uruchomienie); difficulty='nauka' (default); currentStepId='sprawdz-tabliczke' → po _render() lista zawiera `<p class="step-item__rationale">` z textContent === uruchomienie.steps[0].rationalePL ('Każda maszyna ma osobną instrukcję obsługi i zakres uprawnień operatora.').
    - Test R2 (Egzamin ukrywa): store.setState({difficulty:'egzamin'}) → re-render — DOM NIE zawiera żadnego `.step-item__rationale` (querySelectorAll length === 0).
    - Test R3 (tylko aktywny krok): difficulty='nauka'; kroki pending → tylko jeden `.step-item__rationale` (dla aktywnego currentStepId).
    - Test R4 (status='done' ukrywa): trigger advance — krok #1 status='done', currentStepId nadal na #1? NIE — advance ustawia status:done I currentStepId:#2. Test: po advance, krok #1 nie ma rationale (status='done'), krok #2 ma rationale (active, pending status).
    - Test R5 (brak rationalePL graceful): planner: utwórz scenariusz fixture z krokiem bez `rationalePL` (lub usuń pole) — render NIE rzuca, brak `.step-item__rationale` dla tego kroku.
    - Test R6 (subscriber difficulty re-render): zmiana state.difficulty 'nauka'→'egzamin' triggeruje _render → rationale znika. To wymaga że StepPanel ma subscriber na `s => s.difficulty` — verify przez spy na _render LUB sprawdzenie DOM po setState.
    - Test R7 (visual-attest button bez kolizji): aktywny krok kind='visual-attest' (kontrola-narzedzia) w Nauka → DOM zawiera I `.step-item__rationale` I `.phase4-attest-check` button.
    - Test R8 (boundary unchanged): plik nie zyskał nowych importów (importuje tylko `../i18n/pl.js`).

    Update src/ui/StepPanel.js:
    1. W `_wireSubscribers()` (linia 48): dorzuć 4. subskrypcję `this._store.subscribe(s => s.difficulty, () => this._render())`. (3 obecne: currentStepId, steps, isAnimating + nowy difficulty = 4.)
    2. W `_render()` linia ~83 (po `li.textContent = ...`, PRZED `if (step.kind === 'visual-attest' && ...)` block ~88-102):
       ```
       // D-Phase5-11: rationale inline TYLKO w Nauka pod aktywnym kroku non-done.
       // Egzamin: nie renderujemy w ogóle (twardy tryb wiedzy OFF).
       if (
         state.difficulty === 'nauka' &&
         step.id === state.currentStepId &&
         status !== 'done' &&
         step.rationalePL
       ) {
         const rationale = document.createElement('p');
         rationale.className = 'step-item__rationale';
         rationale.textContent = step.rationalePL;
         li.appendChild(rationale);
       }
       ```
    3. Komentarz JSDoc po polsku, identyfikatory ENG. Pole `rationalePL` w scenario — zerowa zmiana danych (verified obecne).
  </behavior>
  <action>
    RED → GREEN:
    1. Dorzucić 8 testów R1-R8 do tests/StepPanel.test.js (nowy describe).
    2. RED: `npm test -- --run tests/StepPanel.test.js` → FAIL (rationale brak).
    3. GREEN: edycja src/ui/StepPanel.js.
    4. `npm test -- --run tests/StepPanel.test.js` PASS.

    Commit `feat(05-06): StepPanel rationale inline Nauka (D-Phase5-11, UI-04)`.
  </action>
  <verify>
    <automated>npm test -- --run tests/StepPanel.test.js</automated>
  </verify>
  <done>
    - 8 nowych asercji R1-R8 PASS.
    - 13 baseline asercji Plan 04-04 PASS (backward compat).
    - rationale renderuje TYLKO w Nauka pod aktywnym non-done krokiem; ukryte w Egzamin.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: StatusPanel difficulty badge + free-roam indicator + toggle (EDU-02)</name>
  <read_first>
    - src/ui/StatusPanel.js (cały — verify _build linie 59-79, _render linie 89-98, _wireSubscribers linie 81-87)
    - tests/StatusPanel.test.js (wzór 8 testów Plan 04-04)
    - .planning/phases/05-educational-layer/05-UI-SPEC.md sekcja "5. Difficulty badge + Free-roam indicator" (linie 321-342) + "Copywriting Contract" (linie 378-382)
    - src/i18n/pl.js (verify Plan 05-01 dorzuca pl.ui.difficultyNauka/Egzamin/freeRoamActive/setDifficultyNauka/Egzamin)
  </read_first>
  <behavior>
    Dorzucone testy do tests/StatusPanel.test.js w nowym describe `Phase 5 — difficulty + free-roam (EDU-02, D-Phase5-01)`:
    - Test S1 (badge initial Nauka): default state.difficulty='nauka' → DOM zawiera `.difficulty-badge.difficulty-badge--nauka` z textContent === pl.ui.difficultyNauka ('📚 Nauka').
    - Test S2 (badge Egzamin variant): store.setState({difficulty:'egzamin'}) → klasa --nauka usunięta, --egzamin dodana; textContent === pl.ui.difficultyEgzamin ('📝 Egzamin').
    - Test S3 (toggle button initial): button `.status-panel__difficulty-toggle` istnieje, textContent === pl.ui.setDifficultyEgzamin ('Przełącz na Egzamin') gdy difficulty='nauka' (label = co STANIE SIĘ po kliku). aria-label === pl.ui.setDifficultyEgzamin.
    - Test S4 (toggle click action): klik na difficulty-toggle gdy difficulty='nauka' → store.difficulty === 'egzamin'. Drugi klik → 'nauka'. (Implementacja: button.onclick → `store.setState({difficulty: state.difficulty==='nauka'?'egzamin':'nauka'})` lub `setDifficulty` akcja z Plan 05-01.)
    - Test S5 (toggle label flip): po setDifficulty('egzamin') → button textContent === pl.ui.setDifficultyNauka ('Przełącz na Naukę').
    - Test S6 (free-roam indicator hidden default): default state.freeRoam=false → `.free-roam-indicator` istnieje w DOM (NIE display:none — UI-SPEC §340-341 visibility:hidden) ale ma `style.visibility === 'hidden'` (lub klasa CSS taka).
    - Test S7 (free-roam indicator visible): store.setState({freeRoam:true}) → `.free-roam-indicator` ma `style.visibility === 'visible'` (lub usunięta hidden klasa); textContent === pl.ui.freeRoamActive ('🆓 Tryb wolny').
    - Test S8 (existing 8 testów intact): pełny `tests/StatusPanel.test.js` (z Plan 04-04) PASS.
    - Test S9 (dispose listener cleanup): dispose() usuwa click listener z difficulty-toggle (analog HC toggle Plan 04-04 pattern).

    Update src/ui/StatusPanel.js:
    1. Rozszerz `_build()` szkielet innerHTML linia 60-67 (XSS-safe — statyczne literały):
       ```
       this._root.innerHTML = `
         <div class="status-panel__bar">
           <span class="status-panel__icon" aria-hidden="true"></span>
           <span class="status-panel__state"></span>
           <span class="status-panel__score"></span>
           <span class="difficulty-badge"></span>
           <button class="status-panel__difficulty-toggle" type="button" aria-pressed="false"></button>
           <span class="free-roam-indicator"></span>
           <button class="status-panel__hc-toggle" type="button" aria-pressed="false"></button>
         </div>
       `;
       ```
       (Kolejność wedle UI-SPEC §323: badge + toggle PRZED HC toggle.)
    2. Dorzuć pola: `this._difficultyBadge`, `this._difficultyToggleBtn`, `this._freeRoamIndicator` — querySelectory analog _iconEl/_stateEl.
    3. Dorzuć bound handler: `this._onDifficultyClick = () => { const cur = this._store.getState().difficulty; this._store.getState().setDifficulty(cur === 'nauka' ? 'egzamin' : 'nauka'); };` + addEventListener na difficulty-toggle.
    4. `_wireSubscribers()` linia 81: dorzuć 2 sub: `s => s.difficulty` i `s => s.freeRoam` (każdy → `_render()`).
    5. `_render()` (linia 89-98): dorzuć update 3 elementów:
       ```
       // D-Phase5-01 difficulty badge: variant klasa + textContent (XSS-safe)
       const isNauka = s.difficulty === 'nauka';
       this._difficultyBadge.className = `difficulty-badge difficulty-badge--${isNauka ? 'nauka' : 'egzamin'}`;
       this._difficultyBadge.textContent = isNauka ? pl.ui.difficultyNauka : pl.ui.difficultyEgzamin;
       // Toggle button — label opisuje "co stanie się po kliknięciu" (przeciwny stan)
       this._difficultyToggleBtn.textContent = isNauka ? pl.ui.setDifficultyEgzamin : pl.ui.setDifficultyNauka;
       this._difficultyToggleBtn.setAttribute('aria-label', this._difficultyToggleBtn.textContent);
       // Free-roam indicator (visibility-toggle by uniknąć reflowu)
       this._freeRoamIndicator.textContent = pl.ui.freeRoamActive;
       this._freeRoamIndicator.style.visibility = s.freeRoam ? 'visible' : 'hidden';
       ```
    6. `dispose()` (linia 101-107): dorzuć `if (this._difficultyToggleBtn && this._onDifficultyClick) this._difficultyToggleBtn.removeEventListener('click', this._onDifficultyClick);`.
    7. Plan 05-01 weryfikacja: pl.ui musi mieć 5 nowych kluczy (difficultyNauka, difficultyEgzamin, freeRoamActive, setDifficultyNauka, setDifficultyEgzamin). Jeśli któryś brak — Plan 05-06 dorzuca jako defensywny patch.
  </behavior>
  <action>
    RED → GREEN:
    1. Dorzucić 9 testów S1-S9 do tests/StatusPanel.test.js.
    2. RED → GREEN edycja src/ui/StatusPanel.js.
    3. `npm test -- --run tests/StatusPanel.test.js` PASS.

    Commit `feat(05-06): StatusPanel difficulty badge + free-roam indicator (EDU-02)`.
  </action>
  <verify>
    <automated>npm test -- --run tests/StatusPanel.test.js</automated>
  </verify>
  <done>
    - 9 nowych asercji S1-S9 PASS.
    - 8 baseline Plan 04-04 PASS.
    - StatusPanel renderuje 7 elementów (icon/state/score/badge/diff-toggle/freeroam/hc-toggle).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: RaycastController free-roam guard (EDU-01)</name>
  <read_first>
    - src/RaycastController.js (cały — _handlePointerUp linie 146-163)
    - tests/RaycastController.test.js (wzór testów click → attemptStep; verify Plan 05-04 dorzuca onHoverChange testy)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "Brownfield: src/RaycastController.js" (linie 531-568)
    - .planning/phases/05-educational-layer/05-CONTEXT.md (D-Phase5-05)
  </read_first>
  <behavior>
    Dorzucone testy do tests/RaycastController.test.js w nowym describe `Phase 5 — free-roam branch (EDU-01, D-Phase5-05)`:
    - Test F1 (default click działa): store.startScenario(uruchomienie); state.freeRoam=false (default); spy na store.attemptStep; symuluj pointerdown + pointerup na meshu (click<5px) → attemptStep wywołane z `{kind:'click', meshId:'...'}`.
    - Test F2 (free-roam blokuje click): store.setState({freeRoam:true}); symuluj ten sam click → attemptStep NIE wywołane (spy 0 calls).
    - Test F3 (hover NIE jest blokowany w free-roam): freeRoam=true; symuluj pointermove → _runHysteresis → _commitHover normalnie wywoływany; tooltip events (onHoverChange) emitowane. (Eksploracja musi pokazywać hover hint + tooltip.)
    - Test F4 (po wyłączeniu free-roam, click znów działa): freeRoam=true → click → 0 wywołań. setState({freeRoam:false}) → ten sam click → attemptStep wywołane.

    Update src/RaycastController.js `_handlePointerUp` (linia 146):
    Na początku metody, PRZED click-vs-drag check (linia 147-150) LUB po (planner wybiera — PRZED jest bardziej efektywne CPU-wise, ale po jest semantyczniej "tylko prawdziwe kliki ignorowane w free-roam"; planner PREFERUJE PO drag check + PRZED raycast):
    ```
    _handlePointerUp(event) {
      const dx = event.clientX - this._downX;
      const dy = event.clientY - this._downY;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist >= CLICK_DRAG_THRESHOLD_PX) return; // drag — nie click

      // D-Phase5-05: free-roam pauzuje SOP, click = no-op (hover wciąż działa
      // — tooltipy i etykiety 3D nadal renderują).
      if (this._store.getState().freeRoam) return;

      const rect = this._renderer.domElement.getBoundingClientRect();
      // ... reszta bez zmian
    }
    ```

    Hover (_runHysteresis, _commitHover, _commitLeave) — ZERO zmian. Free-roam NIE blokuje hover.
  </behavior>
  <action>
    1. Dorzucić 4 testy F1-F4 do tests/RaycastController.test.js (nowy describe).
    2. RED → GREEN: edycja src/RaycastController.js.
    3. `npm test -- --run tests/RaycastController.test.js` PASS (Phase 3 baseline + Plan 05-04 + 4 nowe).

    Commit `feat(05-06): RaycastController free-roam click bypass (EDU-01)`.
  </action>
  <verify>
    <automated>npm test -- --run tests/RaycastController.test.js tests/StepPanel.test.js tests/StatusPanel.test.js</automated>
  </verify>
  <done>
    - 4 nowe asercje F1-F4 PASS.
    - Wszystkie pre-existing testy RaycastController PASS (Phase 3 + Plan 05-04 onHoverChange).
    - Hover w free-roam działa nadal (Test F3); click blokowany (Test F2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.difficulty → StepPanel rationale render | branch warunkowy; rationalePL z scenario JSON (statyczne dane) |
| state.freeRoam → RaycastController bypass | early return przed attemptStep |
| state.difficulty/freeRoam → StatusPanel projection | textContent (XSS-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-XSS | Tampering | rationalePL → DOM | accept | textContent — XSS-safe (Test R1 weryfikuje); rationalePL pochodzi ze scenario JSON (statyczne dane). |
| T-05-06-BYPASS | Tampering | free-roam może być wykorzystane do ominięcia SOP | accept | EDU-01 wymaga free-roam jako celową feature. Pause SOP zachowuje state.currentStepId — wyjście wraca dokładnie do bieżącego kroku (D-Phase5-05). Test F4 weryfikuje że po freeRoam=false klik znów liczy się jako SOP. |
| T-05-06-RACE | Tampering | rapid difficulty toggle przy aktywnym kroku | mitigate | StepPanel subscriber CHANGE-only; re-render idempotent (replaceChildren). EmissiveController stack (Plan 05-05) re-projektuje hint warstwę gdy difficulty zmienia się — Plan 05-05 Test H2-H3 weryfikują. |
| T-05-06-LEAK | Denial of Service | difficulty toggle listener leak | mitigate | Test S9 weryfikuje removeEventListener w dispose (analog HC toggle Plan 04-04 D-Phase4-09 pattern). |
</threat_model>

<verification>
- `npm test -- --run tests/StepPanel.test.js tests/StatusPanel.test.js tests/RaycastController.test.js` — wszystkie PASS.
- `grep -c "rationalePL\|step-item__rationale" src/ui/StepPanel.js` ≥ 2.
- `grep -c "difficulty-badge\|free-roam-indicator\|difficulty-toggle" src/ui/StatusPanel.js` ≥ 6.
- `grep -c "freeRoam" src/RaycastController.js` ≥ 1.
- Pełny suite zielony.
</verification>

<success_criteria>
- StepPanel: rationale inline pod aktywnym non-done krokiem w Nauka; ukryte w Egzamin (UI-04 SC2).
- StatusPanel: badge difficulty + toggle button (cyklotwarczy nauka↔egzamin) + free-roam indicator (visibility-toggle) — EDU-02 SC3 UI side.
- RaycastController: freeRoam=true blokuje click → attemptStep; hover NIE jest blokowany — EDU-01 SC3.
- Backward compat: 21 pre-existing testów (8 StatusPanel + 13 StepPanel + n RaycastController) PASS.
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-06-SUMMARY.md` — opisz: 3 brownfield diff summaries (StepPanel +rationale branch, StatusPanel +3 elementy + toggle handler, RaycastController +1 guard), pl.ui dependencies verify (5 nowych kluczy z Plan 05-01), backward compat status (21 baseline testów intact). Mapuj na D-Phase5-01/05/11.
</output>
