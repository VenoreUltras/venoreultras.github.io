---
phase: 05-educational-layer
plan: 04
type: tdd
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - src/education/TooltipManager.js
  - src/RaycastController.js
  - tests/TooltipManager.test.js
  - tests/RaycastController.test.js
autonomous: true
requirements: [UI-03]
tags: [phase-5, tooltip, floating-ui, hover]

must_haves:
  truths:
    - "TooltipManager używa @floating-ui/dom (computePosition + autoUpdate + flip + shift) z placement:'top'"
    - "600ms hover delay (D-Phase5-08): setTimeout w onHoverEnter; clearTimeout w onHoverLeave i przy ponownym enter"
    - "No-op w difficulty==='egzamin' OR activeModal!==null (D-Phase5-09)"
    - "Content z pl.parts[meshId].description (verified existing 15 entries — NIE tworzymy pl.interactableDescriptions)"
    - "autoUpdate cleanup ZAWSZE wołany w _hide() i dispose() (Pitfall 2)"
    - "RaycastController DI rozszerzone o onHoverChange(meshId|null, mesh|null) callback (Pitfall 7)"
  artifacts:
    - path: "src/education/TooltipManager.js"
      provides: "Hover tooltip UI z @floating-ui/dom"
      min_lines: 100
    - path: "src/RaycastController.js"
      provides: "Rozszerzony DI o onHoverChange callback"
      contains: "onHoverChange"
  key_links:
    - from: "src/education/TooltipManager.js"
      to: "@floating-ui/dom"
      via: "computePosition + autoUpdate + flip + shift"
      pattern: "from ['\\\"]@floating-ui/dom"
    - from: "src/RaycastController.js _commitHover/_commitLeave"
      to: "TooltipManager.onHoverEnter/Leave (via callback)"
      via: "DI onHoverChange callback"
      pattern: "_onHoverChange"
---

<objective>
UI-03: TooltipManager wyświetlający `pl.parts[meshId].description` po 600ms hover delay, pozycjonowany przez @floating-ui/dom z auto-update na scroll/resize. RaycastController rozszerzony o DI callback `onHoverChange` (Pitfall 7 — najczystszy event source dla tooltipów).

Purpose: Spełnia UI-03 SC1 (600ms tooltip + autoUpdate + flip/shift never overflow). Razem z Plan 05-05 (LabelOverlay) i Plan 05-06 (rationale w StepPanel) dopina warstwę informacyjną Phase 5.
Output: Nowa klasa TooltipManager + brownfield-edit RaycastController + 2 zaktualizowane testy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-UI-SPEC.md
@src/RaycastController.js
@src/i18n/pl.js
@src/DisclaimerBanner.js
@src/highlight/EmissiveController.js

<interfaces>
@floating-ui/dom API (Plan 05-01 install):
- computePosition(refEl, floatingEl, {placement, middleware}) → Promise<{x, y}>
- autoUpdate(refEl, floatingEl, fn) → cleanup function
- flip(), shift({padding}) — middleware factories

RaycastController obecny kontrakt (Plan 04-05):
- constructor({renderer, camera, interactables, store, emissive})
- _commitHover(mesh) → emissive.setLayer('hover', mesh, ...); cursor='pointer'
- _commitLeave() → emissive.clearLayer('hover', ...); cursor='default'

Po Plan 05-04 dodaje:
- constructor({...prev, onHoverChange?: (meshId|null, mesh|null) => void})
- _commitHover(mesh) wywołuje this._onHoverChange?.(mesh.userData.id, mesh)
- _commitLeave() wywołuje this._onHoverChange?.(null, null)

pl.parts[id].description: 15 entries verified w src/i18n/pl.js linie 105-166 (kolo-zamachowe..tabliczka-znamionowa) — TooltipManager czyta `pl.parts[meshId]?.description` lub `pl.parts[meshId]?.label` jako fallback (Test 5 weryfikuje).

Store flags (Plan 05-01):
- difficulty: 'nauka'|'egzamin'
- activeModal: null|'help'|'confirm-scenario-switch'

Virtual element pattern dla 3D scene (RESEARCH Open Question #1):
TooltipManager nie ma DOM elementu mesha — tworzy virtual reference z `getBoundingClientRect()` zwracającym pozycję ekranową obliczoną z Vector3.project(camera) ALBO używa canvas DOMElement + offsetowanego rect. PREFEROWANE: virtual element z mesh.position rzutowanym na ekran. Planner implementuje przeliczenie 3D→screen w `_show()` używając `camera`+`renderer.domElement.getBoundingClientRect()`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — testy TooltipManager + rozszerzenie testu RaycastController</name>
  <read_first>
    - .planning/phases/05-educational-layer/05-RESEARCH.md "Pattern 1: @floating-ui/dom" (linie 222-301) + "Pitfall 2" (linie 627-633) + "Pitfall 7" (linie 662-668)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "src/education/TooltipManager.js" (linie 48-99) + "src/RaycastController.js brownfield" (linie 531-568)
    - tests/RaycastController.test.js (cały — wzór DI testów + commitHover/Leave kontrakt)
    - tests/StatusPanel.test.js (wzór jsdom DOM + lifecycle)
    - src/i18n/pl.js linie 105-166 (verify pl.parts shape)
  </read_first>
  <behavior>
    **TooltipManager (tests/TooltipManager.test.js — nowy plik):**
    - Test 1 (DOM mount): `new TooltipManager({store, raycastController})` tworzy `<div class="tooltip" role="tooltip">` w document.body (initial display:none or .tooltip--hidden klasa).
    - Test 2 (no callback registered without raycastController.onHoverChange): planner verify że konstruktor nie wymaga RaycastController jako DI — TooltipManager rejestruje się przez raycastController.onHoverChange = (...) => ... LUB raycastController eksponuje setter/parametr ctor.
    - Test 3 (delay 600ms): onHoverEnter('kolo-zamachowe', mockMeshRef) — useFakeTimers; advanceTimersByTime(599) → tooltip NIE widoczny (display:none); advanceTimersByTime(1) → tooltip widoczny (textContent = pl.parts['kolo-zamachowe'].description).
    - Test 4 (cancel delay): onHoverEnter, advanceTimersByTime(300), onHoverLeave → clearTimeout wykryty (advanceTimersByTime(1000) → tooltip pozostaje hidden, brak show).
    - Test 5 (content from pl.parts): tooltip.textContent === pl.parts['kolo-zamachowe'].description (verbatim, ze 105-160 znaków zgodnie z UI-SPEC).
    - Test 6 (no-op w egzamin): store.setState({difficulty:'egzamin'}); onHoverEnter('kolo-zamachowe', ref); advanceTimersByTime(700) → tooltip hidden (no-op per D-Phase5-09).
    - Test 7 (no-op w modal): store.setState({activeModal:'help'}); onHoverEnter; advance 700 → tooltip hidden.
    - Test 8 (brak description graceful): onHoverEnter('nieznany-mesh', ref); advance 700 → tooltip hidden (silent no-op per UI-SPEC §411).
    - Test 9 (autoUpdate cleanup w hide): mock @floating-ui/dom autoUpdate jako `vi.fn(()=>vi.fn() /* cleanup */)`. onHoverEnter+600ms → autoUpdate wywołany 1×, zwrócony cleanup zapamiętany. onHoverLeave → cleanup wywołany 1× (Pitfall 2).
    - Test 10 (autoUpdate cleanup w dispose): podobnie — onHoverEnter+600ms (active); dispose() → cleanup wywołany.
    - Test 11 (computePosition): mock computePosition jako vi.fn(()=>Promise.resolve({x:10, y:20})); po show → tooltip.style.left==='10px', top==='20px'.
    - Test 12 (placement+middleware): computePosition wywołane z `{placement:'top', middleware: [flipResult, shiftResult]}` — verify args (mock flip/shift jako tag obj sentinels).
    - Test 13 (dispose): dispose() — clearTimeout (jeśli active), autoUpdate cleanup (jeśli active), tooltip element usunięty z DOM.
    - Test 14 (boundary smoke): readFileSync 'src/education/TooltipManager.js' — regex `/import .* from ['"](three|gsap|\.\.\/training\/|\.\.\/highlight\/)/` MUST NOT match. Tylko @floating-ui/dom + ../i18n/pl.js dozwolone.

    **RaycastController (rozszerzenie tests/RaycastController.test.js):**
    - Test 15 (DI onHoverChange callback in ctor): nowy describe `Phase 5 — onHoverChange DI (D-Phase5-Discretion + Pitfall 7)`. Test: `const cb = vi.fn(); const rc = new RaycastController({...standardDeps, onHoverChange: cb});` → ctor NIE rzuca; opcjonalna DI nie łamie istniejących testów (Test 16 verify backward compat).
    - Test 16 (backward compat): konstruktor bez `onHoverChange` (Plan 04-05 signature) nadal działa — `new RaycastController({renderer, camera, interactables, store, emissive})` (bez nowego parametru) NIE rzuca. Existing tests Plan 04-05 PASS.
    - Test 17 (_commitHover calls callback): `rc._commitHover(meshFixture)` → cb wywołane z `(meshFixture.userData.id, meshFixture)` 1×.
    - Test 18 (_commitLeave calls callback): po _commitHover, _commitLeave() → cb wywołane z `(null, null)`.
  </behavior>
  <action>
    RED dla TooltipManager:
    - Stwórz `tests/TooltipManager.test.js` z 14 testami w 5 describe (DOM mount, hover delay, no-op gating, autoUpdate lifecycle, dispose+boundary).
    - Setup: `vi.mock('@floating-ui/dom', () => ({ computePosition: vi.fn(()=>Promise.resolve({x:0,y:0})), autoUpdate: vi.fn(()=>vi.fn()), flip: vi.fn(()=>'flipMW'), shift: vi.fn(()=>'shiftMW') }))` — importowany przed `import { TooltipManager }`.
    - useFakeTimers w beforeEach; useRealTimers w afterEach.
    - Real store z `createTrainingStore()`; ref dla onHoverEnter to mock `{ getBoundingClientRect: () => ({x:100,y:100,width:50,height:50,top:100,left:100,right:150,bottom:150}) }` (virtual element pattern).

    RED dla RaycastController:
    - Dorzucić nowy describe do `tests/RaycastController.test.js` z 4 testami (15-18). Mock interactables i emissive zgodnie z istniejącym wzorcem testów.

    GREEN — implementacja w Task 2.

    Po napisaniu testów: oba pliki MUST FAIL (TooltipManager.js nie istnieje; RaycastController nie ma onHoverChange w ctor). Commit `test(05-04): RED tooltip + raycaster onHoverChange DI`.
  </action>
  <verify>
    <automated>npm test -- --run tests/TooltipManager.test.js tests/RaycastController.test.js 2>&amp;1 | grep -E "FAIL|Cannot find" &amp;&amp; echo "RED OK"</automated>
  </verify>
  <done>
    - tests/TooltipManager.test.js istnieje z 14 asercjami.
    - tests/RaycastController.test.js ma +4 asercje Phase 5 (15-18).
    - npm test pokazuje FAIL na obu plikach (RED).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — TooltipManager + RaycastController onHoverChange DI</name>
  <read_first>
    - tests/TooltipManager.test.js i tests/RaycastController.test.js (kontrakty z Task 1)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "src/education/TooltipManager.js" (linie 48-99) + "Brownfield: RaycastController" (linie 531-568)
    - src/DisclaimerBanner.js (wzór single DOM element + role aria + dispose)
    - src/RaycastController.js (obecny stan — verify _commitHover i _commitLeave linie 114-128)
  </read_first>
  <action>
    **GREEN TooltipManager** (`src/education/TooltipManager.js`):

    1. Header: Phase 5 UI-03; D-Phase5-08/09/12; boundary D-Phase5-26 (store + DOM + @floating-ui/dom + pl.js; NIE THREE/gsap/training/highlight).

    2. Imports: `import { computePosition, autoUpdate, flip, shift } from '@floating-ui/dom';` + `import { pl } from '../i18n/pl.js';`.

    3. Const: `HOVER_DELAY_MS = 600;`

    4. `class TooltipManager`:
       - `constructor({store, raycastController})`: pola `_store`, `_raycastController`, `_tooltip=null`, `_cleanupAutoUpdate=null`, `_hoverTimer=null`, `_currentMeshId=null`, `_currentRefEl=null`. Wołaj `_build()` i `_wireRaycastCallback()`.
       - `_build()`: createElement('div'), className='tooltip tooltip--hidden', `role='tooltip'`, `setAttribute('aria-live','polite')`. `document.body.appendChild(el)`. `this._tooltip = el`.
       - `_wireRaycastCallback()`: jeśli `_raycastController` ma setter/property dla onHoverChange — wpiąć: `this._raycastController._onHoverChange = (id, ref) => { if (id) this.onHoverEnter(id, ref); else this.onHoverLeave(); };`. (Plan 05-04 brownfield-edits RaycastController by przyjąć ten callback w ctor — alternatywnie po ctor; planner wybierze ctor DI by zachować Phase 4 D-Phase4-13 czystą sygnaturę.)
       - `onHoverEnter(meshId, referenceMesh)`: D-Phase5-09 gating — `const {difficulty, activeModal} = this._store.getState(); if (difficulty==='egzamin' || activeModal!==null) return;`. `clearTimeout(this._hoverTimer); this._hoverTimer = setTimeout(() => this._show(meshId, referenceMesh), HOVER_DELAY_MS);`.
       - `onHoverLeave()`: `clearTimeout(this._hoverTimer); this._hide();`.
       - `_show(meshId, referenceMesh)`:
         - `const desc = pl.parts[meshId]?.description; if (!desc) return;` (graceful no-op).
         - `this._tooltip.textContent = desc;` (textContent XSS-safe).
         - `this._tooltip.classList.remove('tooltip--hidden'); this._tooltip.classList.add('tooltip--visible'); this._tooltip.style.display = 'block';` (UI-SPEC §145-146).
         - Virtual element: `const virtualEl = { getBoundingClientRect: () => this._meshToScreenRect(referenceMesh) };` (jeśli referenceMesh to Object3D z position) — ALTERNATYWNIE jeśli referenceMesh to już DOM element z getBoundingClientRect, użyj wprost. Planner ocenia: RaycastController przekazuje THREE.Mesh — TooltipManager potrzebuje projekcji 3D→screen. PREFEROWANE: `_meshToScreenRect(mesh)` używa mesh.position.clone().project(camera) + canvas rect; CAMERA musi być wstrzyknięta jako kolejne DI (`{store, raycastController, camera, renderer}`). Planner dorzuca `camera` i `renderer` do DI ctor — Application Plan 05-07 wstrzyknie z `sceneSetup.camera` i `sceneSetup.renderer`. PROSTSZE: użyj `renderer.domElement` jako virtual reference (cały canvas; tooltip pozycjonuje się względem canvas, nie konkretnego punktu 3D — pierwszy ship; precyzyjna projekcja może być Phase 7). Plan 05-04 wybiera tę uproszczoną strategię: virtual reference = renderer.domElement; placement: 'top'; flip+shift radzą sobie z edge cases. Akceptowalne dla UI-03 SC1 ("auto-update on scroll/resize, never overflow viewport") — overflow chronione przez flip+shift.
         - `this._cleanupAutoUpdate?.(); this._cleanupAutoUpdate = autoUpdate(refEl, this._tooltip, async () => { const {x, y} = await computePosition(refEl, this._tooltip, { placement: 'top', middleware: [flip(), shift({padding: 8})] }); Object.assign(this._tooltip.style, { left: x+'px', top: y+'px', position: 'absolute' }); });`
       - `_hide()`: `this._tooltip.classList.add('tooltip--hidden'); this._tooltip.classList.remove('tooltip--visible'); this._tooltip.style.display = 'none'; this._cleanupAutoUpdate?.(); this._cleanupAutoUpdate = null;`.
       - `dispose()`: `clearTimeout(this._hoverTimer); this._cleanupAutoUpdate?.(); this._tooltip?.remove();`. Idempotent.

    5. Boundary: tylko `@floating-ui/dom` + `../i18n/pl.js` w imports.

    **GREEN RaycastController brownfield** (`src/RaycastController.js`):

    1. Linia 30 (`constructor`): rozszerz destructuring `{renderer, camera, interactables, store, emissive, onHoverChange = null}`. Linia po `this._emissive = emissive;` dorzuć `this._onHoverChange = onHoverChange;` z komentarzem "Phase 5 D-Phase5-08 + Pitfall 7: callback (meshId|null, mesh|null) wołany w _commitHover/_commitLeave. TooltipManager wstrzykuje się przez ten kanał.".

    2. `_commitHover(mesh)` (linia 114): po `this._renderer.domElement.style.cursor = 'pointer';` dorzuć `this._onHoverChange?.(mesh.userData.id, mesh);`.

    3. `_commitLeave()` (linia 120): po `this._committedTarget = null;` (przed cursor='default') dorzuć `this._onHoverChange?.(null, null);` LUB po `style.cursor='default'` — kolejność nie ma znaczenia ale planner umieszcza po nullowaniu target by zachować inwariant "callback dostaje null GDY już wewnętrznie wyczyszczone".

    4. Brak zmian w `dispose()` — `_commitLeave()` jest wołane, więc callback dostaje (null,null) automatycznie.

    5. Brak nowych importów — ZACHOWUJE boundary `RaycastController` (tylko THREE; D-Phase4-13).

    Po implementacji: `npm test -- --run tests/TooltipManager.test.js tests/RaycastController.test.js` MUST PASS. Pełny suite zielony. Commit `feat(05-04): GREEN TooltipManager + RaycastController onHoverChange — UI-03`.
  </action>
  <verify>
    <automated>npm test -- --run tests/TooltipManager.test.js tests/RaycastController.test.js tests/boundaries.test.js</automated>
  </verify>
  <done>
    - 14 asercji TooltipManager + 4 nowe asercje RaycastController PASS.
    - boundaries.test.js zielony (RaycastController nie zyskał nowych zakazanych importów; TooltipManager wpisany w Plan 05-07 do boundaries).
    - Polish-literal scanner zielony (TooltipManager nie ma polskich literałów inline — `pl.parts.*.description` to import).
    - Pełny suite zielony.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RaycastController → TooltipManager | callback DI; tooltip nie zna THREE/sceny |
| pl.parts → DOM (textContent) | dane statyczne, XSS-safe |
| @floating-ui/dom → tooltip style | strony 3rd party (slopcheck OK) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-LEAK | Denial of Service | autoUpdate observer leak po hide/dispose | mitigate | Pitfall 2 mitigation: Test 9+10 weryfikuje `_cleanupAutoUpdate?.()` wywołane zarówno w `_hide()` jak i `dispose()`. ResizeObserver + IntersectionObserver z autoUpdate są wymagane do explicit cleanup. |
| T-05-04-XSS | Tampering | pl.parts[id].description → tooltip.textContent | accept | Wszystkie descriptions statyczne w pl.js (Phase 2 D-Phase2-08). textContent (Test 5) — XSS-safe by construction. |
| T-05-04-TIMER | Tampering | rapid hover enter/leave przy 600ms delay | mitigate | Test 4: clearTimeout w onHoverLeave. Test 3: clearTimeout przy ponownym enter (planner: pierwsza linia onHoverEnter to `clearTimeout(this._hoverTimer)`). |
| T-05-04-DOS | Denial of Service | computePosition async + autoUpdate przy resize spam | accept | @floating-ui/dom @1.7.6 debounces ResizeObserver wewnętrznie; brak ryzyka. Manual QA Plan 05-07 zweryfikuje brak FPS drop. |
</threat_model>

<verification>
- `npm test -- --run tests/TooltipManager.test.js` — 14 PASS.
- `npm test -- --run tests/RaycastController.test.js` — wszystkie (Phase 3+4+5 = baseline + 4 nowe) PASS.
- `grep -c "@floating-ui/dom" src/education/TooltipManager.js` === 1.
- `grep -c "innerHTML" src/education/TooltipManager.js` === 0 (zero — wszystko przez textContent).
- `grep -c "_onHoverChange" src/RaycastController.js` ≥ 3 (constructor + _commitHover + _commitLeave).
- Pełny suite zielony.
</verification>

<success_criteria>
- UI-03 SC1 w testach: 600ms delay + flip+shift middleware + autoUpdate cleanup.
- TooltipManager no-op w Egzaminie i modal (D-Phase5-09).
- RaycastController DI rozszerzone bez złamania backward compat (Test 16).
- Pitfall 2 (autoUpdate leak) i Pitfall 7 (hover callback source) mitygowane.
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-04-SUMMARY.md` — opisz: TooltipManager mount + lifecycle, 600ms delay impl (setTimeout pattern), no-op gating matrix, autoUpdate cleanup w 2 miejscach, virtual reference strategy (canvas as anchor — uproszczenie acceptable dla UI-03 SC1), RaycastController DI extension (onHoverChange optional + backward compat). Mapuj na D-Phase5-08/09/12.
</output>
