---
phase: 05-educational-layer
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/education/AudioController.js
  - tests/AudioController.test.js
autonomous: true
requirements: [EDU-03]
tags: [phase-5, audio, webaudio, boundary-clean]

must_haves:
  truths:
    - "AudioController syntetyzuje alarm (square 600Hz, 2× burst), confirm (sine 880Hz/200ms), hum (sawtooth, freq=80+1.2*RPM, threshold RPM<5)"
    - "Alarm idempotent na transition machineState→'awaria' (jeden burst per awaria, nie per setState gdy state niezmieniony)"
    - "Confirm odpalany na każdej zmianie step status na 'done'"
    - "Mute przez masterGain.gain.linearRampToValueAtTime(audioMuted?0:1, now+0.05) — globalny fade 50ms"
    - "AudioContext lazy (utworzony przy pierwszym triggerze) — user-gesture gating browserów (Pitfall 1)"
    - "Dispose: stop hum oscillator + close AudioContext + unsubscribe wszystko"
  artifacts:
    - path: "src/education/AudioController.js"
      provides: "WebAudio synthesis controller"
      min_lines: 120
    - path: "tests/AudioController.test.js"
      provides: "EDU-03 coverage z jsdom AudioContext mock"
      min_lines: 150
  key_links:
    - from: "src/education/AudioController.js"
      to: "store.subscribe(machineState/steps/audioMuted)"
      via: "Zustand subscribeWithSelector"
      pattern: "store\\.subscribe"
    - from: "tests/AudioController.test.js"
      to: "vi.stubGlobal('AudioContext', ...)"
      via: "Vitest global stub"
      pattern: "vi\\.stubGlobal.*AudioContext"
---

<objective>
EDU-03: WebAudio synthesis controller (alarm/confirm/hum) jako boundary-clean klasa subskrybująca store. Czysty OscillatorNode + GainNode, zero assetów, deterministyczne dla testów przez vi.stubGlobal.

Purpose: Spełnia 4 z 5 SC EDU-03 (alarm 600Hz×2, confirm 880Hz/200ms, hum proporcjonalny do RPM, mute `M` global + persist). Persist w localStorage wykonuje Application bootstrap w Plan 05-07 (analog hcOutlineMode Phase 4).
Output: src/education/AudioController.js + tests/AudioController.test.js z pełnym pokryciem 4 triggerów audio + lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-educational-layer/05-CONTEXT.md
@.planning/phases/05-educational-layer/05-RESEARCH.md
@.planning/phases/05-educational-layer/05-PATTERNS.md
@.planning/phases/05-educational-layer/05-VALIDATION.md
@src/highlight/EmissiveController.js
@src/highlight/HighlightManager.js
@src/state/trainingStore.js

<interfaces>
Z src/state/trainingStore.js (Plan 05-01 dorzuca audioMuted):
- state.machineState: 'oczekiwanie-na-inspekcje'|'gotowa-do-pracy'|'rozpedzanie'|'w-cyklu'|'zatrzymana'|'awaria'|'tryb-wolny'
- state.steps: Record<string, {status:'pending'|'active'|'done'|'error'}>
- state.audioMuted: boolean (Plan 05-01)
- store.subscribe(selector, listener) — zustand subscribeWithSelector (CHANGE-only)

WebAudio API kontrakty (RESEARCH §304-403):
- new AudioContext() — lazy, w _getOrCreateContext()
- ctx.createOscillator() → OscillatorNode {type, frequency: AudioParam, connect, start, stop}
- ctx.createGain() → GainNode {gain: AudioParam, connect}
- audioParam.setValueAtTime(value, atTime), .linearRampToValueAtTime(target, atTime)
- ctx.destination, ctx.currentTime (monotonic), ctx.close() → Promise<void>

Pattern subscriber + initial baseline (analog HighlightManager.js linie 29-63).
Pattern dispose (analog EmissiveController.dispose linie 132-142).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — testy AudioController z AudioContext mock</name>
  <read_first>
    - .planning/phases/05-educational-layer/05-RESEARCH.md sekcja "Pitfall 1: AudioContext w jsdom jest undefined" (linie 606-625) + "WebAudio — mocking pattern dla Vitest/jsdom" (linie 727-767)
    - .planning/phases/05-educational-layer/05-PATTERNS.md sekcja "src/education/AudioController.js" (linie 103-188)
    - tests/EmissiveController.test.js (cały — wzór describe controller + GSAP timeline mock; pattern przeniesiony do AudioContext mock)
    - tests/HighlightManager.test.js (cały — wzór subscriber + initial baseline test)
    - src/state/trainingStore.js (po Plan 05-01 — verify nowe pola)
  </read_first>
  <behavior>
    - Test 1 (constructor): konstruktor `new AudioController({store})` NIE tworzy AudioContext (lazy — Pitfall 1). `global.AudioContext` mock NIE jest wywołany.
    - Test 2 (subscribery): konstruktor rejestruje 3 subskryberów (machineState, steps, audioMuted) — verified przez spy na `store.subscribe` (3 wywołania).
    - Test 3 (alarm trigger): store.setState({machineState:'awaria'}) → AudioController woła `_getOrCreateContext()` → mockCtx.createOscillator wywołany 2× (2-burst). Każdy osc: type='square', frequency.setValueAtTime(600, anyTime), gain envelope (setValueAtTime(0,...) + linearRampToValueAtTime(0.4, ~+0.05) + linearRampToValueAtTime(0, ~+0.3)), osc.start(timeOffset), osc.stop(timeOffset+0.3+ε). Drugi burst offset ≈ 0.4s (0.3 duration + 0.1 gap).
    - Test 4 (alarm idempotent): dwa kolejne setState({machineState:'awaria'}) z tym samym wartością NIE odpala drugiego alarmu (subscriber CHANGE-only). setState({machineState:'gotowa'}) → setState({machineState:'awaria'}) ODPALA drugi alarm (transition '*→awaria' wykryta przez _lastMachineState porównanie).
    - Test 5 (confirm trigger): zmiana state.steps['krok-1'].status z 'pending' na 'done' → mockCtx.createOscillator wywołany 1× z type='sine', frequency.setValueAtTime(880,...), gain 0→0.25→0 w ~200ms. Zmiana wielu kroków jednocześnie odpala N confirmów (każdy nowy 'done' = jeden confirm).
    - Test 6 (confirm nie dla error): zmiana statusu na 'error' NIE odpala confirm.
    - Test 7 (hum updateHum): controller eksponuje `updateHum(rpmEffective)`. Wywołanie z rpm<5 → humGain.gain.linearRampToValueAtTime(0, currentTime+0.05). rpm=50 → humOsc.frequency.linearRampToValueAtTime(80+1.2*50=140, +0.05) i humGain.gain.linearRampToValueAtTime(clamp(0.05+0.005*50, 0, 0.3)=0.3, +0.05). updateHum BEZ utworzonego ctx (lazy guard) → no-op (testy nie zawodzą).
    - Test 8 (mute apply): store.setState({audioMuted:true}) → masterGain.gain.linearRampToValueAtTime(0, +0.05). audioMuted:false → ramp do 1.
    - Test 9 (dispose): dispose() — humOsc.stop() wywołany (try/catch), ctx.close() wywołany, 3 unsubscribery wywołane. Ponowne wywołanie dispose nie rzuca (idempotent).
    - Test 10 (boundary smoke): src/education/AudioController.js NIE importuje 'three', 'gsap', '@floating-ui/dom' ani DOM globals — verify przez readFileSync regex (analog tests/EmissiveController.test.js CRIT-5 regex line check).
  </behavior>
  <action>
    Stworzyć `tests/AudioController.test.js` jako TEST-FIRST. Header pliku zawiera mock AudioContext zgodnie z RESEARCH §727-767:
    - `createMockGain()` zwraca `{gain: {setValueAtTime: vi.fn(), linearRampToValueAtTime: vi.fn(), setTargetAtTime: vi.fn(), value: 1}, connect: vi.fn()}`.
    - `createMockOsc()` zwraca `{type: 'sine', frequency: {setValueAtTime: vi.fn(), linearRampToValueAtTime: vi.fn()}, connect: vi.fn(), start: vi.fn(), stop: vi.fn()}`.
    - `mockCtx` z `currentTime` (getter zwracający mutable `mockCtxCurrentTime`), `createGain`/`createOscillator` factory, `destination: {connect: vi.fn()}`, `close: vi.fn(()=>Promise.resolve())`, `resume: vi.fn(()=>Promise.resolve())`, `state: 'running'`.
    - `vi.stubGlobal('AudioContext', vi.fn(()=>mockCtx))` w beforeEach; `vi.unstubAllGlobals()` w afterEach.

    Real store z `createTrainingStore()` (Plan 05-01 baseline). Po `store.startScenario(uruchomienieFixture)` (import fixture z tests/fixtures/scenario.fixture.js lub uruchomienie scenario directly).

    Asercje 1-10 jak w `<behavior>`. Wszystkie spies na `store.subscribe` przez `vi.spyOn(store, 'subscribe')` zanim `new AudioController({store})` (must be PRZED ctor by capture rejestracji).

    UWAGA timing: nie używać prawdziwego setTimeout — `mockCtxCurrentTime` ustawiane ręcznie; testy weryfikują arguments setValueAtTime/linearRampToValueAtTime przez `expect(mockGain.gain.linearRampToValueAtTime).toHaveBeenCalledWith(0.4, expect.closeTo(0.05, 2))` (closeTo na floating point).

    Test 10 boundary smoke: `readFileSync('src/education/AudioController.js', 'utf-8')` + regex `/import .* from ['"](three|gsap|@floating-ui)/` MUST NOT match.

    Po napisaniu testów: `npm test -- --run tests/AudioController.test.js` → MUST FAIL (plik AudioController.js jeszcze nie istnieje). Commit `test(05-02): RED audio controller — 10 asercji EDU-03`.
  </action>
  <verify>
    <automated>npm test -- --run tests/AudioController.test.js 2>&amp;1 | grep -E "(FAIL|Cannot find module|ENOENT)" &amp;&amp; echo "RED OK"</automated>
  </verify>
  <done>
    - tests/AudioController.test.js istnieje z 10 testami w 4 describe ('constructor + lazy', 'alarm trigger', 'confirm trigger', 'hum + mute', 'dispose', 'boundary smoke').
    - npm test pokazuje FAIL na tym pliku (RED).
    - Commit z message wskazującym RED.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implementacja src/education/AudioController.js</name>
  <read_first>
    - tests/AudioController.test.js (utworzony w Task 1 — kontrakt do spełnienia)
    - .planning/phases/05-educational-layer/05-RESEARCH.md "Pattern 2: AudioController — OscillatorNode synthesis" (linie 304-403)
    - .planning/phases/05-educational-layer/05-PATTERNS.md "src/education/AudioController.js" (linie 103-188)
    - src/highlight/EmissiveController.js (wzór boundary header + const stack + dispose)
    - src/highlight/HighlightManager.js (wzór subscriber + initial baseline + idempotent transition detection)
  </read_first>
  <action>
    Stworzyć `src/education/AudioController.js`. Struktura zgodnie z 05-PATTERNS.md §103-188:

    1. Header komentarz: opis (Phase 5 EDU-03), boundary contract z D-Phase5-26 (importuje TYLKO store przez DI; NIE THREE/gsap/DOM/@floating-ui), lazy AudioContext gating note (Pitfall 1).

    2. Const stack (module-level): ALARM_FREQ=600, ALARM_BURST_DURATION_S=0.3, ALARM_BURST_GAP_S=0.1, ALARM_PEAK_GAIN=0.4, CONFIRM_FREQ=880, CONFIRM_DURATION_S=0.2, CONFIRM_PEAK_GAIN=0.25, HUM_FREQ_BASE=80, HUM_FREQ_SLOPE=1.2, HUM_RPM_THRESHOLD=5, MUTE_RAMP_S=0.05, HUM_RAMP_S=0.05.

    3. Class `AudioController`:
       - `constructor({store})`: pola `_store`, `_ctx=null`, `_masterGain=null`, `_humOsc=null`, `_humGain=null`, `_lastMachineState = store.getState().machineState`, `_lastStepStatuses = snapshot mapy stepId→status z store.getState().steps`, `_unsubscribers=[]`. Wołaj `_wireSubscribers()` na końcu.
       - `_wireSubscribers()`: 3 subskrybery (push do _unsubscribers): (a) `s=>s.machineState` listener `(cur)=>{ if(this._lastMachineState!=='awaria' && cur==='awaria') this.playAlarm(); this._lastMachineState=cur; }` (UWAGA W3: subscribeWithSelector zapewnia CHANGE-only callback, więc `_lastMachineState` porównanie jest defense-in-depth na wypadek gdyby Zustand setState bez selektora wymusiło ponowne odpalenie subscribera z tą samą wartością; alternatywnie planner może usunąć `_lastMachineState` ufając subscribeWithSelector — jeśli usunięte, dodać komentarz "// subscribeWithSelector CHANGE-only — duplikat odpalenia niemożliwy" przy subscriberze. Preferred: zachować `_lastMachineState` jako belt-and-suspenders przy audio-cue idempotency, koszt jednego pola jest pomijalny.); (b) `s=>s.steps` listener `(steps)=>this._detectStepDoneTransition(steps)`; (c) `s=>s.audioMuted` listener `(m)=>this._applyMute(m)`.
       - `_detectStepDoneTransition(steps)`: iteruj po kluczach steps; jeśli `_lastStepStatuses[id]!=='done' && steps[id]?.status==='done'` → `playConfirm()`. Aktualizuj snapshot na koniec.
       - `_getOrCreateContext()`: jeśli `_ctx` istnieje → return. Else: utwórz `new AudioContext()`, masterGain (gain setValueAtTime na podstawie current audioMuted), masterGain.connect(destination); humOsc (sawtooth, freq=HUM_FREQ_BASE), humGain (0), humOsc.connect(humGain).connect(masterGain), humOsc.start(). Jeśli `_ctx.state==='suspended'` → wołać `_ctx.resume()` (RESEARCH Open Question #3).
       - `playAlarm()`: jak Pattern 2 §350-366 — 2 burst loop `[0, ALARM_BURST_DURATION_S+ALARM_BURST_GAP_S].forEach(offset => { ... })`.
       - `playConfirm()`: jak Pattern 2 §368-381 — 1 osc sine 880Hz, envelope 0→0.25 w 0.02s → 0 w 0.2s, stop +0.25.
       - `updateHum(rpmEffective)`: guard `if(!this._ctx || !this._humOsc) return;` (lazy — pozwala wołać przed pierwszym audio trigger). Then: rpm<HUM_RPM_THRESHOLD → gain ramp do 0; else freq = HUM_FREQ_BASE + HUM_FREQ_SLOPE*rpm, gain = Math.min(0.05+0.005*rpm, 0.3), oba przez linearRampToValueAtTime(target, ctx.currentTime + HUM_RAMP_S).
       - `_applyMute(audioMuted)`: jeśli `_masterGain` istnieje → `_masterGain.gain.linearRampToValueAtTime(audioMuted?0:1, this._ctx.currentTime + MUTE_RAMP_S)`. (Bez ctx — no-op; resume nastąpi w _getOrCreateContext.)
       - `dispose()`: for u of _unsubscribers u(); _unsubscribers=[]; jeśli _humOsc: try{_humOsc.stop()}catch{}; jeśli _ctx: try{_ctx.close()}catch{}.

    4. Boundary: ZERO importów — czysta klasa z DI. JSDoc po polsku, identyfikatory ENG.

    Po implementacji: `npm test -- --run tests/AudioController.test.js` → MUST PASS (GREEN). Commit `feat(05-02): GREEN AudioController — WebAudio synthesis EDU-03`.
  </action>
  <verify>
    <automated>npm test -- --run tests/AudioController.test.js</automated>
  </verify>
  <done>
    - 10/10 testów AudioController.test.js PASS.
    - Pełny suite `npm test` zielony (267 baseline + Plan 05-01 ~18 + 10 nowych Audio = ~295).
    - Plik `src/education/AudioController.js` istnieje, zero importów (verify przez grep -c "^import" src/education/AudioController.js → 0).
    - Boundary entry dla AudioController.js NIE jest jeszcze w boundaries.test.js (Plan 05-07 dorzuca; do tego czasu `if(!existsSync) return` pomija — ale plik istnieje, więc trzeba sprawdzić że NIE narusza nic — test boundary nie failuje bo entry brak. Plan 05-07 dorzuca entry).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → AudioController | subskrybery odczytują state; NIGDY nie piszą (jednokierunkowy data flow) |
| AudioController → WebAudio API | browser-native API; brak external network/disk I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-DOS | Denial of Service | OscillatorNode leak (każdy alarm/confirm tworzy nowy osc) | mitigate | Każdy oneshot osc ma `start(time)` + `stop(time+duration)` — WebAudio sam zwalnia. Hum osc jest singleton (long-lived). Test 9 (dispose) weryfikuje cleanup; manual QA w Plan 05-07 sprawdza wielokrotne alarmy bez accumulation. |
| T-05-02-RACE | Tampering | `linearRampToValueAtTime` z atTime < currentTime (Pitfall 6) | mitigate | Każde wywołanie pobiera `ctx.currentTime` świeżo w momencie wywołania (nie cache'uje); RESEARCH §656-660 — pattern bezpieczny. |
| T-05-02-UG | Tampering | AudioContext bez user gesture | accept | Lazy creation w `_getOrCreateContext()` — context tworzony dopiero przy pierwszym audio triggerze. Application bootstrap (Plan 05-07) może wpiąć `ctx.resume()` w disclaimer-dismiss listener jako dodatkowe zabezpieczenie. Browser może zalogować warning przed user-gesture; akceptujemy. |
</threat_model>

<verification>
- `npm test -- --run tests/AudioController.test.js` — 10/10 PASS.
- `grep -E "^import" src/education/AudioController.js` — zero matches (boundary clean).
- `grep -c "createOscillator\|linearRampToValueAtTime\|setValueAtTime" src/education/AudioController.js` ≥ 8 (WebAudio API usage).
- `grep -c "ALARM_FREQ.*600\|CONFIRM_FREQ.*880\|HUM_FREQ_BASE.*80" src/education/AudioController.js` ≥ 3 (locked frequencies D-Phase5-15/16/17).
</verification>

<success_criteria>
- EDU-03 SC4 spełnione w jednostkowej weryfikacji: alarm 600Hz×2 burst, confirm 880Hz/200ms, hum freq scaling formula 80+1.2*RPM z thresholdem RPM<5, mute ramp 50ms.
- Klasa boundary-clean: zero importów (egzekwowane gdy Plan 05-07 dorzuci entry do boundaries.test.js).
- Lazy AudioContext: konstruktor nie wymaga AudioContext mocku (Test 1).
- Dispose idempotent: drugie wywołanie nie rzuca.
</success_criteria>

<output>
Create `.planning/phases/05-educational-layer/05-02-SUMMARY.md` — opisz: const stack values (frequencies + envelopes), 3 subskryberzy + ich kontrakty, lazy AudioContext pattern, dispose order. Mapuj na D-Phase5-13..18.
</output>
