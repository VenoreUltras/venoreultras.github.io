---
phase: 19-egzamin-polaczony-wynik-feedback
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/i18n/pl.js
  - src/ui/SessionOverlay.js
  - tests/sessionOverlay.test.js
autonomous: true
requirements: [EXAM-05]
must_haves:
  truths:
    - "W trybie egzamin po ukończeniu quizu SessionOverlay pokazuje jeden łączny wynik procentowy"
    - "Łączny % = (poprawne_SOP + poprawne_BHP) / (maks_SOP + maks_BHP) × 100"
    - "Werdykt Zaliczony/Niezaliczony wg progu 80% na wyniku łącznym"
    - "Rozbicie widoczne osobno: 'SOP: X/Y kroków | BHP: A/B pytań'"
    - "Tryb nauka/swobodny: SessionOverlay bez zmian (sam wynik SOP, brak regresji)"
    - "Combined liczony WYŁĄCZNIE w widoku — NIGDY zapisany do Zustand (CRIT-V12-5)"
  artifacts:
    - path: "src/ui/SessionOverlay.js"
      provides: "Sekcja łącznego wyniku egzaminu (combined % + werdykt + rozbicie SOP/BHP)"
    - path: "src/i18n/pl.js"
      provides: "Stringi PL: 'Wynik egzaminu', 'Zaliczony'/'Niezaliczony', rozbicie SOP/BHP"
  key_links:
    - from: "src/ui/SessionOverlay.js::_render"
      to: "s.quiz (questions/answers) + metrics.missedSteps"
      via: "lokalne liczenie combined (bez setState)"
      pattern: "quiz\\.(questions|answers)|missedSteps"
---

<objective>
EXAM-05: W trybie egzamin SessionOverlay wyświetla jeden łączny wynik procentowy łączący
ocenę proceduralną (SOP, 3D) i quiz BHP, liczony proporcjonalnie do sumy maksimów, z werdyktem
Zaliczony/Niezaliczony (próg 80%) oraz osobnym rozbiciem „SOP: X/Y kroków | BHP: A/B pytań".
Combined liczony WYŁĄCZNIE w warstwie widoku — nigdy zapisywany do Zustand (CRIT-V12-5).
Tryb nauka/swobodny: bez zmian.

Purpose: Uczeń widzi jeden spójny wynik egzaminu obejmujący obie części, z zachowaną
izolacją scoring/quiz w store.
Output: Rozszerzony SessionOverlay (sekcja combined gated na egzamin + quiz.finishedAt),
stringi PL, testy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-egzamin-polaczony-wynik-feedback/19-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Kontrakty z istniejącego kodu — używaj bezpośrednio. -->

src/ui/SessionOverlay.js — DI: `{ store, scenarios, computeMetrics, rootElementId }`.
- `_build()` — buduje statyczny innerHTML; cache regionów; XSS-safe textContent.
- `_render()` — czyta `s = store.getState()`; `scenario = scenarios[s.session.scenarioId] ?? s.activeScenario`; `metrics = computeMetrics(s.events, scenario)`; pokazuje `metrics.score`/100; retry widoczny tylko gdy `s.difficulty==='nauka'`.
- `_wireSubscribers()` — subscribuje overlayOpen, scoring.score, session.finishedAt, difficulty. (DODAĆ quiz.finishedAt.)
- `_renderIfVisible()` — render tylko gdy widoczny.

BOUNDARY (tests/boundaries.test.js, linie 88-92): SessionOverlay NIE WOLNO importować z
`../state/`, `../training/`, three, gsap, highlight, replay, export, education, floating-ui.
→ poprawność BHP liczyć LOKALNYM helperem w SessionOverlay (NIE importować isCorrect ze store).
Store dostępny przez DI (`this._store`), nie przez import — odczyt `s.quiz` jest OK.

Store quiz slice (src/state/trainingStore.js): `quiz: { questions, currentIndex, answers, score(0-100), finishedAt }`.
- `questions[i]` kształt: mc/tf → `correctIdx`; sequence → `correctOrder`.
- `s.difficulty === 'egzamin'` oznacza tryb egzamin (alias mode; overlay już subskrybuje difficulty).

computeMetrics(events, scenario) → `{ missedSteps:[], score, ... }`.
SOP: total = scenario.steps.length, poprawne = total − missedSteps.length.

Test: vitest jsdom; createTrainingStore; makeOverlay helper; startQuiz(questions)/submitAnswer/finishQuiz w store.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sekcja łącznego wyniku egzaminu w SessionOverlay + stringi PL</name>
  <read_first>
    - src/ui/SessionOverlay.js (cały — _build, _wireSubscribers, _render)
    - src/i18n/pl.js (linie 342-356 — pl.overlay)
    - src/state/trainingStore.js (linie 280-310 — quiz slice: startQuiz/submitAnswer/finishQuiz; oraz isCorrect linie 644-657 jako wzorzec poprawności do ZREPLIKOWANIA lokalnie)
    - src/training/ScoringService.js (computeMetrics → missedSteps)
    - tests/boundaries.test.js (linie 88-92 — zakaz importu ../state/ w SessionOverlay)
  </read_first>
  <behavior>
    - difficulty==='egzamin' AND quiz.finishedAt!==null AND quiz.questions.length>0: render sekcji combined.
      - sopTotal = scenario.steps.length; sopCorrect = sopTotal − metrics.missedSteps.length.
      - bhpTotal = quiz.questions.length; bhpCorrect = liczba i: localIsCorrect(questions[i], answers[i]).
      - pct = Math.round((sopCorrect+bhpCorrect)/(sopTotal+bhpTotal)*100) (guard dzielenia przez 0 → sekcja ukryta).
      - examScoreValue = `${pct}%`; werdykt: pct>=80 → 'Zaliczony', inaczej 'Niezaliczony'.
      - rozbicie = `SOP: ${sopCorrect}/${sopTotal} kroków | BHP: ${bhpCorrect}/${bhpTotal} pytań`.
    - difficulty!=='egzamin' LUB quiz nieukończony: sekcja combined ukryta (hidden), reszta overlay bez zmian (sam wynik SOP) — brak regresji.
    - Combined NIGDY nie trafia do store: brak setState/store write w ścieżce liczenia.
  </behavior>
  <action>
    W src/i18n/pl.js w `pl.overlay` dodaj (PO POLSKU): `examScoreLabel: 'Wynik egzaminu'`, `verdictPassed: 'Zaliczony'`, `verdictFailed: 'Niezaliczony'`, `examScoreValue: (pct) => \`${pct}%\``, `examBreakdown: (sc, st, bc, bt) => \`SOP: ${sc}/${st} kroków | BHP: ${bc}/${bt} pytań\``.
    W SessionOverlay `_build`: dodaj do innerHTML (po `.session-overlay__score-block`, przed `.session-overlay__metrics`) blok `<div class="session-overlay__exam-result" hidden>` zawierający `<span class="session-overlay__exam-label">`, `<span class="session-overlay__exam-value" aria-live="polite">`, `<span class="session-overlay__exam-verdict">`, `<span class="session-overlay__exam-breakdown">`. Po innerHTML scache'uj te elementy (this._examResultEl, this._examValueEl, this._examVerdictEl, this._examBreakdownEl) i ustaw statyczny `this._examLabelEl.textContent = pl.overlay.examScoreLabel`.
    Dodaj prywatny pure helper `_quizCorrectAt(q, answer)` replikujący logikę store.isCorrect (mc/tf: `answer===q.correctIdx`; sequence: `JSON.stringify(answer)===JSON.stringify(q.correctOrder)`). NIE importować ze store (boundary). JSDoc po polsku: „lokalna replika — widok, NIE zapis do store (CRIT-V12-5)".
    W `_render` po policzeniu `metrics`: oblicz combined gdy `s.difficulty==='egzamin' && s.quiz.finishedAt!==null && s.quiz.questions.length>0`. sopTotal=`(scenario?.steps?.length)??0`; sopCorrect=`sopTotal - metrics.missedSteps.length`; bhpTotal=`s.quiz.questions.length`; bhpCorrect = redukcja po answers z `_quizCorrectAt`; jeśli `(sopTotal+bhpTotal)>0` ustaw teksty + `this._examResultEl.hidden=false`, dodaj klasę werdyktu (np. `session-overlay__exam-verdict--pass`/`--fail`, Claude's Discretion). W przeciwnym razie / poza egzaminem: `this._examResultEl.hidden=true`.
    W `_wireSubscribers`: dodaj `this._store.subscribe((s)=>s.quiz.finishedAt, ()=>this._renderIfVisible())`.
    NIGDZIE nie wołaj setState ani nie zapisuj combined do store.
    Polski w komentarzach/JSDoc, identyfikatory angielskie.
  </action>
  <acceptance_criteria>
    - `grep -nE "examScoreLabel|verdictPassed|verdictFailed|examBreakdown" src/i18n/pl.js` zwraca nowe klucze.
    - `grep -nE "session-overlay__exam-(result|value|verdict|breakdown)" src/ui/SessionOverlay.js` pokazuje blok + cache.
    - `grep -nE "quiz\.finishedAt" src/ui/SessionOverlay.js` pokazuje nową subskrypcję.
    - `grep -nvE '^\s*//|^\s*\*' src/ui/SessionOverlay.js | grep -qE "from '(\.\./state/|\.\./training/)" && echo BAD || echo OK` → OK (brak zakazanych importów).
    - `grep -nE "setState|store\.getState\(\)\.\w+\(" src/ui/SessionOverlay.js | grep -iE "combined|exam" ; echo "(brak zapisu combined do store)"` — combined nie zapisywany.
    - `npm test -- tests/boundaries.test.js` zielony.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/boundaries.test.js tests/sessionOverlay.test.js</automated>
  </verify>
  <done>SessionOverlay liczy i renderuje combined % + werdykt + rozbicie tylko w egzaminie po quizie; helper poprawności lokalny (bez importu store); subskrypcja quiz.finishedAt; combined nigdy w store; stringi PL dodane.</done>
</task>

<task type="auto">
  <name>Task 2: Testy łącznego wyniku (egzamin) + brak regresji (nauka)</name>
  <read_first>
    - tests/sessionOverlay.test.js (cały — setup makeOverlay, scenarios, computeMetricsOverride, wzorce asercji)
    - src/ui/SessionOverlay.js (po Task 1 — klasy .session-overlay__exam-*)
    - src/state/trainingStore.js (startQuiz/submitAnswer/finishQuiz/setDifficulty)
  </read_first>
  <action>
    W tests/sessionOverlay.test.js dodaj describe „EXAM-05 łączny wynik":
    Setup: store=createTrainingStore(); setDifficulty('egzamin'); scenario z ≥1 krokiem; computeMetricsOverride zwracający kontrolowane `missedSteps` (np. scenario steps length=15, missedSteps o length 3 → sopCorrect=12). startQuiz(fixture pytań mc/tf z znanymi correctIdx), submitAnswer dla każdego pytania (część poprawnych), finishQuiz(); ustaw overlayOpen (np. store.setState({overlayOpen:true}) lub przez session.finishedAt flow).
    Testy:
    (a) `.session-overlay__exam-result` nie jest hidden; `.session-overlay__exam-value` textContent zawiera oczekiwany `${pct}%` policzony ręcznie z (sopCorrect+bhpCorrect)/(sopTotal+bhpTotal).
    (b) werdykt = pl.overlay.verdictPassed gdy pct>=80, verdictFailed gdy <80 (dwa przypadki: dobierz dane na granicy 80%).
    (c) `.session-overlay__exam-breakdown` textContent === pl.overlay.examBreakdown(sopCorrect,sopTotal,bhpCorrect,bhpTotal).
    (d) Regresja nauka: setDifficulty('nauka'), bez quizu → `.session-overlay__exam-result` jest hidden; wynik SOP renderowany jak dotąd.
    (e) Izolacja: po renderze `store.getState().scoring` niezmieniony względem snapshotu sprzed renderu; `store.getState().quiz.score` nie zawiera wartości combined (brak nowego pola combined w store).
  </action>
  <acceptance_criteria>
    - `npm test -- tests/sessionOverlay.test.js` zielony z nowym describe „EXAM-05".
    - Test (b) pokrywa granicę 80% (pass i fail).
    - Test (d) potwierdza brak sekcji combined w trybie nauka.
    - Test (e) potwierdza brak zapisu combined do store (scoring/quiz nietknięte).
    - `npm test` (cały suite) bez regresji.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/sessionOverlay.test.js && npm test</automated>
  </verify>
  <done>Testy weryfikują combined %, werdykt na granicy 80, rozbicie, brak sekcji w nauce, brak zapisu do store; pełny suite zielony.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store (quiz+scoring) → widok | SessionOverlay czyta dwa izolowane slice'y i łączy w widoku |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering (izolacja) | combined score | mitigate | Combined liczony tylko w `_render`; brak setState; scoring/quiz pozostają osobne (CRIT-V12-5) — egzekwowane testem (e) |
| T-19-05 | Tampering (boundary) | importy SessionOverlay | mitigate | Lokalny helper poprawności zamiast importu ze store; boundaries.test.js gate |
| T-19-06 | Tampering (XSS) | render combined/breakdown | mitigate | textContent dla wszystkich wartości; brak innerHTML z danych |
</threat_model>

<verification>
- `npm test` cały suite bez regresji (baseline 978) + nowe testy EXAM-05.
- `npm test -- tests/boundaries.test.js` zielony (SessionOverlay nie importuje ../state//../training/).
- Izolacja quiz/scoring: brak setState combined; grep `submitAnswer`/`finishQuiz` nie pisze do scoring (niezmienione w store).
- `npm run build` zielony.
- Stringi po polsku w pl.js.
</verification>

<success_criteria>
- W egzaminie po quizie: jeden łączny % + werdykt (próg 80) + rozbicie SOP/BHP.
- Tryb nauka/swobodny bez zmian (brak regresji).
- Combined nigdy w Zustand; SessionOverlay boundary zachowany; suite zielony.
</success_criteria>

<output>
Create `.planning/phases/19-egzamin-polaczony-wynik-feedback/19-02-SUMMARY.md` when done
</output>
